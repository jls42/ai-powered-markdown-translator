### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.12.0** المزوّد `--use_opencode`: ‏OpenCode، الوكيل مفتوح المصدر، نحو المزوّد الذي يختاره — نموذج محلي، أو مجاني بلا حساب، أو اشتراك، أو مفتاح (2026-09-04):

  - **المسار الثامن للمزوّد، وهو مختلف في طبيعته عن المسارات السبعة الأولى.** ‏[OpenCode](https://opencode.ai) ‏(MIT) ليس مزوّد نماذج، بل _موجّهًا_ إلى المزوّدين الذين أعدّهم المستخدم داخل OpenCode نفسه: مفتاح API، أو اشتراك (GitHub Copilot، وChatGPT، وSuperGrok)، أو بوابة OpenCode Zen — التي تتيح نماذج مجانية **بلا حساب** — أو نموذج **محلي** (Ollama، وLM Studio، وllama.cpp). يتحكم البرنامج النصي في `opencode run` بالوضع غير التفاعلي، كما يتحكم في Codex وGrok، ويعيد استخدام أساس العمليات الفرعية نفسه (مجموعة عمليات مستقلة، ثم `SIGTERM` و`SIGKILL` عند انتهاء المهلة، وstdin مغلق دائمًا، وبيئة منقّحة). جرى التحقق عبر **ترجمتين فعليتين**: ملف README هذا كاملًا إلى الإنجليزية باستخدام `opencode/mimo-v2.5-free` — في 49 ثانية، وتمريرة واحدة، وبنية مطابقة للملف المصدر (32 عنوانًا، و26 إغلاقًا لمقاطع الشيفرة، و18 رابطًا، و37 عنوان URL، و37 سطرًا جدوليًا، و135 شيفرة مضمنة) — وملف اختبار باستخدام `ollama/qwen2.5:7b`، محليًا ومن دون أي مفتاح.

  - **`--model provider/modèle` إلزامي، وهذا قرار مقصود.** من دون `--model`، يعود OpenCode إلى إعداداته الافتراضية التي تكون، في تثبيت جديد، `opencode/big-pickle`، وهو نموذج مجاني «خفي» قد تُستخدم تفاعلاته في التدريب — وقد أثبت القياس أن هذا هو النموذج الذي أجاب. كان اختيار ذلك بصمت بدلًا من المستخدم سيمثل بالضبط نوع التحويل غير المرئي الذي يتتبعه هذا المستودع؛ لذلك تسمّي رسالة الخطأ الأمر الذي يسرد النماذج (`opencode models`) وتعرض ثلاثة أمثلة (محلي، ومجاني، واشتراك). ليس لـ `--eco` أي تأثير، وهو يصرّح بذلك. يُمرَّر `--reasoning_effort` كما هو بوصفه `--variant` الخاص بـ OpenCode، وعند الطلب الصريح فقط.

  - **عزل مقاس، لا مفترض.** يعرّف إعداد مضمن (`OPENCODE_CONFIG_CONTENT`، وهو الأخير في ترتيب الدمج لدى OpenCode، ولذلك تكون له الأولوية على إعداد المستخدم من دون استبداله) وكيلًا باسم `aipmt` تُرفض لديه جميع الأدوات (`permission: {"*": "deny"}`): حتى إن السجل لا يعود يعرضها على النموذج، وعندما يُطلب منه «سرد الملفات وتشغيل `id`» يجيب بأنه لا يملك أدوات. جرى تعطيل مشاركة الجلسة، واستبعاد الإضافات الخارجية (`--pure`)، وعدم استخدام `--auto` مطلقًا، مع دليل عمل مؤقت وفارغ. قيس حقنان صامتان وأُوقفا: من دون `OPENCODE_DISABLE_CLAUDE_CODE`، يدخل `~/.claude/CLAUDE.md` الخاص بالمستخدم في **كل** مطالبة (515 رمز إدخال بدلًا من 186 لمجرد «مرحبًا»)؛ ومن دون `OPENCODE_DISABLE_PROJECT_CONFIG`، يدخل `AGENTS.md` الخاص بالدليل الحالي أيضًا — وقد طُبّقت على الترجمة تعليمة تقول «اختم كل إجابة بكلمة BANANA». أما `~/.config/opencode/AGENTS.md` العام فيظل محقونًا: لا يوجد مفتاح لتعطيله، كما أن التحايل عليه عبر `XDG_CONFIG_HOME` محوّر سيحجب مزوّدي المستخدم أيضًا. جرى توثيق ذلك بدلًا من ترقيعه.

  - **`exit 0` لا يثبت شيئًا، إنها CLI ثالثة، وبالاحتياطات نفسها — مع فخين خاصين بها.** لا يؤدي `--agent` غير المعروف إلى فشل `opencode run`: يظهر تحذير على stderr ثم يجري الرجوع **بصمت** إلى وكيل البرمجة مع أدوات نشطة. لذلك، إن لم يؤخذ الإعداد المضمن في الحسبان، فستنطلق الترجمة بواسطة وكيل قادر على الكتابة؛ ولهذا يتحقق عقد المخرجات من غياب هذه الرسالة، إضافة إلى: رمز إرجاع 0، وعدم وجود أي حدث `error`، وعدم وجود أي `tool_use`، وأن يكون آخر `step_finish` في `stop` (إذ إن `length` استجابة مبتورة)، وأن يكون النص غير فارغ. الفخ الثاني: حدث خطأ JSON **مبهم** — «خطأ غير متوقع في الخادم. راجع سجلات الخادم لمعرفة التفاصيل.»، مع مرجع بسيط — بينما لا يوجد السبب الحقيقي (`ProviderModelNotFoundError: Model not found: foo/bar. Did you mean…`، و`ProviderAuthError`، وما إلى ذلك) إلا في السجلات. ومن هنا يأتي `--print-logs --log-level ERROR` وقراءة الحقل `error="…"` من stderr، من دون تتبّع Bun الذي يليه. وهكذا يفشل نموذج غير معروف خلال ثانية واحدة مع تسمية سببه. كما يتجنب `--title` استدعاء LLM طفيليًا: فمن دونه، ينشئ OpenCode عنوانًا للجلسة عبر دورة إضافية على `small_model`.

  - **الأسرار: الترشيح بالنمط نفسه المستخدم مع Codex وGrok، مع استثناء واحد محدد بالاسم.** يُحتفظ بـ `OPENCODE_API_KEY`: فهو مفتاح OpenCode نفسه (بوابة Zen واشتراك Go)، وموجّه إليه باسمه — أي ما يعادل `auth.json` لديه، وليس مفتاحًا يديره aipmt أو يستطيع تحصيل رسوم عليه. يُعدّ المزوّدون داخل OpenCode (`opencode auth login`، و`opencode.json`)، ولا يُعدّون مطلقًا داخل `.env` الخاص بـ aipmt، ولا يصل أي مفتاح منه إلى العملية الفرعية. لا يوجد رفض في CI، خلافًا لأدوات CLI القائمة على الاشتراك: فمفتاح API أو نموذج مستضاف ذاتيًا على مشغّل استخدامان مشروعان.

  - **يتحقق حاجز منع اجتياز المسارات الآن من القيمة المستوفاة، لا من القيمة الخام.** يحتوي `provider/modèle` على `/` كان حاجز الإصدار 1.10.0 يرفضه — وعن حق، لأن `--model` يُستوفى داخل اسم الملف `--include_model`. تستبدل تسمية اسم الملف الآن `/` و`\` و`:` بـ `-` قبل أي استيفاء (`ollama/qwen2.5:7b` ← `ollama-qwen2.5-7b`، لأن `:` غير قانوني في Windows)، ويفحص الحاجز السابق هذه التسمية: يصبح `../../evil` مجرد الاسم `doc-en-..-..-evil.md` تحت الوجهة، بينما يظل `..` وحده مرفوضًا، وكذلك `--target_lang ../x`. ويبقى حاجز النطاق `_ensure_within_directory` طبقة ثانية من دون تغيير.

  - **النماذج المجانية والمحلية، وما جرى قياسه.** يترجم `opencode/mimo-v2.5-free` فقرة خلال 16 ثانية وملف README هذا خلال 49 ثانية؛ ويستغرق `opencode/big-pickle` نحو 40 ثانية لترجمة 200 كلمة، بينما ظل طلبان متزامنان عليه بلا استجابة لمدة 5 دقائق مع أن كلًا منهما ينجح منفردًا؛ ولم يُجب `opencode/nemotron-3.5-lightning-free` بشيء خلال 3 دقائق. ومن هنا جاء `REGEN_PROVIDER=opencode` مع إلزامية `REGEN_MODEL` وتشغيل **مهمتين** بالتوازي. محليًا، يضبط Ollama غالبًا سياقًا من 4,096 رمزًا، بينما يصل طول المقاطع إلى 16,000 محرف: لذلك يلزم `Modelfile` مع `PARAMETER num_ctx 32768`، وتتبع الجودة النموذج — فقد عكس نموذج 7B قائمة وأفسد إغلاق مقطع شيفرة في ملف الاختبار، بينما حافظ نموذج البوابة على كل شيء.

  - **لم تعد ترجمات هذا المستودع تمر مطلقًا عبر API مدفوعة.** كان `regen_translations.sh` يستخدم OpenAI API بمجرد وجود مفتاح عالق في `.env`، ولا يجعل Codex سوى خيار اشتراك صريح. وهذا بالضبط ما حدث أثناء إعداد هذا الإصدار: انطلقت الترجمات الثماني والعشرون عبر OpenAI API، ثم انطلق سجل التغييرات الهندي عبر Gemini API، مع أن اشتراك ChatGPT موجود تحديدًا لتجنب الدفع بحسب الاستخدام. أزيل الاكتشاف التلقائي للمفاتيح: **أصبح Codex هو الخيار الافتراضي، مع `gpt-5.6-sol`**، وهو نموذج الجودة؛ وتتطلب `openai` و`gemini` و`grok` وجود `REGEN_ALLOW_PAID_API=1` بالإضافة إلى `REGEN_PROVIDER`، وهو استثناء مسمّى كي تُطبّق القاعدة لحظة اتخاذ القرار؛ ويفشل `REGEN_PROVIDER` غير المعروف بدلًا من الرجوع إلى API. تثبّت عشرة اختبارات السلوك الافتراضي والرفض والاستثناء. وأُعيدت الترجمات الثماني والعشرون لهذا الإصدار عبر Codex.

  - **وُحّدت آلية التراجع عند تجاوز حد المعدل** (`_retry_on_rate_limit`): كانت حلقتا Codex وGrok متطابقتين باستثناء التسمية، وكانت نسخة ثالثة ستتجاوز عتبة التكرار. ترث أخطاء CLI الثلاثة من `_CliCallError` واحدة؛ ويحظر اختبار خروج أي منها عن هذا التسلسل، وإلا فلن تراها الحلقة المشتركة.

  - **الاختبارات**: ملف جديد `tests/test_opencode_provider.py` ‏(51 اختبارًا) — عقد المخرجات الكامل، والرجوع إلى الوكيل، وقراءة السبب من السجلات، وإزالة تكرار أجزاء النص وتجاهل الأجزاء الاصطناعية، وإنهاء مجموعة العمليات عند انتهاء المهلة، والتراجع عند 429، وإلزامية النموذج والتحقق منه، والفحص المسبق بلا أسرار، وحل مسار الملف التنفيذي، وتوصيل التوجيه، وتسمية اسم الملف، واختبار مضاد لاجتياز المسارات. يوسّع `tests/test_review_hardening.py` حصرية العلامات وغياب الأسرار لتشمل المزوّد الجديد. تتطلب البوابة الآن توثيق **22 علامة** argparse. بلغت الحزمة الكاملة **382 اختبارًا**.

- **1.11.1** تصحيح توثيقي: يعلن ملف README أخيرًا مسارات المزوّد السبعة (2026-09-03):

  - **كانت صفحة PyPI للإصدار 1.11.0 تقول «4 واجهات API + أداة Codex CLI».** تكشف الشيفرة سبعة مسارات — OpenAI وMistral وClaude وGemini وGrok عبر API؛ وCodex ‏(ChatGPT) وGrok عبر الاشتراك، من دون رسوم بحسب الاستخدام. كان وضعا Grok غائبين عن المقدمة وعن نقطة _تعدد المزوّدين_، وكررت الترجمات الأربع عشرة الخطأ. ولأن الوصف الطويل للحزمة ثابت لكل إصدار، فقد تطلب تصحيح الواجهة رقم إصدار جديدًا: وهذا هو سبب وجود هذا الإصدار الوحيد. **لا تغييرات في الشيفرة.**
  - جرت محاذاة `CLAUDE.md` مع ما أدخله النشر: عدّادات البوابة (16، و17 في `--full`)، ومسارات العمل النشطة الأحد عشر، وعدّادا Sonar/Codacy غير المرئيين في `gh pr checks` (النقاط الساخنة، وCodacy API)، ونقل `# nosemgrep` بواسطة `ruff-format`، وبيئات GitHub التي يتطلبها تبادل OIDC، وحقيقة أن _الناشر المعلّق_ لا يحجز الاسم.

- **1.11.0** النشر على PyPI: ‏`pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-09-03):

  - **أصبح البرنامج النصي أحادي الملف حزمة قابلة للتثبيت.** انتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول لسطر الأوامر `aipmt` وما يكافئها `python -m aipmt`. يظل المستودع المستنسخ ضروريًا للمساهمة — إذ توجد فيه الاختبارات والترجمات الثماني والعشرون وأدوات الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` مطلقًا**، لأن التعارض حقيقي وصامت. تثبّت حزمة PyPI المسماة `translate` ‏(v3.8.1، وآخر رفع في 2026-07-06) دليلًا يحمل الاسم نفسه. جرت إعادة إنتاج ذلك داخل venv: يتغلب الدليل على الوحدة، ويختفي `translate.main`، وتتعطل نقطة الدخول عند `AttributeError` — بينما يجيب `pip check` بعبارة «لم يُعثر على متطلبات معطلة» مع rc=0. كان مجرد وجود `pip install translate` لدى المستخدم كافيًا لتعطيل CLI من دون تشخيص قابل للاستفادة. وفي اختبار مضاد على ملف wheel الفعلي: وُضع `pip install translate` فوق الحزمة، وظل `aipmt --help` بقيمة rc=0 قبل ذلك وبعده، وتعايشت أداتا CLI.
    - **اسم توزيع طويل، وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للعثور عليها عبر بحث PyPI؛ إذ سيكون الاختصار وحده متعذر العثور عليه لمن لا يعرف المشروع مسبقًا، بينما يهدف النشر تحديدًا إلى تسهيل اكتشافه. استُبعد مرشحان معقولان بعد التحقق: `ai-markdown-translator` محجوز على npm منذ 2024 لأداة لها الغرض نفسه وتسبق هذا المستودع بـ17 شهرًا؛ و`aimt` لا يختلف إلا بحرف واحد عن `aim` ‏(v3.29.1)، وهي حزمة نشطة في المجال **نفسه** — أسوأ وضع يسبب التباسًا دائمًا. وهناك فخ منهجي أيضًا: يعيد `pypi.org/project/<nom>/` الرمز 200 لأي اسم (صفحة مكافحة الروبوتات)، ولا يُعتد إلا بـ JSON API.
    - **تخطيط `src/` بدلًا من حزمة مسطحة.** كان من شأن الحزمة المسطحة الحفاظ على عبارات `sys.path.insert(..., "..")` الست في الاختبارات، وهذه هي المشكلة تحديدًا: فهي تستورد شجرة المصدر بدلًا من الحزمة، ما سيخفي أي خطأ في التحزيم. التكلفة الفعلية: قاعدة استبدال إضافية واحدة.

  - **أصبح من الممكن أخيرًا إعداد المفاتيح مرة واحدة وإلى الأبد.** لم تكن CLI المثبتة تملك أي إعداد دائم: لم يتبق سوى متغير البيئة و`.env` في الدليل الحالي. صحيح أن `find_dotenv` يصعد حتى جذر النظام، ولذلك كان يجد `~/.env` **عند العمل داخل الدليل الشخصي**، لكنه لم يكن يجد شيئًا عند العمل في مكان آخر — أي تغطية تعتمد على مكان تشغيل الأمر لا على قرار تصميمي. لذلك أضيفت طبقة ثالثة: `~/.config/aipmt/.env`، أسفل الطبقتين الموجودتين.

    - **الأولوية غير مشفّرة**، بل تنتج من `override=False`، وهي القيمة الافتراضية لـ `load_dotenv`: لا تملأ كل طبقة إلا ما تركته السابقة فارغًا. ومن هنا يأتي الترتيب: متغير البيئة ← ‏`.env` الخاص بالمشروع ← إعداد المستخدم، وقد جرى التحقق منه باختبارات **سلوكية** لا بنيوية — فعكس ترتيب الاستدعاءين يؤدي إلى فشلها، وكذلك حذف الطبقة الثالثة.
    - **تنسيق `.env` وليس TOML**، وذلك عن قصد: ‏`python-dotenv` تبعية موجودة بالفعل، والصياغة موثقة بالفعل في ملفات README الخمسة عشر، والملف نفسه يخدم النطاقين. لا تبعية جديدة ولا صياغة جديدة. يتبع الموقع `XDG_CONFIG_HOME` إذا كان **مطلقًا** — إذ تطلب المواصفة تجاهل القيمة النسبية، وإلا عاد موقع الإعداد معتمدًا على الدليل الحالي — و`APPDATA` على Windows.
    - **استُبعد خياران مع بيان السبب.** مخزن مفاتيح النظام (`keyring`) أكثر أمانًا على محطة مكتبية، لكنه يفشل في البيئة عديمة الواجهة — خادم، أو حاوية، أو CI — وهي تحديدًا حالة استخدام الترجمة المجمعة؛ لذا فهو مرشح جيد كخيار صريح، لكنه خيار افتراضي سيئ. أما العلامة `--api-key` فستضع المفتاح في سجل الصدفة وتجعله ظاهرًا في `ps`.
    - **عند غياب المفتاح، لم يعد يظهر تتبّع للاستدعاءات.** كان المستخدم يتلقى مكدس Python يشير إلى `site-packages`، ورسالة تسمي «البيئة أو .env» من دون توضيح مكان إنشاء الثاني. أصبحت الرسالة الآن تسرد المواقع الثلاثة مع مسار كل منها بدقة، ويخرج الأمر بالرمز 2. وشبكة الأمان **ضيقة عن قصد**: ‏`except ValueError` حول مرحلة الإعداد وحدها. فتغليف التنفيذ كله سيحوّل خطأً حقيقيًا وقع أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الفشل الذي يتتبعه هذا المستودع. يقرأ اختبار مصدر `main()` لمنع ذلك.

  - **تصحيح — كان `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** لا يصعد `load_dotenv()` من دون وسيطة ابتداءً من الدليل الحالي، بل من الملف **المستدعي**، أي من `site-packages`. جرى القياس باستخدام نقطة دخول حقيقية لسطر الأوامر شُغّلت من مشروع يملك `.env` خاصًا به: يعيد `find_dotenv()` القيمة `''` ولا يُحمّل المفتاح، بينما يعثر عليه `find_dotenv(usecwd=True)`. لم يكن الخطأ موجودًا حين كانت الأداة لا تعمل إلا من المستودع المستنسخ؛ لكن النشر كان سيجعله منهجيًا، وعرضه الوحيد مفتاح API «مفقود» رغم صحة الإعداد.

  - **كان من الممكن أن تتحول ثلاث بوابات إلى اللون الأخضر بعد أن توقفت عن التحقق من أي شيء.** جرى تشديدها **قبل** النقل، عن قصد: فحاجز الأمان الذي يُكتب بعد التغيير المفترض أن يكتشفه لا يثبت شيئًا. كل منها أخضر في المستودع الأصلي ويتحول إلى الأحمر في نسخة منقولة — وقد جرى قياس الاتجاهين.
    - **يتجاهل Lizard المسار غير الموجود من دون قول أي شيء**: rc=0، «تم تحليل 0 ملف». كان حاجز التعقيد سيمر من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع مخرجات بحجم صفر بايت. أصبح النطاق الآن مصفوفة يُتحقق من وجود كل مُدخل فيها.
    - **لا يفشل `coverage run --source=` عند غياب وحدة**: تحذير على stderr فقط، وrc=0 لكل من unittest و`coverage xml`، مع نشر التقرير رغم ذلك — بعد تقليصه من 1453 إلى 141 تعليمة. كان المشروع سيبدو سليمًا لأنه لم يعد يُحلل إلا بالكاد. يحمي حدّان أدنيان التقرير: الإجمالي، وأكبر ملف جرى قياسه.
    - **مسبار حداثة الترجمات أعمى بنيويًا عن صيغة الاستدعاء**: فهو يرتكز على أعلام argparse، وهي تحديدًا ما لا يغيّره تغيير اسم الملف. أُعيد إنتاج الحالة: نُقلت الوحدة، وظلت ملفات README الخمسة عشر توثّق أمرًا غير موجود، وكانت النتيجة «لا توجد ترجمة قديمة». لذلك يتحقق قسم سابع من الصيغة لا من الخيارات، ويُقارن hook الخاص بـLizard بالنطاق الفعلي للبرنامج النصي — فمفتاحه `files:` لا يُفشل pre-commit عندما يتوقف عن المطابقة، بل يجعله يتخطاه.

  - **يتوقف `requires-python = ">=3.10"` عن كونه مجرد ادعاء.** كان `sonar-project.properties` يعلن بالفعل دعم 3.10-3.12 من دون أن يُختبر ذلك قط، إذ لم تكن محطة التطوير تحتوي إلا على 3.12 — وهو تناقض داخلي كان النشر سيجعله علنيًا. يشغّل workflow للاختبارات الآن الحزمة على 3.10 و3.11 و3.12، مع تثبيت الحزمة، وبالتالي التحقق من حدودها المعلنة.

  - **حدود دنيا، بلا سقف.** يظل `requirements.txt` ملف القفل المختبَر، ويصبح `[project.dependencies]` العقد المعلن: إذ إن نشر الإصدارات الدقيقة الواردة في ملف القفل سيتسبب في تعارضات لدى كل مستخدم لديه حزم أخرى. ولا يوجد كذلك سقف `<N+1` — لأنه سيتناقض مباشرةً مع `check-deps-fresh.sh`، الذي يُفشل حاجز الإصدار عند أي تأخر في إصدار رئيسي. تحل مجموعة الحدود الدنيا المشكلة، ويخرج الاختبار المضاد `openai==1.0.0` بالنتيجة `ResolutionImpossible`، ما يثبت أن الفحص يميّز بدلًا من قبول كل شيء. كما يمنع حارس إصدار `pyproject.toml` من الاختلاف عن إصدار CHANGELOG: لا يسمح PyPI بإعادة استخدام رقم إصدار.

  - **جرى التحقق من البداية إلى النهاية داخل venv جديد**: ملف wheel بحجم يقارب 70 كيلوبايت لا يحتوي إلا على `aipmt/*.py` وdist-info والرخصة؛ و`aipmt --help` بقيمة rc=0 مع 22 علمًا؛ و`python -m aipmt` يعرض «usage: aipmt» لا «usage: \_\_main\_\_.py»؛ وتثبيت `pipx` يعمل؛ والأهم **ترجمة فعلية من الفرنسية إلى الإنجليزية من أي دليل مستخدم**، مع الحفاظ على الخط العريض والقائمة والكود المضمّن والرابط وURL، وعدم ترجمة كتلة الكود. تنجح الاختبارات الـ318 السابقة للترحيل بقائمة معرّفات متطابقة بايتًا ببايت قبل الترحيل وبعده — وهذا ما يثبت عدم تعطيل أي اختبار، لا عبارة «OK»؛ وأُضيف إليها اثنا عشر اختبارًا للإعداد ذي الطبقات الثلاث، ليصبح المجموع 330.

- **1.10.0** المزوّد `--use_codex` (حصة اشتراك ChatGPT)، وتحديث SDK والنماذج، وإصلاح استشهادات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — وسيلتا حماية وضعتْهما PR من دون الالتزام بهما في كل موضع**:

    - **كان الفحص المسبق لـCodex يمرر `.env` كاملًا إلى الملف التنفيذي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: كانت العملية الفرعية ترث `os.environ` كاملًا، وبالتالي كامل `.env` الذي حمّله `load_dotenv`. أظهر القياس باستخدام ملف تنفيذي زائف مزوّد بأدوات رصد وصول **سبعة أسرار** إلى الفحص المسبق — مفاتيح المزوّدين الستة إضافةً إلى `GITHUB_TOKEN` — مقابل **صفر** لدى نظيره `_grok_preflight`، الذي كان يمرر `env=_grok_env()` بالفعل. كان التناقض داخليًا في PR: إذ يوجد `_strip_secret_env` تحديدًا للحفاظ على هذا الثابت، على بُعد بضعة أسطر فقط. جرى استخراج `_codex_env_base()` ومشاركته بين المسارين؛ والقياس بعد الإصلاح: صفر سر في كلا الجانبين.
    - **لم تشمل خاصية «`--deny` مغلق عند الفشل» الصيغة المستخدمة.** برّر التعليق احتواء Grok بالكامل بكون القاعدة ذات البادئة المجهولة تؤدي إلى رفض بدء التشغيل. أظهر القياس على `grok 1.0.13` أن هذا التحقق لا يوجد **إلا للصيغة الموضوعة بين قوسين**: يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل («بادئة أداة مجهولة»)، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. لكن `GROK_DENY_RULES` لم يكن يستخدم إلا أسماء مجردة — وبالتالي كان تغيير اسم أداة من جانب xAI سيزيل طبقة الاحتواء الوحيدة المقاسة من دون أي إشارة، على محطة لا ينطبق فيها أصلًا sandbox الخاص بنظام التشغيل. انتقلت القواعد الثماني المسماة إلى `Prefix(*)`، مع التحقق من كل واحدة بوصفها بادئة معروفة لدى CLI؛ ويظل catch-all المسمى `*` بصيغته الحرفية، وهي الوحيدة المقبولة. يمنع اختبار العودة إلى الصيغة غير المتحقق منها.
    - **وتبيّن أن بقية الجوانب سليمة**: لا يوجد حقن أوامر، إذ تُستخدم صيغة القائمة في كل موضع ولا يُستخدم `shell=True` مطلقًا، ويُمرر محتوى المستند عبر stdin أو `--prompt-file`؛ ولا توجد إزالة تسلسل غير آمنة، إذ يُستخدم `json.loads` فقط مع حارس للنوع؛ ولم يُعثر على أي تجاوز لإصلاح اجتياز المسار عبر سبع حمولات؛ كما يطبّق CLI فعلًا `--deny '*'`، إذ لوحظ `DENY_ENFORCED` عند القراءة من خارج workdir.
    - كان فحص الحداثة المضاف أعلاه يلتف، بالمناسبة، على مبدئه نفسه: إذ كان يتخطى بصمت أي حزمة يفشل طلبها إلى PyPI، ويبقى الحاجز أخضر. وهو يحصي الآن الحزم التي جرت مقارنتها فعليًا ويفشل إذا كانت التغطية ناقصة.

  - **أُعيدت التبعيات إلى المستوى المطلوب، مع شبكتي أمان لمنع تكرار التأخر**:

    - **كان التأخر حقيقيًا ومستمرًا**: `openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر عامين في مخزن الشهادات الجذرية الذي يتحقق من TLS لجميع استدعاءات المزوّدين. حُدد السبب: **لم يكن `.github/dependabot.yml` موجودًا**. من دون هذا الملف، لا يفعّل GitHub سوى _تحديثات الأمان_، ولا يقترح Dependabot طلب PR إلا لتبعية تستهدفها CVE — وهذا يفسر تحديثه `urllib3` و`idna` مع ترك SDK اثنين يتأخران بإصدار رئيسي كامل.
    - **يتعايش الإصداران الرئيسيان بلا تعارض**، خلافًا لما أوحى به تحليل سابق: ينتقل `openai` 3.x و`anthropic` 1.x إلى **`httpx2`**، بينما يظل `mistralai` و`google-genai` على `httpx<1`، لكنهما توزيعتان مختلفتان. جرى التحقق عبر تثبيت فعلي، ثم عبر **مسارات المزوّدين السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الكود المضمّن والروابط في كل مخرج. كان «تجنب حزمتين من مكونات HTTP» تفضيلًا لا عائقًا: وقد حسم القياس المسألة.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: كانت `google-auth` و`cryptography` ومكونات `opentelemetry` مثبتة في venv العمل من دون أن يُصرّح بها قط — ولذلك لم يكن التثبيت الجديد يعيد إنتاج ما يجري اختباره. وعلى العكس، كانت `tokenizers` و`huggingface-hub` و`PyYAML` مدرجة فيه من دون أن يستوردها أو يتطلبها أي شيء، وهي بقايا من `mistralai` 1.x. أُعيد إنشاء الملف بوصفه الإغلاق الكامل لـvenv مبني من التبعيات المباشرة وحدها. لا يُبلغ `pip-audit` عن أي ثغرة معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** (جديد) يفعّل تحديثات الإصدارات الأسبوعية لكل من pip وgithub-actions. جُمعت الإصدارات الثانوية والتصحيحية في PR واحد — إذ ينتهي الأمر بتجاهل PR مستقل لكل تحديث تصحيحي، والضوضاء عدو التحديث؛ أما **الإصدارات الرئيسية فمنفصلة**، ويتطلب كل منها تحققًا عبر استدعاء فعلي.
    - **`scripts/check-deps-fresh.sh`** (جديد وموصول بالحاجز) يجعل التأخر ظاهرًا في نتيجة المشروع: يقترح Dependabot التحديثات لكنه لا يضمنها، وقد تتراكم طلبات PR الخاصة به. التأخر في إصدار رئيسي ← فشل؛ وفي إصدار ثانوي ← تحذير، لأن الحاجز الأحمر باستمرار ينتهي به الأمر إلى أن يُتجاهل؛ وتعذر الوصول إلى PyPI ← تخطٍّ صريح محليًا و**إغلاق عند الفشل في CI**، لأن الفحص الذي لم يُنفذ لا يُعد نجاحًا. جرى التحقق في الاتجاهين: فهو يلتقط الحالة الدقيقة السابقة للإصلاح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير عند التأخر في إصدار ثانوي.

  - **إصلاحات ناتجة عن مراجعة PR هذه** — فحص خمسة وكلاء مراجعة الفروقات بدقة؛ وقد **أُعيد إنتاج جميع النقاط أدناه بالقياس** قبل تصحيحها، وكان اثنان منها تراجعين أُدخلا أعلاه في هذا الإصدار نفسه.
    - **إصلاح انحدار — كان `_NEWS_CITATION_REGEX` يعاني من تراجع أُسّي.** أدخل إصلاح الفقرات المتعددة `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: كان تقاسم المسافات بين `[ \t]+` و`.*` ملتبسًا، وكان هذا الالتباس يتضاعف من تكرار إلى آخر. قيس ذلك على أسطر `>   texte` — وهي إزاحة Markdown مشروعة تمامًا — لا تطابق النمط: **2,589 مللي ثانية لـ14 سطرًا** مقابل 0.04 مللي ثانية بعد التصحيح، مع عامل يقارب 9 لكل سطر مضاف. في وضع `--news`، كان اقتباس كتلي طويل وغير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب يمكن تحديده. يستهلك التكرار الآن السطر كاملًا دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يترك سوى طريقة واحدة للمطابقة في كل تكرار. جرى التحقق على المجموعة الفعلية المؤلفة من 231 مقالًا: **صفر اختلاف** في الالتقاط، ونفس الاقتباسات الـ423، مع استمرار توسيع المتون الـ14 متعددة الفقرات.
    - **كان استخدام علامتي مزوّد في الوقت نفسه يؤدي بصمت إلى فوترة حسب الاستخدام.** كان `--use_codex --use_mistral` مقبولًا؛ يختبر `_select_provider_client` مزوّد Mistral أولًا، بينما يعطي `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. وهكذا كان المستخدم يطلب استخدام حصة اشتراكه ويحصل بدلًا من ذلك على فوترة حسب الاستخدام، من دون أدنى تحذير: وهو تحديدًا نمط الإخفاق الذي وُجد `--use_codex` لمنعه. تمر علامات المزوّد الست الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: سطر أوامر يجمع بين مزوّدين، وكان يُقبل سابقًا بصمت، يفشل الآن عند `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة إنهاء العمل تتحول إلى اللون الأخضر عند تعطل مجسّاتها.** كانت أربعة من أصل ثلاثة عشر تحققًا في `scripts/check-release-ready.sh` تتبع نمط «التقاط stdout، والاستنتاج إذا كان فارغًا» من دون فحص رمز الخروج إطلاقًا: كان الاستثناء (ملف أُعيدت تسميته، `FileNotFoundError`) يكتب إلى stderr، ويترك stdout فارغًا، ثم يخلص التحقق إلى «لا شيء يستدعي الإبلاغ». وقع فخ «وجود `exit 0` لا يثبت شيئًا» داخل النص البرمجي المكتوب لمنعه. يفرض المساعد `probe()` الآن رمز خروج صفريًا **و**علامة نهاية، وترفض المجسّات الاستنتاج من مجموعة معالم فارغة — إذ إن التأكيد على مجموعة فارغة يكون صحيحًا دائمًا. دليل ذلك: أدت إضافة المجموعة الحصرية أعلاه إلى تمرير علامات المزوّد عبر كائن `*_group`، فلم يعد التعبير النمطي القديم `parser\.add_argument\(` يطابقها؛ وخرجت **ست علامات من أصل إحدى وعشرين** بصمت من النطاق، فيما ظلت البوابة خضراء.
    - **كان فحص الأسرار يفوّت أربعة مزوّدين من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: كان `sk-proj-…` (تنسيق OpenAI الحالي) و`sk-ant-api03-…` ينقطعان عند الشرطة الثانية، ولم يكن `AIza…` مشمولًا. وُسّع النمط، واستُبعد `.secrets.baseline` من الفحص. كذلك كان الحارس `.env` يستعلم بواسطة `git diff --cached`، الذي لا يرى سوى الفهرس: لذلك لم يكن `.env` **المُلتزم به سابقًا** — وهي أسوأ حالة — يظهر مطلقًا. وهو يستعلم الآن بواسطة `git ls-files`.
    - **لم يكن «الإحماء المسبق للرمز المميز» في Codex إحماءً فعلًا.** أظهر القياس أن `codex login status` لا يلمس `~/.codex/auth.json` (وقت التعديل والحجم بلا تغيير)، وتقول مساعدته «عرض حالة تسجيل الدخول». ومع ذلك كان التعليق يزعم تحديث الرمز المميز «مرة واحدة، تسلسليًا»، وبالتالي تحييد خطر التحديثات المتزامنة لرمز دوّار أحادي الاستخدام. لم تكن الحماية المعلنة موجودة؛ ويصف التعليق الآن ما تفعله الشيفرة فعلًا، فيما تظل المعالجة الحقيقية هي `max_jobs=4`. كما يحترم الفحص الآن `CODEX_BIN`، الذي كان يتجاهله — إذ كانت محطة عمل لا تحتوي على `codex` ضمن `PATH` تفشل برسالة «غير مصادَق عليه»، وهو تشخيص مضلل.
    - **كان `.env` يُحمّل في صدفة فرعية.** تُستدعى `detect_provider` ضمن استبدال أمر، ولذلك لا تنتقل عمليات التصدير منها إلى الأعلى: كان أي `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` معرّف في `.env` يظل غير مرئي للقراءات المنفذة في `main()`، التي كانت تخلص إلى «تعذر العثور على ملف Grok التنفيذي» رغم صحة الإعداد.
    - **كان التزامن يتجاوز الحد المعلن بنسبة 50%.** وُضع الحارس بعد تشغيل زوج README/CHANGELOG: بلغت الذروة المقاسة **3 عند `max_jobs=2`**. وبالنسبة إلى Grok، الذي تتشارك حصته الأسبوعية خدمات Chat وImagine وVoice ولا يمكن قياسها، لم يكن النص البرمجي يلتزم بالحد الذي فرضه على نفسه. أما العدد النهائي فكان يُعرض من دون مقارنته قط بالعدد 28 — لذلك كان غياب ملف يمر بلا ملاحظة.
    - **عقد خرج Grok: أصبح غياب `stopReason` إخفاقًا.** كانت الشيفرة تطبق «`end_turn` **أو غائب**» حيث يشترط العقد المعلن `end_turn`. كانت حمولة بلا هذا الحقل — أو أُعيدت تسمية حقلها إثر تحديث CLI — تحول الحارس إلى عملية عديمة الأثر بصمت. كذلك لم يعد `max_turn_requests` يُصنّف كتجاوز لحد المعدل (فالذي نُفد هو ميزانية الأدوار: إعادة المحاولة تعيد النتيجة نفسها مقابل انتظار 90 ثانية)، وأُخرج `quota` من علامات تجاوز حد المعدل — للسبب الذي سبق أن ذكره توثيق `_codex_is_rate_limited` من دون أن يطبقه Grok.
    - **أصبحت سلسلة Gemini تُحفظ مؤقتًا لكل نموذج.** كانت تبدأ مجددًا من `minimal` مع كل مقطع، مع أن النموذج الافتراضي يرفضه: كان المسار الاسمي يدفع تكلفة دورة 400 لكل مقطع ويعيد طباعة التحذير نفسه. عندما يتكرر التحذير مئات المرات يتوقف الناس عن قراءته — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مكتوبة صراحة لـCodex وتوجه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق أساس العمليات الفرعية يعمم «shim» على واجهتي CLI مع أن ملف Grok التنفيذي هو ELF أصلي (والتبرير الصحيح هو «وكيل ينشئ عملياته الفرعية الخاصة»)؛ ووُسمت اثنتا عشرة نتيجة SAST في `subprocess` بالعلامتين `# nosec` / `# nosemgrep` مع تبرير، إذ إن صيغة القائمة من دون `shell=True` تجعل الحقن مستحيلًا، كما أن محتوى المستند لا يمر أبدًا عبر argv.
    - **لم يعد أي سر يدخل العمليات الفرعية الوكيلة.** لم تكن قوائم المنع الاسمية تحمي سوى ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). أظهر القياس أن **سبعة أسرار أخرى** كانت تدخل رغم ذلك في كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح واجهة CLI الأخرى، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة البيانات. غير أن واجهتي CLI هاتين **وكيلتان**، ويعمل وكيل Grok من دون صندوق حماية لنظام التشغيل قابل للتطبيق على كثير من محطات Linux. تتم التصفية الآن **بحسب نمط الاسم** (`API_KEY`، و`_TOKEN`، و`SECRET`، و`PASSWORD`، و`CREDENTIALS`) لا بحسب قائمة اسمية، ما يشمل أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن تعرفها هذه الشيفرة. لا تحتاج واجهتا CLI إلى أي منها: فالمصادقة موجودة في `~/.codex` و`~/.grok`، وليس في البيئة أبدًا — وقد جرى التحقق عبر **ترجمة فعلية ناجحة** باستخدام كل واحد من المزوّدين ضمن البيئة المحصنة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يثبت حصرية علامات المزوّد، وعقد `stopReason`، وخطية التعبير النمطي للأخبار، ورسالة الرفض في CI، والحفظ المؤقت في Gemini، وغياب أي سر عن بيئة العمليات الفرعية. وهذا التأكيد الأخير **عام** — فهو يفشل عند وجود مفتاح لا تسميه أي قائمة — بينما كان اختبار التنقية القائم مرآة لثابته ولم يكن قادرًا على اكتشاف أي شيء سوى تعطل حلقته الخاصة. المجموعة الكاملة تضم **311 اختبارًا**.

  - **مزوّدا Grok جديدان**: `--use_grok` (واجهة xAI API، المفتاح `XAI_API_KEY`، بفوترة حسب الاستخدام) و`--use_grok_cli` (واجهة Grok Build CLI الرسمية، يُخصم استخدامها من اشتراك Grok — بالمبدأ نفسه الذي يتبعه `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن نقطة نهاية xAI متوافقة مع OpenAI، يُعاد استخدام العميل و`_call_openai` كما هما، ولا يتغير سوى `base_url`. لم يلزم سوى تعديل واحد يستفيد منه الجميع: يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي يصدرها xAI حيث يصدر OpenAI الصيغة `stop`. النماذج: `grok-4.6` (الجودة) و`grok-4.3` (الاقتصادي). تجدر الإشارة إلى أن خيار Grok الاقتصادي يظل الأغلى في المستودع — $1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ`mistral-small-latest`: يُختار هذا المزوّد لتنوع النماذج، لا للسعر.
    - **وضع CLI**: صُمم على غرار Codex، مع أربعة اختلافات فرضها الواقع — يُمرر الموجّه عبر ملف (`--prompt-file`، إذ لا تقرأ واجهة CLI من stdin وسيكون المقطع في argv مرئيًا ضمن `ps`)، ويكون الخرج كائن JSON واحدًا على stdout (لا JSONL ولا ملف `-o`)، ولا يتيح الاشتراك سوى `grok-4.6` و`grok-4.5`، ولا يمكن تطبيق صندوق الحماية (انظر أدناه). جرى توحيد تشغيل العمليات الفرعية مع Codex في `_codex_run_process`، من دون المساس ببقية مزوّد Codex الذي سبق اختباره.
    - **`exit 0` لا يثبت شيئًا، وفقًا للقياس**: عندما لا تكون المصادقة قائمة، تكتب واجهة CLI القيمة `{"type":"error","message":"Not signed in."}` إلى **stdout** مع رمز خروج **0**. ويتصرف الرفض أو تجاوز عدد الأدوار بالطريقة نفسها. لذلك يشترط عقد الخرج أربعة شروط متزامنة: رمز خروج 0، وغياب حمولة خطأ، و`stopReason == end_turn`، ونص غير فارغ. يتبع الفحص المسبق المنطق نفسه: يخرج `grok models` برمز 0 حتى عند قطع الاتصال، ولا يمكن الاستنتاج إلا من وجود «not authenticated» في stdout.
    - **الاحتواء: عدم تماثل مقصود وموثق.** بينما يعمل Codex ضمن `--sandbox read-only`، لا يمكن تطبيق صندوق حماية Grok على كثير من محطات Linux الحديثة، لسببين نظاميين مستقلين لا يمكن تجاوزهما من دون `sudo`: يحظر AppArmor مساحات أسماء المستخدمين غير المميزة منذ Ubuntu 24.04 (`bwrap: setting up uid map: Permission denied`، وقد أُعيد إنتاجه خارج Grok)، وتفشل قائمة منع مقابس وقت تشغيل الحاويات عندما يكون `/run/podman` في `0700` (لا يعالج المحلل سوى `ErrorKind::NotFound`، ويصبح EACCES قاتلًا). الفخ المحوري: الملف التعريفي **المدمج** الذي يتعذر تطبيقه **يبدأ بلا احتواء بصمت**. لذلك لا يطلب النص البرمجي أي ملف تعريفي افتراضيًا ولا يتراجع بصمت أبدًا — بل يحذر على stderr. تعتمد الحماية على قواعد `--deny` في CLI، بما فيها القاعدة الشاملة `*`، وهي الطبقة الوحيدة التي ثبت بالقياس أنها _تفشل في الوضع المغلق_ (إذ تؤدي قاعدة ذات بادئة مجهولة إلى رفض بدء التشغيل). يتيح `GROK_TRANSLATE_SANDBOX=read-only` اشتراط ذلك، وعندها يفشل بدء التشغيل إذا تعذر على الجهاز الوفاء به.
    - **الضوابط الوقائية**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (فوجود مفتاح سيحوّل الاستخدام إلى فوترة حسب الاستهلاك؛ كما أن `GROK_SANDBOX` موروثًا سيفرض ملفًا تعريفيًا غير قابل للتطبيق مع رسالة مضللة)، وتُعطل مفاتيح MCP/hooks/skills/agents، مع `--disable-web-search`، و`--no-subagents`، و`--no-plan`، ودليل عمل مؤقت، ورفض في CI، ومهلة تقتل مجموعة العمليات، وتراجع تدريجي عند تجاوز حد المعدل. ضُبط `--max-turns` على 6 لا على 1: إذ يُزاد العداد بعد دور الأدوات، وكانت القيمة 1 ستقتطع الخرج.
    - **الحصة**: حصة Grok أسبوعية و**مشتركة مع Chat وImagine وVoice**، ولا يعرضها أي أمر — خلافًا لـCodex الذي يتيح `account/rateLimits/read` قياس الاستهلاك. لذلك يحد `regen_translations.sh` التزامن عند 2 ويحذر بوضوح.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` (24 اختبارًا). المجموعة الكاملة تضم **290 اختبارًا**.
  - **إصلاح خطأ — لم تكن الاقتباسات الإنجليزية متعددة الفقرات محمية إلا جزئيًا (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل في متن الاقتباس سوى سلسلة من أسطر `>` **المتتالية**. وما إن يمتد اقتباس عبر فقرات متعددة (تفصل بينها سطر `>` فارغ) حتى لا يُلتقط ويُستبدل بالعنصر النائب سوى آخرها؛ أما الفقرات السابقة فكانت تُرسل إلى LLM وتعود مترجمة — وهو النقيض التام لما وُجد `--news` لضمانه. يقبل التكرار الآن أسطر `>` الداخلية الفارغة، وأصبح غير جشع ليتوقف عند سطر `>` الفارغ السابق للسطر المائل، لا عند أول سطر يصادفه.
    - **النطاق المقاس** على مجموعة فعلية من 198 مقالًا: تأثر 11 اقتباسًا من أصل 419. لا انحدار — يلتقط التعبير النمطي الجديد العدد نفسه تمامًا من الاقتباسات، ولا تتغير إلا المتون متعددة الفقرات (408 متون متطابقة، و11 موسعة)، ولا يزال من غير الممكن ابتلاع سطر الإسناد `> — …` داخل المتن (مع الإبقاء على الاستباق).
    - **إثبات من البداية إلى النهاية** على مقال بحجم 69 كيلوبايت تُرجم إلى اليابانية/العربية: كانت الفقرة الأولى من أحد الاقتباسات تُعرض سابقًا بصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم بالمثل إلى العربية، أما الآن فتبقى `> GLM-5.3 is now open-weight.`. ويعود عدد أسطر الاقتباس الإنجليزية من 9 إلى 10، وهو عددها في المصدر.
    - تجدر الإشارة إلى أن هذا العيب لم تكتشفه أدوات التحقق اللاحقة، فهي تتحقق من وجود الاقتباس من دون التأكد من اكتماله.
  - **توفير مقاس لدى المزوّد الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ النموذج بـ`gpt-5`، بما في ذلك وضع `--eco`. القياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: `medium` ← 45 رمز استدلال و65 رمز خرج؛ و`none` ← 0 و14. لا يضيف الاستدلال شيئًا إلى الترجمة، ومع ذلك كانت تكلفته تُدفع لكل مقطع من كل ملف. أصبحت القيمة الافتراضية `none` في `--eco`، وتظل `medium` في غير ذلك؛ وتبقى للقيمة الصريحة الممررة عبر CLI الأولوية. يقبل `--reasoning_effort` الآن `none` و`xhigh` إضافة إلى `low`/`medium`/`high` (ليست كلها مقبولة من جميع النماذج: فمثلًا يرفض `gpt-5.4-mini` القيمة `minimal` — وتغطي إعادة المحاولة الحالية من دون المعامل هذه الحالة).
  - **تحديث حزم SDK وترحيل Gemini**: استُبدل `google-generativeai` (انتهى دعمه في 2025-11-30 وأُرشف مستودعه) بحزمة SDK الموحدة **`google-genai`** — `genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير موجّه النظام عبر `system_instruction` بدلًا من دمجه مع المقطع. انتقل `mistralai` إلى **2.9.4** (وأصبح الاستيراد `from mistralai.client import Mistral`؛ أما القديم فيطلق `ImportError`، وفقًا للتحقق داخل wheel)، و`anthropic` إلى **0.125.0**، و`openai` إلى **2.54.0** — وهي آخر الإصدارات قبل الانتقال إلى `httpx2`، لتجنب تشغيل مجموعتي HTTP معًا في venv. وبناءً على ذلك، أُلغي تثبيت إصداري `httpx` 0.28.1 و`pydantic` 2.13.5.
  - **انحداران اكتشفتهما اختبارات فعلية، لا الوثائق**:
    - يرفض `anthropic` بالإصدار 1.0 أو أحدث، من جهة العميل، استدعاءً غير متدفق إذا كان `max_tokens` يوحي بأكثر من 10 دقائق (`ValueError: Streaming is required...`). لم يكن هذا الضابط موجودًا في 0.34.2، وكان يعطل كل استدعاء Claude مع `max_tokens=32768`. صُحح ذلك باستخدام `timeout` صريح (`CLAUDE_TIMEOUT`، و900 ثانية افتراضيًا)، ما يتجنب الانتقال إلى البث لاستدعاء لا تُستخدم منه سوى الاستجابة الكاملة.
    - لا يقبل `thinking_level="minimal"` سوى جزء من كتالوج Gemini: يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` بخطأ 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تسلسل `minimal` ← `low` ← من دون thinking_config، على غرار آلية OpenAI الاحتياطية الموجودة بالفعل — إذ ينبغي ألا يؤدي معامل تحسين مطلقًا إلى إفشال ترجمة.
  - **تجديد النماذج الافتراضية**، بعد التحقق من كل منها باستدعاء فعلي: OpenAI من `gpt-5.5` إلى **`gpt-5.6-terra`** (−60% على دفعة من 28) ومن `gpt-5.4-mini` إلى **`gpt-5.6-luna`** (−73%)؛ وClaude من `claude-sonnet-4-6` إلى **`claude-sonnet-5`** (أرخص وأحدث) ومن `claude-haiku-4-5-20251001` إلى **`claude-haiku-4-5`** (معرّف قياسي بلا تاريخ)؛ وGemini من `gemini-3.1-pro-preview` إلى **`gemini-3.7-flash`** ومن `gemini-3.1-flash-lite-preview` إلى **`gemini-3.1-flash-lite`** (إصدار مستقر وأرخص من `3.5-flash-lite`).
 Mistral من دون تغيير، مع بقاء `mistral-large-latest` أفضل نسبة جودة إلى السعر بين الأربعة. تجدر الإشارة إلى أنه لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — أما Gemini 3.5 Pro، الذي أُعلن عنه في مايو 2026، فلم يُطرح قط؛ وسلسلة 3.5/3.6/3.7 مخصّصة حصريًا لـ Flash.
  - **اختبار A/B مقاس قبل التحول إلى Gemini**: تُرجم `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. بنية متطابقة تمامًا (21 قائمة، و18 كتلة شيفرة، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، خلال **8 ثوانٍ مقابل 48 ثانية**. ونظرًا إلى عدم وجود أي benchmark عام يقارن هذين النموذجين في الترجمة أو النصوص المكتوبة بأبجديات غير لاتينية، لكان التحول قائمًا على مجرد افتراض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفّذ `block.text for block in response.content` من دون تصفية النوع. تُدرج النماذج ذات الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، تكشف `.thinking` لا `.text` — وكان من شأن الترجمة أن تتعطل بسبب `AttributeError` مبهم في المقطع الأول. تُستبعد الآن كتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` (قائمة سلبية للحفاظ على تقبّل نوع مجهول يحمل نصًا)، كما تؤدي الاستجابة التي لا تحتوي على أي كتلة نصية إلى خطأ صريح. ويُمرَّر `thinking={"type": "disabled"}` في كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: إزالة النماذج التي انقضى تاريخ سحبها (سلسلة `magistral-*` المسحوبة في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620`، و`claude-3-7-sonnet-20250219`، و`claude-opus-4-1-20250805`، و`claude-sonnet-4-20250514`). تصحيحات الحدود: Mistral من 128K إلى **256K** (جيل Large 3 / Small 4)، وGemini من 1 000 000 إلى **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` من 200K إلى **1M**، وعائلة `gpt-5.6-*` من 400K إلى **1,05M**. إضافة Claude 5 ‏(`claude-sonnet-5`، و`claude-opus-5`، و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وسلسلة `ministral-*`. تجدر الإشارة إلى أن هذه الحدود تظل إرشادية، إذ يقيّد `translate()` التقسيم عند `min(16000, limite)`.

  - **Provider ‏`--use_codex`**: Provider خامس يتحكم في CLI الرسمي لـ Codex ‏(`codex exec`) في الوضع غير التفاعلي، بدلًا من استدعاء API تُحاسَب بحسب الاستخدام. تُحتسب الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقًا. وهذا هو المسار الوحيد الذي توثّقه OpenAI لهذا الاستخدام: إذ تعرض مصفوفة التوفّر بحسب الخطة «Codex SDK، و`codex exec`، وسير العمل القابل للبرمجة» بوصفها متاحة في Plus/Pro/Business/Enterprise، بينما لا تصادق tokens الخاصة بـ `~/.codex/auth.json` على استدعاءات API Platform (ولا يقرأها هذا السكربت مطلقًا — إذ تظل المصادقة وتجديدها تحت إدارة CLI).
  - **إمكانية تثبيت ملف Codex التنفيذي عبر pip، لا عبر npm فقط**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK ‏`openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عام لاستخدام `--use_codex`. ولا تُضاف الحزمة إلى `requirements.txt`: إذ يبلغ حجم الملف التنفيذي نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين من أجل Provider اختياري. جرى التحقق من البداية إلى النهاية: عند غياب `codex` عن `PATH`، يعثر الحلّ على الملف التنفيذي المضمّن في الحزمة، وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: يُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. فمن دون هذا الحاجز، قد يتسبب مفتاح موجود في `.env` في تحويل Codex إلى المحاسبة بحسب الاستخدام من دون أي إشارة ظاهرة — وهو تحديدًا ما وُجد هذا Provider لتجنّبه.
  - **اختبارات تمنع مشكلات CLI**:
    - يقرأ `codex exec` من stdin **حتى** عند تمرير prompt كوسيط: ومن دون إغلاق stdin، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج مطلقًا (أُعيد إنتاجه: exit 124 بعد 180 ثانية، وصفر بايت). لذلك فإن `communicate(input=...)` إلزامي.
    - إن `codex` المثبّت عبر npm هو shim بلغة Node ينفّذ `spawn` للملف التنفيذي الحقيقي المكتوب بـ Rust: وهذا الأخير **عملية حفيدة** لعملية Python ويبقى حيًا بعد `SIGKILL` الخاص بـ `subprocess.run(timeout=)`، حيث سيواصل استهلاك الحصة. ومن هنا جاء `Popen(start_new_session=True)` + `os.killpg`.
    - قد ينتهي CLI بالرمز 0 رغم إصداره `turn.failed`: لذلك تُفحص مخرجات JSONL ‏(`--json`) إلى جانب رمز الإرجاع، ويؤدي غياب الملف `-o` مع رمز 0 إلى خطأ صريح بدلًا من إنتاج مقطع فارغ.
  - **Back-off عند بلوغ rate limit**: لا ينفّذ CLI أي retry داخلي (`max_retries = 0`). ويجري التصنيف استنادًا إلى بنية payload بصيغة JSON ‏(`status: 429` / `error.type`)، لا إلى سلاسل فرعية — إذ تظهر كلمة «الحصة» في خطأ 429 قابل للاسترداد كما تظهر في `insufficient_quota` نهائي.
  - **حاجز CI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفًا. فالمصادقة عبر الاشتراك غير مخصّصة لـ runner مشترك، كما تنصح OpenAI صراحة بعدم استخدام سير العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` ‏(`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وAPI Platform، لكن حساب ChatGPT لا يملك حق الوصول إلى جميع نماذجها: تُطبَّق allowlist من جهة الخادم من دون تحقق محلي، ويؤدي اختيار نموذج غير معتاد إلى تحذير. وفي خطة Plus، يوفّر Luna ما بين 250 و2 000 رسالة لكل نافذة مدتها 5 ساعات، مقابل 10 إلى 100 لـ Sol: لذا فإن `--eco` هو الوضع الموصى به لكل معالجة دفعية.
  - **إصلاح خلل — كان `regen_translations.sh` ينتهي بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` تابع لـ `main()` لم يعد موجودًا عند تنفيذ trap. وتحت `set -u`، كان ذلك يطلق `failed_log: unbound variable` ويُنهي السكربت بالرمز 1 رغم صحة الترجمات الـ28 — ما كان سيوقف `release.sh --auto` ‏(`set -e`) مباشرة بعد إعادة التوليد، عند أعلى المراحل تكلفة. أصبح المتغير عامًا، وصار trap يتحقق من وجوده. ومن الآثار الجانبية المفيدة أن إخفاقات الترجمة الحقيقية، التي كان هذا الخطأ يحجبها حتى الآن، باتت ظاهرة مجددًا في الملخص النهائي.
  - **`REGEN_MODEL`**: متغير بيئة جديد لـ `regen_translations.sh` يفرض نموذجًا محددًا بدلًا من النموذج الافتراضي للـ Provider، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام نموذج الفئة العليا ضمن حصة الاشتراك بدلًا من نموذج `--eco` الموجّه إلى أحجام العمل الكبيرة.
  - **`regen_translations.sh`**: يتوفر `REGEN_PROVIDER=codex` عبر اشتراك اختياري صريح (ولا يُكتشف تلقائيًا مطلقًا، لتجنّب استهلاك حصة الاشتراك من دون علم المستخدم). ويُجدَّد token مرة واحدة تسلسليًا قبل بدء التوازي — لأن تجديد Codex دوّار وأحادي الاستخدام، وستؤدي المهام المتزامنة إلى إبطال جلسة `codex login` — كما يُخفض التزامن إلى 4.
  - **إعادة الهيكلة المرتبطة**: ينتقل `_dispatch_provider_call` من 8 إلى 6 معاملات عبر `_resolve_provider()` يعيد اسم Provider، بدلًا من تمرير متغير منطقي رابع عبر السلسلة كلها. وتظل المتغيرات المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` بالحد الأدنى.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` ‏(48 اختبارًا) يغطي argv، والبيئة المنقّحة، وعقد منع التمهيد، والإخفاقات الصامتة، وtimeout/killpg، وback-off، وpreflight، وحلّ Provider، وتسلسل استدلال Gemini، وتصفية كتل Claude، واستشهادات الأخبار متعددة الفقرات. مجموعة كاملة من 290 اختبارًا.
  - **تحقق فعلي**: عند ترجمة `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex، تكون البنية مطابقة تمامًا للترجمات المرجعية (14 كتلة شيفرة، و24 عنوانًا، و25 سطر جدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل شيفرة متطابقة حرفًا بحرف، ومن دون أي بقايا placeholders). وفي مقال صحفي حجمه 69 كيلوبايت ضمن وضع `--news`، تجتاز مخرجات `gpt-5.6-luna` و`gpt-5.6-sol` كلتاهما مدقق التطبيق اللاحق للغات en/ja/ar. أما الاستهلاك المقاس عبر `account/rateLimits/read` فظل دون عتبة تقريب العدّاد (0% من نافذة الساعات الخمس) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL الخاص بإسناد الأخبار عند وجود أقواس متداخلة أو بادئة فرنسية (2026-05-11):

  - **إصلاح خلل**: كان استخراج عناوين URL الخاصة بالإسناد في `_protect_news_quotes` يستخدم regex ‏`re.search(r"\((.+?)\)", attribution)` ‏(التقاطًا غير جشع بين قوسين). وفي إسناد من نوع `(relayé par [@user sur X](https://x.com/.../123))` ‏(أقواس متداخلة: `(` محيطة + `]()` لرابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه، ما ينتج سلسلة مبتورة تتضمن البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` ‏(من دون `)` الختامي). والنتيجة أن `_validate_news_post` كان يبحث عن هذه السلسلة في المخرجات المترجمة ويفشل دائمًا (لسببين: `)` مبتور + ترجمة "relayé par" إلى `relayed by`/`weitergeleitet von`/...). ولم يكن بوسع التسلسل الكامل low → medium → high → gpt-5.5 النجاح.
  - **الإصلاح**: أصبح regex هو `re.search(r"\]\(([^)]+)\)", attribution)` — إذ يستهدف تحديدًا `](url)` الخاص برابط Markdown، ويلتقط **عنوان URL الصافي فقط** (من دون بادئة فرنسية أو بتر)، مع الحفاظ عليه ثابتًا بواسطة placeholders ‏`#URL{N}#` أثناء الترجمة. وهو متين أمام النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس محيطة
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py` ضمن الفئة `TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` ‏(الحالة الدقيقة التي أعادت إنتاج خلل Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` ‏(نسخة متغايرة تحتوي على `via`)
  - **فجوة في التغطية**: يتحقق `check-editorial-coverage.py` من صحة الصياغة التحريرية، لا من قابليتها للترجمة بواسطة translator. ومن التحسينات الممكنة (خارج نطاق v1.9.2) إضافة فحص يحاكي استخراج الإسناد في وضع dry-run لاكتشاف الأنماط المعرّضة للخطر **قبل** النشر.

- **1.9.1** إصلاح i18n لتسمية CTA في ملاحظة marker الخاصة بالترجمة (2026-05-10):

  - **إصلاح خلل**: ظلت تسمية `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط marker أعلى الملفات المترجمة **بالفرنسية** لجميع اللغات المستهدفة، بدلًا من اتباع `target_lang`. ولا يراها LLM مطلقًا (إذ تُجمَّع من جهة Python للحفاظ على عنوان URL وslug الخاص بالمستودع)، لذلك لم تكن مرحلة الترجمة قادرة على تداركها. وهذا تراجع صامت منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ15 بتسمياتها المحلية. وأصبح `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` يمرران اللغة المستهدفة. ويُستخدم fallback ‏`fr` إذا كانت اللغة مجهولة (للأمان، ومن دون KeyError).
  - **الاختبارات**: تعديل `test_source_emits_three_paragraphs_repo_title_description_link` ‏(target_lang ‏`ja` ← التسمية اليابانية المتوقعة). واختباران جديدان: `test_source_link_label_localized_per_target_lang` ‏(مُعامل على 7 لغات تشمل أبجديات لاتينية وتصويرية وأبجدية صامتة) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختبارًا في `test_translation_note_position.py` ‏(بدلًا من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع بقيمة افتراضية `target_lang="fr"` — تواصل الجهات البرمجية الخارجية المستدعية التي لا تمرر `args.target_lang` العمل من دون تعديل.
- **1.9** إصلاح الفشل الصامت + أدوات جودة متكاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + تنسيق علامة "embed card"**:
    - خيارات CLI جديدة (إضافية، الإعدادات الافتراضية دون تغيير ← **غير كاسرة للتوافق**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): يضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - يعيد `legacy` إنتاج سلوك v1.8 بدقة (فقرة عريضة `**…**`) **بايت مقابل بايت**.
        - يُصدر `marker` تعريف مرجع رابط Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`)، يليه **اقتباس من 3 فقرات** مُهيكل ليظهر على هيئة "GitHub repo embed card": عنوان المشروع ضمن كود مضمن (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف يترجمه LLM، ورابط CTA (`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. يمكن استخدامه في أثناء البناء بواسطة ملحق remark (راجع مدونة jls42.org ← الملحق `remark-translation-banner`).
    - **ثوابت لا تُرسل مطلقًا إلى LLM**: يُجمّع عنوان المستودع وعنوان URL الخاص بـ GitHub في جانب Python بعد ترجمة العبارة الوصفية. لا يرى LLM مطلقًا المعرّف `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، مما يضمن عدم تغيير أي عارض أو حالة أحرف أو مخطط.
    - **إدراج واعٍ بالـ frontmatter**: في وضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة `---` الختامية** الخاصة بـ frontmatter بصيغة YAML (لضمان أمان Astro Content Collections / gray-matter). تكتشف الدالة المساعدة `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف وتحافظ على سلامته؛ كما **تطلق `RuntimeError`** عند وجود frontmatter مفتوح من دون سياج إغلاق (فيظهر الملف ضمن `failed_files` بدلًا من كتابته مع ملاحظة موضوعة في مكان خاطئ).
    - **قائمة سماح لتنقية اسم النموذج**: تستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع الرجوع إلى `unknown` إذا كانت النتيجة فارغة. يتوافق ذلك مع أداة التحقق في ملحق remark الخاص بـ Astro ويعطّل المحارف التي قد تكسر تنسيق العلامة (المسافة، وعلامة الاقتباس، والقوس، والفاصلة، وما إلى ذلك).
    - **إعادة هيكلة داخلية**: `_append_translation_note` (دالة واحدة متجانسة) ← 7 دوال مساعدة نقية (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فُصل المنشئ عن المُركّب (يعيد المنشئ كتلة نقية بلا فاصل، ويطبّق المُركّب عناصر `\n\n` بحسب الموضع)؛ ويشترك كود الإنتاج والدالة المساعدة المصدرية في المُجمّع نفسه ذي الفقرات الثلاث.
    - **`_quote_lines` مع الحفاظ على الأسطر الفارغة**: يضيف `> ` في بداية كل سطر، مع تحويل الأسطر الفارغة إلى `>` وحده. يتيح ذلك لـ mdast رؤية 3 فقرات منفصلة داخل الاقتباس (العنوان / الوصف / الرابط) بدلًا من فقرة واحدة تحتوي على فواصل أسطر.
    - **`_build_translation_note_block` تكيّفي**: وفقًا لعدد الفقرات التي حافظ عليها LLM (3 = تنسيق البطاقة الكامل، 2 = عبارة + رابط، 1 = وضع احتياطي). لم يعد الوضع الاحتياطي ذي الفقرة الواحدة **يغلّف المحتوى بـ `**...**`** عند اكتشاف رابط Markdown من نوع `](` (إذ يكون عرض `<strong>` حول الرابط هشًا).
    - **توافق تصاعدي**: `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` في جانب `_compose_with_notes` — تواصل كائنات Namespace التي لا تحتوي على هذه السمات (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) العمل دون تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور حرفيًا على مقتطف من المصدر) + طبقة احتمالية (`langdetect`)
    - قائمة السماح `finish_reason` / `stop_reason`: إطلاق `RuntimeError` عند أي حالة خارج قائمة السماح (اقتطاع، content_filter، وما إلى ذلك)
    - `max_tokens` الخاص بـ Claude: `4096` ← `32768` (لتجنب الاقتطاع الكامن في المقاطع ذات 16k، مع هامش للتحويل بين أنظمة الكتابة FR→JA/ZH/KO/AR/HI)
    - تقسيم واعٍ بالعناوين: إعطاء الأولوية لـ H2/H3 في النصف الثاني من المقطع (بحيث يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى رمز خروج غير صفري: يعيد `translate_markdown_file` حالة نمطية `success` / `failure` / `skipped`، ويجعل `main()` القيمة `sys.exit(1)` إذا فشل ملف واحد على الأقل (للملف المنفرد والمعالجة الدفعية)
    - حاجز للمحتوى الفارغ لدى جميع المزوّدين، وفحص معقولية نسبة المصدر إلى المخرجات (≥ 500 محرف، وأقل من 5% = رفض)، والتحقق من العناصر النائبة للكود (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، وإعادة محاولة `BadRequestError` من دون `reasoning_effort`
    - إضافة الاعتمادية `langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("حزمة كاملة على نمط EurekAI"، 14 خطافًا):
    - Pre-commit: ‏ruff (فحص+تنسيق)، وshellcheck، وprettier ‏(md/yaml/json)، وdetect-secrets (حماية 4 مفاتيح API)، وLizard ‏(CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، ونهاية الملف، والملفات الكبيرة، وأسطر shebang، وما إلى ذلك)
    - Pre-push: ‏mypy (وضع متساهل تدريجي)، وOpengrep SAST ‏(translate.py + scripts/)، وpip-audit (وضع تقارير أولي)، وunittest discover ‏(tests/ + scripts/tests/)
    - أغلفة محلية ضمن `scripts/` تستخدم `./venv/bin/python`
    - `scripts/audit_verdict.py`: محلّل JSON لـ pip-audit مع 11 اختبار unittest، وهو نقل إلى Python لمحلّل jls42-astro
    - إصلاح 7 مخالفات أولية من ruff: ‏B904 ‏(raise from) ×2، وB007 ‏(مجلدات غير مستخدمة)، وC408 ‏(قيمة dict حرفية)، وC419 ‏(list-comp)، وSIM105 ‏(contextlib.suppress)، وSIM110 ‏(any())
    - يستثني Lizard مؤقتًا `translate.py` (4 دوال بقيم CCN تتراوح بين 21 و47، وإعادة الهيكلة مخطط لها) — بوابة صارمة على scripts/
  - **SonarCloud + تغطية شاملة**:
    - سير عمل GitHub Actions ‏`SonarCloud` ‏(sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع التغطية عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى README ‏(Quality Gate، وتقييمات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code)
    - `tests/test_silent_failure.py` ‏(`unittest` من المكتبة القياسية): يغطي الحلقات الست في سلسلة أخطاء الفشل الصامت
    - `tests/test_orchestration.py` ‏(+79 اختبارًا): يغطي طبقة التنسيق في `translate.py` ‏(`_resolve_*_filename`، `_existing_translation_exists`، `_record_translation_status`، `_write_output_file`، `translate_directory`، `_validate_input_paths`، `_init_*_client`، `_select_provider_client`، `_normalize_collapsed_markdown`، `_cleanup_source_flag`، وحالات الرجوع الاحتياطية لـ `_validate_news_flags_*` و`_openai_create_with_fallback` عند TypeError وBadRequestError، وتنسيق الموجّه لسلسلة o1، وفروع الإرجاع المبكر في `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` ‏(stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **تغطية الكود الجديد**: 75.5% ← نحو 98% ‏(translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك اختبارات E2E ‏`marker+top|bottom|both` و`legacy+top|bottom|both`)، وإضافة البادئات إلى عدة أسطر، والتوافق الرجعي بايت مقابل بايت (قيمة حرفية مرجعية)، وأداة التنقية، وتقسيم frontmatter (بما في ذلك إطلاق استثناء عند عدم إغلاق السياج)، وتنسيق الفقرات الثلاث، والوضع الاحتياطي ذي الفقرتين، وحاجز الفقرة الواحدة + رابط Markdown، وحاجز أمان حرج `TestLLMPayloadExcludesInvariants` يؤكد عدم إرسال العنوان وعنوان URL مطلقًا إلى LLM. **نجح 190 اختبارًا**، ومن دون أي تراجع.
  - التوثيق: `README.md` ‏(بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` ‏(سير عمل pre-commit + مراقبة مفصلة لـ CI)، وإعادة توليد 28 ترجمة
- **1.8** وضع `--news` + ترقية نماذج 2026 ‏(2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - OpenAI للجودة: `gpt-5` ← `gpt-5.4`
    - OpenAI الاقتصادي: `gpt-5-mini` ← `gpt-5.4-mini`
    - Gemini للجودة: `gemini-3-pro-preview` ← `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` ‏(400k) و`gemini-3.1-pro-preview` ‏(1M)
  - وضع `--news` الأولي: حماية الاقتباسات الإنجليزية بعناصر نائبة `#NEWSQUOTE\d+#`، وربط `LANG_FLAGS` ‏(15 لغة)، وإدارة الأعلام بحسب اللغة المستهدفة
  - التحقق من العناصر النائبة للأخبار قبل استعادتها (تراجع: كان حذف LLM للعنصر النائب ينتج بصمت مخرجات بلا اقتباس)
  - جعل السكربت `regen_translations.sh` قابلًا للنقل (مسارات مطلقة، ومن دون اعتماد على pwd)
  - إضافة رابط اللغة الفرنسية إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة
- **1.7** الميزات الجديدة:
  - خيار `--keep_filename` للاحتفاظ باسم الملف الأصلي عند الترجمة
  - دعم ملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على الكود المضمن**: أصبحت علامات backticks ‏(`` `...` ``) محمية في أثناء الترجمة
  - تحسين موجّه النظام:
    - تحسين معالجة علامات الاقتباس في YAML frontmatter
    - حماية متغيرات القوالب `{variable}`
    - منع ملاحظات المترجم غير المطلوبة
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** الميزات الجديدة:
  - دعم Google Gemini API للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI: ‏`gpt-5` (الجودة)، و`gpt-5-mini` (اقتصادي)
    - Claude: ‏`claude-sonnet-4-5` (الجودة)، و`claude-haiku-4-5` (اقتصادي)
    - Gemini: ‏`gemini-3-pro-preview` (الجودة)، و`gemini-3-flash-preview` (اقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف منفرد (`--file`) دون اجتياز مجلد
  - نمط تسمية مبسط جديد: `{base}-{lang}.md`
  - خيار `--include_model` للاحتفاظ بالتنسيق القديم الذي يتضمن اسم النموذج
  - دعم النماذج غير المدرجة مع حد افتراضي للرموز (128k)
  - ترجمة README إلى 14 لغة
- **1.5** التحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين موجّهات الترجمة:**
    - أُثريت موجّهات الترجمات المباشرة وملاحظات الترجمة لزيادة الوضوح والفعالية، بما يشمل تعليمات مفصلة بشأن الحفاظ على البيانات الوصفية وعناصر التنسيق المحددة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم عمليات الاستيراد لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص وإدارة كتل الكود للحفاظ على التنسيق الأصلي في أثناء الترجمة.
  - **إدارة ملفات المخرجات:**
    - عكس ترتيب النموذج واللغة في أسماء ملفات المخرجات (على سبيل المثال، `f"{base}-{args.target_lang}-{args.model}.md"`)، مما يسهّل تنظيم الترجمات والعثور عليها.
  - **تحسينات متنوعة:**
    - تنظيف الكود بحذف الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكربت وسهولة قراءته.
- **1.4** الميزات الجديدة:
  - دعم Claude API من Anthropic للترجمة
  - تحسين الموجّهات لزيادة الوضوح والفعالية
  - تعديلات طفيفة لتحسين قابلية صيانة الكود
- **1.3** التحسينات والميزات الجديدة:
  - تحسين إدارة كتل الكود
  - تحسين إدارة ملفات المخرجات
  - تحسين اكتشاف الملفات الموجودة
  - خيار `--force` لفرض الترجمة
  - عكس ترتيب النموذج واللغة في اسم ملف المخرجات
- **1.2** إصلاح سجل التغييرات
- **1.1** إضافة دعم Mistral AI API
- **1.0** الإصدار الأولي - دعم OpenAI API

**مقال مترجم من الفرنسية إلى العربية باستخدام gpt-5.6-sol.**
