### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.13.1** حداثة التبعيات: لم يعد التأخر تحذيرًا يمكن ألا يقرأه أحد (2026-09-09):

  - **التحذير الذي لا يُحتسب هو تحذير يفوتنا.** كان `check-deps-fresh.sh` يشير منذ عدة أيام إلى أن `openai` و`anthropic` قد تأخرا، لكن سطر ملخص البوابة لم يكن يحتسب إلا الإخفاقات: إذ كان يعلن «جاهز: N من عمليات التحقق ناجحة» بينما كان التأخر مذكورًا قبل ذلك ببضعة أسطر. ومن يقرأ السطر الأخير — وهذا ما يفعله الجميع — لم يكن بوسعه معرفة ذلك. أصبح الحكم الآن يتضمن عدد التحذيرات، ويتولى عدّاد مخصص إحصاءها بدل طباعتها تباعًا أثناء التنفيذ.
  - **يأتي التأخر مصحوبًا بملاحظات الإصدار.** لا يوضح رقم الإصدار ما الذي تغير، والعثور على الملف الصحيح بنفسك هو تحديدًا الاحتكاك الذي يؤدي إلى تجاوز هذه الخطوة. يعرض الفحص عنوان CHANGELOG لكل حزمة متأخرة، سواء كان التحديث رئيسيًا أم ثانويًا، مع توضيح السبب: فقد سبق أن غيّر تحديث ثانوي لـ SDK أمورًا يعتمد عليها هذا المشروع.
  - **`openai` 3.8.0 → 3.10.0 و`anthropic` 1.3.0 → 1.4.0، مع قراءة ملاحظات الإصدار قبل التثبيت.** من جهة OpenAI، لم يحدث ضمن هذا النطاق سوى تغيير سلوكي واحد — لم يعد ترويس `Retry-After` رقمي يتجاوز سعة العدد ذي الفاصلة العائمة يؤدي إلى الرجوع إلى مهلة تراجع قصيرة، بل يعيد الخطأ الأصلي من دون إعادة المحاولة — ولا شيء يمس ما يعتمد عليه هذا المشروع: لم تتغير سماحية النماذج للحقول الإضافية، وقد جرى التحقق من ذلك في الشيفرة عند كلا الوسمين، ولذلك يستمر قبول `native_finish_reason` و`error` اللذين يضيفهما OpenRouter إلى أحد الخيارات. ومن جهة Anthropic، يرفض حاجز جديد، عبر `TypeError` صريح، كائنًا من حزمة `httpx` في موضع يتوقع فيه SDK النوع `httpx2`؛ وهذا المشروع لا يمرر سوى أعداد ذات فاصلة عائمة، وقد جرى التحقق من ذلك. أما القاعدة التي ترفض استدعاءً غير متدفق يتجاوز عشر دقائق فهي متطابقة بايتًا ببايت بين الإصدارين: و`timeout` الصريح هو بالفعل ما يعفيه منها، إذ تتجاوز رموزه البالغ عددها 32 768 الحد البالغ 21 333.
  - **تم التحقق**: مجموعتان تضمان 502 + 31 اختبارًا على الإصدارات المحدّثة، وإغلاق `requirements.txt` مطابق لما هو مثبّت (41 تثبيتًا للإصدارات)، واستدعاء حقيقي واحد لكل SDK — OpenAI وClaude — على مستند بوضع `--news`، ببنية مطابقة للمصدر.

- **1.13.0** المزوّد `--use_openrouter`: موجّه مدفوع إلى نحو 430 نموذجًا، من بينها النماذج الصينية المفتوحة (2026-09-05):

  - **مسار المزوّد التاسع، يُسلَّم مع الثامن.** لم يُنشر الإصدار 1.12.0 على PyPI: سيصدر الموجّهان، OpenCode وOpenRouter، معًا. يتيح [OpenRouter](https://openrouter.ai) بمفتاح واحد الوصول إلى نماذج لا يعرضها هنا أي مزوّد آخر — Kimi وQwen وDeepSeek وZ.ai — عبر رصيد واحد تُحتسب تكلفته بحسب الاستخدام. وبما أن نقطة النهاية متوافقة مع OpenAI، فالعميل هو نفسه المستخدم مع xAI؛ **وكل ما يميز هذا المزوّد يكمن في فحص تمهيدي**، وكل قاعدة من قواعده مستمدة من قياس أُجري على API.

  - **يُقدَّم النموذج نفسه عبر عشرات المستضيفين ذوي الحدود القصوى المختلفة، والتوجيه لا يراعي ذلك.** أظهرت القياسات وجود 33 مستضيفًا لـ`z-ai/glm-5.2` و23 لـ`z-ai/glm-5.3-flash` — من بينها مستضيف محدود عند **2 048 رمز إخراج**. ولذلك كانت ترجمة طويلة من أصل 23 تصل مبتورة عشوائيًا، من دون أي إشارة. يقرأ الفحص التمهيدي `/api/v1/models/{modèle}/endpoints`، ويستبعد المستضيفين الذين يقل حد إخراجهم عن 8 000 رمز، والذين في حالة متدهورة، والذين لا يعلنون أي حد أقصى، ثم يثبّت الباقين. إن `provider.only` **من دون** `allow_fallbacks: false` ليس سوى تفضيل: إذ يعود الموجّه إلى مستضيف مستبعد، فيفقد التثبيت أي قيمة. وإذا لم يكن أي مستضيف قادرًا على الوفاء بالحد الأقصى، يتوقف الأمر: فالمضي في الترجمة يعني قبول البتر الصامت الذي وُجد هذا الفحص التمهيدي لمنعه.

  - **يُحتسب الاستدلال بسعر الإخراج، وهو مفعّل افتراضيًا في كثير من النماذج.** الطلب نفسه على `z-ai/glm-5.2`، والإجابة «حسنًا»: **107 رموز إكمال بإعداد النموذج الافتراضي، ورمزان عند تعطيل الاستدلال**. وفي الترجمة، التي لا يضيف إليها الاستدلال شيئًا، يعني ذلك زيادة بمقدار 18 ضعفًا لكل مقطع من كل ملف. ولذلك يُعطّل افتراضيًا. أما **النماذج البالغ عددها 288 من أصل 431** التي تفرضه (`reasoning.mandatory`)، فتجيب بـ`400 « Reasoning is mandatory for this endpoint and cannot be disabled »`: وبالنسبة إليها، يقرأ الفحص التمهيدي مستويات الجهد المقبولة ويطلب أدناها (النقطة التالية) — إذ يخصص الجهد **نسبة مئوية من `max_tokens`** يستهلكها الاستدلال أولًا، ولذلك فإن اختيار قيمة عشوائية ينقل خطر الصفحة البيضاء بدل تقليله.

  - **يتلقى النموذج الذي يفرض الاستدلال أدنى جهد يقبله، وقد تقرر ذلك بناءً على قياس.** كان الخيار الأول ألا يُرسل شيء، تجنبًا للتخمين نيابة عن النموذج. وعند اختباره على `z-ai/glm-5.3-flash`، الذي تكون قيمة الكتالوج الافتراضية له `max`، أدى هذا الاختيار إلى إخراج **مبتور عند 32 768 رمزًا** قبل نهاية الترجمة — فضاعت لغتان من أصل أربع عشرة. ولم تكن زيادة السعة ستغير شيئًا: فالجهد يخصص نسبة مئوية منها، وينمو الاستدلال معها. لذلك يقرأ المزوّد `supported_efforts` أثناء الفحص التمهيدي ويطلب أدنى مستوى، مع الرجوع إلى «لا شيء» عندما لا يعلن الكتالوج أي مستوى قابل للاستخدام. وأكد الاختبار المضاد على اللغة التي سببت المشكلة ذلك: كانت تفشل بسبب نفاد الميزانية، وأصبحت الآن تنجح خلال 9 دقائق ببنية مطابقة للمصدر.

  - **أصبح عطل المستضيف الأعلى يحمل اسمه الآن.** يوحّد الموجّه هذه الحالة في `finish_reason=error` مع `native_finish_reason` فارغ — وقد قيس ذلك مرتين، بلغتين، عند 750 ثانية بالضبط. كانت الرسالة العامة تدفع إلى البحث عن عيب في المستند أو في التجزئة؛ أما الآن فتوضح أن العطل يقع لدى المزوّد وأن إعادة المحاولة تكفي غالبًا.

  - **إن `finish_reason=length` مع إخراج فارغ ليس بترًا.** بل يعني أن الاستدلال استهلك الميزانية قبل أول محرف مفيد — وقد قيس استهلاك 15 850 رمز استدلال مقابل 148 رمزًا مفيدًا. وتتطلب الحالتان إجراءين متعاكسين: فتقليل حجم المقاطع لا يفيد في الحالة الأولى. وتفرق الرسالة بينهما صراحة. كما أضيف حاجزان آخران استنادًا إلى قياسات: يجيب الموجّه بالحالة **200 وجسم لا يحمل سوى خطأ** عندما يفشل المستضيف الأعلى (كان `choices[0]` يطلق `TypeError` مبهمًا يحجب الرسالة)، وتُقرأ نافذة السياق من الكتالوج ثم تُسجّل في `MODEL_TOKEN_LIMITS` — إذ إن `DEFAULT_TOKEN_LIMIT` غير صحيح لـ44 نموذجًا في الكتالوج، من بينها نموذجان محدودان عند 4 095 رمزًا.

  - **`--model fournisseur/modèle` إلزامي، ويُتحقق من صيغته قبل أي اتصال بالشبكة.** ليس OpenRouter مزوّدًا: فالاختيار يحدد السعر والترخيص ومعالجة البيانات، ولا يجوز اتخاذه نيابة عن المستخدم. وبما أن الاسم المختصر يُدرج في URL الخاص بالفحص التمهيدي، فإن التحقق ليس مجرد مجاملة لتحسين سهولة الاستخدام، بل هو الحاجز الذي يمنع حقن مسار فيه: يقبل التعبير النمطي المشترك ذو مساحة الاسم بين الموجّهين `a/b/..`، ومن هنا يأتي الرفض الصريح لمقطع المجلد الأب. لا تأثير لـ`--eco`، وهو يصرّح بذلك.

  - **صُححت ثلاث صياغات، إحداها كانت خاطئة.** كان تحذير OpenAI بشأن `codex exec` يستهدف حقن ملف جلسة شخصي على مشغّل مشترك، لا كون المستودع عامًا: وقد ورد بمعنى معاكس في README وCLAUDE.md وفي الشيفرة. كما تغيّر موضع مصادقة OpenCode في الإصدار 1.18.27 (الجدول `credential` في `opencode.db`، بدلًا من `auth.json`)؛ وظل الثابت «لا تُقرأ هنا أبدًا» صحيحًا، لكن العنوان كان قديمًا. وأخيرًا، لم يعد قسم OpenCode يعرض مسارات لم تُختبر قط على أنها متكافئة: فقد جرى قياس بوابة Zen وOllama من البداية إلى النهاية، بينما لم تُختبر GitHub Copilot وLM Studio وllama.cpp، وأصبح README يذكر ذلك الآن.

  - **حملة قياس، وجدول بالنماذج الموصى بها في README.** نُفذت أكثر من ثلاثمئة ترجمة على ثلاث مجموعات من المستندات — مقال مدونة كثيف بوضع `--news`، وملف README هذا بصيغة Markdown القياسية، وأربعة ملفات README لمشاريع معروفة كما هي من GitHub — إلى أربع عشرة لغة. يميز الجدول بين أمرين كانا يُخلطان: الترجمات التي **تكتمل** وتلك التي تكون **بنيتها مطابقة** للمصدر. لم تفقد ثلاثة نماذج أي معلومة على الإطلاق في المستندين الكثيفين: `gemini-3.7-flash` و`gpt-5.6-sol` عبر اشتراك ChatGPT و`z-ai/glm-5.2` عبر OpenRouter — ولم تتجاوز اختلافاتها الوحيدة زوجًا من `**` لم يُنقل في لغة أو لغتين. والخلاصة الأساسية هي أن **العامل الفاصل هو كثافة المستند، لا وضع `--news`**: يفشل Grok عبر الاشتراك ثلاث عشرة مرة من أصل أربع عشرة في مقال المدونة، وينجح في أربعة عشر ملف README عامًا من أصل ستة عشر، والسبب هو فقدانه المسار في مقطع طويل، وقد ثُبت ذلك باختبار مضاد. ويتضمن الجدول تحذيره الخاص: فهو غير شامل، ومؤرخ، والمدد لا تمثل ترتيبًا، وتظل المنهجية الصحيحة هي القياس على مستنداتك الخاصة.

  - **صُحح مقارن البنية قبل نشر أرقامه، لأنه كان ينتج نتيجتين إيجابيتين زائفتين في الكتابات غير اللاتينية.** لم يكن URL متبوعًا بقوس كامل العرض `）` يُفصل بواسطة تعبير نمطي يتوقف عند `)`، ولذلك كانت السلسلة المستخرجة تختلف رغم تطابق URL؛ كما كان اقتباس من خمسة أسطر بالفرنسية يرد في ثلاثة أسطر بالصينية، مما يخفض تعداد الأسطر. وقد جرى التحقق من التصحيحين باختبار مضاد: إذ يظل حذف URL أو قسم أو شيفرة مضمّنة قابلًا للكشف. ومن دونهما، لكانت أرقام Gemini وCodex المنشورة إحدى عشرة لغة من أصل أربع عشرة بدلًا من ثلاث عشرة واثنتي عشرة.

  - **الاختبارات**: ملف جديد `tests/test_openrouter_provider.py` (76 اختبارًا) — التحقق من النموذج ورفض مقطع المجلد الأب، وتثبيت المستضيفين (الحد الأقصى، والحالة، والحد الأقصى غير المعلن، والحد الأدنى المشترك)، وبقاء `allow_fallbacks` خطأ دائمًا، وتعطيل الاستدلال أو تركه وفق `mandatory`، وعقد الإخراج الكامل (خطأ داخل استجابة 200، وعدم وجود خيار، وتمييز الصفحة البيضاء عن البتر، و`finish_reason` غير طبيعي، ومحتوى فارغ)، وإغلاق الفحص التمهيدي عند الفشل إذا تعذر الوصول إلى الكتالوج، أو غاب الاسم المختصر، أو لم يوجد مستضيف سليم، وحصرية العَلَم وتسمية اسم الملف. المجموعة الكاملة تضم **502 اختبارًا**.
  - **إعادة الهيكلة: قُسّمت الوحدة الوحيدة ذات 4,253 سطرًا إلى وحدات، من دون تغيير أي سطر من السلوك.** وُزّع `src/aipmt/translate.py` على `config` و`markdown` و`segmentation` و`guards` و`placeholders` و`news` و`prompts` و`notes` و`naming` و`pipeline` و`cli` وحزمة فرعية `providers/` (وحدة لكل provider، و`base` كأساس، و`registry` للحلّ والتوجيه). كل عملية نقل هي commit ذو إثبات آلي: يقارن مدقّق AST لجميع عقد المستوى الأعلى في الحزمة بلقطة مرجعية، ويتحقق من موضع كل رمز، وبقاء علامات الأمان حرفيًا، وعدم وجود ملف غير متعقّب — وهي أدوات مؤقتة أزالها الإصدار التالي. أما التغييرات الظاهرة: فقد أصبح `aipmt.translate` واجهة تعيد تصدير، مع الحفاظ على هوية الكائن، الأسماء الـ64 التي كانت الوحدة تعرضها من دون بادئة `_` (يحمل `__all__` تسعة منها، وهي API المدعومة؛ والبقية أسماء مستعارة للتوافق)، وتوقف عن إعادة تصدير أسماء التبعيات والمكتبة القياسية الـ29 التي كان يجمعها `import *`؛ ولم يعد التنفيذ المباشر للملف (`python src/aipmt/translate.py`) موجودًا — وتظل `aipmt` و`python -m aipmt` الصيغتين المدعومتين؛ وأصبح `__module__` للدوال العامة هو الخاص بوحدة تعريفها؛ وتُستورد SDK بعد تحميل `.env` بدلًا من قبله، من دون أثر معروف. حُفظت الاختبارات الـ427 حتى على مستوى المعرّفات، ونُقلت إلى الوحدات التي تختبرها: صارت 91 عملية patch كانت تمر عبر الواجهة تستهدف الوحدة التي تقرأ الاسم (ظل اثنان منها ناجحين من دون patch الخاص بهما، وفق القياس)، وثبّتت سبعة اختبارات تعاقدية الواجهة، وأُعيدت كتابة أدوات gates قبل أول عملية نقل كي لا يتمكن أي منها من التحول إلى الأخضر بمجرد توقفه عن التحقق — نطاق Lizard على مستوى المجلدات مع حد أدنى، وقراءة flags من parser المُنشأ، وحد أدنى للتغطية لكل حزمة، و`release.sh` يسرد الوحدات المتعقبة.
  - **إصلاح (مراجعة pull request)**: يرفض OpenRouter سجل كتالوج بلا `context_length` بدل تسجيل القيمة الافتراضية البالغة 128,000 token بوصفها قياسًا — وهو ما كان يعطّل أيضًا تحذير «النموذج غير مُدرج» — وعندما يكون `finish_reason` فارغًا (النوع الموثق `string | null`)، يُعتد بسبب المستضيف الخام، وتكون قيمة `max_tokens` هي `length`؛ والخطأ الذي يحمله الاختيار نفسه يرفض المحتوى الجزئي المصاحب له، بالرسالة نفسها المستخدمة للعطل العلوي الموحّد — تفاصيل المستضيف والسبب الخام والنصيحة مجتمعة. يستجيب OpenCode بخطأ العقد الخاص به، لا بـ`AttributeError`، عند حدث `part: null`، ويرفض تدفق JSONL إذا تعذر تحليل أحد أسطر أحداثه بدل قبول نص جزئي. تقتل واجهات CLI الوكيلة الثلاث مجموعة الوكيل عندما تتلقى تلك العملية `SIGTERM` أثناء الاستدعاء — إذ كان `timeout` الخاص بإعادة التوليد يترك الوكيل حيًا ليستمر في استهلاك حصته — ويظل `SIGKILL` للمجموعة ملتزمًا دائمًا بمهلة السماح، بحيث يترك shim ينتهي بصورة سليمة حفيده حيًا. أُعيد وصل زوج `# fmt: off` / `# fmt: on` كان التقسيم قد فصله؛ وتسمّي مساعدة `--reasoning_effort` موفري الخدمة الأربعة الذين يستخدمونه.
  - **إصلاح (المراجعة الثانية)**: صار الحد الأقصى للمخرجات المطلوب من OpenRouter يحجز الآن الحصة التي يشغلها prompt والمقطع من السياق — إذ يغطي `context_length` الإدخال والإكمال معًا، وكانت طلبات ستة نماذج في الكتالوج تُرسل من دون مساحة للإدخال؛ ويُرفض السياق الأقصر من اللازم قبل أي فوترة. يعود أساس واجهات CLI الوكيلة إلى `terminate` ثم `kill` حيث لا تتوفر مجموعات عمليات POSIX، بدل السماح لـ`AttributeError` بتجاوز حارس timeout واستمرار الانتظار. يُبحث عن علامة `429` في OpenCode بوصفها رقمًا، لا كسلسلة فرعية، إذ كان معرّف خطأ مثل `err_84290b` يطلق 90 ثانية من back-off قبل أن يفشل في النهاية. وأخيرًا، يُعرض endpoint غير قياسي لـOpenRouter في preflight: يكفي `.env` على مستوى المشروع لتعيينه، وهو المفتاح الحقيقي الذي سيُرسل لاحقًا.
  - **الأمان: لم يعد بإمكان `.env` على مستوى المشروع إعادة توجيه استدعاءات API.** يبحث `find_dotenv(usecwd=True)` عن الملف بدءًا من المجلد الحالي ثم المجلدات الأب: كان بإمكان شجرة غير موثوقة — مثل مستودع استُنسخ للتو — أن تضع فيه `OPENROUTER_BASE_URL` أو `XAI_BASE_URL` أو `OPENAI_BASE_URL` (ويقرأ SDK الأخير بنفسه) من دون معرفة أي مفتاح، ثم يُرسل المفتاح الحقيقي القادم من البيئة أو إعدادات المستخدم في ترويسة التفويض إلى خادم تابع لجهة خارجية. يعتمد المرشح على نمط، لا على قائمة: وفق حصر SDK المثبتة، تُقرأ اثنا عشر متغيرًا للتوجيه، ستة منها بواسطة عميل Anthropic وحده — وكانت قائمة مكتوبة يدويًا ستغفل نصفها. لذلك تُرفض من طبقة المشروع جميع المتغيرات التي تطابق `_BASE_URL` أو `_API_BASE` أو `_ENDPOINT`، وproxies ومخازن الشهادات (فتحديد سلطة مُتحكَّم بها يجعل وسيط الاعتراض غير قابل للتمييز عن خادم حقيقي)، وكذلك `XDG_CONFIG_HOME` و`APPDATA`: إذ كان تعيينهما يعني تحديد الملف الذي يشكل طبقة المستخدم، وبالتالي الالتفاف على المرشح. لا تُقبل هذه المتغيرات إلا من الطبقتين اللتين يتحكم فيهما المستخدم: البيئة المصدّرة و`~/.config/aipmt/.env`. كما تُقرأ طبقة المشروع بلا interpolation: يوسّع `load_dotenv` قيمة `${VAR}` افتراضيًا، وكان `.env` غير موثوق يحتوي على `NOM_ANODIN=${OPENAI_API_KEY}` ينسخ المفتاح الحقيقي تحت اسم لا يتعرف عليه الترشيح النمطي للعمليات الفرعية — فيدخل حينها إلى بيئة `codex exec`، خلافًا للثابت المعلن. وأخيرًا، لا يعرض الرفض سوى اسم المتغير: فقد كان URL بالصيغة `https://${CLE}@hôte/` يسرّب المفتاح المُضمَّن إلى السجلات، حتى أثناء رفضه. يُبلَّغ الرفض على stderr مع إرشادات المتابعة — ويُعرَّف مرحّل المؤسسة في إعدادات المستخدم.
  - **إصلاح: يُحسب غلاف مخرجات OpenRouter لكل استدعاء.** يغطي `context_length` الإدخال والإكمال معًا، ولا يضمن احتياطي ثابت مُعاير على نص لاتيني أي حد: وفق القياس باستخدام tokenizer ‏`o200k_base`، تساوي 16,000 محرف 3,200 token بالفرنسية، و12,300 باليابانية، و17,500 في emoji. لذلك تُشتق الميزانية من النص المُرسل فعليًا، مع زيادتها بعدد وحدات البايت UTF-8 الخاصة به: ففي كل tokenizer قائم على دمج وحدات البايت — سواء BPE على مستوى البايت أو SentencePiece مع رجوع إلى البايت، وهي العائلات التي يستخدمها الكتالوج — يعادل كل token بايتًا واحدًا على الأقل، وهذه هي الزيادة الوحيدة المتاحة هنا، حيث يوجّه OpenRouter إلى عشرات tokenizers غير المعروفة لدينا. لم تصلح أي نسبة متوسطة — إذ ينخفض رمز كتابي من المستوى الإضافي إلى 1.33 بايت لكل token، ومحرف مركّب إلى 1.00 — وصار الإدخال مع المخرجات يندرجان الآن ضمن النافذة بحكم التصميم. ويُرفض المقطع الأعلى كثافة من قدرة النموذج المختار قبل الاستدعاء بدلًا من رفضه بعد الفوترة.

- **1.12.0** ‏Provider ‏`--use_opencode`: ‏OpenCode، الوكيل مفتوح المصدر، نحو المورّد الذي يختاره المستخدم — نموذج محلي، أو مجاني بلا حساب، أو اشتراك، أو مفتاح (2026-09-04):

  - **المسار الثامن للـprovider، وهو من طبيعة مختلفة عن المسارات السبعة الأولى.** ‏[OpenCode](https://opencode.ai) (MIT) ليس مورّد نماذج، بل _موجّهًا_ نحو المورّدين الذين أعدّهم المستخدم داخل OpenCode نفسه: مفتاح API، أو اشتراك (GitHub Copilot وChatGPT وSuperGrok)، أو بوابة OpenCode Zen — التي توفر نماذج مجانية **بلا حساب** — أو نموذج **محلي** (Ollama وLM Studio وllama.cpp). يتحكم البرنامج النصي في `opencode run` بالوضع غير التفاعلي، كما يفعل مع Codex وGrok، ويعيد استخدام أساس العمليات الفرعية نفسه (مجموعة عمليات مستقلة، ثم `SIGTERM` و`SIGKILL` عند timeout، وإغلاق stdin دائمًا، وتنقية البيئة). جرى التحقق عبر **ترجمتين فعليتين**: ملف README هذا كاملًا إلى الإنجليزية بواسطة `opencode/mimo-v2.5-free` — خلال 49 ثانية، وفي تمريرة واحدة، وبنية مطابقة للملف المصدر (32 عنوانًا، و26 إغلاقًا لمقاطع code، و18 رابطًا، و37 ‏URL، و37 سطر جدول، و135 مقطع code ضمنيًا) — وملف تجريبي بواسطة `ollama/qwen2.5:7b` محليًا ومن دون أي مفتاح.

  - **`--model provider/modèle` إلزامي، وهذا قرار مقصود.** من دون `--model`، يعود OpenCode إلى قيمته الافتراضية، وهي في تثبيت جديد `opencode/big-pickle`، نموذج «stealth» مجاني قد تُستخدم المحادثات معه في التدريب — ووفق القياس، كان هذا هو النموذج الذي استجاب. إن اختيار ذلك بصمت بدلًا من المستخدم سيكون بالضبط من نوع التحولات الخفية التي يتعقبها هذا المستودع؛ ولذلك تسمّي رسالة الخطأ الأمر الذي يسرد النماذج (`opencode models`) وثلاثة أمثلة (محلي، مجاني، اشتراك). ليس لـ`--eco` أي تأثير، وهو يصرّح بذلك. يُمرر `--reasoning_effort` كما هو بوصفه `--variant` الخاص بـOpenCode، وفقط عند طلب صريح.

  - **عزل مقاس، لا مفترض.** يعرّف إعداد inline ‏(`OPENCODE_CONFIG_CONTENT`، وهو الأخير في ترتيب الدمج لدى OpenCode، ومن ثم له الأولوية على إعداد المستخدم من دون استبداله) وكيلًا `aipmt` تُرفض جميع أدواته (`permission: {"*": "deny"}`): فلا يعرضها السجل للنموذج أصلًا، وعندما يُطلب منه «سرد الملفات وتشغيل `id`» يجيب بأنه لا يملك أدوات. مشاركة الجلسة معطلة، وplugins الخارجية مستبعدة (`--pure`)، ولا يُستخدم `--auto` أبدًا، ومجلد العمل مؤقت وفارغ. جرى قياس عمليتي حقن صامتتين ومنعهما: من دون `OPENCODE_DISABLE_CLAUDE_CODE` يدخل `~/.claude/CLAUDE.md` الخاص بالمستخدم في **كل** prompt ‏(515 token إدخال بدلًا من 186 لعبارة «مرحبًا» بسيطة)؛ ومن دون `OPENCODE_DISABLE_PROJECT_CONFIG` يدخل أيضًا `AGENTS.md` الخاص بالمجلد الحالي — وقد طُبقت على الترجمة تعليمة تقول «اختم كل إجابة بكلمة BANANA». أما `~/.config/opencode/AGENTS.md` العام فيظل محقونًا: فلا يوجد مفتاح يعطله، كما أن الالتفاف عليه باستخدام `XDG_CONFIG_HOME` محرّف سيخفي أيضًا مورّدي المستخدم. لذلك وُثّق بدل اختلاق حل ترقيعي له.

  - **لا يثبت `exit 0` شيئًا؛ إنها ثالث CLI وبردود الفعل نفسها — مع فخين خاصين بها.** لا يؤدي `--agent` غير المعروف إلى فشل `opencode run`: يظهر تحذير على stderr ثم يحدث رجوع **صامت** إلى وكيل البرمجة، مع أدوات مفعّلة. فإذا لم يؤخذ إعداد inline في الحسبان، ستبدأ الترجمة إذن بوكيل قادر على الكتابة؛ ولذلك يتحقق عقد المخرجات من غياب هذه الرسالة، إضافة إلى: رمز إرجاع 0، وعدم وجود أي حدث `error`، وعدم وجود `tool_use`، وأن يكون آخر `step_finish` من النوع `stop` (إذ إن `length` استجابة مبتورة)، وأن يكون النص غير فارغ. الفخ الثاني: حدث الخطأ في JSON **معتم** — «خطأ خادم غير متوقع. راجع سجلات الخادم للحصول على التفاصيل.»، مع مرجع بسيط — ولا يوجد السبب الحقيقي (`ProviderModelNotFoundError: Model not found: foo/bar. Did you mean…` و`ProviderAuthError`…) إلا في السجلات. ومن هنا يأتي `--print-logs --log-level ERROR` وقراءة حقل `error="…"` من stderr، من دون تتبع Bun الذي يليه. وهكذا يفشل النموذج غير المعروف خلال ثانية واحدة، مع تسمية سببه. كما يمنع `--title` استدعاء LLM طفيليًا: فمن دونه، يولّد OpenCode عنوان جلسة عبر جولة إضافية على `small_model`.

  - **الأسرار: الترشيح النمطي نفسه المستخدم مع Codex وGrok، مع استثناء واحد محدد بالاسم.** يُحتفظ بـ`OPENCODE_API_KEY`: فهو مفتاح OpenCode نفسه (بوابة Zen واشتراك Go)، وموجّه إليه باسمه — أي ما يعادل `auth.json` الخاص به، وليس مفتاحًا يديره aipmt أو يمكنه فوترة استخدامه. يُعدّ المورّدون داخل OpenCode ‏(`opencode auth login` و`opencode.json`)، وليس أبدًا داخل `.env` الخاص بـaipmt، ولا يصل أي مفتاح منه إلى العملية الفرعية. لا يوجد رفض في CI، بخلاف واجهات CLI القائمة على الاشتراك: إذ يُعد مفتاح API أو نموذج مستضاف ذاتيًا على runner استخدامًا مشروعًا.

  - **صار حارس منع الاجتياز يتحقق من القيمة بعد interpolation، لا من القيمة الخام.** يحتوي `provider/modèle` على `/` كان حارس الإصدار 1.10.0 يرفضه — وكان محقًا، لأن `--model` كان يُضمَّن في اسم الملف `--include_model`. يستبدل وسم اسم الملف الآن `/` و`\` و`:` بـ`-` قبل أي interpolation ‏(`ollama/qwen2.5:7b` ← `ollama-qwen2.5-7b`، إذ إن `:` غير قانوني على Windows)، ويفحص الحارس العلوي هذا الوسم: فيصبح `../../evil` مجرد الاسم `doc-en-..-..-evil.md` تحت الهدف، ولا يبقى مرفوضًا سوى `..` وحده، وكذلك `--target_lang ../x`. ويظل حارس النطاق `_ensure_within_directory` طبقة ثانية بلا تغيير.

  - **النماذج المجانية والنماذج المحلية، وما جرى قياسه.** يترجم `opencode/mimo-v2.5-free` فقرة خلال 16 ثانية وملف README هذا خلال 49 ثانية؛ ويستغرق `opencode/big-pickle` مدة 40 ثانية لترجمة 200 كلمة، وقد ظل طلبان متزامنان عليه من دون استجابة مدة 5 دقائق، بينما ينجح كل منهما منفردًا؛ ولم يرسل `opencode/nemotron-3.5-lightning-free` أي استجابة خلال 3 دقائق. ومن هنا جاء `REGEN_PROVIDER=opencode` مع إلزام `REGEN_MODEL` وتشغيل **مهمتين** بالتوازي. محليًا، يضبط Ollama غالبًا سياقًا من 4,096 token، بينما تصل المقاطع إلى 16,000 محرف: لذلك يلزم `Modelfile` مع `PARAMETER num_ctx 32768`، وتتبع الجودة النموذج — فقد عكس نموذج 7B ترتيب قائمة وأتلف إغلاق مقطع code في الملف التجريبي، بينما حافظ نموذج البوابة على كل شيء.

  - **لم تعد ترجمات هذا المستودع تمر أبدًا عبر API مدفوعة.** كان `regen_translations.sh` يستخدم OpenAI API فور العثور على مفتاح في `.env`، ولم يجعل Codex سوى خيار opt-in. وهذا بالضبط ما حدث أثناء إعداد هذا الإصدار: أُرسلت الترجمات الـ28 إلى OpenAI API، ثم أُرسل CHANGELOG الهندي إلى Gemini API، مع أن اشتراك ChatGPT موجود تحديدًا لتجنب الدفع حسب الاستخدام. أُزيل الاكتشاف التلقائي للمفاتيح: **Codex هو الخيار الافتراضي، مع `gpt-5.6-sol`**، وهو نموذج الجودة؛ وتتطلب `openai` و`gemini` و`grok` استخدام `REGEN_ALLOW_PAID_API=1` إضافة إلى `REGEN_PROVIDER`، وهو استثناء مسمّى كي تُطبق القاعدة لحظة اتخاذ القرار؛ ويفشل `REGEN_PROVIDER` غير المعروف بدل الرجوع إلى API. تثبّت عشرة اختبارات الخيار الافتراضي والرفض والاستثناء. وأُعيدت الترجمات الـ28 لهذا الإصدار عبر Codex.

  - **وُحّدت آلية back-off عند بلوغ rate limit** ‏(`_retry_on_rate_limit`): فقد كانت حلقتا Codex وGrok متطابقتين باستثناء التسمية، وكانت نسخة ثالثة ستتجاوز حد التكرار. ترث أخطاء CLI الثلاثة من `_CliCallError` واحدة؛ ويمنع اختبار خروج أي منها عن هذا التسلسل، وإلا فلن تراها الحلقة المشتركة.

  - **الاختبارات**: ملف جديد `tests/test_opencode_provider.py` ‏(61 اختبارًا) — عقد مخرجات كامل، والرجوع إلى الوكيل، وقراءة السبب من السجلات، وإزالة تكرار أجزاء النص وتجاهل الأجزاء الاصطناعية، وtimeout يقتل مجموعة العمليات، وback-off عند 429، وإلزام النموذج والتحقق منه، وpreflight بلا أسرار، وحل مسار الملف التنفيذي، وتوصيل dispatch، ووسم اسم الملف، واختبار مضاد للاجتياز. يوسّع `tests/test_review_hardening.py` حصرية flags وغياب الأسرار لتشملا الـprovider الجديد. يتطلب gate الآن توثيق **22 flag** لـargparse. بلغت الحزمة الكاملة **382 اختبارًا**.

- **1.11.1** إصلاح توثيقي: يعلن ملف README أخيرًا مسارات الـprovider السبعة (2026-09-03):
  - **كانت صفحة PyPI للإصدار 1.11.0 تقول «4 واجهات API + واجهة Codex CLI».** بينما يتيح الكود سبعة خيارات — OpenAI وMistral وClaude وGemini وGrok عبر API؛ وCodex (ChatGPT) وGrok عبر الاشتراك، من دون فوترة حسب الاستخدام. كان وضعا Grok غائبين عن المقدمة ونقطة _Multi-Provider_، وكررت الترجمات الأربع عشرة الخطأ. وبما أن الوصف الطويل للحزمة ثابت لكل إصدار، فقد تطلب تصحيح واجهة العرض رقم إصدار جديدًا: وهذا هو السبب الوحيد لوجود هذا الإصدار. **لا تغييرات في الكود.**
  - تمت مواءمة `CLAUDE.md` مع ما قدمه الإصدار: عدادات الـgate ‏(16، و17 في `--full`)، والـworkflows النشطة الأحد عشر، وعدادا Sonar/Codacy غير الظاهرين في `gh pr checks` ‏(hotspots وCodacy API)، ونقل `# nosemgrep` واحد عبر `ruff-format`، وبيئات GitHub التي يتطلبها تبادل OIDC، وحقيقة أن _pending publisher_ لا يحجز الاسم.

- **1.11.0** إصدار على PyPI: ‏`pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-09-03):

  - **أصبح السكربت أحادي الملف حزمة قابلة للتثبيت.** انتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول console باسم `aipmt` والمكافئ `python -m aipmt`. يظل المستودع المستنسخ ضروريًا للمساهمة — إذ توجد فيه الاختبارات والترجمات الـ28 وأدوات الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` مطلقًا**، لأن التصادم حقيقي وصامت. تثبّت حزمة PyPI المسماة `translate` ‏(v3.8.1، آخر رفع في 2026-07-06) مجلدًا بالاسم نفسه. أُعيد إنتاج ذلك في venv: يتغلب المجلد على الوحدة، ويختفي `translate.main`، وتتعطل نقطة الدخول عند `AttributeError` — ومع ذلك يجيب `pip check` بالرسالة «لم يُعثر على متطلبات معطلة» مع rc=0. كان مجرد وجود `pip install translate` لدى المستخدم كافيًا لتعطيل CLI من دون تشخيص قابل للاستفادة. وفي اختبار معاكس على wheel الفعلي: ثُبّت `pip install translate` فوق الحزمة، وظل `aipmt --help` يعيد rc=0 قبل ذلك وبعده، وتعايشت واجهتا CLI.
    - **اسم توزيع طويل وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للعثور عليها عبر بحث PyPI؛ أما الاختصار وحده فلن يعثر عليه من لا يعرف المشروع مسبقًا، في حين أن هدف النشر تحديدًا هو أن يُكتشف. استُبعد مرشحان معقولان بعد التحقق: ‏`ai-markdown-translator` محجوز على npm منذ 2024 لأداة تؤدي الغرض نفسه، وتسبق هذا المستودع بـ17 شهرًا؛ ويفصل حرف واحد فقط بين `aimt` و`aim` ‏(v3.29.1)، وهي حزمة نشطة في المجال نفسه — أسوأ وضع لالتباس دائم. وهناك أيضًا فخ منهجي: يعيد `pypi.org/project/<nom>/` الحالة 200 لأي اسم كان بسبب صفحة مكافحة الروبوتات، ولا يُعتد إلا بـJSON API.
    - **اعتماد layout ‏`src/` بدلًا من حزمة مسطحة.** كانت الحزمة المسطحة ستحافظ على عمليات `sys.path.insert(..., "..")` الست في الاختبارات، وهذا هو العيب تحديدًا: فهي تجعل الاختبارات تستورد شجرة المصدر بدلًا من الحزمة، ما سيخفي أي خطأ في التحزيم. التكلفة الفعلية: قاعدة استبدال إضافية واحدة.

  - **يمكن أخيرًا ضبط المفاتيح مرة واحدة وإلى الأبد.** لم تكن لدى CLI المثبتة أي إعدادات دائمة: لم يكن متاحًا سوى متغير البيئة وملف `.env` في المجلد الحالي. صحيح أن `find_dotenv` يصعد حتى جذر النظام، ولذلك كان يجد `~/.env` **عند العمل داخل المجلد الشخصي**، لكنه لا يجد شيئًا عند العمل في مكان آخر — أي تغطية تعتمد على الموقع الذي يُشغّل منه الأمر، لا على خيار تصميمي. لذلك أُضيفت طبقة ثالثة: ‏`~/.config/aipmt/.env`، بعد الطبقتين القائمتين.

    - **الأولوية غير مشفرة صراحةً**، بل تنتج من `override=False`، وهي القيمة الافتراضية لـ`load_dotenv`: لا تملأ كل طبقة إلا ما تركته السابقة فارغًا. ومن هنا يأتي الترتيب: متغير البيئة ← ملف `.env` الخاص بالمشروع ← إعدادات المستخدم، وقد جرى التحقق منه باختبارات سلوكية لا بنيوية — فعكس ترتيب الاستدعاءين يجعلها تفشل، وكذلك حذف الطبقة الثالثة.
    - **اختير تنسيق `.env` لا TOML** عمدًا: ‏`python-dotenv` تبعية موجودة أصلًا، وصيغته موثقة بالفعل في ملفات README الخمسة عشر، ويُستخدم الملف نفسه للنطاقين. لا تبعية جديدة ولا صيغة جديدة. يتبع الموقع `XDG_CONFIG_HOME` إذا كان **مطلقًا** — إذ تنص المواصفة على تجاهل القيمة النسبية، وإلا عاد موقع الإعدادات تابعًا للمجلد الحالي — ويتبع `APPDATA` على Windows.
    - **استُبعد خياران، ولكل منهما سببه.** مخزن مفاتيح النظام (`keyring`) أكثر أمانًا على حاسوب مكتبي، لكنه يفشل في البيئات headless — الخوادم والحاويات وCI — أي في حالة الاستخدام نفسها الخاصة بالترجمة المجمعة؛ هو مرشح جيد كخيار opt-in، لكنه إعداد افتراضي سيئ. أما flag ‏`--api-key` فسيضع المفتاح في سجل shell ويجعله ظاهرًا في `ps`.
    - **عند غياب المفتاح، لم يعد يظهر تتبع للاستدعاءات.** كان المستخدم يتلقى stack trace من Python يشير إلى `site-packages`، ورسالة تذكر «البيئة أو .env» من دون توضيح مكان إنشاء الثاني. أصبحت الرسالة الآن تسرد المواقع الثلاثة بمساراتها الدقيقة، وينتهي الأمر برمز 2. شبكة الأمان **ضيقة عن قصد**: ‏`except ValueError` حول مرحلة الإعداد وحدها. فتغليف التنفيذ كله سيحوّل خطأً حقيقيًا وقع أثناء الترجمة إلى رسالة مطمئنة — وهي حالة الفشل التي يتعقبها هذا المستودع. يقرأ اختبار مصدر `main()` لمنع ذلك.

  - **إصلاح — كان ملف `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** لا يصعد `load_dotenv()` من المجلد الحالي عند استدعائه بلا وسيطة، بل من الملف المستدعي، أي من `site-packages`. وقد قيس ذلك باستخدام نقطة دخول console حقيقية شُغّلت من مشروع يملك ملف `.env` خاصًا به: يعيد `find_dotenv()` القيمة `''` ولا يُحمّل المفتاح، بينما يعثر عليه `find_dotenv(usecwd=True)`. لم يكن الخطأ موجودًا ما دامت الأداة لا تعمل إلا من المستودع المستنسخ؛ لكن النشر كان سيجعله منهجيًا، مع عرض وحيد يتمثل في الإبلاغ عن مفتاح API «مفقود» رغم صحة الإعدادات.

  - **كانت ثلاثة gates ستصبح خضراء بعد أن توقفت عن التحقق من أي شيء.** جرى تشديدها قبل النقل عمدًا: فحاجز الحماية المكتوب بعد التغيير الذي يُفترض أن يلتقطه لا يثبت شيئًا. كل منها أخضر على المستودع الأصلي ويتحول إلى الأحمر على نسخة منقولة — وقد قيس الاتجاهان.

    - **يتجاهل Lizard المسار الغائب من دون أي تنبيه**: ‏rc=0، و«تم تحليل 0 ملف». كان gate التعقيد سينخفض من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع مخرجات حجمها صفر بايت. أصبح النطاق الآن مصفوفة يُتحقق من وجود كل عنصر فيها.
    - **لا يفشل `coverage run --source=` عند استهداف وحدة غائبة**: مجرد تحذير على stderr، وrc=0 لكل من unittest و`coverage xml`، ثم يُنشر التقرير رغم ذلك — بعد انخفاضه من 1453 إلى 141 statements. كان المشروع سيبدو سليمًا لأنه لم يعد يُحلل إلا بالكاد. يحمي التقرير حدّان أدنيان: الإجمالي، وأكبر ملف جرى قياسه.
    - **مسبار حداثة الترجمات أعمى بنيويًا عن صيغة الاستدعاء**: فهو يرتكز إلى flags الخاصة بـargparse، وهي تحديدًا ما لا يغيره تغيير اسم الملف. وقد أُعيد إنتاج الحالة: نُقلت الوحدة، وظلت ملفات README الخمسة عشر توثق أمرًا لم يعد موجودًا، وكان الحكم «لا توجد ترجمة قديمة». لذلك يتحقق قسم سابع من الصيغة لا من الخيارات، وتُقارن وصلة Lizard بالنطاق الفعلي للسكربت — فمفتاحها `files:` لا يجعل pre-commit يفشل عندما لا يعود متطابقًا، بل يجعله يتخطى الفحص.

  - **لم يعد `requires-python = ">=3.10"` مجرد ادعاء.** كان `sonar-project.properties` يعلن دعم 3.10-3.12 من دون أن يُختبر أي منها قط، إذ لم يكن جهاز التطوير يملك سوى 3.12 — وهو تناقض داخلي كان النشر سيجعله علنيًا. أصبح workflow للاختبارات يشغّل المجموعة على 3.10 و3.11 و3.12، مع تثبيت الحزمة، وبالتالي اختبار حدودها المعلنة.

  - **حدود دنيا بلا سقف.** يظل `requirements.txt` ملف lock المختبر، ويصبح `[project.dependencies]` العقد العام: فنشر إصدارات lock الدقيقة سيسبب تعارضات لكل مستخدم لديه حزم أخرى. ولا يوجد سقف `<N+1` أيضًا — إذ سيتعارض مباشرةً مع `check-deps-fresh.sh` الذي يجعل gate الإصدار يفشل عند أي تأخر في إصدار رئيسي. تُحل مجموعة الحدود الدنيا بنجاح، ويخرج الاختبار المعاكس `openai==1.0.0` بالقيمة `ResolutionImpossible`، ما يثبت أن الفحص يميز الحالات بدلًا من قبول كل شيء. ويمنع حاجز أيضًا إصدار `pyproject.toml` من الاختلاف عن إصدار CHANGELOG: لا يسمح PyPI بإعادة استخدام رقم إصدار.

  - **جرى التحقق من البداية إلى النهاية داخل venv جديد**: ‏wheel بحجم يقارب 70 كيلوبايت لا يحتوي إلا على `aipmt/*.py` وdist-info والترخيص؛ ‏`aipmt --help` يعيد rc=0 مع 22 flag؛ ويعرض `python -m aipmt` العبارة «usage: aipmt» لا «usage: \_\_main\_\_.py»؛ ويعمل تثبيت `pipx`؛ والأهم **تنفيذ ترجمة حقيقية من الفرنسية إلى الإنجليزية من أي مجلد مستخدم**، مع الحفاظ على الخط العريض والقائمة والكود المضمن والرابط وURL، وعدم ترجمة كتلة الكود. تمر الاختبارات الـ318 السابقة للنقل مع قائمة معرفات متطابقة بايتًا ببايت قبل النقل وبعده — وهذا ما يثبت أن أي اختبار لم يُعطّل، لا عبارة «OK»؛ وأضيف إليها اثنا عشر اختبارًا للإعدادات ذات الطبقات الثلاث، ليصبح الإجمالي 330.

- **1.10.0** موفر `--use_codex` ‏(حصة اشتراك ChatGPT)، وتحديث SDKs والنماذج، وإصلاح اقتباسات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — حاجزا حماية وضعهما PR من دون تطبيقهما في كل المواضع**:

    - **كان الفحص المسبق لـCodex يمرر `.env` كله إلى الملف التنفيذي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: كانت العملية الفرعية ترث `os.environ` كاملًا، وبالتالي كامل `.env` الذي حمّله `load_dotenv`. عند القياس بملف تنفيذي زائف مزود بأدوات رصد، وصلت **سبعة أسرار** إلى الفحص المسبق — مفاتيح الموفرين الستة إضافةً إلى `GITHUB_TOKEN` — مقابل **صفر** لدى نظيره `_grok_preflight`، الذي كان يمرر `env=_grok_env()` كما ينبغي. كان التناقض داخل PR نفسه: فـ`_strip_secret_env` موجود تحديدًا للحفاظ على هذا الثابت، على بُعد بضعة أسطر فقط. استُخرج `_codex_env_base()` وأصبح مشتركًا بين المسارين؛ والقياس بعد الإصلاح: صفر أسرار في الجانبين.
    - **خاصية «`--deny` يغلق عند الفشل» لم تشمل الصيغة المستخدمة.** كان التعليق يبرر كامل احتواء Grok بأن قاعدة ذات بادئة مجهولة تمنع بدء التشغيل. وعند القياس على `grok 1.0.13`، اتضح أن هذا التحقق لا يوجد **إلا للصيغة بين قوسين**: يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل برسالة «بادئة أداة مجهولة»، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. لكن `GROK_DENY_RULES` لم يستخدم سوى أسماء مجردة — ولذلك كان تغيير اسم أداة لدى xAI سيزيل طبقة الاحتواء الوحيدة المقاسة من دون أي إشارة، على جهاز لا يطبَّق فيه أصلًا sandbox الخاص بنظام التشغيل. انتقلت القواعد الثماني المسماة إلى `Prefix(*)`، مع التحقق من كل واحدة بوصفها بادئة معروفة لدى CLI؛ ويظل catch-all ‏`*` بصيغته الحرفية، وهي الصيغة الوحيدة المقبولة. يمنع اختبار العودة إلى الصيغة غير المتحقق منها.
    - **وبقية الجوانب نظيفة بعد التحقق**: لا يوجد حقن أوامر، إذ تُستخدم صيغة القائمة في كل موضع ولا يُستخدم `shell=True` أبدًا، ويمر محتوى المستند عبر stdin أو `--prompt-file`؛ ولا توجد إزالة تسلسل غير آمنة، إذ لا يُستخدم سوى `json.loads` مع حارس للنوع؛ ولم يُعثر على تجاوز لإصلاح اجتياز المسار باستخدام سبع حمولات؛ ويطبق CLI ‏`--deny '*'` فعليًا، إذ لوحظ `DENY_ENFORCED` عند محاولة قراءة خارج workdir.
    - كما كان فحص الحداثة المضاف أعلاه يلتف على مبدئه الخاص: فإذا فشل طلب PyPI لحزمة، كان يتخطاها بصمت ويبقى gate أخضر. أصبح الآن يحصي الحزم التي جرت مقارنتها فعليًا ويفشل إن كانت التغطية ناقصة.

  - **حُدّثت التبعيات، وأضيفت شبكتا أمان لمنع تكرار التأخر**:

    - **كان التأخر حقيقيًا ومزمنًا**: ‏`openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر عامين في مخزن الشهادات الجذرية الذي يتحقق من TLS لكل استدعاءات الموفرين. جرى تحديد السبب: **لم يكن `.github/dependabot.yml` موجودًا**. من دون هذا الملف، لا يفعّل GitHub إلا _security updates_، ولا يقترح Dependabot ‏PR إلا لتبعية تستهدفها CVE — ما يفسر تحديثه `urllib3` و`idna` مع ترك SDKين يتأخران بإصدار رئيسي كامل.
    - **يتعايش الإصداران الرئيسيان من دون تعارض**، خلافًا لما أوحى به استدلال سابق: ينتقل `openai` ‏3.x و`anthropic` ‏1.x إلى **`httpx2`**، بينما يظل `mistralai` و`google-genai` على `httpx<1`، لكنها توزيعات منفصلة. جرى التحقق بتثبيت فعلي، ثم عبر **مسارات الموفرين السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الكود المضمن والروابط في كل مخرج. كان «تجنب حزمتي HTTP» تفضيلًا لا عائقًا: وحسمه القياس.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: كانت `google-auth` و`cryptography` وحزمة `opentelemetry` مثبتة في venv العمل من دون التصريح بها قط — ولذلك لم يكن التثبيت الجديد يعيد إنتاج ما يجري اختباره. وفي المقابل، كانت `tokenizers` و`huggingface-hub` و`PyYAML` مدرجة فيه من دون أن يستوردها أو يحتاج إليها أي شيء، وهي بقايا من `mistralai` ‏1.x. أُعيد توليد الملف بوصفه الإغلاق الكامل لـvenv مبني فقط من التبعيات المباشرة. لا يعثر `pip-audit` على أي ثغرة معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** ‏(جديد) يفعّل تحديثات الإصدارات الأسبوعية لـpip وgithub-actions. جُمعت الإصدارات الصغرى والتصحيحات في PR واحد — فاقتراح PR لكل تحديث patch ينتهي بالتجاهل، والضجيج عدو التحديث؛ أما **الإصدارات الرئيسية فمنفصلة**، ويتطلب كل منها تحققًا عبر استدعاء حقيقي.
    - **`scripts/check-deps-fresh.sh`** ‏(جديد وموصول بالـgate) يجعل التأخر ظاهرًا في حكم المشروع: يقترح Dependabot التحديثات لكنه لا يضمنها، وقد تتراكم PRs الخاصة به. تأخر إصدار رئيسي ← فشل؛ إصدار ثانوي ← تحذير، لأن gate الذي يبقى أحمر دائمًا ينتهي إلى التجاهل؛ تعذر الوصول إلى PyPI ← تخطٍّ صريح محليًا و**إغلاق عند الفشل في CI**، لأن الفحص الذي لم يُنفذ ليس نجاحًا. جرى التحقق في الاتجاهين: يلتقط الحالة الدقيقة السابقة للإصلاح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير عند وجود إصدار ثانوي.

  - **إصلاحات ناتجة من مراجعة هذا PR** — فحص خمسة وكلاء مراجعة الـdiff بدقة؛ وقد **أُعيد إنتاج جميع النقاط أدناه بالقياس** قبل إصلاحها، وكان اثنان منها انحدارين أُدخلا أعلاه ضمن الإصدار نفسه.
    - **إصلاح تراجع — كان `_NEWS_CITATION_REGEX` يعاني من تراجع خلفي أُسّي.** كان إصلاح الفقرات المتعددة قد أدخل `(?:[ \t]*$|[ \t]+.*)` في التكرار: تقاسم المسافات بين `[ \t]+` و`.*` ملتبس، ويتضاعف هذا الالتباس من تكرار إلى آخر. جرى القياس على أسطر `>   texte` — وهي إزاحة بادئة Markdown سليمة تمامًا — لا تطابق النمط: **2,589 مللي ثانية لـ14 سطرًا** مقابل 0.04 مللي ثانية بعد التصحيح، بمعامل يقارب 9 لكل سطر مضاف. في وضع `--news`، كان اقتباس كتلي طويل وغير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب يمكن تحديده. يستهلك التكرار الآن السطر كاملًا دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يترك سوى طريقة واحدة للمطابقة في كل تكرار. جرى التحقق على المتن الفعلي المؤلف من 231 مقالة: **صفر اختلافات** في الالتقاط، ونفس الاقتباسات الـ423، مع بقاء أجسام الاقتباسات الـ14 متعددة الفقرات موسّعة.
    - **كان تفعيل علامتي provider في الوقت نفسه يؤدي إلى فوترة حسب الاستخدام بصمت.** كان `--use_codex --use_mistral` مقبولًا؛ يختبر `_select_provider_client` ‏Mistral أولًا، ويمنح `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. وهكذا كان المستخدم يطلب استخدام حصة اشتراكه ويحصل بدلًا منها على فوترة حسب الاستخدام، من دون أي تحذير: وهذا بالضبط نمط الإخفاق الذي وُجد `--use_codex` لمنعه. تمر علامات provider الست الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: سطر أوامر يجمع بين provider اثنين، وكان مقبولًا بصمت حتى الآن، يفشل الآن عند `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة إتمام العمل تتحول إلى الأخضر عند تعطل مجسّاتها.** كانت أربعة من فحوص `scripts/check-release-ready.sh` الثلاثة عشر تتبع نمط «التقاط stdout، والاستنتاج إن كان فارغًا» من دون التحقق من رمز الإرجاع مطلقًا: كان استثناء ما (ملف أُعيدت تسميته، `FileNotFoundError`) يكتب إلى stderr ويترك stdout فارغًا، فيستنتج الفحص أنه «لا شيء يستدعي الإبلاغ». وهكذا تكرر داخل السكربت المكتوب لمنعه فخ «لا يثبت `exit 0` شيئًا». يفرض المساعد `probe()` الآن رمز إرجاع صفريًا **و**علامة نهاية، وترفض المجسّات الاستنتاج انطلاقًا من مجموعة معالم فارغة — لأن التوكيد على مجموعة فارغة يكون صحيحًا دائمًا. للتوضيح: أدت إضافة المجموعة الحصرية أعلاه إلى تمرير علامات provider عبر كائن `*_group` لم يعد التعبير النمطي القديم `parser\.add_argument\(` يطابقه؛ فخرجت **ست علامات من أصل إحدى وعشرين** من النطاق بصمت، بينما ظلت البوابة خضراء.
    - **كان فحص الأسرار يفوّت أربعة providers من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: كان `sk-proj-…` (تنسيق OpenAI الحالي) و`sk-ant-api03-…` ينقطعان عند الشرطة الثانية، ولم يكن `AIza…` مشمولًا. وُسّع النمط، واستُبعد `.secrets.baseline` من الفحص. وكانت الحماية `.env` تستعلم أيضًا من `git diff --cached`، الذي لا يرى سوى الفهرس: لذلك لم يكن `.env` **المُضمَّن سابقًا في commit** — وهي أسوأ حالة — يظهر فيه مطلقًا. وهي تستعلم الآن من `git ls-files`.
    - **لم يكن «الإحماء المسبق للرمز» في Codex إحماءً فعليًا.** أظهر القياس أن `codex login status` لا يلمس `~/.codex/auth.json` (وقت التعديل والحجم لا يتغيران)، وتقول مساعدته «عرض حالة تسجيل الدخول». ومع ذلك كان التعليق يدّعي تحديث الرمز «مرة واحدة، على نحو تسلسلي»، وبالتالي تحييد خطر التحديثات المتزامنة لرمز دوّار أحادي الاستخدام. لم تكن الحماية المعلنة موجودة؛ ويصف التعليق الآن ما يفعله الكود فعلًا، بينما يظل الإجراء الوقائي الحقيقي هو `max_jobs=4`. كما يحترم الفحص `CODEX_BIN` الآن، بعدما كان يتجاهله — إذ كانت محطة عمل لا تحتوي على `codex` ضمن `PATH` تفشل برسالة «غير مصادق عليه»، وهو تشخيص مضلل.
    - **كان `.env` يُحمَّل في shell فرعي.** تُستدعى `detect_provider` عبر استبدال الأوامر، ولذلك لم تكن قيمها المصدّرة تنتقل إلى الأعلى: كان أي `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` معرّف في `.env` يظل غير مرئي للقراءات المنفذة في `main()`، التي كانت تستنتج «تعذر العثور على الملف التنفيذي لـGrok» رغم صحة الإعداد.
    - **كان التزامن يتجاوز الحد المعلن بنسبة 50%.** وُضعت الحماية بعد تشغيل زوج README/CHANGELOG: وبلغت الذروة المقاسة **3 عند `max_jobs=2`**. وبالنسبة إلى Grok، الذي يتشارك حصته الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يكن السكربت يلتزم إذًا بالحد الذي فرضه على نفسه. أما العد النهائي، فكان يُعرض من دون مقارنته مطلقًا بالعدد 28 — ولذلك كان غياب ملف يمر من دون ملاحظة.
    - **عقد مخرجات Grok: أصبح غياب `stopReason` إخفاقًا.** كان الكود يطبق «`end_turn` **أو غائب**» حيث يشترط العقد المعلن `end_turn`. وكانت حمولة بلا هذا الحقل — أو أُعيدت تسمية حقلها إثر تحديث CLI — تحول الحماية بصمت إلى عملية بلا أثر. إضافة إلى ذلك، لم يعد `max_turn_requests` مصنفًا على أنه rate limit (فالمستنفد هو ميزانية الدورات: تعيد المحاولة النتيجة نفسها مقابل انتظار 90 ثانية)، وأُزيل `quota` من علامات rate limit — للسبب الذي كان docstring الخاص بـ`_codex_is_rate_limited` يذكره أصلًا من دون أن يطبقه Grok.
    - **أصبحت سلسلة Gemini محفوظة مؤقتًا حسب النموذج.** كانت تبدأ مجددًا من `minimal` عند كل مقطع، مع أن النموذج الافتراضي يرفضه: فكان المسار الاعتيادي يدفع كلفة رحلة 400 ذهابًا وإيابًا لكل مقطع ويعيد طباعة التحذير نفسه. التحذير الذي يتكرر مئات المرات يكف الناس عن قراءته — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مكتوبة صراحةً لـCodex وتوجّه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق الأساس الخاص بالعمليات الفرعية يعمم «shim» على أداتي CLI مع أن ملف Grok التنفيذي هو ELF أصلي (والتبرير الصحيح هو «وكيل ينشئ عملياته الفرعية الخاصة»)؛ وُسمت اثنتا عشرة ملاحظة SAST تخص `subprocess` بـ`# nosec` / `# nosemgrep` مع ذكر المبرر، إذ تجعل صيغة القائمة من دون `shell=True` الحقن مستحيلًا، كما أن محتوى المستند لا يمر مطلقًا عبر argv.
    - **لم يعد أي سر يدخل إلى العمليات الفرعية الوكيلة.** لم تكن قوائم المنع الاسمية تحمي سوى ثابت **الفوترة** (Codex بلا `OPENAI_API_KEY`، وGrok بلا `XAI_API_KEY`). أظهر القياس أن **سبعة أسرار أخرى** كانت مع ذلك تدخل إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح أداة CLI الأخرى، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة البيانات. لكن أداتي CLI هاتين **وكيلان**، ويعمل وكيل Grok من دون sandbox لنظام التشغيل قابلة للتطبيق على كثير من محطات Linux. لذلك يجري الترشيح الآن **بحسب نمط الاسم** (`API_KEY`، `_TOKEN`، `SECRET`، `PASSWORD`، `CREDENTIALS`) لا وفق قائمة اسمية، ما يشمل أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن يعرفها هذا الكود. ولا تحتاج أداتا CLI إلى أي منها: تعيش المصادقة في `~/.codex` و`~/.grok`، ولا تعيش مطلقًا في البيئة — وقد جرى التحقق عبر **ترجمة فعلية مكتملة** باستخدام كل من provider الاثنين مع البيئة المحصّنة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يثبّت حصرية علامات provider، وعقد `stopReason`، وخطية التعبير النمطي للأخبار، ورسالة الرفض في CI، والحفظ المؤقت لـGemini، وغياب أي سر من بيئة العمليات الفرعية. وهذا التوكيد الأخير **عام** — يفشل مع مفتاح لا تسميه أي قائمة — بينما كان اختبار التنقية الحالي مجرد مرآة لثابته ولم يكن قادرًا على اكتشاف أي شيء سوى تعطل حلقته الخاصة. أصبحت الحزمة الكاملة تضم **311 اختبارًا**.

  - **provider جديدان لـGrok**: ‏`--use_grok` (واجهة xAI API، المفتاح `XAI_API_KEY`، بفوترة حسب الاستخدام) و`--use_grok_cli` (أداة Grok Build CLI الرسمية، تُحتسب من اشتراك Grok — وفق المبدأ نفسه الذي يتبعه `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن endpoint الخاص بـxAI متوافق مع OpenAI، يُعاد استخدام العميل و`_call_openai` كما هما، ولا يتغير سوى `base_url`. لم يلزم سوى تكييف واحد يعود بالنفع على الجميع: يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي يصدرها xAI حيث يصدر OpenAI ‏`stop`. النماذج: `grok-4.6` (الجودة) و`grok-4.3` (الاقتصادي). وتجدر الإشارة إلى أن خيار Grok الاقتصادي يظل الأغلى في المستودع — ‏$1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ`mistral-small-latest`: يُختار هذا provider لتنوع النماذج، لا للسعر.
    - **وضع CLI**: مبني على نمط Codex، مع أربعة اختلافات فرضها الواقع — يُمرر prompt عبر ملف (`--prompt-file`، لأن CLI لا يقرأ stdin ولأن مقطعًا في argv سيكون ظاهرًا في `ps`)، والمخرج كائن JSON واحد على stdout (لا JSONL ولا ملف `-o`)، ولا يتيح الاشتراك سوى `grok-4.6` و`grok-4.5`، ولا يمكن تطبيق sandbox (انظر أدناه). جرى توحيد تشغيل العمليات الفرعية مع Codex في `_codex_run_process`، من دون المساس ببقية provider الخاص بـCodex الذي خضع للاختبار بالفعل.
    - **لا يثبت `exit 0` شيئًا، وفق القياس**: عندما لا يكون المستخدم مصادقًا عليه، تكتب CLI ‏`{"type":"error","message":"Not signed in."}` إلى **stdout** مع رمز إرجاع **0**. ويتصرف الرفض أو تجاوز عدد الدورات بالطريقة نفسها. لذلك يشترط عقد المخرجات أربعة شروط متزامنة: رمز إرجاع 0، وغياب حمولة خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع الفحص المسبق المنطق نفسه: يخرج `grok models` بالرمز 0 حتى عند قطع الاتصال، ولا يمكن الاستنتاج إلا من وجود «غير مصادق عليه» في stdout.
    - **الاحتواء: لا تماثل مقصود وموثّق.** بينما يعمل Codex داخل `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـGrok على كثير من محطات Linux الحديثة، لسببين نظاميين مستقلين لا يمكن تجاوزهما من دون `sudo`: يمنع AppArmor مساحات أسماء المستخدمين غير المميّزين منذ Ubuntu 24.04 ‏(`bwrap: setting up uid map: Permission denied`، وقد أُعيد إنتاج المشكلة خارج Grok)، وتفشل قائمة منع مقابس وقت تشغيل الحاويات عندما يكون `/run/podman` في `0700` (لا يعالج resolver سوى `ErrorKind::NotFound`، ويصبح EACCES خطأً قاتلًا). الفخ الأساسي: ملف تعريف **مدمج** يتعذر تطبيقه يؤدي إلى التشغيل **من دون احتواء وبصمت**. لذلك لا يطلب السكربت أي ملف تعريف افتراضيًا ولا يتراجع بصمت أبدًا — بل يحذر عبر stderr. وتعتمد الحماية على قواعد `--deny` في CLI، بما فيها قاعدة catch-all ‏`*`، وهي الطبقة الوحيدة التي ثبت بالقياس أنها _fail-closed_ (تؤدي قاعدة ذات بادئة مجهولة إلى رفض التشغيل). يتيح `GROK_TRANSLATE_SANDBOX=read-only` اشتراط ذلك، وعندها يفشل التشغيل إن تعذر على الجهاز الوفاء به.
    - **ضوابط الحماية**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (إذ يؤدي وجود مفتاح إلى التحول إلى الفوترة حسب الاستخدام؛ ويفرض `GROK_SANDBOX` موروث ملف تعريف غير قابل للتطبيق مع رسالة مضللة)، وتُعطل مفاتيح MCP/hooks/skills/agents، مع `--disable-web-search` و`--no-subagents` و`--no-plan` ومجلد عمل مؤقت ورفض في CI ومهلة تقتل مجموعة العمليات وback-off عند rate limit. ضُبط `--max-turns` على 6 لا على 1: فالعداد يزداد بعد دورة الأدوات، وكانت القيمة 1 ستقتطع المخرجات.
    - **الحصة**: مخزون Grok أسبوعي و**مشترك مع Chat وImagine وVoice**، ولا يعرضه أي أمر — بخلاف Codex الذي يتيح `account/rateLimits/read` تقدير الاستهلاك. لذلك يحد `regen_translations.sh` التزامن إلى 2 ويحذر من ذلك صراحةً.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` (24 اختبارًا). أصبحت الحزمة الكاملة تضم **290 اختبارًا**.
  - **إصلاح خطأ — لم تكن اقتباسات EN متعددة الفقرات محمية إلا جزئيًا (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل، بوصفه جسم الاقتباس، سوى سلسلة من أسطر `>` **المتتالية**. وما إن يمتد اقتباس عبر عدة فقرات (يفصل بينها سطر `>` فارغ)، حتى كان يُلتقط آخرها فقط ويُستبدل بالعنصر النائب؛ أما ما سبقه فكان يُرسل إلى LLM ويعود مترجمًا — وهو النقيض التام لما وُجد `--news` لضمانه. يقبل التكرار الآن أسطر `>` الداخلية الفارغة وأصبح غير جشع، ليتوقف عند سطر `>` الفارغ السابق للسطر المائل، لا عند أول سطر يصادفه.
    - **النطاق المقاس** على متن فعلي من 198 مقالة: تأثر 11 اقتباسًا من أصل 419. لا تراجع — يلتقط التعبير النمطي الجديد العدد نفسه تمامًا من الاقتباسات، ولا تتسع سوى الأجسام متعددة الفقرات (408 أجسام متطابقة، و11 جسمًا موسعًا)، ولا يزال من المتعذر ابتلاع سطر الإسناد `> — …` ضمن الجسم (أُبقي lookahead).
    - **إثبات شامل** على مقالة حجمها 69 كيلوبايت تُرجمت إلى ja/ar: تظل الفقرة الأولى من اقتباس، التي كانت سابقًا تظهر `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم بالمثل إلى العربية، على حالها الآن بوصفها `> GLM-5.3 is now open-weight.`. ويعود عدد أسطر الاقتباس بالإنجليزية من 9 إلى 10، وهو عددها في المصدر.
    - تجدر الإشارة إلى أن أدوات التحقق اللاحقة لم تكن تكتشف هذا العيب، إذ تتحقق من وجود الاقتباس من دون التأكد من اكتماله.
  - **توفير مقاس على provider الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ النموذج بـ`gpt-5`، بما في ذلك وضع `--eco`. قياس باستخدام `gpt-5.4-mini` لترجمة جملة من عشر كلمات: `medium` ← 45 reasoning tokens و65 output tokens؛ ‏`none` ← 0 و14. لا يضيف الاستدلال شيئًا إلى الترجمة، ومع ذلك كانت كلفته تُدفع عن كل مقطع من كل ملف. تصبح القيمة الافتراضية `none` في `--eco` وتظل `medium` في غير ذلك؛ وتبقى للقيمة الصريحة الممررة عبر CLI الأولوية. يقبل `--reasoning_effort` الآن `none` و`xhigh` بالإضافة إلى `low`/`medium`/`high` (ليست جميعها مقبولة لدى كل النماذج: فمثلًا يرفض `gpt-5.4-mini` القيمة `minimal` — وتغطي إعادة المحاولة الحالية بلا معامل هذه الحالة).
  - **تحديث حزم SDK وترحيل Gemini**: استُبدل `google-generativeai` (انتهى دعمه في 2025-11-30 وأُرشف مستودعه) بحزمة SDK الموحدة **`google-genai`** — ‏`genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير prompt النظام عبر `system_instruction` بدلًا من ضمّه إلى المقطع. رُقّي `mistralai` إلى **2.9.4** (وأصبح الاستيراد `from mistralai.client import Mistral`؛ أما القديم فيطلق `ImportError`، كما جرى التحقق داخل wheel)، و`anthropic` إلى **0.125.0** و`openai` إلى **2.54.0** — وهي أحدث الإصدارات قبل الانتقال إلى `httpx2`، لتجنب تشغيل حزمتين من HTTP معًا داخل venv. وبناءً على ذلك أُزيل تثبيت `httpx` عند 0.28.1 و`pydantic` عند 2.13.5.
  - **تراجعان التقطتهما اختبارات فعلية، لا الوثائق**:
    - يرفض `anthropic` بالإصدار ≥ 1.0 من جهة العميل استدعاءً غير متدفق يوحي `max_tokens` فيه بمدة تتجاوز 10 دقائق (`ValueError: Streaming is required...`). لم يكن هذا الضابط موجودًا في 0.34.2 وكان يعطل كل استدعاء لـClaude باستخدام `max_tokens=32768`. صُحح ذلك عبر `timeout` صريح (`CLAUDE_TIMEOUT`، والقيمة الافتراضية 900 ثانية)، ما يجنب التحول إلى streaming لاستدعاء لا تُستخدم منه سوى الاستجابة الكاملة.
    - لا يقبل `thinking_level="minimal"` سوى جزء من كتالوج Gemini: يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` بخطأ 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تسلسل `minimal` ← `low` ← بلا thinking_config، على غرار fallback الخاص بـOpenAI الموجود أصلًا — إذ ينبغي ألا يؤدي معامل تحسين مطلقًا إلى إفشال الترجمة.
  - **تجديد النماذج الافتراضية**، وقد جرى التحقق من كل منها باستدعاء فعلي: OpenAI ‏`gpt-5.5` ← **`gpt-5.6-terra`** (−60% على دفعة من 28) و`gpt-5.4-mini` ← **`gpt-5.6-luna`** (−73%)؛ وClaude ‏`claude-sonnet-4-6` ← **`claude-sonnet-5`** (أرخص وأحدث) و`claude-haiku-4-5-20251001` ← **`claude-haiku-4-5`** (معرّف قياسي بلا تاريخ)؛ وGemini ‏`gemini-3.1-pro-preview` ← **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` ← **`gemini-3.1-flash-lite`** (إصدار مستقر، وأرخص من `3.5-flash-lite`).
 Mistral دون تغيير، ويظل `mistral-large-latest` صاحب أفضل نسبة جودة/سعر بين النماذج الأربعة. تجدر الإشارة إلى أنه لا يوجد أي نموذج Gemini أحدث من فئة Pro بعد `gemini-3.1-pro-preview` — أما Gemini 3.5 Pro، الذي أُعلن عنه في مايو 2026، فلم يصدر قط؛ وسلسلة 3.5/3.6/3.7 مخصّصة حصريًا لفئة Flash.
  - **قياس A/B قبل تحويل Gemini**: تُرجم `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. بنية متطابقة تمامًا (21 قائمة، و18 كتلة برمجية، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، خلال **8 ثوانٍ مقابل 48 ثانية**. ونظرًا إلى عدم وجود أي benchmark عام يقارن بين هذين النموذجين في الترجمة أو النصوص المكتوبة بأبجديات غير لاتينية، لكان التحويل قائمًا على مجرد افتراض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفّذ `block.text for block in response.content` من دون تصفية النوع. تُدرج النماذج ذات الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، تعرض `.thinking` بدلًا من `.text` — وكان من شأن الترجمة أن تتعطل عند أول مقطع بسبب `AttributeError` مبهم. تُستبعد الآن كتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` (باستخدام قائمة سلبية، للحفاظ على تقبّل نوع غير معروف يحتوي على نص)، وتؤدي الاستجابة الخالية تمامًا من أي كتلة نصية إلى خطأ صريح. ويُمرَّر `thinking={"type": "disabled"}` مع كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: إزالة النماذج التي انقضى تاريخ سحبها (فئة `magistral-*` المسحوبة في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620`، و`claude-3-7-sonnet-20250219`، و`claude-opus-4-1-20250805`، و`claude-sonnet-4-20250514`). تصحيحات الحدود: Mistral من 128K إلى **256K** (جيل Large 3 / Small 4)، وGemini من 1 000 000 إلى **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` من 200K إلى **1M**، وعائلة `gpt-5.6-*` من 400K إلى **1,05M**. إضافة Claude 5 (`claude-sonnet-5`، و`claude-opus-5`، و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وفئة `ministral-*`. تجدر الإشارة إلى أن هذه الحدود تظل إرشادية، إذ يحدّ `translate()` التقسيم عند `min(16000, limite)`.

  - **الموفّر `--use_codex`**: موفّر خامس يشغّل CLI الرسمي لـCodex ‏(`codex exec`) في الوضع غير التفاعلي، بدلًا من استدعاء API تُحاسب بحسب الاستخدام. تُحتسب الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقًا. وهذا هو المسار الوحيد الذي توثقه OpenAI لهذا الاستخدام: فمصفوفة التوفّر بحسب الخطة تدرج «Codex SDK، و`codex exec`، وسير العمل القابل للبرمجة» على أنه متاح ضمن Plus/Pro/Business/Enterprise، بينما لا تصادق رموز `~/.codex/auth.json` على استدعاءات API Platform (ولا يقرأها هذا السكربت مطلقًا — إذ يظل CLI مسؤولًا عن المصادقة وتجديدها).
  - **إمكانية تثبيت ملف Codex التنفيذي عبر pip، وليس عبر npm فقط**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي داخل `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK ‏`openai-codex`). وبذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عام لاستخدام `--use_codex`. ولا تُضاف الحزمة إلى `requirements.txt`: إذ يبلغ حجم الملف التنفيذي نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين من أجل موفّر اختياري. جرى التحقق من البداية إلى النهاية: عند غياب `codex` عن `PATH`، تعثر عملية الحل على الملف التنفيذي المضمّن في الحزمة وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: تُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. فمن دون هذا الإجراء الوقائي، قد يؤدي وجود مفتاح في `.env` إلى تحويل Codex إلى الفوترة بحسب الاستخدام من دون أي مؤشر مرئي — وهو بالضبط ما صُمم هذا الموفّر لتجنبه.
  - **تثبيت سلوكيات CLI الخادعة عبر الاختبارات**:
    - يقرأ `codex exec` الإدخال القياسي **حتى** عند تمرير الموجّه كوسيط: ومن دون إغلاق الإدخال القياسي، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج مطلقًا (أُعيد إنتاجه: خروج 124 بعد 180 ثانية، بصفر بايت). لذا فإن `communicate(input=...)` إلزامي.
    - إن `codex` المثبّت بواسطة npm هو غلاف Node ينفّذ `spawn` لتشغيل ملف Rust التنفيذي الحقيقي: ويكون هذا الأخير **حفيدًا** لعملية Python ويبقى حيًا بعد `SIGKILL` الخاص بـ`subprocess.run(timeout=)`، حيث كان سيواصل استهلاك الحصة. ومن هنا استخدام `Popen(start_new_session=True)` + `os.killpg`.
    - قد يخرج CLI بالرمز 0 رغم إصداره `turn.failed`: لذلك يُفحص خرج JSONL ‏(`--json`) بالإضافة إلى رمز الإرجاع، ويؤدي غياب ملف `-o` مع رمز 0 إلى خطأ صريح بدلًا من إنتاج مقطع فارغ.
  - **Back-off عند بلوغ rate limit**: لا ينفّذ CLI أي retry داخلي (`max_retries = 0`). ويجري التصنيف اعتمادًا على بنية حمولة JSON ‏(`status: 429` / `error.type`)، لا على السلاسل الفرعية — إذ تظهر كلمة «الحصة» في كل من خطأ 429 قابل للاسترداد و`insufficient_quota` نهائي.
  - **حماية CI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفًا. فالمصادقة عبر الاشتراك غير مخصّصة لـrunner مشترك، كما تنصح OpenAI صراحةً بعدم استخدام سير العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` ‏(`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وAPI Platform، لكن حساب ChatGPT لا يحق له الوصول إليها كلها: تُطبَّق allowlist من جهة الخادم، من دون تحقق محلي، ويؤدي اختيار نموذج غير معتاد إلى تحذير. ضمن خطة Plus، يوفّر Luna ما بين 250 و2 000 رسالة لكل نافذة من 5 ساعات، مقابل 10 إلى 100 لـSol: ويُعد `--eco` الوضع الموصى به لأي معالجة دفعية.
  - **إصلاح خلل — كان `regen_translations.sh` يخرج بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` تابع لـ`main()` لم يعد موجودًا عند تنفيذ trap. وتحت `set -u`، كان ذلك يثير `failed_log: unbound variable` ويجعل السكربت يخرج بالرمز 1 رغم صحة الترجمات الـ28 — وهو ما كان سيوقف `release.sh --auto` ‏(`set -e`) بعد إعادة التوليد مباشرةً، عند أكثر المراحل كلفة. أصبح المتغير عامًا، وصار trap يتحقق من وجوده. أثر جانبي مفيد: عادت إخفاقات الترجمة الحقيقية، التي كان هذا الخطأ يخفيها سابقًا، إلى الظهور في الملخص النهائي.
  - **`REGEN_MODEL`**: متغير بيئة جديد لـ`regen_translations.sh` يفرض نموذجًا محددًا بدلًا من القيمة الافتراضية للموفّر، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج المتقدم ضمن حصة الاشتراك بدلًا من نموذج `--eco` الموجّه للحجم.
  - **`regen_translations.sh`**: يتوفر `REGEN_PROVIDER=codex` عبر اشتراك صريح فقط (ولا يُكتشف تلقائيًا أبدًا، تجنبًا لاستهلاك حصة الاشتراك من دون علم المستخدم). ويُجدَّد الرمز مرة واحدة تسلسليًا قبل بدء التوازي — لأن تجديد Codex دوّار وأحادي الاستخدام، ومن شأن المهام المتزامنة إبطال جلسة `codex login` — كما يُخفض التزامن إلى 4.
  - **إعادة الهيكلة المرتبطة**: ينتقل `_dispatch_provider_call` من 8 إلى 6 معاملات عبر `_resolve_provider()` يعيد اسم موفّر، بدلًا من قيمة منطقية رابعة تُمرَّر عبر السلسلة كلها. وتظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` بالحد الأدنى.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` (48 اختبارًا) يغطي argv، والبيئة المنقّحة، وعقد منع التمهيد، والإخفاقات الصامتة، وانتهاء المهلة/killpg، وback-off، وpreflight، وحل الموفّر، وتسلسل استدلال Gemini، وتصفية كتل Claude، واستشهادات الأخبار متعددة الفقرات. المجموعة الكاملة تضم 290 اختبارًا.
  - **التحقق الفعلي**: ينتج عن ترجمة `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex بنية مطابقة تمامًا للترجمات المرجعية (14 كتلة برمجية، و24 عنوانًا، و25 سطر جدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل برمجية متطابقة حرفًا بحرف، ومن دون أي بقايا للـplaceholders). وفي مقال صحفي حجمه 69 كيلوبايت بوضع `--news`، يجتاز خرجا `gpt-5.6-luna` و`gpt-5.6-sol` كلاهما مدقق التطبيق اللاحق للغات en/ja/ar. الاستهلاك المقاس عبر `account/rateLimits/read`: ظل دون عتبة تقريب العداد (0% من نافذة 5 ساعات) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL لإسناد الأخبار عند وجود أقواس متداخلة أو بادئة فرنسية (2026-05-11):

  - **إصلاح الخلل**: كان استخراج عناوين URL الخاصة بالإسناد داخل `_protect_news_quotes` يستخدم regex ‏`re.search(r"\((.+?)\)", attribution)` (التقاطًا كسولًا بين قوسين). وفي إسناد من نوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` محيطة + `]()` الخاصة برابط Markdown)، كان الالتقاط يتوقف عند أول `)` يعثر عليه ← سلسلة مبتورة وتتضمن البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائية). النتيجة: كان `_validate_news_post` يبحث عن هذه السلسلة في الخرج المترجم ويفشل بصورة منهجية (لسببين: `)` مبتور + ترجمة «نقله» إلى `relayed by`/`weitergeleitet von`/...). ولم يكن بمقدور التسلسل الكامل low ← medium ← high ← gpt-5.5 النجاح.
  - **الإصلاح**: أصبحت regex هي `re.search(r"\]\(([^)]+)\)", attribution)` — فهي تستهدف تحديدًا `](url)` الخاصة برابط Markdown، وتلتقط **عنوان URL الصافي وحده** (من دون البادئة الفرنسية أو البتر)، وهو ثابت تحافظ عليه placeholders ‏`#URL{N}#` أثناء الترجمة. وتتعامل بمتانة مع النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس محيطة
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py` ضمن الفئة `TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة الدقيقة المعاد إنتاجها لخلل Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (نسخة بديلة باستخدام `via`)
  - **فجوة التغطية**: يتحقق `check-editorial-coverage.py` من صحة الصياغة التحريرية، لا من قابليتها للترجمة بواسطة translator. وقد يتمثل تحسين محتمل (خارج نطاق v1.9.2) في فحص يحاكي استخراج الإسناد في dry-run لاكتشاف الأنماط الخطرة **قبل** النشر.

- **1.9.1** إصلاح i18n لتسمية CTA في ملاحظة علامة الترجمة (2026-05-10):

  - **إصلاح الخلل**: ظلت تسمية `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط العلامة أعلى الملفات المترجمة **بالفرنسية** لجميع اللغات المستهدفة، بدلًا من اتباع `target_lang`. لا يراها LLM مطلقًا (إذ تُركَّب من جهة Python للحفاظ على عنوان URL وslug الخاصين بالمستودع)، ولذلك لم تكن مرحلة الترجمة قادرة على تداركها. وهو انحدار صامت منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ15 بتسمياتها المترجمة. ويمرر `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` الآن اللغة المستهدفة. ويُستخدم fallback ‏`fr` إذا كانت اللغة غير معروفة (للسلامة، ومن دون KeyError).
  - **الاختبارات**: تعديل `test_source_emits_three_paragraphs_repo_title_description_link` ‏(target_lang ‏`ja` ← التسمية اليابانية المتوقعة). اختباران جديدان: `test_source_link_label_localized_per_target_lang` (مُعامل على 7 لغات تغطي النصوص اللاتينية، والتصويرية، والأبجدية الصامتة) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختبارًا في `test_translation_note_position.py` (بدلًا من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع بقيمة افتراضية `target_lang="fr"` — تواصل الجهات البرمجية الخارجية المستدعية التي لا تمرر `args.target_lang` العمل من دون تعديل.
- **1.9** إصلاح silent-failure + أدوات جودة متكاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + تنسيق marker "embed card"**:
    - خيارات CLI جديدة (إضافية، الإعدادات الافتراضية لم تتغير ← **غير كاسرة للتوافق**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): يضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - يعيد `legacy` إنتاج سلوك v1.8 بدقة (فقرة غامقة `**…**`) **byte-for-byte**.
        - يُصدر `marker` تعريف link reference غير مرئي في Markdown ‏(`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`)، يتبعه **blockquote من 3 فقرات** منظّم ليظهر على هيئة "GitHub repo embed card": عنوان المشروع ضمن code inline ‏(`**\`ai-powered-markdown-translator\`\*\*`)، ووصف يترجمه LLM، ورابط CTA ‏(`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. يمكن استثماره أثناء build بواسطة plugin من نوع remark (راجع مدونة jls42.org ← plugin ‏`remark-translation-banner`).
    - **ثوابت لا تُرسل أبدًا إلى LLM**: يُجمَّع عنوان repo وعنوان URL الخاص بـ GitHub في جانب Python بعد ترجمة العبارة الوصفية. لا يرى LLM مطلقًا slug ‏`ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تغيير renderer أو حالة الأحرف أو scheme.
    - **إدراج يراعي frontmatter**: في وضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة `---` الختامية** لـ frontmatter بصيغة YAML (لضمان سلامة Astro Content Collections / gray-matter). يكتشف helper ‏`_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف ويحافظ على سلامته؛ كما **يرفع `RuntimeError`** عند وجود frontmatter مفتوح بلا fence إغلاق (فيظهر الملف ضمن `failed_files` بدلًا من كتابته مع ملاحظة في موضع خاطئ).
    - **Sanitizer للنموذج قائم على whitelist**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع fallback إلى `unknown` إذا كانت النتيجة فارغة. يتوافق ذلك مع المدقّق في جانب plugin ‏remark الخاص بـ Astro، ويحيّد المحارف التي قد تكسر تنسيق marker (المسافات، وعلامات الاقتباس، والأقواس، والفواصل، وغيرها).
    - **إعادة هيكلة داخلية**: ‏`_append_translation_note` (دالة واحدة متجانسة) ← 7 helpers نقية (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فُصل builder عن composer (يعيد builder كتلة نقية بلا فاصل، بينما يطبّق composer عناصر `\n\n` وفقًا للموضع)؛ ويستخدم كود الإنتاج وhelper المصدر أداة التجميع نفسها ذات الفقرات الثلاث.
    - **`_quote_lines` مع الحفاظ على الأسطر الفارغة**: يضيف `> ` في بداية كل سطر، ويحوّل الأسطر الفارغة إلى `>` وحده. يتيح ذلك لـ mdast رؤية 3 فقرات منفصلة داخل blockquote (العنوان / الوصف / الرابط) بدلًا من فقرة واحدة ذات فواصل أسطر.
    - **`_build_translation_note_block` تكيّفي**: وفقًا لعدد الفقرات التي حافظ عليها LLM (3 = تنسيق card كامل، 2 = عبارة + رابط، 1 = fallback). لم يعد fallback ذو الفقرة الواحدة **يغلّف المحتوى بـ `**...**`** عند اكتشاف رابط Markdown ‏`](` (إذ يكون عرض `<strong>` حول رابط هشًا).
    - **توافق تصاعدي**: ‏`getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` في جانب `_compose_with_notes` — تواصل كائنات Namespace التي لا تحتوي على هاتين السمتين (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) العمل من دون تعديل.
  - **إصلاح silent-failure في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع providers ‏(OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور حرفيًا على مقتطف من المصدر) + طبقة احتمالية (`langdetect`)
    - قائمة whitelist لـ `finish_reason` / `stop_reason`: رفع `RuntimeError` لأي حالة خارج whitelist ‏(truncation وcontent_filter وغيرهما)
    - ‏`max_tokens` في Claude: ‏`4096` ← `32768` (لتجنب truncation الكامن في مقاطع 16k، مع هامش للتحويل بين أنظمة الكتابة FR→JA/ZH/KO/AR/HI)
    - تقسيم يراعي العناوين: إعطاء الأولوية لـ H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى exit code غير صفري: يعيد `translate_markdown_file` حالة محددة النوع `success` / `failure` / `skipped`، ويُنفّذ `main()` الأمر `sys.exit(1)` إذا فشل ملف واحد على الأقل (سواء لملف منفرد أو batch)
    - حارس للمحتوى الفارغ لدى جميع providers، وفحص منطقي لنسبة المصدر إلى المخرجات (≥ 500 محرف، وأقل من 5% = رفض)، والتحقق من placeholders الخاصة بالكود (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، وإعادة محاولة `BadRequestError` من دون `reasoning_effort`
    - إضافة التبعية `langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("من نوع EurekAI المتكامل"، 14 hooks):
    - ‏Pre-commit: ‏ruff ‏(lint+format)، وshellcheck، وprettier ‏(md/yaml/json)، وdetect-secrets ‏(حماية 4 مفاتيح API)، وLizard ‏(CCN ≤ 12)، وpre-commit-hooks v5 ‏(المسافات البيضاء، وEOF، والملفات الكبيرة، وshebangs، وغيرها)
    - ‏Pre-push: ‏mypy (وضع متساهل تدريجي)، وOpengrep SAST ‏(translate.py + scripts/)، وpip-audit (وضع reporting أولي)، وunittest discover ‏(tests/ + scripts/tests/)
    - ‏Wrappers محلية في `scripts/` تستخدم `./venv/bin/python`
    - ‏`scripts/audit_verdict.py`: parser بصيغة JSON لـ pip-audit مع 11 اختبارًا باستخدام unittest، وهو نقل إلى Python للـ parser الخاص بـ jls42-astro
    - إصلاح 7 مخالفات أولية في ruff: ‏B904 ‏(raise from) ×2، وB007 ‏(unused dirs)، وC408 ‏(dict literal)، وC419 ‏(list-comp)، وSIM105 ‏(contextlib.suppress)، وSIM110 ‏(any())
    - يستبعد Lizard مؤقتًا `translate.py` (4 دوال بقيم CCN بين 21 و47، مع إعادة هيكلة مخطط لها) — gate صارم على scripts/
  - **SonarCloud + تغطية شاملة**:
    - سير عمل GitHub Actions ‏`SonarCloud` ‏(sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع coverage عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى README ‏(Quality Gate، وتصنيفات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code)
    - ‏`tests/test_silent_failure.py` ‏(`unittest` من stdlib): يغطي الحلقات الست في سلسلة أخطاء silent-failure
    - ‏`tests/test_orchestration.py` ‏(+79 اختبارًا): يغطي طبقة orchestration في `translate.py` ‏(`_resolve_*_filename`، `_existing_translation_exists`، `_record_translation_status`، `_write_output_file`، `translate_directory`، `_validate_input_paths`، `_init_*_client`، `_select_provider_client`، `_normalize_collapsed_markdown`، `_cleanup_source_flag`، `_validate_news_flags_*`، وfallbacks الخاصة بـ `_openai_create_with_fallback` لكل من TypeError وBadRequestError، وتنسيق prompt لسلسلة o1، وفروع early-return في `_validate_translation_output`)
    - ‏`scripts/tests/test_audit_verdict.py`: تغطية `main()` ‏(stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **Coverage on new code**: ‏75.5% ← نحو 98% ‏(translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك E2E ‏`marker+top|bottom|both` و`legacy+top|bottom|both`)، وإضافة البادئة إلى عدة أسطر، والتوافق الرجعي byte-for-byte ‏(golden literal)، وsanitizer، وتقسيم frontmatter (بما في ذلك الرفع عند عدم إغلاق fence)، وتنسيق الفقرات الثلاث، وfallback ذي الفقرتين، وحارس الفقرة الواحدة + رابط Markdown، وحاجز أمان حرج `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان + URL لا يُرسلان أبدًا إلى LLM. **نجح 190 اختبارًا**، من دون أي تراجع.
  - التوثيق: ‏`README.md` ‏(بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` ‏(سير عمل pre-commit + مراقبة CI مفصلة)، وإعادة توليد 28 ترجمة
- **1.8** وضع `--news` + ترقية نماذج 2026 ‏(2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - جودة OpenAI: ‏`gpt-5` ← `gpt-5.4`
    - OpenAI الاقتصادي: ‏`gpt-5-mini` ← `gpt-5.4-mini`
    - جودة Gemini: ‏`gemini-3-pro-preview` ← `gemini-3.1-pro-preview`
  - إضافة حدود tokens لكل من `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` ‏(400k)، و`gemini-3.1-pro-preview` ‏(1M)
  - الوضع الأولي لـ `--news`: حماية الاقتباسات الإنجليزية باستخدام placeholders ‏`#NEWSQUOTE\d+#`، وmapping ‏`LANG_FLAGS` ‏(15 لغة)، وإدارة الأعلام وفقًا للغة الهدف
  - التحقق من placeholders الخاصة بالأخبار قبل استعادتها (تراجع: كان حذف LLM للـ placeholder ينتج بصمت مخرجات بلا اقتباس)
  - جعل script ‏`regen_translations.sh` قابلًا للنقل (مسارات مطلقة، بلا اعتماد على pwd)
  - إضافة رابط اللغة الفرنسية إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة
- **1.7** الميزات الجديدة:
  - الخيار `--keep_filename` للاحتفاظ باسم الملف الأصلي أثناء الترجمة
  - دعم ملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على inline code**: أصبحت علامات backticks ‏(`` `...` ``) محمية أثناء الترجمة
  - تحسين system prompt:
    - معالجة أفضل لعلامات الاقتباس في YAML frontmatter
    - حماية متغيرات template ‏`{variable}`
    - منع ملاحظات المترجم غير المطلوبة
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** الميزات الجديدة:
  - دعم Google Gemini API للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - ‏OpenAI: ‏`gpt-5` (جودة)، و`gpt-5-mini` (اقتصادي)
    - ‏Claude: ‏`claude-sonnet-4-5` (جودة)، و`claude-haiku-4-5` (اقتصادي)
    - ‏Gemini: ‏`gemini-3-pro-preview` (جودة)، و`gemini-3-flash-preview` (اقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف منفرد (`--file`) من دون المرور عبر مجلد
  - نمط تسمية مبسّط جديد: `{base}-{lang}.md`
  - الخيار `--include_model` للاحتفاظ بالتنسيق القديم الذي يتضمن اسم النموذج
  - دعم النماذج غير المدرجة مع حد افتراضي من tokens ‏(128k)
  - ترجمة README إلى 14 لغة
- **1.5** التحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين prompts الترجمة:**
    - أُثريت prompts الخاصة بالترجمات المباشرة وملاحظات الترجمة لتحقيق وضوح وفعالية أفضل، بما يشمل تعليمات مفصلة حول الحفاظ على البيانات الوصفية وعناصر التنسيق المحددة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم imports لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص وإدارة كتل الكود للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات المخرجات:**
    - عكس ترتيب النموذج واللغة في اسم ملفات المخرجات (مثلًا `f"{base}-{args.target_lang}-{args.model}.md"`)، ما يسهّل تنظيم الترجمات والعثور عليها.
  - **تحسينات متنوعة:**
    - تنظيف الكود بإزالة الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية script وسهولة قراءته.
- **1.4** الميزات الجديدة:
  - دعم Claude API من Anthropic للترجمة
  - تحسين prompts لزيادة الوضوح والفعالية
  - تعديلات طفيفة لتحسين قابلية صيانة الكود
- **1.3** التحسينات والميزات الجديدة:
  - تحسين إدارة كتل الكود
  - تحسين إدارة ملفات المخرجات
  - تحسين اكتشاف الملفات الموجودة
  - الخيار `--force` لفرض الترجمة
  - عكس ترتيب النموذج واللغة في اسم ملف المخرجات
- **1.2** إصلاح changelog
- **1.1** إضافة دعم Mistral AI API
- **1.0** الإصدار الأولي - دعم OpenAI API

**مقال مترجم من الفرنسية إلى العربية باستخدام gpt-5.6-sol.**
