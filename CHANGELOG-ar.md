### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.11.0** النشر على PyPI: `pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-09-03):

  - **أصبح البرنامج النصي أحادي الملف حزمة قابلة للتثبيت.** انتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول لوحدة التحكم `aipmt` وما يعادلها `python -m aipmt`. يظل استنساخ المستودع ضروريًا للمساهمة — إذ توجد فيه الاختبارات والترجمات الـ28 وأدوات ضمان الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` مطلقًا**، لأن التعارض حقيقي وصامت. حزمة PyPI `translate` (الإصدار v3.8.1، وآخر رفع في 2026-07-06) تثبّت **دليلًا** بالاسم نفسه. أُعيد إنتاج ذلك في venv: يتغلب الدليل على الوحدة، ويختفي `translate.main`، وتتوقف نقطة الدخول عند `AttributeError` — بينما يعرض `pip check` الرسالة «No broken requirements found» مع rc=0. كان من الممكن أن يؤدي مجرد `pip install translate` لدى المستخدم إلى تعطيل CLI من دون تشخيص مفيد. واختبار مضاد على wheel الحقيقي: `pip install translate` فوق الحزمة، و`aipmt --help` يعيد rc=0 قبل التثبيت وبعده، وتتعايش أداتا CLI.
    - **اسم توزيع طويل، وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للعثور عليها عبر البحث في PyPI؛ أما استخدام اختصار وحده فسيجعلها غير قابلة للعثور عليها لمن لا يعرف المشروع مسبقًا، مع أن الهدف من النشر تحديدًا هو أن يتم العثور عليها. استُبعد مرشحان معقولان بعد التحقق: `ai-markdown-translator` محجوز على npm منذ 2024 لأداة تؤدي الغرض نفسه، وتسبق هذا المستودع بـ17 شهرًا؛ و`aimt` لا يختلف إلا بحرف واحد عن `aim` (v3.29.1)، وهي حزمة نشطة في **المجال نفسه** — أسوأ إعداد لحدوث التباس مستمر. وتنبيه منهجي بالمناسبة: يعيد `pypi.org/project/<nom>/` الرمز 200 لأي اسم (صفحة مضادة للروبوتات)، ولا يُعتد إلا بواجهة JSON البرمجية.
    - **التخطيط `src/` بدلًا من حزمة مسطحة.** كانت الحزمة المسطحة ستحافظ على `sys.path.insert(..., "..")` الستة الخاصة بالاختبارات، وهذا هو العيب تحديدًا: فهي تجعل الاستيراد يتم من شجرة المصدر بدلًا من الحزمة، مما يخفي أي خطأ في التغليف. التكلفة الفعلية: قاعدة استبدال إضافية.

  - **أصبح بالإمكان إعداد المفاتيح مرة واحدة وإلى الأبد.** لم تكن لدى CLI المثبتة أي إعدادات **مستمرة**: لم يبقَ سوى متغير البيئة و`.env` الخاص بالدليل الحالي. صحيح أن `find_dotenv` كان يصعد حتى جذر النظام، ولذلك يعثر على `~/.env` **عند العمل داخل الدليل الشخصي**، لكنه لا يعثر على شيء عند العمل في مكان آخر — تغطية تعتمد على المكان الذي أُطلق منه الأمر، لا على خيار تصميم. لذلك أُضيفت طبقة ثالثة: `~/.config/aipmt/.env`، أسفل الطبقتين الموجودتين.

    - **الأولوية غير مُرمّزة**، بل تنتج عن `override=False`، وهي القيمة الافتراضية لـ`load_dotenv`: كل طبقة لا تملأ إلا ما تركته الطبقة السابقة فارغًا. ومن هنا يأتي الترتيب: متغير البيئة ← `.env` الخاص بالمشروع ← إعدادات المستخدم، وقد جرى التحقق منه باختبارات **السلوك** لا باختبارات البنية — فعكس ترتيب الاستدعاءين يفشلها، وكذلك إزالة الطبقة الثالثة.
    - **التنسيق `.env` وليس TOML**، عن قصد: إن `python-dotenv` تبعية موجودة أصلًا، وبنيتها موثقة مسبقًا في ملفات README الخمسة عشر، والملف نفسه يخدم النطاقين. لا تبعية جديدة ولا صياغة جديدة. يتبع الموقع `XDG_CONFIG_HOME` إذا كان **مطلقًا** — إذ تطلب المواصفة تجاهل القيمة النسبية، وإلا لأصبح موقع الإعدادات معتمدًا مجددًا على الدليل الحالي — و`APPDATA` على Windows.
    - **استُبعد خياران، مع سبب استبعادهما.** مدير مفاتيح النظام (`keyring`) أكثر أمانًا على جهاز مكتبي، لكنه يفشل في الوضع headless — الخادم والحاوية وCI — أي حالة الاستخدام نفسها للترجمة الدفعية؛ لذا فهو مرشح جيد للاختيار الصريح، لكنه افتراضي سيئ. أما الخيار `--api-key` فسيضع المفتاح في سجل shell ويجعله ظاهرًا في `ps`.
    - **من دون مفتاح، لم يعد هناك أثر لاستدعاء Python.** كان المستخدم يتلقى مكدس Python يشير إلى `site-packages`، ورسالة تذكر «البيئة أو .env» من دون توضيح مكان إنشاء الخيار الثاني. أما الآن فتسرد الرسالة المواقع الثلاثة مع مسار كل منها بدقة، ويخرج الأمر بالرمز 2. وشبكة الأمان **ضيقة عن قصد**: `except ValueError` في مرحلة الإعداد وحدها. إن تغليف التنفيذ بأكمله سيحوّل خطأ حقيقيًا يحدث أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الفشل الذي يتعقبه هذا المستودع. ويقرأ أحد الاختبارات مصدر `main()` لمنع ذلك.

  - **إصلاح — كان `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** إن `load_dotenv()` من دون وسيط لا يصعد من الدليل الحالي، بل من ملف **المستدعي**، أي من `site-packages`. وقد قيس ذلك باستخدام نقطة دخول حقيقية لوحدة التحكم، أُطلقت من مشروع يملك `.env` الخاص به: يعيد `find_dotenv()` القيمة `''` ولا يُحمّل المفتاح، بينما يعثر عليه `find_dotenv(usecwd=True)`. لم يكن الخطأ موجودًا ما دامت الأداة لا تعمل إلا من المستودع المستنسخ؛ لكن النشر كان سيجعله منهجيًا، بحيث تكون العَرَض الوحيد مفتاح API «مفقودًا» رغم صحة الإعدادات.

  - **كانت ثلاث بوابات ستتحول إلى اللون الأخضر بعد أن توقفت عن التحقق من أي شيء.** جرى تشديدها **قبل** النقل، عن قصد: فحاجز يُكتب بعد التغيير الذي يُفترض أن يلتقطه لا يثبت شيئًا. كل واحدة منها خضراء على المستودع الأصلي وتتحول إلى الأحمر على نسخة منقولة — وقد قيس الاتجاهان.

    - **يتجاهل Lizard مسارًا غير موجود من دون قول شيء**: rc=0، و«0 file analyzed». كانت بوابة التعقيد ستنتقل من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع خرج حجمه صفر بايت. أصبح النطاق الآن مصفوفة يُتحقق من وجود كل إدخال فيها.
    - **لا يفشل `coverage run --source=` عند غياب وحدة**: تحذير على stderr وحده، وrc=0 لكل من unittest و`coverage xml`، مع نشر التقرير رغم ذلك — مبتورًا من 1453 إلى 141 عبارة. كان المشروع سيبدو سليمًا لأنه لم يعد يُحلَّل إلا جزئيًا تقريبًا. وتحافظ عتبتان دنيا على التقرير: الإجمالي، وأكبر ملف جرى قياسه.
    - **مسبار حداثة الترجمات أعمى بنيويًا عن صيغة الاستدعاء**: فهو يرتكز على أعلام argparse، وهي بالضبط الأشياء التي لا يغيرها إعادة تسمية ملف. أُعيد إنتاج الحالة: نُقلت الوحدة، وظلت ملفات README الخمسة عشر توثق أمرًا غير موجود، فجاء الحكم «لا توجد ترجمة قديمة». لذلك يتحقق قسم سابع من **الصيغة** لا من الخيارات، وتُقارن إضافة Lizard بالنطاق الفعلي للبرنامج النصي — فمفتاحها `files:` لا يجعل pre-commit يفشل عندما يتوقف عن المطابقة، بل يجعله **يتخطى** الفحص.

  - **لم يعد `requires-python = ">=3.10"` مجرد ادعاء.** كان `sonar-project.properties` يعلن مسبقًا دعم 3.10-3.12 من دون أن يختبر أي شيء ذلك فعليًا، إذ لم يكن جهاز التطوير يضم سوى 3.12 — وهو تناقض داخلي كان النشر سيجعله علنيًا. يشغّل سير عمل الاختبارات الآن المجموعة على 3.10 و3.11 و3.12، مع تثبيت **الحزمة** وبالتالي حدودها العامة.

  - **حدود دنيا بلا سقف.** يظل `requirements.txt` هو القفل المختبَر، بينما يصبح `[project.dependencies]` هو العقد العام: فنشر الإصدارات الدقيقة الموجودة في القفل سيسبب تعارضات لدى كل مستخدم لديه حزم أخرى. ولا يوجد سقف `<N+1` أيضًا — إذ سيتعارض مباشرة مع `check-deps-fresh.sh`، الذي يجعل بوابة الإصدار تفشل عند أي تأخر في الإصدار الرئيسي. تحل مجموعة الحدود الدنيا المشكلة، ويُخرج الاختبار المضاد `openai==1.0.0` بالرمز `ResolutionImpossible`، مما يثبت أن الفحص يميز الحالات بدل قبول كل شيء. كما يمنع حاجز آخر إصدار `pyproject.toml` من الاختلاف عن إصدار CHANGELOG: إذ لا يسمح PyPI بإعادة استخدام رقم.

  - **تم التحقق من البداية إلى النهاية في venv جديد**: wheel بحجم يقارب 70 كيلوبايت لا يحتوي إلا على `aipmt/*.py` وdist-info والترخيص؛ و`aipmt --help` يعيد rc=0 مع 22 علمًا؛ و`python -m aipmt` يعرض «usage: aipmt» لا «usage: \_\_main\_\_.py»؛ وتثبيت `pipx` يعمل؛ والأهم **ترجمة فعلية من fr→en من أي دليل مستخدم**، مع الحفاظ على الخط العريض والقائمة وcode inline والرابط وURL، وعدم ترجمة كتلة الشيفرة. وقد اجتازت الاختبارات الـ318 السابقة للنقل مع قائمة معرفات مطابقة تمامًا على مستوى البايت قبل النقل وبعده — وهذا ما يثبت أن أي اختبار لم يُعطَّل، لا مجرد ظهور «OK»؛ وأُضيف إليها اثنا عشر اختبارًا للإعدادات ذات الطبقات الثلاث، ليصبح الإجمالي 330.

- **1.10.0** موفر `--use_codex` (حصة اشتراك ChatGPT)، وتحديث SDKs والنماذج، وإصلاح الاستشهادات الإخبارية متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — حاجزان كانت PR تضعهما من دون الالتزام بهما في كل المواضع**:

    - **كان preflight الخاص بـCodex يمرر كامل `.env` إلى الملف التنفيذي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: لذلك كانت العملية الفرعية ترث كامل `os.environ`، أي كامل `.env` الذي حمّله `load_dotenv`. وعند القياس باستخدام ملف تنفيذي زائف مزود بأداة قياس، وصلت **سبعة أسرار** إلى preflight — مفاتيح الموفرين الستة إضافة إلى `GITHUB_TOKEN` — مقابل **صفر** لدى نظيره `_grok_preflight`، الذي كان يمرر `env=_grok_env()` كما ينبغي. كان التناقض داخليًا في PR: فـ`_strip_secret_env` موجود تحديدًا للحفاظ على هذا الثابت، وعلى بضع سطور فقط. استُخرج `_codex_env_base()` وأصبح مشتركًا بين المسارين؛ وبعد الإصلاح: 0 سرًا في كلا الجانبين.
    - **لم تكن خاصية «`--deny` fail-closed» تغطي الصيغة المستخدمة.** كان التعليق يبرر كامل عزل Grok بحجة أن قاعدة ذات بادئة غير معروفة ترفض بدء التشغيل. لكن القياس على `grok 1.0.13` أظهر أن هذا التحقق موجود **للصيغة الموضوعة بين قوسين فقط**: إذ يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل («unknown tool prefix»)، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. وبما أن `GROK_DENY_RULES` لم يستخدم إلا أسماء مجردة — فإن إعادة تسمية أداة من جانب xAI كانت ستزيل طبقة العزل الوحيدة التي جرى قياسها، من دون أي إشارة، على جهاز لا ينطبق عليه عزل نظام التشغيل أصلًا. تمر القواعد الثماني المسماة بصيغة `Prefix(*)`، مع التحقق من أن كل واحدة منها بادئة معروفة لدى CLI؛ أما `*` الشامل فيبقى بصيغته الحرفية، وهي الصيغة الوحيدة المقبولة. ويمنع اختبار العودة إلى الصيغة غير المتحقق منها.
    - **تم التحقق من بقية الجوانب بنظافة**: لا حقن للأوامر (صيغة قائمة في كل المواضع، ولا `shell=True` مطلقًا، ومحتوى الوثيقة عبر stdin أو `--prompt-file`)، ولا إلغاء تسلسل غير آمن (`json.loads` فقط، مع حارس نوع)، وإصلاح اجتياز المسارات من دون العثور على تجاوز عبر سبع حمولات، كما أن `--deny '*'` مطبق فعليًا بواسطة CLI (`DENY_ENFORCED` لوحظ عند قراءة خارج workdir).
    - كان فحص حداثة الترجمات المضاف أعلاه يتجاوز مبدئه الخاص: فإذا فشل طلب حزمة PyPI، كان يتخطاها بصمت وتبقى البوابة خضراء. أما الآن فيعد الحزم التي قورنت فعليًا ويفشل إذا كانت التغطية غير مكتملة.

  - **رُفعت إصدارات التبعيات، وأضيف حاجزان لمنع تكرار التأخر**:

    - **كان التأخر حقيقيًا ومستمرًا**: `openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر عامين في مخزن شهادات الجذر الذي يتحقق من TLS لجميع استدعاءات الموفرين. السبب محدد: **لم يكن هناك `.github/dependabot.yml`**. ومن دون هذا الملف، لا يفعّل GitHub سوى _security updates_، ولا يقترح Dependabot PR إلا لتبعية تستهدفها CVE — وهو ما يفسر أنه حدّث `urllib3` و`idna`، لكنه ترك SDKين يتأخران عن إصدار رئيسي.
    - **تتعايش الإصداران الرئيسيان بلا تعارض**، خلافًا لما أوحى به استدلال سابق: إذ ينتقل `openai` 3.x و`anthropic` 1.x إلى **`httpx2`**، بينما يظل `mistralai` و`google-genai` على `httpx<1`، لكنهما توزيعتان منفصلتان. جرى التحقق بالتثبيت الفعلي، ثم عبر **مسارات الموفرين السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على code inline والروابط في كل خرج. كان «تجنب مكدسين HTTP» تفضيلًا لا عائقًا؛ وقد حسم القياس الأمر.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: فقد كان `google-auth` و`cryptography` ومكدس `opentelemetry` مثبتًا في venv العمل من دون التصريح بها قط — لذلك لم يكن التثبيت الجديد يعيد إنتاج ما جرى اختباره. وبالمقابل، كانت `tokenizers` و`huggingface-hub` و`PyYAML` مدرجة فيه من دون استيرادها أو طلبها من أي شيء، وهي بقايا `mistralai` 1.x. أُعيد توليد الملف بوصفه الإغلاق الكامل لـvenv مبنيًا من التبعيات المباشرة وحدها. ولا يُظهر `pip-audit` أي ثغرة معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** (جديد) يفعّل تحديثات الإصدارات الأسبوعية وpip وgithub-actions. تُجمع التحديثات الثانوية والتصحيحية في PR واحدة — إذ ينتهي الأمر بتجاهل patch bump لكل PR، كما أن الضوضاء عدو التحديث؛ أما **الإصدارات الرئيسية فتُفصل**، ويتطلب كل منها تحققًا عبر استدعاء فعلي.
    - **`scripts/check-deps-fresh.sh`** (جديد، موصول بالبوابة) يجعل التأخر ظاهرًا في حكم المشروع: يقترح Dependabot التحديثات ولا يضمنها، وقد تتراكم PRs الخاصة به. تأخر إصدار رئيسي → فشل؛ وتأخر إصدار ثانوي → تحذير، لأن البوابة الحمراء باستمرار ينتهي بها الأمر إلى التجاهل؛ وتعذر الوصول إلى PyPI → تخطٍّ صريح محليًا و**فشل مغلق في CI**، إذ إن الفحص غير المنفذ ليس نجاحًا. جرى التحقق في الاتجاهين: فهو يلتقط الحالة الدقيقة السابقة للإصلاح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير عند وجود تأخر ثانوي.

  - **إصلاحات ناتجة عن مراجعة PR هذه** — دقّق خمسة وكلاء مراجعة في diff؛ وقد **أُعيد إنتاج جميع النقاط التالية بالقياس** قبل إصلاحها، وكان اثنان منها تراجعين أُدخلا في وقت سابق ضمن الإصدار نفسه.
- **تم إصلاح التراجع — كان لدى `_NEWS_CITATION_REGEX` تراجع أُسّي.** كان إصلاح الفقرات المتعددة قد أدخل `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: مشاركة المسافات بين `[ \t]+` و`.*` ملتبسة، وهذا الالتباس يتضاعف من تكرار إلى آخر. قيس ذلك على أسطر `>   texte` — وهي مسافة بادئة Markdown قانونية تمامًا — لا تطابق النمط: **2 589 ms لـ 14 سطرًا** مقابل 0,04 ms بعد التصحيح، مع عامل ~9 لكل سطر مضاف. في وضع `--news`، كان يكفي اقتباس طويل وغير مطابق لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب قابل للتحديد. يستهلك التكرار الآن السطر كاملًا دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يترك إلا طريقة واحدة للمطابقة في كل تكرار. جرى التحقق على المجموعة الحقيقية المكوّنة من 231 مقالًا: **لا اختلافات إطلاقًا** في الاستخراج، مع الاقتباسات الـ423 نفسها، وبقاء الأجسام الـ14 متعددة الفقرات موسّعة.
    - **كان استخدام علمَي provider معًا يؤدي إلى الفوترة حسب الاستخدام بصمت.** كان `--use_codex --use_mistral` مقبولًا؛ وكان `_select_provider_client` يختبر Mistral أولًا، بينما يعطي `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. لذلك كان المستخدم يطلب حصة اشتراكه ويحصل على فوترة حسب الاستخدام، من دون أي تحذير: وهو بالضبط نمط الفشل الذي وُجد `--use_codex` لمنعه. تمر أعلام provider الستة الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: يفشل الآن سطر أوامر يجمع بين providerين، بعدما كان يُقبل بصمت، عند `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة نهاية العمل تنتقل إلى الحالة الخضراء عندما تتعطل مجسّاتها.** كانت أربع عمليات تحقق من أصل ثلاث عشرة في `scripts/check-release-ready.sh` تتبع نمط «التقاط stdout والاستنتاج إذا كان فارغًا» من دون فحص رمز الخروج: إذ كانت الاستثناءات (إعادة تسمية ملف، `FileNotFoundError`) تكتب إلى stderr، وتترك stdout فارغًا، ثم تستنتج عملية التحقق «لا شيء يستدعي التنبيه». وقد أُعيد إنتاج فخ «إن `exit 0` لا يثبت شيئًا» داخل البرنامج النصي الذي كُتب لمنعه. يفرض helper باسم `probe()` الآن رمز خروج صفريًا **وعلامة نهاية**، كما ترفض المجسات الاستنتاج من مجموعة مؤشرات فارغة — إذ إن assertion على مجموعة فارغة تكون صحيحة دائمًا. والدليل على ذلك: أدى إضافة المجموعة الحصرية أعلاه إلى تمرير أعلام provider عبر كائن `*_group`، لم تعد regex القديمة `parser\.add_argument\(` تطابقه؛ وكانت **ستة أعلام من أصل واحد وعشرين** تخرج بصمت من النطاق، بينما تبقى البوابة خضراء.
    - **كان فحص الأسرار يفوّت أربعة provider من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: إذ كان `sk-proj-…` (تنسيق OpenAI الحالي) و`sk-ant-api03-…` يفشلان عند شرطتهما الثانية، كما لم يكن `AIza…` مشمولًا. وُسّع النمط، واستُبعد `.secrets.baseline` من الفحص. وكانت الحماية `.env` تستعلم أيضًا عن `git diff --cached`، الذي لا يرى إلا الفهرس: لذلك لم يكن `.env` **المُرسل إلى commit بالفعل** — وهو أسوأ سيناريو — يظهر فيه إطلاقًا. وهي تستعلم الآن عن `git ls-files`.
    - **لم يكن «الإحماء المسبق للرمز» في Codex كذلك فعليًا.** أظهر القياس أن `codex login status` لا يلمس `~/.codex/auth.json` (لم يتغير mtime ولا الحجم)، كما أن مساعدته تقول «إظهار حالة تسجيل الدخول». ومع ذلك، كان التعليق يدّعي أنه يحدّث الرمز «مرة واحدة، بالتسلسل»، وبالتالي يعطّل خطر عمليات التحديث المتزامنة لرمز دوّار ذي استخدام واحد. لم تكن الحماية المعلنة موجودة؛ ويقول التعليق الآن ما يفعله الكود فعلًا، بينما يبقى الحل الحقيقي هو `max_jobs=4`. كما يراعي الفحص `CODEX_BIN`، الذي كان يتجاهله — إذ كان جهاز لا يحتوي على `codex` في `PATH` يفشل برسالة «غير موثّق»، وهي رسالة تشخيصية مضللة.
    - **كان تحميل `.env` يتم في shell فرعي.** يُستدعى `detect_provider` داخل استبدال أمر، ولذلك لم تكن عمليات التصدير الصادرة منه تصعد إلى البيئة: فكان `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` المعرّف في `.env` يظل غير مرئي للقراءات المنفذة في `main()`، التي كانت تستنتج «تعذر العثور على Grok binary» رغم صحة الإعداد.
    - **كانت المنافسة تتجاوز الحد المعلن بنسبة 50٪.** وُضعت الحماية بعد تشغيل زوج README/CHANGELOG، وقيسَت ذروة قدرها **3 لـ `max_jobs=2`**. وفي Grok، الذي تُشارك حصته الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يكن الحد الذي يفرضه البرنامج النصي متحققًا إذن. أما العدّ النهائي فكان يُعرض من دون مقارنته بـ28 — ولذلك كان مرور ملف مفقود من دون ملاحظة.
    - **عقد خرج Grok: أصبح غياب `stopReason` فشلًا.** كان الكود يطبق «`end_turn` **أو غائب**» في موضع يتطلب فيه العقد المعلن `end_turn`. وكان payload بلا هذا الحقل — أو حقل أعادت تحديثة CLI تسميته — يحوّل الحماية إلى عملية لا تفعل شيئًا بصمت. إضافة إلى ذلك، لم يعد `max_turn_requests` مصنفًا كـ rate limit (فالذي استُنفد هو ميزانية الجولات: وإعادة المحاولة تعيد النتيجة نفسها، مقابل انتظار 90 ثانية)، كما أُزيل `quota` من مؤشرات rate limit — للسبب الذي كان docstring الخاص بـ`_codex_is_rate_limited` يذكره أصلًا من دون أن يطبقه Grok.
    - **أصبحت سلسلة Gemini المحفوظة مؤقتًا تعتمد على النموذج.** كانت تبدأ من `minimal` في كل مقطع، مع أن النموذج الافتراضي يرفضه: فكان المسار المعتاد يدفع ذهابًا وإيابًا بقيمة 400 لكل مقطع، ويعيد طباعة التحذير نفسه. والتحذير المتكرر مئات المرات يتوقف عن أن يُقرأ — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مضمّنةً لـCodex وتوجّه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق طبقة العمليات الفرعية يعمّم «shim» على CLIين، مع أن Grok binary هو ELF أصلي (والتبرير الصحيح هو «agent ينشئ عملياته الفرعية الخاصة»)؛ كما عُلّمت اثنتا عشرة نتيجة SAST على `subprocess` بالوسمين `# nosec` / `# nosemgrep` مع التبرير، إذ إن صيغة القائمة من دون `shell=True` تجعل الحقن مستحيلًا، ومحتوى المستند لا يمر إطلاقًا عبر argv.
    - **لم يعد أي سر يدخل إلى العمليات الفرعية الوكيلة.** كانت قوائم الرفض الاسمية تحمي فقط ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). لكن القياس أظهر أن **سبعة أسرار أخرى** كانت تدخل رغم ذلك إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح CLI الآخر، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة المرور. وهذان CLI هما **agents**، كما أن Grok يعمل من دون sandbox لنظام التشغيل يمكن تطبيقه على كثير من أجهزة Linux. يُجرى الترشيح الآن **حسب نمط الاسم** (`API_KEY` و`_TOKEN` و`SECRET` و`PASSWORD` و`CREDENTIALS`) بدلًا من قائمة اسمية، ما يغطي أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن يعرفها هذا الكود. ولا يحتاج CLI إلى أي منها: فالمصادقة موجودة في `~/.codex` و`~/.grok`، ولا توجد أبدًا في البيئة — وقد تم التحقق من ذلك عبر **ترجمة حقيقية مكتملة** باستخدام كل من providerين مع البيئة المشددة.
    - **الاختبارات**: ملف جديد باسم `tests/test_review_hardening.py` (21 اختبارًا) يثبت حصرية أعلام provider، وعقد `stopReason`، وخطية regex الخاصة بـnews، ورسالة الرفض في CI، والحفظ المؤقت لـGemini، وغياب أي سر من بيئة العمليات الفرعية. وهذا التأكيد الأخير **عام** — إذ يفشل عند وجود مفتاح لا تسميه أي قائمة — في حين أن اختبار التنقيح الحالي كان مرآةً لثابته، ولم يكن يستطيع اكتشاف شيء سوى تعطل حلقته الخاصة. أصبح مجموع الاختبارات **311 اختبارًا**.

  - **مزودا Grok جديدان**: `--use_grok` (واجهة xAI، بالمفتاح `XAI_API_KEY`، مع فوترة حسب الاستخدام) و`--use_grok_cli` (CLI الرسمي لـGrok Build، يُخصم من اشتراك Grok — بالمبدأ نفسه الخاص بـ`--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن نقطة نهاية xAI متوافقة مع OpenAI، أُعيد استخدام العميل و`_call_openai` كما هما، ولا يتغير سوى `base_url`. وكان التكييف الوحيد الضروري مفيدًا للجميع: إذ يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي تصدرها xAI، في حين تصدر OpenAI `stop`. النماذج: `grok-4.6` (الجودة) و`grok-4.3` (الاقتصادي). تجدر الإشارة إلى أن النسخة الاقتصادية من Grok تظل الأغلى في المستودع — $1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ`mistral-small-latest`؛ لذا يُختار هذا provider لتنوع النموذج، لا للسعر.
    - **وضع CLI**: صُمم على غرار Codex، مع أربعة اختلافات فرضها الواقع — يُرسل prompt عبر ملف (`--prompt-file`؛ إذ إن CLI لا يقرأ stdin، كما أن مقطعًا في argv سيظهر في `ps`)، والخرج عبارة عن كائن JSON واحد على stdout (ليس JSONL ولا ملف `-o`)، ولا يعرض الاشتراك سوى `grok-4.6` و`grok-4.5`، ولا يمكن تطبيق sandbox (انظر أدناه). جرى توحيد تشغيل العمليات الفرعية مع Codex في `_codex_run_process`، من دون المساس ببقية provider الخاص بـCodex، الذي خضع للاختبار مسبقًا.
    - **لا يثبت `exit 0` شيئًا، وفق القياس**: عندما لا يكون CLI موثّقًا، يكتب `{"type":"error","message":"Not signed in."}` على **stdout** مع **رمز خروج 0**. ويتصرف الرفض أو تجاوز الجولات بالطريقة نفسها. لذلك يتطلب عقد الخرج أربعة شروط متزامنة: رمز خروج 0، وغياب payload خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع preflight المنطق نفسه: إذ يُخرج `grok models` القيمة 0 حتى وهو غير متصل، ولا يمكن الاستنتاج إلا من وجود «not authenticated» في stdout.
    - **الاحتواء: عدم تماثل مقصود وموثّق.** في حين يعمل Codex ضمن `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـGrok على كثير من أجهزة Linux الحديثة لسببين مستقلين على مستوى النظام ولا يمكن تجاوزهما من دون `sudo`: يحظر AppArmor مساحات أسماء المستخدمين غير المميّزة منذ Ubuntu 24.04 (`bwrap: setting up uid map: Permission denied`، وقد أُعيد إنتاجه خارج Grok)، كما تفشل قائمة رفض مقابس تشغيل الحاويات عندما يكون `/run/podman` في `0700` (لا يعيد resolver المحاولة إلا لـ`ErrorKind::NotFound`، بينما يصبح EACCES قاتلًا). والفخ المركزي هو أن ملفًا شخصيًا **مدمجًا** لا يمكن تطبيقه **يبدأ دون احتواء بصمت**. لذلك لا يطلب البرنامج النصي أي ملف شخصي افتراضيًا، ولا يعود بصمت إلى وضع آخر — بل يحذّر على stderr. وتعتمد الحماية على قواعد `--deny` الخاصة بـCLI، بما في ذلك catch-all `*`، وهي الطبقة الوحيدة المقيسة التي تعمل بأسلوب _fail-closed_ (إذ تؤدي قاعدة ذات بادئة غير معروفة إلى رفض بدء التشغيل). ويسمح `GROK_TRANSLATE_SANDBOX=read-only` بفرض ذلك، وعندها يفشل بدء التشغيل إذا تعذر على الجهاز الوفاء به.
    - **عناصر الحماية**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (فوجود مفتاح سيحوّل الفوترة إلى فوترة حسب الاستخدام؛ كما أن `GROK_SANDBOX` موروثًا سيفرض ملفًا شخصيًا غير قابل للتطبيق مع رسالة مضللة)، وتُعطّل مفاتيح MCP/hooks/skills/agents، مع `--disable-web-search` و`--no-subagents` و`--no-plan`، ودليل عمل مؤقت، ورفض في CI، ومهلة زمنية تقتل مجموعة العمليات، وتراجع تدريجي عند rate limit. تُثبّت قيمة `--max-turns` على 6 لا على 1: إذ يُزاد العداد بعد جولة الأدوات، والقيمة 1 ستقتطع الخرج.
    - **الحصة**: تجمع حصة Grok أسبوعية و**مشتركة مع Chat وImagine وVoice**، ولا يكشفها أي أمر — خلافًا لـCodex، الذي يتيح `account/rateLimits/read` حساب الاستهلاك. لذلك يحد `regen_translations.sh` المنافسة إلى 2 ويصدر تحذيرًا صريحًا.
    - **الاختبارات**: ملف جديد باسم `tests/test_grok_provider.py` (24 اختبارًا). أصبح مجموع الاختبارات **290 اختبارًا**.
  - **خطأ مُصلح — كانت الاقتباسات الإنجليزية متعددة الفقرات محمية جزئيًا فقط (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل بوصفه جسم الاقتباس إلا سلسلة من أسطر `>` **المتتالية**. وعندما كان الاقتباس يغطي فقرات متعددة (تفصل بينها سطر `>` فارغ)، كان يُلتقط الجزء الأخير فقط ويُستبدل بالعنصر النائب؛ بينما كانت الأجزاء السابقة تذهب إلى LLM وتعود مترجمة — وهو العكس تمامًا لما وُجد `--news` لضمانه. يقبل التكرار الآن أسطر `>` الفارغة الداخلية، وأصبح غير جشع، ليتوقف عند `>` الفارغ الذي يسبق السطر المائل، لا عند أول سطر يُصادفه.
    - **الحجم المقيس** على مجموعة حقيقية من 198 مقالًا: 11 اقتباسًا من أصل 419 متأثرة. لا تراجع — إذ تلتقط regex الجديدة العدد نفسه تمامًا من الاقتباسات، ولا تتمدد إلا الأجسام متعددة الفقرات (408 أجسام متطابقة و11 جسمًا ممددًا)، كما لا يمكن لسطر الإسناد `> — …` أن يُمتص في الجسم (إذ جرى الحفاظ على lookahead).
    - **دليل شامل من البداية إلى النهاية** على مقال حجمه 69 كيلوبايت تُرجم إلى ja/ar: كانت الفقرة الأولى من اقتباس، التي كانت تُعرض سابقًا بصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم بالطريقة نفسها إلى العربية، تبقى الآن بصيغة `> GLM-5.3 is now open-weight.`. وعاد عدد أسطر الاقتباس بالإنجليزية من 9 إلى 10، وهو عدد أسطر المصدر.
    - تجدر الإشارة إلى أن هذا الخلل لم تكتشفه أدوات التحقق اللاحقة، التي تفحص وجود الاقتباس من دون التحقق من اكتماله.
  - **اقتصاد مقيس في provider الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ النموذج بـ`gpt-5`، بما في ذلك في `--eco`. ووفق القياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: `medium` → 45 رمز reasoning و65 رمز خرج؛ `none` → 0 و14. لا يضيف reasoning شيئًا إلى الترجمة، وكان يُدفع ثمنه في كل مقطع من كل ملف. أصبح الافتراضي `none` في `--eco`، ويظل `medium` خلاف ذلك؛ كما تبقى القيمة الصريحة الممررة عبر CLI ذات أولوية. يقبل `--reasoning_effort` الآن `none` و`xhigh` إضافةً إلى `low`/`medium`/`high` (ولا تقبل جميع النماذج هذه القيم: فمثلًا يرفض `minimal` القيمة `gpt-5.4-mini` — وتغطي إعادة المحاولة الحالية من دون المعامل هذه الحالة).
  - **تحديث SDK وترحيل Gemini**: استُبدل `google-generativeai` (انتهى دعمه في 2025-11-30، وأُرشف مستودعه) بـSDK الموحد **`google-genai`** — `genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير prompt النظام على هيئة `system_instruction` بدلًا من دمجه مع المقطع. انتقل `mistralai` إلى **2.9.4** (وأصبح الاستيراد `from mistralai.client import Mistral`؛ بينما يرفع القديم `ImportError`، وقد جرى التحقق من ذلك داخل wheel)، و`anthropic` إلى **0.125.0**، و`openai` إلى **2.54.0** — وهي آخر الإصدارات قبل الانتقال إلى `httpx2`، لتجنب التعايش بين حزمتَي HTTP داخل venv. وبناءً على ذلك، أُتيح `httpx` 0.28.1 و`pydantic` 2.13.5.
  - **تم اكتشاف تراجعين باختبارات حقيقية، لا بالوثائق**:
    - يرفض `anthropic` ≥ 1.0 من جهة العميل استدعاءً غير متدفق يتوقع `max_tokens` فيه مدة تتجاوز 10 دقائق (`ValueError: Streaming is required...`). لم تكن هذه الحماية موجودة في 0.34.2، وكانت تعطل كل استدعاء Claude باستخدام `max_tokens=32768`. عولج ذلك عبر `timeout` صريح (`CLAUDE_TIMEOUT`، ومدته الافتراضية 900 ثانية)، ما يتجنب التحول إلى streaming لاستدعاء لا نستخدم منه سوى الاستجابة الكاملة.
    - لا يقبل سوى جزء من كتالوج Gemini `thinking_level="minimal"`: إذ يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` برمز 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهي سلسلة `minimal` → `low` → لا شيء من thinking_config، على غرار fallback الخاص بـOpenAI الموجود أصلًا — فلا ينبغي لمعامل تحسين أن يتسبب أبدًا في فشل ترجمة.
  - **تجديد النماذج الافتراضية**، مع التحقق من كل نموذج عبر استدعاء حقيقي: OpenAI `gpt-5.5` → **`gpt-5.6-terra`** (−60٪ على دفعة من 28) و`gpt-5.4-mini` → **`gpt-5.6-luna`** (−73٪)؛ Claude `claude-sonnet-4-6` → **`claude-sonnet-5`** (أرخص وأحدث) و`claude-haiku-4-5-20251001` → **`claude-haiku-4-5`** (المعرّف الرسمي من دون تاريخ)؛ Gemini `gemini-3.1-pro-preview` → **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` → **`gemini-3.1-flash-lite`** (إصدار مستقر، وأرخص من `3.5-flash-lite`).
بقاء Mistral دون تغيير، يظل `mistral-large-latest` الأفضل من حيث القيمة مقابل السعر بين النماذج الأربعة. تجدر الإشارة إلى أنه لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — فلم يُطرح Gemini 3.5 Pro، الذي أُعلن عنه في مايو 2026؛ إذ تقتصر سلسلة 3.5/3.6/3.7 على Flash حصراً.
  - **قياس A/B قبل التحول إلى Gemini**: تُرجمت `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. وكانت البنية متطابقة تماماً (21 قائمة، و18 كتلة تعليمات برمجية، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، في **8 ثوانٍ مقابل 48 ثانية**. وبما أنه لم يكن هناك أي benchmark عام يقارن بين هذين النموذجين في الترجمة أو في البرامج النصية غير اللاتينية، فقد استند التحول بخلاف ذلك إلى مجرد افتراض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفذ `block.text for block in response.content` من دون تصفية النوع. وتُدرج نماذج الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، التي تعرض `.thinking` بدلاً من `.text` — وكانت الترجمة ستتعطل عند أول `AttributeError` مبهم. تُستبعد الآن الكتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` (قائمة استبعاد، للحفاظ على التسامح مع نوع غير معروف يحمل نصاً)، ويؤدي غياب أي كتلة نصية إلى رفع خطأ صريح. يُمرَّر `thinking={"type": "disabled"}` في كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: حذف النماذج التي تجاوز تاريخ سحبها (سلسلة `magistral-*` المسحوبة في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620` و`claude-3-7-sonnet-20250219` و`claude-opus-4-1-20250805` و`claude-sonnet-4-20250514`). تصحيحات الحدود: Mistral 128K → **256K** (توليدا Large 3 / Small 4)، وGemini من 1 000 000 → **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` من 200K → **1M**، وعائلة `gpt-5.6-*` من 400K → **1,05M**. أُضيف Claude 5 (`claude-sonnet-5` و`claude-opus-5` و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وسلسلة `ministral-*`. تجدر الإشارة إلى أن هذه الحدود تظل إرشادية، إذ يضع `translate()` حداً للتقسيم عند `min(16000, limite)`.

  - **المزوّد `--use_codex`**: المزوّد الخامس الذي يشغّل CLI Codex الرسمي (`codex exec`) في الوضع غير التفاعلي، بدلاً من استدعاء API تُحتسب تكلفته حسب الاستخدام. تُخصم الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقاً. وهذا هو المسار الوحيد الموثق من OpenAI لهذا الاستخدام: إذ تسرد مصفوفة التوفر حسب الخطة «Codex SDK, `codex exec`, and scriptable workflows» على أنها متاحة في Plus/Pro/Business/Enterprise، في حين أن رموز `~/.codex/auth.json` لا توثّق استدعاءات Platform API (ولا يقرأها هذا البرنامج النصي مطلقاً — إذ تظل المصادقة وتجديدها من اختصاص CLI).
  - **إتاحة تثبيت Codex الثنائي عبر pip، وليس عبر npm فقط**: يبحث `_resolve_codex_binary()` عن الملف الثنائي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي اعتماد SDK `openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عالمياً لاستخدام `--use_codex`. ولا تُضاف الحزمة إلى `requirements.txt`، إذ يبلغ حجم الملف الثنائي نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين رغم كونه مزوّداً اختيارياً. تم التحقق من المسار كاملاً: مع غياب `codex` عن `PATH`، يعثر الحل على الملف الثنائي المضمّن وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: يُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. ومن دون هذا الحاجز، كان من الممكن لمفتاح موجود في `.env` أن يحوّل Codex إلى الفوترة حسب الاستخدام من دون أي إشارة مرئية — وهو بالضبط ما أُنشئ هذا المزوّد لتجنبه.
  - **إحكام مطبات CLI بالاختبارات**:
    - يقرأ `codex exec` الإدخال من stdin **حتى** عندما يُمرَّر prompt كوسيط: ومن دون إغلاق stdin، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج مطلقاً (أُعيد إنتاج الحالة: exit 124 بعد 180 ثانية، وصفر بايت). لذلك يُعد `communicate(input=...)` إلزامياً.
    - إن `codex` المثبّت عبر npm هو shim لـ Node يقوم `spawn` بالملف الثنائي الحقيقي المكتوب بـ Rust؛ وهذا الأخير **حفيد** عملية Python ويظل قائماً بعد `SIGKILL` الخاص بـ`subprocess.run(timeout=)`، حيث سيواصل استهلاك الحصة. ومن هنا يأتي `Popen(start_new_session=True)` + `os.killpg`.
    - يمكن لـ CLI أن يخرج بالرمز 0 رغم إصداره `turn.failed`: لذلك يُفحص خرج JSONL (`--json`) إضافة إلى رمز الإرجاع، ويؤدي غياب ملف `-o` مع رمز 0 إلى رفع خطأ صريح بدلاً من إنتاج مقطع فارغ.
  - **التراجع عند تجاوز معدل الطلب**: لا يطبّق CLI أي إعادة محاولة داخلية (`max_retries = 0`). ويُجرى التصنيف اعتماداً على بنية حمولة JSON (`status: 429` / `error.type`)، لا على السلاسل الجزئية — فكلمة «quota» تظهر في كل من 429 القابل للاسترداد و`insufficient_quota` النهائي.
  - **حاجز CI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفاً. فالمصادقة بالاشتراك غير مخصصة لعامل تشغيل مشترك، كما أن OpenAI لا توصي صراحةً بهذا المسار في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (للجودة) و`gpt-5.6-luna` (`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وPlatform API، لكن حساب ChatGPT لا يملك حق الوصول إلى جميع أفرادها: تُطبَّق قائمة السماح على الخادم من دون تحقق محلي، ويؤدي النموذج غير المعتاد إلى إصدار تحذير. في خطة Plus، يوفّر Luna من 250 إلى 2,000 رسالة لكل نافذة مدتها 5 ساعات مقابل 10-100 لدى Sol: يُعد `--eco` الوضع الموصى به لأي معالجة دفعية.
  - **إصلاح خطأ — كان `regen_translations.sh` يخرج بخطأ رغم نجاح كامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` من `main()` لم يعد موجوداً عند تنفيذ trap. وفي ظل `set -u`، كان ذلك يرفع `failed_log: unbound variable` ويجعل البرنامج النصي يخرج بالرمز 1 رغم صحة الترجمات الثماني والعشرين — ما كان سيوقف `release.sh --auto` (`set -e`) مباشرة بعد إعادة التوليد، في أغلى مرحلة. أصبح المتغير عاماً، ويتحقق trap من وجوده. ومن الآثار الجانبية المفيدة أن إخفاقات الترجمة الحقيقية، التي كانت مخفية حتى الآن بسبب هذا الخطأ، أصبحت تظهر مجدداً في الملخص الختامي.
  - **`REGEN_MODEL`**: متغير بيئي جديد من `regen_translations.sh` يفرض نموذجاً محدداً فوق الإعداد الافتراضي للمزوّد، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج الأعلى جودة ضمن حصة الاشتراك بدلاً من نموذج `--eco` الموجّه إلى المعالجة الكمية.
  - **`regen_translations.sh`**: إتاحة `REGEN_PROVIDER=codex` عبر تفعيل صريح اختياري (ولا يُكتشف تلقائياً مطلقاً، حتى لا تُستهلك حصة الاشتراك من دون علم المستخدم). يُجدَّد الرمز مرة واحدة بشكل تسلسلي قبل فتح التوازي — إذ إن تجديد Codex دوّار ويُستخدم مرة واحدة، وكانت المهام المتزامنة ستبطل جلسة `codex login` — وتُخفض درجة التوازي إلى 4.
  - **إعادة الهيكلة المرتبطة**: ينتقل `_dispatch_provider_call` من 8 إلى 6 معاملات عبر `_resolve_provider()` يعيد اسم المزوّد، بدلاً من تمرير قيمة منطقية رابعة عبر السلسلة بأكملها. وتظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` أدنى.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` (48 اختباراً) يغطي argv، والبيئة المنقّاة، وعقد منع المقدمة، والإخفاقات الصامتة، والمهلة/killpg، والتراجع، وpreflight، وحلّ المزوّد، وتسلسل استدلال Gemini، وتصفية كتل Claude، واقتباسات الأخبار متعددة الفقرات. أصبحت المجموعة الكاملة تضم 290 اختباراً.
  - **التحقق الفعلي**: إن `README.md` الخاص بالمشروع، عند ترجمته إلى **14 لغة** عبر Codex، يعطي بنية متطابقة تماماً مع الترجمات المرجعية (14 كتلة تعليمات برمجية، و24 عنواناً، و25 سطراً في الجدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل تعليمات برمجية متطابقة حرفياً، ومن دون أي بقايا للـ placeholder). وفي مقال إخباري حجمه 69 كيلوبايت بوضع `--news`، تجتاز المخرجات `gpt-5.6-luna` و`gpt-5.6-sol` كلتاهما أداة التحقق التطبيقية اللاحقة في en/ja/ar. وقد بقي الاستهلاك المقاس عبر `account/rateLimits/read` دون حد التقريب في العداد (0% من نافذة 5 ساعات) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL للإسناد الإخباري مع الأقواس المتداخلة أو البادئة الفرنسية (2026-05-11):

  - **إصلاح خطأ**: كان استخراج عناوين URL للإسناد في `_protect_news_quotes` يستخدم التعبير النمطي `re.search(r"\((.+?)\)", attribution)` (التقاط كسول بين قوسين). وفي إسناد من النوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` الخارجية + `]()` الخاصة برابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة تتضمن البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائي). وكانت النتيجة أن `_validate_news_post` يبحث عن هذه السلسلة في الخرج المترجم ويفشل باستمرار (لسببين: `)` مبتور، وعبارة "relayé par" مترجمة إلى `relayed by`/`weitergeleitet von`/...). لذلك لم يكن من الممكن أن يمرّ التسلسل الكامل low → medium → high → gpt-5.5.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — إذ يستهدف تحديداً `](url)` الخاص برابط Markdown، ويلتقط **عنوان URL النقي فقط** (من دون البادئة الفرنسية أو الاقتطاع)، مع الحفاظ على الثبات بواسطة placeholders `#URL{N}#` أثناء الترجمة. وهو متين أمام النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس خارجية
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py`، ضمن الفئة `TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة المطابقة المعاد إنتاجها لخطأ Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (متغير يتضمن `via`)
  - **تغطية الفجوة**: يتحقق `check-editorial-coverage.py` من الصياغة التحريرية، لكنه لا يتحقق من قابلية الترجمة بواسطة translator. ومن التحسينات الممكنة (خارج نطاق الإصدار v1.9.2) إضافة فحص يحاكي استخراج الإسناد في dry-run لاكتشاف الأنماط المعرضة للمشكلات قبل النشر.

- **1.9.1** إصلاح i18n لوسم CTA في ملاحظة marker الخاصة بالترجمة (2026-05-10):

  - **إصلاح خطأ**: ظل وسم `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط marker أعلى الملفات المترجمة **بالفرنسية** لجميع اللغات المستهدفة، بدلاً من اتباع `target_lang`. ولا يراه LLM مطلقاً (إذ يُركّب على جانب Python للحفاظ على عنوان URL وslug المستودع)، لذلك لم تتمكن مرحلة الترجمة من تصحيحه. وكان ذلك تراجعاً صامتاً منذ إضافة التنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الخمس عشرة بوسمها المحلي. يمرّر `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` اللغة المستهدفة الآن. ويُستخدم fallback بقيمة `fr` إذا كانت اللغة غير معروفة (للأمان، ومنع KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link` (من target_lang `ja` → وسم ياباني متوقع). اختباران جديدان: `test_source_link_label_localized_per_target_lang` (مُعلَّم على 7 لغات تغطي النصوص اللاتينية والإيديوغرافية والأبجدية) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختباراً في `test_translation_note_position.py` (بدلاً من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع ذو قيمة افتراضية `target_lang="fr"` — يظل المستدعون البرمجيون الخارجيون من دون `args.target_lang` عاملين من دون تعديل.
- **1.9** إصلاح الفشل الصامت + مجموعة أدوات جودة كاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07) :
  - **ملاحظة ترجمة متعددة المواضع + تنسيق marker "embed card"** :
    - خيارات CLI جديدة (إضافية، مع بقاء القيم الافتراضية دون تغيير → **غير كاسرة للتوافق**) :
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): يضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`) :
        - `legacy` يعيد إنتاج سلوك v1.8 بدقة (فقرة عريضة `**…**`) **بايتًا ببايت**.
        - `marker` يُصدر تعريف مرجع رابط Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`)، يتبعه **blockquote من 3 فقرات** منظم لعرض من نوع "GitHub repo embed card": اسم المشروع بصيغة inline code (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف مترجم بواسطة LLM، ورابط CTA (`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. ويمكن استغلاله أثناء البناء بواسطة إضافة remark (راجع مدونة jls42.org → الإضافة `remark-translation-banner`).
    - **الثوابت التي لا تُرسل مطلقًا إلى LLM**: يُركّب اسم المستودع وعنوان URL الخاص بـ GitHub في Python بعد ترجمة الجملة الوصفية. لا يرى LLM مطلقًا slug `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، مما يضمن عدم تغيير أي renderer أو كاسر أو scheme.
    - **إدراج يراعي Frontmatter**: في الوضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة `---` الختامية** لـ frontmatter بصيغة YAML (لضمان أمان Astro Content Collections / gray-matter). يكتشف المساعد `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف ويحافظ على سلامته؛ ويثير `RuntimeError` عند وجود frontmatter مفتوح دون fence إغلاق (فيُرسل الملف إلى `failed_files` بدل كتابته مع ملاحظة في موضع خاطئ).
    - **Sanitizer للنموذج باستخدام whitelist**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع استخدام `unknown` كقيمة بديلة إذا كان فارغًا. ويتوافق ذلك مع أداة التحقق الموجودة في إضافة remark الخاصة بـ Astro، ويعطّل المحارف التي قد تكسر تنسيق marker (المسافة، وعلامة الاقتباس، والقوس، والفاصلة، وغيرها).
    - **إعادة هيكلة داخلية**: من `_append_translation_note` (دالة ضخمة واحدة) إلى 7 مساعدين نقيين (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فُصلت عملية البناء عن التركيب (يعيد builder كتلة نقية بلا فاصل، بينما يطبّق composer قيم `\n\n` بحسب الموضع)؛ كما يشترك الإنتاج والمساعد المصدر في نفس المُجمّع ذي الفقرات الثلاث.
    - **`_quote_lines` مع الحفاظ على الأسطر الفارغة**: يسبق كل سطر بـ `> `، محولًا الأسطر الفارغة إلى `>` فقط. ويسمح ذلك لـ mdast برؤية 3 فقرات منفصلة داخل blockquote (العنوان / الوصف / الرابط) بدل فقرة واحدة تحتوي على فواصل أسطر.
    - **`_build_translation_note_block` متكيف**: وفقًا لعدد الفقرات التي حافظ عليها LLM (3 = تنسيق البطاقة الكامل، 2 = العبارة + الرابط، 1 = البديل). لم يعد البديل ذو الفقرة الواحدة **يغلّف بـ `**...**`** عند اكتشاف رابط Markdown `](` (بسبب العرض الهش لـ `<strong>` حول الرابط).
    - **التوافق مع الإصدارات السابقة**: يواصل `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` العمل على جانب `_compose_with_notes` — إذ تستمر Namespace التي لا تحتوي على هذه السمات (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) في العمل دون تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة** :
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور على مقتطف المصدر حرفيًا) + طبقة احتمالية (`langdetect`).
    - قائمة السماح `finish_reason` / `stop_reason`: إطلاق `RuntimeError` عند أي حالة خارج قائمة السماح (الاقتطاع، وcontent_filter، وغير ذلك).
    - `max_tokens` في Claude: من `4096` إلى `32768` (لتجنب الاقتطاع الكامن في المقاطع ذات 16k، مع هامش للانتقال بين النصوص FR→JA/ZH/KO/AR/HI).
    - تقسيم يراعي العناوين: إعطاء الأولوية لـ H2/H3 في النصف الثاني من المقطع (بحيث يبدأ كل مقطع بقسم دلالي كامل).
    - تمرير الأخطاء حتى رمز الخروج غير الصفري: يعيد `translate_markdown_file` حالة نمطية `success` / `failure` / `skipped`، ويعيد `main()` `sys.exit(1)` إذا فشل ملف واحد على الأقل (في الوضع الفردي ووضع الدفعة).
    - حارس المحتوى الفارغ لدى جميع المزوّدين، ونسبة سلامة المصدر/المخرج (≥ 500 محرف، ورفض إذا كانت < 5%)، والتحقق من placeholders البرمجية (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، و`BadRequestError` لإعادة المحاولة دون `reasoning_effort`.
    - إضافة التبعية `langdetect==1.0.9`.
  - **أدوات جودة ما قبل الالتزام** ("نوع EurekAI الكامل"، 14 hook) :
    - Pre-commit: ‏ruff (lint+format)، وshellcheck، وprettier (md/yaml/json)، وdetect-secrets (4 مفاتيح API محمية)، وLizard (CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، ونهاية الملف، والملفات الكبيرة، وshebangs، وغير ذلك).
    - Pre-push: ‏mypy (وضع lax تدريجي)، وOpengrep SAST (translate.py + scripts/)، وpip-audit (وضع إعداد التقارير الأولي)، وunittest discover (tests/ + scripts/tests/).
    - أغلفة محلية داخل `scripts/` تستخدم `./venv/bin/python`.
    - `scripts/audit_verdict.py`: محلل JSON لـ pip-audit مع 11 اختبار unittest، وهو إصدار Python من محلل jls42-astro.
    - إصلاح 7 مخالفات أولية لـ ruff: ‏B904 (raise from) ×2، وB007 (دوال dirs غير المستخدمة)، وC408 (حرفية dict)، وC419 (list-comp)، وSIM105 (contextlib.suppress)، وSIM110 (any()).
    - يستثني Lizard مؤقتًا `translate.py` (4 دوال بدرجات CCN من 21 إلى 47، وإعادة الهيكلة مخطط لها) — مع تفعيل البوابة الصارمة على scripts/.
  - **SonarCloud وتغطية شاملة** :
    - سير عمل GitHub Actions `SonarCloud` (sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع التغطية عبر `coverage.xml`.
    - 11 شارة SonarCloud في أعلى README (Quality Gate، وتصنيفات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code).
    - `tests/test_silent_failure.py` (`unittest` من stdlib): يغطي حلقات السلسلة الستة الخاصة بخطأ الفشل الصامت.
    - `tests/test_orchestration.py` (+79 اختبارًا): يغطي طبقة تنسيق `translate.py` (`_resolve_*_filename`، و`_existing_translation_exists`، و`_record_translation_status`، و`_write_output_file`، و`translate_directory`، و`_validate_input_paths`، و`_init_*_client`، و`_select_provider_client`، و`_normalize_collapsed_markdown`، و`_cleanup_source_flag`، و`_validate_news_flags_*`، و`_openai_create_with_fallback` TypeError + BadRequestError fallbacks، وتنسيق prompt لسلسلة o1، وفروع early-return في `_validate_translation_output`).
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` (stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess.
    - **التغطية على الشيفرة الجديدة**: من 75.5% إلى نحو 98% (translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%).
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك E2E `marker+top|bottom|both` و`legacy+top|bottom|both`)، وإضافة البادئة متعددة الأسطر، والتوافق byte-for-byte (golden literal)، وsanitizer، وتقسيم frontmatter (بما في ذلك إطلاق الخطأ عند عدم إغلاق fence)، وتنسيق الفقرات الثلاث، والبديل ذي الفقرتين، وحارس الفقرة الواحدة + رابط Markdown، وحاجزًا حرجًا `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان وURL لا يُرسلان مطلقًا إلى LLM. **نجح 190 اختبارًا**، دون أي تراجع.
  - التوثيق: `README.md` (بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` (سير عمل pre-commit ومراقبة CI مفصلة)، وإعادة توليد 28 ترجمة.
- **1.8** وضع `--news` + تحديث النماذج لعام 2026 (2026-03-17، الوسم `v1.8`) :
  - تحديث النماذج الافتراضية (مارس 2026) :
    - جودة OpenAI: من `gpt-5` إلى `gpt-5.4`
    - اقتصادية OpenAI: من `gpt-5-mini` إلى `gpt-5.4-mini`
    - جودة Gemini: من `gemini-3-pro-preview` إلى `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` (400k) و`gemini-3.1-pro-preview` (1M).
  - الوضع الأولي `--news`: حماية الاقتباسات EN باستخدام placeholders `#NEWSQUOTE\d+#`، وتعيين `LANG_FLAGS` (15 لغة)، وإدارة الأعلام بحسب اللغة الهدف.
  - التحقق من placeholders الخاصة بالأخبار قبل الاستعادة (تراجع: كان LLM يحذف placeholder فينتج بصمت مخرجًا بلا اقتباس).
  - جعل السكربت `regen_translations.sh` قابلًا للنقل (مسارات مطلقة، دون اعتماد على pwd).
  - إضافة رابط Français إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة.
- **1.7** مستجدات :
  - خيار `--keep_filename` للحفاظ على اسم الملف الأصلي أثناء الترجمة.
  - دعم الملف `.env` لتحميل مفاتيح API تلقائيًا.
  - **الحفاظ على inline code**: أصبحت علامات backticks (`` `...` ``) محمية الآن أثناء الترجمة.
  - تحسين system prompt :
    - إدارة أفضل لعلامات الاقتباس في YAML frontmatter.
    - حماية متغيرات القالب `{variable}`.
    - منع ملاحظات المترجم غير المطلوبة.
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org).
- **1.6** مستجدات :
  - دعم Google Gemini API للترجمة (`--use_gemini`).
  - تحديث النماذج الافتراضية لعام 2026 :
    - OpenAI: `gpt-5` (جودة)، `gpt-5-mini` (اقتصادي)
    - Claude: `claude-sonnet-4-5` (جودة)، `claude-haiku-4-5` (اقتصادي)
    - Gemini: `gemini-3-pro-preview` (جودة)، `gemini-3-flash-preview` (اقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة.
  - ترجمة ملف واحد (`--file`) دون استعراض مجلد.
  - نمط تسمية جديد مبسط: `{base}-{lang}.md`.
  - خيار `--include_model` للحفاظ على التنسيق القديم الذي يتضمن اسم النموذج.
  - دعم النماذج غير المدرجة مع حد رموز افتراضي (128k).
  - ترجمة README إلى 14 لغة.
- **1.5** تحسينات :
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** تحديث `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** تحديث `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` وتحديث `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين prompts الترجمة:**
    - أُثريت prompts الخاصة بالترجمات المباشرة وملاحظات الترجمة لتحسين الوضوح والكفاءة، بما في ذلك تعليمات مفصلة للحفاظ على metadata وعناصر التنسيق المحددة.
  - **إعادة هيكلة الشيفرة:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم imports لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص وإدارة كتل الشيفرة للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات المخرج:**
    - عكس النموذج واللغة في أسماء ملفات المخرج (مثل `f"{base}-{args.target_lang}-{args.model}.md"`)، مما يسهّل تنظيم الترجمات والعثور عليها.
  - **تحسينات متنوعة:**
    - تنظيف الشيفرة بحذف الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكربت وقابليته للقراءة.
- **1.4** مستجدات :
  - دعم Anthropic Claude API للترجمة.
  - تحسين prompts لزيادة الوضوح والكفاءة.
  - تعديلات طفيفة لتحسين صيانة الشيفرة.
- **1.3** تحسينات وميزات جديدة :
  - تحسين إدارة كتل الشيفرة.
  - تحسين إدارة ملفات المخرج.
  - تحسين اكتشاف الملفات الموجودة.
  - خيار `--force` لفرض الترجمة.
  - عكس النموذج واللغة في اسم ملف المخرج.
- **1.2** إصلاح changelog.
- **1.1** إضافة دعم Mistral AI API.
- **1.0** الإصدار الأولي - دعم OpenAI API.

**مقال مترجم من الفرنسية إلى العربية باستخدام gpt-5.6-luna.**
