### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.10.0** موفّر `--use_codex` (حصة اشتراك ChatGPT)، وتحديث حزم SDK والنماذج، وإصلاح اقتباسات الأخبار متعددة الفقرات (2026-08-29):

  - **إصلاحات ناتجة عن مراجعة طلب الدمج هذا** — فحص خمسة وكلاء مراجعة الفروقات بدقة؛ وقد **أُعيد إنتاج جميع** النقاط أدناه بالقياس قبل إصلاحها، وكانت اثنتان منها تراجعات أُدخلت في موضع سابق من الإصدار نفسه.

    - **إصلاح تراجع — كان `_NEWS_CITATION_REGEX` يعاني من تراجع خلفي أُسّي.** أدخل إصلاح الفقرات المتعددة `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: فتقاسم المسافات بين `[ \t]+` و`.*` ملتبس، ويتضاعف هذا الالتباس من تكرار إلى آخر. جرى القياس على أسطر `>   texte` — وهي إزاحة بادئة مشروعة تمامًا في Markdown — لا تطابق النمط: **2,589 مللي ثانية لـ14 سطرًا** مقابل 0.04 مللي ثانية بعد الإصلاح، بمعامل يقارب 9 لكل سطر مُضاف. في وضع `--news`، كان اقتباس كتلي طويل وغير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب قابل للتحديد. يستهلك التكرار الآن السطر بأكمله دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يترك سوى طريقة واحدة للمطابقة في كل تكرار. جرى التحقق على المجموعة الفعلية المكوّنة من 231 مقالة: **صفر اختلاف** في الالتقاط، ونفس الاقتباسات البالغ عددها 423، مع استمرار توسيع الأجسام الأربعة عشر متعددة الفقرات.
    - **كان تفعيل علامتَي موفّر في الوقت نفسه يؤدي بصمت إلى الفوترة حسب الاستخدام.** كان `--use_codex --use_mistral` مقبولًا؛ يختبر `_select_provider_client` ‏Mistral أولًا، ويعطي `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. وهكذا كان المستخدم يطلب استخدام حصة اشتراكه ويحصل بدلًا منها على فوترة حسب الاستخدام من دون أي تحذير: وهذا بالضبط نمط الإخفاق الذي وُجد `--use_codex` لمنعه. تمر علامات الموفّرين الست الآن عبر `add_mutually_exclusive_group` واحد. **تغيير في السلوك**: يفشل الآن سطر أوامر يجمع بين موفّرَين، بعدما كان يُقبل سابقًا بصمت، عند `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة اكتمال العمل تتحول إلى اللون الأخضر عندما تتعطل مجسّاتها.** كانت أربعة من فحوص `scripts/check-release-ready.sh` الثلاثة عشر تتبع نمط «التقاط stdout، والاستنتاج إن كان فارغًا» من دون التحقق قط من رمز الخروج: كان الاستثناء (ملف أُعيدت تسميته، `FileNotFoundError`) يكتب إلى stderr، ويترك stdout فارغًا، ثم يخلص الفحص إلى أنه «لا شيء يستدعي الإبلاغ». وهكذا أُعيد إنتاج فخ «إن `exit 0` لا يثبت شيئًا» داخل السكربت المكتوب لمنعه. يفرض المساعد `probe()` الآن رمز خروج صفريًا **و**علامة نهاية، وترفض المجسّات الاستنتاج استنادًا إلى مجموعة فارغة من العلامات — إذ إن التوكيد على مجموعة فارغة يكون صحيحًا دائمًا. توضيحًا لذلك: أدى إدخال المجموعة الحصرية أعلاه إلى تمرير علامات الموفّرين عبر كائن `*_group`، فلم يعد التعبير النمطي القديم `parser\.add_argument\(` يطابقها؛ وخرجت **ست علامات من أصل إحدى وعشرين** بصمت من نطاق الفحص، مع بقاء البوابة خضراء.
    - **كان فحص الأسرار يفوّت أربعة موفّرين من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: لذا كان `sk-proj-…` (تنسيق OpenAI الحالي) و`sk-ant-api03-…` ينقطعان عند الشرطة الثانية، ولم يكن `AIza…` مشمولًا. وُسّع النمط، واستُبعد `.secrets.baseline` من الفحص. كذلك كانت الحماية `.env` تستعلم عن `git diff --cached`، الذي لا يرى سوى الفهرس: لذلك لم يكن `.env` **الذي سبق إيداعه** — وهو أسوأ سيناريو — يظهر فيه أبدًا. وهي تستعلم الآن عن `git ls-files`.
    - **لم تكن «تهيئة الرمز المميز» في Codex تهيئة فعلية.** أظهر القياس أن `codex login status` لا يلمس `~/.codex/auth.json` (ظل وقت التعديل والحجم بلا تغيير)، وتقول صفحة مساعدته «عرض حالة تسجيل الدخول». ومع ذلك كان التعليق يدّعي تحديث الرمز المميز «مرة واحدة، تسلسليًا»، ومن ثم تحييد خطر عمليات التحديث المتزامنة لرمز مميز دوّار صالح لاستخدام واحد. لم تكن الحماية المعلنة موجودة؛ وأصبح التعليق الآن يصف ما يفعله الكود فعلًا، بينما يظل الإجراء الوقائي الحقيقي هو `max_jobs=4`. كما يحترم الفحص `CODEX_BIN`، الذي كان يتجاهله — إذ كان جهاز لا يحتوي `codex` في `PATH` يفشل برسالة «غير مصادق عليه»، وهو تشخيص مضلل.
    - **كان `.env` يُحمّل داخل صدفة فرعية.** تُستدعى `detect_provider` عبر إحلال الأوامر، ولذلك لم تكن متغيراتها المصدّرة تصل إلى الأعلى: فكان `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` المعرّف في `.env` يظل غير مرئي لعمليات القراءة المنفذة في `main()`، التي كانت تخلص إلى «تعذر العثور على الملف التنفيذي لـGrok» رغم صحة الإعداد.
    - **كان التزامن يتجاوز الحد المعلن بنسبة 50%.** وُضعت الحماية بعد تشغيل زوج README/CHANGELOG: وبلغت الذروة المقاسة **3 عند `max_jobs=2`**. وفي Grok، الذي يتقاسم حصته الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يكن السكربت يلتزم بالحد الذي فرضه على نفسه. أما العدد النهائي، فكان يُعرض من دون مقارنته مطلقًا بالعدد 28 — ولذلك كان غياب ملف يمر من دون اكتشاف.
    - **عقد مخرجات Grok: أصبح غياب `stopReason` إخفاقًا.** كان الكود يطبّق «`end_turn` **أو غائب**» في موضع يقتضي فيه العقد المعلن `end_turn`. كانت حمولة بلا هذا الحقل — أو يُعاد فيها تسمية الحقل بعد تحديث CLI — تحوّل الحماية إلى عملية لا تفعل شيئًا بصمت. كذلك لم يعد `max_turn_requests` مصنّفًا على أنه بلوغ لحد المعدل (فالذي نفد هو ميزانية الأدوار: تؤدي إعادة المحاولة إلى النتيجة نفسها بتكلفة انتظار قدرها 90 ثانية)، وأُزيل `quota` من علامات بلوغ حد المعدل — للسبب الذي كانت سلسلة توثيق `_codex_is_rate_limited` توضحه بالفعل من دون أن يطبقه Grok.
    - **يُحفظ تسلسل Gemini مؤقتًا بحسب النموذج.** كان يبدأ مجددًا من `minimal` عند كل مقطع، مع أن النموذج الافتراضي يرفضه: وهكذا كان المسار المعتاد يدفع تكلفة رحلة ذهاب وإياب باستجابة 400 لكل مقطع ويعيد طباعة التحذير نفسه. والتحذير الذي يتكرر مئات المرات لا يعود يُقرأ — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مكتوبة نصيًا خصيصًا لـCodex، وتوجّه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق أساس العمليات الفرعية يعمم مفهوم «shim» على أداتَي CLI، مع أن الملف التنفيذي لـGrok هو ملف ELF أصلي (والتبرير الصحيح هو «وكيل ينشئ عملياته الفرعية الخاصة»)؛ وُسِمت اثنتا عشرة نتيجة SAST في `subprocess` بالوسمين `# nosec` / `# nosemgrep` مع تقديم مبرر، إذ إن صيغة القائمة من دون `shell=True` تجعل الحقن مستحيلًا، كما أن محتوى المستند لا يمر عبر argv مطلقًا.
    - **لم يعد أي سر يدخل العمليات الفرعية الوكيلية.** لم تكن قوائم المنع الاسمية تحمي سوى ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). وأظهر القياس أن **سبعة أسرار أخرى** كانت تدخل رغم ذلك كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح أداة CLI الأخرى، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة البيانات. غير أن أداتَي CLI هاتين **وكيلان**، ويعمل وكيل Grok من دون sandbox على مستوى نظام التشغيل يمكن تطبيقه على كثير من أجهزة Linux. أصبحت التصفية تُجرى الآن **بحسب نمط الاسم** (`API_KEY`، و`_TOKEN`، و`SECRET`، و`PASSWORD`، و`CREDENTIALS`) لا بحسب قائمة اسمية، مما يشمل أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن يعرفها هذا الكود. ولا تحتاج أدوات CLI إلى أي منها: إذ توجد المصادقة في `~/.codex` و`~/.grok`، وليس في البيئة مطلقًا — وقد جرى التحقق عبر **ترجمة فعلية مكتملة** باستخدام كل من الموفّرَين مع البيئة المشددة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يثبّت حصرية علامات الموفّرين، وعقد `stopReason`، وخطية التعبير النمطي للأخبار، ورسالة الرفض في CI، والحفظ المؤقت في Gemini، وغياب أي سر من بيئة العمليات الفرعية. وهذا التوكيد الأخير **عام** — إذ يفشل عند وجود مفتاح لا تسميه أي قائمة — بخلاف اختبار التنقية الموجود، الذي كان مرآة لثابته ولم يكن قادرًا على اكتشاف أي شيء سوى تعطل حلقته الخاصة. أصبح مجموع الحزمة الكاملة **311 اختبارًا**.
  - **موفّران جديدان لـ Grok**: ‏`--use_grok` (واجهة xAI API، المفتاح `XAI_API_KEY`، بفوترة حسب الاستخدام) و`--use_grok_cli` (واجهة Grok Build CLI الرسمية، يُخصم استخدامها من اشتراك Grok — بالمبدأ نفسه الذي يتبعه `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن نقطة نهاية xAI متوافقة مع OpenAI، يُعاد استخدام العميل و`_call_openai` كما هما، ولا يتغير سوى `base_url`. لم يلزم سوى تكييف واحد يستفيد منه الجميع: يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي تصدرها xAI حيث تصدر OpenAI الصيغة `stop`. النماذج: `grok-4.6` (للجودة) و`grok-4.3` (للاقتصاد). تجدر الإشارة إلى أن نموذج Grok الاقتصادي يظل الأغلى في المستودع — ‏$1.25/$2.50 لكل مليون مقابل $0.15/$0.60 للنموذج `mistral-small-latest`: يُختار هذا الموفّر لتنوّع النماذج، لا للسعر.
    - **وضع CLI**: صُمّم على غرار Codex، مع أربعة اختلافات فرضها الواقع — تُمرّر المطالبة عبر ملف (`--prompt-file`، إذ لا تقرأ واجهة CLI من stdin، كما أن مقطعًا في argv سيكون ظاهرًا في `ps`)، ويكون الخرج كائن JSON واحدًا على stdout (لا JSONL ولا ملف `-o`)، ولا يتيح الاشتراك سوى `grok-4.6` و`grok-4.5`، كما لا يمكن تطبيق sandbox (انظر أدناه). جرى توحيد تشغيل العمليات الفرعية مع Codex داخل `_codex_run_process`، من دون المساس ببقية موفّر Codex الذي سبق اختباره.
    - **‏`exit 0` لا يثبت شيئًا، وفقًا للقياس**: عند عدم المصادقة، تكتب واجهة CLI ‏`{"type":"error","message":"Not signed in."}` على **stdout** مع رمز خروج **0**. ويتصرف الرفض أو تجاوز عدد الجولات بالطريقة نفسها. لذلك يتطلب عقد الخرج تحقق أربعة شروط في آن واحد: رمز خروج 0، وغياب حمولة خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع الفحص المسبق المنطق نفسه: يخرج `grok models` بالرمز 0 حتى عند قطع الاتصال، ولا يمكن الاستنتاج إلا من وجود «غير مصادَق عليه» في stdout.
    - **الاحتواء: تفاوت مقصود وموثّق.** بينما يعمل Codex ضمن `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـ Grok على كثير من أجهزة Linux الحديثة، لسببين مستقلين على مستوى النظام لا يمكن تجاوزهما من دون `sudo`: يمنع AppArmor مساحات أسماء المستخدمين غير المميّزة منذ Ubuntu 24.04 ‏(`bwrap: setting up uid map: Permission denied`، وأُعيد إنتاج المشكلة خارج Grok)، كما تفشل قائمة منع مقابس وقت تشغيل الحاويات عندما يكون `/run/podman` في `0700` (لا يعالج المحلّل سوى `ErrorKind::NotFound`، بينما يصبح EACCES خطأً فادحًا). الفخ الأساسي: ملف تعريف **مدمج** يتعذر تطبيقه **يبدأ بصمت من دون احتواء**. لذلك لا يطلب السكربت أي ملف تعريف افتراضي، ولا يلجأ بصمت إلى بديل أبدًا — بل يصدر تحذيرًا على stderr. وتعتمد الحماية على قواعد `--deny` في واجهة CLI، بما فيها القاعدة الشاملة `*`، وهي طبقة الحماية الوحيدة التي ثبت بالقياس أنها _تفشل مغلقة_ (إذ تؤدي قاعدة ذات بادئة مجهولة إلى رفض بدء التشغيل). يتيح `GROK_TRANSLATE_SANDBOX=read-only` فرض استخدامها، وعندئذ يفشل بدء التشغيل إذا تعذر على الجهاز الالتزام بها.
    - **ضوابط الأمان**: تُحذف `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (إذ قد يؤدي وجود مفتاح إلى التحول إلى الفوترة حسب الاستخدام؛ كما أن `GROK_SANDBOX` موروثًا سيفرض ملف تعريف غير قابل للتطبيق مع رسالة مضللة)، وتُعطّل مفاتيح MCP/hooks/skills/agents، مع `--disable-web-search` و`--no-subagents` و`--no-plan`، ودليل عمل مؤقت، ورفض التشغيل في CI، ومهلة تقتل مجموعة العمليات، وتراجع تدريجي عند بلوغ حد المعدّل. ضُبط `--max-turns` على 6 لا على 1: إذ يُزاد العداد بعد جولة الأدوات، وستؤدي القيمة 1 إلى اقتطاع الخرج.
    - **الحصة**: مجمّع Grok أسبوعي و**مشترك مع Chat وImagine وVoice**، ولا يكشفه أي أمر — بخلاف Codex الذي يتيح `account/rateLimits/read` فيه تحديد الاستهلاك بالأرقام. لذلك يحد `regen_translations.sh` التزامن إلى 2 ويصدر تحذيرًا صريحًا.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` (24 اختبارًا). المجموعة الكاملة تضم **290 اختبارًا**.
  - **إصلاح خلل — لم تكن الاقتباسات الإنجليزية متعددة الفقرات محمية إلا جزئيًا (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل، بوصفه متنًا للاقتباس، إلا سلسلة من أسطر `>` **المتتالية**. وما إن يمتد اقتباس عبر عدة فقرات (تفصل بينها سطر `>` فارغ) حتى لا تُلتقط وتُستبدل بالعنصر النائب إلا الفقرة الأخيرة؛ أما الفقرات السابقة فكانت تُرسل إلى LLM وتعود مترجمة — وهو النقيض التام لما صُمّم `--news` لضمانه. بات التكرار يقبل الآن أسطر `>` الداخلية الفارغة وأصبح غير جشع، ليتوقف عند سطر `>` الفارغ الذي يسبق السطر المكتوب بالمائل، لا عند أول سطر فارغ يصادفه.
    - **الحجم المقاس** على مجموعة فعلية من 198 مقالًا: تأثر 11 اقتباسًا من أصل 419. لا تراجعات — يلتقط التعبير النمطي الجديد العدد نفسه تمامًا من الاقتباسات؛ وحدها المتون متعددة الفقرات توسعت (408 متون متطابقة و11 متنًا موسعًا)، وما زال يتعذر ابتلاع سطر الإسناد `> — …` داخل المتن (مع الإبقاء على النظرة الاستباقية).
    - **إثبات شامل** على مقال بحجم 69 كيلوبايت تُرجم إلى اليابانية والعربية: تظل الفقرة الأولى من أحد الاقتباسات الآن `> GLM-5.3 is now open-weight.`، بعدما كانت تُعرض سابقًا بصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم كذلك إلى العربية. وعاد عدد أسطر الاقتباس الإنجليزية من 9 إلى 10، وهو عددها في المصدر.
    - تجدر الإشارة إلى أن أدوات التحقق اللاحقة لم تكتشف هذا العيب، لأنها تتحقق من وجود الاقتباس من دون التأكد من اكتماله.
  - **توفير مقاس لدى الموفّر الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` متى بدأ اسم النموذج بـ `gpt-5`، بما في ذلك عند استخدام `--eco`. قياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: ‏`medium` ← ‏45 رمز استدلال و65 رمز خرج؛ ‏`none` ← ‏0 و14. لا يضيف الاستدلال شيئًا إلى الترجمة، ومع ذلك كانت تكلفته تُدفع عن كل مقطع من كل ملف. أصبحت القيمة الافتراضية `none` في `--eco` وتظل `medium` في الحالات الأخرى؛ وتبقى للقيمة الصريحة الممررة عبر CLI الأولوية. يقبل `--reasoning_effort` الآن `none` و`xhigh` بالإضافة إلى `low`/`medium`/`high` (ليست جميعها مقبولة لدى كل النماذج: فعلى سبيل المثال يرفض `gpt-5.4-mini` القيمة `minimal` — ويغطي ذلكَ إعادةُ المحاولة الحالية من دون المَعلمة).
  - **تحديث حزم SDK وترحيل Gemini**: استُبدلت `google-generativeai` (انتهى دعمها في 2025-11-30 وأُرشف مستودعها) بحزمة SDK الموحدة **`google-genai`** — ‏`genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير مطالبة النظام في `system_instruction` بدلًا من دمجها مع المقطع. انتقلت `mistralai` إلى الإصدار **2.9.4** (وأصبح الاستيراد `from mistralai.client import Mistral`؛ أما القديم فيثير `ImportError`، كما جرى التحقق داخل حزمة wheel)، و`anthropic` إلى **0.125.0**، و`openai` إلى **2.54.0** — وهي أحدث الإصدارات قبل الانتقال إلى `httpx2`، تجنبًا لتشغيل مكدّسي HTTP معًا في venv. وبناءً على ذلك أُزيل الحظر عن `httpx` 0.28.1 و`pydantic` 2.13.5.
  - **تراجعان كشفت عنهما اختبارات فعلية، لا الوثائق**:
    - ترفض `anthropic` بالإصدار 1.0 فأحدث من جهة العميل استدعاءً غير متدفق يوحي `max_tokens` فيه بمدة تتجاوز 10 دقائق (`ValueError: Streaming is required...`). لم يكن هذا الضابط موجودًا في 0.34.2، وكان يعطّل كل استدعاء لـ Claude باستخدام `max_tokens=32768`. أُصلح ذلك بإضافة `timeout` صريحة (`CLAUDE_TIMEOUT`، والقيمة الافتراضية 900 ثانية)، مما يغني عن التحول إلى البث لاستدعاء لا يُستخدم منه سوى الرد الكامل.
    - لا تقبل `thinking_level="minimal"` إلا بعض نماذج كتالوج Gemini: يدعمها `gemini-3.1-flash-lite`، بينما يرفضها `gemini-3.7-flash` و`gemini-3.1-pro-preview` بخطأ 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تسلسل تراجعي `minimal` ← ‏`low` ← من دون thinking_config، على غرار آلية OpenAI الاحتياطية الموجودة سلفًا — إذ يجب ألا تتسبب مَعلمة تحسين في إفشال ترجمة مطلقًا.
  - **تجديد النماذج الافتراضية**، وقد جرى التحقق من كل واحد باستدعاء فعلي: ‏OpenAI ‏`gpt-5.5` ← **`gpt-5.6-terra`** (−60% على دفعة من 28) و`gpt-5.4-mini` ← **`gpt-5.6-luna`** (−73%)؛ ‏Claude ‏`claude-sonnet-4-6` ← **`claude-sonnet-5`** (أقل تكلفة وأحدث) و`claude-haiku-4-5-20251001` ← **`claude-haiku-4-5`** (المعرّف القياسي من دون تاريخ)؛ ‏Gemini ‏`gemini-3.1-pro-preview` ← **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` ← **`gemini-3.1-flash-lite`** (الإصدار المستقر، وهو أقل تكلفة من `3.5-flash-lite`). لم يتغير Mistral، إذ يظل `mistral-large-latest` أفضل النماذج الأربعة من حيث القيمة مقابل السعر. تجدر الإشارة إلى عدم وجود أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — إذ لم يصدر قط Gemini 3.5 Pro، الذي أُعلن عنه في مايو 2026؛ وسلسلة 3.5/3.6/3.7 مخصصة حصريًا لـ Flash.
  - **اختبار A/B مقاس قبل نقل Gemini**: ترجم `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. كانت البنية متطابقة تمامًا (21 قائمة، و18 كتلة شيفرة، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، خلال **8 ثوانٍ مقابل 48 ثانية**. ونظرًا إلى عدم وجود أي اختبار معياري عام يقارن هذين النموذجين في الترجمة أو النصوص غير اللاتينية، لكان الانتقال قائمًا على مجرد افتراض لولا هذا القياس.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفّذ `block.text for block in response.content` من دون تصفية النوع. تُدرج النماذج ذات الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، تعرض `.thinking` لا `.text` — وكان ذلك سيعطّل الترجمة بخطأ `AttributeError` مبهم عند أول مقطع. باتت كتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` تُستبعد الآن (باستخدام قائمة سلبية للحفاظ على التسامح مع نوع مجهول يحمل نصًا)، ويؤدي الرد الذي لا يحتوي أي كتلة نصية إلى خطأ صريح. يُمرر `thinking={"type": "disabled"}` مع كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: حُذفت النماذج التي انقضى تاريخ سحبها (سلسلة `magistral-*` المسحوبة في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620`، و`claude-3-7-sonnet-20250219`، و`claude-opus-4-1-20250805`، و`claude-sonnet-4-20250514`). وصُححت الحدود: ‏Mistral ‏128K ← **256K** (جيل Large 3 / Small 4)، وGemini ‏1 000 000 ← **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` ‏200K ← **1M**، وعائلة `gpt-5.6-*` ‏400K ← **1.05M**. وأُضيفت Claude 5 ‏(`claude-sonnet-5` و`claude-opus-5` و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وسلسلة `ministral-*`. تجدر الإشارة إلى أن هذه الحدود تظل إرشادية، إذ يضع `translate()` حدًا أقصى للتقسيم يبلغ `min(16000, limite)`.
  - **المزوّد `--use_codex`**: المزوّد الخامس الذي يشغّل Codex CLI الرسمي (`codex exec`) في الوضع غير التفاعلي، بدلًا من استدعاء API تُحاسَب حسب الاستخدام. تُخصم الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقًا. وهذه هي الطريقة الوحيدة التي توثّقها OpenAI لهذا الاستخدام: إذ تسرد مصفوفة الإتاحة حسب الخطة «Codex SDK و`codex exec` ومسارات العمل القابلة للبرمجة» باعتبارها متاحة في Plus/Pro/Business/Enterprise، في حين أن رموز `~/.codex/auth.json` لا تصادق على استدعاءات API Platform (ولا يقرأها هذا السكربت مطلقًا — إذ تظل المصادقة وتجديدها تحت إدارة CLI).
  - **إمكانية تثبيت ملف Codex التنفيذي عبر pip، وليس npm فقط**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK ‏`openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عام لاستخدام `--use_codex`. لم تُضف الحزمة إلى `requirements.txt`: يبلغ حجم الملف التنفيذي نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين من أجل مزوّد اختياري. جرى التحقق من البداية إلى النهاية: عند غياب `codex` عن `PATH`، يعثر الحلّ على الملف التنفيذي المضمّن في الحزمة وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: تُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. من دون هذا الإجراء الوقائي، قد يؤدي وجود مفتاح في `.env` إلى تحويل Codex إلى الفوترة حسب الاستخدام من دون أي إشارة ظاهرة — وهو بالضبط ما وُجد هذا المزوّد لتجنّبه.
  - **فخاخ CLI المثبّتة بالاختبارات**:
    - يقرأ `codex exec` الإدخال القياسي **حتى** عندما يُمرَّر المحث كوسيط: من دون إغلاق الإدخال القياسي، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج مطلقًا (أُعيد إنتاجه: خروج 124 بعد 180 ثانية، وصفر بايت). لذلك فإن `communicate(input=...)` إلزامي.
    - إن `codex` المثبّت بواسطة npm عبارة عن وسيط Node يقوم بـ`spawn` للملف التنفيذي الحقيقي المكتوب بلغة Rust: وهذا الأخير **عملية حفيدة** لعملية Python، وينجو من `SIGKILL` الخاص بـ`subprocess.run(timeout=)`، حيث كان سيواصل استهلاك الحصة. ومن هنا جاء `Popen(start_new_session=True)` + `os.killpg`.
    - قد يخرج CLI بالرمز 0 رغم إصداره `turn.failed`: لذلك يُفحَص خرج JSONL ‏(`--json`) إلى جانب رمز الإرجاع، ويؤدي غياب الملف `-o` مع رمز 0 إلى إطلاق خطأ صريح بدلًا من إنتاج مقطع فارغ.
  - **تراجع أُسّي عند بلوغ حد المعدل**: لا يطبّق CLI أي إعادة محاولة داخلية (`max_retries = 0`). يجري التصنيف استنادًا إلى بنية حمولة JSON ‏(`status: 429` / `error.type`)، وليس إلى سلاسل فرعية — إذ تظهر كلمة «الحصة» في خطأ 429 قابل للاسترداد كما تظهر في `insufficient_quota` نهائي.
  - **إجراء وقائي في CI**: يُرفَض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفًا. فالمصادقة عبر الاشتراك غير مخصّصة لمشغّل مشترك، كما تنصح OpenAI صراحةً بعدم استخدام مسار العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` ‏(`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وAPI Platform، لكن حساب ChatGPT لا يملك صلاحية الوصول إليها كلها: تُطبَّق قائمة السماح من جانب الخادم، من دون تحقق محلي، ويؤدي اختيار نموذج غير معتاد إلى إطلاق تحذير. في خطة Plus، يوفّر Luna ما بين 250 و2,000 رسالة لكل نافذة مدتها 5 ساعات، مقابل 10 إلى 100 لـSol: ويُعد `--eco` الوضع الموصى به لأي معالجة دفعية.
  - **إصلاح خلل — كان `regen_translations.sh` يخرج بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` تابع لـ`main()` لم يعد موجودًا عند تنفيذ المصيدة. تحت `set -u`، كان ذلك يطلق `failed_log: unbound variable` ويجعل السكربت يخرج بالرمز 1 رغم صحة الترجمات الـ28 — وهو ما كان سيوقف `release.sh --auto` ‏(`set -e`) مباشرةً بعد إعادة التوليد، عند الخطوة الأعلى تكلفة. أصبح المتغير عامًا، وصارت المصيدة تتحقق من وجوده. أثر جانبي مفيد: أصبحت إخفاقات الترجمة الحقيقية، التي كان هذا الخطأ يحجبها سابقًا، ظاهرة مجددًا في الملخص الختامي.
  - **`REGEN_MODEL`**: متغير بيئة جديد لـ`regen_translations.sh` يفرض نموذجًا محددًا بدلًا من النموذج الافتراضي للمزوّد، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج المتقدم ضمن حصة الاشتراك بدلًا من نموذج `--eco` الموجّه إلى الحجم الكبير.
  - **`regen_translations.sh`**: أصبح `REGEN_PROVIDER=codex` متاحًا عبر اشتراك صريح (ولا يُكتشف تلقائيًا مطلقًا، كي لا تُستهلك حصة الاشتراك من دون علم المستخدم). يُجدَّد الرمز مرة واحدة تسلسليًا قبل بدء التوازي — لأن تجديد Codex دوري وأحادي الاستخدام، وستؤدي المهام المتزامنة إلى إبطال جلسة `codex login` — ويُخفَّض التزامن إلى 4.
  - **إعادة الهيكلة المرتبطة**: انتقل `_dispatch_provider_call` من 8 إلى 6 معاملات عبر `_resolve_provider()` يعيد اسم مزوّد، بدلًا من قيمة منطقية رابعة تنتشر عبر السلسلة بأكملها. تظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` محدود.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` ‏(48 اختبارًا) يغطي معاملات سطر الأوامر، والبيئة المنقّاة، وعقد منع المقدمة، والإخفاقات الصامتة، والمهلة الزمنية/killpg، والتراجع الأُسّي، والفحص المسبق، وحلّ المزوّد، وتسلسل الاستدلال في Gemini، وتصفية كتل Claude، واستشهادات الأخبار متعددة الفقرات. تضم المجموعة الكاملة 290 اختبارًا.
  - **التحقق الفعلي**: تُظهر ترجمة `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex بنية مطابقة تمامًا للترجمات المرجعية (14 كتلة برمجية، و24 عنوانًا، و25 سطر جدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل برمجية متطابقة حرفًا بحرف، ومن دون أي بقايا للعناصر النائبة). وفي مقال إخباري حجمه 69 كيلوبايت بوضع `--news`، يجتاز خرجا `gpt-5.6-luna` و`gpt-5.6-sol` كليهما مدقق التطبيق اللاحق للغات en/ja/ar. الاستهلاك المقاس عبر `account/rateLimits/read`: ظل دون عتبة تقريب العداد (0% من نافذة الساعات الخمس) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL لإسناد الأخبار عند وجود أقواس متداخلة أو بادئة فرنسية (2026-05-11):

  - **إصلاح الخلل**: كان استخراج عناوين URL الخاصة بالإسناد في `_protect_news_quotes` يستخدم التعبير النمطي `re.search(r"\((.+?)\)", attribution)` (التقاط غير جشع بين قوسين). وفي إسناد من النوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` محيطة + `]()` لرابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة تتضمن أيضًا البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائي). النتيجة: كان `_validate_news_post` يبحث عن هذه السلسلة في الخرج المترجم ويفشل دائمًا (لسببين: `)` مبتور + ترجمة "relayé par" إلى `relayed by`/`weitergeleitet von`/...). ولم يكن التسلسل الكامل low ← medium ← high ← gpt-5.5 قادرًا على النجاح.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — إذ يستهدف تحديدًا `](url)` لرابط Markdown، ويلتقط **عنوان URL الخام فقط** (من دون البادئة الفرنسية أو البتر)، وهو ثابت تحافظ عليه العناصر النائبة `#URL{N}#` أثناء الترجمة. وهو متين أمام النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس محيطة
  - **الاختبارات**: اختباران جديدان في فئة `TestNewsCitationExtraction` من `test_silent_failure.py`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة الدقيقة التي أعادت إنتاج خلل Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (صيغة بديلة باستخدام `via`)
  - **فجوة التغطية**: يتحقق `check-editorial-coverage.py` من صحة الصياغة التحريرية، لكنه لا يتحقق من قابليتها للترجمة بواسطة المترجم. ومن التحسينات الممكنة (خارج نطاق v1.9.2) إضافة فحص يحاكي استخراج الإسناد في تشغيل تجريبي لاكتشاف الأنماط المحفوفة بالمخاطر **قبل** النشر.

- **1.9.1** إصلاح تدويل تسمية الدعوة إلى الإجراء في ملاحظة علامة الترجمة (2026-05-10):

  - **إصلاح الخلل**: كانت تسمية `[Voir le projet sur GitHub ↗]` لرابط الدعوة إلى الإجراء في شريط العلامة أعلى الملفات المترجمة تظل **بالفرنسية** لجميع اللغات المستهدفة، بدلًا من اتباع `target_lang`. لا يراها LLM مطلقًا (إذ تُجمَّع من جانب Python للحفاظ على عنوان URL ومعرّف المستودع)، لذلك لم تكن مرحلة الترجمة قادرة على تصحيحها. وهو تراجع صامت منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ15 بتسمياتها المحلية. يمرّر `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` الآن اللغة المستهدفة. والرجوع الاحتياطي هو `fr` إذا كانت اللغة مجهولة (للأمان، ومن دون KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link` ‏(اللغة المستهدفة `ja` ← التسمية اليابانية المتوقعة). واختباران جديدان: `test_source_link_label_localized_per_target_lang` (مُعلَّم على 7 لغات تغطي الأبجديات اللاتينية والمقطعية والأبجدية الصامتة) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختبارًا في `test_translation_note_position.py` (بدلًا من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع بقيمة افتراضية `target_lang="fr"` — تواصل الجهات البرمجية الخارجية المستدعية التي لا تمرّر `args.target_lang` العمل من دون تعديل.
- **1.9** إصلاح الفشل الصامت + أدوات جودة متكاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + تنسيق العلامة "embed card"**:
    - خيارات CLI جديدة (إضافية، والقيم الافتراضية لم تتغير ← **غير كاسرة للتوافق**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): يضع الملاحظة في أعلى الملف المترجم أو أسفله أو في كلا الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - يعيد `legacy` إنتاج سلوك الإصدار 1.8 بدقة (فقرة عريضة `**…**`) **بايتًا ببايت**.
        - يُصدر `marker` تعريف مرجع رابط Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`)، يتبعه **اقتباس كتلي من 3 فقرات** منظّم لعرض من نوع "GitHub repo embed card": عنوان المشروع ضمن كود مضمّن (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف يترجمه LLM، ورابط CTA (`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. ويمكن استخدامه أثناء البناء بواسطة ملحق remark (راجع مدونة jls42.org ← الملحق `remark-translation-banner`).
    - **ثوابت لا تُرسل أبدًا إلى LLM**: يُجمّع عنوان المستودع وعنوان URL الخاص بـ GitHub في جانب Python بعد ترجمة العبارة الوصفية. لا يرى LLM مطلقًا المعرّف `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تغيير أي عارض أو حالة أحرف أو مخطط.
    - **إدراج يراعي frontmatter**: في وضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة `---` الختامية** لـ YAML frontmatter (لضمان سلامة Astro Content Collections / gray-matter). يكتشف المساعد `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف ويحافظ على سلامته؛ كما **يُطلق `RuntimeError`** عند وجود frontmatter مفتوح بلا سياج إغلاق (فيُدرج الملف ضمن `failed_files` بدلًا من كتابته مع ملاحظة في موضع خاطئ).
    - **منقّي النموذج بقائمة سماح**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع الرجوع إلى `unknown` إذا كانت النتيجة فارغة. وهذا يطابق المدقّق الموجود في جانب ملحق remark الخاص بـ Astro ويعطّل المحارف التي قد تكسر تنسيق العلامة (المسافة، وعلامة الاقتباس، والقوس، والفاصلة، وغير ذلك).
    - **إعادة هيكلة داخلية**: `_append_translation_note` (دالة واحدة متجانسة) ← 7 مساعدات خالصة (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فُصل المنشئ عن المركّب (يعيد المنشئ كتلة خالصة بلا فاصل، بينما يطبّق المركّب `\n\n` وفقًا للموضع)؛ ويشترك كود الإنتاج والمساعد المصدري في المجمّع نفسه المؤلف من 3 فقرات.
    - **يحافظ `_quote_lines` على الأسطر الفارغة**: يضيف `> ` إلى بداية كل سطر، مع تحويل الأسطر الفارغة إلى `>` وحده. وهذا يتيح لـ mdast رؤية 3 فقرات منفصلة داخل الاقتباس الكتلي (العنوان / الوصف / الرابط) بدلًا من فقرة واحدة ذات فواصل أسطر.
    - **`_build_translation_note_block` تكيّفي**: وفقًا لعدد الفقرات التي حافظ عليها LLM (3 = تنسيق البطاقة الكامل، 2 = عبارة + رابط، 1 = بديل احتياطي). لم يعد البديل الاحتياطي ذو الفقرة الواحدة **يلتف بـ `**...**`** عند اكتشاف رابط Markdown باسم `](` (إذ إن عرض `<strong>` حول رابط هش).
    - **توافق تصاعدي**: `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` في جانب `_compose_with_notes` — تواصل كائنات Namespace التي لا تتضمن هذه السمات (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) العمل بلا تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور حرفيًا على مقتطف المصدر) + طبقة احتمالية (`langdetect`)
    - قائمة سماح `finish_reason` / `stop_reason`: إطلاق `RuntimeError` لأي حالة خارج قائمة السماح (الاقتطاع، وcontent_filter، وغير ذلك)
    - `max_tokens` في Claude: `4096` ← `32768` (يتجنب الاقتطاع الكامن في المقاطع ذات 16k، مع هامش لاختلاف أنظمة الكتابة من الفرنسية إلى اليابانية/الصينية/الكورية/العربية/الهندية)
    - تقسيم يراعي العناوين: أولوية لـ H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى رمز خروج غير صفري: يعيد `translate_markdown_file` حالة ذات نوع محدد `success` / `failure` / `skipped`، ويستخدم `main()` القيمة `sys.exit(1)` إذا فشل ملف واحد على الأقل (في وضع الملف الواحد والدُفعات)
    - حاجز للمحتوى الفارغ لدى جميع المزوّدين، وفحص معقولية نسبة المصدر/المخرجات (≥ 500 محرف، وأقل من 5% = رفض)، والتحقق من العناصر النائبة للكود (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، وإعادة محاولة `BadRequestError` بلا `reasoning_effort`
    - إضافة الاعتمادية `langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("نمط EurekAI المتكامل"، 14 خطافًا):
    - Pre-commit: ‏ruff (فحص+تنسيق)، وshellcheck، وprettier ‏(md/yaml/json)، وdetect-secrets (حماية 4 مفاتيح API)، وLizard ‏(CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، ونهاية الملف، والملفات الكبيرة، وshebangs، وغير ذلك)
    - Pre-push: ‏mypy (وضع متساهل تدريجي)، وOpengrep SAST ‏(translate.py + scripts/)، وpip-audit (وضع إعداد التقارير الأولي)، وunittest discover ‏(tests/ + scripts/tests/)
    - أغلفة محلية في `scripts/` تستخدم `./venv/bin/python`
    - `scripts/audit_verdict.py`: محلل JSON لـ pip-audit مع 11 اختبارًا باستخدام unittest، وهو نقل متوافق مع Python لمحلل jls42-astro
    - إصلاح 7 مخالفات أولية من ruff: ‏B904 ‏(raise from) ×2، وB007 ‏(مجلدات غير مستخدمة)، وC408 ‏(dict literal)، وC419 ‏(list-comp)، وSIM105 ‏(contextlib.suppress)، وSIM110 ‏(any())
    - يستبعد Lizard مؤقتًا `translate.py` (4 دوال بقيم CCN من 21 إلى 47، وإعادة الهيكلة مخطط لها) — بوابة صارمة على scripts/
  - **SonarCloud + تغطية شاملة**:
    - سير عمل GitHub Actions باسم `SonarCloud` ‏(sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع التغطية عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى README ‏(Quality Gate، وتصنيفات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code)
    - `tests/test_silent_failure.py` ‏(`unittest` من المكتبة القياسية): يغطي الحلقات الست في سلسلة أخطاء الفشل الصامت
    - `tests/test_orchestration.py` (+79 اختبارًا): يغطي طبقة التنسيق في `translate.py` ‏(`_resolve_*_filename`، و`_existing_translation_exists`، و`_record_translation_status`، و`_write_output_file`، و`translate_directory`، و`_validate_input_paths`، و`_init_*_client`، و`_select_provider_client`، و`_normalize_collapsed_markdown`، و`_cleanup_source_flag`، و`_validate_news_flags_*`، والبدائل الاحتياطية `_openai_create_with_fallback` لـ TypeError + BadRequestError، وتنسيق موجّه سلسلة o1، وفروع الإرجاع المبكر في `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` ‏(stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **التغطية في الكود الجديد**: 75.5% ← نحو 98% ‏(translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك اختبارات E2E ‏`marker+top|bottom|both` و`legacy+top|bottom|both`)، وإضافة البادئة إلى عدة أسطر، والتوافق الرجعي بايتًا ببايت (قيمة حرفية ذهبية)، والمنقّي، وتقسيم frontmatter (بما في ذلك إطلاق خطأ عند عدم إغلاق السياج)، وتنسيق الفقرات الثلاث، والبديل الاحتياطي ذي الفقرتين، وحاجز الفقرة الواحدة + رابط Markdown، وحاجز حماية حاسم `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان+عنوان URL لا يُرسلان أبدًا إلى LLM. **نجح 190 اختبارًا**، بلا أي تراجع.
  - التوثيق: `README.md` (بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` (سير عمل pre-commit + مراقبة CI مفصّلة)، وإعادة توليد 28 ترجمة
- **1.8** وضع `--news` + ترقية نماذج 2026 (2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - جودة OpenAI: ‏`gpt-5` ← `gpt-5.4`
    - OpenAI الاقتصادي: ‏`gpt-5-mini` ← `gpt-5.4-mini`
    - جودة Gemini: ‏`gemini-3-pro-preview` ← `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` ‏(400k) و`gemini-3.1-pro-preview` ‏(1M)
  - وضع `--news` الأولي: حماية الاستشهادات الإنجليزية باستخدام العناصر النائبة `#NEWSQUOTE\d+#`، والتعيين `LANG_FLAGS` ‏(15 لغة)، وإدارة الأعلام وفقًا للغة الهدف
  - التحقق من العناصر النائبة للأخبار قبل الاستعادة (تراجع: كان حذف LLM للعنصر النائب ينتج بصمت مخرجات بلا استشهاد)
  - جعل السكربت `regen_translations.sh` قابلًا للنقل (مسارات مطلقة، ومن دون اعتماد على pwd)
  - إضافة رابط اللغة الفرنسية إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة
- **1.7** الميزات الجديدة:
  - خيار `--keep_filename` للاحتفاظ باسم الملف الأصلي أثناء الترجمة
  - دعم الملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على الكود المضمّن**: أصبحت علامات backticks ‏(`` `...` ``) محمية أثناء الترجمة
  - تحسين موجّه النظام:
    - معالجة أفضل لعلامات الاقتباس في YAML frontmatter
    - حماية متغيرات القالب `{variable}`
    - حظر ملاحظات المترجم غير المطلوبة
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** الميزات الجديدة:
  - دعم Google Gemini API للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI: ‏`gpt-5` (الجودة)، و`gpt-5-mini` (الاقتصادي)
    - Claude: ‏`claude-sonnet-4-5` (الجودة)، و`claude-haiku-4-5` (الاقتصادي)
    - Gemini: ‏`gemini-3-pro-preview` (الجودة)، و`gemini-3-flash-preview` (الاقتصادي)
  - وضع اقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف واحد (`--file`) من دون استعراض مجلد
  - نمط تسمية مبسّط جديد: `{base}-{lang}.md`
  - خيار `--include_model` للاحتفاظ بالتنسيق القديم الذي يتضمن اسم النموذج
  - دعم النماذج غير المدرجة بحد افتراضي للرموز (128k)
  - ترجمة README إلى 14 لغة
- **1.5** التحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين موجّهات الترجمة:**
    - أُثريت موجّهات الترجمات المباشرة وملاحظات الترجمة لتحسين الوضوح والكفاءة، بما في ذلك تعليمات مفصّلة بشأن الحفاظ على البيانات الوصفية وعناصر التنسيق المحددة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم عمليات الاستيراد لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص وإدارة كتل الكود للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات المخرجات:**
    - عكس ترتيب النموذج واللغة في اسم ملفات المخرجات (على سبيل المثال، `f"{base}-{args.target_lang}-{args.model}.md"`)، ما يسهّل تنظيم الترجمات والعثور عليها.
  - **تحسينات متنوعة:**
    - تنظيف الكود عبر إزالة الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكربت وسهولة قراءته.
- **1.4** الميزات الجديدة:
  - دعم Claude API من Anthropic للترجمة
  - تحسين الموجّهات لزيادة الوضوح والكفاءة
  - تعديلات طفيفة لتحسين قابلية صيانة الكود
- **1.3** التحسينات والميزات الجديدة:
  - تحسين إدارة كتل الكود
  - تحسين إدارة ملفات المخرجات
  - تحسين اكتشاف الملفات الموجودة
  - خيار `--force` لفرض الترجمة
  - عكس ترتيب النموذج واللغة في اسم ملف المخرجات
- **1.2** إصلاح سجل التغييرات
- **1.1** إضافة دعم Mistral AI API
- **1.0** الإصدار الأولي - دعم OpenAI API

**مقال مترجم من الفرنسية إلى العربية باستخدام gpt-5.6-sol.**
