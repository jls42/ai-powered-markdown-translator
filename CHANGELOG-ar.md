### Changelog

🌍 [Français](CHANGELOG.md) | [English](CHANGELOG-en.md) | [Español](CHANGELOG-es.md) | [中文](CHANGELOG-zh.md) | [Deutsch](CHANGELOG-de.md) | [日本語](CHANGELOG-ja.md) | [한국어](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [हिन्दी](CHANGELOG-hi.md) | [Italiano](CHANGELOG-it.md) | [Nederlands](CHANGELOG-nl.md) | [Polski](CHANGELOG-pl.md) | [Português](CHANGELOG-pt.md) | [Română](CHANGELOG-ro.md) | [Svenska](CHANGELOG-sv.md)

- **1.15.0** مزود `--use_antigravity`: الترجمة باستخدام حصة اشتراك Google AI Pro أو Ultra، عبر أداة CLI الرسمية `agy`؛ وبالنسبة لجميع المزودين، لم يعد الاختصار Ctrl-C يترك أي وكيل يعمل في الخلفية، ولم يعد المقطع الفارغ يتسبب في فشل الملف، ولم يعد ملف `.env` الخاص بالمشروع يحدد الملف التنفيذي المُشغّل (2026-09-26):

  - **المسار العاشر للمزود: اشتراك Google، دون فوترة حسب الاستخدام.** يمكن لمن يدفع مقابل Google AI Pro أو Ultra الآن خصم ترجماته من هذه الحصة بدلاً من الدفع لكل رمز على واجهة برمجة تطبيقات Gemini. إن `agy`، أداة CLI الرسمية لـ Antigravity، هي المسار الوحيد للوصول إلى هذه الحصة: لم يعد Gemini CLI يخدم حسابات Pro أو Ultra أو الحسابات المجانية منذ 18 يونيو 2026، ولا تتم مصادقة Antigravity SDK إلا عبر مفتاح API أو Google Cloud. يقود `--use_antigravity` الأداة في وضع headless، مثل Codex وGrok CLI، على نفس الأساس — مجموعة عمليات خاصة ونظيفة، ثم `SIGTERM` يليه `SIGKILL` عند انتهاء المهلة، وبيئة منقاة. تم التحقق من البداية إلى النهاية على agy 1.2.11: دليل Markdown — بنية front matter، خط عريض، رابط بأقواس، تعليمات برمجية مضمنة، كتلة برمجية مسننة داخل قائمة، جدول، واقتباس — تمت ترجمته إلى الإنجليزية في 14 ثانية شاملاً الفحص المسبق، وبنية متطابقة مع المصدر، مع إضافة ملاحظة الترجمة؛ ثم، بعد تصحيحات المراجعة، إلى الألمانية في 19 ثانية، ببنية متطابقة، ودون كتابة أي شيء في `~/.gemini`.

  - **لا يظل أي مسار مدفوع مفتوحاً، وكل تشغيل يتحقق من ذلك.** يعرف agy مسارات أخرى غير الاشتراك — مفتاح API، مشروع Google Cloud، بوابة، وأرصدة ذكاء اصطناعي مدفوعة — وتؤدي العديد من متغيرات البيئة إلى تحويله إليها بصمت دون عرض أي شيء. تم قياس ذلك: يرسل `AGY_GATEWAY_URL` المستند إلى بوابة تابعة لجهة خارجية، ويحوّل `AGY_ADC_AUTH` إلى مشروع Google Cloud مدفوع، ويعيد `CLOUD_CODE_URL` توجيه نقطة النهاية الموثقة إلى أي عنوان. وبالتالي، فإن بيئة العملية الفرعية هي قائمة سماح وليست قائمة حظر: `PATH`، واللغة والمنطقة الزمنية، والطرفية، والهوية، والبروكسيات وشهادات الجهاز وناقل الجلسة — ما أثبت `env -i` أن agy يحتاجه —، دون أي مفتاح. أما قائمة الحظر بحسب عائلات البادئات (`AGY_`، `GOOGLE_`، `GEMINI_`، `CLOUD_CODE_`...)، والتي جُرّبت أولاً، فقد أغفلت بعضها: كشفت مراجعة لسلاسل الملف التنفيذي عن تجاوزات لنقاط النهاية لم تغطها أي بادئة، مثل `AICODE_ENDPOINT_URL` و`UNLEASH_URL` و`GCE_METADATA_HOST`. هناك إعدادان يفلتان من متغيرات البيئة ولا يمكن قراءتهما إلا عبر `agy -p /config`: أرصدة الذكاء الاصطناعي المدفوعة التي تتدخل عند استنفاد الحصة، والاتصال عبر مشروع Google Cloud. يقوم فحص مسبق لا يستهلك أي حصة — الاستخدام صفر، كما تم قياسه — برفض الترجمة إذا كان أي منهما نشطاً أو إذا تم تكوين مفتاح API، كما أن غياب أي إعداد من `/config` يُعد رفضاً لكل من الثلاثة: حيث يجب ألا يسمح أي إصدار من agy يعيد تسمية أحد الإعدادات باجتياز فحص لم يتحقق من أي شيء. وأخيراً، يجب أن يثبت سجل كل استدعاء المصادقة عبر الاشتراك (`authMethod=consumer`)، كما أن ظهور أي طريقة مصادقة أخرى فيه، بأي من الصيغتين اللتين يستخدمهما الملف التنفيذي، يؤدي إلى رفض الاستجابة: فالاستدعاء نفسه هو المعيار المعتمد، وليس التكوين الذي تم إعداده.

  - **لا يملك وكيل الترجمة أي أدوات، ويتم إثبات تحميله في كل استدعاء.** يكتب كل استدعاء، في دليل مؤقت للاستخدام الواحد، وكيلاً بصيغة وكلاء المشاريع الخاصة بـ agy: `tools: []`، و`excludeDefaultComponents: true`، و`inheritCustomizations: false`. حوالي 800 رمز إدخال بدلاً من 12,400 رمز مع الوكيل الافتراضي، والإعداد الثالث ليس زائداً عن الحاجة: فقد كان استخدام `excludeDefaultComponents` وحده يسمح لخطاف مساحة العمل بتنفيذ أوامره وحقن نصه. يكمن الفخ الحقيقي في مكان آخر: عدم العثور على الوكيل لا يؤدي إلى فشل agy. يرجع برمز خروج 0، وحالة `SUCCESS`، ليرد وكيل البرمجة، مع 57 أداة نشطة — فعند إدخال «Le chat dort.»، أجاب: «صه، دعنا لا نوقظه! 🐱💤 ما الذي يمكنني فعله لك اليوم؟». لا يظهر أي شيء على stdout ولا على stderr: وحده سجل الاستدعاء يوضح ذلك. لذلك تُرفض الاستجابة إذا أعلن السجل عن هذا التراجع، وكذلك إذا خلا من العلامة الإيجابية لتحميل الوكيل — حيث يؤدي أي تغيير في تنسيق السجل إلى رفض الترجمة بدلاً من إزالة الحماية بصمت. يجب أن تشغل كل علامة سطراً كاملاً في السجل، بالتنسيق المقاس — درجة الخطورة، التاريخ، الوقت، خيط التنفيذ، الملف والسطر: يحتوي الملف التنفيذي أيضاً على سطر ينسخ رسالة المستخدم في السجل — لم يُشاهد قط بمستوى التسجيل الافتراضي، عبر 78 سجلاً حقيقياً —، ويجب ألا يتمكن مستند يقتبس هذه الرسائل من تجاوز الحماية بدلاً من agy.

  - **لا يتم المساس بإعدادات agy الخاصة بالمستخدم ولا بسجله.** يعمل كل استدعاء باستخدام HOME ودليل مؤقت ودليل عمل خاصة، وتُحذف جميعها بعد ذلك. ومع ذلك، تتم مصادقة agy عبر سلسلة مفاتيح النظام، والتي لا يقرأها aipmt مطلقاً. لا يتم توارث أي شيء: لا أذونات المستخدم — التي كانت تسمح للوكيل الافتراضي بتشغيل `ls` و`git` دون طلب أي إذن —، ولا قواعده العامة، أو ملحقاته، أو خوادم MCP، أو الخطافات والمهارات الخاصة به. ولا يُضاف أي شيء إلى سجله، حيث كان كل استدعاء يترك محادثة بحجم حوالي 200 كيلوبايت، بما في ذلك النص المترجم: وقد تركت حملة الفحوصات التجريبية فيه حوالي 95 محادثة وأزاحت 17 مدخلاً من ذاكرة تخزين مؤقت يبلغ حدها الأقصى 100. تم التحقق: لم يُكتب أي ملف في `~/.gemini/antigravity-cli`.

  - **رفض في حال الإغلاق التلقائي حيث لا يمكن ضمان العزل.** في نظام Linux بدون ناقل جلسة D-Bus — جلسات SSH، أو الحاويات، أو الخوادم —، يحفظ agy رمزه في ملف داخل `~/.gemini` بدلاً من سلسلة المفاتيح، كما هو معلن في سجل تغييراته الخاص (1.1.3)، وعزل كل استدعاء يحجب هذا الملف. أصبح الرفض يحدث الآن قبل أي تشغيل، مع توضيح سببه الحقيقي، بدلاً من الانتظار لمدة دقيقة لرمز تسجيل الدخول متبوعاً بنصيحة لإعادة الاتصال لن تحل أي مشكلة. في نظام Windows، يقرأ agy متغيرات `USERPROFILE` و`APPDATA` و`TEMP`، وليس `HOME` أو `TMPDIR` — وفقاً لتوثيق Go وسلاسل الملف التنفيذي —: فلا شيء هناك يعزل الاستدعاء عن إعدادات المستخدم أو سجله، وبالتالي يُرفض `--use_antigravity` هناك طالما لم يتم قياس ذلك والتحقق منه. يعمل على macOS، دون أن يتم قياسه؛ وتعمل حماية الفوترة، التي تقرأ التكوين الفعلي، بثبات هناك.

  - **`exit 0` لا يثبت شيئاً، أداة CLI الرابعة.** لا تُقبل الاستجابة إلا برمز إرجاع صفري، وكائن JSON بحالة `SUCCESS`، وسجل متوافق، ودون إجراءات مرفوضة، ودون أوامر، ودون أخطاء في كائن JSON، ودون أي تحذير بشأن الاقتطاع على stderr، وبنص غير فارغ. لا يُستخدم `--print-timeout` مطلقاً: فعند انتهاء مهلته، يُرجع agy مخرجات جزئية بحالة `SUCCESS`، والرمز 0؛ فالمهلة الزمنية خارجية وتقوم بإنهاء مجموعة العمليات. أي مقطع يبدأ بـ `/commande` أو `!commande` أو `@fichier` تتم ترجمته، ولا يتم تنفيذه أو تحليله مطلقاً — فبدون `--disable-slash-commands`، كان يتم تنفيذ «/help» بدلاً من ترجمته. ولم يعد المقطع الفارغ يصل إلى agy، حيث يقوم مسار المعالجة بإرجاعه كما هو (انظر أدناه)، ويظل مرفوضاً قبل الاستدعاء كشبكة أمان أخيرة: فعند إدخال فارغ، يفتح agy واجهته التفاعلية بالرمز 0. تتبع إعادة المحاولة حقل `retryable` من سطر `AGY_ERROR` الذي يكتبه agy على stderr منذ إصداره 1.2.6: فهو يعيد بالفعل محاولة الأخطاء المؤقتة بنفسه — 502، 503، 504، وتجاوز الحدود في الدقيقة —، وهو وحده من يعلم ما إذا كان الفشل المتبقي لا يزال مؤقتاً. في حال غياب هذا الحقل، لا تتم إعادة المحاولة عندما تشير الرسالة إلى الاستنفاد — فلدى Google، يحمل `RESOURCE_EXHAUSTED` نفس الرمز 429 مثل حد الطلبات في الدقيقة، لكن استنفاد نافذة الـ 5 ساعات لا يُستعاد في غضون 90 ثانية —، وتقتصر المحاولة فيما عدا ذلك على قيود معدل الطلبات فقط. تعتمد الاستجابة نفس نهاية السطر الخاصة بالمقطع: حيث ينهي agy دائماً استجابته بسطر جديد، مما كان يتسبب عند الدمج في تقسيم فقرة مجزأة في منتصف جملة إلى شطرين. تعرض الجلسة غير المتصلة رسالة تطلب تشغيل `agy` لإعادة الاتصال. وأخيراً، يمنع `AGY_CLI_DISABLE_AUTO_UPDATE=true` دليلاً جديداً لـ HOME من تشغيل أداة التحقق من التحديثات في جلسته الخاصة في كل استدعاء، بعيداً عن متناول المهلة الزمنية — بينما القيمة `1` لا تأثير لها.

  - **النماذج: جهد التفكير جزء من الاسم.** المعرّفات هي تلك الخاصة بـ `agy models` — `gemini-3.8-flash-medium`، `gemini-3.7-flash-low`، `gemini-3.1-pro-high`، `claude-sonnet-4-6`... يتم رفض الاسم الأساسي بمفرده، والذي سيرفضه agy، قبل الاستدعاء، مع إظهار اللواحق المتاحة: `-medium` أو `-low` لنموذج Flash، و`-high` أو `-low` لنموذج Pro. الإعداد الافتراضي هو `gemini-3.8-flash-medium`، و`gemini-3.7-flash-low` في `--eco`، والمحددة عبر الحملات الموضحة أدناه. `--reasoning_effort` لا تأثير له ويذكر ذلك صراحة. يستهلك كل من Claude وGPT-OSS من حصة منفصلة وأصغر بكثير — حوالي 1% من نافذة الـ 5 ساعات لكل استدعاء مقاس، مقارنة بـ 0.05% في Flash — ويذكر تحذير بذلك. يمكن قراءة الحصة المتبقية مجاناً عبر `agy -p /usage`. مرفوض في بيئات CI، مثل Codex وGrok CLI: حيث يتم حفظ الاتصال في سلسلة مفاتيح شخصية، وتقترح الرسالة استخدام `--use_gemini` مع مفتاح Google.

  - **حملتان في 26 سبتمبر: تم قياس الإعدادات الافتراضية بدقة، وليس افتراضها.** تم إجراؤهما بواسطة `aipmt --use_antigravity` نفسه، بأربع ترجمات متوازية، قورنت كل منها بمصدرها بواسطة `scripts/compare_structure.py`، مع قراءة الحصة عبر `agy -p /usage` قبل وبعد. في الصباح، وعلى مراجعة ملف README هذا المنشورة مع 1.14.0 وعلى مقال المراقبة الكثيف لقياسات README، في وضع `--news`، كتب `gemini-3.7-flash-medium` اللغات الأربع عشرة لكلا المستندين: المقال دون أي اختلاف، بمتوسط وسيط قدره 3 دقائق و14 ثانية لكل لغة، وملف README مع اختلاف كلمة واحدة عريضة فقط، في الكورية، في دقيقة و22 ثانية. بعد الظهر، وبناءً على طلب المؤلف، تمت مقارنة Gemini 3.8 Flash مع 3.7 على نفس الحزمة، ملف README هذا وسجل التغييرات هذا في إصدارهما 1.15.0: عند جهد التفكير المتوسط، كانت البنية متطابقة للاثنين؛ فكر 3.8 بقدر ما فكر 3.7 — 63% من رموز الإخراج مقابل 60%، مقروءة من كائن JSON الخاص بـ agy — وكلّف سُبع حصة إضافية، مقابل زيادة في الوقت بمقدار الربع. ثم أعيدت حملة الصباح باستخدام `gemini-3.8-flash-medium`: أربع عشرة لغة من أصل أربع عشرة دون أي اختلاف في المقال، بمتوسط وسيط 3 دقائق و59 ثانية، تماماً مثل ملف README، دقيقة و43 ثانية؛ وبقيت الروابط الداخلية سليمة في ملفات README الأربعة عشر، في حين كسر 3.7 بعضها في الإيطالية؛ وفي الإنجليزية، قام النموذج بإزالة سطور الترجمة الفرنسية، دون اختلاق أعلام، وبقيت الاقتباسات الإنجليزية سليمة. ومن هنا جاءت الإعدادات الافتراضية: `gemini-3.8-flash-medium`، و`gemini-3.7-flash-low` في `--eco`. عند جهد التفكير المنخفض، لا يفكر أي من نموذجي Flash ويكلف كل منهما النصف، لكن نموذج 3.8 low ضاعف الروابط الداخلية الثلاثة لملف README في الهندية، وهو ما لم يفعله 3.7 low في أي من الترجمات التي كتبها. التكلفة المقاسة على مستوى حساب المؤلف: 13 إلى 16 نقطة من نافذة الـ 5 ساعات لكل مليون حرف مصدر في `gemini-3.8-flash-medium` — الحملة المعادة، 28 ترجمة و1.8 مليون حرف، استهلكت 24.5 نقطة منها، و4.1 نقطة من حصة الأسبوع.

  - **شروط الاستخدام ومعالجة البيانات، موضحة بكل صراحة.** تحظر شروط Antigravity (القسم 6) والأسئلة الشائعة الخاصة بها الوصول إلى الخدمة عبر برنامج تابع لجهة خارجية باستخدام تسجيل الدخول إلى Antigravity — وتم ذكر Claude Code وOpenClaw وOpenCode هناك —، تحت طائلة تعليق الحساب. لا يقرأ aipmt الرمز المميز أو يعيد استخدامه مطلقاً: بل يقوم بتشغيل الملف التنفيذي الرسمي في وضع headless الذي توثقه Google للبرامج النصية وبيئات CI، وقد اعتبر أحد مسؤولي Google، في المنتدى الرسمي، أن تشغيل `agy -p` من برنامج نصي محلي لعمله الخاص يُعد أمراً «معيارياً» — وهو مجرد رد، وليس التزاماً رسمياً. لا يوجد نص يحسم حالة الأداة الموزعة: يوضح ملف README أن المستخدم يتحمل مسؤولية حسابه. يتيح القسم 5 من نفس الشروط لـ Google استخدام المحادثات المتبادلة — المطالبات، والاستجابات، والبيانات الوصفية — لتحسين منتجاتها والتعلم الآلي لديها، ومراجعتها من قبل مراجعين بشريين، دون التمييز بين الخطة المجانية وخطتي Pro وUltra: يخصص ملف README هذا المسار للمستندات العامة فقط، ويشير تحذيره النهائي إلى ذلك.

  - **يمكن لترجمات هذا المستودع أن تمر عبر هذا المسار، دون استثناء.** إن `REGEN_PROVIDER=antigravity` هو اشتراك وليس واجهة برمجة تطبيقات مفوترة: فهو يعمل دون `REGEN_ALLOW_PAID_API`، بأربع مهام متوازية — حيث سارت الحملة بأكملها بأربع مهام متوازية دون أي خطأ — ومع سقف 1,800 ثانية لكل مهمة لـ Codex، وهو ما تؤكده القياسات: فسجل التغييرات بأكمله باللغة الهندية، وهو الملف الذي يفقد فيه Codex العناصر البديلة، تمت ترجمته دون أي اختلاف في 4 دقائق و33 ثانية بواسطة 3.7 Flash في إصداره 1.14.1، وفي 5 دقائق و50 ثانية بواسطة 3.8 Flash في إصداره 1.15.0، وهو أطول بالخمس: أي أقل من الحد الأقصى بخمس مرات على الأقل. التجديد الكامل، 2.2 مليون حرف، يقدّر بـ 30 إلى 36 نقطة من نافذة الـ 5 ساعات في `gemini-3.8-flash-medium`. يسبق فحص واحد، وهو الفحص المسبق للوحدة، المهام الـ 28: وإذا كان agy غير متصل أو مضبوطاً على الفوترة، فلن يُشغّل أي منها. يظل الخيار الافتراضي هو Codex مع `gpt-5.6-sol`.

  - **انقطاع رابط داخلي دون أن يلاحظه شيء.** يبتلع الرمز المميز الذي يحمي نقطة التثبيت أقواسه: فيتلقى النموذج رابطاً يبدو تالفاً، عبارة عن أقواس معقوفة يتبعها الرمز المميز مجرداً، وفي بعض الأحيان يعمد إلى «إصلاحه». وعند الاستعادة، يُنتج `[texte]((#ancre))`، الذي لا يؤدي إلى أي مكان، دون أن تبلغ أي آلية حماية أو `compare_structure.py` عن ذلك. حدث هذا في ملفي README من أول عملية إعادة توليد لهذا الإصدار، ومع جميع النماذج التي جُربت، وبشكل أكثر تكراراً عندما يكون الرابط نفسه بين قوسين — وهي حالة رابط أضافه هذا الإصدار للتو إلى ملف README هذا. يتحقق فحص النشر الآن من عدد الروابط الداخلية السليمة في كل ترجمة، ولم يعد ملف README هذا يضع الروابط بين أقواس؛ وما زال تصحيح معالجة الرمز المميز نفسه قيد الإنجاز.

  - **أصبح الاختصار Ctrl-C وإغلاق الطرفية يوقفان الوكيل الآن، لجميع أدوات CLI الأربع.** تعمل Codex وGrok CLI وOpenCode وAntigravity في جلساتها الخاصة، خارج مجموعة العمليات الأمامية: وبالتالي فإن إشارة `SIGINT` الناتجة عن Ctrl-C لا تصل إليها، وعند الضغط على Ctrl-C، لا ينتظرها `Popen` سوى ربع ثانية قبل إنهاء التحكم. تم التحقق من ذلك أثناء المراجعة عبر ملف تنفيذي وهمي: كان الوكيل ينجو من aipmt ويكمل عمله مستهلكاً الحصة، للحصول على نتيجة مهدورة — أربعة في وقت واحد أثناء إعادة التوليد. يؤدي تلقي `KeyboardInterrupt` أو `SystemExit` أثناء الاستدعاء الآن إلى إنهاء مجموعة الوكيل قبل التصعيد للأعلى. كان إغلاق الطرفية أو انقطاع جلسة SSH (`SIGHUP`) ينهي Python دون أي تنظيف: فيستمر الوكيل، ويبقى الدليل الخاص بـ Antigravity على القرص مع سجله، الذي يحمل عنوان الحساب. يتبع `SIGHUP` الآن مسار `SIGTERM`: إنهاء مجموعة الوكيل، ثم الخروج بالرمز 129. أما في حالة تجاهل `SIGHUP`، بالتشغيل عبر `nohup`، فيظل متجاهلاً: فهذا اختيار المستخدم. يمر الفحص المسبق لـ Antigravity — `agy --version`، ثم `agy -p /config` — عبر المعالج نفسه: فتؤدي أي إشارة تُستقبل أثناء هذا الفحص إلى إيقاف agy وحذف الدليل الخاص أيضاً، بدلاً من تركه على القرص مع سجله.

  - **لم يعد بمقدور مشغل العمليات الفرعية استهداف كامل الجلسة.** في نظام Linux، تكون قيمة `killpg(1, sig)` مساوية لـ `kill(-1, sig)`: حيث تُرسل الإشارة إلى جميع عمليات المستخدم. حدث هذا أثناء كتابة هذا الإصدار عبر حملة طفرات برمجية (mutations): استدعى مُتحوّل (mutant) دالة `Popen` مقترنة بـ `MagicMock` مجرد، فمرّ الخطأ الناتج عبر مسار المقاطعة الجديد، وأدى معرف العملية (pid) للعملية الوهمية، الذي يحوّله Python إلى 1، إلى إنهاء كامل الجلسة الرسومية. أصبح `_codex_kill_group`، المشترك بين واجهات سطر الأوامر (CLI) الأربع، لا يستهدف مجموعة إلا إذا كان معرف العملية عددًا صحيحًا حقيقيًا أكبر من 1، وكان `getpgid` يُرجع نفس هذا المعرف — حيث يكون العميل المشغَّل في جلسته الخاصة زعيم مجموعته — ولم تكن هي مجموعة العملية الحالية؛ وإلا، فإنه يستهدف العملية الابنة المباشرة فقط. في بيئة الإنتاج، لا يُغير هذا الضابط الاحترازي أي شيء: فهو يغلق الحالة التي قد يتسبب فيها خطأ برمجي في إرجاع شيء آخر. أصبحت البدائل المزدوجة (mocks) لـ `Popen` التي لا يُفترض استخدامها تفشل بشكل قاطع وفوري، وتحمل العمليات الوهمية معرف pid مستحيلاً، وتعمل حملات الطفرات داخل مساحة أسماء PID.

  - **لم يعد المقطع الفارغ يتسبب في فشل الملف، بغض النظر عن المزود.** يمكن لـ `segment_text` أن يعزل النهاية الفارغة لمستند: 16,001 حرفًا تنتهي بثلاث فواصل أسطر تُنتج مقطعًا ثانيًا مكونًا من فاصلي أسطر. كان Antigravity يرفض ذلك قبل الاستدعاء — وتم تكرار الحالة أثناء المراجعة — ولدى مزود API، كانت الاستجابة الفارغة تقع تحت طائلة حارس المحتوى الفارغ: وفي كلتا الحالتين، كان الملف بأكمله يفشل، بعد استهلاك الحصة النسبية (quota) للمقاطع السابقة. أصبح المقطع الفارغ يُعاد الآن كما هو، دون أي استدعاء.

  - **الأمان: لم يعد بإمكان ملف `.env` الخاص بالمشروع اختيار الملف الثنائي المنفَّذ.** تُحدد `CODEX_BIN` و`GROK_BIN` و`OPENCODE_BIN` و`AGY_BIN` البرنامج الذي تشغله الترجمة بصلاحيات المستخدم: وإذا تم تعيينها بواسطة ملف `.env` لمستودع تم استنساخه للتو، فكانت تتسبب في تشغيل ملف من هذا المستودع منذ أول عملية ترجمة. لذلك تنضم اللاحقة `_BIN` إلى `_BASE_URL` و`_API_BASE` و`_ENDPOINT` في مرشح طبقة المشروع، مع `GROK_HOME`، الذي يُبحث تحته عن ثنائي Grok (`$GROK_HOME/bin/grok`). تظل هذه المتغيرات مقبولة من البيئة ومن `~/.config/aipmt/.env`، ويتم الإبلاغ عن كل رفض عبر stderr مع توضيح المكان المخصص لتعيينها.

  - **الاختبارات**: ملف جديد `tests/test_antigravity_provider.py` (103 اختبارات)، يغطي 100% من التعليمات والتفريعات في الوحدة — argv دقيق ولا يكون أبدًا `-p`، تمرير المقاطع عبر stdin، عميل مكتوب بدون أدوات أو وراثة، بيئة مقصورة على قائمة السماح، مجلد خاص يُحذف بعد النجاح أو الفشل أو انتهاء المهلة أو تلقي إشارة، عقد مخرجات كامل، تراجع العميل ومسار الفوترة مقروءان من أسطر سجل بالتنسيق الفعلي، `AGY_ERROR` و`retryable`، فحص فوترة أولي (preflight) صارم، رفض التشغيل على Windows ودون ناقل جلسة، لواحق المجهود، نهايات الأسطر مأخوذة من المقطع، ربط التوزيع وتفريع إعادة التوليد. لا يطلق أي اختبار agy: لم يتم استدعاء ملف ثنائي وهمي وُضع في بداية `PATH` في أي مسار تشغيل، بما في ذلك في ظل ظروف CI، مع إخفاء ناقل الجلسة و`CI=true`. يقاطع `tests/test_provider_base.py` عملية فرعية حقيقية عبر Ctrl-C وعبر `SIGHUP`، بما في ذلك تحت `nohup`، ويُحكم الضابط الاحترازي لـ `killpg` — سواء كان معرف العملية `MagicMock`، أو قيمة منطقية، أو 1 أو أقل، أو المجموعة الحالية أو مجموعة عملية أخرى: لا تُرسل إشارة مجموعة أبدًا؛ ويطلق `tests/test_key_resolution.py` عملية جديدة لكل متغير `_BIN`؛ ويترجم `tests/test_orchestration.py` مستندًا تشكل نهايته الفارغة مقطعًا منفصلاً. وصلت المجموعة الكاملة إلى **650 اختبارًا**.

- **1.14.1** Mistral: تجاوز معدل الطلبات لم يعد يفشل الملف، وتأكيد النماذج الافتراضية عبر القياس (2026-09-17):

  - **كان الخطأ HTTP 429 يؤدي إلى فقدان الملف بأكمله.** لا تعيد حزمة SDK الخاصة بـ `mistralai` المحاولة افتراضيًا على الإطلاق، على عكس حزم SDK من OpenAI وAnthropic، كما أن حدود Mistral منخفضة وخاصة بكل نموذج: 15 طلبًا في الدقيقة لـ Large 3، و100,000 توكن في الدقيقة لـ Small 4، بناءً على قياسات أُجريت على حساب المستودع. فقدت حملة مكونة من أربع ترجمات متزامنة على Small خمس ترجمات من أصل أربع وثمانين. يعيد العميل المحاولة الآن عند أخطاء 429 و5xx وأخطاء الاتصال، مع انتظار متصاعد من ثانيتين إلى 60 ثانية لمدة خمس دقائق كحد أقصى: لا يحدد خطأ 429 من Mistral مدة الانتظار، وتُحسب حدوده بالدقيقة. اختبار مضاد على API: عشرون استدعاءً متتاليًا على Large 3، نجحت جميعها العشرين، وجاء الاستدعاء السادس عشر بعد انتظار دام 33 ثانية.
  - **لم تعد الاستجابة في شكل كتل تفكير تعطل الترجمة.** يقوم Small 4 وMedium 3.5 بالتفكير المنطقي عند طلب ذلك منهما، وحينها تصبح الاستجابة قائمة من كتل `thinking` ثم `text` بدلاً من سلسلة نصية. لا يقوم أي منهما بالتفكير افتراضيًا (وفق القياس)، ولكن أي تغيير في الإعداد الافتراضي من جانب المزود كان سينتهي بـ `AttributeError`: يتم استبعاد كتل التفكير، تمامًا كما هو الحال مع Claude.
  - **تأكيد النماذج الافتراضية: `mistral-large-latest` و`mistral-small-latest`.** تمت مقارنة النموذج الأحدث الوحيد، Mistral Medium 3.5، في مواجهتهما عبر اللغات الأربع عشرة، على مقال الرصد الكثيف من جدول التوافق وعلى ملف README. في المقال، يفقد عناوين فرعية أو يغير مستواها في 9 لغات من أصل 14 — ترجمة واحدة فقط بدون أي تفاوت، مقابل 6 لـ Large 3 و8 لـ Small 4 — ويكلف 3 أضعاف سعر Large 3 في المدخلات، و5 أضعاف في المخرجات. تشير الأسماء المستعارة إلى Large 3 (`mistral-large-2512`) وSmall 4 (`mistral-small-2603`).
  - **محاذاة `MODEL_TOKEN_LIMITS` مع واجهة برمجة التطبيقات**: 262,144 توكن لمجموعة Mistral بدلاً من 256,000، و131,072 لـ `ministral-3b`، ولم تعد المعرفات المؤرخة، بما في ذلك `mistral-medium-2604`، تطلق التحذير «غير مدرج». لم يتغير تقسيم المقاطع، الذي يظل محددًا بسقف 16,000 حرف.

- **1.14.0** جدول توافق في مقدمة README، وحملة القياس التي تؤسس له (2026-09-09):

  - **الحكم الخاص بكل نموذج، في أعلى README، باللغة الفرنسية.** كانت هناك أكثر من ثلاثمائة ترجمة تنتظر في منتصف المستند، موزعة على ثلاثة جداول كان يلزم قراءتها ومقارنتها. يحسم الجدول الجديد الأمر في سطر واحد لكل نموذج، ويوضح بالتحديد ما يختلف: «كلمة بخط عريض تم نسيانها، بالعربية واليابانية» بدلاً من مجرد نسبة. لم يُسجل للمقارن أي تفاوت لثلاثة نماذج على المستندين الكثيفين — `gemini-3.7-flash`، و`gpt-5.6-sol` عبر اشتراك ChatGPT، و`z-ai/glm-5.2` عبر OpenRouter.
  - **حملة إعادة قياس شملت 176 ترجمة.** تعود القياسات الأصلية إلى 4 سبتمبر وكانت تخص ملف README تغير كثيرًا منذ ذلك الحين. أُعيد قياس ثمانية نماذج على ملف README هذا، واثنان على مقال رصد في المدونة بالوضع `--news`، وواحد على أربعة ملفات README عامة — FastAPI وOllama وtldr-pages وVue.js. ثلاث نتائج استحقت التدوين: يفقد Mistral **عنوان قسم بالكامل** في اللغات العربية والهندية والكورية، دون أن يرصد ذلك أي حارس من مسار المعالجة — فالعنوان لا يمكن استبداله برمز، ولا يوجد ما يتحقق منه حاليًا أثناء الترجمة، حتى وإن اكتشفه المقارن بعد ذلك؛ وفشل Grok في خمس لغات لمقال رصد، حيث فقد أربعة مقاطع برمجية مضمنة وثلاثة عناوين URL منذ المقطع الأول، مع أنه كان يصمد على ملف README؛ وقدّم Gemini نحو 55 ترجمة بدون تفاوت من أصل 56 على ملفات README العامة الأربعة.
  - **انضمام أداة مقارنة الهيكل إلى المستودع.** كان ملف README يصف هذا البروتوكول بأنه الطريقة الصحيحة للحكم على النموذج — «قارن عدد الأقسام، والروابط، وعناوين URL المميزة، وكتل الأكواد البرمجية» — دون توفير الأداة. ملف `scripts/compare_structure.py` هو الذي أنتج هذه الجداول، مصحوبًا بأحد عشر اختبارًا تثبت أنه يرصد أي عنوان URL، أو كتلة كود، أو قسم، أو كود مضمن، أو سطر جدول، أو اقتباس مفقود. وهو يشتمل على التصحيحين الخاصين بنظم الكتابة غير اللاتينية اللذين جعلَاه موثوقًا: لم يعد عنوان URL المتبوع بقوس كامل العرض يُحسب على أنه مختلف، ولم يعد الاقتباس الأقصر باللغة الصينية يُعتبر اقتباسًا مفقودًا.
  - **فحص الحزمة، قبل النشر وبعده.** تُنفّذ مجموعة الاختبارات على التعليمات البرمجية المصدرية وتظل ناجحة حتى لو كانت الحزمة الموزعة معطوبة — كوحدة منسية في إعدادات setuptools، أو نقطة دخول لا يمكن حلها. يبني `scripts/check-package-smoke.sh --wheel` حزمة التوزيع، ويثبتها في بيئة افتراضية مؤقتة خارج المستودع، ويتحقق من أنها لا تحتوي إلا على الحزمة، ومن استجابة صيغتي التنفيذ، واستيراد جميع الوحدات، ثم يترجم مستندًا حقيقيًا ويقارن هيكله. يقوم `--pypi` بالشيء نفسه انطلاقًا من الفهرس: وهو التحقق الوحيد الذي يثبت أن هذه البايتات المحددة هي التي تم إرسالها، ولا يسمح PyPI أبدًا بإعادة نشر إصدار سابق. وتنفّذ بوابة الفحص `--full` الجزء غير المتصل بالإنترنت من ذلك، دون استدعاء أي نموذج ودون تكلفة.
  - **تصحيح سبعة ادعاءات في ملف README، تم تكرارها والتحقق منها جميعًا قبل إعادة كتابتها.** كان الادعاء «لا شيء خاطئ يصل إلى قرصك» غير صحيح: فالنموذج الذي يحذف قسمًا أو يُعدل تاريخًا في الترويسة الأمامية (front matter) ينتج ملفًا يقوم مسار المعالجة بكتابته بالحالة `success`، وقد تم تكرار ذلك باستخدام استجابة محاكاة. يوضح ملف README الآن ما تحميه الرموز النائلة — كتل الأكواد، والكود المضمن، وعناوين URL، والمراسخ، والاقتباسات — وما لا تتحقق منه حراسات مسار المعالجة حاليًا. عبارة «بدون تفاوت» لا تعني «مطابق»: فالمقارن يقوم بعدّ العناصر دون قراءة محتواها، ولا يشير إلى حذف عنوان من المستوى الرابع، ولا إلى استبدال نص كود مضمن، ولا إلى تبديل خيار CLI — وهي ثلاث حالات تمت ملاحظتها بالقياس، وتمت إزالة عبارة «نفس الخيارات»، إذ لا يملك المقارن أي عداد للخيارات. لم تكن تعليمات التثبيت من أجل المساهمة تنتج أداة صالحة للاستخدام: فـ `pip install -r requirements.txt` لا يثبت سوى التبعيات، وكان أمر `python -m aipmt` يعيد `No module named aipmt`، وكان `pip install -e .` مفقودًا — تمامًا مثل سطر تثبيت `pre-commit`، الغائب عن كلا ملفي التبعيات. كان الوعد المذكور بشأن OpenRouter للسياقات الصغيرة يعلن عكس ما ينص عليه الكود: يتم رفض أي نموذج تقل نافذة سياقه عن **16,400 توكن** قبل أي استدعاء، وتم قياس ذلك عند 4,095 و8,192 و16,384. لم يكن المرشح الذي يمنع ملف `.env` الخاص بالمشروع من إعادة توجيه استدعاءات API موثقًا في أي مكان؛ وقد تم توثيقه الآن، مع قائمة بالمتغيرات المعنية. وأخيرًا، رقمان: كانت إحدى خلايا Grok في الجدول الرئيسي مأخوذة من حملة 9 سبتمبر في حين أشار عمودها إلى حملة 4 سبتمبر، وعبارة «رابط مكسور» المنسوبة إلى Gemini كانت عكس ما أظهره القياس — فهو يحوّل ثلاثة عناوين URL مجردة إلى روابط، ولا يفقد أيًا منها.

  - **لم يعد فشل الكتابة يترك ترجمة مبتورة على القرص.** يقتطع `open(cible, "w")` الملف قبل ملئه: وكان حدوث خطأ أثناء الكتابة — مثل امتلاء القرص — يترك ملف هدف جزئيًا. تم تكرار الحالة: كتابة ثمانية بايتات، وإرجاع الحالة `failure`، ثم عند إعادة التشغيل التالية يعثر على هذا الملف، فيعيد `skipped` ويحتفظ به. كان الفشل يتحول إلى ترجمة مبتورة لا يعود أي شيء ينبه إليها، وكان رمز الخروج يساوي 0. أصبح المحتوى يُكتب الآن في ملف مؤقت في نفس الدليل — فعملية `os.replace` ليست ذرية إلا على نظام الملفات نفسه — ثم يُعاد تسميته، مع حذف الملف المؤقت في حال فشل الكتابة، بما في ذلك عند حدوث مقاطعة. يأتي هذا الملف المؤقت من `mkstemp`، وأظهرت المراجعة أن استخدام اسم متوقع كان سينقل المشكلة فقط: فرابط رمزي مزروع عند `cible.md.aipmt-tmp` كان يؤدي إلى كتابة الترجمة في الملف المستهدف، خارج دليل الإخراج، مع إرجاع `success`؛ وكان تشغيل عمليتين متزامنتين على نفس الهدف يتشاركان هذا الاسم، فتقوم الثانية بإعادة التسمية بينما الأولى لا تزال تكتب في نفس الـ inode — مع الإعلان عن نجاح الهدف بمحتوى مختلط (`BBBAAAAA` بدلاً من `BBBBBBBB`، وفق القياس). يقوم `mkstemp` بإنشاء الملف بوضع `O_CREAT | O_EXCL` وباسم فريد، مما يغلق كلتا الثغرتين. تُستعاد أذونات الهدف الحالي صراحةً قبل الاستبدال: يقوم `mkstemp` بالإنشاء بالأذونات 0600، ولولا ذلك لتحول هدف كان عن قصد بالأذونات 0600 إلى 0664، وأصبح هدف بالأذونات 0644 يُخدَّم عبر خادم ويب غير قابل للقراءة. تُحكم ثمانية اختبارات هذا السلوك بالكامل، وتؤدي طفرتان في الكود إلى فشلها.

  - **أصبح بيان النقاء قادرًا على التصريح عن علامة استبعاد مُعاد كتابتها.** كان الفحص يتطلب أن ينجو كل سطر يحتوي على `# nosec` أو `# nosemgrep` أو `NOSONAR` من المرجع كلمة بكلمة. غير أن نقل الكتابة إلى ملف مؤقت يعيد تسمية المتغير المفتوح: يبقى استبعاد Sonar كما هو، لكن سطره يتغير. تصرّح الفئة `markers` في البيان عن هذا الزوج `old` ← `new` مع ذكر السبب. شرطان، والشرط الثاني جاء بناءً على مراجعة: يجب أن يكون البديل موجودًا، **وأن يحمل نفس الاستبعادات الموجودة في السطر الأصلي**. فبدونه، كان التصريح عن `value = 1  # NOSONAR` ← `value = 1` يمر بنجاح، بحيث كان مجرد تصريح كافيًا لإزالة علامة — وهو بالضبط ما وُجد هذا الفحص لمنعه. ويبقى أي اختفاء غير مصرّح به مرفوضًا.

  - **استبعاد حملتين من الجداول بسبب مقاطعتهما بواسطة شيء آخر غير النموذج.** فقد Grok جلسة سطر الأوامر بعد اثنتي عشرة لغة من ملف README هذا — حيث رُفضت لغتان في غضون أربع ثوانٍ، دون أي استدعاء للنموذج — وتم خنق `qwen3.8-flash` بواسطة الخطأ HTTP 429 من موفر الاستضافة الرئيسي الخاص به بعد لغتين. لا تُعطى درجات لحملة تم قطعها: وسيتم إعادتها بالكامل أو عدم إدراجها إطلاقًا. وفي السياق نفسه، أُعيدت معايرة الفترات الزمنية لجدول «README لهذا المشروع» وفقًا لحملة 9 سبتمبر — إذ كانت تعود لمراجعة مكونة من 508 أسطر، مقارنة بـ 785 سطرًا تم قياسها، وكانت تعلن دقيقتين و04 ثوانٍ لـ `gpt-5.6-sol` في حين أظهر القياس 6 دقائق و46 ثانية.

  - **إعادة صياغة ملف README بالكامل: 600 سطر بدلاً من 1,007، وفق تسلسل منطقي للقارئ.** ما تقوم به الأداة، والتثبيت، والإعداد، والخطوات الأولى، ثم جدول التوافق — الموضوع بعد التثبيت، عندما يعرف المستخدم ما يشغله —، والخيارات، وفصل لكل مزود، والقياسات المفصلة، وأخيرًا كيفية المساهمة. تم حذف سرد الحملات، ونوادر القياسات، والتكرار بين الأقسام؛ وكل رقم تم الإبقاء عليه هو رقم الجداول، وكل تأكيد للسلوك يتطابق مع كود هذا الإصدار. ويبقى سجل إعادة الهيكلة محفوظًا في سجل git.

  - **يوضح الجدول الرئيسي الآن عدد اللغات التي يطالها كل تفاوت.** كان يعلن «✅ 14 لغة» ثم، في العمود المجاور، «كلمة بخط عريض» — دون توضيح ما إذا كان ذلك ينطبق على لغة واحدة أم على اللغات الأربع عشرة. لم يكن القارئ قادرًا على الحكم، وكان الانطباع سيئًا في حين أن نتيجة القياس جيدة: ففي ملف README هذا، لا يوجد لدى Gemini سوى لغة واحدة من أصل أربع عشرة تحتوي على كلمة إضافية بخط عريض، ولدى Sol لغتان، ولدى GLM ثلاث. تحتوي كل خلية الآن على عدد اللغات المعنية وتحديدها، ورقم أعمدة الحكم محدد بجملة واحدة — عدد اللغات، من أصل أربع عشرة، التي كُتبت ترجمتها دون أي تفاوت —، مع دليل يوضح معنى الرموز الثلاثة. والتصنيف معتمد من صاحب المشروع: ✅ لا يوجد أي تفاوت؛ ⚠️ كل شيء مترجم ولم يتغير سوى بعض التنسيق، ولا ينقص أي شيء؛ ❌ رُفضت لغة واحدة على الأقل، أو ينقص محتوى في ملف مكتوب.

  - **إعادة حساب حملة المقال بأكملها باستخدام المقارن الحالي.** نُشر رقمان قبل تصحيح المقارن لنظم الكتابة غير اللاتينية، وكان يحسب لهما تفاوتات غير موجودة فعليًا: انتقل `qwen3.8-flash` من ثلاث عشرة إلى أربع عشرة لغة بدون تفاوت، و`qwen3.7-flash` من سبع إلى ثماني لغات. وصُححت الفقرة الخاصة بـ `gpt-oss` في الوقت نفسه: ففقراته المتروكة باللغة الفرنسية لم تصل أبدًا إلى ملف مكتمل، حيث رفض حارس المقتطفات غير المترجمة اللغات الأربع المعنية — وهذا هو الفرق بين نموذج يتم إيقافه ونموذج لا يُلاحظ مروره، وقد ذكر ملف README هذا الفارق صراحة.

  - **إخلاء مسؤولية في نهاية ملف README.** يستبعد القسمان 15 و16 من رخصة GPL v3 بالفعل أي ضمان، لكن لا أحد يقرأ الرخصة قبل تثبيت أداة ما. لذا تم توضيح أربع نقاط جليّة: تجب مراجعة الترجمة الآلية قبل النشر، إذ إن آليات الحماية لا تغطي العناوين ولا الجداول ولا الـ front matter ولا المعنى؛ والوثيقة المترجمة تُرسل إلى المزود المختار وفق شروطه الخاصة، مع إمكانية قيام بعض النماذج المجانية بإعادة استخدام المحادثات — بينما يظل النموذج المحلي هو السبيل الوحيد دون خروج للبيانات؛ واستدعاءات واجهة برمجة التطبيقات (API) مدفوعة ولا يضع البرنامج سقفاً للنفقات؛ والقياسات المنشورة هي ملاحظات مؤرخة وليست ضمانات.

  - **عائلة `gpt-5.6-*` بأكملها متاحة عبر اشتراك ChatGPT**، وبحسب القياس: يعطي `--model gpt-5.6-terra` عبر Codex النموذج الذي توفره واجهة برمجة التطبيقات افتراضياً. ويظل الخيار الافتراضي هو `gpt-5.6-sol`، وهو النموذج الوحيد الذي تم قياسه على أربع عشرة لغة دون أي فقدان في وثيقة كثيفة، وهو الذي يترجم هذا المستودع.

- **1.13.1** حداثة الاعتماديات: التأخير لم يعد تحذيراً يمكن للمرء تجاهل قراءته (2026-09-09):

  - **التحذير الذي لا يُحسب هو تحذير يتم تفويته.** كان `check-deps-fresh.sh` يشير منذ عدة أيام إلى أن `openai` و`anthropic` قد تأخرا في التحديث، لكن سطر ملخص بوابة التحقق (gate) كان يحسب حالات الفشل فقط: فقد كان يعلن «جاهز: N عملية تحقق باللون الأخضر» بينما كان التأخير مكتوباً بضعة أسطر للأعلى. وأي شخص يقرأ السطر الأخير — وهو ما يفعله الجميع — لم يكن بوسعه معرفة ذلك. بات الحكم يتضمن الآن عدد التحذيرات، ويقوم عداد مخصص بحصرها بدلاً من طباعتها تباعاً.
  - **التأخير يأتي مصحوباً بملاحظات الإصدار.** فالرقم لا يوضح ما تغير، والبحث الذاتي عن الملف المناسب هو تحديداً العقبة التي تجعل المستخدم يتجاوز هذه الخطوة. يعرض الفحص الآن رابط CHANGELOG لكل حزمة متأخرة، سواء كانت التحديثات رئيسية أو فرعية، مع ذكر السبب: فقد سبق لتحديث فرعي لحزمة SDK أن عدّل أموراً يعتمد عليها هذا المشروع.
  - **`openai` من 3.8.0 إلى 3.10.0 و`anthropic` من 1.3.0 إلى 1.4.0، قراءة ملاحظات الإصدار قبل التثبيت.** من جانب OpenAI، حدث تغيير سلوكي وحيد خلال هذه الفترة — لم يعد الترويسة `Retry-After` الرقمية التي تتجاوز سعة العدد العشري تؤدي إلى التراجع إلى خوارزمية تراجع (backoff) قصيرة بل تُرجع الخطأ الأصلي دون إعادة محاولة — ولا شيء يمس ما يعتمد عليه هذا المشروع: فتسامح النماذج مع الحقول الإضافية لم يتغير، وتم التحقق منه في الكود عند كلا الوسمين، وبالتالي فإن `native_finish_reason` و`error` اللذين يضيفهما OpenRouter إلى أحد الخيارات يواصلان العمل دون مشكلة. ومن جانب Anthropic، ترفض آلية حماية جديدة عبر `TypeError` صريح كائناً من الحزمة `httpx` حيث تتوقع حزمة SDK كائن `httpx2`؛ وهذا المشروع لا يمرر سوى أعداد عشرية، وتم التحقق من ذلك. والقاعدة التي ترفض أي استدعاء غير متدفق يتجاوز عشر دقائق متطابقة بايت لبايت بين الإصدارين: والواقع أن `timeout` الصريح هو ما يعفي من ذلك، حيث تتجاوز الـ 32,768 توكن الخاصة به عتبة الـ 21,333.
  - **تم التحقق**: مجموعات اختبارات تضم 502 + 31 اختباراً على الإصدارات المحدثة، وإغلاق `requirements.txt` مطابق للمثبت (41 تثبيتاً دقيقاً)، واستدعاء فعلي واحد لكل حزمة SDK — OpenAI وClaude — على وثيقة في وضع `--news`، مع هيكل مطابق للمصدر.

- **1.13.0** مزود `--use_openrouter`: موجه مدفوع لنحو 430 نموذجاً، بما في ذلك النماذج الصينية المفتوحة (2026-09-05):

  - **المسار التاسع للمزودين، تم تسليمه مع المسار الثامن.** لم يتم نشر الإصدار 1.12.0 على PyPI: فالموجهان، OpenCode وOpenRouter، يصدران معاً. يتيح [OpenRouter](https://openrouter.ai) الوصول بمفتاح واحد إلى نماذج لا يوفرها أي مزود آخر هنا — Kimi وQwen وDeepSeek وZ.ai — برصيد موحد يُحاسب حسب الاستخدام. وبما أن نقطة النهاية متوافقة مع OpenAI، فإن العميل هو نفسه المستخدم لـ xAI؛ **كل ما يميز هذا المزود يكمن في فحص ما قبل التشغيل (preflight)**، وكل قاعدة من قواعده مستمدة من قياسات فعلية على واجهة برمجة التطبيقات.

  - **يتم تقديم النموذج نفسه بواسطة عشرات المضيفين بسقوف متباينة، والتوجيه لا يراعي ذلك.** القياس: 33 مضيفاً لـ `z-ai/glm-5.2`، و23 لـ `z-ai/glm-5.3-flash` — أحدهم محدد بسقف **2,048 توكن للإخراج**. وبالتالي فإن ترجمة طويلة واحدة من أصل 23 كانت تخرج مقتطعة، عشوائياً، دون أدنى تنبيه. يقرأ فحص ما قبل التشغيل `/api/v1/models/{modèle}/endpoints`، ويستبعد المضيفين الذين يقل سقفهم عن 8,000 توكن للإخراج، والذين يعانون من حالة متدهورة، والذين لا يصرحون بأي سقف، ثم يثبت الباقين. إن `provider.only` **دون** `allow_fallbacks: false` ليس سوى تفضيل: إذ يعود الموجه إلى مضيف مستبعد، ويفقد التثبيت قيمته. وإذا لم يلتزم أي مضيف بالسقف، يتوقف الأمر: فالترجمة رغم ذلك تعني قبول الاقتطاع الصامت الذي وُجد هذا الفحص لمنعه.

  - **يُحاسب التفكير بسعر الإخراج، ويكون مفعّلاً افتراضياً في العديد من النماذج.** بالطلب نفسه على `z-ai/glm-5.2`، والرد «OK»: **107 توكن إكمال وفق الإعداد الافتراضي للنموذج، و2 عند إيقاف التفكير**. وفي الترجمة، التي لا يضيف لها التفكير أي فائدة، يمثل ذلك تضاعفاً بمقدار 18 مرة في كل جزء من كل ملف. لذا تم إيقافه افتراضياً. والنماذج البالغ عددها **288 من أصل 431** التي تفرضه (`reasoning.mandatory`) ترد بـ `400 « Reasoning is mandatory for this endpoint and cannot be disabled »`: بالنسبة لها، يقرأ فحص ما قبل التشغيل مستويات الجهد المقبولة ويطلب أدناها (النقطة التالية) — يخصص الجهد **نسبة مئوية من `max_tokens`** يستهلكها التفكير أولاً، بحيث تؤدي أي قيمة تُختار عشوائياً إلى إزاحة خطر الصفحة البيضاء بدلاً من تقليله.

  - **النموذج الذي يفرض التفكير يتلقى أدنى مستوى جهد يقبله، وكان القياس هو الفيصل في هذا القرار.** كان الخيار الأول هو عدم إرسال أي شيء، لتجنب التخمين نيابة عن النموذج. وعند اختباره على `z-ai/glm-5.3-flash`، الذي يكون خياره الافتراضي في الدليل هو `max`، أدى هذا الخيار إلى إخراج **مقتطع عند 32,768 توكن** قبل نهاية الترجمة — مع فقدان لغتين من أصل أربع عشرة لغة. ولم يكن رفع السعة ليغير شيئاً: فالجهد يخصص نسبة مئوية منها، ويتسع التفكير مع اتساعها. لذا يقرأ المزود `supported_efforts` أثناء فحص ما قبل التشغيل ويطلب أدنى مستوى، مع التراجع إلى «لا شيء» عندما لا يعلن الدليل عن أي خيار قابل للاستخدام. اختبار التحقق المضاد على اللغة المعنية: كانت تفشل بسبب نفاد الميزانية، وأصبحت تكتمل الآن في 9 دقائق بهيكل مطابق للمصدر.

  - **أصبح عطل المضيف المصدر يحمل اسمه الآن.** يوحّد الموجه هذه الحالة في `finish_reason=error` مع `native_finish_reason` فارغ — تم قياس ذلك مرتين، على لغتين، عند 750 ثانية بالضبط. وكانت الرسالة العامة تدفع للبحث عن خطأ في الوثيقة أو في التقسيم؛ أما الآن فهي تفيد بأن العطل من جانب المزود وأن مجرد إعادة المحاولة غالباً ما تفي بالغرض.

  - **إن `finish_reason=length` مع مخرجات فارغة ليس اقتطاعاً.** بل هو استهلاك الميزانية بالكامل بواسطة التفكير قبل ظهور أول حرف مفيد — تم قياس ذلك عند 15,850 توكن تفكير مقابل 148 توكن مفيد. وتتطلب الحالتان إجراءات متعارضة: فتقليل حجم الأجزاء لا يجدي نفعاً في الحالة الأولى. وتفرق الرسالة بينهما بشكل صريح. وهناك آليتا حماية أخريان ناتجتان عن القياسات: يرد الموجه بـ **200 مع متن لا يحمل سوى خطأ** عندما يفشل المضيف المصدر (كان `choices[0]` يطلق خطأ `TypeError` مبهماً يحجب الرسالة)، وتُقرأ نافذة السياق من الدليل ثم تُسجل في `MODEL_TOKEN_LIMITS` — علماً بأن `DEFAULT_TOKEN_LIMIT` غير صحيح لـ 44 نموذجاً في الدليل، اثنان منها محددان بسقف 4,095 توكن.

  - **`--model fournisseur/modèle` إلزامي، ويتم التحقق من صيغته قبل أي اتصال بالشبكة.** OpenRouter ليس مزوداً مستقلاً: فالاختيار يحدد السعر والرخصة ومعالجة البيانات، ولا يتم نيابة عن المستخدم. وبما أن الاسم اللطيف (slug) يُدمج في عنوان URL الخاص بفحص ما قبل التشغيل، فإن التحقق ليس مجرد ميزة لتحسين تجربة الاستخدام، بل هو خط دفاع يمنع حقن مسار فيه: فالتعبير النمطي ذو مساحة الأسماء المشترك بين الموجهين يقبل `a/b/..`، ومن هنا جاء الرفض الصريح للمسار الأب. و`--eco` عديم التأثير ويشير إلى ذلك صراحة.

  - **تصحيح ثلاث صياغات، إحداها كانت خاطئة.** كان تحذير OpenAI بشأن `codex exec` يستهدف حقن ملف جلسة شخصي على بيئة تشغيل (runner) مشتركة، وليس الطبيعة العامة للمستودع: فقد تم الاستشهاد به بشكل عكسي في README وفي CLAUDE.md وفي الكود البرمجي. وتغير موقع مصادقة OpenCode في 1.18.27 (الجدول `credential` من `opencode.db`، ولم يعد `auth.json`)؛ وظل المبدأ الثابت «لا يُقرأ هنا أبداً» صحيحاً، بينما كان العنوان قديماً. وأخيراً، لم يعد قسم OpenCode يقدّم مسارات لم تُختبر قط على أنها متكافئة: فقد تم قياس جسر Zen وOllama من البداية إلى النهاية، في حين لم يتم ذلك مع GitHub Copilot وLM Studio وllama.cpp، وأصبح ملف README يذكر ذلك صراحة.

  - **حملة قياس، وجدول بالنماذج الموصى بها في ملف README.** تم تنفيذ أكثر من ثلاثمائة عملية ترجمة على ثلاث مجموعات من الوثائق — مقال مدونة كثيف في وضع `--news`، وملف README هذا بصيغة Markdown قياسية، وأربعة ملفات README لمشاريع معروفة أُخذت كما هي من GitHub — نحو أربع عشرة لغة. يميز الجدول بين أمرين كانا يختلطان على المستخدمين: الترجمات التي **تكتمل بنجاح** وتلك التي يكون **هيكلها مطابقاً** للمصدر. ثلاثة نماذج لم تفقد أي معلومة على الإطلاق في الوثيقتين الكثيفتين: `gemini-3.7-flash`، و`gpt-5.6-sol` عبر اشتراك ChatGPT، و`z-ai/glm-5.2` عبر OpenRouter — وكانت فروقها الوحيدة عبارة عن زوج من `**` لم يُنقل في لغة أو لغتين. والخلاصة الأساسية هي أن **العامل الحاسم هو كثافة الوثيقة، وليس وضع `--news`**: إذ يفشل Grok عبر الاشتراك ثلاث عشرة مرة من أصل أربع عشرة في مقال المدونة وينجح في أربعة عشر ملف README عاماً من أصل ستة عشر، والسبب وراء ذلك هو انقطاع في الأجزاء الطويلة تم تأكيده باختبار مضاد. ويحمل الجدول تحذيره الخاص: فهو ليس شاملاً، وهو مؤرخ، والمُدد الزمنية لا تعكس أي ترتيب تفضيلي، والنهج السليم يظل هو القياس على وثائقك الخاصة.

  - **تم تصحيح أداة مقارنة الهيكل قبل نشر أرقامها، لأنها كانت تعطي نتيجتين إيجابيتين كاذبتين مع أنظمة الكتابة غير اللاتينية.** إذ إن أي رابط URL يتبعه قوس كامل العرض `）` لم يكن يُقطع بواسطة تعبير نمطي يتوقف عند `)`، وكانت السلسلة المستخرجة تختلف رغم تطابق الرابط؛ كما أن اقتباساً من خمسة أسطر بالفرنسية يقع في ثلاثة أسطر بالصينية كان يؤدي إلى انخفاض في عدد الأسطر. وتم التحقق من كلا التصحيحين باختبار مضاد: فحذف رابط أو قسم أو كود مضمن يظل قابلاً للاكتشاف. ولولا هذان التصحيحان، لكانت نتائج Gemini وCodex قد نُشرت عند إحدى عشرة لغة من أصل أربع عشرة بدلاً من ثلاث عشرة واثنتي عشرة.

  - **الاختبارات**: ملف جديد `tests/test_openrouter_provider.py` (76 اختباراً) — التحقق من النموذج ورفض المسار الأب، وتثبيت المضيفين (السقف، الحالة، السقف غير المصرح به، الحد الأدنى المشترك)، و`allow_fallbacks` دائماً خطأ، وإيقاف التفكير أو تركه وفقاً لـ `mandatory`، وعقد إخراج مكتمل (خطأ في استجابة 200، انعدام الخيارات، التمييز بين الصفحة البيضاء والاقتطاع، و`finish_reason` غير طبيعي، والمحتوى الفارغ)، وفشل فحص ما قبل التشغيل عند الإغلاق (fail-closed) في حال تعذر الوصول إلى الدليل، وغياب الاسم اللطيف (slug)، وغياب المضيف السليم، وحصرية الخيار وتسمية ملفات الإخراج. وتصل المجموعة الكاملة إلى **502 اختباراً**.

  - **إعادة هيكلة: تم تقسيم الوحدة البرمجية الوحيدة المكونة من 4,253 سطرًا إلى وحدات متعددة، دون تغيير سطر واحد من السلوك.** تم توزيع `src/aipmt/translate.py` على `config`، و`markdown`، و`segmentation`، و`guards`، و`placeholders`، و`news`، و`prompts`، و`notes`، و`naming`، و`pipeline`، و`cli` وحزمة فرعية `providers/` (وحدة واحدة لكل provider، و`base` كأساس، و`registry` للحل والتوجيه). كل نقلة عبارة عن commit يتم إثبات صحته ميكانيكيًا: تقارن أداة فحص شجرة القواعد المجردة (AST) لجميع عقد المستوى الأول في الحزمة بلقطة مرجعية، وتتحقق من موضع كل رمز، وبقاء علامات الأمان حرفيًا، وعدم وجود أي ملف غير متتبع — وهي أدوات مؤقتة، أُزيلت في الإصدار التالي. ما يتغير ويمكن ملاحظته: يصبح `aipmt.translate` واجهة (façade) تُعيد كشف الأسماء الـ 64 التي كانت الوحدة تعرضها دون بادئة `_`، وذلك عبر مطابقة الكائنات (يحمل `__all__` تسعة منها، وهي واجهة برمجة التطبيقات المدعومة؛ والأسماء الأخرى هي أسماء مستعارة للتوافقية)، ويتوقف عن إعادة تصدير الأسماء الـ 29 للتبعيات والمكتبة القياسية التي كان يجمعها `import *`؛ ولم يعد التنفيذ المباشر للملف (`python src/aipmt/translate.py`) موجودًا — ويظل `aipmt` و`python -m aipmt` هما الصيغتان المدعومتان؛ وأصبح `__module__` للدوال العامة هو الخاص بوحدة تعريفها؛ ويتم استيراد حزم SDK بعد تحميل `.env` وليس قبله، دون أي عواقب معروفة. تم الإبقاء على جميع الاختبارات الـ 427 بنفس المعرّفات تمامًا وترحيلها إلى الوحدات التي تختبرها: 91 تصحيحًا كانت تمر عبر الواجهة تستهدف الآن الوحدة التي تستعلم عن الاسم (اثنان منها ظلا باللون الأخضر بدون التصحيح الخاص بهما، بعد القياس)، وسبعة اختبارات عقود تقفل الواجهة، كما أُعيدت كتابة أدوات بوابات الفحص قبل النقلة الأولى حتى لا يتحول أي منها إلى اللون الأخضر بالتوقف عن التحقق — نطاق Lizard بالأدلة مع حد أدنى، والأعلام تُقرأ من المحلل المبني، وحد أدنى للتغطية لكل حزمة، و`release.sh` الذي يعدد الوحدات المتابعة.
  - **إصلاح (مراجعة طلب السحب)**: يرفض OpenRouter بطاقة كتالوج بدون `context_length` بدلًا من تسجيل القيمة الافتراضية البالغة 128,000 رمز كإجراء — وهو ما كان يُعطل أيضًا التحذير «نموذج غير مدرج» — وعندما يكون `finish_reason` قيمة فارغة (النوع الموثق `string | null`)، فإن السبب الخام للمستضيف هو المعتمد، حيث يساوي `max_tokens` القيمة `length`؛ والخطأ المحمول بواسطة الخيار نفسه يرفض المحتوى الجزئي المصاحب له، مع نفس الرسالة مثل العطل الأولي الموحد — تفاصيل المستضيف والسبب الخام والنصيحة مجتمعة. يرد OpenCode بخطأ العقد الخاص به، وليس `AttributeError`، عند حدث `part: null`، ويرفض تدفق JSONL الذي يحتوي على سطر حدث غير قابل للقراءة بدلًا من قبول نص جزئي. تقتل واجهات CLI الوكيلة الثلاث مجموعة الوكيل عندما تتلقى هذه العملية `SIGTERM` أثناء الاستدعاء — كان `timeout` الخاص بإعادة التوليد يترك الوكيل على قيد الحياة ويستهلك حصته — ويتبع `SIGKILL` الخاص بالمجموعة دائمًا مهلة السماح، وهو shim يموت بنظافة تاركًا عمليته الفرعية حية. تم إعادة ربط زوج `# fmt: off` / `# fmt: on` الذي فُصل بسبب التقسيم؛ وتحدد مساعدة `--reasoning_effort` موفري الخدمة الأربعة الذين يستهلكونه.
  - **إصلاح (المراجعة الثانية)**: يحجز سقف الإخراج المطلوب من OpenRouter الآن حصة السياق التي تستهلكها رسالة الحث والقطاع — يغطي `context_length` الإدخال والإكمال معًا، وكانت ستة نماذج في الكتالوج تُرسل طلباتها دون وجود مساحة كافية للإدخال؛ ويتم رفض السياق القصير للغاية قبل أي فوترة. تتراجع قاعدة واجهات CLI الوكيلة إلى `terminate` ثم `kill` حيث لا تتوفر مجموعات عمليات POSIX، بدلًا من السماح لـ `AttributeError` بتجاوز حارس المهلة الزمنية وتمديد الانتظار. يُبحث عن علامة `429` في OpenCode كرقم وليس كسلسلة فرعية، حيث كان معرّف خطأ مثل `err_84290b` يُشغّل تراجعًا تدريجيًا لمدة 90 ثانية قبل أن يفشل في النهاية على أي حال. أخيرًا، يتم عرض نقطة نهاية OpenRouter غير القياسية أثناء الفحص المسبق: يكفي وجود `.env` خاص بالمشروع لتعيينها، وهو ما كان سيتسبب في إرسال المفتاح الحقيقي لاحقًا.
  - **أمان: لم يعد بإمكان `.env` الخاص بالمشروع إعادة توجيه استدعاءات واجهة برمجة التطبيقات.** يبحث `find_dotenv(usecwd=True)` عن الملف بدءًا من الدليل الحالي والأدلة الأصلية له: يمكن لشجرة ملفات غير موثوقة — مستودع تم استنساخه للتو — أن تضع فيه `OPENROUTER_BASE_URL`، أو `XAI_BASE_URL`، أو `OPENAI_BASE_URL` (والأخير تقرأه حزمة SDK نفسها) دون معرفة أي مفتاح، ليُرسل المفتاح الحقيقي، الوارد من البيئة أو من تكوين المستخدم، لاحقًا في ترويسة المصادقة لخادم خارجي. يعتمد المرشح على نمط وليس على قائمة: بعد الفحص عبر حزم SDK المثبتة، تبيّن قراءة اثني عشر متغيرًا للتوجيه، ستة منها يقرؤها عميل Anthropic وحده — وكان من شأن حصر مكتوب يدويًا أن يغفل نصفها. لذلك، يُرفض من طبقة المشروع أي متغير ينتهي بـ `_BASE_URL` أو `_API_BASE` أو `_ENDPOINT`، والوكلاء ومخازن الشهادات (إذ إن الإشارة إلى هيئة شهادات خاضعة للسيطرة تجعل أداة الاعتراض غير قابلة للتمييز عن الخادم الحقيقي)، بالإضافة إلى `XDG_CONFIG_HOME` و`APPDATA`: حيث كان تعيينهما يعادل تحديد أي ملف يشكّل طبقة المستخدم، وبالتالي الالتفاف على المرشح بطرق ملتوية. لا تُقبل هذه المتغيرات إلا من الطبقتين اللتين يتحكم فيهما المستخدم: البيئة المصدرة و`~/.config/aipmt/.env`. علاوة على ذلك، تُقرأ طبقة المشروع دون استيفاء: يقوم `load_dotenv` بتوسيع `${VAR}` افتراضيًا، وكان ملف `.env` غير موثوق يحتوي على `NOM_ANODIN=${OPENAI_API_KEY}` ينسخ المفتاح الحقيقي تحت اسم لا يتعرف عليه التصفية بالنمط للعمليات الفرعية — مما يجعله يدخل في بيئة `codex exec`، خلافًا للثابت المعلن. وأخيرًا، لا يُظهر الرفض سوى اسم المتغير: إذ كان عنوان URL بالصيغة `https://${CLE}@hôte/` يسرب المفتاح المستوفى في السجلات، حتى أثناء رفضه. يتم توضيح الرفض على stderr مع بيان الخطوات الواجب اتباعها — يتم الإعلان عن وسيط الشركة في تكوين المستخدم.
  - **إصلاح: يتم حساب غلاف الإخراج لـ OpenRouter لكل استدعاء.** يغطي `context_length` الإدخال والإكمال معًا، والاحتياطي الثابت الذي تمت معايرته على النص اللاتيني لا يحد من شيء: فبقياس ذلك عبر أداة الترميز `o200k_base`، تعادل 16,000 خانة 3,200 رمز في اللغة الفرنسية، لكنها تعادل 12,300 في اليابانية و17,500 في الرموز التعبيرية. لذا فإن الميزانية تُحسب من النص المرسل فعليًا، مضافًا إليه حده الأعلى بعدد بايتات UTF-8: ففي أي أداة ترميز تدمج البايتات — BPE على مستوى البايت، أو SentencePiece مع الرجوع للبايت، وهي العائلات التي يستخدمها الكتالوج — يساوي الرمز بايتًا واحدًا على الأقل، وهذا هو الحد الأعلى الوحيد المتاح هنا، حيث يوجه OpenRouter الطلبات إلى العشرات من أدوات الترميز التي لا نعرفها. لم تكن أي نسبة متوسطة مناسبة — ينخفض الرمز الفكري في المستوى التكميلي إلى 1.33 بايت لكل رمز، وحرف الدمج إلى 1.00 —، وبات الإدخال بالإضافة إلى الإخراج ملائمين للنافذة هيكليًا. يتم رفض أي قطاع كثيف للغاية بالنسبة للنموذج المختار قبل الاستدعاء بدلًا من رفضه بعد الفوترة.

- **1.12.0** موفر الخدمة `--use_opencode`: OpenCode، الوكيل مفتوح المصدر، إلى المزود الذي يختاره — نموذج محلي، أو مجاني بدون حساب، أو اشتراك، أو مفتاح (2026-09-04):

  - **مسار المزود الثامن، ذو طبيعة مختلفة عن المسارات السبعة الأولى.** [OpenCode](https://opencode.ai) (MIT) ليس موفرًا للنماذج بل هو _موجّه_ إلى النماذج التي ضبطها المستخدم في OpenCode نفسه: مفتاح API، أو اشتراك (GitHub Copilot، أو ChatGPT، أو SuperGrok)، أو بوابة OpenCode Zen — التي توفر نماذج مجانية **بدون حساب** —، أو نموذج **محلي** (Ollama، أو LM Studio، أو llama.cpp). يتحكم السكربت في `opencode run` في الوضع غير التفاعلي، تمامًا كما يتحكم في Codex وGrok، ويعيد استخدام نفس قاعدة العمليات الفرعية (مجموعة عمليات خاصة، و`SIGTERM` ثم `SIGKILL` عند انتهاء المهلة، وstdin مغلق دائمًا، وبيئة منقاة). تم التحقق عبر **ترجمتين فعليتين**: ملف README هذا بأكمله إلى الإنجليزية عبر `opencode/mimo-v2.5-free` — في 49 ثانية، وتمريرة واحدة، وبنية مطابقة لملف المصدر (32 عنوانًا، و26 إغلاق كتلة كود، و18 رابطًا، و37 عنوان URL، و37 سطر جدول، و135 كودًا مضمنًا) — وملف تجريبي عبر `ollama/qwen2.5:7b`، محليًا، دون أي مفتاح.

  - **`--model provider/modèle` إلزامي، وهذا خيار مقصود.** بدون `--model`، يعود OpenCode إلى وضعه الافتراضي الخاص، والذي يكون في التثبيت النظيف `opencode/big-pickle`، وهو نموذج مجاني «خفي» يمكن استخدام محادثاته للتدريب — وبحسب القياس، كان هذا النموذج هو الذي استجاب. واختيار ذلك ضمنيًا نيابة عن المستخدم يمثل تمامًا نوع التحويل غير المرئي الذي يتتبعه هذا المستودع؛ لذا تذكر رسالة الخطأ الأمر الذي يسرد النماذج (`opencode models`) وثلاثة أمثلة (محلي، مجاني، اشتراك). `--eco` ليس له أي تأثير ويذكر ذلك صراحة. يتم تمرير `--reasoning_effort` كما هو كـ `--variant` لـ OpenCode، بناءً على طلب صريح فقط.

  - **عزل مقاس، وليس مفترضًا.** يحدد تكوين مضمن (`OPENCODE_CONFIG_CONTENT`، وهو الأخير في ترتيب الدمج لـ OpenCode، وبالتالي له الأولوية على تكوين المستخدم دون استبداله) وكيلاً `aipmt` رُفضت جميع أدواته (`permission: {"*": "deny"}`): لم يعد السجل يعرضها حتى على النموذج، والذي عند مطالبته بـ «سرد الملفات وتشغيل `id`» يجيب بأنه لا يمتلك أدوات. تمت إضافة مشاركة الجلسة كمعطلة، واستبعاد المكونات الإضافية الخارجية (`--pure`)، وعدم استخدام `--auto` إطلاقًا، مع دليل عمل مؤقت وفارغ. تم قياس عمليتي حقن صامتتين وقطعهما: بدون `OPENCODE_DISABLE_CLAUDE_CODE`، يدخل `~/.claude/CLAUDE.md` الخاص بالمستخدم في **كل** رسالة حث (515 رمز إدخال بدلًا من 186 لمجرد «Bonjour»)؛ وبدون `OPENCODE_DISABLE_PROJECT_CONFIG`، يدخل ملف `AGENTS.md` من الدليل الحالي أيضًا — حيث طُبقت تعليمة «إنهاء كل إجابة بكلمة BANANA» على الترجمة. أما `~/.config/opencode/AGENTS.md` العام فيظل محقونًا: فلا يوجد مفتاح يستبعده، والالتفاف عليه عبر `XDG_CONFIG_HOME` مخصص من شأنه أيضًا إخفاء موفري الخدمة الخاصين بالمستخدم. تم توثيقه بدلًا من اللجوء إلى حلول ترقيعية.

  - **`exit 0` لا يثبت شيئًا، واجهة CLI ثالثة، نفس ردود الفعل — مع فخين خاصين بهذا الأخير.** إن وجود `--agent` غير معروف لا يجعل `opencode run` يفشل: تحذير على stderr وتراجع **صامت** إلى وكيل البرمجة، مع بقاء الأدوات نشطة. وإذا لم يُؤخذ التكوين المضمن في الاعتبار، فستبدأ الترجمة إذن باستخدام وكيل قادر على الكتابة؛ يتحقق عقد الإخراج من غياب هذه الرسالة، بالإضافة إلى: رمز إرجاع 0، وعدم وجود أي حدث `error`، وعدم وجود أي `tool_use`، وكون آخر `step_finish` هو `stop` (حيث يُعد `length` استجابة مقتطعة)، ونص غير فارغ. الفخ الثاني: حدث الخطأ بتنسيق JSON **غامض** — «Unexpected server error. Check server logs for details.»، مع مرجع بسيط فقط — والسبب الفعلي (`ProviderModelNotFoundError: Model not found: foo/bar. Did you mean…`، `ProviderAuthError`...) لا يوجد إلا في السجلات. ومن هنا جاء `--print-logs --log-level ERROR` وقراءة الحقل `error="…"` من stderr، دون تتبع Bun الذي يليه. وبالتالي يفشل النموذج غير المعروف في ثانية واحدة، مع ذكر سببه. ويتجنب `--title` بالمناسبة استدعاء LLM طفيليًا: بدونه، يولد OpenCode عنوان جلسة عبر جولة إضافية على `small_model`.

  - **الأسرار: نفس التصفية بالنمط مثل Codex وGrok، مع استثناء اسمي وحيد.** يتم الاحتفاظ بـ `OPENCODE_API_KEY`: فهو مفتاح OpenCode نفسه (بوابة Zen، واشتراك Go)، والموجّه إليه باسمه — وهو ما يعادل `auth.json` الخاص به، وليس مفتاحًا يديره aipmt أو يمكنه فوترته. يتم تكوين موفري الخدمة داخل OpenCode (`opencode auth login`، `opencode.json`)، وليس أبدًا في `.env` الخاص بـ aipmt، والذي لا يصل أي مفتاح منه إلى العملية الفرعية. لا يوجد رفض في CI، على عكس واجهات CLI القائمة على الاشتراك: فمفتاح API أو نموذج مستضاف ذاتيًا على runner يمثلان استخدامات مشروعة.

  - **حارس منع الاختراق يتحقق الآن من القيمة المستوفاة، وليس من القيمة الخام.** يحتوي `provider/modèle` على `/` كان حارس الإصدار 1.10.0 يرفضه — بحق، نظرًا لأن `--model` يُستوفى في اسم الملف `--include_model`. يستبدل وصف اسم الملف الآن `/` و`\` و`:` بـ `-` قبل أي استيفاء (`ollama/qwen2.5:7b` ← `ollama-qwen2.5-7b`، حيث إن `:` غير قانوني في Windows)، ويفحص الحارس الأولي هذا الوصف: يصبح `../../evil` مجرد الاسم `doc-en-..-..-evil.md` تحت الهدف، ويظل `..` وحده مرفوضًا، وكذلك `--target_lang ../x`. ويظل حارس النطاق `_ensure_within_directory` هو الطبقة الثانية، دون تغيير.

  - **النماذج المجانية والنماذج المحلية، ما تم قياسه.** يترجم `opencode/mimo-v2.5-free` فقرة في 16 ثانية وملف README هذا في 49 ثانية؛ ويستغرق `opencode/big-pickle` 40 ثانية لكل 200 كلمة، وظل طلبان متزامنان دون استجابة لمدة 5 دقائق في حين نجح كل طلب بمفرده؛ ولم يستجب `opencode/nemotron-3.5-lightning-free` بأي شيء خلال 3 دقائق. ومن هنا جاء `REGEN_PROVIDER=opencode` مع كون `REGEN_MODEL` إلزاميًا و**مهمتين** بالتوازي. على الجانب المحلي، غالبًا ما يقوم Ollama بتهيئة سياق قدره 4,096 رمزًا في حين تصل المقاطع إلى 16,000 حرف: لذا يلزم استخدام `Modelfile` مع `PARAMETER num_ctx 32768`، وتتبع الجودة النموذج — فقد عكس نموذج 7B قائمة وأفسد إغلاق كتلة كود في الملف التجريبي، بينما حافظ نموذج البوابة على كل شيء.

  - **لم تعد الترجمات الخاصة بهذا المستودع تمر أبدًا عبر واجهة برمجة تطبيقات مدفوعة.** كان `regen_translations.sh` يعتمد واجهة برمجة تطبيقات OpenAI بمجرد وجود مفتاح في `.env`، ولم يكن يجعل Codex سوى خيار إضافي. وهذا بالضبط ما حدث أثناء إعداد هذا الإصدار: أُرسلت الترجمات الـ 28 إلى واجهة برمجة تطبيقات OpenAI، ثم ملف CHANGELOG باللغة الهندية إلى واجهة Gemini، بالرغم من أن اشتراك ChatGPT موجود خصيصًا لتجنب الدفع حسب الاستخدام. اختفى الاكتشاف التلقائي للمفاتيح: **Codex هو الخيار الافتراضي، مع `gpt-5.6-sol`**، نموذج الجودة؛ ويتطلب كل من `openai` و`gemini` و`grok` وجود `REGEN_ALLOW_PAID_API=1` بالإضافة إلى `REGEN_PROVIDER`، وهو استثناء محدد بالاسم لتطبيق القاعدة عند اتخاذ القرار؛ ويفشل أي `REGEN_PROVIDER` غير معروف بدلًا من الرجوع إلى واجهة برمجة التطبيقات. تقفل عشرة اختبارات السلوك الافتراضي والرفض والاستثناء. وأُعيدت الترجمات الـ 28 لهذا الإصدار عبر Codex.

  - **تمت إعادة تصنيف التراجع التدريجي عند حد المعدل** (`_retry_on_rate_limit`): كانت حلقات Codex وGrok متطابقة باستثناء الصياغة، وكان من شأن نسخة ثالثة أن تتجاوز حد التكرار. تشتق أخطاء CLI الثلاثة من نفس `_CliCallError`؛ ويمنع اختبارٌ خروج أي منها عنها، وإلا فلن تراها الحلقة المشتركة بعد الآن.

  - **الاختبارات**: ملف جديد `tests/test_opencode_provider.py` (61 اختبارًا) — عقد إخراج كامل، وتراجع الوكيل، والسبب مقروء من السجلات، وأجزاء النص المزالة التكرار والأجزاء الاصطناعية المتجاهلة، وانتهاء المهلة الذي ينهي مجموعة العمليات، والتراجع التدريجي عند 429، والنموذج الإلزامي والمتحقق منه، وفحص مسبق دون أسرار، وحل الملف التنفيذي، وتوصيل التوجيه، ووصف اسم الملف واختبار عكسي للاختراق. يوسع `tests/test_review_hardening.py` حصرية الأعلام وغياب الأسرار ليشمل موفر الخدمة الجديد. وتتطلب البوابة الآن توثيق **22 علمًا** في argparse. مجموعة اختبارات كاملة تبلغ **382 اختبارًا**.

- **1.11.1** تصحيح في التوثيق: يُعلن ملف README أخيرًا عن مسارات موفري الخدمة السبعة (2026-09-03):

  - **كانت صفحة PyPI للإصدار 1.11.0 تذكر «4 واجهات برمجة تطبيقات (APIs) + واجهة سطر أوامر (CLI) Codex».** يعرض الكود سبعة — OpenAI وMistral وClaude وGemini وGrok عبر API؛ وCodex (ChatGPT) وGrok عبر الاشتراك، دون فوترة حسب الاستخدام. كان وضعا Grok غائبين عن جملة الجذب وعن النقطة _Multi-Provider_، وكانت الترجمات الـ 14 تكرر الخطأ. ونظراً لأن الوصف المطول للحزمة يكون ثابتاً لكل إصدار، فإن تصحيح الواجهة كان يتطلب رقماً جديداً: وهذا هو السبب الوحيد لوجود هذا الإصدار. **لا توجد أي تغييرات في الكود.**
  - تم ضبط `CLAUDE.md` ليتوافق مع ما قدمه النشر: عدادات بوابة الفحص (gate) (16، و17 في `--full`)، وسير العمل (workflows) الإحدى عشرة النشطة، وعدادا Sonar/Codacy غير المرئيين في `gh pr checks` (النقاط الساخنة (hotspots)، وواجهة برمجة تطبيقات Codacy)، ونقل `# nosemgrep` بواسطة `ruff-format`، وبيئات GitHub التي يتطلبها تبادل OIDC، وحقيقة أن الـ _pending publisher_ لا يحجز الاسم.

- **1.11.0** النشر على PyPI: `pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، دون استنساخ المستودع (2026-09-03):

  - **تحول البرنامج النصي وحيد الملف إلى حزمة قابلة للتثبيت.** ينتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة إدخال سطر أوامر (console entry point) `aipmt` وما يعادلها `python -m aipmt`. يظل المستودع المستنسخ ضرورياً للمساهمة — حيث توجد الاختبارات و28 ترجمة وأدوات الجودة — ولكن ليس للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` أبداً**، لأن التعارض حقيقي وصامت. تثبت حزمة PyPI المسماة `translate` (الإصدار v3.8.1، آخر رفع في 2026-07-06) دليلاً بالاسم نفسه. أُعيد إنتاج ذلك في venv: يتفوق الدليل على الوحدة البرمجية، ويختفي `translate.main`، وتتعطل نقطة الإدخال مع ظهور `AttributeError` — ويجيب `pip check` بعبارة «No broken requirements found» مع rc=0. كان مجرد تنفيذ `pip install translate` لدى المستخدم كافياً لتعطيل CLI دون تشخيص قابل للاستفادة منه. اختبار عكسي على ملف wheel الحقيقي: `pip install translate` فوق الحزمة، و`aipmt --help` ينتج عنه rc=0 قبل وبعد، ويتعايش كلا الـ CLI معاً.
    - **اسم توزيع طويل، وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للعثور عليها عبر البحث في PyPI؛ إذ سيكون الاختصار وحده غير قابل للعثور لمن لا يعرف المشروع مسبقاً، في حين أن الهدف من النشر هو تحديداً أن يُعثر عليه. تم استبعاد مرشحين محتملين بعد التحقق: `ai-markdown-translator` محجوز على npm منذ 2024 لأداة لها الغرض نفسه، وتسبق هذا المستودع بـ 17 شهراً؛ و`aimt` يختلف بحرف واحد عن `aim` (الإصدار v3.29.1)، وهي حزمة نشطة في المجال نفسه — وهو أسوأ سيناريو لارتباك دائم. فخ منهجي بالمناسبة: يعيد `pypi.org/project/<nom>/` الرمز 200 لأي اسم كان (صفحة لمكافحة الروبوتات)، وواجهة برمجة تطبيقات JSON وحدها هي المعتمدة.
    - **هيكل `src/` بدلاً من الحزمة المسطحة.** كانت الحزمة المسطحة ستحافظ على عناصر `sys.path.insert(..., "..")` الستة في الاختبارات، وذلك تحديداً هو العيب: إذ إنها تجعل الاستيراد يتم من شجرة المصدر بدلاً من الحزمة، مما قد يخفي أي خطأ في التحزيم. التكلفة الحقيقية: قاعدة استبدال إضافية واحدة.

  - **أصبح من الممكن أخيراً تكوين المفاتيح مرة واحدة وإلى الأبد.** لم تكن أداة CLI المثبتة تمتلك أي تكوين دائم: لم يتبق سوى متغير البيئة وملف `.env` في الدليل الحالي. يصعد `find_dotenv` بالتأكيد حتى جذر النظام وكان يجد بالتالي `~/.env` **عند العمل تحت الدليل الشخصي**، لكن لا شيء عند العمل في مكان آخر — وهي تغطية كانت تعتمد على المكان الذي يُشغَّل منه الأمر، وليس خياراً تصميمياً. لذلك أُضيفت طبقة ثالثة: `~/.config/aipmt/.env`، أسفل الطبقتين الموجودتين.

    - **الأولوية ليست مشفرة بشكل صريح**، بل تنبع من `override=False`، وهي القيمة الافتراضية لـ `load_dotenv`: لا تملأ كل طبقة إلا ما تركته الطبقة السابقة فارغاً. ومن هنا يأتي الترتيب: متغير البيئة ← `.env` الخاص بالمشروع ← تكوين المستخدم، والذي تم التحقق منه باختبارات السلوك وليس البنية — فعكس ترتيب الاستدعاءين يؤدي إلى فشلها، وكذلك إزالة الطبقة الثالثة.
    - **تنسيق `.env` وليس TOML**، عن قصد: `python-dotenv` يُعد بالفعل تبعية، وبناء الجملة موثق بالفعل في ملفات README الـ 15، والملف نفسه يخدم كلا النطاقين. لا توجد تبعيات ولا بناء جملة جديد. يتبع المسار `XDG_CONFIG_HOME` إذا كان **مطلقاً** — تتطلب المواصفات تجاهل القيمة النسبية، وإلا أصبح موقع التكوين تابعاً مرة أخرى للدليل الحالي — و`APPDATA` على Windows.
    - **تم استبعاد خيارين، مع ذكر السبب.** سلسلة مفاتيح النظام (`keyring`) أكثر أماناً على جهاز مكتبي لكنها تفشل في البيئات غير الرسومية (headless) — الخادم، الحاوية، CI — أي حالة الاستخدام الفعلية للترجمة المجمعة؛ خيار جيد عبر التفعيل الاختياري (opt-in)، لكنه خيار افتراضي سيئ. أما الراية (flag) `--api-key` فستجعل المفتاح ينتهي به المطاف في سجل سطر الأوامر (shell history) وتجعله مرئياً في `ps`.
    - **في حال غياب المفتاح، لا تتبع للمكدس (traceback) بعد الآن.** كان المستخدم يتلقى مكدس تتبع Python يشير إلى `site-packages`، ورسالة تذكر «البيئة أو .env» دون تحديد مكان إنشاء الثاني. يسرد النظام الآن المواقع الثلاثة مع مسارها الدقيق، وينتهي الأمر بالرمز 2. شبكة الأمان **ضيقة عن قصد**: `except ValueError` يغطي مرحلة التكوين فقط. إن تغليف كامل عملية التنفيذ كان سيحول خطأً برمجياً حقيقياً يحدث أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الفشل الذي يتتبعه هذا المستودع ويمنعه. يقرأ اختبارٌ كود مصدر `main()` لحظر ذلك.

  - **تصحيح — كان يتم تجاهل `.env` الخاص بالمستخدم بمجرد تثبيت الأداة.** لا يصعد `load_dotenv()` دون وسائط من الدليل الحالي بل من الملف المُستدعي، أي من `site-packages`. عند القياس باستخدام نقطة إدخال سطر أوامر حقيقية تم تشغيلها من مشروع يمتلك ملف `.env` خاصاً به: يعيد `find_dotenv()` القيمة `''` ولا يتم تحميل المفتاح، في حين يعثر عليه `find_dotenv(usecwd=True)`. لم يكن هذا الخطأ موجوداً طالما كانت الأداة تعمل فقط من داخل المستودع المستنسخ؛ وكان النشر سيجعله يحدث باستمرار، مع كون العرض الوحيد هو مفتاح API «مفقود» رغم صحة التكوين.

  - **ثلاث بوابات فحص (gates) كانت ستعطي الضوء الأخضر رغم توقفها عن التحقق من أي شيء.** تم تشديدها عمداً قبل عملية النقل: فحاجز الحماية المكتوب بعد التغيير المفترض به رصده لا يثبت شيئاً. كل بوابة تعطي الضوء الأخضر في المستودع الأصلي وتتحول إلى الأحمر في نسخة منقولة — وتم قياس كلا الاتجاهين.

    - **يتجاهل Lizard المسار الغائب بصمت**: rc=0، و«0 file analyzed». وكانت بوابة التعقيد ستنتقل من 158 دالة / 2247 سطر nloc إلى 3 دوال / 34 سطر nloc، مع مخرجات بحجم صفر بايت. أصبح النطاق الآن مصفوفة يتم التحقق من وجود كل عنصر فيها.
    - **لا يفشل `coverage run --source=` عند غياب وحدة برمجية**: يظهر تحذير على stderr فقط، مع rc=0 لكل من unittest و`coverage xml`، ويُنشر التقرير على أي حال — بعد تقليصه من 1453 إلى 141 عبارة برمجية (statements). كان المشروع سيبدو سليماً لأنه بالكاد خضع للتحليل. يحمي حدّان أدنيان التقرير: الإجمالي، وأكبر ملف تم قياسه.
    - **أداة فحص حداثة الترجمات عمياء بنيوياً عن شكل الاستدعاء**: فهي تعتمد على رايات argparse، وهو تحديداً ما لا يغيره إعادة تسمية الملف. أُعيد إنتاج ذلك: نُقلت الوحدة، وملفات README الـ 15 لا تزال توثق أمراً غير موجود، والنتيجة «لا توجد ترجمة قديمة». لذلك يتحقق قسم سابع من الشكل وليس من الخيارات، ويتم مطابقة خطاف (hook) Lizard مع النطاق الفعلي للبرنامج النصي — ومفتاحه `files:` لا يؤدي إلى فشل pre-commit عندما يتوقف عن المطابقة، بل يجعله يتخطاه بالكامل.

  - **`requires-python = ">=3.10"` لم يعد مجرد ادعاء.** كان `sonar-project.properties` يعلن بالفعل عن 3.10-3.12 دون أن يختبر ذلك أي شيء على الإطلاق، إذ لم يكن جهاز التطوير يحتوي إلا على 3.12 — وهو تناقض داخلي كان النشر سيفضحه علناً. يقوم سير عمل الاختبارات الآن بتشغيل المجموعة على الإصدارات 3.10 و3.11 و3.12، عن طريق تثبيت الحزمة وبالتالي حدودها العامة المعلنة.

  - **حدود دنيا، دون سقف أعلى.** يظل `requirements.txt` هو ملف القفل (lock) الذي تم اختباره، ويصبح `[project.dependencies]` هو العقد العام: إذ إن نشر الإصدارات الدقيقة لملف القفل سيؤدي إلى تعارضات لدى أي مستخدم لديه حزم أخرى. ولا يوجد سقف `<N+1` أيضاً — فهو سيتعارض بشكل مباشر مع `check-deps-fresh.sh`، الذي يفشل بوابة الإصدار (release gate) عند أي تأخر في إصدار رئيسي (major). تحل مجموعة الحدود الدنيا المشكلة، والاختبار العكسي `openai==1.0.0` ينتهي بالرمز `ResolutionImpossible`، مما يثبت أن الفحص يميز بدقة بدلاً من قبول كل شيء. علاوة على ذلك، يمنع حارس أمان إصدار `pyproject.toml` من الاختلاف عن إصدار CHANGELOG: إذ لا يسمح PyPI بإعادة استخدام رقم إصدار سابق.

  - **تم التحقق منه من البداية إلى النهاية في venv جديد**: حزمة wheel بحجم حوالي 70 كيلوبايت لا تحتوي إلا على `aipmt/*.py`، وdist-info والترخيص؛ و`aipmt --help` ينتج rc=0 مع 22 راية؛ و`python -m aipmt` يعرض «usage: aipmt» وليس «usage: \_\_main\_\_.py»؛ وتثبيت `pipx` يعمل بنجاح؛ والأهم من ذلك **ترجمة فعلية من fr إلى en من دليل مستخدم عشوائي**، مع الحفاظ على النص العريض، والقائمة، والترميز المضمن، والرابط وعنوان URL، وعدم ترجمة كتلة الكود. تنجح الاختبارات الـ 318 السابقة للهجرة مع قائمة معرّفات متطابقة بالبايت تماماً قبل وبعد — وهذا ما يثبت عدم تحييد أي اختبار، وليس مجرد كلمة «OK»؛ وتُضاف إليها اثنا عشر اختباراً للتكوين ذي الطبقات الثلاث، ليصل المجموع إلى 330.

- **1.10.0** موفر الخدمة `--use_codex` (حصة اشتراك ChatGPT)، وتحديث حزم SDK والنماذج، وإصلاح اقتباسات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — حاجزَا حماية وضعتهما طلب السحب (PR) دون الالتزام بهما في كل مكان**:

    - **كان الفحص المسبق (preflight) لـ Codex ينقل كامل `.env` إلى الملف التنفيذي الثنائي.** استدعى `_codex_preflight` الدالة `subprocess.run` **دون `env=`**: فورثت العملية الفرعية كامل `os.environ`، وبالتالي كامل `.env` المحمل بواسطة `load_dotenv`. عند القياس باستخدام ملف ثنائي وهمي مُهيأ للفحص: وصلت **سبعة أسرار** إلى الفحص المسبق — مفاتيح موفري الخدمة الستة بالإضافة إلى `GITHUB_TOKEN` — مقابل **صفر** لنظيره `_grok_preflight`، الذي كان يمرر `env=_grok_env()` بشكل صحيح. كان هذا التناقض داخلياً في الـ PR نفسها: إذ إن `_strip_secret_env` موجود خصيصاً للحفاظ على هذه الخاصية الثابتة، على بعد أسطر قليلة. تم استخراج `_codex_env_base()` ومشاركته بين المسارين؛ القياس بعد التصحيح: 0 أسرار على كلا الجانبين.
    - **خاصية «`--deny` الفشل بالإغلاق (fail-closed)» لم تكن تغطي الصيغة المستخدمة.** برر التعليق كامل عزل Grok بحقيقة أن أي قاعدة ذات بادئة غير معروفة ترفض بدء التشغيل. عند القياس على `grok 1.0.13`، يتبين أن هذا التحقق **لا يوجد إلا للصيغة المقوسة**: يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل («unknown tool prefix»)، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. في حين أن `GROK_DENY_RULES` لم يكن يستخدم سوى أسماء مجردة — وبالتالي فإن إعادة تسمية أي أداة من جانب xAI كانت ستزيل الطبقة الوحيدة المقاسة للعزل دون أدنى إشعار، وذلك على جهاز لا ينطبق عليه صندوق حماية نظام التشغيل (OS sandbox) أصلاً. تم تحويل القواعد الثماني المسماة إلى `Prefix(*)`، مع التحقق من كل منها كبادئة معروفة لـ CLI؛ ويبقى الخيار الشامل (catch-all) `*` بصيغته الحرفية، وهي الوحيدة المقبولة. يمنع اختبارٌ العودةَ إلى الصيغة غير المتحقق منها.
    - **تم التحقق من النظافة البرمجية في جوانب أخرى**: لا يوجد أي حقن للأوامر (صيغة القائمة في كل مكان، ولا يُستخدم `shell=True` مطلقاً، وتمرير محتوى المستند عبر stdin أو `--prompt-file`)، ولا يوجد فك تسلسل غير آمن (`json.loads` فقط، مع حماية الأنواع)، وتصحيح اجتياز المسار دون العثور على أي تجاوز عبر سبع حمولات ضارة (payloads)، وتطبيق `--deny '*'` فعلياً بواسطة CLI (لوحظ `DENY_ENFORCED` عند القراءة خارج workdir).
    - كان فحص الحداثة المضاف أعلاه يلتف بالمناسبة حول مبدئه الخاص: فالحزمة التي تفشل عملية طلبها من PyPI كان يتم تخطيها بصمت، مع إعطاء بوابة الفحص الضوء الأخضر. أصبح الآن يحصي الحزم التي تمت مقارنتها بالفعل ويفشل إذا كانت التغطية غير مكتملة.

  - **ترقية التبعيات، وشبكتا أمان لمنع تكرار التأخر**:

    - **كان التأخر حقيقياً ومستمراً**: انتقل `openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر سنتين في مخزن شهادات الجذر الذي يتحقق من TLS لجميع استدعاءات موفري الخدمة. السبب المحدد: **لم يكن هناك وجود لملف `.github/dependabot.yml`**. فبدون هذا الملف، لا يفعّل GitHub سوى _تحديثات الأمان_، ولا يقترح Dependabot أي PR إلا لتبعية متأثرة بـ CVE — وهو ما يفسر قيامه بترقية `urllib3` و`idna` مع ترك حزمتي SDK تنحرفان بفارق إصدار رئيسي كامل.
    - **الإصداران الرئيسيان يتعايشان دون تعارض**، على عكس ما كان يخشاه استنتاج سابق: يهاجر `openai` 3.x و`anthropic` 1.x إلى **`httpx2`** بينما يظل `mistralai` و`google-genai` على `httpx<1`، إلا أنهما توزيعتان منفصلتان. تم التحقق من ذلك عبر تثبيت فعلي، ثم عبر **اختبار مسارات موفري الخدمة السبعة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الكود المضمن والروابط في كل مخرج. كان «تجنب وجود مجموعتي HTTP برمجيتين» مجرد تفضيل وليس عائقاً: وقد حسم القياس التجريبي ذلك.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: كان `google-auth` و`cryptography` ومجموعة `opentelemetry` مثبتة في venv العمل دون أن يُعلن عنها على الإطلاق — وبالتالي لم يكن أي تثبيت جديد يعيد إنتاج ما تم اختباره. وعلى العكس من ذلك، كان `tokenizers` و`huggingface-hub` و`PyYAML` موجودة فيه دون استيرادها أو طلبها من أي شيء، كبقايا من `mistralai` 1.x. أُعيد إنشاء الملف ليمثل الإغلاق الكامل لـ venv مبني استناداً إلى التبعيات المباشرة فقط. ولا يُظهر `pip-audit` أي ثغرات أمنية معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** (جديد) يفعّل التحديثات الأسبوعية للإصدارات، لـ pip وgithub-actions. تُجمع الإصدارات الفرعية (minor) والتصحيحية (patch) في طلب سحب (PR) واحد — فتحديث تصحيحي لكل طلب سحب ينتهي به المطاف مهملاً، والضوضاء هي عدو التحديث؛ **أما التحديثات الرئيسية (major) فتكون منفصلة**، وتتطلب كل منها تحققاً عبر استدعاء فعلي.
    - **`scripts/check-deps-fresh.sh`** (جديد، وموصول في بوابة الفحص) يجعل التأخر مرئياً في النتيجة النهائية للمشروع: يقترح Dependabot التحديثات لكنه لا يضمنها، ويمكن لطلبات السحب الخاصة به أن تتراكم. التأخر في إصدار رئيسي ← فشل؛ إصدار فرعي ← تحذير، لأن بقاء بوابة الفحص باللون الأحمر دائماً يؤدي إلى إهمالها؛ عدم إمكانية الوصول إلى PyPI ← تخطٍ صريح محلياً و**إخفاق بالإغلاق (fail-closed) في CI**، إذ إن الفحص غير المنفذ لا يُعد نجاحاً. تم التحقق في كلا الاتجاهين: فهو يرصد الحالة الدقيقة لما قبل التصحيح (`openai 2.54.0→3.6.0`، و`certifi 2024.8.30→2026.7.22`) ويكتفي بالتحذير عند وجود تحديث فرعي.

  - **تصحيحات ناتجة عن مراجعة هذا الـ PR** — قام خمسة وكلاء مراجعة بفحص الفروقات (diff) بدقة؛ وقد تم **إعادة إنتاج النقاط أدناه بالقياس التجريبي** قبل التصحيح، واثنتان منها كانتا تراجعين (regressions) أُدخلا في موضع سابق من هذا الإصدار نفسه.

    - **تم إصلاح تراجع — كان لـ `_NEWS_CITATION_REGEX` تراجع تعقبي أسي (exponential backtracking).** أدخل إصلاح الفقرات المتعددة `(?:[ \t]*$|[ \t]+.*)` في التكرار: تقاسم المسافات بين `[ \t]+` و`.*` ملتبس، ويتضاعف هذا الالتباس من تكرار إلى آخر. تم القياس على أسطر `>   texte` — وهي إزاحة Markdown صالحة تمامًا — لا تطابق النمط: **2,589 ملي ثانية لـ 14 سطرًا** مقابل 0.04 ملي ثانية بعد التصحيح، مع عامل يتضاعف بنحو 9 أضعاف لكل سطر إضافي. في وضع `--news`، كان اقتباس كتلي (blockquote) طويل وغير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة (job timeout)، دون سبب يمكن تحديده. يستهلك التكرار الآن السطر بأكمله دفعة واحدة (`\n^>(?![ \t]*—).*`)، مما يترك طريقة واحدة فقط للمطابقة في كل دورة تكرار. تم التحقق على المجموعة الفعلية المكونة من 231 مقالًا: **صفر فرق** في الالتقاط، ونفس الـ 423 اقتباسًا، والـ 14 متنًا متعدد الفقرات لا تزال ممتدة.
    - **رايتان متزامنتان لمزودي الخدمة (provider flags) كانتا تفرضان فوترة بحسب الاستخدام في صمت.** كان `--use_codex --use_mistral` مقبولًا؛ يختبر `_select_provider_client` مزود Mistral أولًا، ويعطي `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكلاهما كان يؤول إلى Mistral. وبالتالي، كان المستخدم يطلب حصة اشتراكه فيحصل على فوترة بحسب الاستخدام، دون أدنى تحذير: وهو بالتحديد نمط الفشل الذي وجد `--use_codex` لمنعه. تمر رايات المزود الست الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: يفشل الآن سطر الأوامر الذي يجمع بين مزودين، والذي كان يُقبل في صمت سابقًا، مع إظهار `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة نهاية العمل تتحول إلى اللون الأخضر عند تعطل مجساتها.** كانت أربعة من أصل ثلاثة عشر فحصًا في `scripts/check-release-ready.sh` تتبع نمط «التقاط stdout، والاستنتاج إن كان فارغًا» دون فحص رمز الإرجاع أبدًا: حيث كان أي استثناء (إعادة تسمية ملف، `FileNotFoundError`) يكتب على stderr، ويترك stdout فارغًا، فيستنتج الفحص أنه «لا شيء للإبلاغ عنه». تكرر فخ «`exit 0` لا يثبت شيئًا» داخل السكربت المكتوب خصيصًا لمنعه. تفرض دالة مساعدة `probe()` الآن رمز إرجاع صفريًا **و**حارس نهاية (sentinel)، وترفض المجسات الاستنتاج بناءً على مجموعة علامات مرجعية فارغة — إذ إن التحقق (assertion) على مجموعة فارغة يكون صحيحًا دائمًا. إثبات ذلك: أدت إضافة المجموعة الحصرية أعلاه إلى تمرير رايات المزود عبر كائن `*_group`، الذي لم يعد التعبير النمطي القديم `parser\.add_argument\(` يطابقه؛ وخرجت **ست رايات من أصل إحدى وعشرين** من النطاق في صمت، بينما ظلت البوابة خضراء.
    - **كان فحص الأسرار (secrets scan) يفوّت أربعة مزودين من أصل ستة.** تستثني الفئة `[A-Za-z0-9]` الشرطة: حيث كان `sk-proj-…` (تنسيق OpenAI الحالي) و`sk-ant-api03-…` ينكسران عند شرطتهما الثانية، ولم يكن `AIza…` مغطى. تم توسيع النمط، واستبعاد `.secrets.baseline` من الفحص. علاوة على ذلك، كانت آلية الحماية `.env` تستعلم من `git diff --cached`، الذي لا يرى سوى الفهرس: ولم يكن أي `.env` **تم تثبيته بالفعل (committed)** — وهي أسوأ الحالات — يظهر فيه أبدًا. وهي تستعلم الآن من `git ls-files`.
    - **لم تكن «تهيئة الرمز المميز (token warm-up)» الخاصة بـ Codex تهيئة فعلية.** تم القياس: لا يمس `codex login status` الملف `~/.codex/auth.json` (لم يتغير كل من mtime وحجم الملف)، وتشير رسالة المساعدة الخاصة به إلى «Show login status». ورغم ذلك، كان التعليق يزعم تحديث الرمز «مرة واحدة، بالتتابع»، وبالتالي تحييد خطر عمليات التحديث المتزامنة لرمز متناوب أحادي الاستخدام. الحماية المعلنة لم تكن موجودة؛ ويصف التعليق الآن ما يفعله الكود بالفعل، وتظل الحماية الحقيقية هي `max_jobs=4`. بالإضافة إلى ذلك، يراعي الفحص الآن `CODEX_BIN` الذي كان يتجاهله — إذ كان أي جهاز يفتقر إلى `codex` في `PATH` يفشل مع رسالة «غير مصادق عليه»، وهو تشخيص مضلل.
    - **كان يتم تضمين (source) `.env` داخل غلاف فرعي (sub-shell).** يتم استدعاء `detect_provider` كاستبدال للأمر (command substitution)، بحيث لم تكن عمليات التصدير (exports) الخاصة به تنتقل إلى الغلاف الأعلى: فكان أي `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` محدد في `.env` يظل غير مرئي لعمليات القراءة المنفذة في `main()`، والتي كانت تستنتج أن «ملف Grok الثنائي غير موجود» على إعدادات صحيحة تمامًا.
    - **تجاوزت المعالجة المتزامنة الحد المعلن بنسبة 50%.** كانت آلية الحماية موضوعة بعد تشغيل زوج README/CHANGELOG: حيث سُجلت ذروة قياس بلغت **3 لـ `max_jobs=2`**. بالنسبة لـ Grok، الذي يتم تقاسم حصته الأسبوعية مع Chat/Imagine/Voice دون إمكانية قياسها، لم يتم الالتزام بالحد الذي يفرضه السكربت على نفسه. أما العدد الإجمالي النهائي، فكان يُعرض دون مقارنته مطلقًا بالرقم 28 — فكان أي ملف مفقود يمر دون ملاحظته.
    - **عقد مخرجات Grok: يعتبر غياب `stopReason` فشلًا من الآن فصاعدًا.** كان الكود يطبق «`end_turn` **أو غائب**» في حين يتطلب العقد المعلن `end_turn`. وكانت أي حمولة خالية من الحقل — أو يُعاد تسمية حقلها عبر تحديث لـ CLI — تحول الحماية إلى عملية فارغة (no-op) صامتة. بالإضافة إلى ذلك، لم يعد `max_turn_requests` يُصنف على أنه حد للمعدل (rate limit) (إذ إن ميزانية الجولات هي التي نفدت: وإعادة المحاولة تكرر نفس النتيجة، مع خسارة 90 ثانية من الانتظار)، ويُستبعد `quota` من علامات حدود المعدل — للسبب الذي كانت سلسلة التوثيق (docstring) لـ `_codex_is_rate_limited` تنص عليه بالفعل دون أن يطبقه Grok.
    - **حفظ نتائج تعاقب (cascade memoization) Gemini حسب النموذج.** كان التعاقب يبدأ مجددًا من `minimal` عند كل مقطع، على الرغم من أن النموذج الافتراضي يرفضه: وكان المسار الطبيعي يدفع تكلفة طلب ورد بخطأ 400 لكل مقطع ويعيد طباعة التحذير نفسه. التحذير الذي يتكرر مئات المرات لا يعود يُقرأ — وهكذا يتحول إلى قناع يحجب المشاكل.
    - **متفرقات**: كانت رسالة الرفض في التكامل المستمر (CI) مكتوبة كقيمة ثابتة (hardcoded) لـ Codex وتوجه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق البنية الأساسية للعمليات الفرعية يعمم مصطلح «shim» على كلا أداتي CLI بينما الملف الثنائي لـ Grok هو ملف ELF أصلي (والتبرير الصحيح هو «وكيل يُنشئ عملياته الفرعية الخاصة»)؛ وتم تمييز اثنتي عشرة نتيجة فحص SAST حول `subprocess` كـ `# nosec` / `# nosemgrep` مع تبرير، إذ إن صيغة القائمة دون `shell=True` تجعل الحقن مستحيلًا كما أن محتوى المستند لا يمر أبدًا عبر argv.
    - **لم تعد أي أسرار تدخل إلى العمليات الفرعية للوكلاء (agentic sub-processes).** لم تكن قوائم الحظر بالاسم تحمي سوى ثوابت **الفوترة** (Codex دون `OPENAI_API_KEY`، وGrok دون `XAI_API_KEY`). تم القياس: كانت **سبعة أسرار أخرى** تدخل رغم ذلك في كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح أداة CLI الأخرى، و`OPENAI_BASE_URL`، وهو ليس سرًا لكنه يعيد توجيه حركة المرور. غير أن أداتي CLI هاتين هما **وكيلان**، ويعمل وكيل Grok دون بيئة عزل لنظام التشغيل (OS sandbox) قابلة للتطبيق على العديد من أجهزة Linux. يتم التصفية الآن **حسب نمط الاسم** (`API_KEY`، و`_TOKEN`، و`SECRET`، و`PASSWORD`، و`CREDENTIALS`) وليس بقائمة محددة بالاسم، مما يغطي أيضًا المتغيرات التي يضيفها المستخدم في `.env` دون أن يكون هذا الكود على علم بها. لا يلزم أي منها لأدوات CLI: فالمصادقة توجد في `~/.codex` و`~/.grok`، وليس في البيئة أبدًا — وتم التحقق من ذلك عبر **ترجمة فعلية ناجحة** باستخدام كلا المزودين مع البيئة المحصنة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يضمن حصرية رايات المزود، وعقد `stopReason`, وخطية التعبير النمطي للأخبار، ورسالة الرفض في CI، وحفظ نتائج Gemini، وعدم وجود أي أسرار في بيئة العمليات الفرعية. هذا التحقق الأخير هو تحقق **عام** — فهو يفشل عند وجود مفتاح لا تذكره أي قائمة — في حين كان اختبار التنقية السابق انعكاسًا لثوابته الخاصة ولم يكن قادرًا على اكتشاف أي شيء سوى تعطل حلقته التكرارية الخاصة. اكتملت المجموعة بـ **311 اختبارًا**.

  - **مزودان جديدان لـ Grok**: `--use_grok` (واجهة برمجة تطبيقات xAI، المفتاح `XAI_API_KEY`، فوترة بحسب الاستخدام) و`--use_grok_cli` (أداة Grok Build CLI الرسمية، مخصومة من اشتراك Grok — نفس مبدأ `--use_codex`).
    - **وضع واجهة برمجة التطبيقات (API)، حوالي 40 سطرًا**: نظرًا لأن نقطة نهاية xAI متوافقة مع OpenAI، يُعاد استخدام العميل و`_call_openai` كما هما، مع تغيير `base_url` فقط. كان هناك تعديل وحيد مطلوب وقد استفاد منه الجميع: يقبل `finish_reason` الآن `end_turn`، وهي الصيغة الصادرة عن xAI في حين تُصدر OpenAI الصيغة `stop`. النماذج: `grok-4.6` (الجودة) و`grok-4.3` (الاقتصادي). تجدر الإشارة إلى أن النموذج الاقتصادي لـ Grok يظل الأغلى في المستودع — 1.25$ / 2.50$ لكل مليون مقابل 0.15$ / 0.60$ لـ `mistral-small-latest`: يتم اختيار هذا المزود لتنوع النماذج، وليس من أجل السعر.
    - **وضع CLI**: مصمم على غرار Codex، مع أربعة اختلافات فرضتها التجربة الميدانية — يتم إرسال الموجه عبر ملف (`--prompt-file`، حيث لا يقرأ CLI من stdin وسيكون أي مقطع في argv مرئيًا في `ps`)، والمخرجات عبارة عن كائن JSON واحد على stdout (لا JSONL ولا ملف `-o`)، والاشتراك لا يتيح سوى `grok-4.6` و`grok-4.5`، ولا يمكن تطبيق بيئة العزل (انظر أدناه). تم تجميع تشغيل العمليات الفرعية بشكل مشترك مع Codex في `_codex_run_process`، دون المساس ببقية مزود Codex الذي تم اختباره مسبقًا.
    - **`exit 0` لا يثبت شيئًا، تم القياس**: عند عدم المصادقة، يكتب CLI العبارة `{"type":"error","message":"Not signed in."}` على **stdout** مع رمز إرجاع **0**. ويتصرف الرفض أو تجاوز عدد الجولات بنفس الطريقة. وبالتالي، يتطلب عقد المخرجات أربعة شروط متزامنة: رمز إرجاع 0، وغياب حمولة الخطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع الفحص المسبق (preflight) نفس المنطق: ينتهي `grok models` بالرمز 0 حتى عند انقطاع الاتصال، ولا يتيح استخلاص النتيجة سوى وجود «not authenticated» في stdout.
    - **العزل: عدم تماثل مقصود وموثق.** في حين يعمل Codex في `--sandbox read-only`، فإن بيئة عزل Grok غير قابلة للتطبيق على العديد من أجهزة Linux الحديثة، لسببين نظاميين مستقلين لا يمكن تجاوزهما دون `sudo`: إذ يحظر AppArmor مساحات أسماء المستخدمين (user namespaces) غير المميزة منذ Ubuntu 24.04 (`bwrap: setting up uid map: Permission denied`، أُعيد إنتاجها خارج Grok)، وتفشل قائمة حظر مآخذ (sockets) بيئة تشغيل الحاويات عندما يكون `/run/podman` في وضع `0700` (لا يعالج المحلل سوى `ErrorKind::NotFound`، فيصبح خطأ EACCES قاتلًا). الفخ الأساسي: الملف التعريفي **المدمج** الذي يتعذر تطبيقه **يبدأ العمل دون عزل في صمت**. لذلك لا يطلب السكربت أي ملف تعريفي افتراضيًا ولا يتراجع بصمت أبدًا — بل يطلق تحذيرًا على stderr. تعتمد الحماية على قواعد `--deny` الخاصة بـ CLI، بما في ذلك القاعدة الشاملة `*`، وهي الطبقة الوحيدة التي ثبت أنها _fail-closed_ (أي قاعدة ببادئة غير معروفة تؤدي إلى رفض بدء التشغيل). يتيح `GROK_TRANSLATE_SANDBOX=read-only` اشتراط ذلك، وفي هذه الحالة يفشل بدء التشغيل إذا لم يتمكن الجهاز من استيفائه.
    - **آليات الحماية**: تمت إزالة `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (إذ قد يتسبب وجود مفتاح في التحويل إلى الفوترة بحسب الاستخدام؛ كما أن وجود `GROK_SANDBOX` موروث سيفرض ملفًا تعريفيًا غير قابل للتطبيق مع رسالة مضللة)، وتعطيل قواطع MCP/hooks/skills/agents، و`--disable-web-search`، و`--no-subagents`، و`--no-plan`، ودليل عمل مؤقت، والرفض في CI، ومهلة توقف تنهي مجموعة العمليات، والتراجع التدريجي (back-off) عند بلوغ حد المعدل. تم ضبط `--max-turns` على 6 وليس 1: حيث تتم زيادة العداد بعد جولة استخدام الأدوات، وضبطه على 1 سيؤدي إلى اقتطاع المخرجات.
    - **الحصة**: الحصة المجمعة لـ Grok أسبوعية و**مشتركة مع Chat وImagine وVoice**، ولا يوجد أمر يعرض تفاصيلها — على عكس Codex حيث يتيح `account/rateLimits/read` قياس الاستهلاك بدقة. وبالتالي يحدد `regen_translations.sh` عدد العمليات المتزامنة باثنتين ويحذر بوضوح.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` (24 اختبارًا). اكتملت المجموعة بـ **290 اختبارًا**.
  - **تم إصلاح خطأ — كانت الاقتباسات الإنجليزية (EN) متعددة الفقرات محمية جزئيًا فقط (الوضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل كمتن للاقتباس سوى سلسلة من أسطر `>` **المتتالية**. وبمجرد أن يغطي الاقتباس عدة فقرات (يفصل بينها سطر `>` فارغ)، كان يتم التقاط الفقرة الأخيرة فقط واستبدالها بالنائب؛ بينما كانت الفقرات السابقة تُرسل إلى LLM وتعود مترجمة — وهو العكس تمامًا لما وجد `--news` لضمانه. يقبل التكرار الآن أسطر `>` الفارغة الداخلية ويصبح غير جشع (non-greedy)، ليتوقف عند `>` الفارغ الذي يسبق السطر المائل وليس عند أول سطر يواجهه.
    - **النطاق المقاس** على مجموعة فعلية من 198 مقالًا: تأثر 11 اقتباسًا من أصل 419. لا يوجد أي تراجع — يلتقط التعبير النمطي الجديد نفس عدد الاقتباسات تمامًا، مع تمديد المتون متعددة الفقرات فقط (408 متون متطابقة، و11 ممتدة)، ولا يمكن أبدًا استيعاب سطر الإسناد `> — …` داخل المتن (تم الاحتفاظ بالاستشراف المسبق lookahead).
    - **إثبات شامل (end-to-end)** على مقال بحجم 69 كيلوبايت مترجم إلى ja/ar: الفقرة الأولى من الاقتباس، التي كانت تُترجم سابقًا إلى `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم بالمثل في العربية، تظل الآن `> GLM-5.3 is now open-weight.`. ويعود عدد أسطر الاقتباس باللغة الإنجليزية من 9 إلى 10، وهو نفس العدد في المصدر.
    - تجدر الإشارة: لم تكن أدوات التحقق اللاحقة تكتشف هذا الخلل، إذ تكتفي بالتحقق من وجود الاقتباس دون التأكد من اكتماله.
  - **توفير مقاس على المزود الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ اسم النموذج بـ `gpt-5`، بما في ذلك في `--eco`. قياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: `medium` ← 45 رمز استنتاج (reasoning tokens) و65 رمز مخرجات؛ و`none` ← 0 و14. الاستنتاج لا يضيف شيئًا للترجمة، وكان يُدفع ثمنه في كل مقطع من كل ملف. أصبحت القيمة الافتراضية `none` في `--eco` وتظل `medium` في غير ذلك؛ وتظل الأولوية لأي قيمة صريحة ممررة عبر CLI. يقبل `--reasoning_effort` الآن `none` و`xhigh` بالإضافة إلى `low`/`medium`/`high` (ليست جميعها مقبولة من كافة النماذج: على سبيل المثال، يرفض `gpt-5.4-mini` القيمة `minimal` — وتغطي آلية إعادة المحاولة الحالية دون معلمات هذه الحالة).
  - **تحديث حزم SDK والانتقال إلى Gemini**: تم استبدال `google-generativeai` (انتهى الدعم في 2025-11-30، وأُرشف المستودع) بحزمة SDK الموحدة **`google-genai`** — `genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير موجه النظام عبر `system_instruction` بدلًا من دمجه بالمقطع. تم ترقية `mistralai` إلى **2.9.4** (يصبح الاستيراد `from mistralai.client import Mistral`؛ حيث يُطلق القديم استثناء `ImportError`، تم التحقق من ذلك في الـ wheel)، و`anthropic` إلى **0.125.0** و`openai` إلى **2.54.0** — وهي أحدث الإصدارات قبل التحول إلى `httpx2`، لتجنب تشغيل مجموعتي بروتوكول HTTP معًا داخل بيئة venv. وتم فك حظر `httpx` 0.28.1 و`pydantic` 2.13.5 تبعًا لذلك.
  - **تراجعان تم اكتشافهما باختبارات فعلية، وليس عبر الوثائق**:
    - يرفض `anthropic` ≥ 1.0 من جانب العميل أي استدعاء غير متدفق (non-streamed) يتوقع فيه `max_tokens` استغراق أكثر من 10 دقائق (`ValueError: Streaming is required...`). لم يكن إجراء الحماية هذا موجودًا في 0.34.2 وكان يعطل أي استدعاء لـ Claude مع `max_tokens=32768`. تم تصحيح ذلك عبر `timeout` صريح (`CLAUDE_TIMEOUT`، 900 ثانية افتراضيًا)، مما يتجنب التبديل إلى البث المباشر (streaming) لاستدعاء لا نحتاج منه سوى الاستجابة الكاملة.
    - لا يُقبل `thinking_level="minimal"` إلا من قبل جزء من قائمة نماذج Gemini: يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` بخطأ 400. ومن هنا جاءت `_gemini_generate_with_fallback`، وهي تعاقب `minimal` ← `low` ← بدون thinking_config، على غرار آلية الاحتياط (fallback) الموجودة بالفعل لـ OpenAI — إذ لا ينبغي أبدًا لمعلمة تحسين أن تتسبب في فشل الترجمة.
  - **تحديث النماذج الافتراضية**، وكل منها مؤكد باستدعاء فعلي: OpenAI `gpt-5.5` ← **`gpt-5.6-terra`** (انخفاض بنسبة 60% على دفعة مكونة من 28) و`gpt-5.4-mini` ← **`gpt-5.6-luna`** (انخفاض بنسبة 73%)؛ Claude `claude-sonnet-4-6` ← **`claude-sonnet-5`** (أرخص وأحدث) و`claude-haiku-4-5-20251001` ← **`claude-haiku-4-5`** (معرف أساسي بدون تاريخ)؛ Gemini `gemini-3.1-pro-preview` ← **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` ← **`gemini-3.1-flash-lite`** (إصدار مستقر، وأرخص من `3.5-flash-lite`).
 Mistral دون تغيير، مع بقاء `mistral-large-latest` صاحب أفضل نسبة أداء/سعر بين الأربعة. يُلاحظ: لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — فنموذج Gemini 3.5 Pro، الذي أُعلن عنه في مايو 2026، لم يصدر أبدًا؛ وسلسلة 3.5/3.6/3.7 تقتصر حصريًا على Flash.
  - **اختبار A/B مقاس قبل التبديل إلى Gemini**: تمت ترجمة `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. بنية متطابقة تمامًا (21 قائمة، و18 كتلة برمجية، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، في غضون **8 ثوانٍ مقابل 48 ثانية**. ونظرًا لعدم وجود أي معيار قياس عام يقارن بين هذين النموذجين في الترجمة أو النصوص غير اللاتينية، كان التبديل سيعتمد لولا ذلك على مجرد افتراض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفذ `block.text for block in response.content` دون تصفية النوع. تُدرج نماذج التفكير التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، والتي تعرض `.thinking` وليس `.text` — مما كان سيتسبب في تعطل الترجمة مع `AttributeError` مبهم في المقطع الأول. يتم الآن استبعاد الكتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` (قائمة حظر، لتظل متسامحة مع أي نوع غير معروف يحمل نصًا)، وأي استجابة بدون أي كتلة نصية تطلق خطأً صريحًا. يتم تمرير `thinking={"type": "disabled"}` في كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: إزالة النماذج التي انقضى تاريخ إيقافها (سلسلة `magistral-*` التي تم إيقافها في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620`، و`claude-3-7-sonnet-20250219`، و`claude-opus-4-1-20250805`، و`claude-sonnet-4-20250514`). تصحيح الحدود: Mistral من 128K → **256K** (جيل Large 3 / Small 4)، وGemini من 1 000 000 → **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` من 200K → **1M**، وعائلة `gpt-5.6-*` من 400K → **1,05M**. إضافة Claude 5 (`claude-sonnet-5`، و`claude-opus-5`، و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest` وسلسلة `ministral-*`. يُلاحظ: تظل هذه الحدود استرشادية، حيث يحدد `translate()` سقف التجزئة عند `min(16000, limite)`.

  - **المزود `--use_codex`**: مزود خامس يتحكم في CLI الرسمي لـ Codex (`codex exec`) في الوضع غير التفاعلي، بدلاً من استدعاء API مدفوعة بحسب الاستخدام. يتم احتساب الترجمة من حصة اشتراك ChatGPT المدفوع بالفعل. هذا هو المسار الوحيد الموثق من قِبل OpenAI لهذا الاستخدام: تسرد مصفوفة التوفر بحسب الخطة كلاً من «Codex SDK, `codex exec`, and scriptable workflows» بوصفها متاحة في خطط Plus/Pro/Business/Enterprise، في حين أن رموز `~/.codex/auth.json` لا تصادق على الاستدعاءات إلى API Platform (ولا يقرؤها هذا السكريبت إطلاقًا — حيث تظل المصادقة وتحديثها مدارة بواسطة CLI).
  - **ملف Codex الثنائي قابل للتثبيت عبر pip، وليس عبر npm فقط**: يبحث `_resolve_codex_binary()` عن الملف الثنائي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** المنشورة بواسطة OpenAI (وهي التبعية الخاصة بـ SDK `openai-codex`). وبذلك لم يعد مشروع Python بحاجة إلى تثبيت عام عبر npm لاستخدام `--use_codex`. لم تتم إضافة الحزمة إلى `requirements.txt`: إذ يبلغ حجم الملف الثنائي حوالي 250 ميغابايت، وهو ما كان سيُفرض على جميع المستخدمين لمجرد مزود اختياري. تم التحقق من البداية إلى النهاية: مع غياب `codex` من `PATH`، يجد البحث الملف الثنائي المضمن في الحزمة وتكتمل الترجمة بالكامل في 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: تتم إزالة `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. وبدون هذا الإجراء الوقائي، يمكن لمفتاح موجود في `.env` أن يحول Codex إلى الفوترة بحسب الاستخدام دون أي إشارة مرئية — وهو تمامًا ما وُجد هذا المزود لتجنبه.
  - **أفخاخ CLI المحمية بواسطة الاختبارات**:
    - يقرأ `codex exec` الدخل القياسي stdin **حتى** عندما يتم تمرير الموجه كمعامل: فبدون إغلاق stdin، ينتظر الأمر حتى انتهاء المهلة دون استدعاء النموذج على الإطلاق (تمت إعادة إنتاجه: خروج برمز 124 بعد 180 ثانية، بصفر بايت). لذا فإن `communicate(input=...)` إلزامي.
    - يُعد `codex` المثبت عبر npm بمثابة Node shim يقوم بـ `spawn` للملف الثنائي الحقيقي لـ Rust: وهذا الأخير هو **حفيد** لعملية Python ويظل حيًا بعد `SIGKILL` الخاص بـ `subprocess.run(timeout=)`، حيث سيستمر في استهلاك الحصة. ومن هنا جاء `Popen(start_new_session=True)` + `os.killpg`.
    - يمكن لـ CLI أن يخرج بالرمز 0 رغم إصداره لـ `turn.failed`: يتم فحص مخرجات JSONL (`--json`) بالإضافة إلى رمز الإرجاع، ويؤدي غياب الملف `-o` مع الرمز 0 إلى إطلاق خطأ صريح بدلاً من إنتاج مقطع فارغ.
  - **التراجع التدريجي عند حد المعدل**: لا يطبق CLI أي إعادة محاولة داخلية (`max_retries = 0`). يتم التصنيف بناءً على بنية حمولة JSON (`status: 429` / `error.type`)، وليس على سلاسل نصية فرعية — حيث تظهر كلمة «quota» في كلٍّ من خطأ 429 القابل للاسترداد و`insufficient_quota` النهائي.
  - **حماية CI**: يتم رفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` محددًا. المصادقة عبر الاشتراك ليست مخصصة لبيئات التشغيل المشتركة، وتوصي OpenAI صراحة بعدم استخدام سير العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` (`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وAPI Platform، لكن حساب ChatGPT لا يملك صلاحية الوصول إلى كل شيء فيها: يتم تطبيق قائمة السماح من جانب الخادم، دون تحقق محلي، ويؤدي استخدام نموذج غير معتاد إلى إطلاق تحذير. في خطة Plus، يوفر Luna من 250 إلى 2000 رسالة لكل نافذة مدتها 5 ساعات مقابل 10 إلى 100 رسالة لـ Sol: لذا فإن `--eco` هو الوضع الموصى به لأي معالجة مجمعة.
  - **إصلاح خطأ برمجي — كان `regen_translations.sh` يخرج بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهي متغير `local` خاص بـ `main()` لم يعد موجودًا وقت تنفيذ trap. تحت `set -u`، كان هذا يثير `failed_log: unbound variable` ويجعل السكريبت يخرج برمز 1 على الرغم من أن الترجمات الـ 28 كانت صحيحة — وهو ما كان سيعطل `release.sh --auto` (`set -e`) مباشرة بعد إعادة التوليد، في المرحلة الأكثر استهلاكًا للموارد. أصبح المتغير عامًا ويتحقق trap من وجوده. أثر جانبي مفيد: أصبحت حالات الفشل الحقيقية في الترجمة، التي كانت محجوبة سابقًا بسبب هذا الخطأ، مرئية مجددًا في ملخص النهاية.
  - **`REGEN_MODEL`**: متغير بيئة جديد لـ `regen_translations.sh` يفرض نموذجًا محددًا متجاوزًا الإعداد الافتراضي للمزود، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج المتقدم في حصة الاشتراك بدلاً من النموذج `--eco` الموجه للحجم الكبير.
  - **`regen_translations.sh`**: يتوفر `REGEN_PROVIDER=codex` عبر اشتراك صريح (لا يتم اكتشافه تلقائيًا أبدًا، لتجنب استهلاك حصة الاشتراك دون علم المستخدم). يتم تحديث الرمز المميز مرة واحدة بالتتابع قبل فتح المعالجة المتوازية — نظرًا لأن تحديث Codex دوري وللاستخدام لمرة واحدة فقط، فإن المهام المتزامنة كانت ستبطل جلسة `codex login` — كما تم تخفيض التزامن إلى 4.
  - **إعادة هيكلة برمجية مرتبطة**: تم تقليص معلمات `_dispatch_provider_call` من 8 إلى 6 عبر `_resolve_provider()` يعيد اسم المزود، بدلاً من تمرير قيمة منطقية رابعة عبر السلسلة بأكملها. تظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` مع حد أدنى من `Namespace`.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` (48 اختبارًا) يغطي argv، والبيئة المنقحة، وعقد منع التمهيد، وحالات الفشل الصامتة، وtimeout/killpg، والتراجع التدريجي، وpreflight، وتحديد المزود، وتسلسل التفكير في Gemini، وتصفية كتل Claude، واقتباسات الأخبار متعددة الفقرات. مجموعة الاختبارات الكاملة تضم 290 اختبارًا.
  - **التحقق الفعلي**: يعطي ملف `README.md` الخاص بالمشروع والمترجم إلى **14 لغة** عبر Codex بنية مطابقة تمامًا للترجمات المرجعية (14 كتلة برمجية، و24 عنوانًا، و25 صفًا في الجداول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل برمجية متطابقة حرفًا بحرف، وانعدام تام لبقايا العناصر النائبة). وفي مقال إخباري بحجم 69 كيلوبايت في وضع `--news`، تجتاز كل من مخرجات `gpt-5.6-luna` و`gpt-5.6-sol` أداة التحقق التطبيقية اللاحقة للغات en/ja/ar. الاستهلاك المقاس عبر `account/rateLimits/read`: ظل أقل من حد التقريب في العداد (0% من نافذة الـ 5 ساعات) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL للإحالة الإخبارية مع أقواس متداخلة أو بادئة فرنسية (2026-05-11):

  - **إصلاح خطأ برمجي**: كان استخراج عناوين URL للإحالة في `_protect_news_quotes` يستخدم التعبير النمطي `re.search(r"\((.+?)\)", attribution)` (التقاط كسول بين قوسين). وفي إحالة من النوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` شاملة + `]()` لرابط markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة نصية مقتطعة + تتضمن البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (بدون `)` النهائي). النتيجة: كان `_validate_news_post` يبحث عن هذه السلسلة في المخرجات المترجمة ويفشل باستمرار (لسببين: `)` مقتطع + ترجمة "relayé par" إلى `relayed by`/`weitergeleitet von`...). لم يكن بإمكان التسلسل الكامل low → medium → high → gpt-5.5 أن ينجح.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — يستهدف تحديدًا `](url)` الخاص برابط markdown، ويلتقط **فقط عنوان URL الخالص** (بدون البادئة الفرنسية أو الاقتطاع)، وهو ثابت تحافظ عليه العناصر النائبة `#URL{N}#` أثناء الترجمة. قوي في التعامل مع كلا النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية بدون أقواس شاملة
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py` الفئة `TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة الدقيقة المُعادة لخطأ Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (نسخة مختلفة مع `via`)
  - **تغطية الثغرة**: يتحقق `check-editorial-coverage.py` من صحة الصياغة التحريرية ولكن ليس من قابلية الترجمة بواسطة المترجم. أحد التحسينات الممكنة (خارج نطاق الإصدار v1.9.2) هو إجراء فحص يحاكي استخراج الإحالة في تشغيل تجريبي لاكتشاف الأنماط الخطرة قَبْلَ النشر.

- **1.9.1** إصلاح تدويل تسمية CTA في ملاحظة الترجمة marker (2026-05-10):

  - **إصلاح خطأ برمجي**: كانت التسمية `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط marker أعلى الملفات المترجمة تظل **باللغة الفرنسية** لجميع اللغات الهدف، بدلاً من اتباع `target_lang`. لا يراها LLM أبدًا (حيث يتم تجميعها من جانب Python للحفاظ على عنوان URL والاسم التعريفي للمستودع)، وبالتالي لم تتمكن مرحلة الترجمة من تداركها. تراجع غير معلن منذ إضافة التنسيق `marker` في الإصدار v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ 15 بتسميتها المترجمة محليًا. ينقل كل من `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` الآن اللغة الهدف. خطة بديلة `fr` إذا كانت اللغة غير معروفة (للأمان، دون حدوث KeyError).
  - **الاختبارات**: تم تعديل `test_source_emits_three_paragraphs_repo_title_description_link` (اللغة الهدف `ja` ← التسمية اليابانية المتوقعة). اختباران جديدان: `test_source_link_label_localized_per_target_lang` (مضبوط بمعلمات عبر 7 لغات تغطي النصوص اللاتينية، والإيديوغرافية، والأبجدية) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختبارًا في `test_translation_note_position.py` (بدلاً من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع دالة مع افتراضي `target_lang="fr"` — تستمر جهات الاستدعاء البرمجية الخارجية بدون `args.target_lang` في العمل دون أي تعديل.

- **1.9** إصلاح الفشل الصامت (silent-failure) + أدوات جودة كاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07) :
  - **ملاحظة ترجمة متعددة المواضع + تنسيق وسم "embed card"** :
    - خيارات CLI جديدة (تراكمية، مع بقاء القيم الافتراضية دون تغيير → **non breaking**) :
      - `--note_position {top,bottom,both}` (الافتراضي : `bottom`) : يضع الملاحظة في الأعلى، أو في الأسفل، أو في كلا الموضعين من الملف المترجم.
      - `--note_format {legacy,marker}` (الافتراضي : `legacy`) :
        - `legacy` يعيد إنتاج سلوك الإصدار v1.8 بدقة (فقرة بخط عريض `**…**`) **byte-for-byte**.
        - `marker` يُنشئ تعريف مرجع رابط Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`) متبوعًا بـ **اقتباس (blockquote) من 3 فقرات** مهيكل لعرض من نوع "GitHub repo embed card" : عنوان المشروع في كود مضمن (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف مترجم بواسطة LLM، ورابط CTA (`[Voir le projet sur GitHub ↗](URL)`) مع سهم مرئي. قابل للاستخدام أثناء البناء بواسطة إضافة remark (راجع مدونة jls42.org → الإضافة `remark-translation-banner`).
    - **ثوابت لا تُرسل أبدًا إلى LLM** : يتم تجميع عنوان المستودع ورابط GitHub في جانب Python بعد ترجمة الجملة الوصفية. لا يرى LLM أبدًا الـ slug `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، مما يضمن عدم تعديل أي مصيّر/حالة أحرف/مخطط (renderer/casse/scheme).
    - **إدراج يراعي الـ frontmatter (Frontmatter-aware insertion)** : في الوضع `top` أو `both`، يتم إدراج الملاحظة **بعد كتلة `---` للإغلاق** الخاصة بـ YAML frontmatter (أمان Astro Content Collections / gray-matter). تكتشف الدالة المساعدة `_split_frontmatter` العنصر `---\n…\n---\n` في بداية الملف وتحافظ على سلامته؛ و**تُطلق `RuntimeError`** عند وجود frontmatter مفتوح بدون سياج إغلاق (يُرفع الملف إلى `failed_files` بدلاً من كتابته بملاحظة في غير موضعها).
    - **معقّم النموذج بالقائمة البيضاء (Sanitizer modèle whitelist)** : يستبدل `_sanitize_model` أي حرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع احتياطي `unknown` إذا كان فارغًا. يتوافق مع أداة التحقق من جانب إضافة remark Astro ويبطل مفعول الأحرف التي قد تكسر تنسيق الوسم (المسافات، علامات الاقتباس، الأقواس، الفواصل، إلخ).
    - **إعادة هيكلة داخلية** : `_append_translation_note` (دالة أحادية واحدة) → 7 دوال مساعدة نقية (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فصل الباني/المركّب (يعيد الباني كتلة نقية دون فواصل، ويطبق المركّب `\n\n` وفقًا للموضع)؛ ويشترك كود الإنتاج والدالة المساعدة للمصدر في نفس مجمّع الفقرات الثلاث.
    - **`_quote_lines` الحافظ للأسطر الفارغة (blank-preserving)** : يسبق كل سطر بـ `> `، محولاً الأسطر الفارغة إلى `>` بمفرده. يتيح لـ mdast رؤية 3 فقرات متمايزة داخل الاقتباس (العنوان / الوصف / الرابط) بدلاً من فقرة واحدة مع فواصل أسطر.
    - **`_build_translation_note_block` تكيّفي** : وفقًا لعدد الفقرات التي احتفظ بها LLM (3 = تنسيق بطاقة كامل، 2 = جملة + رابط، 1 = احتياطي/fallback). الاحتياطي المكوّن من فقرة واحدة **لم يعد يغلّف بـ `**...**`** عند اكتشاف رابط Markdown `](` (تصيير هش لـ `<strong>` حول الرابط).
    - **توافق رجعي** : `getattr(args, "note_position", "bottom")` و `getattr(args, "note_format", "legacy")` من جانب `_compose_with_notes` — كائنات Namespace التي تفتقر إلى هذه السمات (الاختبارات الحالية، الاستدعاءات البرمجية الخارجية) تواصل العمل دون أي تعديل.
  - **إصلاح الفشل الصامت (silent-failure) في الترجمات الطويلة** :
    - التحقق من اللغة بعد الترجمة لدى جميع المزودين (OpenAI، Mistral، Claude، Gemini) : طبقة حتمية (العثور على مقتطف المصدر حرفيًا) + طبقة احتمالية (`langdetect`)
    - القائمة البيضاء لـ `finish_reason` / `stop_reason` : إطلاق `RuntimeError` عند أي حالة خارج القائمة البيضاء (truncation، content_filter، إلخ)
    - `max_tokens` Claude : `4096` → `32768` (يتجنب الاقتطاع الكامن في المقاطع بحجم 16k، وهامش بين أنظمة الكتابة المختلفة FR→JA/ZH/KO/AR/HI)
    - تقسيم يراعي العناوين (heading-aware) : أولوية H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى رمز خروج غير صفري : يُرجع `translate_markdown_file` حالة ذات نوع محدد `success` / `failure` / `skipped`، مع `main()` `sys.exit(1)` في حال فشل ملف واحد على الأقل (ملف فردي ودفعات)
    - حماية من المحتوى الفارغ (Empty-content guard) لدى جميع المزودين، فحص سلامة النسبة بين المصدر والناتج (≥ 500 حرف، < 5% = رفض)، التحقق من العناصر النائبة للكود (`#CODEBLOCK`/`#INLINECODE`)، تسوية ما بعد LLM (الفواصل/الروابط الملتصقة بالعنوان)، إعادة محاولة `BadRequestError` بدون `reasoning_effort`
    - إضافة الاعتمادية `langdetect==1.0.9`
  - **أدوات الجودة لـ pre-commit** ("من نوع EurekAI كامل"، 14 خطافًا) :
    - Pre-commit : ruff (تدقيق وتنسيق lint+format)، shellcheck، prettier (md/yaml/json)، detect-secrets (حماية 4 مفاتيح API)، Lizard (CCN ≤ 12)، pre-commit-hooks v5 (المسافات البيضاء، نهاية الملف EOF، الملفات الكبيرة، سطور shebang، إلخ)
    - Pre-push : mypy (وضع متساهل تدريجي)، Opengrep SAST (translate.py + scripts/)، pip-audit (وضع التقارير الأولي)، unittest discover (tests/ + scripts/tests/)
    - مغلِّفات محلية في `scripts/` تستخدم `./venv/bin/python`
    - `scripts/audit_verdict.py` : محلل JSON لـ pip-audit مع 11 اختبار unittest، وهو نقل إلى Python مقتبس من محلل jls42-astro
    - تصحيح 7 مخالفات أولية لـ ruff : B904 (raise from) ×2، B007 (unused dirs)، C408 (dict literal)، C419 (list-comp)، SIM105 (contextlib.suppress)، SIM110 (any())
    - يستثني Lizard مؤقتًا `translate.py` (4 دوال ذات تعقيد CCN من 21 إلى 47، مع التخطيط لإعادة هيكلتها) — بوابة تدقيق صارمة على scripts/
  - **SonarCloud + تغطية شاملة** :
    - سير عمل GitHub Actions `SonarCloud` (sonarcloud.yml + sonar-project.properties) : تحليل عند كل دَفْع (push) وطلب سحب (pull-request)، وتغطية عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى README (Quality Gate، تقييمات Security/Reliability/Maintainability، Coverage، Vulnerabilities، Bugs، Code Smells، Duplicated Lines، Technical Debt، Lines of Code)
    - `tests/test_silent_failure.py` (`unittest` stdlib) : يغطي الحلقات الست في سلسلة خطأ الفشل الصامت (silent-failure)
    - `tests/test_orchestration.py` (+79 اختبارًا) : يغطي طبقة التنسيق لـ `translate.py` (`_resolve_*_filename`، `_existing_translation_exists`، `_record_translation_status`، `_write_output_file`، `translate_directory`، `_validate_input_paths`، `_init_*_client`، `_select_provider_client`، `_normalize_collapsed_markdown`، `_cleanup_source_flag`، `_validate_news_flags_*`، خيارات fallback لـ `_openai_create_with_fallback` TypeError + BadRequestError، وتنسيق مطالبات o1-series، وفروع early-return لـ `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py` : تغطية لـ `main()` (stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **Coverage on new code** : 75.5% → ~98% (translate.py 98%، scripts/audit_verdict.py 97%)
  - **الاختبارات** : يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك E2E `marker+top|bottom|both` و `legacy+top|bottom|both`)، والبادئات متعددة الأسطر، والتوافق الرجعي byte-for-byte (golden literal)، والمعقّم (sanitizer)، وفصل frontmatter (بما في ذلك إطلاق استثناء عند عدم إغلاق السياج)، وتنسيق الـ 3 فقرات، واحتياطي الفقرتين، وحارس الفقرة الواحدة + رابط Markdown، وحاجز أمان حاسم `TestLLMPayloadExcludesInvariants` يؤكد (assert) عدم إرسال العنوان + رابط URL إلى LLM أبدًا. **نجاح 190 اختبارًا**، و0 تراجع.
  - التوثيق : `README.md` (الفرنسية + 14 ترجمة) مع شارات، و`CLAUDE.md` (سير عمل pre-commit + مراقبة CI مفصلة)، وإعادة توليد 28 ترجمة
- **1.8** وضع `--news` + ترقية نماذج 2026 (2026-03-17، الوسم `v1.8`) :
  - تحديث النماذج الافتراضية (مارس 2026) :
    - OpenAI الجودة : `gpt-5` → `gpt-5.4`
    - OpenAI الاقتصادي : `gpt-5-mini` → `gpt-5.4-mini`
    - Gemini الجودة : `gemini-3-pro-preview` → `gemini-3.1-pro-preview`
  - إضافة حدود الرموز (tokens) لـ `gpt-5.4`، و`gpt-5.4-mini`، و`gpt-5.4-nano` (400k) و`gemini-3.1-pro-preview` (1M)
  - وضع `--news` الأولي : حماية الاقتباسات الإنجليزية باستخدام عناصر نائبة `#NEWSQUOTE\d+#`، ومطابقة `LANG_FLAGS` (15 لغة)، وإدارة الأعلام بحسب اللغة المستهدفة
  - التحقق من العناصر النائبة للأخبار قبل الاستعادة (تراجع سابق : كان LLM الذي يحذف العنصر النائب يُنتج بصمت مخرجات دون اقتباس)
  - جعل السكريبت `regen_translations.sh` محمولاً (مسارات مطلقة، دون اعتماد على pwd)
  - إضافة رابط اللغة الفرنسية في أشرطة اللغات بـ README/CHANGELOG، وإعادة توليد 28 ترجمة
- **1.7** الميزات الجديدة :
  - الخيار `--keep_filename` للاحتفاظ باسم الملف الأصلي أثناء الترجمة
  - دعم الملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على الكود المضمن** : علامات الفاصلة العليا المائلة (`` `...` ``) محمية الآن أثناء الترجمة
  - تحسين المطالبة النظامية (system prompt) :
    - معالجة أفضل لعلامات الاقتباس في YAML frontmatter
    - حماية متغيرات القوالب `{variable}`
    - منع ملاحظات المترجم غير المطلوبة
  - تم اختباره بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** الميزات الجديدة :
  - دعم واجهة برمجة تطبيقات Google Gemini للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026 :
    - OpenAI : `gpt-5` (الجودة)، `gpt-5-mini` (الاقتصادي)
    - Claude : `claude-sonnet-4-5` (الجودة)، `claude-haiku-4-5` (الاقتصادي)
    - Gemini : `gemini-3-pro-preview` (الجودة)، `gemini-3-flash-preview` (الاقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف واحد (`--file`) دون فحص دليل كامل
  - نمط تسمية جديد ومبسط : `{base}-{lang}.md`
  - الخيار `--include_model` للاحتفاظ بالتنسيق القديم مع اسم النموذج
  - دعم النماذج غير المدرجة مع حد افتراضي للرموز (128k)
  - ترجمة README إلى 14 لغة
- **1.5** التحسينات :
  - **تحديث مفاتيح API والنماذج الافتراضية :**
    - **OpenAI :** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI :** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic :** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين مطالبات الترجمة :**
    - تم إثراء المطالبات المخصصة للترجمات المباشرة وملاحظات الترجمة لتحقيق قدر أكبر من الوضوح والفعالية، بما يشمل تعليمات مفصلة حول الحفاظ على البيانات الوصفية وعناصر التنسيق المحددة.
  - **إعادة هيكلة الكود :**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم الاستيرادات لتحسين قابلية القراءة والصيانة.
    - تحسين تجزئة النصوص وإدارة كتل الكود للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات المخرجات :**
    - عكس موضع النموذج واللغة في اسم ملفات المخرجات (على سبيل المثال، `f"{base}-{args.target_lang}-{args.model}.md"`)، مما يسهل تنظيم الترجمات والبحث عنها.
  - **تحسينات متنوعة :**
    - تنظيف الكود بحذف الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكريبت وقابليته للقراءة.
- **1.4** الميزات الجديدة :
  - دعم واجهة برمجة تطبيقات Claude من Anthropic للترجمة
  - تحسين المطالبات لمزيد من الوضوح والفعالية
  - تعديلات طفيفة لتحسين صيانة الكود
- **1.3** التحسينات والميزات الجديدة :
  - تحسين إدارة كتل الكود
  - تحسين إدارة ملفات المخرجات
  - تحسين اكتشاف الملفات الموجودة مسبقًا
  - الخيار `--force` لفرض الترجمة
  - عكس موضع النموذج واللغة في اسم ملف المخرجات
- **1.2** إصلاح سجل التغييرات (changelog)
- **1.1** إضافة دعم واجهة برمجة تطبيقات Mistral AI
- **1.0** الإصدار الأولي - دعم واجهة برمجة تطبيقات OpenAI

**مقال مترجم من الفرنسية إلى العربية بواسطة gemini-3.8-flash-medium.**
