### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.13.0** المزوّد `--use_openrouter`: موجّه مدفوع إلى نحو 430 نموذجًا، من بينها النماذج الصينية المفتوحة (2026-09-05):

  - **المسار التاسع للمزوّد، وقد أُطلق مع الثامن.** لم يُنشر الإصدار 1.12.0 على PyPI: إذ صدر الموجّهان، OpenCode وOpenRouter، معًا. يتيح [OpenRouter](https://openrouter.ai) عبر مفتاح واحد الوصول إلى نماذج لا يعرضها هنا أي مزوّد آخر — Kimi وQwen وDeepSeek وZ.ai — باستخدام رصيد موحّد تُحتسب تكلفته حسب الاستخدام. ولأن endpoint متوافق مع OpenAI، فالعميل هو نفسه المستخدم مع xAI؛ **وكل ما يميّز هذا المزوّد محصور في preflight**، وكل قاعدة فيه مستمدة من قياس أُجري على API.

  - **يُقدَّم النموذج نفسه عبر عشرات المستضيفين ذوي الحدود القصوى المختلفة، والتوجيه لا يرى ذلك.** وفق القياس: 33 مستضيفًا لـ`z-ai/glm-5.2` و23 لـ`z-ai/glm-5.3-flash` — أحدهم محدود بـ**2,048 token للإخراج**. لذلك كانت ترجمة طويلة من أصل 23 تُبتَر عشوائيًا من دون أدنى إشارة. يقرأ preflight القيمة `/api/v1/models/{modèle}/endpoints`، ويستبعد المستضيفين الذين يقل حد إخراجهم عن 8,000 token، والذين تكون حالتهم متدهورة، والذين لا يعلنون أي حد أقصى، ثم يثبّت الباقين. إن `provider.only` **من دون** `allow_fallbacks: false` ليس سوى تفضيل: إذ يعود الموجّه إلى مستضيف مستبعَد، فيفقد التثبيت أي قيمة. وإذا لم يستوفِ أي مستضيف الحد الأقصى، يتوقف الأمر: فمواصلة الترجمة تعني قبول البتر الصامت الذي وُجد هذا preflight لمنعه.

  - **تُحتسب تكلفة الاستدلال بتعرفة الإخراج، وهو مفعّل افتراضيًا في نماذج كثيرة.** الطلب نفسه على `z-ai/glm-5.2`، والإجابة «حسنًا»: **107 tokens إكمال باستخدام الإعداد الافتراضي للنموذج، و2 عند تعطيل الاستدلال**. وفي الترجمة، حيث لا يضيف الاستدلال شيئًا، يعني ذلك تضاعف التكلفة 18 مرة لكل مقطع من كل ملف. لذلك يُعطّل افتراضيًا. أما **النماذج الـ288 من أصل 431** التي تفرضه (`reasoning.mandatory`)، فتستجيب بـ`400 « Reasoning is mandatory for this endpoint and cannot be disabled »`: وبالنسبة إليها، يقرأ preflight مستويات الجهد المقبولة ويطلب أدناها (النقطة التالية) — إذ يخصص الجهد **نسبة مئوية من `max_tokens`** يستهلكها الاستدلال أولًا، ولذلك فإن اختيار قيمة عشوائيًا ينقل خطر الصفحة الفارغة بدلًا من تقليله.

  - **يتلقى النموذج الذي يفرض الاستدلال أدنى جهد يقبله، وقد حُسم ذلك بالقياس.** كان الخيار الأول ألّا يُرسل شيء، تجنبًا للتخمين بدلًا من النموذج. وعند اختباره على `z-ai/glm-5.3-flash`، الذي إعداد الكتالوج الافتراضي له هو `max`، نتج إخراج **مبتور عند 32,768 token** قبل نهاية الترجمة — فضاعت لغتان من أصل أربع عشرة. ولم تكن زيادة السعة لتغير شيئًا: فالجهد يخصص نسبة منها، ويزداد الاستدلال معها. لذلك يقرأ المزوّد `supported_efforts` أثناء preflight ويطلب أدنى قيمة، مع الرجوع إلى «لا شيء» عندما لا يعلن الكتالوج أي قيمة قابلة للاستخدام. وأثبت الاختبار المضاد على اللغة التي كانت تفشل النتيجة: فقد كانت تفشل بسبب نفاد الميزانية، وأصبحت الآن تنجح خلال 9 دقائق ببنية مطابقة للمصدر.

  - **يحمل تعطل المستضيف upstream اسمه الآن.** يطبّع الموجّه هذه الحالة إلى `finish_reason=error` مع `native_finish_reason` فارغ — وقد قيس ذلك مرتين، بلغتين، عند 750 ثانية بالضبط. كانت الرسالة العامة تدفع إلى البحث عن عيب في المستند أو في التقسيم؛ أما الآن فتوضح أن العطل لدى المزوّد وأن إعادة المحاولة تكفي غالبًا.

  - **إن `finish_reason=length` المصحوب بإخراج فارغ ليس بترًا.** بل يعني أن الاستدلال استهلك الميزانية قبل أول محرف مفيد — وقد قيس بـ15,850 token للاستدلال مقابل 148 مفيدًا. وتتطلب الحالتان إجراءين متعاكسين: فتقليل حجم المقاطع لا يفيد في الحالة الأولى. وتفرّق الرسالة بينهما بوضوح. كما أضيف حاجزان آخران بناءً على القياسات: يستجيب الموجّه بـ**200 مع جسم لا يحتوي إلا على خطأ** عندما يفشل المستضيف upstream (كان `choices[0]` يطلق `TypeError` مبهمًا يحجب الرسالة)، وتُقرأ نافذة السياق من الكتالوج ثم تُسجّل في `MODEL_TOKEN_LIMITS` — إذ إن `DEFAULT_TOKEN_LIMIT` غير صحيح لـ44 نموذجًا في الكتالوج، من بينها نموذجان محدودان بـ4,095 token.

  - **إن `--model fournisseur/modèle` إلزامي، ويُتحقق من صيغته قبل أي اتصال بالشبكة.** OpenRouter ليس مزوّدًا: فالاختيار يحدد السعر والترخيص ومعالجة البيانات، ولا يجوز اتخاذه بدلًا من المستخدم. وبما أن slug يُدرج في URL الخاص بـpreflight، فالتحقق ليس مجرد تحسين لسهولة الاستخدام، بل هو الحاجز الذي يمنع حقن مسار فيه: يقبل regex المشترك ذو namespace بين الموجّهين `a/b/..`، ومن هنا يأتي الرفض الصريح للمقطع الأب. لا تأثير لـ`--eco`، وهو يصرّح بذلك.

  - **صُححت ثلاث صياغات، إحداها كانت خاطئة.** كان تحذير OpenAI بشأن `codex exec` يستهدف حقن ملف جلسة شخصي في runner مشترك، لا كون المستودع عامًا: وقد اقتُبس بعكس معناه في README وCLAUDE.md والشيفرة. تغيّر موضع مصادقة OpenCode في 1.18.27 (الجدول `credential` في `opencode.db` بدلًا من `auth.json`)؛ وظل الثابت «لا تُقرأ هنا مطلقًا» صحيحًا، لكن العنوان كان قديمًا. وأخيرًا، لم يعد قسم OpenCode يعرض مسارات لم تُختبر قط بوصفها متكافئة: فقد اختُبرت بوابة Zen وOllama من البداية إلى النهاية، بينما لم تُختبر GitHub Copilot وLM Studio وllama.cpp، ويوضح README ذلك الآن.

  - **حملة قياس وجدول بالنماذج الموصى بها في README.** نُفذت أكثر من ثلاثمائة ترجمة على ثلاث مجموعات من المستندات — مقالة مدونة كثيفة في وضع `--news`، وREADME هذا بصيغة Markdown القياسية، وأربعة ملفات README لمشروعات معروفة أُخذت كما هي من GitHub — إلى أربع عشرة لغة. يميّز الجدول بين أمرين كانا يُخلطان: الترجمات التي **تكتمل** وتلك التي تكون **بنيتها مطابقة** للمصدر. لم تفقد ثلاثة نماذج أي معلومة قط في المستندين الكثيفين: `gemini-3.7-flash` و`gpt-5.6-sol` عبر اشتراك ChatGPT و`z-ai/glm-5.2` عبر OpenRouter — وكانت اختلافاتها الوحيدة زوجًا من `**` لم يُنقل في لغة أو لغتين. والخلاصة الأساسية هي أن **العامل الفارق هو كثافة المستند، لا وضع `--news`**: يفشل Grok عبر الاشتراك ثلاث عشرة مرة من أصل أربع عشرة في مقالة المدونة، وينجح في أربعة عشر README عامًا من أصل ستة عشر، والسبب هو خروجه عن المسار في مقطع طويل، وقد تحقق ذلك باختبار مضاد. ويتضمن الجدول تحذيره الخاص: فهو غير شامل، ومؤرخ، والمدد لا ترتب شيئًا، وتظل المنهجية الصحيحة هي القياس على مستندات المستخدم نفسه.

  - **صُحح مقارن البنية قبل نشر أرقامه، لأنه كان ينتج نتيجتين إيجابيتين كاذبتين في الكتابات غير اللاتينية.** لم يكن URL متبوعًا بقوس كامل العرض `）` يُفصل بواسطة regex يتوقف عند `)`، ولذلك كانت السلسلة المستخرجة تختلف رغم تطابق URL؛ كما كان اقتباس من خمسة أسطر بالفرنسية يشغل ثلاثة أسطر بالصينية يؤدي إلى انخفاض عدّ قائم على الأسطر. وتأكد التصحيحان باختبار مضاد: فما زالت إزالة URL أو قسم أو code inline تُكتشف. ولولاهما، لكانت نتائج Gemini وCodex قد نُشرت بإحدى عشرة لغة من أصل أربع عشرة بدلًا من ثلاث عشرة واثنتي عشرة.

  - **الاختبارات**: ملف جديد `tests/test_openrouter_provider.py` ‏(60 اختبارًا) — التحقق من النموذج ورفض المقطع الأب، وتثبيت المستضيفين (الحد الأقصى، والحالة، والحد الأقصى غير المعلن، والحد الأدنى المشترك)، وبقاء `allow_fallbacks` خاطئًا دائمًا، وتعطيل الاستدلال أو إبقاؤه وفق `mandatory`، وعقد الإخراج الكامل (خطأ داخل 200، وعدم وجود اختيار، وتمييز الصفحة الفارغة عن البتر، و`finish_reason` غير الطبيعي، والمحتوى الفارغ)، وإغلاق preflight عند الفشل إذا تعذر الوصول إلى الكتالوج، أو غاب slug، أو لم يوجد مستضيف سليم، وحصرية flag وتسمية اسم الملف. أصبحت الحزمة الكاملة تضم **473 اختبارًا**.

  - **إعادة هيكلة: قُسّمت الوحدة الوحيدة المؤلفة من 4,253 سطرًا إلى وحدات، من دون تغيير سطر واحد من السلوك.** وُزّع `src/aipmt/translate.py` على `config` و`markdown` و`segmentation` و`guards` و`placeholders` و`news` و`prompts` و`notes` و`naming` و`pipeline` و`cli` وحزمة فرعية `providers/` (وحدة لكل مزوّد، و`base` كأساس، و`registry` للحل وdispatch). وكل عملية نقل هي commit بدليل آلي: يقارن مدقق AST لجميع عقد المستوى الأعلى في الحزمة بلقطة مرجعية، ويتحقق من موضع كل رمز، وبقاء علامات الأمان حرفيًا، وعدم وجود ملف غير متعقّب — وهي أدوات مؤقتة أزالها الإصدار التالي. أما التغييرات الظاهرة فهي: أصبح `aipmt.translate` واجهة تعيد عرض الأسماء الـ64 التي كانت الوحدة تعرضها من دون البادئة `_`، مع الحفاظ على هوية الكائن (`__all__` يضم تسعة منها، وهي API المدعومة؛ أما البقية فهي aliases للتوافق)، وتوقف عن إعادة تصدير أسماء التبعيات والمكتبة القياسية الـ29 التي كان يجمعها `import *`؛ ولم يعد التنفيذ المباشر للملف (`python src/aipmt/translate.py`) موجودًا — ويظل `aipmt` و`python -m aipmt` الصيغتين المدعومتين؛ وأصبح `__module__` للدوال العامة هو وحدة تعريفها؛ كما أصبحت SDK تُستورد بعد تحميل `.env` بدلًا من قبله، من دون أثر معروف. حُفظت الاختبارات الـ427 حتى على مستوى المعرّفات، ونُقلت إلى الوحدات التي تختبرها: أصبحت 91 patches كانت تمر عبر الواجهة تستهدف الآن الوحدة التي تقرأ الاسم (ظل اثنان منها ناجحين من دون patch الخاص بهما، وفق القياس)، وتثبّت سبعة اختبارات عقد الواجهة، وأُعيدت كتابة أدوات gates قبل أول عملية نقل حتى لا يتمكن أي منها من التحول إلى الأخضر بتوقفه عن التحقق — نطاق Lizard على مستوى الأدلة مع حد أدنى، وقراءة flags من parser المنشأ، وحد أدنى للتغطية لكل حزمة، و`release.sh` الذي يسرد الوحدات المتعقبة.
  - **إصلاح (مراجعة pull request)**: يرفض OpenRouter سجل كتالوج من دون `context_length` بدلًا من تسجيل القيمة الافتراضية البالغة 128,000 token باعتبارها قياسًا — وهو ما كان يعطّل أيضًا تحذير «النموذج غير مدرج» — وعندما يكون `finish_reason` فارغًا (النوع الموثق `string | null`)، يُعتد بسبب المستضيف الخام، إذ تساوي `max_tokens` القيمة `length`. يستجيب OpenCode بخطأ العقد الخاص به، لا بـ`AttributeError`، عند حدث `part: null`، ويرفض تدفق JSONL إذا تعذرت قراءة أحد أسطر أحداثه بدلًا من قبول نص جزئي. تقتل واجهات CLI الوكيلة الثلاث مجموعة الوكيل عندما تستقبل هذه العملية `SIGTERM` أثناء الاستدعاء — كان `timeout` الخاص بـregen يترك الوكيل حيًا ليواصل استهلاك حصته — ويتبع `SIGKILL` الخاص بالمجموعة دائمًا مهلة السماح، إذ إن shim يموت بصورة سليمة يترك حفيده حيًا. أُعيد وصل زوج `# fmt: off` / `# fmt: on` كان التقسيم قد فصله؛ وتسمّي مساعدة `--reasoning_effort` المزوّدين الأربعة الذين يستخدمونه.

- **1.12.0** المزوّد `--use_opencode`: ‏OpenCode، الوكيل مفتوح المصدر، نحو المزوّد الذي يختاره المستخدم — نموذج محلي، أو مجاني بلا حساب، أو اشتراك، أو مفتاح (2026-09-04):

  - **المسار الثامن للمزوّد، وهو مختلف في طبيعته عن المسارات السبعة الأولى.** ليس [OpenCode](https://opencode.ai) ‏(MIT) مزوّد نماذج، بل _موجّهًا_ نحو المزوّدين الذين أعدّهم المستخدم داخل OpenCode نفسه: مفتاح API، أو اشتراك (GitHub Copilot أو ChatGPT أو SuperGrok)، أو بوابة OpenCode Zen — التي توفر نماذج مجانية **من دون حساب** — أو نموذج **محلي** (Ollama أو LM Studio أو llama.cpp). يتحكم السكربت في `opencode run` بالوضع غير التفاعلي، مثلما يتحكم في Codex وGrok، ويعيد استخدام أساس subprocess نفسه (مجموعة عمليات مستقلة، و`SIGTERM` ثم `SIGKILL` عند timeout، وإغلاق stdin دائمًا، وتنقية البيئة). وقد تحقق ذلك عبر **ترجمتين فعليتين**: README هذا كاملًا إلى الإنجليزية عبر `opencode/mimo-v2.5-free` — خلال 49 ثانية، بتمريرة واحدة، وبنية مطابقة للملف المصدر (32 عنوانًا، و26 إغلاقًا للشيفرة، و18 رابطًا، و37 URL، و37 سطر جدول، و135 code inline) — وملف اختبار عبر `ollama/qwen2.5:7b` محليًا ومن دون أي مفتاح.

  - **إن `--model provider/modèle` إلزامي، وهو اختيار مقصود.** من دون `--model`، يعود OpenCode إلى إعداده الافتراضي الخاص، الذي يكون في التثبيت الجديد `opencode/big-pickle`، وهو نموذج «stealth» مجاني قد تُستخدم المحادثات معه في التدريب — وقد أثبت القياس أن هذا النموذج هو الذي أجاب. إن اختيار ذلك بصمت بدلًا من المستخدم سيكون تمامًا من نوع التحول الخفي الذي يتتبعه هذا المستودع؛ لذلك تسمّي رسالة الخطأ الأمر الذي يسرد النماذج (`opencode models`) وثلاثة أمثلة (محلي، ومجاني، واشتراك). لا تأثير لـ`--eco`، وهو يصرّح بذلك. ويُمرّر `--reasoning_effort` كما هو بوصفه `--variant` الخاص بـOpenCode، وعند طلب صريح فقط.

  - **عزل مقاس، لا مفترض.** يعرّف إعداد inline ‏(`OPENCODE_CONFIG_CONTENT`، وهو الأخير في ترتيب دمج OpenCode، ولذلك تكون له الأولوية على إعداد المستخدم من دون استبداله) وكيلًا `aipmt` تُرفض جميع أدواته (`permission: {"*": "deny"}`): فلا يعرضها السجل أصلًا على النموذج، وعندما يُطلب منه «سرد الملفات وتشغيل `id`» يجيب بأنه لا يملك أدوات. عُطّلت مشاركة الجلسة، واستُبعدت plugins الخارجية (`--pure`)، ولم يُستخدم `--auto` مطلقًا، وكان دليل العمل مؤقتًا وفارغًا. وقيس حقنان صامتان وعُطّلا: فمن دون `OPENCODE_DISABLE_CLAUDE_CODE`، يدخل `~/.claude/CLAUDE.md` الخاص بالمستخدم في **كل** prompt ‏(515 token إدخال بدلًا من 186 لعبارة «مرحبًا» بسيطة)؛ ومن دون `OPENCODE_DISABLE_PROJECT_CONFIG`، يدخل أيضًا `AGENTS.md` الخاص بالدليل الحالي — وقد طُبقت على الترجمة تعليمة تقول «اختم كل إجابة بكلمة BANANA». أما `~/.config/opencode/AGENTS.md` العام فيظل محقونًا: فلا يوجد مفتاح يعطله، كما أن التحايل عليه باستخدام `XDG_CONFIG_HOME` محرّف سيحجب أيضًا مزوّدي المستخدم. لذلك وُثق بدلًا من ترقيعه.

  - **إن `exit 0` لا يثبت شيئًا: إنها واجهة CLI ثالثة، وبالاحتياطات نفسها — لكن مع فخين خاصين بها.** لا يؤدي `--agent` مجهول إلى فشل `opencode run`: يظهر تحذير على stderr ثم يحدث رجوع **صامت** إلى وكيل البرمجة مع أدوات مفعّلة. لذلك، إذا لم يؤخذ إعداد inline في الحسبان، فستنطلق الترجمة باستخدام وكيل قادر على الكتابة؛ ومن ثم يتحقق عقد الإخراج من غياب هذه الرسالة، إضافة إلى: return code يساوي 0، وعدم وجود أي حدث `error`، وعدم وجود `tool_use`، وأن يكون آخر `step_finish` في `stop` (إذ إن `length` إجابة مبتورة)، وألّا يكون النص فارغًا. أما الفخ الثاني فهو أن حدث خطأ JSON **مبهم** — «خطأ غير متوقع في الخادم. راجع سجلات الخادم للحصول على التفاصيل.»، مع مرجع بسيط — وأن السبب الحقيقي (`ProviderModelNotFoundError: Model not found: foo/bar. Did you mean…` و`ProviderAuthError`…) لا يوجد إلا في السجلات. ومن هنا يأتي `--print-logs --log-level ERROR` وقراءة الحقل `error="…"` من stderr، من دون trace الخاص بـBun الذي يليه. وبذلك يفشل النموذج المجهول خلال ثانية واحدة مع تسمية سببه. كما يمنع `--title` استدعاء LLM إضافيًا غير ضروري: فمن دونه، يولّد OpenCode عنوان جلسة عبر جولة إضافية على `small_model`.
  - **الأسرار: التصفية نفسها حسب النمط كما في Codex وGrok، مع استثناء اسمي واحد.** تُحفظ `OPENCODE_API_KEY`: فهي مفتاح OpenCode نفسه (بوابة Zen، اشتراك Go)، وموجّهة إليه باسمه — أي ما يعادل `auth.json` الخاص به، وليست مفتاحًا يديره aipmt أو يمكنه احتساب تكلفته. تُضبط الجهات المزوّدة في OpenCode ‏(`opencode auth login`، `opencode.json`)، ولا تُضبط مطلقًا في `.env` الخاص بـaipmt، إذ لا يصل أي مفتاح منه إلى العملية الفرعية. ولا يوجد رفض في CI، بخلاف واجهات CLI المعتمدة على الاشتراك: فاستخدام مفتاح API أو نموذج مستضاف ذاتيًا على مشغّل يُعد مشروعًا.

  - **يتحقق حاجز منع اجتياز المسارات الآن من القيمة بعد الاستيفاء، لا من القيمة الخام.** يحتوي `provider/modèle` على `/` كان حاجز الإصدار 1.10.0 يرفضه — وكان محقًا، إذ يُستوفى `--model` داخل اسم الملف `--include_model`. يستبدل وسم اسم الملف الآن `/` و`\` و`:` بـ`-` قبل أي استيفاء (`ollama/qwen2.5:7b` ← `ollama-qwen2.5-7b`، لأن `:` غير قانوني على Windows)، ويفحص الحاجز الأولي هذا الوسم: يصبح `../../evil` الاسم البسيط `doc-en-..-..-evil.md` تحت الوجهة، ولا يبقى مرفوضًا سوى `..` منفردًا، وكذلك `--target_lang ../x`. ويظل حاجز النطاق `_ensure_within_directory` طبقة الحماية الثانية بلا تغيير.

  - **النماذج المجانية والنماذج المحلية، وما جرى قياسه.** يترجم `opencode/mimo-v2.5-free` فقرة في 16 ثانية وملف README هذا في 49 ثانية؛ ويستغرق `opencode/big-pickle` مدة 40 ثانية لمعالجة 200 كلمة، بينما ظل طلبان متزامنان فيه بلا استجابة طوال 5 دقائق مع أن كلًا منهما ينجح منفردًا؛ ولم يُرجع `opencode/nemotron-3.5-lightning-free` أي استجابة خلال 3 دقائق. ومن هنا جاء `REGEN_PROVIDER=opencode` مع إلزامية `REGEN_MODEL` وتشغيل **مهمتين** بالتوازي. محليًا، يضبط Ollama غالبًا سياقًا من 4,096 token، بينما يصل طول المقاطع إلى 16,000 محرف: لذلك لا بد من `Modelfile` مع `PARAMETER num_ctx 32768`، وتتبع الجودة النموذج المستخدم — فقد عكس نموذج 7B ترتيب قائمة وأفسد سياج كتلة برمجية في ملف الاختبار، بينما حافظ نموذج البوابة على كل شيء.

  - **لم تعد ترجمات هذا المستودع تمر مطلقًا عبر API مدفوعة.** كان `regen_translations.sh` يستخدم OpenAI API بمجرد وجود مفتاح في `.env`، ولم يجعل Codex إلا خيارًا يتطلب التفعيل الصريح. وهذا بالضبط ما حدث أثناء إعداد هذا الإصدار: أُرسلت الترجمات الـ28 إلى OpenAI API، ثم أُرسل CHANGELOG الهندي إلى Gemini API، مع أن اشتراك ChatGPT موجود تحديدًا لتجنّب الدفع حسب الاستخدام. أُزيل الاكتشاف التلقائي للمفاتيح: **Codex هو الخيار الافتراضي، مع `gpt-5.6-sol`**، وهو نموذج الجودة؛ وتتطلب `openai` و`gemini` و`grok` استخدام `REGEN_ALLOW_PAID_API=1` إضافة إلى `REGEN_PROVIDER`، وهو استثناء مسمّى كي تُطبّق القاعدة عند اتخاذ القرار؛ ويفشل أي `REGEN_PROVIDER` غير معروف بدل الرجوع إلى API. وتثبّت عشرة اختبارات الخيار الافتراضي والرفض والاستثناء. وأُعيد إنشاء الترجمات الـ28 لهذا الإصدار عبر Codex.

  - **وُحّدت آلية التراجع التدريجي عند تجاوز حد المعدل** (`_retry_on_rate_limit`): كانت حلقتا Codex وGrok متطابقتين باستثناء التسمية، وكانت نسخة ثالثة ستتجاوز عتبة التكرار. ترث أخطاء CLI الثلاثة من `_CliCallError` واحدة؛ ويمنع اختبار خروج أي منها عن هذا التسلسل، وإلا فلن تلتقطها الحلقة المشتركة.

  - **الاختبارات**: ملف جديد `tests/test_opencode_provider.py` (51 اختبارًا) — عقد المخرجات الكامل، والرجوع الاحتياطي للوكيل، وقراءة السبب من السجلات، وإزالة تكرار أجزاء النص وتجاهل الأجزاء الاصطناعية، وإنهاء مجموعة العمليات عند انتهاء المهلة، والتراجع التدريجي عند 429، وإلزامية النموذج والتحقق منه، والفحص المسبق دون أسرار، وحل مسار الملف التنفيذي، وتوصيل الإرسال، ووسم اسم الملف والاختبار المضاد لاجتياز المسارات. يوسّع `tests/test_review_hardening.py` حصرية الأعلام وغياب الأسرار لتشمل المزوّد الجديد. تتطلب البوابة الآن توثيق **22 علمًا** من argparse. المجموعة الكاملة تضم **382 اختبارًا**.

- **1.11.1** تصحيح في التوثيق: يعلن README أخيرًا مسارات المزوّد السبعة (2026-09-03):

  - **كانت صفحة PyPI للإصدار 1.11.0 تقول «4 واجهات API + واجهة Codex CLI».** بينما يتيح الكود سبعة مسارات — OpenAI وMistral وClaude وGemini وGrok عبر API؛ وCodex ‏(ChatGPT) وGrok عبر الاشتراك، من دون احتساب تكلفة حسب الاستخدام. كان نمطا Grok غائبين عن العبارة الافتتاحية وعن نقطة _المزوّدات المتعددة_، وكررت الترجمات الـ14 الخطأ. ولأن الوصف الطويل للحزمة ثابت لكل إصدار، فقد تطلب تصحيح الواجهة رقم إصدار جديدًا: وهذا هو السبب الوحيد لوجود هذا الإصدار. **لا تغييرات في الكود.**
  - تمت محاذاة `CLAUDE.md` مع ما أدخله النشر: عدادات البوابة (16، و17 في `--full`)، ومسارات العمل النشطة الأحد عشر، وعدادا Sonar/Codacy غير الظاهرين في `gh pr checks` ‏(hotspots وCodacy API)، ونقل `# nosemgrep` بواسطة `ruff-format`، وبيئات GitHub التي يتطلبها تبادل OIDC، وحقيقة أن _الناشر المعلّق_ لا يحجز الاسم.

- **1.11.0** النشر على PyPI: ‏`pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-09-03):

  - **أصبح السكربت أحادي الملف حزمة قابلة للتثبيت.** انتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول لوحدة التحكم `aipmt` وما يعادلها `python -m aipmt`. يظل المستودع المستنسخ ضروريًا للمساهمة — إذ توجد فيه الاختبارات والترجمات الـ28 وأدوات الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` مطلقًا**، لأن التعارض حقيقي وصامت. تثبّت حزمة PyPI المسماة `translate` ‏(v3.8.1، وآخر رفع في 2026-07-06) دليلًا يحمل الاسم نفسه. أُعيد إنتاج ذلك داخل venv: يتغلب الدليل على الوحدة، ويختفي `translate.main`، وتتعطل نقطة الدخول عند `AttributeError` — بينما يجيب `pip check` بالرسالة «لم يُعثر على متطلبات معطلة» مع rc=0. كان مجرد وجود `pip install translate` لدى المستخدم كفيلًا بتعطيل CLI من دون تشخيص قابل للاستخدام. وفي اختبار مضاد على wheel الفعلي: ثُبّت `pip install translate` فوق الحزمة، وظل `aipmt --help` يعيد rc=0 قبل ذلك وبعده، وتتعايش واجهتا CLI.
    - **اسم توزيع طويل وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للاكتشاف عبر بحث PyPI؛ أما الاختصار وحده فلن يعثر عليه من لا يعرف المشروع مسبقًا، مع أن هدف النشر تحديدًا هو إتاحة اكتشافه. واستُبعد مرشحان معقولان بعد التحقق: `ai-markdown-translator` محجوز على npm منذ 2024 لأداة تؤدي الغرض نفسه وتسبق هذا المستودع بـ17 شهرًا؛ ويفصل حرف واحد فقط بين `aimt` و`aim` ‏(v3.29.1)، وهي حزمة نشطة في المجال **نفسه** — أسوأ وضع ممكن لإحداث التباس دائم. وهناك أيضًا فخ منهجي: يعيد `pypi.org/project/<nom>/` حالة 200 لأي اسم كان (صفحة لمكافحة الروبوتات)، ولا يُعتد إلا بـJSON API.
    - **تخطيط `src/` بدل الحزمة المسطحة.** كان من شأن الحزمة المسطحة الحفاظ على عبارات `sys.path.insert(..., "..")` الست في الاختبارات، وهذا هو العيب تحديدًا: فهي تجعل الاختبارات تستورد شجرة المصدر بدل الحزمة، ما كان سيحجب أي خطأ في التحزيم. التكلفة الفعلية: قاعدة استبدال إضافية واحدة.

  - **يمكن أخيرًا ضبط المفاتيح مرة واحدة بصورة دائمة.** لم يكن لواجهة CLI المثبتة أي إعداد **دائم**: لم يتبق سوى متغير البيئة و`.env` في الدليل الحالي. صحيح أن `find_dotenv` يصعد حتى جذر النظام، ولذلك كان يعثر على `~/.env` **عند العمل داخل الدليل الشخصي**، لكنه لم يكن يعثر على شيء عند العمل في مكان آخر — تغطية تعتمد على المكان الذي شُغّل منه الأمر، لا على خيار تصميمي. لذلك أُضيفت طبقة ثالثة: `~/.config/aipmt/.env`، بعد الطبقتين الحاليتين.

    - **الأولوية غير مرمّزة صراحةً**، بل تنتج من `override=False`، وهي القيمة الافتراضية لـ`load_dotenv`: لا تملأ كل طبقة إلا ما تركته الطبقة السابقة فارغًا. ومن هنا يأتي ترتيب متغير البيئة ← ملف `.env` الخاص بالمشروع ← إعداد المستخدم، وقد جرى التحقق منه باختبارات **السلوك** لا البنية — فعكس ترتيب الاستدعاءين يجعلها تفشل، وكذلك حذف الطبقة الثالثة.
    - **تنسيق `.env` وليس TOML**، عن قصد: ‏`python-dotenv` تبعية موجودة بالفعل، والصياغة موثقة بالفعل في ملفات README الخمسة عشر، ويُستخدم الملف نفسه للنطاقين. لا تبعية جديدة ولا صياغة جديدة. يتبع الموقع `XDG_CONFIG_HOME` إذا كان **مطلقًا** — إذ تطلب المواصفة تجاهل القيمة النسبية، وإلا لعاد موقع الإعداد معتمدًا على الدليل الحالي — ويتبع `APPDATA` على Windows.
    - **استُبعد خياران، مع بيان سببهما.** مخزن مفاتيح النظام (`keyring`) أكثر أمانًا على جهاز مكتبي، لكنه يفشل في البيئات عديمة الواجهة — الخوادم والحاويات وCI — أي في حالة الاستخدام الأساسية نفسها للترجمة على دفعات؛ فهو مرشح جيد كخيار يتطلب التفعيل الصريح، لكنه خيار افتراضي سيئ. أما العلم `--api-key` فسيضع المفتاح في سجل shell ويجعله ظاهرًا في `ps`.
    - **لم يعد غياب المفتاح ينتج تتبّع استدعاءات.** كان المستخدم يتلقى مكدس Python يشير إلى `site-packages` ورسالة تذكر «البيئة أو .env» من دون توضيح مكان إنشاء الثاني. وهي تعدّد الآن المواقع الثلاثة مع مسار كل منها بدقة، ويخرج الأمر بالرمز 2. شبكة الأمان **ضيقة عن قصد**: ‏`except ValueError` حول مرحلة الإعداد وحدها. فتغليف التنفيذ كله كان سيحوّل خطأً حقيقيًا يقع أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الفشل الذي يتعقبه هذا المستودع. ويقرأ اختبار مصدر `main()` لمنع ذلك.

  - **تصحيح — كان ملف `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** لا يصعد `load_dotenv()` عند استدعائه بلا وسيطة من الدليل الحالي، بل من الملف **المستدعي**، أي من `site-packages`. جرى قياس ذلك باستخدام نقطة دخول حقيقية لوحدة التحكم شُغّلت من مشروع لديه ملف `.env` خاص به: يعيد `find_dotenv()` القيمة `''` ولا يُحمّل المفتاح، بينما يعثر عليه `find_dotenv(usecwd=True)`. لم يكن الخطأ موجودًا ما دامت الأداة لا تعمل إلا من المستودع المستنسخ؛ وكان النشر سيجعله منهجيًا، بعرض وحيد يتمثل في اعتبار مفتاح API «مفقودًا» رغم صحة الإعداد.

  - **كان من الممكن أن تتحول ثلاث بوابات إلى اللون الأخضر بعد أن توقفت عن التحقق من أي شيء.** شُدّدت هذه البوابات **قبل** النقل، عن قصد: فحاجز الحماية المكتوب بعد التغيير الذي يُفترض أن يرصده لا يثبت شيئًا. تكون كل واحدة منها خضراء على المستودع الأصلي وتتحول إلى الأحمر على نسخة منقولة — وقد قيس الاتجاهان.

    - **يتجاهل Lizard المسار الغائب من دون قول شيء**: ‏rc=0، و«0 ملف جرى تحليله». كان من الممكن أن تنتقل بوابة التعقيد من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع مخرجات حجمها صفر بايت. أصبح النطاق الآن مصفوفة يُتحقق من وجود كل عنصر فيها.
    - **لا يفشل `coverage run --source=` عند استهداف وحدة غائبة**: تحذير على stderr فقط، وrc=0 سواء مع unittest أو `coverage xml`، ومع ذلك يُنشر التقرير — بعد انخفاضه من 1453 إلى 141 عبارة. كان المشروع سيبدو سليمًا لأنه لم يعد يُحلل إلا بالكاد. يحمي التقرير حدان أدنيان: الإجمالي، وأكبر ملف مقاس.
    - **مسبار حداثة الترجمات أعمى بنيويًا عن شكل الاستدعاء**: فهو يرتكز على أعلام argparse، وهي تحديدًا ما لا يتغير عند إعادة تسمية الملف. أُعيد إنتاج الحالة: نُقلت الوحدة، وظلت ملفات README الخمسة عشر توثّق أمرًا غير موجود، وكانت النتيجة «لا توجد ترجمات قديمة». لذلك يتحقق قسم سابع من **الشكل** لا الخيارات، وتُقارن وصلة Lizard بالنطاق الفعلي للسكربت — فمفتاحها `files:` لا يجعل pre-commit يفشل عندما لا يعود مطابقًا، بل يجعله **يتخطى التنفيذ**.

  - **لم يعد `requires-python = ">=3.10"` مجرد ادعاء.** كان `sonar-project.properties` يعلن بالفعل دعم 3.10-3.12 من دون أن يكون أي شيء قد اختبره قط، لأن جهاز التطوير لا يحتوي إلا على 3.12 — تناقض داخلي كان النشر سيجعله علنيًا. ينفّذ مسار عمل للاختبارات الآن المجموعة على 3.10 و3.11 و3.12، مع تثبيت **الحزمة** وبالتالي اختبار حدودها العامة.

  - **حدود دنيا، بلا حد أقصى.** يظل `requirements.txt` ملف القفل المختبَر، ويصبح `[project.dependencies]` العقد العام: فنشر الإصدارات الدقيقة من ملف القفل سيسبب تعارضات لأي مستخدم لديه حزم أخرى. ولا يوجد حد أقصى لـ`<N+1` أيضًا — إذ سيتعارض مباشرةً مع `check-deps-fresh.sh`، الذي يُفشل بوابة الإصدار عند أي تأخر في الإصدار الرئيسي. تُحل مجموعة الحدود الدنيا بنجاح، بينما يخرج الاختبار المضاد `openai==1.0.0` بالقيمة `ResolutionImpossible`، ما يثبت أن الفحص يميّز بدل قبول كل شيء. ويمنع حاجز أيضًا إصدار `pyproject.toml` من الاختلاف عن إصدار CHANGELOG: لا يسمح PyPI بإعادة استخدام رقم إصدار.

  - **جرى التحقق من البداية إلى النهاية داخل venv جديد**: ملف wheel بحجم يقارب 70 كيلوبايت لا يحتوي إلا على `aipmt/*.py` وdist-info والترخيص؛ ويعيد `aipmt --help` الرمز rc=0 مع 22 علمًا؛ ويعرض `python -m aipmt` العبارة «usage: aipmt» بدل «usage: \_\_main\_\_.py»؛ ويعمل تثبيت `pipx`؛ والأهم، **ترجمة فعلية من الفرنسية إلى الإنجليزية من أي دليل مستخدم**، مع الحفاظ على الخط العريض والقائمة والكود المضمّن والرابط وعنوان URL، ومن دون ترجمة كتلة الكود. تمر الاختبارات الـ318 السابقة للنقل بقائمة معرّفات متطابقة بايتًا ببايت قبل النقل وبعده — وهذا ما يثبت أن أي اختبار لم يُعطّل، لا عبارة «OK»؛ وأُضيف إليها اثنا عشر اختبارًا للإعداد ذي الطبقات الثلاث، ليصبح المجموع 330.

- **1.10.0** المزوّد `--use_codex` ‏(حصة اشتراك ChatGPT)، وتحديث حزم SDK والنماذج، وإصلاح اقتباسات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — حاجزا حماية وضعهما طلب الدمج من دون تطبيقهما في كل المواضع**:
    - **كان فحص Codex التمهيدي يمرّر `.env` بالكامل إلى الملف التنفيذي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: إذ كانت العملية الفرعية ترث `os.environ` كاملًا، وبالتالي كامل `.env` الذي حمّله `load_dotenv`. وبالقياس باستخدام ملف تنفيذي وهمي مزوّد بأدوات رصد: وصلت **سبعة أسرار** إلى الفحص التمهيدي — مفاتيح المزوّدين الستة بالإضافة إلى `GITHUB_TOKEN` — مقابل **صفر** لدى نظيره `_grok_preflight`، الذي كان يمرّر `env=_grok_env()` بالفعل. كان التناقض داخليًا في PR: إذ يوجد `_strip_secret_env` تحديدًا للحفاظ على هذا الثابت، على بُعد بضعة أسطر فقط. استُخرج `_codex_env_base()` وصار مشتركًا بين المسارين؛ والقياس بعد التصحيح: صفر من الأسرار في كلا الجانبين.
    - **لم تكن خاصية «الإغلاق الآمن عند فشل `--deny`» تغطي الصيغة المستخدمة.** كان التعليق يبرّر عزل Grok كاملًا بأن القاعدة ذات البادئة المجهولة تمنع بدء التشغيل. وبالقياس على `grok 1.0.13`، لا يوجد هذا التحقق **إلا للصيغة الموضوعة بين قوسين**: يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل («بادئة أداة مجهولة»)، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. لكن `GROK_DENY_RULES` لم يكن يستخدم سوى أسماء مجردة؛ ومن ثم كان من شأن إعادة تسمية أداة لدى xAI أن تزيل طبقة العزل الوحيدة التي أثبتها القياس من دون أي إشارة، على جهاز لا ينطبق عليه أصلًا sandbox نظام التشغيل. نُقلت القواعد الثماني المسماة إلى `Prefix(*)`، مع التحقق من كل واحدة منها بوصفها بادئة معروفة لدى CLI؛ أما قاعدة الالتقاط الشاملة `*` فتبقى بصيغتها الحرفية، وهي الوحيدة المقبولة. ويمنع اختبارٌ العودة إلى الصيغة غير الخاضعة للتحقق.
    - **وتحقّقنا من سلامة الجوانب الأخرى**: لا يوجد حقن أوامر (تُستخدم صيغة القائمة في كل موضع، ولا يُستخدم `shell=True` مطلقًا، ويمر محتوى المستند عبر stdin أو `--prompt-file`)، ولا توجد إزالة تسلسل غير آمنة (`json.loads` فقط، مع حارس للنوع)، ولم يُعثر على أي تجاوز لتصحيح اجتياز المسار عبر سبع حمولات، كما يطبّق CLI بالفعل `--deny '*'` (رُصد `DENY_ENFORCED` عند قراءة ملف خارج workdir).
    - وكان فحص الحداثة المضاف أعلاه يتجاوز مبدأه الخاص بالمناسبة: فإذا فشل طلب PyPI لحزمة ما، كان يتخطاها بصمت وتظهر البوابة باللون الأخضر. وهو الآن يحصي الحزم التي قورنت فعليًا ويفشل إذا كانت التغطية غير مكتملة.

  - **تحديث التبعيات، وإضافة شبكتي أمان لمنع تكرار التأخر**:

    - **كان التأخر حقيقيًا ومزمنًا**: `openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر عامين في مخزن شهادات الجذر الذي يتحقق من TLS لجميع استدعاءات المزوّدين. وقد حُدّد السبب: **لم يكن هناك `.github/dependabot.yml`**. من دون هذا الملف، لا يفعّل GitHub سوى _تحديثات الأمان_، ولا يقترح Dependabot ملف PR إلا لتبعية تستهدفها CVE؛ وهذا يفسّر تحديثه `urllib3` و`idna` مع ترك SDK اثنين يتأخران بإصدار رئيسي كامل.
    - **يتعايش الإصداران الرئيسيان من دون تعارض**، خلافًا لما أوحى به استنتاج سابق: ينتقل `openai` 3.x و`anthropic` 1.x إلى **`httpx2`**، بينما يبقى `mistralai` و`google-genai` على `httpx<1`، لكنهما توزيعتان منفصلتان. جرى التحقق بتثبيت فعلي، ثم عبر **مسارات المزوّدين السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الشيفرة المضمّنة والروابط في كل مخرج. كان «تجنّب مكدّسي HTTP» تفضيلًا لا عائقًا؛ وقد حسم القياس المسألة.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: كانت `google-auth` و`cryptography` ومكدّس `opentelemetry` مثبّتة في venv العمل من دون التصريح عنها قط؛ ولذلك لم يكن التثبيت الجديد يعيد إنتاج ما جرى اختباره. وعلى العكس، كانت `tokenizers` و`huggingface-hub` و`PyYAML` مدرجة فيه من دون أن يستوردها أو يحتاج إليها أي شيء، وهي بقايا من `mistralai` 1.x. أُعيد توليد الملف ليكون الإغلاق الكامل لبيئة venv مبنية انطلاقًا من التبعيات المباشرة وحدها. ولا يُبلغ `pip-audit` عن أي ثغرة معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** (جديد) يفعّل تحديثات الإصدارات الأسبوعية لكل من pip وgithub-actions. تُجمّع الإصدارات الثانوية والتصحيحات في PR واحد — لأن إنشاء PR لكل تحديث تصحيحي ينتهي بتجاهله، والضجيج عدو التحديث؛ أما **الإصدارات الرئيسية فتُفصل**، ويتطلب كل منها تحققًا عبر استدعاء فعلي.
    - **`scripts/check-deps-fresh.sh`** (جديد وموصول بالبوابة) يجعل التأخر ظاهرًا في حكم المشروع: يقترح Dependabot التحديثات لكنه لا يضمنها، وقد تتراكم ملفات PR الخاصة به. التأخر في إصدار رئيسي ← فشل؛ وفي إصدار ثانوي ← تحذير، لأن البوابة التي تبقى حمراء باستمرار ينتهي بها الأمر إلى التجاهل؛ وتعذّر الوصول إلى PyPI ← تخطٍّ صريح محليًا و**إغلاق آمن عند الفشل في CI**، لأن الفحص الذي لم يُنفّذ لا يُعد نجاحًا. جرى التحقق في الاتجاهين: فهو يلتقط الحالة الدقيقة السابقة للتصحيح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير عند وجود إصدار ثانوي.

  - **تصحيحات ناتجة عن مراجعة PR هذا** — فحص خمسة وكلاء مراجعة الفروقات بدقة؛ وقد **أُعيد إنتاج جميع النقاط أدناه بالقياس** قبل تصحيحها، وكان اثنان منها انحدارين أُدخلا أعلاه في الإصدار نفسه.

    - **تصحيح انحدار — كان `_NEWS_CITATION_REGEX` يعاني من تتبّع خلفي أُسّي.** أدخل إصلاح الفقرات المتعددة `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: كان توزيع المسافات بين `[ \t]+` و`.*` ملتبسًا، ويتضاعف هذا الالتباس من تكرار إلى آخر. وبالقياس على أسطر `>   texte` — وهي إزاحة Markdown صحيحة تمامًا — لا تطابق النمط: **2,589 مللي ثانية لـ14 سطرًا** مقابل 0.04 مللي ثانية بعد التصحيح، مع معامل يقارب 9 لكل سطر مضاف. في وضع `--news`، كان اقتباس مطوّل وغير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب قابل للتحديد. يستهلك التكرار الآن السطر كاملًا دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يترك سوى طريقة واحدة للمطابقة في كل تكرار. جرى التحقق على المجموعة الفعلية المكوّنة من 231 مقالة: **صفر اختلاف** في الالتقاط، ونفس الاقتباسات البالغ عددها 423، مع استمرار توسيع المتون الأربعة عشر متعددة الفقرات.
    - **كان استخدام علامتي مزوّد في الوقت نفسه يؤدي إلى فوترة حسب الاستخدام، بصمت.** كان `--use_codex --use_mistral` مقبولًا؛ ويختبر `_select_provider_client` ‏Mistral أولًا، بينما يمنح `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهيان إلى Mistral. وهكذا كان المستخدم يطلب حصة اشتراكه ويحصل على فوترة حسب الاستخدام، من دون أي تحذير: وهو بالضبط نمط الفشل الذي وُجد `--use_codex` لمنعه. تمر علامات المزوّد الست الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: سطر أوامر يجمع بين مزوّدين، وكان يُقبل سابقًا بصمت، يفشل الآن عند `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة نهاية العمل تظهر باللون الأخضر عندما تتعطل مجسّاتها.** كانت أربعة من فحوص `scripts/check-release-ready.sh` الثلاثة عشر تتبع نمط «التقاط stdout، والاستنتاج إذا كان فارغًا» من دون التحقق مطلقًا من رمز الخروج: كان الاستثناء (ملف أعيدت تسميته، `FileNotFoundError`) يكتب إلى stderr ويترك stdout فارغًا، فيستنتج الفحص أنه «لا شيء يستدعي الإبلاغ». وهكذا تكرّر فخ «لا يثبت `exit 0` شيئًا» داخل السكربت المكتوب لمنعه. يفرض المساعد `probe()` الآن رمز خروج صفريًا **وعلامة نهاية**، وترفض المجسّات الاستنتاج بناءً على مجموعة فارغة من العلامات — لأن أي توكيد على مجموعة فارغة يكون صحيحًا دائمًا. والدليل: أدت إضافة المجموعة الحصرية أعلاه إلى تمرير علامات المزوّد عبر كائن `*_group`، فلم تعد regex القديمة `parser\.add_argument\(` تطابقه؛ وخرجت **ست علامات من أصل إحدى وعشرين** من النطاق بصمت، مع بقاء البوابة خضراء.
    - **كان فحص الأسرار يفوّت أربعة مزوّدين من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: كان `sk-proj-…` (تنسيق OpenAI الحالي) و`sk-ant-api03-…` ينقطعان عند الشرطة الثانية، ولم يكن `AIza…` مشمولًا. وُسّع النمط، واستُبعد `.secrets.baseline` من الفحص. وإضافة إلى ذلك، كان الحارس `.env` يستعلم عبر `git diff --cached`، الذي لا يرى سوى الفهرس: فلم يكن `.env` **المُلتزم به بالفعل** — وهي أسوأ حالة — يظهر فيه قط. وهو يستعلم الآن عبر `git ls-files`.
    - **لم تكن «تهيئة الرمز المميز» في Codex تهيئة فعلية.** أظهر القياس أن `codex login status` لا يلمس `~/.codex/auth.json` (بقي وقت التعديل والحجم دون تغيير)، وتقول مساعدته «عرض حالة تسجيل الدخول». ومع ذلك كان التعليق يدّعي تحديث الرمز المميز «مرة واحدة، تسلسليًا»، وبالتالي تحييد خطر التحديثات المتزامنة لرمز مميز دوّار أحادي الاستخدام. لم تكن الحماية المعلنة موجودة؛ ويصف التعليق الآن ما تفعله الشيفرة، بينما يظل الحل الفعلي هو `max_jobs=4`. كما يحترم الفحص `CODEX_BIN`، الذي كان يتجاهله — إذ كان جهاز لا يحتوي `codex` في `PATH` يفشل برسالة «غير مصادَق عليه»، وهو تشخيص مضلل.
    - **كان `.env` يُحمّل في shell فرعي.** تُستدعى `detect_provider` داخل استبدال أمر، ولذلك لم تكن قيم export الخاصة بها تنتقل إلى الأعلى: كان `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` المعرّف في `.env` يظل غير مرئي للقراءات المنفذة في `main()`، التي تستنتج «تعذّر العثور على ملف Grok التنفيذي» رغم صحة الإعداد.
    - **كان التوازي يتجاوز الحد المعلن بنسبة 50%.** وُضع الحارس بعد إطلاق زوج README/CHANGELOG: وبلغت الذروة المقاسة **3 بالنسبة إلى `max_jobs=2`**. وبالنسبة إلى Grok، الذي يتقاسم حصته الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يكن السكربت يلتزم إذن بالحد الذي فرضه على نفسه. أما العدد النهائي فكان يُعرض من دون مقارنته قط بالعدد 28؛ ولذلك كان غياب ملف يمر من دون اكتشاف.
    - **عقد مخرجات Grok: أصبح غياب `stopReason` فشلًا الآن.** كانت الشيفرة تطبّق «`end_turn` **أو غائب**» في الموضع الذي كان العقد المعلن يتطلب فيه `end_turn`. وكانت حمولة بلا هذا الحقل — أو تغيّرت تسمية الحقل فيها بسبب تحديث CLI — تحوّل الحارس إلى عملية عديمة الأثر بصمت. وإضافة إلى ذلك، لم يعد `max_turn_requests` يُصنّف حدًا للمعدل (فقد استُنفدت ميزانية الجولات: إعادة المحاولة تعيد النتيجة نفسها مقابل انتظار 90 ثانية)، وأُخرج `quota` من علامات حد المعدل — للسبب الذي كان docstring الخاص بـ`_codex_is_rate_limited` ينص عليه بالفعل من دون أن يطبقه Grok.
    - **أصبحت سلسلة Gemini محفوظة مؤقتًا لكل نموذج.** كانت تبدأ مجددًا من `minimal` عند كل مقطع، رغم أن النموذج الافتراضي يرفضه: كان المسار الاعتيادي يدفع كلفة رحلة ذهاب وإياب بخطأ 400 لكل مقطع، ويعيد طباعة التحذير نفسه. والتحذير الذي يتكرر مئات المرات يتوقف الناس عن قراءته — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مشفّرة صراحةً لـCodex وتوجّه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق أساس العمليات الفرعية يعمّم وصف «shim» على واجهتي CLI كلتيهما، مع أن ملف Grok التنفيذي هو ELF أصلي (والتبرير الصحيح هو «وكيل ينشئ عملياته الفرعية الخاصة»)؛ وصُنّفت اثنتا عشرة نتيجة SAST في `subprocess` على أنها `# nosec` / `# nosemgrep` مع توضيح السبب، إذ تجعل صيغة القائمة من دون `shell=True` الحقن مستحيلًا، ولا يمر محتوى المستند عبر argv مطلقًا.
    - **لم يعد أي سر يدخل إلى العمليات الفرعية الوكيلة.** لم تكن قوائم المنع الاسمية تحمي سوى ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). وبالقياس: كانت **سبعة أسرار أخرى** تدخل رغم ذلك إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح CLI الآخر، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة المرور. لكن واجهتي CLI هاتين **وكيلتان**، ويعمل وكيل Grok من دون sandbox قابل للتطبيق من نظام التشغيل على كثير من أجهزة Linux. يجري الترشيح الآن **حسب نمط الاسم** (`API_KEY` و`_TOKEN` و`SECRET` و`PASSWORD` و`CREDENTIALS`)، لا حسب قائمة اسمية، ما يشمل أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن تعرفها هذه الشيفرة. ولا تحتاج واجهتا CLI إلى أي منها: إذ توجد المصادقة في `~/.codex` و`~/.grok`، وليس في البيئة مطلقًا — وقد جرى التحقق عبر **ترجمة فعلية ناجحة** باستخدام كل واحد من المزوّدين مع البيئة المحصّنة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يثبّت حصرية علامات المزوّد، وعقد `stopReason`، وخطية regex الخاصة بالأخبار، ورسالة الرفض في CI، والحفظ المؤقت في Gemini، وغياب أي سر من بيئة العمليات الفرعية. والتوكيد الأخير **عام** — إذ يفشل عند وجود مفتاح لا تسمّيه أي قائمة — بينما كان اختبار التنقية الحالي مجرد مرآة لثابته، ولم يكن قادرًا على اكتشاف أي شيء سوى تعطّل حلقته الخاصة. المجموعة الكاملة عند **311 اختبارًا**.
  - **مزودان جديدان لـ Grok**: ‏`--use_grok` (واجهة API من xAI، المفتاح `XAI_API_KEY`، والفوترة حسب الاستخدام) و`--use_grok_cli` (أداة CLI الرسمية Grok Build، تُحتسب من اشتراك Grok — بالمبدأ نفسه الذي يعتمده `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن نقطة نهاية xAI متوافقة مع OpenAI، يُعاد استخدام العميل و`_call_openai` كما هما، ولا يتغير سوى `base_url`. لم يلزم سوى تعديل واحد يستفيد منه الجميع: يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي تصدرها xAI حيث تصدر OpenAI ‏`stop`. النماذج: `grok-4.6` (الجودة) و`grok-4.3` (الاقتصادي). تجدر الإشارة إلى أن نموذج Grok الاقتصادي يظل الأغلى في المستودع — ‏$1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ `mistral-small-latest`: يُختار هذا المزود لتنوع النماذج، لا للسعر.
    - **وضع CLI**: صُمم على غرار Codex، مع أربعة اختلافات فرضها الواقع — تُمرر المطالبة عبر ملف (`--prompt-file`، إذ لا تقرأ أداة CLI من stdin وسيكون أي مقطع في argv مرئيًا داخل `ps`)، ويكون الناتج كائن JSON واحدًا على stdout (لا JSONL ولا ملف `-o`)، ولا يتيح الاشتراك سوى `grok-4.6` و`grok-4.5`، كما يتعذر تطبيق sandbox (انظر أدناه). جرى توحيد تشغيل العمليات الفرعية مع Codex داخل `_codex_run_process`، من دون المساس ببقية مزود Codex الذي سبق اختباره.
    - **`exit 0` لا يثبت شيئًا، وفقًا للقياس**: عندما لا تكون المصادقة مفعلة، تكتب أداة CLI ‏`{"type":"error","message":"Not signed in."}` على **stdout** مع رمز خروج **0**. ويتصرف رفض الطلب أو تجاوز عدد الجولات بالطريقة نفسها. لذلك يتطلب عقد الإخراج تحقق أربعة شروط متزامنة: رمز خروج 0، وعدم وجود حمولة خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع الفحص المسبق المنطق نفسه: يخرج `grok models` بالرمز 0 حتى عند انقطاع الاتصال، ولا يمكن الاستنتاج إلا من وجود «غير مصادق عليه» في stdout.
    - **الاحتواء: تفاوت مقصود وموثق.** بينما يعمل Codex في `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـ Grok على كثير من أجهزة Linux الحديثة، لسببين مستقلين على مستوى النظام لا يمكن تجاوزهما من دون `sudo`: يحظر AppArmor مساحات أسماء المستخدمين غير المميزة منذ Ubuntu 24.04 ‏(`bwrap: setting up uid map: Permission denied`، وأُعيد إنتاج المشكلة خارج Grok)، كما تفشل قائمة حظر مقابس وقت تشغيل الحاويات عندما يكون `/run/podman` في `0700` (لا يعالج المحلل سوى `ErrorKind::NotFound`، ويصبح EACCES خطأً فادحًا). الفخ الأساسي: ملف تعريف **مدمج** يتعذر تطبيقه **يبدأ بلا احتواء بصمت**. لذلك لا يطلب البرنامج النصي أي ملف تعريف افتراضيًا ولا يتراجع بصمت مطلقًا — بل يصدر تحذيرًا على stderr. تعتمد الحماية على قواعد `--deny` الخاصة بأداة CLI، بما فيها القاعدة الشاملة `*`، وهي طبقة الحماية الوحيدة التي قيس أنها تعمل وفق مبدأ _الإغلاق عند الفشل_ (تؤدي قاعدة ذات بادئة مجهولة إلى رفض بدء التشغيل). يتيح `GROK_TRANSLATE_SANDBOX=read-only` اشتراطها، وفي هذه الحالة يفشل بدء التشغيل إذا تعذر على الجهاز الوفاء بها.
    - **ضوابط الأمان**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (إذ سيؤدي وجود مفتاح إلى التحول نحو الفوترة حسب الاستخدام؛ كما أن وراثة `GROK_SANDBOX` ستفرض ملف تعريف غير قابل للتطبيق مع رسالة مضللة)، وتُعطل مفاتيح MCP/hooks/skills/agents، مع `--disable-web-search` و`--no-subagents` و`--no-plan`، ودليل عمل مؤقت، ورفض التشغيل في CI، ومهلة تقتل مجموعة العمليات، وتراجع تدريجي عند بلوغ حد المعدل. ضُبط `--max-turns` على 6 لا على 1: إذ تزداد قيمة العداد بعد جولة الأدوات، وسيؤدي ضبطها على 1 إلى اقتطاع الناتج.
    - **الحصة**: مجمع Grok أسبوعي و**مشترك مع Chat وImagine وVoice**، ولا يعرضه أي أمر — بخلاف Codex الذي يتيح `account/rateLimits/read` فيه قياس الاستهلاك. لذلك يحد `regen_translations.sh` التزامن عند 2 ويصدر تحذيرًا صريحًا.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` (24 اختبارًا). مجموعة الاختبارات الكاملة تضم **290 اختبارًا**.
  - **إصلاح خلل — لم تكن الاقتباسات الإنجليزية متعددة الفقرات محمية إلا جزئيًا (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل متنًا للاقتباس سوى سلسلة من أسطر `>` **المتتالية**. بمجرد أن يمتد اقتباس عبر عدة فقرات (تفصل بينها سطر `>` فارغ)، لا يُلتقط ويُستبدل بالعنصر النائب سوى آخرها؛ أما الفقرات السابقة فكانت تُرسل إلى LLM وتعود مترجمة — وهو النقيض تمامًا مما وُجد `--news` لضمانه. يقبل التكرار الآن أسطر `>` الداخلية الفارغة وأصبح غير جشع، ليتوقف عند `>` الفارغ الذي يسبق السطر المكتوب بالمائل، لا عند أول سطر فارغ يصادفه.
    - **النطاق المقاس** على مجموعة فعلية من 198 مقالة: تأثر 11 اقتباسًا من أصل 419. ولا توجد أي انتكاسة — يلتقط التعبير النمطي الجديد العدد نفسه تمامًا من الاقتباسات، ولا تمتد سوى المتون متعددة الفقرات (408 متون متطابقة، و11 ممتدة)، ولا يزال من المتعذر ابتلاع سطر الإسناد `> — …` ضمن المتن (مع الإبقاء على الاستباق).
    - **إثبات شامل من البداية إلى النهاية** على مقالة بحجم 69 كيلوبايت مترجمة إلى اليابانية والعربية: تظل الفقرة الأولى من اقتباس، التي كانت سابقًا تُعرض على هيئة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم بالطريقة نفسها إلى العربية، الآن `> GLM-5.3 is now open-weight.`. ويعود عدد أسطر الاقتباس بالإنجليزية من 9 إلى 10، أي إلى عددها في المصدر.
    - تجدر الإشارة إلى أن هذا العيب لم تكتشفه أدوات التحقق اللاحقة، التي تتحقق من وجود الاقتباس من دون التأكد من اكتماله.
  - **توفير مقاس على المزود الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ اسم النموذج بـ `gpt-5`، بما في ذلك عند استخدام `--eco`. القياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: ‏`medium` ← 45 رمز استدلال و65 رمز إخراج؛ ‏`none` ← 0 و14. لا يضيف الاستدلال شيئًا إلى الترجمة، ومع ذلك كانت تكلفته تُدفع لكل مقطع من كل ملف. أصبحت القيمة الافتراضية `none` في `--eco`، وتظل `medium` في غير ذلك؛ وتبقى لأي قيمة صريحة تُمرر عبر CLI الأولوية. يقبل `--reasoning_effort` الآن `none` و`xhigh` إلى جانب `low`/`medium`/`high` (ولا تقبل جميع النماذج كل هذه القيم: فعلى سبيل المثال، يرفض `gpt-5.4-mini` القيمة `minimal` — وتغطي هذا السيناريو إعادة المحاولة الموجودة من دون المعلمة).
  - **تحديث حزم SDK وترحيل Gemini**: استُبدلت `google-generativeai` (انتهى دعمها في 2025-11-30 وأُرشف مستودعها) بحزمة SDK الموحدة **`google-genai`** — ‏`genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير مطالبة النظام عبر `system_instruction` بدلًا من دمجها مع المقطع. رُقيت `mistralai` إلى **2.9.4** (أصبح الاستيراد `from mistralai.client import Mistral`؛ أما القديم فيثير `ImportError`، كما جرى التحقق داخل wheel)، و`anthropic` إلى **0.125.0**، و`openai` إلى **2.54.0** — وهي أحدث الإصدارات السابقة للانتقال إلى `httpx2`، تفاديًا لتشغيل مكدسي HTTP معًا داخل venv. وبناءً على ذلك، فُك تثبيت `httpx` عند 0.28.1 و`pydantic` عند 2.13.5.
  - **انتكاسان اكتشفتهما اختبارات فعلية، لا الوثائق**:
    - يرفض `anthropic` بالإصدار ≥ 1.0 من جهة العميل استدعاءً غير متدفق يوحي `max_tokens` الخاص به بأنه سيتجاوز 10 دقائق (`ValueError: Streaming is required...`). لم يكن هذا الضابط موجودًا في 0.34.2، وكان يعطل كل استدعاء لـ Claude باستخدام `max_tokens=32768`. جرى إصلاحه بواسطة `timeout` صريح (`CLAUDE_TIMEOUT`، والافتراضي 900 ثانية)، مما يتجنب التحول إلى التدفق لاستدعاء لا يُستخدم منه سوى الرد الكامل.
    - لا تقبل `thinking_level="minimal"` سوى مجموعة فرعية من كتالوج Gemini: يدعمها `gemini-3.1-flash-lite`، بينما يرفضها `gemini-3.7-flash` و`gemini-3.1-pro-preview` بخطأ 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تسلسل احتياطي `minimal` ← `low` ← من دون أي thinking_config، على غرار آلية OpenAI الاحتياطية الموجودة أصلًا — إذ يجب ألا تتسبب معلمة تحسين مطلقًا في فشل ترجمة.
  - **تجديد النماذج الافتراضية**، وقد جرى التحقق من كل منها باستدعاء فعلي: ‏OpenAI ‏`gpt-5.5` ← **`gpt-5.6-terra`** (أقل بنسبة 60% على دفعة من 28) و`gpt-5.4-mini` ← **`gpt-5.6-luna`** (أقل بنسبة 73%)؛ وClaude ‏`claude-sonnet-4-6` ← **`claude-sonnet-5`** (أرخص وأحدث) و`claude-haiku-4-5-20251001` ← **`claude-haiku-4-5`** (معرّف قياسي بلا تاريخ)؛ وGemini ‏`gemini-3.1-pro-preview` ← **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` ← **`gemini-3.1-flash-lite`** (إصدار مستقر، وأرخص من `3.5-flash-lite`). لم يتغير Mistral، إذ يظل `mistral-large-latest` أفضل نسبة جودة إلى سعر بين الأربعة. تجدر الإشارة إلى عدم وجود أي نموذج أحدث من `gemini-3.1-pro-preview` ضمن فئة Gemini Pro — إذ لم يصدر Gemini 3.5 Pro قط، رغم الإعلان عنه في مايو 2026؛ وسلسلة 3.5/3.6/3.7 مخصصة حصريًا لـ Flash.
  - **اختبار A/B مقاس قبل تحويل Gemini**: ترجم `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. كانت البنية متطابقة تمامًا (21 قائمة، و18 كتلة تعليمات برمجية، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، في **8 ثوانٍ مقابل 48 ثانية**. وبغياب أي معيار أداء عام يقارن بين هذين النموذجين في الترجمة أو النصوص غير اللاتينية، لكان التحول مبنيًا على مجرد افتراض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفذ `block.text for block in response.content` من دون تصفية النوع. تُقحم النماذج ذات الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، تكشف `.thinking` لا `.text` — وكان من شأن الترجمة أن تتعطل بسبب `AttributeError` مبهم عند أول مقطع. أصبحت كتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` تُستبعد الآن (بقائمة سلبية، للحفاظ على تقبل نوع مجهول يحمل نصًا)، ويثير الرد الخالي من أي كتلة نصية خطأً صريحًا. ويُمرر `thinking={"type": "disabled"}` مع كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: حُذفت النماذج التي مضى تاريخ سحبها (سلسلة `magistral-*` المسحوبة في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620`، و`claude-3-7-sonnet-20250219`، و`claude-opus-4-1-20250805`، و`claude-sonnet-4-20250514`). تصحيحات الحدود: ‏Mistral ‏128K ← **256K** (جيل Large 3 / Small 4)، وGemini ‏1 000 000 ← **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` ‏200K ← **1M**، وعائلة `gpt-5.6-*` ‏400K ← **1,05M**. أُضيفت Claude 5 ‏(`claude-sonnet-5`، و`claude-opus-5`، و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وسلسلة `ministral-*`. تجدر الإشارة إلى أن هذه الحدود تظل استرشادية، إذ يضع `translate()` حدًا أقصى للتقسيم عند `min(16000, limite)`.
  - **المزوّد `--use_codex`**: المزوّد الخامس الذي يشغّل Codex CLI الرسمي (`codex exec`) في الوضع غير التفاعلي، بدلًا من استدعاء API تُحتسب تكلفته حسب الاستخدام. تُحتسب الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقًا. وهذا هو المسار الوحيد الذي توثّقه OpenAI لهذا الاستخدام: فمصفوفة التوفر حسب الخطة تدرج «Codex SDK و`codex exec` وسير العمل القابل للبرمجة» على أنه متاح في Plus/Pro/Business/Enterprise، بينما لا تصادق رموز `~/.codex/auth.json` على استدعاءات API Platform (ولا يقرأها هذا السكربت مطلقًا — إذ تظل المصادقة وتجديدها تحت إدارة CLI).
  - **ملف Codex التنفيذي قابل للتثبيت عبر pip، وليس عبر npm فقط بعد الآن**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK `openai-codex`). لذلك لم يعد مشروع Python يحتاج إلى تثبيت npm عام لاستخدام `--use_codex`. لا تُضاف الحزمة إلى `requirements.txt`: إذ يبلغ حجم الملف التنفيذي نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين من أجل مزوّد اختياري. جرى التحقق من البداية إلى النهاية: مع غياب `codex` عن `PATH`، يعثر الحل على الملف التنفيذي المضمّن في الحزمة وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: تُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. من دون هذا الإجراء الوقائي، قد يتسبب مفتاح موجود في `.env` في تحويل Codex إلى الفوترة حسب الاستخدام من دون أي إشارة ظاهرة — وهو بالضبط ما أُنشئ هذا المزوّد لتجنبه.
  - **مشكلات CLI الخفية المثبّتة باختبارات**:
    - يقرأ `codex exec` الإدخال القياسي **حتى** عند تمرير الموجّه كوسيط: ومن دون إغلاق الإدخال القياسي، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج مطلقًا (أُعيد إنتاجه: خروج 124 بعد 180 ثانية، صفر بايت). لذلك فإن `communicate(input=...)` إلزامي.
    - إن `codex` المثبّت عبر npm هو وسيط Node يقوم بـ`spawn` للملف التنفيذي Rust الحقيقي: وهذا الأخير **عملية حفيدة** لعملية Python ويظل قيد التشغيل بعد `SIGKILL` الخاص بـ`subprocess.run(timeout=)`، حيث سيواصل استهلاك الحصة. ومن هنا يأتي استخدام `Popen(start_new_session=True)` + `os.killpg`.
    - قد ينهي CLI التنفيذ بالرمز 0 رغم إصداره `turn.failed`: لذلك يُفحص خرج JSONL (`--json`) إضافةً إلى رمز الإرجاع، ويؤدي غياب الملف `-o` مع رمز 0 إلى رفع خطأ صريح بدلًا من إنتاج مقطع فارغ.
  - **التراجع التدريجي عند بلوغ حد المعدل**: لا ينفّذ CLI أي إعادة محاولة داخلية (`max_retries = 0`). يتم التصنيف بناءً على بنية حمولة JSON (`status: 429` / `error.type`)، لا على السلاسل الفرعية — إذ تظهر كلمة «الحصة» في كل من خطأ 429 القابل للاسترداد و`insufficient_quota` النهائي.
  - **إجراء وقائي لـCI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفًا. فالمصادقة عبر الاشتراك غير مخصصة لمشغّل مشترك، وتنصح OpenAI صراحةً بعدم استخدام سير العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` (`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وAPI Platform، لكن حساب ChatGPT لا يملك حق الوصول إلى كل نماذجها: تُطبّق قائمة السماح من جهة الخادم من دون تحقق محلي، ويؤدي استخدام نموذج غير معتاد إلى ظهور تحذير. ضمن خطة Plus، يتيح Luna ما بين 250 و2,000 رسالة لكل نافذة مدتها 5 ساعات، مقابل 10 إلى 100 لـSol: ويُعد `--eco` الوضع الموصى به لأي معالجة دفعية.
  - **إصلاح خلل — كان `regen_translations.sh` ينهي التنفيذ بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` خاص بـ`main()` لم يعد موجودًا عند تنفيذ المصيدة. تحت `set -u`، كان ذلك يرفع `failed_log: unbound variable` ويجعل السكربت ينهي التنفيذ بالرمز 1 رغم صحة الترجمات الـ28 — وهو ما كان سيوقف `release.sh --auto` (`set -e`) مباشرةً بعد إعادة التوليد، في أكثر المراحل كلفة. أصبح المتغير عامًا، وصارت المصيدة تتحقق من وجوده. ومن الآثار الجانبية المفيدة أن إخفاقات الترجمة الحقيقية، التي كان هذا الخطأ يحجبها سابقًا، أصبحت ظاهرة مجددًا في الملخص النهائي.
  - **`REGEN_MODEL`**: متغير بيئة جديد لـ`regen_translations.sh` يفرض نموذجًا محددًا بدل النموذج الافتراضي للمزوّد، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج المتطور من حصة الاشتراك بدل نموذج `--eco` الموجّه للأحجام الكبيرة.
  - **`regen_translations.sh`**: أصبح `REGEN_PROVIDER=codex` متاحًا عبر تفعيل صريح (ولا يُكتشف تلقائيًا مطلقًا، حتى لا تُستهلك حصة الاشتراك من دون علم المستخدم). يُجدّد الرمز مرة واحدة تسلسليًا قبل بدء التوازي — لأن تجديد Codex دوري وأحادي الاستخدام، وستؤدي المهام المتزامنة إلى إبطال جلسة `codex login` — ويُخفض عدد العمليات المتزامنة إلى 4.
  - **إعادة الهيكلة المرتبطة**: ينتقل `_dispatch_provider_call` من 8 إلى 6 معاملات عبر `_resolve_provider()` يعيد اسم المزوّد، بدلًا من تمرير قيمة منطقية رابعة عبر السلسلة بأكملها. وتظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` بالحد الأدنى.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` (48 اختبارًا) يغطي argv، والبيئة المنقّاة، وعقد منع المقدمة، والإخفاقات الصامتة، والمهلة الزمنية/killpg، والتراجع التدريجي، والفحص المسبق، وحل المزوّد، وتسلسل استدلال Gemini، وتصفية كتل Claude، واستشهادات الأخبار متعددة الفقرات. تضم الحزمة الكاملة 290 اختبارًا.
  - **التحقق الفعلي**: تؤدي ترجمة `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex إلى بنية مطابقة تمامًا للترجمات المرجعية (14 كتلة برمجية، و24 عنوانًا، و25 سطر جدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل برمجية متطابقة حرفًا بحرف، ومن دون أي بقايا لعناصر نائبة). وفي مقال صحفي حجمه 69 كيلوبايت ضمن وضع `--news`، يجتاز خرجا `gpt-5.6-luna` و`gpt-5.6-sol` كلاهما مدقق التطبيق اللاحق للغات en/ja/ar. أما الاستهلاك المقاس عبر `account/rateLimits/read` فقد ظل دون عتبة تقريب العداد (0% من نافذة الساعات الخمس) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL لإسناد الأخبار عند وجود أقواس متداخلة أو بادئة فرنسية (2026-05-11):

  - **إصلاح خلل**: كان استخراج عناوين URL للإسناد في `_protect_news_quotes` يستخدم التعبير النمطي `re.search(r"\((.+?)\)", attribution)` (التقاط كسول بين قوسين). وفي إسناد من النوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` المحيطة + `]()` الخاصة برابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة تتضمن البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائية). النتيجة: كان `_validate_news_post` يبحث عن هذه السلسلة في الخرج المترجم ويفشل دائمًا (لسببين: `)` مبتور + ترجمة "relayé par" إلى `relayed by`/`weitergeleitet von`/...). ولم يكن بوسع التسلسل الكامل low ← medium ← high ← gpt-5.5 النجاح.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — فهو يستهدف تحديدًا `](url)` الخاصة برابط Markdown، ويلتقط **عنوان URL الخالص فقط** (من دون البادئة الفرنسية أو البتر)، وهو ثابت تحافظ عليه العناصر النائبة `#URL{N}#` أثناء الترجمة. وهو متين أمام النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس محيطة
  - **الاختبارات**: اختباران جديدان في الفئة `TestNewsCitationExtraction` ضمن `test_silent_failure.py`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة المطابقة تمامًا لخلل Genspark CEO E2B المعاد إنتاجه)
    - `test_extract_attribution_url_with_french_prefix` (صيغة بديلة تتضمن `via`)
  - **فجوة في التغطية**: يتحقق `check-editorial-coverage.py` من صحة الصياغة التحريرية، لكن ليس من قابليتها للترجمة بواسطة المترجم. وقد يكون من التحسينات الممكنة (خارج نطاق v1.9.2) إضافة فحص يحاكي استخراج الإسناد في تشغيل تجريبي لاكتشاف الأنماط الخطرة قبل النشر.

- **1.9.1** إصلاح التدويل لتسمية CTA في ملاحظة علامة الترجمة (2026-05-10):

  - **إصلاح خلل**: كانت تسمية `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط العلامة أعلى الملفات المترجمة تظل **بالفرنسية** لجميع اللغات المستهدفة، بدلًا من اتباع `target_lang`. لا يراها LLM مطلقًا (إذ تُجمّع من جهة Python للحفاظ على عنوان URL ورابط المستودع)، ولذلك لم تكن مرحلة الترجمة قادرة على تداركها. كان هذا تراجعًا صامتًا منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ15 بتسمياتها المحلية. وأصبح `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` يمرران اللغة المستهدفة. مع رجوع احتياطي إلى `fr` إذا كانت اللغة غير معروفة (للأمان، ومن دون KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link` (اللغة المستهدفة `ja` ← التسمية اليابانية المتوقعة). واختباران جديدان: `test_source_link_label_localized_per_target_lang` (معلّم بسبع لغات تغطي الكتابات اللاتينية والتصويرية والأبجدية غير المصوّتة) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختبارًا في `test_translation_note_position.py` (بدلًا من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع بقيمة افتراضية `target_lang="fr"` — تواصل الجهات الخارجية التي تستدعيه برمجيًا من دون `args.target_lang` العمل من دون تعديل.
- **1.9** إصلاح الفشل الصامت + أدوات جودة متكاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + تنسيق العلامة "embed card"**:
    - خيارات CLI جديدة (إضافية، الإعدادات الافتراضية لم تتغير ← **غير كاسرة للتوافق**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): يضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - يعيد `legacy` إنتاج سلوك v1.8 بدقة (فقرة بخط عريض `**…**`) **byte-for-byte**.
        - يُصدر `marker` تعريف مرجع رابط Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`) يتبعه **اقتباس مكوّن من 3 فقرات** منظّم لعرض من نوع "GitHub repo embed card": عنوان المشروع ضمن كود مضمّن (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف يترجمه LLM، ورابط CTA (`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. يمكن استخدامه أثناء البناء بواسطة ملحق remark (راجع مدونة jls42.org ← الملحق `remark-translation-banner`).
    - **ثوابت لا تُرسل مطلقًا إلى LLM**: يُجمَّع عنوان المستودع وعنوان URL الخاص بـ GitHub في جانب Python بعد ترجمة العبارة الوصفية. لا يرى LLM مطلقًا المعرّف `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تغيير أي renderer أو حالة أحرف أو scheme.
    - **إدراج مدرك للـ frontmatter**: في وضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة الإغلاق `---`** الخاصة بـ YAML frontmatter (لضمان أمان Astro Content Collections / gray-matter). تكتشف الدالة المساعدة `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف وتحافظ على سلامته؛ كما **ترفع `RuntimeError`** عند وجود frontmatter مفتوح بلا fence إغلاق (فيظهر الملف ضمن `failed_files` بدلًا من كتابته مع ملاحظة في موضع خاطئ).
    - **قائمة سماح لتنقية النموذج**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع استخدام `unknown` احتياطيًا إذا كانت النتيجة فارغة. يتوافق ذلك مع أداة التحقق في ملحق remark الخاص بـ Astro ويحيّد المحارف التي قد تكسر تنسيق العلامة (المسافة، وعلامة الاقتباس، والقوس، والفاصلة، وغيرها).
    - **إعادة هيكلة داخلية**: `_append_translation_note` (دالة واحدة متجانسة) ← 7 دوال مساعدة نقية (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فُصل builder عن composer (يعيد builder كتلة نقية بلا فاصل، بينما يطبّق composer قيم `\n\n` وفقًا للموضع)؛ ويستخدم كود الإنتاج والدالة المساعدة للمصدر أداة التجميع نفسها ذات الفقرات الثلاث.
    - **يحافظ `_quote_lines` على الأسطر الفارغة**: يسبق كل سطر بـ `> `، ويحوّل الأسطر الفارغة إلى `>` فقط. يتيح ذلك لـ mdast رؤية ثلاث فقرات منفصلة داخل الاقتباس (العنوان / الوصف / الرابط) بدلًا من فقرة واحدة تحتوي على فواصل أسطر.
    - **`_build_translation_note_block` تكيّفي**: بحسب عدد الفقرات التي حافظ عليها LLM (3 = تنسيق بطاقة كامل، 2 = عبارة + رابط، 1 = تنسيق احتياطي). لم يعد التنسيق الاحتياطي ذي الفقرة الواحدة **يغلّف النص بـ `**...**`** عند اكتشاف رابط Markdown من النوع `](` (إذ يكون عرض `<strong>` حول الرابط هشًا).
    - **التوافق التصاعدي**: استخدام `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` في جانب `_compose_with_notes` — وتستمر كائنات Namespace التي لا تحتوي على هذه السمات (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) في العمل بلا تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور حرفيًا على مقتطف المصدر) + طبقة احتمالية (`langdetect`)
    - قائمة سماح `finish_reason` / `stop_reason`: رفع `RuntimeError` لأي حالة خارج قائمة السماح (الاقتطاع، وcontent_filter، وغيرها)
    - `max_tokens` في Claude: `4096` ← `32768` (يتجنب الاقتطاع الكامن في المقاطع بحجم 16k، مع هامش للتحويل بين أنظمة الكتابة من FR إلى JA/ZH/KO/AR/HI)
    - تقسيم مدرك للعناوين: إعطاء الأولوية لـ H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى رمز خروج غير صفري: يعيد `translate_markdown_file` حالة محددة النوع `success` / `failure` / `skipped`، وينفذ `main()` الأمر `sys.exit(1)` إذا فشل ملف واحد على الأقل (للملف الواحد والمعالجة الدفعية)
    - حارس للمحتوى الفارغ لدى جميع المزوّدين، وفحص منطقي لنسبة المصدر إلى المخرجات (≥ 500 محرف، وأقل من 5% = رفض)، والتحقق من العناصر النائبة للكود (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، وإعادة محاولة `BadRequestError` دون `reasoning_effort`
    - إضافة التبعية `langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("نوع EurekAI المتكامل"، 14 hook):
    - Pre-commit: ‏ruff (فحص+تنسيق)، وshellcheck، وprettier ‏(md/yaml/json)، وdetect-secrets (حماية 4 مفاتيح API)، وLizard ‏(CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، ونهاية الملف، والملفات الكبيرة، وshebangs، وغيرها)
    - Pre-push: ‏mypy (وضع متساهل تدريجي)، وOpengrep SAST ‏(translate.py + scripts/)، وpip-audit (وضع تقارير أولي)، وunittest discover ‏(tests/ + scripts/tests/)
    - أغلفة محلية في `scripts/` تستخدم `./venv/bin/python`
    - `scripts/audit_verdict.py`: محلل JSON لـ pip-audit مع 11 اختبارًا باستخدام unittest، وهو نقل إلى Python لمحلل jls42-astro
    - إصلاح 7 مخالفات أولية في ruff: ‏B904 ‏(raise from) ×2، وB007 ‏(unused dirs)، وC408 ‏(dict literal)، وC419 ‏(list-comp)، وSIM105 ‏(contextlib.suppress)، وSIM110 ‏(any())
    - يستبعد Lizard مؤقتًا `translate.py` (4 دوال بقيم CCN بين 21 و47، وإعادة الهيكلة مخطط لها) — مع بوابة صارمة على scripts/
  - **SonarCloud + تغطية شاملة**:
    - سير عمل GitHub Actions في `SonarCloud` ‏(sonarcloud.yml + sonar-project.properties): تحليل مع كل push وpull-request، وتغطية عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى README ‏(Quality Gate، وتقييمات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code)
    - `tests/test_silent_failure.py` ‏(`unittest` من stdlib): يغطي الحلقات الست في سلسلة أخطاء الفشل الصامت
    - `tests/test_orchestration.py` (+79 اختبارًا): يغطي طبقة التنسيق في `translate.py` ‏(`_resolve_*_filename`، `_existing_translation_exists`، `_record_translation_status`، `_write_output_file`، `translate_directory`، `_validate_input_paths`، `_init_*_client`، `_select_provider_client`، `_normalize_collapsed_markdown`، `_cleanup_source_flag`، وحالات TypeError + BadRequestError الاحتياطية في `_validate_news_flags_*` و`_openai_create_with_fallback`، وتنسيق prompt لسلسلة o1، وفروع الإرجاع المبكر في `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` ‏(stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **التغطية في الكود الجديد**: 75.5% ← نحو 98% ‏(translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما فيها اختبارات E2E ‏`marker+top|bottom|both` و`legacy+top|bottom|both`)، وإضافة البادئة متعددة الأسطر، والتوافق الرجعي byte-for-byte (قيمة golden حرفية)، وأداة التنقية، وتقسيم frontmatter (بما في ذلك رفع خطأ عند عدم إغلاق fence)، وتنسيق الفقرات الثلاث، والتنسيق الاحتياطي ذي الفقرتين، وحارس الفقرة الواحدة + رابط Markdown، وحاجز حماية بالغ الأهمية `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان وURL لا يُرسلان مطلقًا إلى LLM. **نجح 190 اختبارًا**، بلا أي تراجع.
  - التوثيق: `README.md` ‏(بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` ‏(سير عمل pre-commit + مراقبة CI مفصلة)، وإعادة توليد 28 ترجمة
- **1.8** وضع `--news` + ترقية نماذج 2026 ‏(2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - جودة OpenAI: ‏`gpt-5` ← `gpt-5.4`
    - OpenAI الاقتصادي: `gpt-5-mini` ← `gpt-5.4-mini`
    - جودة Gemini: ‏`gemini-3-pro-preview` ← `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` ‏(400k) و`gemini-3.1-pro-preview` ‏(1M)
  - وضع `--news` الأولي: حماية الاقتباسات الإنجليزية بعناصر نائبة `#NEWSQUOTE\d+#`، وربط `LANG_FLAGS` ‏(15 لغة)، وإدارة العلامات حسب اللغة المستهدفة
  - التحقق من العناصر النائبة للأخبار قبل الاستعادة (تراجع: كان حذف LLM للعنصر النائب ينتج بصمت مخرجات بلا اقتباس)
  - جعل السكربت `regen_translations.sh` قابلًا للنقل (مسارات مطلقة، بلا اعتماد على pwd)
  - إضافة رابط الفرنسية إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة
- **1.7** الميزات الجديدة:
  - خيار `--keep_filename` للاحتفاظ باسم الملف الأصلي أثناء الترجمة
  - دعم الملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على الكود المضمّن**: أصبحت علامات backticks ‏(`` `...` ``) محمية أثناء الترجمة
  - تحسين prompt النظام:
    - معالجة أفضل لعلامات الاقتباس في YAML frontmatter
    - حماية متغيرات القالب `{variable}`
    - منع ملاحظات المترجم غير المطلوبة
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** الميزات الجديدة:
  - دعم Google Gemini API للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI: ‏`gpt-5` (الجودة)، و`gpt-5-mini` (الاقتصادي)
    - Claude: ‏`claude-sonnet-4-5` (الجودة)، و`claude-haiku-4-5` (الاقتصادي)
    - Gemini: ‏`gemini-3-pro-preview` (الجودة)، و`gemini-3-flash-preview` (الاقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف واحد (`--file`) دون استعراض مجلد
  - نمط تسمية جديد ومبسّط: `{base}-{lang}.md`
  - خيار `--include_model` للاحتفاظ بالتنسيق القديم المتضمن اسم النموذج
  - دعم النماذج غير المدرجة مع حد افتراضي للرموز (128k)
  - ترجمة README إلى 14 لغة
- **1.5** التحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين prompts الترجمة:**
    - أُثريت prompts الخاصة بالترجمات المباشرة وملاحظات الترجمة لزيادة الوضوح والكفاءة، بما يشمل تعليمات مفصلة حول الحفاظ على البيانات الوصفية وعناصر التنسيق المحددة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم imports لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص وإدارة كتل الكود للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات المخرجات:**
    - عكس ترتيب النموذج واللغة في اسم ملفات المخرجات (مثلًا، `f"{base}-{args.target_lang}-{args.model}.md"`)، ما يسهّل تنظيم الترجمات والبحث عنها.
  - **تحسينات متنوعة:**
    - تنظيف الكود بحذف الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكربت وسهولة قراءته.
- **1.4** الميزات الجديدة:
  - دعم Claude API من Anthropic للترجمة
  - تحسين prompts لزيادة الوضوح والكفاءة
  - تعديلات طفيفة لتحسين قابلية صيانة الكود
- **1.3** التحسينات والميزات الجديدة:
  - تحسين إدارة كتل الكود
  - تحسين إدارة ملفات المخرجات
  - تحسين اكتشاف الملفات الموجودة
  - خيار `--force` لفرض الترجمة
  - عكس ترتيب النموذج واللغة في اسم ملف المخرجات
- **1.2** إصلاح سجل التغييرات
- **1.1** إضافة دعم Mistral AI API
- **1.0** الإصدار الأولي - دعم OpenAI API

**مقال مترجم من الفرنسية إلى العربية باستخدام gpt-5.6-sol.**
