### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.10.0** مزوّد `--use_codex` (حصة اشتراك ChatGPT)، وتحديث حزم SDK والنماذج، وإصلاح اقتباسات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — حاجزا أمان أضافهما طلب الدمج من دون تطبيقهما في كل المواضع**:

    - **كان فحص Codex المسبق يمرّر `.env` كاملًا إلى الملف التنفيذي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: ترث العملية الفرعية `os.environ` كاملًا، ومن ثم كامل `.env` الذي حمّله `load_dotenv`. أظهر القياس باستخدام ملف تنفيذي زائف مزوّد بأدوات رصد أن **سبعة أسرار** كانت تصل إلى الفحص المسبق — مفاتيح المزوّدين الستة بالإضافة إلى `GITHUB_TOKEN` — مقابل **صفر** لدى نظيره `_grok_preflight`، الذي كان يمرّر `env=_grok_env()` فعلًا. كان التناقض داخل طلب الدمج نفسه: إذ يوجد `_strip_secret_env` تحديدًا للحفاظ على هذا الثابت، على بُعد بضعة أسطر. جرى استخراج `_codex_env_base()` ومشاركته بين المسارين؛ والقياس بعد التصحيح: 0 أسرار في كلا الجانبين.
    - **لم تشمل خاصية «إغلاق `--deny` عند الفشل» الصيغة المستخدمة.** برّر التعليق احتواء Grok بالكامل بأن القاعدة ذات البادئة المجهولة تمنع بدء التشغيل. وبالقياس على `grok 1.0.13`، لا يوجد هذا التحقق **إلا للصيغة ذات الأقواس**: يمنع `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل («بادئة أداة مجهولة»)، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. لكن `GROK_DENY_RULES` لم يستخدم سوى أسماء مجردة — ولذلك كان من شأن إعادة تسمية أداة لدى xAI أن تزيل طبقة الاحتواء الوحيدة المقاسة بلا أي إشارة، على جهاز لا ينطبق عليه أصلًا صندوق حماية نظام التشغيل. انتقلت القواعد الثماني المسماة إلى `Prefix(*)`، وتُحقّق من كل واحدة بوصفها بادئة معروفة لدى CLI؛ وتبقى قاعدة الالتقاط الشامل `*` بصيغتها الحرفية، وهي الوحيدة المقبولة. يمنع اختبارٌ العودة إلى الصيغة غير المتحقق منها.
    - **وتحققت السلامة فيما عدا ذلك**: لا يوجد حقن أوامر (تُستخدم صيغة القائمة دائمًا، ولا يُستخدم `shell=True` مطلقًا، ويُمرّر محتوى المستند عبر الإدخال القياسي أو `--prompt-file`)، ولا توجد إزالة تسلسل غير آمنة (`json.loads` فقط، مع حارس نوع)، ولم يُعثر على تجاوز لإصلاح اجتياز المسار باستخدام سبع حمولات، ويطبّق CLI فعلًا `--deny '*'` (رُصد `DENY_ENFORCED` عند القراءة من خارج دليل العمل).
    - كان فحص الحداثة المضاف أعلاه يتجاوز مبدأه بالمناسبة: إذ كان يتخطى بصمت أي حزمة يفشل طلبها إلى PyPI، ويبقى الحاجز أخضر. وهو يحصي الآن الحزم التي جرت مقارنتها فعلًا ويفشل إذا كانت التغطية غير مكتملة.

  - **تحديث الاعتماديات، وإضافة شبكتي أمان لمنع تكرار التأخر**:

    - **كان التأخر حقيقيًا ومزمنًا**: `openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر عامين في مخزن الشهادات الجذرية الذي يتحقق من TLS لجميع استدعاءات المزوّدين. حُدد السبب: **لم يكن `.github/dependabot.yml` موجودًا**. من دون هذا الملف، لا يفعّل GitHub سوى _التحديثات الأمنية_، ولا يقترح Dependabot طلب دمج إلا لاعتمادية مستهدفة بثغرة CVE — وهذا يفسر تحديثه `urllib3` و`idna` مع ترك حزمتَي SDK تنحرفان بإصدار رئيسي كامل.
    - **يتعايش الإصداران الرئيسيان بلا تعارض**، خلافًا لما أوحى به استنتاج سابق: ينتقل `openai` 3.x و`anthropic` 1.x إلى **`httpx2`**، بينما يبقى `mistralai` و`google-genai` على `httpx<1`، لكنهما توزيعتان منفصلتان. جرى التحقق بتثبيت فعلي، ثم عبر **اختبار مسارات المزوّدين السبعة من طرف إلى طرف** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الشيفرة المضمنة والروابط في كل مخرج. كان «تجنب مكدسي HTTP» تفضيلًا لا عائقًا، وقد حسم القياس المسألة.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: كانت `google-auth` و`cryptography` ومكدس `opentelemetry` مثبتة في بيئة العمل الافتراضية من دون التصريح بها قط — ولذلك لم يكن تثبيت جديد يعيد إنتاج ما جرى اختباره. وعلى العكس، كانت `tokenizers` و`huggingface-hub` و`PyYAML` مدرجة فيه من دون أن تُستورد أو يطلبها أي شيء، وهي بقايا من `mistralai` 1.x. أُعيد توليد الملف بوصفه الإغلاق الكامل لبيئة افتراضية مبنية انطلاقًا من الاعتماديات المباشرة وحدها. لا يُبلغ `pip-audit` عن أي ثغرة معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** (جديد) يفعّل تحديثات الإصدارات الأسبوعية لكل من pip وgithub-actions. تُجمع الإصدارات الثانوية والتصحيحية في طلب دمج واحد — إذ ينتهي الأمر بتجاهل تحديث تصحيحي واحد لكل طلب دمج، والضجيج عدو التحديث؛ أما **الإصدارات الرئيسية فمنفصلة**، ويتطلب كل منها تحققًا عبر استدعاء فعلي.
    - **`scripts/check-deps-fresh.sh`** (جديد، وموصول بالحاجز) يجعل التأخر ظاهرًا في نتيجة المشروع: يقترح Dependabot التحديثات لكنه لا يضمنها، وقد تتراكم طلبات الدمج الخاصة به. تأخر إصدار رئيسي ← فشل؛ إصدار ثانوي ← تحذير، لأن الحاجز الذي يظل أحمر دائمًا ينتهي به الأمر إلى التجاهل؛ تعذر الوصول إلى PyPI ← تخطٍّ صريح محليًا و**إغلاق عند الفشل في CI**، لأن الفحص الذي لم يُنفذ لا يُعد نجاحًا. جرى التحقق في الاتجاهين: فهو يلتقط الحالة الدقيقة قبل التصحيح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير عند وجود إصدار ثانوي.

  - **إصلاحات ناتجة عن مراجعة طلب الدمج هذا** — فحص خمسة وكلاء مراجعة الفروق بدقة؛ وقد **أُعيد إنتاج** جميع النقاط أدناه **بالقياس** قبل التصحيح، وكان اثنان منها تراجعين أُدخلا أعلاه في الإصدار نفسه.

    - **إصلاح تراجع — كان `_NEWS_CITATION_REGEX` يعاني من تراجع خلفي أُسّي.** أدخل إصلاح الفقرات المتعددة `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: فتوزيع المسافات بين `[ \t]+` و`.*` ملتبس، ويتضاعف هذا الالتباس من تكرار إلى آخر. جرى القياس على أسطر `>   texte` — وهي إزاحة Markdown مشروعة تمامًا — لا تطابق النمط: **2 589 ms لأربعة عشر سطرًا** مقابل 0,04 ms بعد التصحيح، بمعامل يقارب 9 لكل سطر مضاف. في وضع `--news`، كان اقتباس كتلي طويل وغير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب يمكن تحديده. يستهلك التكرار الآن السطر كاملًا دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يترك سوى طريقة واحدة للمطابقة في كل تكرار. جرى التحقق على المجموعة الفعلية المكونة من 231 مقالًا: **صفر اختلافات** في الالتقاط، والاقتباسات الـ423 نفسها، مع بقاء المتون الأربعة عشر متعددة الفقرات موسعة.
    - **كان استخدام علمي مزوّد في آن واحد يؤدي إلى فوترة بحسب الاستخدام بصمت.** كان `--use_codex --use_mistral` مقبولًا؛ ويختبر `_select_provider_client` مزوّد Mistral أولًا، فيما يمنح `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. وهكذا كان المستخدم يطلب استخدام حصة اشتراكه ويحصل بدلًا من ذلك على فوترة بحسب الاستخدام، من دون أدنى تحذير: وهذا هو تحديدًا نمط الفشل الذي وُجد `--use_codex` لمنعه. تمر أعلام المزوّدين الستة الآن عبر `add_mutually_exclusive_group`. **تغيير سلوكي**: يفشل الآن سطر أوامر يجمع مزوّدين عند `argument --use_mistral: not allowed with argument --use_codex`، بعدما كان يُقبل بصمت.
    - **كان حاجز نهاية العمل يتحول إلى الأخضر عند تعطل مجساته.** اتبعت أربعة من فحوص `scripts/check-release-ready.sh` الثلاثة عشر نمط «التقاط stdout والاستنتاج إذا كان فارغًا» من دون مراجعة رمز الخروج قط: كان الاستثناء (ملف أُعيدت تسميته، `FileNotFoundError`) يكتب إلى stderr ويترك stdout فارغًا، فيستنتج الفحص أنه «لا شيء يستدعي الإبلاغ». وهكذا أُعيد إنتاج فخ «`exit 0` لا يثبت شيئًا» داخل البرنامج النصي المكتوب لمنعه. يفرض المساعد `probe()` الآن رمز خروج صفريًا **وكذلك** علامة نهاية، وترفض المجسات الاستنتاج من مجموعة علامات فارغة — لأن التوكيد على مجموعة فارغة صحيح دائمًا. والدليل: جعلت إضافة المجموعة الحصرية أعلاه أعلام المزوّدين تمر عبر كائن `*_group` لم يعد التعبير النمطي القديم `parser\.add_argument\(` يطابقه؛ فخرجت **ستة أعلام من أصل واحد وعشرين** من النطاق بصمت، مع بقاء الحاجز أخضر.
    - **كان فحص الأسرار يفوّت أربعة مزوّدين من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: فكان `sk-proj-…` (تنسيق OpenAI الحالي) و`sk-ant-api03-…` ينقطعان عند الشرطة الثانية، ولم يكن `AIza…` مشمولًا. وُسع النمط، واستُبعد `.secrets.baseline` من الفحص. وكان الحارس `.env` يستعلم أيضًا عبر `git diff --cached`، الذي لا يرى سوى الفهرس: لذلك لم يكن `.env` **المُودع بالفعل** — وهي أسوأ حالة — يظهر فيه أبدًا. وهو يستعلم الآن عبر `git ls-files`.
    - **لم تكن «تهيئة الرمز المميز» في Codex تهيئة فعلًا.** أظهر القياس أن `codex login status` لا يلمس `~/.codex/auth.json` (ظل وقت التعديل والحجم من دون تغيير)، وتقول مساعدته «إظهار حالة تسجيل الدخول». ومع ذلك، كان التعليق يدّعي تحديث الرمز المميز «مرة واحدة، تسلسليًا»، وبالتالي تحييد خطر التحديثات المتزامنة لرمز مميز دوّار أحادي الاستخدام. لم تكن الحماية المعلنة موجودة؛ وأصبح التعليق يصف الآن ما تفعله الشيفرة، فيما يظل التدبير الحقيقي هو `max_jobs=4`. كما يحترم الفحص `CODEX_BIN` الذي كان يتجاهله — إذ كان جهاز لا يحتوي `codex` في `PATH` يفشل برسالة «غير مصادق عليه»، وهو تشخيص مضلل.
    - **كان `.env` يُحمّل في قشرة فرعية.** يُستدعى `detect_provider` داخل استبدال أمر، ولذلك لم تكن صادراته تنتقل إلى الأعلى: فكان `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` المعرّف في `.env` يظل غير مرئي للقراءات المنفذة في `main()`، التي تستنتج «تعذر العثور على ملف Grok التنفيذي» رغم صحة الإعداد.
    - **كان التزامن يتجاوز الحد المعلن بنسبة 50 %.** وُضع الحارس بعد تشغيل زوج README/CHANGELOG: فبلغت الذروة المقاسة **3 عند `max_jobs=2`**. وفي Grok، الذي يتقاسم حصته الأسبوعية مع Chat وImagine وVoice ولا يمكن قياسها، لم يكن البرنامج النصي يلتزم إذن بالحد الذي فرضه على نفسه. أما العدد النهائي فكان يُعرض من دون مقارنته قط بالعدد 28 — ولذلك كان غياب ملف يمر من دون اكتشاف.
    - **عقد مخرجات Grok: أصبح غياب `stopReason` فشلًا.** كانت الشيفرة تطبق «`end_turn` **أو غائب**» في موضع يشترط فيه العقد المعلن `end_turn`. كانت حمولة بلا هذا الحقل — أو أُعيدت تسمية الحقل فيها بتحديث CLI — تحول الحارس إلى عملية عديمة الأثر بصمت. كذلك لم يعد `max_turn_requests` مصنفًا ضمن بلوغ حد المعدل (فقد نفدت ميزانية الجولات: تعيد المحاولة النتيجة نفسها، مقابل انتظار 90 s)، وأُزيل `quota` من علامات بلوغ حد المعدل — للسبب الذي كان توثيق `_codex_is_rate_limited` يذكره بالفعل من دون أن يطبقه Grok.
    - **أصبحت سلسلة Gemini تُحفظ مؤقتًا بحسب النموذج.** كانت تبدأ مجددًا من `minimal` عند كل مقطع، مع أن النموذج الافتراضي يرفضه: فكان المسار الاسمي يدفع تكلفة رحلة ذهاب وإياب باستجابة 400 لكل مقطع، ويعيد طباعة التحذير نفسه. والتحذير الذي يتكرر مئات المرات لا يعود مقروءًا — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مكتوبة صراحةً لـCodex وتوجه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق أساس العمليات الفرعية يعمم مفهوم «shim» على واجهتَي CLI، مع أن ملف Grok التنفيذي هو ملف ELF أصلي (والتبرير الصحيح هو «وكيل يُنشئ عملياته الفرعية الخاصة»)؛ وصُنّفت اثنتا عشرة ملاحظة SAST على `subprocess` على أنها `# nosec` / `# nosemgrep` مع تبرير، إذ تجعل صيغة القائمة من دون `shell=True` الحقن مستحيلًا، ولا يمر محتوى المستند عبر argv قط.
    - **لم يعد أي سر يدخل العمليات الفرعية الوكيلة.** لم تكن قوائم المنع الاسمية تحمي سوى ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). أظهر القياس أن **سبعة أسرار أخرى** كانت تدخل مع ذلك إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح CLI الآخر، و`OPENAI_BASE_URL` الذي ليس سرًا لكنه يعيد توجيه حركة البيانات. إلا أن واجهتَي CLI هاتين **وكيلان**، ويعمل وكيل Grok من دون صندوق حماية لنظام التشغيل قابل للتطبيق على كثير من أجهزة Linux. باتت التصفية تتم الآن **بحسب نمط الاسم** (`API_KEY` و`_TOKEN` و`SECRET` و`PASSWORD` و`CREDENTIALS`)، لا بقائمة اسمية، وهو ما يشمل أيضًا المتغيرات التي يضيفها المستخدم في `.env` من دون أن تعرفها هذه الشيفرة. لا تحتاج واجهتا CLI إلى أي منها: فالمصادقة موجودة في `~/.codex` و`~/.grok`، ولا توجد أبدًا في البيئة — وقد جرى التحقق عبر **ترجمة فعلية مكتملة** باستخدام كل من المزوّدين مع البيئة المحصنة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يثبّت حصرية أعلام المزوّدين، وعقد `stopReason`، وخطية التعبير النمطي للأخبار، ورسالة الرفض في CI، والحفظ المؤقت في Gemini، وغياب أي سر من بيئة العمليات الفرعية. وهذا التوكيد الأخير **عام** — إذ يفشل عند مفتاح لا تسميه أي قائمة — بينما كان اختبار التنقية القائم مرآة لثابته، ولم يكن قادرًا على اكتشاف أي شيء سوى تعطل حلقته الخاصة. بلغ مجموع الحزمة **311 اختبارًا**.
  - **موفّران جديدان لـ Grok**: ‏`--use_grok` ‏(واجهة API لـ xAI، المفتاح `XAI_API_KEY`، والفوترة حسب الاستخدام) و`--use_grok_cli` ‏(واجهة CLI الرسمية لـ Grok Build، يُخصم استخدامها من اشتراك Grok — بالمبدأ نفسه الذي يتبعه `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن نقطة نهاية xAI متوافقة مع OpenAI، يُعاد استخدام العميل و`_call_openai` كما هما، ولا يتغير سوى `base_url`. لم يلزم سوى تعديل واحد يستفيد منه الجميع: يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي تُصدرها xAI حيث تُصدر OpenAI ‏`stop`. النماذج: `grok-4.6` ‏(الجودة) و`grok-4.3` ‏(الاقتصادي). تجدر الإشارة إلى أن خيار Grok الاقتصادي يظل الأغلى في المستودع — ‏$1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ `mistral-small-latest`: يُختار هذا الموفّر لتنوّع النماذج، لا للسعر.
    - **وضع CLI**: صُمّم على غرار Codex، مع أربعة اختلافات فرضها الواقع — يُمرَّر المحث عبر ملف (`--prompt-file`، إذ لا تقرأ واجهة CLI من stdin وسيكون أي مقطع في argv ظاهرًا في `ps`)، ويكون الخرج كائن JSON واحدًا على stdout ‏(ليس JSONL ولا ملف `-o`)، ولا يتيح الاشتراك سوى `grok-4.6` و`grok-4.5`، كما يتعذر تطبيق sandbox ‏(انظر أدناه). جرى توحيد تشغيل العمليات الفرعية مع Codex في `_codex_run_process`، من دون المساس ببقية موفّر Codex الذي سبق اختباره.
    - **`exit 0` لا يثبت شيئًا، وهذا مُقاس**: عندما لا يكون المستخدم موثّقًا، تكتب واجهة CLI ‏`{"type":"error","message":"Not signed in."}` على **stdout** مع رمز خروج **0**. ويتصرف رفض الطلب أو تجاوز عدد الجولات بالطريقة نفسها. لذلك يتطلب عقد الخرج تحقق أربعة شروط في آن واحد: رمز خروج 0، وعدم وجود حمولة خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع الفحص المسبق المنطق نفسه: ينتهي `grok models` بالرمز 0 حتى عند انقطاع تسجيل الدخول، ولا يمكن الاستنتاج إلا من وجود «غير موثّق» في stdout.
    - **العزل: تفاوت مقصود وموثّق.** بينما يعمل Codex في `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـ Grok على كثير من أجهزة Linux الحديثة لسببين مستقلين على مستوى النظام لا يمكن تجاوزهما من دون `sudo`: يمنع AppArmor مساحات أسماء المستخدمين غير المميّزة منذ Ubuntu 24.04 ‏(`bwrap: setting up uid map: Permission denied`، وأُعيد إنتاج المشكلة خارج Grok)، وتفشل قائمة حظر مقابس وقت تشغيل الحاويات عندما يكون `/run/podman` في `0700` ‏(لا يتدارك المحلّل سوى `ErrorKind::NotFound`، بينما يصبح EACCES خطأً قاتلًا). الفخ الأساسي: ملف تعريف **مدمج** يتعذر تطبيقه يؤدي إلى التشغيل **بلا عزل وبصمت**. لذلك لا يطلب السكربت أي ملف تعريف افتراضيًا ولا يتراجع بصمت مطلقًا — بل يعرض تحذيرًا على stderr. تعتمد الحماية على قواعد `--deny` الخاصة بواجهة CLI، بما فيها قاعدة الالتقاط الشامل `*`، وهي طبقة الحماية الوحيدة التي ثبت بالقياس أنها _تغلق عند الفشل_ (إذ تؤدي قاعدة ذات بادئة مجهولة إلى رفض التشغيل). يتيح `GROK_TRANSLATE_SANDBOX=read-only` فرض استخدامها، وعندئذ يفشل التشغيل إن تعذر على الجهاز الالتزام بها.
    - **ضوابط الحماية**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (فوجود مفتاح سيحوّلها إلى الفوترة حسب الاستخدام؛ كما أن وراثة `GROK_SANDBOX` ستفرض ملف تعريف غير قابل للتطبيق مع رسالة مضللة)، وتُعطّل مفاتيح MCP والخطافات والمهارات والوكلاء، مع `--disable-web-search` و`--no-subagents` و`--no-plan`، ودليل عمل مؤقت، ورفض التشغيل في CI، ومهلة تقتل مجموعة العمليات، وتراجع تدريجي عند بلوغ حد المعدل. ضُبط `--max-turns` على 6 لا على 1: يُزاد العداد بعد جولة الأدوات، ولذلك كانت القيمة 1 ستقتطع الخرج.
    - **الحصة**: مخزون Grok أسبوعي و**مشترك مع Chat وImagine وVoice**، ولا يعرضه أي أمر — بخلاف Codex الذي يتيح `account/rateLimits/read` فيه قياس الاستهلاك. لذلك يحد `regen_translations.sh` التزامن عند 2 ويعرض تحذيرًا صريحًا.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` ‏(24 اختبارًا). تضم المجموعة الكاملة **290 اختبارًا**.
  - **إصلاح خلل — لم تكن اقتباسات EN متعددة الفقرات محمية إلا جزئيًا (في وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل بوصفه متنًا للاقتباس سوى سلسلة من أسطر `>` **المتتالية**. ما إن يمتد اقتباس عبر عدة فقرات (تفصل بينها سطر `>` فارغ)، حتى كانت الفقرة الأخيرة وحدها تُلتقط وتُستبدل بالعنصر النائب، بينما تُرسل الفقرات السابقة إلى LLM وتعود مترجمة — وهو النقيض التام للضمان الذي وُجد `--news` من أجله. يقبل التكرار الآن أسطر `>` الداخلية الفارغة وأصبح غير جشع، لكي يتوقف عند `>` الفارغ الذي يسبق السطر المائل، لا عند أول سطر فارغ يصادفه.
    - **النطاق المقاس** على مجموعة فعلية من 198 مقالة: تأثر 11 اقتباسًا من أصل 419. لا توجد أي انتكاسة — يلتقط التعبير النمطي الجديد العدد نفسه تمامًا من الاقتباسات، ولا تتسع سوى المتون متعددة الفقرات (408 متون متطابقة، و11 ممتدة)، ولا يزال من المتعذر إدراج سطر الإسناد `> — …` ضمن المتن (مع الإبقاء على الاستباق).
    - **إثبات شامل من البداية إلى النهاية** على مقالة بحجم 69 كيلوبايت تُرجمت إلى ja/ar: أصبحت الفقرة الأولى من أحد الاقتباسات، التي كانت تُعرض سابقًا بصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم كذلك إلى العربية، تبقى الآن `> GLM-5.3 is now open-weight.`. وعاد عدد أسطر الاقتباس بالإنجليزية من 9 إلى 10، وهو عددها في المصدر.
    - تجدر الإشارة إلى أن أدوات التحقق اللاحقة لم تكتشف هذا العيب، لأنها تتحقق من وجود الاقتباس من دون التأكد من اكتماله.
  - **توفير مُقاس لدى الموفّر الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ اسم النموذج بـ `gpt-5`، بما في ذلك عند استخدام `--eco`. قياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: `medium` ← ‏45 رمز استدلال و65 رمز خرج؛ `none` ← ‏0 و14. لا يضيف الاستدلال شيئًا إلى الترجمة، ومع ذلك كانت تكلفته تُدفع لكل مقطع من كل ملف. أصبحت القيمة الافتراضية `none` في `--eco`، وتظل `medium` في الحالات الأخرى؛ وتبقى للقيمة الصريحة الممررة عبر CLI الأولوية. يقبل `--reasoning_effort` الآن `none` و`xhigh` إضافةً إلى `low`/`medium`/`high` ‏(لا تقبل جميع النماذج كل هذه القيم: فمثلًا يرفض `gpt-5.4-mini` القيمة `minimal` — وتغطي إعادة المحاولة الحالية من دون المعلّمة هذه الحالة).
  - **تحديث حزم SDK وترحيل Gemini**: استُبدل `google-generativeai` ‏(انتهى دعمه في 2025-11-30 وأُرشف مستودعه) بحزمة SDK الموحّدة **`google-genai`** — ‏`genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير محث النظام في `system_instruction` بدلًا من دمجه مع المقطع. انتقل `mistralai` إلى **2.9.4** ‏(وأصبح الاستيراد `from mistralai.client import Mistral`؛ أما القديم فيطلق `ImportError`، وقد جرى التحقق من ذلك في wheel)، و`anthropic` إلى **0.125.0**، و`openai` إلى **2.54.0** — وهي آخر الإصدارات قبل الانتقال إلى `httpx2`، لتجنب تشغيل حزمتَي HTTP معًا في venv. وبناءً على ذلك، أُتيح `httpx` 0.28.1 و`pydantic` 2.13.5.
  - **انتكاسان اكتشفتهما اختبارات فعلية، لا الوثائق**:
    - يرفض `anthropic` بالإصدار ≥ 1.0 من جهة العميل استدعاءً غير متدفق تشير قيمة `max_tokens` فيه إلى مدة تتجاوز 10 دقائق (`ValueError: Streaming is required...`). لم يكن هذا الضابط موجودًا في 0.34.2، وكان يعطّل كل استدعاء لـ Claude باستخدام `max_tokens=32768`. أُصلح ذلك عبر `timeout` صريح (`CLAUDE_TIMEOUT`، بقيمة افتراضية 900 ثانية)، ما يتجنب الانتقال إلى التدفق في استدعاء لا يُستخدم منه سوى الرد الكامل.
    - لا يقبل `thinking_level="minimal"` سوى جزء من كتالوج Gemini: يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` بخطأ 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تسلسل `minimal` ← ‏`low` ← من دون thinking_config، على غرار آلية OpenAI الاحتياطية الموجودة مسبقًا — يجب ألا تؤدي معلّمة تحسين مطلقًا إلى فشل ترجمة.
  - **تجديد النماذج الافتراضية**، وقد تم التحقق من كل منها عبر استدعاء فعلي: OpenAI ‏`gpt-5.5` ← **`gpt-5.6-terra`** ‏(أقل بنسبة 60٪ على دفعة من 28) و`gpt-5.4-mini` ← **`gpt-5.6-luna`** ‏(أقل بنسبة 73٪)؛ Claude ‏`claude-sonnet-4-6` ← **`claude-sonnet-5`** ‏(أرخص وأحدث) و`claude-haiku-4-5-20251001` ← **`claude-haiku-4-5`** ‏(المعرّف القياسي من دون تاريخ)؛ Gemini ‏`gemini-3.1-pro-preview` ← **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` ← **`gemini-3.1-flash-lite`** ‏(إصدار مستقر وأرخص من `3.5-flash-lite`). بقي Mistral بلا تغيير، إذ يظل `mistral-large-latest` أفضل النماذج الأربعة من حيث نسبة الجودة إلى السعر. تجدر الإشارة إلى عدم وجود أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — فنموذج Gemini 3.5 Pro، المُعلن عنه في مايو 2026، لم يصدر قط؛ وسلسلة 3.5/3.6/3.7 مخصصة بالكامل لفئة Flash.
  - **اختبار A/B مُقاس قبل تحويل Gemini**: ترجم `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. كانت البنية متطابقة تمامًا (21 قائمة، و18 كتلة تعليمات برمجية، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، خلال **8 ثوانٍ مقابل 48 ثانية**. ونظرًا إلى عدم وجود أي معيار أداء عام يقارن هذين النموذجين في الترجمة أو النصوص غير اللاتينية، لكان التحويل قائمًا على مجرد افتراض لولا هذا الاختبار.
  - **تصفية كتل رد Claude**: كان `_call_claude` ينفذ `block.text for block in response.content` من دون تصفية النوع. تُدرج النماذج ذات الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، تكشف `.thinking` لا `.text` — وكان من شأن ذلك تعطيل الترجمة بسبب `AttributeError` مبهم منذ المقطع الأول. باتت كتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` تُستبعد الآن (بقائمة سلبية، حفاظًا على تقبّل نوع مجهول يحمل نصًا)، ويؤدي الرد الخالي تمامًا من أي كتلة نصية إلى خطأ صريح. يُمرَّر `thinking={"type": "disabled"}` في كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: حُذفت النماذج التي انقضى تاريخ سحبها (سلسلة `magistral-*` المسحوبة في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620`، و`claude-3-7-sonnet-20250219`، و`claude-opus-4-1-20250805`، و`claude-sonnet-4-20250514`). تصحيحات الحدود: Mistral ‏128K ← **256K** ‏(جيل Large 3 / Small 4)، وGemini ‏1 000 000 ← **1 048 576** ‏(حد الإدخال الفعلي)، و`claude-opus-4-5` ‏200K ← **1M**، وعائلة `gpt-5.6-*` ‏400K ← **1.05M**. أُضيفت Claude 5 ‏(`claude-sonnet-5` و`claude-opus-5` و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وسلسلة `ministral-*`. تجدر الإشارة إلى أن هذه الحدود تظل إرشادية، إذ يحد `translate()` التقسيم عند `min(16000, limite)`.
  - **المزوّد `--use_codex`**: مزوّد خامس يشغّل Codex CLI الرسمي (`codex exec`) في الوضع غير التفاعلي، بدلًا من استدعاء API تُحتسب تكلفتها حسب الاستخدام. تُخصم الترجمة من حصة اشتراك ChatGPT المدفوع مسبقًا. وهذا هو المسار الوحيد الذي توثّقه OpenAI لهذا الاستخدام: إذ تدرج مصفوفة التوفّر حسب الخطة «Codex SDK و`codex exec` ومسارات العمل القابلة للبرمجة النصية» على أنها متاحة في Plus/Pro/Business/Enterprise، بينما لا تُصادق رموز `~/.codex/auth.json` على استدعاءات API Platform (ولا يقرأها هذا السكربت مطلقًا — إذ تظل المصادقة وتجديدها تحت إدارة CLI).
  - **ملف Codex التنفيذي قابل للتثبيت عبر pip، وليس عبر npm فقط**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK `openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عام لاستخدام `--use_codex`. لا تُضاف الحزمة إلى `requirements.txt`: إذ يبلغ حجم الملف التنفيذي نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين من أجل مزوّد اختياري. جرى التحقق من البداية إلى النهاية: مع غياب `codex` عن `PATH`، يعثر الحلّ على الملف التنفيذي المضمّن في الحزمة وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: تُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. ومن دون هذا الإجراء الوقائي، قد يتسبب مفتاح موجود في `.env` في تحويل Codex إلى الفوترة حسب الاستخدام من دون أي إشارة ظاهرة — وهو تحديدًا ما وُجد هذا المزوّد لتجنّبه.
  - **أخطاء CLI الشائعة المثبّتة بالاختبارات**:
    - يقرأ `codex exec` الإدخال القياسي **حتى** عندما يُمرّر الموجّه كوسيط: ومن دون إغلاق الإدخال القياسي، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج مطلقًا (أُعيد إنتاجه: خروج 124 بعد 180 ثانية، وصفر بايت). لذلك فإن `communicate(input=...)` إلزامي.
    - `codex` المثبّت عبر npm هو غلاف Node يقوم عبر `spawn` بتشغيل ملف Rust التنفيذي الحقيقي: وهذا الأخير **عملية حفيدة** لعملية Python وينجو من `SIGKILL` الخاص بـ`subprocess.run(timeout=)`، حيث سيواصل استهلاك الحصة. ومن هنا جاء `Popen(start_new_session=True)` + `os.killpg`.
    - قد يخرج CLI بالرمز 0 رغم إصداره `turn.failed`: لذلك يُفحص خرج JSONL ‏(`--json`) بالإضافة إلى رمز الإرجاع، ويؤدي غياب الملف `-o` مع الرمز 0 إلى رفع خطأ صريح بدلًا من إنتاج مقطع فارغ.
  - **التراجع التدريجي عند بلوغ حد المعدّل**: لا ينفّذ CLI أي إعادة محاولة داخليًا (`max_retries = 0`). ويجري التصنيف اعتمادًا على بنية حمولة JSON ‏(`status: 429` / `error.type`)، لا على سلاسل فرعية — إذ تظهر كلمة «الحصة» في كل من خطأ 429 القابل للاسترداد و`insufficient_quota` النهائي.
  - **إجراء وقائي في CI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفًا. فالمصادقة عبر الاشتراك غير مخصّصة لمشغّل مشترك، كما تنصح OpenAI صراحةً بعدم استخدام مسار العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` ‏(`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وAPI Platform، لكن حساب ChatGPT لا يملك حق الوصول إلى كل نماذجها: تُطبّق قائمة السماح من جانب الخادم من دون تحقق محلي، ويؤدي اختيار نموذج غير معتاد إلى إطلاق تحذير. في خطة Plus، يوفّر Luna ما بين 250 و2,000 رسالة لكل نافذة مدتها 5 ساعات، مقابل 10 إلى 100 لـSol: ويُعد `--eco` الوضع الموصى به لكل معالجة دفعية.
  - **إصلاح خلل — كان `regen_translations.sh` يخرج بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` تابع لـ`main()` لم يعد موجودًا عند تنفيذ المصيدة. وتحت `set -u`، كان ذلك يرفع `failed_log: unbound variable` ويجعل السكربت يخرج بالرمز 1 رغم صحة الترجمات الـ28 — ما كان سيوقف `release.sh --auto` ‏(`set -e`) مباشرةً بعد إعادة التوليد، في المرحلة الأعلى تكلفة. أصبح المتغير عامًا، وصارت المصيدة تتحقق من وجوده. أثر جانبي مفيد: عادت إخفاقات الترجمة الحقيقية، التي كان هذا الخطأ يحجبها سابقًا، إلى الظهور في الملخص النهائي.
  - **`REGEN_MODEL`**: متغير بيئة جديد لـ`regen_translations.sh` يفرض نموذجًا محددًا بدلًا من النموذج الافتراضي للمزوّد، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج المتقدم من حصة الاشتراك بدلًا من نموذج `--eco` الموجّه للأحجام الكبيرة.
  - **`regen_translations.sh`**: يتوفر `REGEN_PROVIDER=codex` عبر اشتراك صريح فقط (ولا يُكتشف تلقائيًا مطلقًا، حتى لا تُستهلك حصة الاشتراك من دون علم المستخدم). يُجدّد الرمز مرة واحدة تسلسليًا قبل بدء التوازي — لأن تجديد Codex تدويري وأحادي الاستخدام، وقد تُبطل المهام المتزامنة جلسة `codex login` — ويُخفض التزامن إلى 4.
  - **إعادة الهيكلة المرتبطة**: ينتقل `_dispatch_provider_call` من 8 معاملات إلى 6 عبر `_resolve_provider()` يعيد اسم مزوّد، بدلًا من قيمة منطقية رابعة تُمرّر عبر السلسلة بأكملها. وتظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` أدنى.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` (48 اختبارًا) يغطي argv، والبيئة المنقّحة، وعقد منع التمهيد، والإخفاقات الصامتة، والمهلة/killpg، والتراجع التدريجي، والفحص المسبق، وحلّ المزوّد، وتسلسل استدلال Gemini، وتصفية كتل Claude، واستشهادات الأخبار متعددة الفقرات. تضم المجموعة الكاملة 290 اختبارًا.
  - **التحقق الفعلي**: تُظهر ترجمة `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex بنية مطابقة تمامًا للترجمات المرجعية (14 كتلة برمجية، و24 عنوانًا، و25 سطر جدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل برمجية متطابقة حرفًا بحرف، ومن دون أي بقايا لعناصر نائبة). وفي مقال إخباري بحجم 69 كيلوبايت في وضع `--news`، يجتاز خرجا `gpt-5.6-luna` و`gpt-5.6-sol` كلاهما مدقق التطبيق اللاحق باللغات en/ja/ar. أما الاستهلاك المقاس عبر `account/rateLimits/read` فقد ظل دون عتبة تقريب العدّاد (0% من نافذة 5 ساعات) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL لنَسب الأخبار عند وجود أقواس متداخلة أو بادئة فرنسية (2026-05-11):

  - **إصلاح الخلل**: كان استخراج عناوين URL الخاصة بالنَّسب في `_protect_news_quotes` يستخدم التعبير النمطي `re.search(r"\((.+?)\)", attribution)` (التقاط كسول بين قوسين). وعند وجود نسبة من النوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` المحيط + `]()` الخاص برابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة وتتضمن البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائية). والنتيجة: كان `_validate_news_post` يبحث عن هذه السلسلة في الخرج المترجم ويفشل منهجيًا (لسببين: `)` مبتور + ترجمة "relayé par" إلى `relayed by`/`weitergeleitet von`/...). ولم يكن بإمكان التسلسل الكامل low ← medium ← high ← gpt-5.5 النجاح.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — إذ يستهدف تحديدًا `](url)` الخاص برابط Markdown، ويلتقط **عنوان URL الخالص فقط** (من دون البادئة الفرنسية أو البتر)، وهو ثابت تحافظ عليه العناصر النائبة `#URL{N}#` أثناء الترجمة. وهو متين أمام النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس محيطة
  - **الاختبارات**: اختباران جديدان في الفئة `TestNewsCitationExtraction` ضمن `test_silent_failure.py`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة الدقيقة المعاد إنتاجها لخلل Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (صيغة بديلة تتضمن `via`)
  - **فجوة التغطية**: يتحقق `check-editorial-coverage.py` من صحة الصياغة التحريرية، لكن ليس من قابليتها للترجمة بواسطة المترجم. ومن التحسينات الممكنة (خارج نطاق v1.9.2) إضافة فحص يحاكي استخراج النَّسب في تشغيل تجريبي لاكتشاف الأنماط الخطرة **قبل** النشر.

- **1.9.1** إصلاح التدويل لملصق CTA في ملاحظة علامة الترجمة (2026-05-10):

  - **إصلاح الخلل**: ظل ملصق `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط العلامة أعلى الملفات المترجمة **بالفرنسية** لجميع اللغات المستهدفة، بدلًا من اتباع `target_lang`. ولا يراه LLM مطلقًا (إذ يُركّب من جانب Python للحفاظ على عنوان URL ومعرّف slug للمستودع)، لذلك لم تكن مرحلة الترجمة قادرة على تداركه. وهو تراجع صامت منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ15 بملصقاتها المحلية. وأصبح `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` يمرّران اللغة المستهدفة. ويُستخدم `fr` كقيمة احتياطية إذا كانت اللغة غير معروفة (للسلامة وتفادي KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link` (اللغة المستهدفة `ja` ← الملصق الياباني المتوقع). واختباران جديدان: `test_source_link_label_localized_per_target_lang` (مُعلَّم على 7 لغات تغطي النصوص اللاتينية والتصويرية والأبجدية الساكنة) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختبارًا في `test_translation_note_position.py` (بدلًا من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع بقيمة افتراضية `target_lang="fr"` — تواصل الجهات البرمجية الخارجية المستدعية التي لا تمرّر `args.target_lang` العمل من دون تعديل.
- **1.9** إصلاح الفشل الصامت + أدوات جودة متكاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + تنسيق العلامة "embed card"**:
    - خيارات CLI جديدة (إضافية، والقيم الافتراضية دون تغيير ← **لا تتسبب في تغييرات كاسرة للتوافق**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): يضع الملاحظة في أعلى الملف المترجم أو أسفله أو في كلا الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - يعيد `legacy` إنتاج سلوك الإصدار v1.8 بدقة (فقرة بخط عريض `**…**`) **بايت مقابل بايت**.
        - يُصدر `marker` تعريف مرجع رابط غير مرئي في Markdown ‏(`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`)، يتبعه **اقتباس كتلي من 3 فقرات** منظّم لعرض شبيه بـ "GitHub repo embed card": عنوان المشروع ضمن كود مضمّن (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف يترجمه LLM، ورابط دعوة إلى اتخاذ إجراء (`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. يمكن استخدامه في أثناء البناء عبر إضافة remark (راجع مدونة jls42.org ← الإضافة `remark-translation-banner`).
    - **ثوابت لا تُرسل مطلقًا إلى LLM**: يُجمَّع عنوان المستودع وعنوان URL الخاص بـ GitHub في Python بعد ترجمة العبارة الوصفية. لا يرى LLM مطلقًا المعرّف النصي `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تغيير أي عارض أو حالة أحرف أو مخطط.
    - **إدراج مدرك للبيانات الوصفية الأمامية**: في وضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة `---` الختامية** للبيانات الوصفية الأمامية بصيغة YAML (لضمان سلامة Astro Content Collections / gray-matter). تكتشف الدالة المساعدة `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف وتحافظ على سلامته؛ كما **تطلق `RuntimeError`** عند وجود بيانات وصفية أمامية مفتوحة بلا سياج إغلاق (فيُدرج الملف ضمن `failed_files` بدلًا من كتابته بملاحظة في موضع خاطئ).
    - **منقّي النموذج بالقائمة البيضاء**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع استخدام `unknown` احتياطيًا إذا كانت النتيجة فارغة. يتوافق ذلك مع أداة التحقق في إضافة remark الخاصة بـ Astro ويحيّد المحارف التي قد تفسد تنسيق العلامة (المسافة، وعلامة الاقتباس، والقوس، والفاصلة، وغيرها).
    - **إعادة هيكلة داخلية**: `_append_translation_note` (دالة واحدة متجانسة) ← 7 دوال مساعدة صِرفة (`_translation_note_invariants`، و`_build_translation_note_phrase`، و`_assemble_translation_note_paragraphs`، و`_build_translation_note_source`، و`_sanitize_model`، و`_quote_lines`، و`_split_frontmatter`، و`_build_translation_note_block`، و`_compose_with_notes`). فُصل المنشئ عن المؤلّف (يعيد المنشئ كتلة صِرفة بلا فاصل، بينما يطبّق المؤلّف `\n\n` وفقًا للموضع)؛ ويشترك كود الإنتاج والدالة المساعدة المصدرية في المجمّع نفسه ذي الفقرات الثلاث.
    - **يحافظ `_quote_lines` على الأسطر الفارغة**: يضيف `> ` إلى بداية كل سطر، مع تحويل الأسطر الفارغة إلى `>` وحده. يتيح ذلك لـ mdast رؤية 3 فقرات منفصلة داخل الاقتباس الكتلي (العنوان / الوصف / الرابط) بدلًا من فقرة واحدة تحتوي على فواصل أسطر.
    - **`_build_translation_note_block` تكيّفي**: وفقًا لعدد الفقرات التي حافظ عليها LLM (3 = تنسيق البطاقة الكامل، 2 = العبارة + الرابط، 1 = التنسيق الاحتياطي). لم يعد التنسيق الاحتياطي ذو الفقرة الواحدة **يغلّف المحتوى بـ `**...**`** عند اكتشاف رابط Markdown ‏`](` (إذ يكون عرض `<strong>` حول رابط هشًا).
    - **توافق تصاعدي**: `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` في `_compose_with_notes` — تستمر كائنات Namespace التي لا تتضمن هاتين السمتين (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) في العمل دون تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور على مقتطف المصدر حرفيًا) + طبقة احتمالية (`langdetect`)
    - القائمة البيضاء `finish_reason` / `stop_reason`: إطلاق `RuntimeError` لأي حالة خارج القائمة البيضاء (الاقتطاع، وcontent_filter، وما إلى ذلك)
    - `max_tokens` في Claude: ‏`4096` ← `32768` (يتجنب الاقتطاع الكامن في المقاطع بحجم 16k، مع هامش للتحويل بين أنظمة الكتابة FR→JA/ZH/KO/AR/HI)
    - تجزئة مدركة للعناوين: إعطاء الأولوية لـ H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى رمز خروج غير صفري: يعيد `translate_markdown_file` حالة نمطية `success` / `failure` / `skipped`، ويُرجع `main()` القيمة `sys.exit(1)` إذا فشل ملف واحد على الأقل (في وضع الملف الواحد ووضع الدفعة)
    - حاجز للمحتوى الفارغ لدى جميع المزوّدين، ونسبة تحقق معقولة بين المصدر والمخرجات (≥ 500 محرف، وأقل من 5% = رفض)، والتحقق من العناصر النائبة للكود (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، وإعادة محاولة `BadRequestError` من دون `reasoning_effort`
    - إضافة الاعتمادية `langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("حزمة كاملة على نمط EurekAI"، 14 خطافًا):
    - Pre-commit: ‏ruff (الفحص+التنسيق)، وshellcheck، وprettier ‏(md/yaml/json)، وdetect-secrets (حماية 4 مفاتيح API)، وLizard ‏(CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، ونهاية الملف، والملفات الكبيرة، وأسطر shebang، وما إلى ذلك)
    - Pre-push: ‏mypy (وضع متساهل تدريجي)، وOpengrep SAST ‏(translate.py + scripts/)، وpip-audit (وضع الإبلاغ الأولي)، وunittest discover ‏(tests/ + scripts/tests/)
    - أغلفة محلية في `scripts/` تستخدم `./venv/bin/python`
    - `scripts/audit_verdict.py`: محلل JSON لـ pip-audit مع 11 اختبار unittest، وهو نقل إلى Python لمحلل jls42-astro
    - إصلاح 7 مخالفات أولية من ruff: ‏B904 ‏(raise from) ×2، وB007 ‏(مجلدات غير مستخدمة)، وC408 ‏(قيمة dict حرفية)، وC419 ‏(list-comp)، وSIM105 ‏(contextlib.suppress)، وSIM110 ‏(any())
    - يستثني Lizard مؤقتًا `translate.py` (4 دوال بقيم CCN تتراوح بين 21 و47، مع التخطيط لإعادة هيكلتها) — بوابة صارمة على scripts/
  - **SonarCloud + تغطية شاملة**:
    - سير عمل GitHub Actions ‏`SonarCloud` ‏(sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع التغطية عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى README ‏(Quality Gate، وتقييمات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code)
    - `tests/test_silent_failure.py` ‏(`unittest` من المكتبة القياسية): يغطي الحلقات الست في سلسلة أخطاء الفشل الصامت
    - `tests/test_orchestration.py` (+79 اختبارًا): يغطي طبقة التنسيق في `translate.py` ‏(`_resolve_*_filename`، و`_existing_translation_exists`، و`_record_translation_status`، و`_write_output_file`، و`translate_directory`، و`_validate_input_paths`، و`_init_*_client`، و`_select_provider_client`، و`_normalize_collapsed_markdown`، و`_cleanup_source_flag`، و`_validate_news_flags_*`، والتنسيقات الاحتياطية `_openai_create_with_fallback` لـ TypeError + BadRequestError، وتنسيق المطالبة لسلسلة o1، وفروع الإرجاع المبكر في `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` ‏(stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **التغطية على الكود الجديد**: 75.5% ← نحو 98% ‏(translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك اختبارات E2E ‏`marker+top|bottom|both` و`legacy+top|bottom|both`)، وإضافة البادئة إلى عدة أسطر، والتوافق مع الإصدارات السابقة بايتًا مقابل بايت (قيمة حرفية مرجعية)، والمنقّي، وتقسيم البيانات الوصفية الأمامية (بما في ذلك إطلاق استثناء عند عدم إغلاق السياج)، وتنسيق الفقرات الثلاث، والتنسيق الاحتياطي ذي الفقرتين، وحاجز الفقرة الواحدة + رابط Markdown، وحاجز أمان حرج `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان+عنوان URL لا يُرسلان مطلقًا إلى LLM. **نجح 190 اختبارًا**، دون أي تراجع.
  - التوثيق: `README.md` ‏(بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` ‏(سير عمل pre-commit + مراقبة مفصلة لـ CI)، وإعادة توليد 28 ترجمة
- **1.8** وضع `--news` + ترقية نماذج 2026 ‏(2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - جودة OpenAI: ‏`gpt-5` ← `gpt-5.4`
    - OpenAI الاقتصادي: `gpt-5-mini` ← `gpt-5.4-mini`
    - جودة Gemini: ‏`gemini-3-pro-preview` ← `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` ‏(400k) و`gemini-3.1-pro-preview` ‏(1M)
  - وضع `--news` الأولي: حماية الاقتباسات الإنجليزية باستخدام العناصر النائبة `#NEWSQUOTE\d+#`، وتعيين `LANG_FLAGS` ‏(15 لغة)، وإدارة الأعلام وفقًا للغة الهدف
  - التحقق من العناصر النائبة للأخبار قبل استعادتها (تراجع: كان حذف LLM للعنصر النائب يُنتج بصمت مخرجات بلا اقتباس)
  - جعل السكربت `regen_translations.sh` قابلًا للنقل (مسارات مطلقة، بلا اعتماد على pwd)
  - إضافة رابط اللغة الفرنسية إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة
- **1.7** ميزات جديدة:
  - الخيار `--keep_filename` للاحتفاظ باسم الملف الأصلي عند الترجمة
  - دعم الملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على الكود المضمّن**: أصبحت علامات backtick ‏(`` `...` ``) محمية الآن أثناء الترجمة
  - تحسين مطالبة النظام:
    - معالجة أفضل لعلامات الاقتباس في البيانات الوصفية الأمامية بصيغة YAML
    - حماية متغيرات القالب `{variable}`
    - منع ملاحظات المترجم غير المطلوبة
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** ميزات جديدة:
  - دعم Google Gemini API للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI: ‏`gpt-5` (الجودة)، و`gpt-5-mini` (الاقتصادي)
    - Claude: ‏`claude-sonnet-4-5` (الجودة)، و`claude-haiku-4-5` (الاقتصادي)
    - Gemini: ‏`gemini-3-pro-preview` (الجودة)، و`gemini-3-flash-preview` (الاقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف واحد (`--file`) من دون استعراض مجلد
  - نمط تسمية مبسّط جديد: `{base}-{lang}.md`
  - الخيار `--include_model` للاحتفاظ بالتنسيق القديم الذي يتضمن اسم النموذج
  - دعم النماذج غير المدرجة بحد افتراضي للرموز (128k)
  - ترجمة README إلى 14 لغة
- **1.5** تحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين مطالبات الترجمة:**
    - أُثريت مطالبات الترجمات المباشرة وملاحظات الترجمة لزيادة الوضوح والكفاءة، بما يشمل تعليمات مفصلة للحفاظ على البيانات الوصفية وعناصر التنسيق المحددة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم عمليات الاستيراد لتحسين سهولة القراءة والصيانة.
    - تحسين تجزئة النصوص وإدارة كتل الكود للحفاظ على التنسيق الأصلي في أثناء الترجمة.
  - **إدارة ملفات المخرجات:**
    - عكس ترتيب النموذج واللغة في اسم ملفات المخرجات (مثلًا، `f"{base}-{args.target_lang}-{args.model}.md"`)، ما يسهّل تنظيم الترجمات والعثور عليها.
  - **تحسينات متنوعة:**
    - تنظيف الكود بإزالة الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكربت وسهولة قراءته.
- **1.4** ميزات جديدة:
  - دعم Claude API من Anthropic للترجمة
  - تحسين المطالبات لزيادة الوضوح والكفاءة
  - تعديلات طفيفة لتحسين قابلية صيانة الكود
- **1.3** تحسينات وميزات جديدة:
  - تحسين إدارة كتل الكود
  - تحسين إدارة ملفات المخرجات
  - تحسين اكتشاف الملفات الموجودة
  - الخيار `--force` لفرض الترجمة
  - عكس ترتيب النموذج واللغة في اسم ملف المخرجات
- **1.2** إصلاح سجل التغييرات
- **1.1** إضافة دعم Mistral AI API
- **1.0** الإصدار الأولي - دعم OpenAI API

**مقال مترجم من الفرنسية إلى العربية باستخدام gpt-5.6-sol.**
