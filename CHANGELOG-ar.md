### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.13.0** المزوّد `--use_openrouter`: موجّه مدفوع إلى نحو 430 نموذجًا، منها النماذج الصينية المفتوحة (2026-09-05):

  - **مسار المزوّد التاسع، طُرح مع الثامن.** لم يُنشر الإصدار 1.12.0 على PyPI: فقد صدر الموجّهان، OpenCode وOpenRouter، معًا. يتيح [OpenRouter](https://openrouter.ai) بمفتاح واحد الوصول إلى نماذج لا يعرضها هنا أي مزوّد آخر — Kimi وQwen وDeepSeek وZ.ai — عبر رصيد موحّد تُحتسب تكلفته حسب الاستخدام. وبما أن نقطة النهاية متوافقة مع OpenAI، فالعميل هو نفسه المستخدم مع xAI؛ **وكل ما يميّز هذا المزوّد قائم على فحص تمهيدي**، وكل قاعدة من قواعده ناتجة عن قياس أُجري على API.

  - **يُقدَّم النموذج نفسه عبر عشرات المستضيفين ذوي الحدود القصوى المختلفة، والتوجيه لا يراعي ذلك.** وفق القياس: 33 مستضيفًا لـ `z-ai/glm-5.2` و23 لـ `z-ai/glm-5.3-flash` — أحدها محدود عند **2 048 token للإخراج**. لذلك كانت ترجمة طويلة تمر عبر أحد المستضيفين الـ23 فتخرج مبتورة عشوائيًا، بلا أي إشارة. يقرأ الفحص التمهيدي `/api/v1/models/{modèle}/endpoints`، ويستبعد المستضيفين الذين يقل حد إخراجهم عن 8 000 token، ومن لديهم حالة متدهورة، ومن لا يصرّحون بأي حد، ثم يثبّت الباقين. إن `provider.only` **من دون** `allow_fallbacks: false` ليس سوى تفضيل: إذ يعود الموجّه إلى مستضيف مستبعد، فيفقد التثبيت أي قيمة. وإذا لم يستوفِ أي مستضيف الحد الأقصى، يتوقف الأمر: فمواصلة الترجمة تعني قبول البتر الصامت الذي وُجد هذا الفحص التمهيدي لمنعه.

  - **تُحتسب تكلفة الاستدلال بتعرفة الإخراج، وهو مفعّل افتراضيًا في نماذج كثيرة.** الطلب نفسه على `z-ai/glm-5.2`، والإجابة «حسنًا»: **107 tokens إكمال بالإعداد الافتراضي للنموذج، و2 عند إيقاف الاستدلال**. وفي الترجمة، حيث لا يضيف الاستدلال شيئًا، يعني ذلك تضاعفًا بمقدار 18 لكل مقطع من كل ملف. لذا يُعطّل افتراضيًا. أما **النماذج الـ288 من أصل 431** التي تفرضه (`reasoning.mandatory`) فتجيب بـ `400 « Reasoning is mandatory for this endpoint and cannot be disabled »`: وبالنسبة إليها، يقرأ الفحص التمهيدي مستويات الجهد المقبولة ويطلب أدناها (النقطة التالية) — إذ يخصّص الجهد **نسبة مئوية من `max_tokens`** يستهلكها الاستدلال أولًا، ولذلك فإن اختيار قيمة عشوائية ينقل خطر الصفحة البيضاء بدلًا من تقليله.

  - **يتلقى النموذج الذي يفرض الاستدلال أدنى جهد يقبله، وقد حُسم ذلك بالقياس.** كان الخيار الأول ألا يُرسل شيء، تجنبًا للتخمين نيابة عن النموذج. وعند اختباره على `z-ai/glm-5.3-flash`، الذي تكون قيمة الكتالوج الافتراضية له `max`، أدّى هذا الخيار إلى إخراج **مبتور عند 32 768 token** قبل نهاية الترجمة — فضاعت لغتان من أصل أربع عشرة. ولم تكن زيادة السعة لتغيّر شيئًا: فالجهد يخصص نسبة منها، والاستدلال ينمو معها. لذلك يقرأ المزوّد `supported_efforts` أثناء الفحص التمهيدي ويطلب أدنى قيمة، مع الرجوع إلى «لا شيء» عندما لا يعلن الكتالوج أي قيمة قابلة للاستخدام. وقد أكد الاختبار العكسي ذلك على اللغة التي كانت تفشل: كانت تتوقف بسبب نفاد الميزانية، وأصبحت الآن تنجح في 9 دقائق ببنية مطابقة للمصدر.

  - **أصبح عطل المستضيف الأعلى يحمل اسمه الآن.** يطبّع الموجّه هذه الحالة إلى `finish_reason=error` مع `native_finish_reason` فارغ — وقد قيس ذلك مرتين، على لغتين، عند 750 ثانية بالضبط. كانت الرسالة العامة تدفع إلى البحث عن خلل في المستند أو في التقسيم؛ أما الآن فتوضح أن العطل لدى المزوّد وأن إعادة المحاولة تكفي غالبًا.

  - **إن `finish_reason=length` مع إخراج فارغ ليس بترًا.** بل يعني أن الاستدلال استهلك الميزانية قبل أول محرف مفيد — وفق القياس، 15 850 token للاستدلال مقابل 148 مفيدًا. وتتطلب الحالتان إجراءين متعاكسين: فلا يفيد تقليل حجم المقاطع في الحالة الأولى. وتفرّق الرسالة بينهما صراحةً. كما جاءت وسيلتا حماية أخريان من القياسات: يجيب الموجّه بحالة **200 مع متن لا يحتوي إلا على خطأ** عندما يفشل المستضيف الأعلى (كان `choices[0]` يطلق `TypeError` مبهمًا يحجب الرسالة)، وتُقرأ نافذة السياق من الكتالوج ثم تُسجّل في `MODEL_TOKEN_LIMITS` — إذ إن `DEFAULT_TOKEN_LIMIT` خاطئ لـ44 نموذجًا في الكتالوج، منها نموذجان محدودان عند 4 095 token.

  - **`--model fournisseur/modèle` إلزامي، ويُتحقق من صيغته قبل أي اتصال بالشبكة.** ليس OpenRouter مزوّدًا: فالاختيار يحدد السعر والترخيص ومعالجة البيانات، ولا يجوز اتخاذه نيابة عن المستخدم. وبما أن slug يُدرج في عنوان URL الخاص بالفحص التمهيدي، فليس التحقق مجرد تحسين لقابلية الاستخدام، بل هو الحاجز الذي يمنع حقن مسار فيه: يقبل regex المشترك ذو namespace للموجّهين `a/b/..`، ومن هنا جاء الرفض الصريح للمقطع الأب. أما `--eco` فلا تأثير له، وهو يوضح ذلك.

  - **صُححت ثلاث صياغات، إحداها كانت خاطئة.** كان تحذير OpenAI بشأن `codex exec` يتعلق بحقن ملف جلسة شخصي في runner مشترك، لا بكون المستودع عامًا: وقد ورد بعكس معناه في README وCLAUDE.md وفي الشيفرة. وتغيّر موضع مصادقة OpenCode في الإصدار 1.18.27 (الجدول `credential` في `opencode.db` بدلًا من `auth.json`)؛ ظل الثابت «لا يُقرأ هنا مطلقًا» صحيحًا، لكن العنوان كان قديمًا. وأخيرًا، لم يعد قسم OpenCode يعرض مسارات لم تُختبر قط كما لو كانت متكافئة: فقد اختُبرت بوابة Zen وOllama من البداية إلى النهاية، بينما لم تُختبر GitHub Copilot وLM Studio وllama.cpp، وأصبح README يذكر ذلك الآن.

  - **حملة قياس وجدول للنماذج الموصى بها في README.** نُفذت أكثر من ثلاثمئة ترجمة على ثلاث مجموعات من المستندات — مقالة مدونة كثيفة في وضع `--news`، وREADME هذا بصيغة Markdown القياسية، وأربعة ملفات README لمشروعات معروفة مأخوذة من GitHub كما هي — إلى أربع عشرة لغة. يميّز الجدول بين أمرين كانا يُخلطان: الترجمات التي **تكتمل** وتلك التي تكون **بنيتها مطابقة** للمصدر. لم تفقد ثلاثة نماذج أي معلومة قط في المستندين الكثيفين: `gemini-3.7-flash` و`gpt-5.6-sol` عبر اشتراك ChatGPT و`z-ai/glm-5.2` عبر OpenRouter — وكانت اختلافاتها الوحيدة زوجًا من `**` لم يُنقل في لغة أو لغتين. والخلاصة الأساسية هي أن **العامل الفارق هو كثافة المستند، لا وضع `--news`**: يفشل Grok عبر الاشتراك ثلاث عشرة مرة من أصل أربع عشرة في مقالة المدونة، وينجح في أربعة عشر README عامًا من أصل ستة عشر، ويرجع ذلك إلى فقدانه المسار في مقطع طويل، وهو ما أكده اختبار عكسي. ويحمل الجدول تحذيره الخاص: فهو غير شامل، ومؤرخ، ولا ترتّب المدد أي شيء، ويظل النهج السليم هو القياس على مستندات المستخدم نفسه.

  - **صُحح مقارن البنية قبل نشر أرقامه، لأنه كان ينتج نتيجتين إيجابيتين كاذبتين في أنظمة الكتابة غير اللاتينية.** لم يكن عنوان URL المتبوع بقوس كامل العرض `）` يُفصل بواسطة regex يتوقف عند `)`، ولذلك كانت السلسلة المستخرجة تختلف رغم تطابق عنوان URL؛ كما أن اقتباسًا من خمسة أسطر بالفرنسية يشغل ثلاثة أسطر بالصينية كان يخفض عدّ الأسطر. وقد ثُبّت التصحيحان باختبار عكسي: فما زالت إزالة عنوان URL أو قسم أو شيفرة مضمنة تُكتشف. ولولاهما، لكانت نتائج Gemini وCodex المنشورة إحدى عشرة لغة من أصل أربع عشرة بدلًا من ثلاث عشرة واثنتي عشرة.

  - **الاختبارات**: ملف جديد `tests/test_openrouter_provider.py` ‏(76 اختبارًا) — التحقق من النموذج ورفض المقطع الأب، وتثبيت المستضيفين (الحد الأقصى، والحالة، والحد غير المعلن، والحد الأدنى المشترك)، وكون `allow_fallbacks` خاطئًا دائمًا، وتعطيل الاستدلال أو إبقاؤه وفق `mandatory`، وعقد الإخراج الكامل (خطأ داخل استجابة 200، وعدم وجود أي اختيار، وتمييز الصفحة البيضاء عن البتر، و`finish_reason` غير طبيعي، ومحتوى فارغ)، وإغلاق الفحص التمهيدي عند الفشل إذا تعذر الوصول إلى الكتالوج أو غاب slug أو لم يوجد مستضيف سليم، وحصرية flag وتسمية اسم الملف. بلغ مجموع الحزمة **502 اختبارًا**.

  - **إعادة هيكلة: قُسّمت الوحدة الوحيدة ذات 4 253 سطرًا إلى وحدات، من دون تغيير سطر واحد من السلوك.** وُزّع `src/aipmt/translate.py` على `config` و`markdown` و`segmentation` و`guards` و`placeholders` و`news` و`prompts` و`notes` و`naming` و`pipeline` و`cli` وحزمة فرعية `providers/` (وحدة لكل مزوّد، و`base` كأساس، و`registry` للحل والتوجيه). وكل عملية نقل هي commit ذو دليل آلي: إذ يقارن مدقق AST لجميع عُقد المستوى الأعلى في الحزمة بلقطة مرجعية، ويتحقق من موضع كل رمز، وبقاء علامات الأمان حرفيًا، وعدم وجود ملف غير متتبّع — وهي أدوات مؤقتة أزيلت في الإصدار التالي. أما التغييرات الظاهرة: فيصبح `aipmt.translate` واجهة تعيد عرض الأسماء الـ64 التي كانت الوحدة تعرضها بلا بادئة `_`، مع الحفاظ على هوية الكائن (`__all__` يحمل تسعة منها، وهي API المدعومة؛ والبقية أسماء مستعارة للتوافق)، ويتوقف عن إعادة تصدير أسماء التبعيات والمكتبة القياسية الـ29 التي كان يلتقطها `import *`؛ ولم يعد التنفيذ المباشر للملف (`python src/aipmt/translate.py`) موجودًا — ويظل `aipmt` و`python -m aipmt` الصيغتين المدعومتين؛ وأصبح `__module__` للدوال العامة هو الخاص بوحدة تعريفها؛ وتُستورد SDK بعد تحميل `.env` بدلًا من قبل ذلك، بلا أثر معروف. حُفظت الاختبارات الـ427 حتى على مستوى المعرّفات، ونُقلت إلى الوحدات التي تختبرها: فأصبح 91 patch كان يمر عبر الواجهة يستهدف الوحدة التي ترجع إلى الاسم (ظل اثنان منها ناجحين حتى من دون patch الخاص بهما، وفق القياس)، وتثبّت سبعة اختبارات عقد الواجهة، وأُعيدت كتابة أدوات البوابات قبل أول عملية نقل حتى لا يتمكن أي منها من التحول إلى الحالة الناجحة بمجرد التوقف عن التحقق — نطاق Lizard على مستوى الأدلة مع حد أدنى، وقراءة flags من parser المُنشأ، وحد أدنى للتغطية حسب الحزمة، و`release.sh` الذي يسرد الوحدات المتتبعة.
  - **إصلاح (مراجعة pull request)**: يرفض OpenRouter سجل كتالوج بلا `context_length` بدلًا من تسجيل القيمة الافتراضية البالغة 128 000 token على أنها قياس — وهو ما كان يعطل أيضًا التحذير «النموذج غير مدرج» — وعندما يكون `finish_reason` فارغًا (النوع الموثق `string | null`)، يُعتد بالسبب الخام من المستضيف، إذ تساوي `max_tokens` القيمة `length`؛ كما أن الخطأ المحمول في الاختيار نفسه يؤدي إلى رفض المحتوى الجزئي المصاحب له، مع الرسالة نفسها الخاصة بعطل المصدر الأعلى المطبّع — فتجمع تفاصيل المستضيف والسبب الخام والنصيحة. يستجيب OpenCode بخطأ العقد الخاص به، لا بـ `AttributeError`، عند حدث `part: null`، ويرفض تدفق JSONL إذا تعذرت قراءة أحد أسطر الأحداث بدلًا من قبول نص جزئي. تقتل أدوات CLI الوكيلة الثلاث مجموعة الوكيل عندما تتلقى هذه العملية `SIGTERM` أثناء الاستدعاء — كان `timeout` الخاص بإعادة التوليد يترك الوكيل حيًا يستهلك حصته — ويتبع `SIGKILL` الخاص بالمجموعة دائمًا مهلة السماح، إذ كان shim ينتهي بطريقة سليمة يترك حفيده حيًا. وأُعيد وصل زوج `# fmt: off` / `# fmt: on` الذي فصله التقسيم؛ كما تسمّي مساعدة `--reasoning_effort` المزوّدين الأربعة الذين يستخدمونه.
  - **إصلاح (المراجعة الثانية)**: بات حد الإخراج المطلوب من OpenRouter يحتفظ بالجزء الذي يشغله prompt والمقطع من السياق — إذ يغطي `context_length` الإدخال والإكمال معًا، وكانت طلبات ستة نماذج في الكتالوج تُرسل بلا مساحة للإدخال؛ ويُرفض السياق الأقصر من اللازم قبل احتساب أي تكلفة. كما يعود الأساس المشترك لأدوات CLI الوكيلة إلى `terminate` ثم `kill` في البيئات التي لا تتوفر فيها مجموعات عمليات POSIX، بدلًا من السماح لـ `AttributeError` بتجاوز حاجز انتهاء المهلة وإطالة الانتظار. ويُبحث عن مؤشر `429` في OpenCode باعتباره رقمًا لا سلسلة فرعية، إذ كان معرّف خطأ مثل `err_84290b` يطلق انتظارًا تصاعديًا لمدة 90 ثانية قبل أن يفشل على أي حال. وأخيرًا، تُعرض نقطة نهاية OpenRouter غير القياسية أثناء الفحص التمهيدي: يكفي `.env` خاص بالمشروع لتعيين واحدة، وهو المفتاح الحقيقي الذي كان سيُرسل إليها لاحقًا.
  - **الأمان: لم يعد بإمكان `.env` خاص بالمشروع إعادة توجيه استدعاءات API.** يبحث `find_dotenv(usecwd=True)` عن الملف انطلاقًا من الدليل الحالي وآبائه: ولذلك كان بإمكان شجرة غير موثوقة — مثل مستودع استُنسخ للتو — أن تضع فيها `OPENROUTER_BASE_URL` أو `XAI_BASE_URL` أو `OPENAI_BASE_URL` (وتقرأ SDK الأخيرة بنفسها) من دون معرفة أي مفتاح، ثم يُرسل المفتاح الحقيقي، القادم من البيئة أو إعدادات المستخدم، داخل ترويسة التفويض إلى خادم خارجي. يعتمد المرشح على نمط لا على قائمة: وقد أظهر حصر SDK المثبتة أنها تقرأ اثني عشر متغير توجيه، يقرأ عميل Anthropic وحده ستة منها — وكانت قائمة مكتوبة يدويًا ستغفل نصفها. لذا تُرفض من طبقة المشروع جميع المتغيرات التي تطابق `_BASE_URL` أو `_API_BASE` أو `_ENDPOINT`، إضافةً إلى proxies ومخازن الشهادات (فتحديد مرجع مصدّقات خاضع للسيطرة يجعل الوسيط غير قابل للتمييز عن خادم حقيقي)، وكذلك `XDG_CONFIG_HOME` و`APPDATA`: إذ كان تعيينهما يعني تحديد الملف الذي يشكّل طبقة المستخدم، وبالتالي الالتفاف على المرشح من طريق جانبي. ولا تُقبل هذه المتغيرات إلا من الطبقتين اللتين يتحكم فيهما المستخدم، وهما البيئة المصدّرة و`~/.config/aipmt/.env`. كما تُقرأ طبقة المشروع من دون استيفاء: إذ يوسّع `load_dotenv` القيمة `${VAR}` افتراضيًا، وكان `.env` غير موثوق يحتوي على `NOM_ANODIN=${OPENAI_API_KEY}` ينسخ المفتاح الحقيقي تحت اسم لا يتعرف إليه الترشيح النمطي للعمليات الفرعية — فيدخل حينئذ إلى بيئة `codex exec` خلافًا للثابت المعلن. وأخيرًا، لا يعرض الرفض إلا اسم المتغير: فقد كان عنوان URL بالصيغة `https://${CLE}@hôte/` يسرّب المفتاح المستوفى إلى السجلات رغم رفضه. ويُعلن الرفض على stderr مع بيان الإجراء المطلوب — إذ يجب تعريف مرحّل الشركة في إعدادات المستخدم.
  - **إصلاح: تُحسب سعة إخراج OpenRouter لكل استدعاء.** يغطي `context_length` الإدخال والإكمال معًا، ولا يضمن احتياطي ثابت مضبوط على نص لاتيني أي حد: فعند القياس باستخدام tokenizer ‏`o200k_base`، تعادل 16 000 محرف 3 200 token بالفرنسية، لكنها تعادل 12 300 باليابانية و17 500 عند استخدام emoji. لذلك تُستمد الميزانية من النص المرسل فعليًا، مع زيادتها بعدد بايتاته بترميز UTF-8: ففي كل tokenizer يعتمد دمج البايتات — سواء BPE على مستوى البايت أو SentencePiece مع الرجوع إلى البايت، وهي العائلات التي يستخدمها الكتالوج — يعادل token بايتًا واحدًا على الأقل، وهذه هي الزيادة الوحيدة المتاحة هنا، حيث يوجّه OpenRouter إلى عشرات tokenizers غير المعروفة لنا. ولم تكن أي نسبة متوسطة مناسبة — إذ قد ينخفض رمز تصويري من المستوى التكميلي إلى 1.33 بايت لكل token، ومحرف مركب إلى 1.00 — وبذلك أصبح الإدخال والإخراج معًا داخل النافذة بحكم طريقة الإنشاء. ويُرفض المقطع الأكثر كثافة مما يسمح به النموذج المختار قبل الاستدعاء بدلًا من رفضه بعد احتساب التكلفة.

- **1.12.0** المزوّد `--use_opencode`: ‏OpenCode، الوكيل المفتوح المصدر، إلى المزوّد الذي يختاره المستخدم — نموذج محلي، أو مجاني بلا حساب، أو اشتراك، أو مفتاح (2026-09-04):
  - **مسار ثامن للمزوّد، يختلف في طبيعته عن المسارات السبعة الأولى.** ليس [OpenCode](https://opencode.ai) (MIT) مزوّد نماذج، بل هو _موجّه_ إلى المزوّدين الذين أعدّهم المستخدم داخل OpenCode نفسه: مفتاح API، أو اشتراك (GitHub Copilot، ChatGPT، SuperGrok)، أو بوابة OpenCode Zen — التي تتيح نماذج مجانية **من دون حساب** —، أو نموذج **محلي** (Ollama، LM Studio، llama.cpp). يتحكم السكربت في `opencode run` في الوضع غير التفاعلي، كما يتحكم في Codex وGrok، ويعيد استخدام البنية الأساسية نفسها للعمليات الفرعية (مجموعة عمليات مستقلة، `SIGTERM` ثم `SIGKILL` عند انتهاء المهلة، وإغلاق stdin دائمًا، وتنقية البيئة). جرى التحقق عبر **ترجمتين فعليتين**: ملف README هذا كاملًا إلى الإنجليزية بواسطة `opencode/mimo-v2.5-free` — خلال 49 ثانية، وفي تمريرة واحدة، وبنية مطابقة للملف المصدر (32 عنوانًا، و26 إغلاقًا لكتل الشيفرة، و18 رابطًا، و37 عنوان URL، و37 سطر جدول، و135 شيفرة مضمنة) — وملف اختبار بواسطة `ollama/qwen2.5:7b`، محليًا، من دون أي مفتاح.

  - **`--model provider/modèle` إلزامي، وهذا قرار مقصود.** من دون `--model`، يعود OpenCode إلى إعداداته الافتراضية، وهي في التثبيت الجديد `opencode/big-pickle`، وهو نموذج مجاني «خفي» قد تُستخدم التبادلات معه في التدريب — وقد أثبت القياس أن هذا هو النموذج الذي أجاب. إن اختيار ذلك بصمت بدلًا من المستخدم سيكون بالضبط نوع التحويل غير المرئي الذي يتعقبه هذا المستودع؛ ولذلك تسمّي رسالة الخطأ الأمر الذي يسرد النماذج (`opencode models`) وتعرض ثلاثة أمثلة (محلي، مجاني، اشتراك). لا تأثير لـ `--eco`، وهو يصرّح بذلك. ويُمرَّر `--reasoning_effort` كما هو بوصفه `--variant` الخاص بـ OpenCode، وفقط عند طلب صريح.

  - **عزل مُقاس، لا مُفترض.** يعرّف إعداد مضمن (`OPENCODE_CONFIG_CONTENT`، وهو الأخير في ترتيب الدمج لدى OpenCode، ولذلك تكون له الأولوية على إعداد المستخدم من دون استبداله) وكيلًا باسم `aipmt` تُرفض له جميع الأدوات (`permission: {"*": "deny"}`): حتى إن السجل لا يعود يعرضها على النموذج، الذي يجيب، عندما يُطلب منه «سرد الملفات وتشغيل `id`»، بأنه لا يملك أدوات. مشاركة الجلسة معطلة، والإضافات الخارجية مستبعدة (`--pure`)، ولا يُستخدم `--auto` مطلقًا، ودليل العمل مؤقت وفارغ. جرى قياس حقنتين صامتتين وإيقافهما: من دون `OPENCODE_DISABLE_CLAUDE_CODE`، يدخل `~/.claude/CLAUDE.md` الخاص بالمستخدم في **كل** مطالبة (515 رمز إدخال بدلًا من 186 لمجرد «مرحبًا»)؛ ومن دون `OPENCODE_DISABLE_PROJECT_CONFIG`، يدخل أيضًا `AGENTS.md` الخاص بالدليل الحالي — وقد طُبّقت على الترجمة تعليمة تقول «اختم كل إجابة بكلمة BANANA». أما `~/.config/opencode/AGENTS.md` العام، فيظل محقونًا: لا يوجد مفتاح يستبعده، كما أن التحايل عليه عبر `XDG_CONFIG_HOME` مُحوَّر سيخفي أيضًا مزوّدي المستخدم. جرى توثيق ذلك بدلًا من ترقيعه.

  - **لا يثبت `exit 0` شيئًا؛ إنها CLI ثالثة، وتُطبّق عليها الاحتياطات نفسها — مع فخّين خاصين بها.** لا يؤدي `--agent` غير المعروف إلى فشل `opencode run`: يظهر تحذير على stderr ثم يحدث رجوع **صامت** إلى وكيل البرمجة مع بقاء الأدوات نشطة. لذا، إذا لم يؤخذ الإعداد المضمن في الحسبان، فستنطلق الترجمة بوكيل قادر على الكتابة؛ ولذلك يتحقق عقد المخرجات من غياب هذه الرسالة، بالإضافة إلى: رمز خروج 0، وعدم وجود أي حدث `error`، وعدم وجود أي `tool_use`، وأن يكون آخر `step_finish` في الحالة `stop` (إذ إن `length` يعني استجابة مبتورة)، وألا يكون النص فارغًا. الفخ الثاني: حدث الخطأ بصيغة JSON **مبهم** — «خطأ غير متوقع في الخادم. راجع سجلات الخادم لمزيد من التفاصيل.»، مع مرجع بسيط — ولا يظهر السبب الحقيقي (`ProviderModelNotFoundError: Model not found: foo/bar. Did you mean…`، و`ProviderAuthError`...) إلا في السجلات. ومن هنا يأتي `--print-logs --log-level ERROR` وقراءة الحقل `error="…"` من stderr، من دون أثر Bun الذي يليه. وهكذا يفشل النموذج غير المعروف خلال ثانية واحدة مع تسمية السبب. كما يمنع `--title` استدعاء LLM زائدًا: فمن دونه، ينشئ OpenCode عنوانًا للجلسة عبر دورة إضافية على `small_model`.

  - **الأسرار: التصفية بالنمط نفسها المستخدمة مع Codex وGrok، باستثناء واحد محدد بالاسم.** يُحتفظ بـ `OPENCODE_API_KEY`: فهو مفتاح OpenCode نفسه (بوابة Zen، واشتراك Go)، وموجّه إليه باسمه — أي ما يعادل `auth.json` الخاص به، وليس مفتاحًا يديره aipmt أو يستطيع احتساب رسوم عليه. يُعدّ المزوّدون داخل OpenCode (`opencode auth login`، و`opencode.json`)، وليس أبدًا داخل `.env` الخاص بـ aipmt، إذ لا يصل أي من مفاتيحه إلى العملية الفرعية. لا يوجد رفض في CI، بخلاف CLI الخاصة بالاشتراكات: فاستخدام مفتاح API أو نموذج مستضاف ذاتيًا على مشغّل استخدام مشروع.

  - **يتحقق حاجز منع اجتياز المسارات الآن من القيمة بعد الاستيفاء، لا من القيمة الخام.** يحتوي `provider/modèle` على `/` كان حاجز الإصدار 1.10.0 يرفضه — وكان محقًا، لأن `--model` كان يُستوفى داخل اسم الملف `--include_model`. تستبدل تسمية اسم الملف الآن `/` و`\` و`:` بـ `-` قبل أي استيفاء (`ollama/qwen2.5:7b` ← `ollama-qwen2.5-7b`، لأن `:` غير قانوني على Windows)، ويفحص الحاجز السابق هذه التسمية: يصبح `../../evil` الاسم البسيط `doc-en-..-..-evil.md` داخل الوجهة، ولا يُرفض إلا `..` وحده، وكذلك `--target_lang ../x`. ويظل حاجز النطاق `_ensure_within_directory` الطبقة الثانية من دون تغيير.

  - **النماذج المجانية والنماذج المحلية، وما جرى قياسه.** يترجم `opencode/mimo-v2.5-free` فقرة خلال 16 ثانية وملف README هذا خلال 49 ثانية؛ ويستغرق `opencode/big-pickle` مدة 40 ثانية لترجمة 200 كلمة، كما ظلت عليه طلبات متزامنة بلا استجابة لمدة 5 دقائق، بينما ينجح كل طلب منفردًا؛ ولم يقدّم `opencode/nemotron-3.5-lightning-free` أي استجابة خلال 3 دقائق. ومن هنا يأتي `REGEN_PROVIDER=opencode` مع إلزامية `REGEN_MODEL` وتشغيل **مهمتين** بالتوازي. محليًا، يضبط Ollama غالبًا سياقًا من 4,096 رمزًا، بينما يصل طول المقاطع إلى 16,000 محرف: لذا يلزم `Modelfile` مع `PARAMETER num_ctx 32768`، وتتبع الجودةُ النموذجَ — فقد عكس نموذج 7B ترتيب قائمة وأفسد إغلاق كتلة شيفرة في ملف الاختبار، بينما حافظ نموذج البوابة على كل شيء.

  - **لم تعد ترجمات هذا المستودع تمر مطلقًا عبر API مدفوعة.** كان `regen_translations.sh` يستخدم OpenAI API بمجرد وجود مفتاح في `.env`، ولم يجعل Codex سوى خيار اشتراك صريح. وهذا بالضبط ما حدث أثناء إعداد هذا الإصدار: أُرسلت الترجمات الـ28 عبر OpenAI API، ثم أُرسل CHANGELOG الهندي عبر Gemini API، مع أن اشتراك ChatGPT موجود تحديدًا لتجنب الدفع بحسب الاستخدام. أُزيل الاكتشاف التلقائي للمفاتيح: **أصبح Codex هو الخيار الافتراضي، مع `gpt-5.6-sol`**، وهو نموذج الجودة؛ وتتطلب `openai` و`gemini` و`grok` وجود `REGEN_ALLOW_PAID_API=1` بالإضافة إلى `REGEN_PROVIDER`، وهو استثناء مسمّى كي تُطبّق القاعدة لحظة اتخاذ القرار؛ ويفشل `REGEN_PROVIDER` غير المعروف بدل الرجوع إلى API. تثبّت عشرة اختبارات الخيار الافتراضي والرفض والاستثناء. وأُعيد تنفيذ الترجمات الـ28 لهذا الإصدار عبر Codex.

  - **جرت مركزة التراجع التدريجي عند بلوغ حد المعدل** (`_retry_on_rate_limit`): كانت حلقتا Codex وGrok متطابقتين باستثناء التسمية، وكانت إضافة نسخة ثالثة ستتجاوز حد التكرار. تشتق أخطاء CLI الثلاثة من `_CliCallError` واحدة؛ ويمنع اختبار خروج أي واحد منها عن هذه الفئة، لأن الحلقة المشتركة لن تراه عندئذ.

  - **الاختبارات**: ملف جديد باسم `tests/test_opencode_provider.py` (61 اختبارًا) — عقد المخرجات الكامل، والرجوع إلى الوكيل، وقراءة السبب من السجلات، وإزالة تكرار أجزاء النص وتجاهل الأجزاء الاصطناعية، وقتل مجموعة العمليات عند انتهاء المهلة، والتراجع التدريجي عند 429، وإلزامية النموذج والتحقق منه، والفحص المسبق من دون سر، وتحديد الملف التنفيذي، وتوصيل آلية التوجيه، وتسمية اسم الملف، واختبار مضاد لاجتياز المسارات. يوسّع `tests/test_review_hardening.py` حصرية الأعلام وغياب الأسرار لتشمل المزوّد الجديد. وتتطلب البوابة الآن توثيق **22 علمًا** من argparse. المجموعة الكاملة تضم **382 اختبارًا**.

- **1.11.1** تصحيح للتوثيق: يعلن README أخيرًا عن مسارات المزوّد السبعة (2026-09-03):

  - **كانت صفحة PyPI للإصدار 1.11.0 تقول «4 واجهات API + Codex CLI».** لكن الشيفرة توفر سبعة مسارات — OpenAI وMistral وClaude وGemini وGrok عبر API؛ وCodex (ChatGPT) وGrok عبر الاشتراك، من دون فوترة بحسب الاستخدام. كان نمطا Grok غائبين من المقدمة ومن بند _تعدد المزوّدين_، وكررت الترجمات الـ14 الخطأ. ولأن الوصف الطويل للحزمة ثابت لكل إصدار، تطلّب تصحيح الواجهة رقم إصدار جديدًا: وهذا هو السبب الوحيد لوجود هذا الإصدار. **لا تغييرات في الشيفرة.**
  - جرت مواءمة `CLAUDE.md` مع ما أدخله النشر: عدادات البوابة (16، و17 في `--full`)، وسير العمل الأحد عشر النشط، وعدادا Sonar/Codacy غير الظاهرين في `gh pr checks` (النقاط الساخنة، وCodacy API)، ونقل `# nosemgrep` بواسطة `ruff-format`، وبيئات GitHub التي يتطلبها تبادل OIDC، وحقيقة أن _الناشر المعلّق_ لا يحجز الاسم.

- **1.11.0** النشر على PyPI: `pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-09-03):

  - **أصبح السكربت أحادي الملف حزمة قابلة للتثبيت.** انتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول طرفية `aipmt` وما يعادلها `python -m aipmt`. يظل المستودع المستنسخ ضروريًا للمساهمة — إذ توجد فيه الاختبارات والترجمات الـ28 وأدوات الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` أبدًا**، لأن التعارض حقيقي وصامت. تثبّت حزمة PyPI المسماة `translate` (الإصدار 3.8.1، وآخر رفع في 2026-07-06) دليلًا يحمل الاسم نفسه. جرى استنساخ الحالة داخل venv: يتغلب الدليل على الوحدة، ويختفي `translate.main`، وتتعطل نقطة الدخول عند `AttributeError` — بينما يجيب `pip check` بالعبارة «لم يُعثر على متطلبات معطلة» مع rc=0. كان مجرد `pip install translate` لدى المستخدم كفيلًا بتعطيل CLI من دون تشخيص قابل للاستخدام. والاختبار المضاد على ملف wheel الحقيقي: تثبيت `pip install translate` فوق الحزمة، ثم `aipmt --help` مع rc=0 قبل ذلك وبعده، وتتعايش واجهتا CLI.
    - **اسم توزيع طويل، وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للاكتشاف عبر بحث PyPI؛ فالاختصار وحده لن يجده من لا يعرف المشروع مسبقًا، بينما يستهدف النشر تحديدًا قابلية الاكتشاف. واستُبعد مرشحان معقولان بعد التحقق: `ai-markdown-translator` محجوز على npm منذ 2024 لأداة تؤدي الغرض نفسه، وتسبق هذا المستودع بـ17 شهرًا؛ كما أن `aimt` لا يختلف إلا بحرف واحد عن `aim` (الإصدار 3.29.1)، وهي حزمة نشطة في المجال **نفسه** — أسوأ وضع ممكن لالتباس دائم. وهناك أيضًا فخ منهجي: يعيد `pypi.org/project/<nom>/` الحالة 200 لأي اسم كان (صفحة مضادة للروبوتات)، ولا يُعتد إلا بـ JSON API.
    - **تخطيط `src/` بدل الحزمة المسطحة.** كان من شأن الحزمة المسطحة الحفاظ على أوامر `sys.path.insert(..., "..")` الستة في الاختبارات، وهذه هي المشكلة تحديدًا: فهي تستورد شجرة المصدر بدل الحزمة، ما سيخفي أي خطأ في التحزيم. التكلفة الفعلية: قاعدة استبدال إضافية واحدة.

  - **أصبح من الممكن أخيرًا إعداد المفاتيح مرة واحدة بصورة دائمة.** لم تكن CLI المثبتة تملك أي إعداد **دائم**: لم يكن متاحًا سوى متغير البيئة و`.env` في الدليل الحالي. صحيح أن `find_dotenv` يصعد حتى جذر النظام، ولذلك كان يعثر على `~/.env` **عند العمل داخل الدليل الشخصي**، لكنه لم يكن يعثر على شيء عند العمل من مكان آخر — أي إن التغطية كانت تعتمد على مكان تشغيل الأمر، لا على قرار تصميمي. لذلك أضيفت طبقة ثالثة: `~/.config/aipmt/.env`، أسفل الطبقتين الحاليتين.

    - **الأولوية غير مضمّنة في الشيفرة**، بل تنتج من `override=False`، وهي القيمة الافتراضية لـ `load_dotenv`: لا تملأ كل طبقة إلا ما تركته سابقتها فارغًا. ومن هنا يأتي الترتيب: متغير البيئة ← ملف `.env` الخاص بالمشروع ← إعداد المستخدم، وقد جرى التحقق منه باختبارات **سلوكية** لا بنيوية — إذ تفشل عند عكس ترتيب الاستدعاءين، وكذلك عند إزالة الطبقة الثالثة.
    - **تنسيق `.env` وليس TOML**، عن قصد: فـ `python-dotenv` تبعية موجودة بالفعل، وبنيته موثقة بالفعل في ملفات README الخمسة عشر، ويُستخدم الملف نفسه للنطاقين. لا تبعية جديدة ولا صياغة جديدة. يتبع الموقع `XDG_CONFIG_HOME` إذا كان **مطلقًا** — إذ تشترط المواصفة تجاهل القيمة النسبية، وإلا عاد موقع الإعداد معتمدًا على الدليل الحالي — ويستخدم `APPDATA` على Windows.
    - **استُبعد خياران، ولكل منهما سببه.** مخزن مفاتيح النظام (`keyring`) أكثر أمانًا على جهاز مكتبي، لكنه يفشل في البيئات عديمة الواجهة — الخوادم، والحاويات، وCI — أي في حالة الاستخدام الأساسية نفسها للترجمة على دفعات؛ لذا فهو مرشح جيد كخيار صريح، لكنه خيار افتراضي سيئ. أما العلم `--api-key` فسيضع المفتاح في سجل shell ويجعله ظاهرًا في `ps`.
    - **عند غياب المفتاح، لم يعد يظهر أثر استدعاء.** كان المستخدم يتلقى مكدس استدعاءات Python يشير إلى `site-packages`، ورسالة تذكر «البيئة أو .env» من دون تحديد مكان إنشاء الثاني. والآن تسرد الرسالة المواقع الثلاثة مع مسار كل منها بدقة، ويخرج الأمر بالرمز 2. شبكة الأمان **ضيقة عن قصد**: `except ValueError` في مرحلة الإعداد وحدها. فتغليف التنفيذ كله سيحوّل خطأً حقيقيًا يحدث أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الفشل الذي يتعقبه هذا المستودع. ويقرأ اختبار مصدر `main()` لمنع ذلك.

  - **تصحيح — كان ملف `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** لا يصعد `load_dotenv()` من الدليل الحالي عند استدعائه من دون وسيط، بل من الملف **المستدعي**، أي من `site-packages`. جرى قياس ذلك باستخدام نقطة دخول طرفية حقيقية شُغّلت من مشروع يملك ملف `.env` خاصًا به: يعيد `find_dotenv()` القيمة `''` ولا يُحمّل المفتاح، بينما يعثر عليه `find_dotenv(usecwd=True)`. لم يكن الخطأ موجودًا عندما كانت الأداة تعمل فقط من المستودع المستنسخ؛ لكن النشر كان سيجعله منهجيًا، ولم يكن له عرض سوى ظهور مفتاح API على أنه «مفقود» رغم صحة الإعداد.

  - **كان يمكن لثلاث بوابات أن تتحول إلى اللون الأخضر رغم توقفها عن التحقق من أي شيء.** جرى تشديدها **قبل** النقل، عن قصد: فحاجز الحماية المكتوب بعد التغيير الذي يُفترض أن يكتشفه لا يثبت شيئًا. كل بوابة خضراء على المستودع الأصلي وتتحول إلى الحمراء على نسخة منقولة — وقد قيس الاتجاهان.

    - **يتجاهل Lizard المسار الغائب من دون أن يقول شيئًا**: rc=0، و«0 ملف محلل». كانت بوابة التعقيد ستنتقل من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع مخرجات حجمها صفر بايت. أصبح النطاق الآن مصفوفة يُتحقق من وجود كل مدخل فيها.
    - **لا يفشل `coverage run --source=` عند تطبيقه على وحدة غائبة**: يظهر تحذير على stderr فقط، مع rc=0 لكل من unittest و`coverage xml`، ويُنشر التقرير رغم ذلك — بعد انخفاضه من 1453 إلى 141 عبارة. كان المشروع سيبدو سليمًا لأنه لم يعد يُحلل تقريبًا. يحمي التقرير حدّان أدنيان: الإجمالي، وأكبر ملف خضع للقياس.
    - **مسبار حداثة الترجمات أعمى بنيويًا عن صيغة الاستدعاء**: فهو يرتكز على أعلام argparse، وهي بالضبط ما لا يتغير عند إعادة تسمية الملف. جرى استنساخ الحالة: نُقلت الوحدة، وظلت ملفات README الخمسة عشر توثّق أمرًا غير موجود، وكانت النتيجة «لا توجد ترجمات قديمة». لذلك يتحقق قسم سابع من **الصيغة** لا من الخيارات، ويُقارن خطاف Lizard بالنطاق الفعلي للسكربت — فمفتاحه `files:` لا يتسبب في فشل pre-commit عندما يتوقف عن المطابقة، بل يجعله **يتخطى التنفيذ**.
  - **لم يعد `requires-python = ">=3.10"` مجرد ادعاء.** كان `sonar-project.properties` يعلن بالفعل دعم الإصدارات 3.10-3.12 من دون أن يُختبر أيٌّ منها قط، إذ لم تكن محطة التطوير تحتوي إلا على 3.12 — وهو تناقض داخلي كان النشر سيكشفه للعامة. يشغّل سير عمل الاختبارات الآن الحزمة على 3.10 و3.11 و3.12، مع تثبيت الحزمة، وبالتالي اختبار حدودها المعلنة.

  - **حدود دنيا بلا سقف.** يظل `requirements.txt` ملف القفل المختبَر، بينما يصبح `[project.dependencies]` العقد العام: فنشر الإصدارات الدقيقة الواردة في ملف القفل سيسبب تعارضات لكل مستخدم لديه حزم أخرى. ولا يوجد أيضًا سقف `<N+1` — إذ سيتعارض مباشرةً مع `check-deps-fresh.sh`، الذي يُفشل بوابة الإصدار عند أي تأخر في إصدار رئيسي. تُحل مجموعة الحدود الدنيا، ويخرج الاختبار المضاد `openai==1.0.0` بالرمز `ResolutionImpossible`، ما يثبت أن الفحص يميّز الحالات بدلًا من قبول كل شيء. كما يمنع حارسٌ إصدارَ `pyproject.toml` من الاختلاف عن إصدار CHANGELOG: إذ لا يسمح PyPI بإعادة استخدام رقم إصدار.

  - **تم التحقق من البداية إلى النهاية داخل venv جديد**: ملف wheel بحجم يقارب 70 كيلوبايت لا يحتوي إلا على `aipmt/*.py` وdist-info والترخيص؛ و`aipmt --help` بالرمز rc=0 مع 22 خيارًا؛ و`python -m aipmt` يعرض «usage: aipmt» لا «usage: \_\_main\_\_.py»؛ وتثبيت `pipx` يعمل؛ والأهم **ترجمة فعلية من الفرنسية إلى الإنجليزية انطلاقًا من أي مجلد مستخدم**، مع الحفاظ على النص العريض والقائمة والكود المضمن والرابط وURL، ومن دون ترجمة كتلة الكود. تنجح الاختبارات الـ318 السابقة للترحيل بقائمة معرّفات متطابقة بايتًا ببايت قبل الترحيل وبعده — وهذا ما يثبت أن أي اختبار لم يُعطّل، لا عبارة «OK»؛ وتُضاف إليها اثنا عشر اختبارًا للإعداد ذي الطبقات الثلاث، ليصبح المجموع 330.

- **1.10.0** المزوّد `--use_codex` (حصة اشتراك ChatGPT)، وتحديث SDK والنماذج، وإصلاح اقتباسات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — حاجزان أضافتهما PR من دون تطبيقهما في كل المواضع**:

    - **كان الفحص المسبق لـCodex يمرّر `.env` كاملًا إلى الملف التنفيذي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: وهكذا كانت العملية الفرعية ترث `os.environ` كاملًا، بما في ذلك `.env` بأكمله الذي حمّله `load_dotenv`. وعند القياس باستخدام ملف تنفيذي زائف مزوّد بأدوات رصد، وصلت **سبعة أسرار** إلى الفحص المسبق — مفاتيح المزوّدين الستة بالإضافة إلى `GITHUB_TOKEN` — مقابل **صفر** لنظيره `_grok_preflight`، الذي كان يمرّر `env=_grok_env()` بالفعل. كان التناقض داخل PR نفسها: إذ يوجد `_strip_secret_env` تحديدًا للحفاظ على هذا الثابت، على بُعد بضعة أسطر فقط. جرى استخراج `_codex_env_base()` ومشاركته بين المسارين؛ والقياس بعد الإصلاح: صفر من الأسرار في كلا المسارين.
    - **لم تشمل خاصية «`--deny` مغلق عند الفشل» الصيغة المستخدمة.** كان التعليق يبرر احتواء Grok بالكامل بأن قاعدة ذات بادئة مجهولة تمنع بدء التشغيل. أظهر القياس على `grok 1.0.13` أن هذا التحقق لا يوجد **إلا للصيغة الموضوعة بين قوسين**: يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل («بادئة أداة مجهولة»)، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. لكن `GROK_DENY_RULES` لم يكن يستخدم إلا أسماء مجردة — ولذلك كان من شأن إعادة تسمية أداة لدى xAI إزالة طبقة الاحتواء الوحيدة المقاسة من دون أي إشارة، على محطة لا ينطبق فيها أصلًا sandbox نظام التشغيل. انتقلت القواعد الثماني المسماة إلى `Prefix(*)`، مع التحقق من كل واحدة منها بوصفها بادئة معروفة لدى CLI؛ وتظل قاعدة الالتقاط الشامل `*` بصيغتها الحرفية، وهي الصيغة الوحيدة المقبولة. يمنع اختبارٌ العودة إلى الصيغة غير المتحقق منها.
    - **وتم التحقق من سلامة بقية الجوانب**: لا يوجد حقن أوامر (تُستخدم صيغة القائمة في كل المواضع، ولا يُستخدم `shell=True` مطلقًا، ويمر محتوى المستند عبر stdin أو `--prompt-file`)، ولا توجد إزالة تسلسل غير آمنة (`json.loads` فقط، مع حارس للنوع)، ولم يُعثر على تجاوز لإصلاح اجتياز المسار عبر سبع حمولات، كما أن `--deny '*'` يطبقه CLI فعليًا (رُصد `DENY_ENFORCED` عند قراءة خارج workdir).
    - كان فحص الحداثة المضاف أعلاه يتجاوز مبدأه هو نفسه بالمناسبة: إذ كانت الحزمة التي يفشل طلبها إلى PyPI تُتخطى بصمت وتظل البوابة خضراء. وهو الآن يحصي الحزم التي جرت مقارنتها فعليًا ويفشل إن كانت التغطية غير مكتملة.

  - **تحديث التبعيات، وإضافة شبكتي أمان لمنع تكرار التأخر**:

    - **كان التأخر حقيقيًا ومستمرًا**: `openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر عامين في مخزن الشهادات الجذرية الذي يتحقق من TLS لجميع استدعاءات المزوّدين. حُدد السبب: **لم يكن `.github/dependabot.yml` موجودًا**. من دون هذا الملف، لا يفعّل GitHub سوى _التحديثات الأمنية_، ولا يقترح Dependabot طلب PR إلا لتبعية تستهدفها CVE — وهذا يفسر تحديثه `urllib3` و`idna` مع ترك SDK اثنين يبتعدان بإصدار رئيسي كامل.
    - **يتعايش الإصداران الرئيسيان من دون تعارض**، خلافًا لما أوحى به استدلال سابق: ينتقل `openai` 3.x و`anthropic` 1.x إلى **`httpx2`**، بينما يظل `mistralai` و`google-genai` على `httpx<1`، لكنهما توزيعتان منفصلتان. جرى التحقق عبر تثبيت فعلي، ثم عبر **مسارات المزوّدين السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الكود المضمن والروابط في كل مخرج. كانت «تجنّب مكدسين لـHTTP» تفضيلًا لا عائقًا: وقد حسم القياس الأمر.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: كانت `google-auth` و`cryptography` ومكدس `opentelemetry` مثبتة في venv العمل من دون التصريح بها قط — ولذلك لم يكن التثبيت الجديد يعيد إنتاج ما يجري اختباره. وعلى العكس، كانت `tokenizers` و`huggingface-hub` و`PyYAML` مدرجة فيه من دون أن يستوردها أو يتطلبها أي شيء، وهي بقايا من `mistralai` 1.x. أُعيد توليد الملف بوصفه الإغلاق الكامل لـvenv مبني من التبعيات المباشرة وحدها. لا يبلغ `pip-audit` عن أي ثغرة معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** (جديد) يفعّل تحديثات الإصدارات الأسبوعية لـpip وgithub-actions. تُجمع الإصدارات الثانوية والتصحيحية في PR واحدة — إذ ينتهي الأمر بتجاهل تحديث تصحيحي واحد لكل PR، والضجيج عدو التحديث؛ أما **الإصدارات الرئيسية فتظل منفصلة**، ويتطلب كل منها تحققًا باستدعاء فعلي.
    - **`scripts/check-deps-fresh.sh`** (جديد، وموصول بالبوابة) يجعل التأخر ظاهرًا في حكم المشروع: يقترح Dependabot التحديثات، لكنه لا يضمنها، وقد تتراكم طلبات PR الخاصة به. التأخر في إصدار رئيسي ← فشل؛ والتأخر في إصدار ثانوي ← تحذير، لأن البوابة الحمراء باستمرار ينتهي بها الأمر إلى التجاهل؛ وتعذر الوصول إلى PyPI ← تخطٍّ صريح محليًا و**إغلاق عند الفشل في CI**، لأن الفحص الذي لم يُنفذ لا يُعد نجاحًا. جرى التحقق في الاتجاهين: فهو يلتقط الحالة الدقيقة السابقة للإصلاح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير بشأن إصدار ثانوي.

  - **إصلاحات نتجت عن مراجعة PR هذه** — فحص خمسة وكلاء مراجعة الفروقات بدقة؛ وأُعيد إنتاج جميع النقاط أدناه **بالقياس** قبل تصحيحها، وكان اثنان منها تراجعين أُدخلا سابقًا في هذا الإصدار نفسه.

    - **إصلاح تراجع — كان `_NEWS_CITATION_REGEX` يعاني من تتبع رجعي أُسّي.** أدخل إصلاح الفقرات المتعددة `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: فتقاسم المسافات بين `[ \t]+` و`.*` ملتبس، ويتضاعف هذا الالتباس من تكرار إلى آخر. أظهر القياس على أسطر `>   texte` — وهي مسافة بادئة سليمة تمامًا في Markdown — لا تطابق النمط: **2,589 ملّي ثانية لـ14 سطرًا** مقابل 0.04 ملّي ثانية بعد التصحيح، مع عامل يقارب 9 لكل سطر مضاف. في وضع `--news`، كان اقتباس طويل غير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب يمكن تحديده. يستهلك التكرار الآن السطر كاملًا دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يترك إلا طريقة واحدة للمطابقة في كل تكرار. جرى التحقق على المتن الفعلي المؤلف من 231 مقالة: **لا اختلافات** في الالتقاط، ونفس الاقتباسات الـ423، مع استمرار توسيع المتون الـ14 متعددة الفقرات.
    - **كان استخدام خياري مزوّد في آن واحد يؤدي إلى فوترة حسب الاستخدام بصمت.** كان `--use_codex --use_mistral` مقبولًا؛ ويختبر `_select_provider_client` مزوّد Mistral أولًا، بينما يعطي `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. وهكذا كان المستخدم يطلب حصة اشتراكه ويحصل بدلًا منها على فوترة حسب الاستخدام من دون أدنى تحذير: وهو بالضبط نمط الفشل الذي وُجد `--use_codex` لمنعه. تمر خيارات المزوّدين الستة الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: يفشل الآن سطر الأوامر الذي يجمع بين مزوّدين عند `argument --use_mistral: not allowed with argument --use_codex`، بعدما كان يُقبل بصمت.
    - **كانت بوابة نهاية العمل تتحول إلى اللون الأخضر عندما تتعطل مجسّاتها.** كانت أربعة من الفحوص الثلاثة عشر في `scripts/check-release-ready.sh` تتبع نمط «التقاط stdout، والاستنتاج إذا كان فارغًا» من دون الرجوع مطلقًا إلى رمز الخروج: فكان الاستثناء (ملف أعيدت تسميته، `FileNotFoundError`) يكتب إلى stderr، ويترك stdout فارغًا، وينتهي الفحص إلى «لا شيء يستدعي الإبلاغ». وهكذا تكرر فخ «وجود `exit 0` لا يثبت شيئًا» داخل السكربت المكتوب لمنعه. يفرض المساعد `probe()` الآن رمز خروج صفريًا **وكذلك** علامة نهاية، وترفض المجسات استخلاص نتيجة من مجموعة معالم فارغة — لأن التأكيد على مجموعة فارغة يكون صحيحًا دائمًا. والدليل: أدت إضافة المجموعة الحصرية أعلاه إلى تمرير خيارات المزوّد عبر كائن `*_group`، فلم يعد التعبير النمطي القديم `parser\.add_argument\(` يطابقه؛ وخرجت **ستة خيارات من أصل واحد وعشرين** من النطاق بصمت، مع بقاء البوابة خضراء.
    - **كان فحص الأسرار يفوّت أربعة مزوّدين من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: ولذلك كان `sk-proj-…` (صيغة OpenAI الحالية) و`sk-ant-api03-…` ينقطعان عند الشرطة الثانية، ولم يكن `AIza…` مشمولًا. وُسّع النمط، واستُبعد `.secrets.baseline` من الفحص. كما كان الحارس `.env` يستعلم من `git diff --cached`، الذي لا يرى إلا الفهرس: ولذلك لم يكن `.env` **المسجّل بالفعل في commit** — وهو أسوأ الحالات — يظهر فيه قط. وهو يستعلم الآن من `git ls-files`.
    - **لم تكن «تهيئة الرمز المميز» لـCodex تهيئة فعلية.** أظهر القياس أن `codex login status` لا يلمس `~/.codex/auth.json` (إذ ظل وقت التعديل والحجم من دون تغيير)، وتقول مساعدته «عرض حالة تسجيل الدخول». ومع ذلك كان التعليق يدعي تحديث الرمز المميز «مرة واحدة، بصورة متسلسلة»، وبالتالي تحييد خطر عمليات التحديث المتزامنة لرمز مميز دوّار صالح للاستخدام مرة واحدة. لم تكن الحماية المعلنة موجودة؛ ويصف التعليق الآن ما يفعله الكود فعلًا، بينما يظل الحل الحقيقي هو `max_jobs=4`. كما يحترم الفحص `CODEX_BIN` الآن، بعدما كان يتجاهله — إذ كانت محطة لا تحتوي على `codex` ضمن `PATH` تفشل برسالة «غير مصادق عليه»، وهو تشخيص مضلل.
    - **كان `.env` يُحمّل عبر source داخل shell فرعي.** تُستدعى `detect_provider` داخل استبدال أمر، ولذلك لم تكن قيم export الخاصة بها تنتقل إلى الأعلى: فكان `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` المعرّف داخل `.env` يظل غير مرئي للقراءات المنفذة في `main()`، التي كانت تستنتج «لم يُعثر على الملف التنفيذي لـGrok» رغم صحة الإعداد.
    - **كانت درجة التوازي تتجاوز الحد المعلن بنسبة 50%.** وُضع الحارس بعد تشغيل زوج README/CHANGELOG: وكانت الذروة المقاسة **3 لـ`max_jobs=2`**. وفي Grok، الذي تتشارك حصته الأسبوعية خدمات Chat وImagine وVoice ولا يمكن قياسها، لم يكن السكربت يحترم الحد الذي فرضه على نفسه. أما العدد النهائي، فكان يُعرض من دون مقارنته قط بالعدد 28 — ولذلك كان غياب ملف يمر من دون اكتشاف.
    - **عقد مخرجات Grok: أصبح غياب `stopReason` يُعد فشلًا.** كان الكود يطبق «`end_turn` **أو غائب**» في موضع يفرض فيه العقد المعلن `end_turn`. وكانت حمولة بلا هذا الحقل — أو أُعيدت تسمية حقلها بفعل تحديث CLI — تحوّل الحارس إلى إجراء عديم الأثر بصمت. كذلك لم يعد `max_turn_requests` مصنفًا بوصفه تجاوزًا لحد المعدل (فالذي نضب هو ميزانية الجولات: وإعادة المحاولة تعيد النتيجة نفسها، مقابل انتظار 90 ثانية)، وأُزيل `quota` من علامات تجاوز حد المعدل — للسبب الذي كان docstring الخاص بـ`_codex_is_rate_limited` يذكره بالفعل من دون أن يطبقه Grok.
    - **تُحفظ سلسلة الرجوع الخاصة بـGemini مؤقتًا حسب النموذج.** كانت تبدأ مجددًا من `minimal` عند كل مقطع، مع أن النموذج الافتراضي يرفضه: ولذلك كان المسار الاعتيادي يدفع تكلفة رحلة ذهاب وإياب باستجابة 400 لكل مقطع، ويعيد طباعة التحذير نفسه. والتحذير الذي يتكرر مئات المرات يتوقف الناس عن قراءته — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مكتوبة بصورة ثابتة لـCodex وتوجّه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق أساس العمليات الفرعية يعمم وصف «shim» على أداتي CLI مع أن ملف Grok التنفيذي هو ELF أصلي (والتبرير الصحيح هو «وكيل ينشئ عملياته الفرعية الخاصة»)؛ وُسِمت اثنتا عشرة ملاحظة SAST على `subprocess` بـ`# nosec` / `# nosemgrep` مع تبرير، إذ تجعل صيغة القائمة من دون `shell=True` الحقن مستحيلًا، ولا يمر محتوى المستند عبر argv مطلقًا.
    - **لم يعد أي سر يدخل إلى العمليات الفرعية الوكيلية.** لم تكن قوائم المنع الاسمية تحمي إلا ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). وأظهر القياس أن **سبعة أسرار أخرى** كانت تدخل إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح أداة CLI الأخرى، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة البيانات. غير أن أداتي CLI هاتين **وكيلان**، ويعمل وكيل Grok من دون sandbox لنظام التشغيل قابل للتطبيق على العديد من محطات Linux. تتم التصفية الآن **حسب نمط الاسم** (`API_KEY` و`_TOKEN` و`SECRET` و`PASSWORD` و`CREDENTIALS`) لا حسب قائمة اسمية، ما يشمل أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن يعرفها هذا الكود. ولا تحتاج أدوات CLI إلى أي منها: إذ توجد المصادقة داخل `~/.codex` و`~/.grok`، لا داخل البيئة مطلقًا — وقد جرى التحقق عبر **ترجمة فعلية مكتملة** باستخدام كل واحد من المزوّدين مع البيئة المحصّنة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يثبّت حصرية خيارات المزوّد، وعقد `stopReason`، وخطية التعبير النمطي للأخبار، ورسالة الرفض في CI، والحفظ المؤقت لـGemini، وغياب أي سر عن بيئة العمليات الفرعية. وهذا التأكيد الأخير **عام** — إذ يفشل عند وجود مفتاح لا تسميه أي قائمة — بينما كان اختبار التنقية القائم مرآةً لثابته، ولم يكن قادرًا على اكتشاف أي شيء سوى تعطل حلقته الخاصة. المجموعة الكاملة تضم **311 اختبارًا**.
  - **موفّران جديدان لـ Grok**: `--use_grok` ‏(واجهة API من xAI، المفتاح `XAI_API_KEY`، والفوترة حسب الاستخدام) و`--use_grok_cli` ‏(واجهة CLI الرسمية لـ Grok Build، ويُخصم استخدامها من اشتراك Grok — بالمبدأ نفسه الذي يعتمده `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن endpoint الخاص بـ xAI متوافق مع OpenAI، أُعيد استخدام العميل و`_call_openai` كما هما، ولم يتغير سوى `base_url`. لم يلزم سوى تعديل واحد يستفيد منه الجميع: بات `finish_reason` يقبل الآن `end_turn`، وهي الصيغة التي يصدرها xAI حيث يصدر OpenAI ‏`stop`. النماذج: `grok-4.6` ‏(الجودة) و`grok-4.3` ‏(الاقتصادي). تجدر الإشارة إلى أن نموذج Grok الاقتصادي يظل الأغلى في المستودع — $1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ `mistral-small-latest`: يُختار هذا الموفّر لتنوّع النماذج، لا للسعر.
    - **وضع CLI**: صُمّم على غرار Codex، مع أربعة اختلافات فرضها الواقع — يُمرَّر prompt عبر ملف (`--prompt-file`، إذ لا يقرأ CLI من stdin، كما أن تمرير مقطع عبر argv سيجعله ظاهرًا في `ps`)، ويكون الخرج كائن JSON واحدًا على stdout ‏(لا JSONL ولا ملف `-o`)، ولا يتيح الاشتراك سوى `grok-4.6` و`grok-4.5`، كما يتعذر تطبيق sandbox ‏(انظر أدناه). جرى توحيد تشغيل العمليات الفرعية مع Codex داخل `_codex_run_process`، من دون المساس ببقية موفّر Codex الذي سبق اختباره.
    - **`exit 0` لا يثبت شيئًا، وفقًا للقياس**: عندما لا تكون المصادقة مفعّلة، يكتب CLI ‏`{"type":"error","message":"Not signed in."}` إلى **stdout** مع رمز خروج **0**. ويتصرف الرفض أو تجاوز عدد الجولات بالطريقة نفسها. لذا يشترط عقد الخرج تحقق أربعة شروط في آن واحد: رمز خروج 0، وغياب payload خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع فحص preflight المنطق نفسه: ينتهي `grok models` بالرمز 0 حتى عند انقطاع الاتصال، ولا يمكن الاستنتاج إلا من وجود «غير مصادَق عليه» في stdout.
    - **العزل: تفاوت مقصود وموثّق.** بينما يعمل Codex داخل `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـ Grok على كثير من أجهزة Linux الحديثة لسببين نظاميين مستقلين لا يمكن تجاوزهما من دون `sudo`: يمنع AppArmor نطاقات أسماء المستخدمين غير المميّزة منذ Ubuntu 24.04 ‏(`bwrap: setting up uid map: Permission denied`، وقد أُعيد إنتاج المشكلة خارج Grok)، كما تفشل قائمة منع sockets الخاصة ببيئة تشغيل الحاويات عندما يكون `/run/podman` في `0700` ‏(لا يعالج resolver سوى `ErrorKind::NotFound`، فيما يصبح EACCES خطأً فادحًا). والمطبّ الأساسي هو أن profile **مدمجًا** يتعذر تطبيقه قد **يبدأ بصمت من دون عزل**. لذلك لا يطلب السكربت أي profile افتراضيًا ولا يتراجع بصمت أبدًا — بل يصدر تحذيرًا على stderr. وتعتمد الحماية على قواعد `--deny` الخاصة بـ CLI، بما فيها قاعدة catch-all ‏`*`، وهي الطبقة الوحيدة التي أثبت القياس أنها _تفشل في وضع مغلق_ (فوجود قاعدة ذات بادئة مجهولة يؤدي إلى رفض بدء التشغيل). يتيح `GROK_TRANSLATE_SANDBOX=read-only` اشتراطها، وعندئذ يفشل بدء التشغيل إذا تعذر على الجهاز الالتزام بها.
    - **ضوابط الأمان**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (إذ قد يؤدي وجود مفتاح إلى التحول نحو الفوترة حسب الاستخدام؛ كما أن وراثة `GROK_SANDBOX` ستفرض profile غير قابل للتطبيق مع رسالة مضللة)، وتُعطّل مفاتيح MCP/hooks/skills/agents، مع `--disable-web-search` و`--no-subagents` و`--no-plan` ومجلد عمل مؤقت ورفض التشغيل في CI ومهلة زمنية تقتل مجموعة العمليات وback-off عند بلوغ rate limit. ضُبط `--max-turns` على 6 لا على 1: إذ يُزاد العداد بعد جولة الأدوات، وستؤدي القيمة 1 إلى اقتطاع الخرج.
    - **الحصة**: مجمّع Grok أسبوعي و**مشترك مع Chat وImagine وVoice**، ولا يعرضه أي أمر — بخلاف Codex الذي يتيح `account/rateLimits/read` فيه قياس الاستهلاك. لذلك يحد `regen_translations.sh` التزامن إلى 2 ويصدر تحذيرًا صريحًا.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` ‏(24 اختبارًا). المجموعة الكاملة تضم **290 اختبارًا**.
  - **إصلاح خلل — لم تكن الاقتباسات الإنجليزية متعددة الفقرات محمية إلا جزئيًا (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل في متن الاقتباس إلا سلسلة من أسطر `>` **المتتالية**. وما إن يمتد اقتباس على عدة فقرات (تفصل بينها سطر `>` فارغ)، حتى تُلتقط الفقرة الأخيرة وحدها ويُستعاض عنها بالعنصر النائب؛ أما الفقرات السابقة فكانت تُرسل إلى LLM وتعود مترجمة — أي النقيض تمامًا مما وُجد `--news` لضمانه. بات التكرار يقبل الآن أسطر `>` الفارغة داخليًا، وأصبح غير جشع كي يتوقف عند `>` الفارغ السابق للسطر المكتوب بالمائل، لا عند أول سطر فارغ يصادفه.
    - **النطاق المقاس** على corpus حقيقي من 198 مقالًا: شملت المشكلة 11 اقتباسًا من أصل 419. لم يحدث أي تراجع — تلتقط regex الجديدة العدد نفسه تمامًا من الاقتباسات، ولا تتوسع إلا المتون متعددة الفقرات (408 متون متطابقة، و11 ممتدة)، ولا يزال من المتعذر ابتلاع سطر الإسناد `> — …` ضمن المتن (مع الإبقاء على lookahead).
    - **إثبات شامل من البداية إلى النهاية** على مقال بحجم 69 كيلوبايت مترجم إلى ja/ar: الفقرة الأولى من أحد الاقتباسات، التي كانت تُعرض سابقًا بصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم بالمثل إلى العربية، تظل الآن `> GLM-5.3 is now open-weight.`. وعاد عدد أسطر الاقتباس الإنجليزية من 9 إلى 10، وهو عددها في المصدر.
    - تجدر الإشارة إلى أن هذا الخلل لم ترصده أدوات التحقق اللاحقة، إذ تتحقق من وجود الاقتباس من دون التأكد من اكتماله.
  - **توفير مقاس لدى الموفّر الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ اسم النموذج بـ `gpt-5`، بما في ذلك عند استخدام `--eco`. وأظهر قياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: `medium` ← 45 reasoning tokens و65 output tokens؛ و`none` ← 0 و14. لا يضيف الاستدلال أي قيمة إلى الترجمة، ومع ذلك كانت تكلفته تُدفع عن كل مقطع في كل ملف. أصبحت القيمة الافتراضية `none` في `--eco`، وتظل `medium` في الحالات الأخرى؛ وتبقى للقيمة الصريحة الممررة عبر CLI الأولوية. بات `--reasoning_effort` يقبل الآن `none` و`xhigh` بالإضافة إلى `low`/`medium`/`high` ‏(لا تقبل جميع النماذج كل هذه القيم: فعلى سبيل المثال يرفض `gpt-5.4-mini` القيمة `minimal` — وتغطي إعادة المحاولة الحالية من دون هذا المَعلم تلك الحالة).
  - **تحديث SDK وترحيل Gemini**: استُبدل `google-generativeai` ‏(انتهى دعمه في 2025-11-30 وأُرشف مستودعه) بحزمة SDK الموحّدة **`google-genai`** — ‏`genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير system prompt في `system_instruction` بدلًا من إلحاقه بالمقطع. رُقّي `mistralai` إلى **2.9.4** (وأصبح import هو `from mistralai.client import Mistral`؛ أما القديم فيثير `ImportError`، وقد جرى التحقق من ذلك داخل wheel)، ورُقّي `anthropic` إلى **0.125.0** و`openai` إلى **2.54.0** — وهي آخر الإصدارات قبل الانتقال إلى `httpx2`، لتجنب تعايش مكدسين من HTTP داخل venv. وبناءً على ذلك أُتيح أيضًا `httpx` 0.28.1 و`pydantic` 2.13.5.
  - **تراجعان كشفتهما اختبارات حقيقية، لا الوثائق**:
    - يرفض `anthropic` بالإصدار ≥ 1.0 من جانب العميل استدعاءً غير متدفق يشير فيه `max_tokens` إلى مدة تتجاوز 10 دقائق (`ValueError: Streaming is required...`). لم يكن هذا الضابط موجودًا في 0.34.2، وكان يعطّل كل استدعاء لـ Claude يستخدم `max_tokens=32768`. أُصلح ذلك بإضافة `timeout` صريح (`CLAUDE_TIMEOUT`، والقيمة الافتراضية 900 ثانية)، مما يجنب الانتقال إلى streaming لاستدعاء لا تُستخدم منه سوى الاستجابة الكاملة.
    - لا يقبل `thinking_level="minimal"` سوى جزء من كتالوج Gemini: يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` بخطأ 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تسلسل `minimal` ← `low` ← من دون thinking_config، على غرار fallback الخاص بـ OpenAI الموجود أصلًا — إذ يجب ألا يؤدي مَعلم تحسين مطلقًا إلى فشل ترجمة.
  - **تجديد النماذج الافتراضية**، وقد جرى التحقق من كل منها عبر استدعاء حقيقي: OpenAI ‏`gpt-5.5` ← **`gpt-5.6-terra`** ‏(−60% على batch من 28) و`gpt-5.4-mini` ← **`gpt-5.6-luna`** ‏(−73%)؛ وClaude ‏`claude-sonnet-4-6` ← **`claude-sonnet-5`** ‏(أرخص وأحدث) و`claude-haiku-4-5-20251001` ← **`claude-haiku-4-5`** ‏(المعرّف القياسي من دون تاريخ)؛ وGemini ‏`gemini-3.1-pro-preview` ← **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` ← **`gemini-3.1-flash-lite`** ‏(إصدار مستقر وأرخص من `3.5-flash-lite`). بقي Mistral بلا تغيير، إذ يظل `mistral-large-latest` صاحب أفضل نسبة جودة إلى سعر بين الأربعة. تجدر الإشارة إلى أنه لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — فنموذج Gemini 3.5 Pro، الذي أُعلن عنه في مايو 2026، لم يصدر قط؛ وسلسلة 3.5/3.6/3.7 مخصصة بالكامل لـ Flash.
  - **اختبار A/B مقاس قبل تحويل Gemini**: ترجم `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. كانت البنية متطابقة تمامًا (21 قائمة، و18 كتلة code، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع URLs)، خلال **8 ثوانٍ مقابل 48 ثانية**. ونظرًا إلى عدم وجود أي benchmark عام يقارن هذين النموذجين في الترجمة أو السكربتات غير اللاتينية، لكان التحويل قائمًا على مجرد افتراض لولا ذلك.
  - **ترشيح كتل استجابة Claude**: كان `_call_claude` ينفذ `block.text for block in response.content` من دون ترشيح النوع. تُدرج النماذج ذات الاستدلال التكيّفي (Sonnet 5 وما بعده) كتلة `thinking` بين الكتل، وهي تعرض `.thinking` لا `.text` — مما كان سيعطّل الترجمة بسبب `AttributeError` مبهم عند أول مقطع. باتت كتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` مستبعدة الآن (قائمة سلبية للحفاظ على التسامح مع نوع مجهول يحمل نصًا)، وتؤدي أي استجابة لا تحتوي على كتلة نصية إلى خطأ صريح. يُمرر `thinking={"type": "disabled"}` مع كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: إزالة النماذج التي انقضى تاريخ سحبها (سلسلة `magistral-*` المسحوبة في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620`، و`claude-3-7-sonnet-20250219`، و`claude-opus-4-1-20250805`، و`claude-sonnet-4-20250514`). تصحيحات الحدود: Mistral ‏128K ← **256K** ‏(جيل Large 3 / Small 4)، وGemini ‏1 000 000 ← **1 048 576** ‏(حد الإدخال الفعلي)، و`claude-opus-4-5` ‏200K ← **1M**، وعائلة `gpt-5.6-*` ‏400K ← **1.05M**. إضافة Claude 5 ‏(`claude-sonnet-5`، و`claude-opus-5`، و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وسلسلة `ministral-*`. تجدر الإشارة إلى أن هذه الحدود تظل إرشادية، إذ يحد `translate()` التقسيم عند `min(16000, limite)`.
  - **Provider `--use_codex`**: خامس provider يتحكم في CLI الرسمي لـ Codex ‏(`codex exec`) في الوضع غير التفاعلي، بدلًا من استدعاء API تُحتسب تكلفتها حسب الاستخدام. تُخصم الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقًا. وهذا هو المسار الوحيد الذي توثقه OpenAI لهذا الاستخدام: إذ تُدرج مصفوفة التوافر حسب الخطة «Codex SDK و`codex exec` ومسارات العمل القابلة للبرمجة» على أنها متاحة في Plus/Pro/Business/Enterprise، بينما لا تُصادق رموز `~/.codex/auth.json` على استدعاءات API Platform (ولا يقرأها هذا السكربت مطلقًا — تظل المصادقة وتجديدها تحت إدارة CLI).
  - **ملف Codex التنفيذي قابل للتثبيت عبر pip، وليس عبر npm فقط بعد الآن**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK ‏`openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عام لاستخدام `--use_codex`. لا تُضاف الحزمة إلى `requirements.txt`: يبلغ حجم الملف التنفيذي نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين من أجل provider اختياري. جرى التحقق من البداية إلى النهاية: مع غياب `codex` عن `PATH`، يعثر الحل على الملف التنفيذي المضمّن في الحزمة وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: يُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. من دون هذا الإجراء الوقائي، قد يؤدي وجود مفتاح في `.env` إلى تحويل Codex إلى الفوترة حسب الاستخدام من دون أي إشارة ظاهرة — وهو بالضبط ما أُنشئ هذا provider لتجنبه.
  - **مصائد CLI المثبتة باختبارات**:
    - يقرأ `codex exec` الإدخال القياسي **حتى** عند تمرير الموجّه كوسيط: من دون إغلاق الإدخال القياسي، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج مطلقًا (أُعيد إنتاجه: خروج 124 بعد 180 ثانية، وصفر بايت). لذلك فإن `communicate(input=...)` إلزامي.
    - إن `codex` المثبّت عبر npm هو shim لـ Node يقوم عبر `spawn` بتشغيل الملف التنفيذي الحقيقي المكتوب بـ Rust: ويكون هذا الأخير **حفيدًا** لعملية Python ويظل حيًا بعد `SIGKILL` الخاص بـ `subprocess.run(timeout=)`، حيث سيواصل استهلاك الحصة. ومن هنا يأتي `Popen(start_new_session=True)` + `os.killpg`.
    - قد يخرج CLI بالرمز 0 رغم إصداره `turn.failed`: لذلك يُفحص خرج JSONL ‏(`--json`) بالإضافة إلى رمز الإرجاع، ويؤدي غياب الملف `-o` مع الرمز 0 إلى رفع خطأ صريح بدلًا من إنتاج مقطع فارغ.
  - **التراجع التدريجي عند تجاوز حد المعدل**: لا يطبّق CLI أي إعادة محاولة داخلية (`max_retries = 0`). يجري التصنيف استنادًا إلى بنية حمولة JSON ‏(`status: 429` / `error.type`)، وليس إلى سلاسل فرعية — إذ تظهر كلمة «الحصة» في خطأ 429 قابل للاسترداد كما تظهر في `insufficient_quota` نهائي.
  - **إجراء وقائي لـ CI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفًا. فالمصادقة عبر الاشتراك غير مخصصة لـ runner مشترك، كما أن OpenAI تنصح صراحةً بعدم استخدام مسار العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` ‏(`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وAPI Platform، لكن حساب ChatGPT لا يملك صلاحية الوصول إلى جميع نماذجها: تُطبّق قائمة السماح من جهة الخادم، من دون تحقق محلي، ويؤدي اختيار نموذج غير معتاد إلى إطلاق تحذير. في خطة Plus، يتيح Luna ما بين 250 و2,000 رسالة لكل نافذة مدتها 5 ساعات، مقابل 10 إلى 100 لـ Sol: ويُعد `--eco` الوضع الموصى به لأي معالجة دفعية.
  - **إصلاح خلل — كان `regen_translations.sh` ينتهي بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` تابع لـ `main()` لم يعد موجودًا عند تنفيذ trap. تحت `set -u`، كان ذلك يطلق `failed_log: unbound variable` ويجعل السكربت يخرج بالرمز 1 رغم صحة الترجمات الـ28 — وهو ما كان سيوقف `release.sh --auto` ‏(`set -e`) مباشرة بعد إعادة التوليد، في أكثر المراحل تكلفة. أصبح المتغير عامًا، وصار trap يتحقق من وجوده. أثر جانبي مفيد: عادت إخفاقات الترجمة الحقيقية، التي كان هذا الخطأ يخفيها سابقًا، إلى الظهور في الملخص الختامي.
  - **`REGEN_MODEL`**: متغير بيئة جديد لـ `regen_translations.sh` يفرض نموذجًا محددًا بدل النموذج الافتراضي للـ provider، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج المتقدم ضمن حصة الاشتراك بدل نموذج `--eco` الموجّه للحجم.
  - **`regen_translations.sh`**: يتوافر `REGEN_PROVIDER=codex` عبر اشتراك اختياري صريح (ولا يُكتشف تلقائيًا مطلقًا، تجنبًا لاستهلاك حصة الاشتراك دون علم المستخدم). يُجدّد الرمز مرة واحدة تسلسليًا قبل بدء التوازي — إذ إن تجديد Codex تدويري وأحادي الاستخدام، وستؤدي المهام المتزامنة إلى إبطال جلسة `codex login` — ويُخفض التزامن إلى 4.
  - **إعادة الهيكلة المرتبطة**: ينتقل `_dispatch_provider_call` من 8 معاملات إلى 6 عبر `_resolve_provider()` يعيد اسم provider، بدلًا من تمرير قيمة منطقية رابعة عبر السلسلة بأكملها. وتظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` بالحد الأدنى.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` ‏(48 اختبارًا) يغطي argv، والبيئة المنقحة، وعقد منع المقدمة، والإخفاقات الصامتة، والمهلة الزمنية/killpg، والتراجع التدريجي، والفحص المسبق، وحلّ provider، وتسلسل الاستدلال في Gemini، وتصفية كتل Claude، واستشهادات الأخبار متعددة الفقرات. أصبحت المجموعة الكاملة تضم 290 اختبارًا.
  - **التحقق الفعلي**: ينتج عن ترجمة `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex بنية مطابقة تمامًا للترجمات المرجعية (14 كتلة برمجية، و24 عنوانًا، و25 سطر جدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل برمجية متطابقة حرفًا بحرف، ومن دون أي بقايا لعناصر نائبة). وفي مقال صحفي حجمه 69 كيلوبايت بوضع `--news`، يجتاز خرجا `gpt-5.6-luna` و`gpt-5.6-sol` كلاهما أداة التحقق التطبيقية اللاحقة للغات en/ja/ar. أما الاستهلاك المقاس عبر `account/rateLimits/read` فقد ظل دون عتبة تقريب العداد (0% من نافذة الساعات الخمس) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL لإسناد الأخبار ذي الأقواس المتداخلة أو البادئة الفرنسية (2026-05-11):

  - **إصلاح خلل**: كان استخراج عناوين URL الخاصة بالإسناد في `_protect_news_quotes` يستخدم التعبير النمطي `re.search(r"\((.+?)\)", attribution)` (التقاطًا كسولًا بين قوسين). وفي إسناد من نوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` محيط + `]()` لرابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة تتضمن البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائية). النتيجة: كان `_validate_news_post` يبحث عن هذه السلسلة في الخرج المترجم ويفشل دائمًا (لسببين: بتر `)` + ترجمة "relayé par" إلى `relayed by`/`weitergeleitet von`/...). ولم يكن التسلسل الكامل low ← medium ← high ← gpt-5.5 قادرًا على النجاح.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — فهو يستهدف تحديدًا `](url)` الخاص برابط Markdown، ويلتقط **عنوان URL الصافي فقط** (من دون البادئة الفرنسية أو البتر)، وهو ثابت تحافظ عليه العناصر النائبة `#URL{N}#` أثناء الترجمة. ويتعامل بمتانة مع النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس محيطة
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py` ضمن الفئة `TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة المطابقة لخلل Genspark CEO E2B الذي أُعيد إنتاجه)
    - `test_extract_attribution_url_with_french_prefix` (صيغة مختلفة تحتوي على `via`)
  - **فجوة التغطية**: يتحقق `check-editorial-coverage.py` من صحة البنية التحريرية، لكنه لا يتحقق من قابلية ترجمتها بواسطة translator. ومن التحسينات الممكنة (خارج نطاق v1.9.2) إضافة فحص يحاكي استخراج الإسناد بوضع dry-run لاكتشاف الأنماط الخطرة **قبل** النشر.

- **1.9.1** إصلاح i18n لتسمية CTA في ملاحظة علامة الترجمة (2026-05-10):

  - **إصلاح خلل**: كانت تسمية `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط العلامة أعلى الملفات المترجمة تظل **بالفرنسية** لجميع اللغات المستهدفة، بدلًا من اتباع `target_lang`. لا يراها LLM مطلقًا (إذ تُركّب في جهة Python للحفاظ على عنوان URL وslug المستودع)، ولذلك لم تكن مرحلة الترجمة قادرة على تداركها. وهذا تراجع صامت منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ15 بتسمياتها المحلية. وأصبح `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` يمرران اللغة المستهدفة. ويُستخدم `fr` كقيمة احتياطية إذا كانت اللغة مجهولة (للأمان ومنع KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link` ‏(target_lang ‏`ja` ← التسمية اليابانية المتوقعة). وأُضيف اختباران جديدان: `test_source_link_label_localized_per_target_lang` (معلّم على 7 لغات تغطي الكتابات اللاتينية والتصويرية والأبجدية الصامتة) و`test_source_link_label_falls_back_to_french_for_unknown_target`. المجموع: 40 اختبارًا في `test_translation_note_position.py` (بدلًا من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع بقيمة افتراضية `target_lang="fr"` — تستمر الجهات الخارجية التي تستدعيه برمجيًا من دون `args.target_lang` في العمل دون تعديل.
- **1.9** إصلاح الفشل الصامت + أدوات جودة متكاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + تنسيق العلامة "embed card"**:
    - خيارات CLI جديدة (إضافية، الإعدادات الافتراضية دون تغيير ← **غير كاسرة للتوافق**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): تضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين معًا.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - يعيد `legacy` إنتاج سلوك v1.8 بدقة (فقرة بخط عريض `**…**`) **بايتًا ببايت**.
        - يُصدر `marker` تعريف مرجع رابط Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`) يتبعه **اقتباس كتلي من 3 فقرات** منظم ليُعرض كـ "GitHub repo embed card": عنوان المشروع ضمن كود مضمّن (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف مترجم بواسطة LLM، ورابط دعوة إلى اتخاذ إجراء (`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. يمكن استثماره في أثناء البناء عبر إضافة remark (راجع مدونة jls42.org ← الإضافة `remark-translation-banner`).
    - **ثوابت لا تُرسل مطلقًا إلى LLM**: يُجمَّع عنوان المستودع وعنوان URL الخاص بـ GitHub في جانب Python بعد ترجمة العبارة الوصفية. لا يرى LLM مطلقًا المعرّف النصي `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تغيير أي عارض أو حالة أحرف أو مخطط.
    - **إدراج واعٍ بالـ frontmatter**: في وضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة الإغلاق `---`** الخاصة بـ YAML frontmatter (لضمان سلامة Astro Content Collections / gray-matter). تكتشف الدالة المساعدة `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف وتحافظ على سلامته؛ كما **ترفع `RuntimeError`** عند وجود frontmatter مفتوح بلا سياج إغلاق (فيظهر الملف ضمن `failed_files` بدلًا من كتابته مع ملاحظة موضوعة في مكان خاطئ).
    - **منقّي النموذج بالقائمة البيضاء**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع الرجوع إلى `unknown` إذا كانت النتيجة فارغة. يتوافق ذلك مع أداة التحقق في إضافة remark الخاصة بـ Astro ويحيّد المحارف التي قد تكسر تنسيق العلامة (المسافة، وعلامة الاقتباس، والقوس، والفاصلة، وما إلى ذلك).
    - **إعادة هيكلة داخلية**: `_append_translation_note` (دالة واحدة متجانسة) ← 7 دوال مساعدة صافية (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فُصل المنشئ عن المُركِّب (يعيد المنشئ كتلة صافية بلا فاصل، بينما يطبق المُركِّب `\n\n` وفقًا للموضع)؛ ويشترك كود الإنتاج والدالة المساعدة المصدرية في المُجمِّع نفسه ذي الفقرات الثلاث.
    - **حفاظ `_quote_lines` على الأسطر الفارغة**: يضيف `> ` إلى بداية كل سطر، محولًا الأسطر الفارغة إلى `>` وحده. يتيح ذلك لـ mdast رؤية 3 فقرات منفصلة داخل الاقتباس الكتلي (العنوان / الوصف / الرابط) بدلًا من فقرة واحدة ذات فواصل أسطر.
    - **`_build_translation_note_block` تكيّفي**: وفقًا لعدد الفقرات التي حافظ عليها LLM (3 = تنسيق البطاقة الكامل، 2 = عبارة + رابط، 1 = خيار احتياطي). لم يعد الخيار الاحتياطي ذو الفقرة الواحدة **يغلّف المحتوى داخل `**...**`** عند اكتشاف رابط Markdown من النوع `](` (إذ يكون عرض `<strong>` حول الرابط هشًا).
    - **توافق تصاعدي**: `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` في `_compose_with_notes` — تستمر كائنات Namespace التي لا تحتوي على هاتين السمتين (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) في العمل دون تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور حرفيًا على مقتطف من المصدر) + طبقة احتمالية (`langdetect`)
    - القائمة البيضاء `finish_reason` / `stop_reason`: رفع `RuntimeError` عند أي حالة غير مدرجة في القائمة البيضاء (الاقتطاع، وcontent_filter، وما إلى ذلك)
    - `max_tokens` في Claude: `4096` ← `32768` (يتجنب الاقتطاع الكامن في المقاطع ذات 16k، مع هامش للتحويل بين أنظمة الكتابة FR→JA/ZH/KO/AR/HI)
    - تجزئة واعية بالعناوين: إعطاء الأولوية لـ H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى رمز خروج غير صفري: يعيد `translate_markdown_file` حالة ذات نوع محدد `success` / `failure` / `skipped`، وينفذ `main()` الأمر `sys.exit(1)` إذا فشل ملف واحد على الأقل (سواء لملف منفرد أو دفعة)
    - حاجز للمحتوى الفارغ لدى جميع المزوّدين، وفحص منطقي لنسبة المصدر إلى المخرجات (≥ 500 محرف، وأقل من 5% = رفض)، والتحقق من العناصر النائبة للكود (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، وإعادة محاولة `BadRequestError` من دون `reasoning_effort`
    - إضافة الاعتمادية `langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("بنهج EurekAI المتكامل"، 14 خطافًا):
    - Pre-commit: ‏ruff (التدقيق+التنسيق)، وshellcheck، وprettier (md/yaml/json)، وdetect-secrets (حماية 4 مفاتيح API)، وLizard ‏(CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، ونهاية الملف، والملفات الكبيرة، وأسطر shebang، وما إلى ذلك)
    - Pre-push: ‏mypy (وضع متساهل تدريجي)، وOpengrep SAST ‏(translate.py + scripts/)، وpip-audit (وضع الإبلاغ الأولي)، وunittest discover ‏(tests/ + scripts/tests/)
    - أغلفة محلية ضمن `scripts/` تستخدم `./venv/bin/python`
    - `scripts/audit_verdict.py`: محلل JSON لـ pip-audit مع 11 اختبارًا باستخدام unittest، وهو نقل إلى Python لمحلل jls42-astro
    - إصلاح 7 مخالفات أولية من ruff: ‏B904 ‏(raise from) ×2، وB007 ‏(dirs غير مستخدمة)، وC408 ‏(dict literal)، وC419 ‏(list-comp)، وSIM105 ‏(contextlib.suppress)، وSIM110 ‏(any())
    - يستثني Lizard مؤقتًا `translate.py` (4 دوال بقيم CCN بين 21 و47، وإعادة الهيكلة مخطط لها) — بوابة صارمة على scripts/
  - **SonarCloud + تغطية شاملة**:
    - سير عمل GitHub Actions ‏`SonarCloud` ‏(sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع قياس التغطية عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى README ‏(Quality Gate، وتصنيفات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code)
    - `tests/test_silent_failure.py` ‏(`unittest` من المكتبة القياسية): يغطي الحلقات الست في سلسلة أخطاء الفشل الصامت
    - `tests/test_orchestration.py` ‏(+79 اختبارًا): يغطي طبقة التنسيق في `translate.py` ‏(`_resolve_*_filename`، و`_existing_translation_exists`، و`_record_translation_status`، و`_write_output_file`، و`translate_directory`، و`_validate_input_paths`، و`_init_*_client`، و`_select_provider_client`، و`_normalize_collapsed_markdown`، و`_cleanup_source_flag`، و`_validate_news_flags_*`، والخيارات الاحتياطية `_openai_create_with_fallback` لأخطاء TypeError + BadRequestError، وتنسيق موجّه سلسلة o1، وفروع العودة المبكرة في `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` ‏(stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **التغطية على الكود الجديد**: 75.5% ← نحو 98% ‏(translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك اختبارات E2E ‏`marker+top|bottom|both` و`legacy+top|bottom|both`)، وإضافة البادئات إلى أسطر متعددة، والتوافق الرجعي بايتًا ببايت (قيمة مرجعية حرفية)، والمنقّي، وتقسيم frontmatter (بما في ذلك رفع خطأ عند عدم إغلاق السياج)، وتنسيق الفقرات الثلاث، والخيار الاحتياطي ذي الفقرتين، وحاجز الفقرة الواحدة + رابط Markdown، وحاجز أمان حاسم `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان+عنوان URL لا يُرسلان مطلقًا إلى LLM. **نجح 190 اختبارًا**، بلا أي تراجع.
  - التوثيق: `README.md` ‏(بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` ‏(سير عمل pre-commit + مراقبة CI مفصلة)، وإعادة توليد 28 ترجمة
- **1.8** وضع `--news` + ترقية نماذج 2026 ‏(2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - جودة OpenAI: ‏`gpt-5` ← `gpt-5.4`
    - OpenAI الاقتصادي: `gpt-5-mini` ← `gpt-5.4-mini`
    - جودة Gemini: ‏`gemini-3-pro-preview` ← `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4`، و`gpt-5.4-mini`، و`gpt-5.4-nano` ‏(400k)، و`gemini-3.1-pro-preview` ‏(1M)
  - وضع `--news` الأولي: حماية الاقتباسات الإنجليزية بعناصر نائبة `#NEWSQUOTE\d+#`، وتعيين `LANG_FLAGS` ‏(15 لغة)، وإدارة الأعلام وفقًا للغة الهدف
  - التحقق من العناصر النائبة للأخبار قبل الاستعادة (تراجع: كان حذف LLM للعنصر النائب ينتج بصمت مخرجات بلا اقتباس)
  - جعل السكربت `regen_translations.sh` قابلًا للنقل (مسارات مطلقة، بلا اعتماد على pwd)
  - إضافة رابط اللغة الفرنسية إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة
- **1.7** الميزات الجديدة:
  - خيار `--keep_filename` للاحتفاظ باسم الملف الأصلي عند الترجمة
  - دعم ملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على الكود المضمّن**: أصبحت علامات backticks ‏(`` `...` ``) محمية الآن في أثناء الترجمة
  - تحسين موجّه النظام:
    - معالجة أفضل لعلامات الاقتباس في YAML frontmatter
    - حماية متغيرات القالب `{variable}`
    - منع ملاحظات المترجم غير المطلوبة
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** الميزات الجديدة:
  - دعم Google Gemini API للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI: ‏`gpt-5` (للجودة)، و`gpt-5-mini` (اقتصادي)
    - Claude: ‏`claude-sonnet-4-5` (للجودة)، و`claude-haiku-4-5` (اقتصادي)
    - Gemini: ‏`gemini-3-pro-preview` (للجودة)، و`gemini-3-flash-preview` (اقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف واحد (`--file`) من دون استعراض مجلد
  - نمط تسمية جديد ومبسّط: `{base}-{lang}.md`
  - خيار `--include_model` للاحتفاظ بالتنسيق القديم المتضمن اسم النموذج
  - دعم النماذج غير المدرجة مع حد افتراضي للرموز (128k)
  - ترجمة README إلى 14 لغة
- **1.5** التحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين موجّهات الترجمة:**
    - أُثريت موجّهات الترجمات المباشرة وملاحظات الترجمة لتحقيق وضوح وكفاءة أفضل، بما يشمل تعليمات مفصلة حول الحفاظ على البيانات الوصفية وعناصر التنسيق المحددة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم عمليات الاستيراد لتحسين قابلية القراءة والصيانة.
    - تحسين تجزئة النصوص وإدارة كتل الكود للحفاظ على التنسيق الأصلي في أثناء الترجمة.
  - **إدارة ملفات المخرجات:**
    - عكس ترتيب النموذج واللغة في اسم ملفات المخرجات (على سبيل المثال، `f"{base}-{args.target_lang}-{args.model}.md"`)، ما يسهّل تنظيم الترجمات والعثور عليها.
  - **تحسينات متنوعة:**
    - تنظيف الكود بإزالة الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكربت وقابليته للقراءة.
- **1.4** الميزات الجديدة:
  - دعم Claude API من Anthropic للترجمة
  - تحسين الموجّهات لزيادة الوضوح والكفاءة
  - تعديلات طفيفة لتحسين قابلية صيانة الكود
- **1.3** التحسينات والميزات الجديدة:
  - تحسين إدارة كتل الكود
  - تحسين إدارة ملفات المخرجات
  - تحسين اكتشاف الملفات الموجودة
  - خيار `--force` لفرض الترجمة
  - عكس ترتيب النموذج واللغة في اسم ملف المخرجات
- **1.2** إصلاح سجل التغييرات
- **1.1** إضافة دعم Mistral AI API
- **1.0** الإصدار الأولي - دعم OpenAI API

**مقال مترجم من الفرنسية إلى العربية باستخدام gpt-5.6-sol.**
