### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.11.1** إصلاح في التوثيق: يعلن README أخيرًا عن مسارات provider السبعة (2026-09-03) :

  - **كانت صفحة PyPI للإصدار 1.11.0 تقول «4 واجهات API + CLI Codex».** لكن الكود يعرّض سبعة مسارات — OpenAI وMistral وClaude وGemini وGrok عبر API؛ وCodex (ChatGPT) وGrok عبر الاشتراك، من دون فوترة بحسب الاستخدام. كان نمطا Grok غائبين من المقدمة ومن نقطة _Multi-Provider_، وكانت الترجمات الأربع عشرة تكرر الخطأ. وبما أن الوصف الطويل للحزمة يُجمَّد بحسب الإصدار، كان تصحيح الواجهة يتطلب رقم إصدار جديد: وهذا هو السبب الوحيد لوجود هذا الإصدار. **لا تغيير في الكود.**
  - يتوافق `CLAUDE.md` مع ما أضافه الإصدار المنشور: عدّادات البوابة (16، و17 في `--full`)، وسير العمل الأحد عشر النشطًا، وعدادا Sonar/Codacy غير المرئيين في `gh pr checks` (hotspots وCodacy API)، ونقل `# nosemgrep` واحد لكل `ruff-format`، وبيئات GitHub المطلوبة لتبادل OIDC، وحقيقة أن _pending publisher_ لا يحجز الاسم.

- **1.11.0** النشر على PyPI: `pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-09-03) :

  - **أصبح السكربت أحادي الملف حزمة قابلة للتثبيت.** انتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول لوحدة التحكم `aipmt` وما يعادلها `python -m aipmt`. يظل استنساخ المستودع ضروريًا للمساهمة — إذ توجد فيه الاختبارات والترجمات الـ28 وأدوات الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` مطلقًا**، لأن التصادم حقيقي وصامت. فحزمة PyPI `translate` (الإصدار v3.8.1، وآخر رفع في 2026-07-06) تثبّت دليلاً بالاسم نفسه. عند إعادة الإنتاج في venv: يتغلب الدليل على الوحدة، ويختفي `translate.main`، وتتعطل نقطة الدخول عند `AttributeError` — بينما يعرض `pip check` الرسالة «No broken requirements found» مع rc=0. كان مجرد `pip install translate` لدى المستخدم كافيًا لتعطيل CLI من دون تشخيص مفيد. واختبار مضاد على wheel الحقيقي: `pip install translate` فوق الحزمة، و`aipmt --help` بقيمة rc=0 قبل وبعد، مع تعايش CLIَين.
    - **اسم توزيع طويل، وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للعثور عليها عبر بحث PyPI؛ أما اختصار وحده فلن يجده من لا يعرف المشروع مسبقًا، مع أن هدف النشر تحديدًا هو أن يُعثر عليه. استُبعد مرشحان محتملان بعد التحقق: `ai-markdown-translator` محجوز على npm منذ 2024 لأداة تؤدي الغرض نفسه، وتسبق هذا المستودع بـ17 شهرًا؛ و`aimt` لا يختلف إلا بحرف واحد عن `aim` (v3.29.1)، وهي حزمة نشطة في المجال نفسه — أسوأ إعداد لالتباس مستمر. ملاحظة منهجية مهمة: يعيد `pypi.org/project/<nom>/` الحالة 200 لأي اسم (صفحة مضادة للروبوتات)، ولا يُعتد إلا بواجهة JSON.
    - **التخطيط `src/` بدلًا من حزمة مسطحة.** كانت الحزمة المسطحة ستحافظ على `sys.path.insert(..., "..")` الستة الخاصة بالاختبارات، وهذا هو العيب تحديدًا: فهي تجعل الاستيراد يتم من شجرة المصدر بدلًا من الحزمة، ما سيخفي أي خطأ في التغليف. والتكلفة الفعلية: قاعدة استبدال إضافية.

  - **أصبح بالإمكان إعداد المفاتيح مرة واحدة وإلى الأبد.** لم يكن لدى CLI المثبّت أي إعداد **مستمر**: لم يبقَ سوى متغير البيئة و`.env` في الدليل الحالي. صحيح أن `find_dotenv` يصعد حتى جذر النظام، ولذلك كان يعثر على `~/.env` **عند العمل من الدليل الشخصي**، لكنه لم يكن يعثر على شيء عند العمل من مكان آخر — تغطية تعتمد على موضع تشغيل الأمر، لا على خيار تصميمي. لذلك أُضيفت طبقة ثالثة: `~/.config/aipmt/.env`، تحت الطبقتين الموجودتين.

    - **الأولوية غير مشفّرة صراحةً**، بل تنتج من `override=False`، وهي القيمة الافتراضية لـ `load_dotenv`: كل طبقة لا تملأ إلا ما تركته الطبقة السابقة فارغًا. ومن هنا يأتي الترتيب: متغير البيئة ← `.env` الخاص بالمشروع ← إعداد المستخدم، وقد جرى التحقق منه باختبارات **السلوك** لا البنية — إذ يؤدي عكس ترتيب الاستدعاءين إلى فشل الاختبارات، وكذلك إزالة الطبقة الثالثة.
    - **تنسيق `.env` وليس TOML**، عن قصد: فـ`python-dotenv` اعتماد موجود أصلًا، والصياغة موثقة بالفعل في ملفات README الخمسة عشر، والملف نفسه يخدم النطاقين. لا اعتماد أو صياغة جديدة. يتبع الموضع `XDG_CONFIG_HOME` إذا كان **مطلقًا** — إذ تطلب المواصفة تجاهل قيمة نسبية، وإلا فسيعود موضع الإعداد معتمدًا على الدليل الحالي — و`APPDATA` على Windows.
    - **استُبعد خياران، مع سبب استبعادهما.** مخزن مفاتيح النظام (`keyring`) أكثر أمانًا على جهاز مكتبي، لكنه يفشل في headless — خادم أو حاوية أو CI — أي حالة الاستخدام نفسها للترجمة الدفعية؛ لذا فهو مرشح جيد كخيار opt-in، لكنه افتراضي سيئ. أما flag `--api-key` فسيضع المفتاح في سجل shell ويجعله ظاهرًا في `ps`.
    - **من دون مفتاح، لا مزيد من أثر الاستدعاء.** كان المستخدم يتلقى مكدس Python يشير إلى `site-packages`، ورسالة تذكر «البيئة أو .env» من دون أن توضح أين يُنشأ الخيار الثاني. وهو يسرد الآن المواضع الثلاثة مع مسار كل منها بدقة، ويخرج الأمر بالحالة 2. والشبكة **ضيقة عن قصد**: يحيط `except ValueError` بمرحلة الإعداد وحدها. أما تغليف التنفيذ كله فسيحوّل خطأ حقيقيًا يحدث أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الفشل الذي يتعقبه هذا المستودع. ويقرأ اختبار مصدر `main()` لمنع ذلك.

  - **إصلاح — كان `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** لا يصعد `load_dotenv()` من دون وسيط من الدليل الحالي، بل من ملف **المستدعي**، أي من `site-packages`. وقد قيس ذلك باستخدام نقطة دخول فعلية لوحدة التحكم، شُغّلت من مشروع يملك `.env` خاصًا به: يعيد `find_dotenv()` القيمة `''` ولا يُحمّل المفتاح، في حين أن `find_dotenv(usecwd=True)` يعثر عليه. لم يكن الخطأ موجودًا ما دامت الأداة تعمل من المستودع المستنسخ فقط؛ لكن النشر كان سيجعله منهجيًا، مع عرض وحيد هو أن مفتاح API «مفقود» رغم صحة الإعداد.

  - **كانت ثلاث بوابات ستتحول إلى الأخضر بعد أن توقفت عن التحقق من أي شيء.** وقد جرى تشديدها **قبل** النقل، عمدًا: فحاجز الحماية المكتوب بعد التغيير الذي يفترض أن يلتقطه لا يثبت شيئًا. كل بوابة خضراء على المستودع الأصلي وحمراء على نسخة منقولة — وقد قيس الاتجاهان.

    - **يتجاهل Lizard مسارًا غير موجود من دون قول شيء**: rc=0، و«0 file analyzed». وكانت بوابة التعقيد ستنتقل من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع خرج حجمه صفر بايت. وأصبح النطاق الآن مصفوفةً يجري التحقق من وجود كل مدخل فيها.
    - **لا يفشل `coverage run --source=` عند غياب وحدة**: تحذير على stderr وحده، وrc=0 لكل من unittest و`coverage xml`، مع نشر التقرير رغم ذلك — مبتورًا من 1453 إلى 141 تعليمة. وكان المشروع سيبدو سليمًا لأنه لم يعد يُحلَّل تقريبًا. وتحافظ على التقرير حدّان أدنى: الإجمالي، وأكبر ملف جرى قياسه.
    - **إن مسبار حداثة الترجمات أعمى بنيويًا عن شكل الاستدعاء**: فهو يرتكز على flags الخاصة بـargparse، وهي بالضبط الأشياء التي لا يغيّرها إعادة تسمية ملف. وقد أُعيد إنتاج الحالة: نُقلت الوحدة، وظلت ملفات README الخمسة عشر توثق أمرًا غير موجود، فكانت النتيجة «لا توجد ترجمة قديمة». لذلك يتحقق قسم سابع من **الشكل** لا الخيارات، وتُقارن إضافة Lizard بالنطاق الفعلي للسكربت — إذ إن مفتاحها `files:` لا يجعل pre-commit يفشل عندما لا يعود يطابق شيئًا، بل يجعله **يتخطاه**.

  - **لم يعد `requires-python = ">=3.10"` مجرد ادعاء.** كان `sonar-project.properties` يعلن بالفعل عن 3.10-3.12 من دون أن يختبر ذلك أي شيء، لأن جهاز التطوير لا يحتوي إلا على 3.12 — وهو تناقض داخلي كان النشر سيجعله علنيًا. وينفذ سير عمل الاختبارات الآن المجموعة على 3.10 و3.11 و3.12، مع تثبيت **الحزمة**، وبالتالي اختبار حدودها العامة.

  - **حدود دنيا، من دون سقف أعلى.** يظل `requirements.txt` هو القفل المختبَر، بينما يصبح `[project.dependencies]` العقد العام: فنشر الإصدارات الدقيقة للقفل سيسبب تعارضات لدى كل مستخدم لديه حزم أخرى. ولا يوجد سقف `<N+1` أيضًا — إذ سيتعارض مباشرةً مع `check-deps-fresh.sh`، الذي يجعل بوابة الإصدار تفشل عند أي تأخر في الإصدار الرئيسي. تحل مجموعة الحدود الدنيا المشكلة، ويُظهر الاختبار المضاد `openai==1.0.0` قيمة `ResolutionImpossible`، ما يثبت أن الفحص يميّز بدلًا من قبول كل شيء. كما يمنع حاجز آخر إصدار `pyproject.toml` من الاختلاف عن إصدار CHANGELOG: فلا يسمح PyPI بإعادة استخدام رقم إصدار.

  - **تم التحقق من البداية إلى النهاية في venv جديد**: wheel بحجم يقارب 70 كيلوبايت لا يحتوي إلا على `aipmt/*.py` وdist-info والترخيص؛ و`aipmt --help` بقيمة rc=0 مع 22 flag؛ و`python -m aipmt` الذي يعرض «usage: aipmt» لا «usage: \_\_main\_\_.py»؛ وتثبيت `pipx` يعمل؛ والأهم **ترجمة فعلية من fr→en من أي دليل مستخدم**، مع الحفاظ على الخط العريض والقائمة والكود المضمّن والرابط وعنوان URL، ومن دون ترجمة كتلة الكود. وقد نجحت الاختبارات الـ318 السابقة للنقل بقائمة معرّفات مطابقة تمامًا على مستوى البايت قبل النقل وبعده — وهذا ما يثبت أن أي اختبار لم يُعطّل، لا كلمة «OK»؛ وأُضيف إليها اثنا عشر اختبارًا لإعداد الطبقات الثلاث، ليصبح المجموع 330.

- **1.10.0** مزود `--use_codex` (حصة اشتراك ChatGPT)، وتحديث SDKs والنماذج، وإصلاح اقتباسات الأخبار متعددة الفقرات (2026-08-29) :

  - **مراجعة أمنية — حاجزا حماية أضافتهما PR من دون تطبيقهما في كل المواضع** :

    - **كان preflight الخاص بـCodex يمرر كل `.env` إلى الملف التنفيذي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: فكانت العملية الفرعية ترث `os.environ` كاملًا، وبالتالي كامل `.env` الذي حمّله `load_dotenv`. وبالقياس باستخدام ملف تنفيذي زائف مزود بأداة رصد: وصلت **سبعة أسرار** إلى preflight — مفاتيح providers الستة إضافة إلى `GITHUB_TOKEN` — مقابل **صفر** في نظيره `_grok_preflight`، الذي كان يمرر `env=_grok_env()` كما ينبغي. وكان التناقض داخليًا في PR: فـ`_strip_secret_env` موجود تحديدًا للحفاظ على هذا الثابت، وعلى بضع سطور فقط. استُخرج `_codex_env_base()` واحد ومشاركته بين المسارين؛ وبعد الإصلاح أظهر القياس: صفر أسرار في كلا الجانبين.
    - **لم تكن خاصية «`--deny` fail-closed» تغطي الصيغة المستخدمة.** كان التعليق يبرر احتواء Grok بالكامل بحجة أن قاعدة ذات بادئة غير معروفة ترفض بدء التشغيل. لكن القياس على `grok 1.0.13` أظهر أن هذا التحقق موجود **فقط للصيغة بين القوسين**: يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل («unknown tool prefix»)، بينما تُقبل `--deny 'CeciNestPasUnOutil'` بصمت. وكان `GROK_DENY_RULES` يستخدم أسماء عارية فقط — لذا كان تغيير اسم أداة من جانب xAI سيزيل طبقة الاحتواء الوحيدة التي جرى قياسها، من دون أي إشارة، على جهاز لا ينطبق فيه أصلًا sandbox نظام التشغيل. تمر القواعد المسماة الثماني في `Prefix(*)`، وقد جرى التحقق من كل واحدة منها بوصفها بادئة معروفة في CLI؛ أما `*` الشامل فيبقى بصيغته الحرفية، وهي الصيغة المقبولة الوحيدة. ويمنع اختبار العودة إلى الصيغة غير المتحقق منها.
    - **تم التحقق من سلامة بقية الجوانب**: لا حقن للأوامر (صيغة قائمة في كل مكان، وليس `shell=True` مطلقًا، ومحتوى المستند عبر stdin أو `--prompt-file`)، ولا إلغاء تسلسل غير آمن (`json.loads` فقط، مع حارس نوع)، وإصلاح لاجتياز المسار من دون العثور على تجاوز في سبع حمولات، وتطبيق `--deny '*'` فعليًا من جانب CLI (`DENY_ENFORCED` لوحظ عند القراءة خارج workdir).
    - كان فحص حداثة الحزم المضاف أعلاه يتجاوز مبدؤه هو أيضًا: إذ كان يتخطى بصمت حزمةً تفشل استعلاماتها إلى PyPI، فتبقى البوابة خضراء. وهو يحسب الآن الحزم التي قورنت فعليًا ويفشل إذا كانت التغطية غير مكتملة.

  - **تحديث التبعيات، مع شبكتَي حماية لمنع تكرار التأخر** :

    - **كان التأخر حقيقيًا ومستمرًا**: انتقل `openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر عامين في مخزن شهادات الجذر الذي يتحقق من TLS لجميع استدعاءات providers. وقد حُدد السبب: **لم يكن هناك `.github/dependabot.yml`**. ومن دون هذا الملف، لا يفعّل GitHub إلا _security updates_، ولا يقترح Dependabot PR إلا لتبعية تستهدفها CVE — وهو ما يفسر تحديثه `urllib3` و`idna` مع ترك SDKين يتأخران عن إصدار رئيسي.
    - **يتعايش الإصداران الرئيسيان من دون تعارض**، خلافًا لما أوحى به استدلال سابق: ينتقل `openai` 3.x و`anthropic` 1.x إلى **`httpx2`**، بينما يبقى `mistralai` و`google-genai` على `httpx<1`، لكنهما توزيعتان منفصلتان. جرى التحقق بالتثبيت الفعلي، ثم عبر **مسارات provider السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الكود المضمّن والروابط في كل خرج. وكان «تجنب مكدسين HTTP» تفضيلًا لا عائقًا: وقد حسم القياس الأمر.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: كان `google-auth` و`cryptography` ومكدس `opentelemetry` مثبتًا في venv العمل من دون التصريح به — لذلك لم يكن التثبيت الجديد يعيد إنتاج ما جرى اختباره. وعلى العكس، كانت `tokenizers` و`huggingface-hub` و`PyYAML` موجودة فيه من دون استيرادها أو طلبها من أي شيء، وهي بقايا `mistralai` 1.x. وأُعيد توليد الملف بوصفه الإغلاق الكامل لـvenv مبنيًا من التبعيات المباشرة وحدها. ولا يعثر `pip-audit` على أي ثغرة معروفة في المجموعة الجديدة.
    - يفعّل **`.github/dependabot.yml`** (جديد) تحديثات الإصدارات الأسبوعية وpip وgithub-actions. وتُجمع التحديثات الثانوية والإصلاحية في PR واحدة — إذ ينتهي الأمر بتجاهل patch bump واحد لكل PR، كما أن الضوضاء عدو التحديث؛ أما **الإصدارات الرئيسية فتُفصل**، ويتطلب كل منها تحققًا باستدعاء فعلي.
    - يجعل **`scripts/check-deps-fresh.sh`** (جديد، موصولًا بالبوابة) التأخر ظاهرًا في نتيجة المشروع: يقترح Dependabot التحديثات ولا يضمنها، وقد تتراكم PRs الخاصة به. تأخر إصدار رئيسي → فشل؛ تأخر إصدار ثانوي → تحذير، لأن البوابة الحمراء دائمًا ينتهي بها الأمر إلى التجاهل؛ تعذر الوصول إلى PyPI → تخطٍّ صريح محليًا و**فشل مغلق في CI**، إذ إن الفحص غير المنفذ ليس نجاحًا. وقد جرى التحقق في الاتجاهين: فهو يلتقط الحالة الدقيقة السابقة للإصلاح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير عند التأخر الثانوي.

  - **إصلاحات نتجت عن مراجعة PR هذه** — دقّق خمسة وكلاء مراجعة في diff؛ وقد جرى **إعادة إنتاج جميع النقاط أدناه بالقياس** قبل إصلاحها، وكان اثنان منها تراجعين أُدخلا في وقت سابق من الإصدار نفسه.
- **تم إصلاح تراجع — كان لدى `_NEWS_CITATION_REGEX` استرجاع خلفي أُسّي.** كان إصلاح الفقرات المتعددة قد أدخل `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: مشاركة المسافات بين `[ \t]+` و`.*` غامضة، ويتضاعف هذا الغموض من تكرار إلى آخر. قيس ذلك على أسطر `>   texte` — وهي مسافة بادئة Markdown قانونية تمامًا — لا تطابق النمط: **2589 ms لـ 14 سطرًا** مقابل 0.04 ms بعد التصحيح، بمعامل يقارب 9 لكل سطر مضاف. في وضع `--news`، كان يكفي اقتباس كتلي طويل وغير مطابق لتتجمد الترجمة حتى انتهاء مهلة المهمة، من دون سبب قابل للتحديد. يستهلك التكرار الآن السطر كاملًا دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يبقى سوى أسلوب واحد للمطابقة في كل تكرار. تم التحقق على مجموعة البيانات الحقيقية المكونة من 231 مقالًا: **لا اختلافات** في الالتقاط، مع نفس الاقتباسات الـ423، وبقاء الأجسام الـ14 متعددة الفقرات موسعة.
    - **كان تفعيل علامتي provider معًا يُحاسب حسب الاستخدام بصمت.** كان `--use_codex --use_mistral` مقبولًا؛ إذ يختبر `_select_provider_client` Mistral أولًا، بينما يعطي `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. لذلك كان المستخدم يطلب من حصة اشتراكه ويحصل على محاسبة حسب الاستخدام، من دون أي تحذير: وهذا بالضبط نمط الفشل الذي وُجد `--use_codex` لمنعه. تمر علامات provider الست الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: سطر أوامر يجمع بين providerين، وكان مقبولًا بصمت حتى الآن، يفشل الآن عند `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة نهاية العمل تتحول إلى الأخضر عندما تتعطل مجساتها.** كانت أربع من عمليات التحقق الثلاث عشرة في `scripts/check-release-ready.sh` تتبع نمط «التقاط stdout والاستنتاج إذا كان فارغًا» من دون فحص رمز الإرجاع: كان الاستثناء (ملف أُعيدت تسميته، `FileNotFoundError`) يُكتب إلى stderr، ويترك stdout فارغًا، فتستنتج عملية التحقق «لا شيء للإبلاغ عنه». لقد تكرر داخل البرنامج نفسه الفخ القائل إن «`exit 0` لا يثبت شيئًا»، وهو البرنامج الذي كُتب لمنعه. ويفرض مساعد `probe()` الآن رمز إرجاع صفريًا **وعلامة نهاية**، كما ترفض المجسات الاستنتاج من مجموعة فارغة من العلامات — إذ إن assertion على مجموعة فارغة يكون صحيحًا دائمًا. البرهان: أدت إضافة المجموعة الحصرية أعلاه إلى تمرير علامات provider عبر كائن `*_group` لم يعد التعبير النمطي القديم `parser\.add_argument\(` يطابقه؛ فخرجت **ست علامات من أصل 21** من النطاق بصمت، وبقيت البوابة خضراء.
    - **كان فحص الأسرار يفوّت أربعة providers من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: إذ كان كل من `sk-proj-…` (التنسيق الحالي لـ OpenAI) و`sk-ant-api03-…` يتعطلان عند شرطتهما الثانية، ولم يكن `AIza…` مشمولًا. تم توسيع النمط، مع استبعاد `.secrets.baseline` من الفحص. وكانت الحماية `.env` تستعلم كذلك عن `git diff --cached`، الذي لا يرى إلا الفهرس: لذلك لم يكن `.env` **المُرحّل بالفعل** — وهو أسوأ سيناريو — يظهر فيه إطلاقًا. وهي تستعلم الآن عن `git ls-files`.
    - **لم يكن «الإحماء المسبق للرمز» في Codex كذلك فعليًا.** أظهر القياس أن `codex login status` لا يلمس `~/.codex/auth.json` (بقي كل من mtime والحجم دون تغيير)، كما أن مساعدته تقول «إظهار حالة تسجيل الدخول». ومع ذلك، كان التعليق يزعم أنه يحدّث الرمز «مرة واحدة، بالتسلسل»، وبالتالي يعطل خطر عمليات التحديث المتزامنة لرمز دوّار ذي استخدام واحد. لم تكن الحماية المعلنة موجودة؛ ويصف التعليق الآن ما يفعله الكود، بينما تظل الوسيلة الحقيقية هي `max_jobs=4`. كما أن الفحص يحترم `CODEX_BIN`، الذي كان يتجاهله — فكان جهاز بلا `codex` في `PATH` يفشل برسالة «غير موثّق»، وهو تشخيص مضلل.
    - **كان `.env` يُحمّل من داخل shell فرعي.** يُستدعى `detect_provider` ضمن استبدال أوامر، ولذلك لم تكن صادراته تصعد إلى shell الأب: فكان `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` المعرّف في `.env` يظل غير مرئي للقراءات المنفذة في `main()`، التي كانت تستنتج «تعذر العثور على ثنائي Grok» رغم صحة الإعداد.
    - **تجاوز التزامن الحد المعلن بنسبة 50٪.** وُضعت الحماية بعد إطلاق زوج README/CHANGELOG، وقيسَت ذروة قدرها **3 لـ `max_jobs=2`**. وعلى Grok، الذي تُشارك حصته الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يكن الحد الذي يفرضه البرنامج محققًا. أما العد النهائي فكان يُعرض من دون مقارنته بالعدد 28 — فكان يمر ملف مفقود من دون ملاحظة.
    - **عقد مخرجات Grok: غياب `stopReason` أصبح الآن فشلًا.** كان الكود يطبق «`end_turn` **أو غائب**» في موضع يتطلب فيه العقد المعلن `end_turn`. وكان payload بلا الحقل — أو حقل أعادت تحديث CLI تسميته — يحول الحماية إلى no-op صامت. إضافة إلى ذلك، لم يعد `max_turn_requests` مصنفًا كحد للمعدل (فالذي نفد هو ميزانية الجولات: ستعيد المحاولة النتيجة نفسها، مقابل انتظار 90 ثانية)، كما خرج `quota` من علامات حد المعدل — للسبب الذي كان docstring الخاص بـ `_codex_is_rate_limited` يذكره أصلًا من دون أن يطبقه Grok.
    - **أصبحت سلسلة Gemini المتسلسلة محفوظة مؤقتًا حسب النموذج.** كانت تبدأ من `minimal` في كل مقطع، رغم أن النموذج الافتراضي يرفضه: فكان المسار الاسمي يدفع ذهابًا وإيابًا بحالة 400 لكل مقطع ويعيد طباعة التحذير نفسه. والتحذير المتكرر مئات المرات يتوقف عن أن يكون مقروءًا — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مضمّنة صراحةً لـ Codex، وتوجه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق أساس العملية الفرعية يعمم «shim» على CLIَين، رغم أن ثنائي Grok هو ELF أصلي (والتبرير الصحيح هو «عميل يطلق عملياته الفرعية بنفسه»)؛ وتم وسم اثني عشر finding من SAST على `subprocess` بـ `# nosec` / `# nosemgrep` مع تبرير، إذ إن صيغة القائمة من دون `shell=True` تجعل الحقن مستحيلًا، كما أن محتوى المستند لا يمر مطلقًا عبر argv.
    - **لم يعد أي سر يدخل إلى العمليات الفرعية الوكيلة.** كانت قوائم المنع الاسمية تحمي فقط invariant **المحاسبة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). وأظهر القياس أن **سبعة أسرار أخرى** كانت تدخل رغم ذلك إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح CLI الآخر، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة المرور. وهذان CLI هما **عميلان**، كما أن عميل Grok يعمل من دون sandbox لنظام التشغيل على كثير من أجهزة Linux. يجري الفلترة الآن **حسب نمط الاسم** (`API_KEY`، `_TOKEN`، `SECRET`، `PASSWORD`، `CREDENTIALS`) بدلًا من قائمة اسمية، ما يغطي أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن يعرفها هذا الكود. ولا حاجة إلى أي منها في CLI: فالمصادقة موجودة في `~/.codex` و`~/.grok`، وليست في البيئة مطلقًا — وقد تم التحقق من ذلك عبر **ترجمة حقيقية ناجحة** باستخدام كل provider من الاثنين مع البيئة المشددة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يثبت حصرية علامات provider، وعقد `stopReason`، وخطية التعبير النمطي للأخبار، ورسالة الرفض في CI، وحفظ Gemini المؤقت، وغياب أي سر من بيئة العمليات الفرعية. هذا التأكيد **عام** — ويفشل عند وجود مفتاح لا تسميه أي قائمة — بينما كان اختبار التنقيح الموجود مرآةً لثابته ولا يستطيع اكتشاف شيء سوى تعطل حلقته الخاصة. أصبحت المجموعة الكاملة **311 اختبارًا**.

  - **موفرا Grok جديدان**: `--use_grok` (واجهة xAI، بالمفتاح `XAI_API_KEY`، ومحاسبة حسب الاستخدام) و`--use_grok_cli` (CLI الرسمي لـ Grok Build، يُخصم من اشتراك Grok — بالمبدأ نفسه المستخدم في `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن نقطة نهاية xAI متوافقة مع OpenAI، يُعاد استخدام العميل و`_call_openai` كما هما، ولا يتغير سوى `base_url`. ولم يلزم سوى تكييف واحد يستفيد منه الجميع: إذ يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي تصدرها xAI بينما تصدر OpenAI `stop`. النماذج: `grok-4.6` (الجودة) و`grok-4.3` (الاقتصادي). تجدر الإشارة إلى أن النسخة الاقتصادية من Grok تظل الأغلى في المستودع — $1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ `mistral-small-latest`؛ لذا يُختار هذا provider لتنوع النموذج، لا للسعر.
    - **وضع CLI**: مبني على Codex، مع أربعة اختلافات فرضها الواقع — يُمرر prompt عبر ملف (`--prompt-file`، إذ إن CLI لا يقرأ stdin، كما أن وجود مقطع في argv سيجعله ظاهرًا في `ps`)، والمخرجات كائن JSON واحد على stdout (لا JSONL ولا ملف `-o`)، ولا يعرض الاشتراك سوى `grok-4.6` و`grok-4.5`، ولا يمكن تطبيق sandbox (انظر أدناه). جرى تفكيك إطلاق العمليات الفرعية مع Codex في `_codex_run_process`، من دون المساس ببقية provider الخاص بـ Codex الذي تم اختباره مسبقًا.
    - **`exit 0` لا يثبت شيئًا، وقد تم قياس ذلك**: عندما لا يكون المستخدم موثّقًا، يكتب CLI قيمة `{"type":"error","message":"Not signed in."}` على **stdout** مع رمز إرجاع **0**. ويتصرف الرفض أو تجاوز الجولات بالطريقة نفسها. لذلك يتطلب عقد المخرجات أربعة شروط متزامنة: رمز إرجاع 0، وغياب payload خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع الفحص الأولي المنطق نفسه: يعيد `grok models` القيمة 0 حتى عند انقطاع الاتصال، ولا يمكن الاستنتاج إلا من وجود «not authenticated» في stdout.
    - **الاحتواء: عدم تماثل مقصود وموثق.** في حين يعمل Codex في `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـ Grok على كثير من أجهزة Linux الحديثة لسببين مستقلين على مستوى النظام ولا يمكن تجاوزهما من دون `sudo`: إذ يحظر AppArmor مساحات أسماء المستخدمين غير المميّزة منذ Ubuntu 24.04 (`bwrap: setting up uid map: Permission denied`، وقد أُعيد إنتاج ذلك خارج Grok)، كما تفشل قائمة منع مقابس وقت تشغيل الحاويات عندما يكون `/run/podman` في `0700` (ولا يعالج المحلل سوى `ErrorKind::NotFound`، بينما يصبح EACCES قاتلًا). الفخ المركزي: إن **الملف التعريفي المدمج** الذي يتعذر تطبيقه **يبدأ العمل بلا احتواء بصمت**. لذلك لا يطلب البرنامج أي ملف تعريفي افتراضيًا، ولا يعود بصمت إلى وضع آخر — بل يحذر عبر stderr. وتعتمد الحماية على قواعد `--deny` الخاصة بـ CLI، بما في ذلك catch-all `*`، وهي الطبقة الوحيدة المقاسة ذات السلوك _fail-closed_ (فإن تسببت قاعدة ذات بادئة غير معروفة في رفض بدء التشغيل). ويسمح `GROK_TRANSLATE_SANDBOX=read-only` بفرض ذلك، وعندها يفشل بدء التشغيل إذا تعذر على الجهاز احترامه.
    - **وسائل الحماية**: يُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (فوجود مفتاح سيحوّل المحاسبة إلى حسب الاستخدام؛ كما أن `GROK_SANDBOX` موروثًا سيفرض ملفًا تعريفيًا غير قابل للتطبيق مع رسالة مضللة)، وتُعطل مفاتيح MCP/hooks/skills/agents، و`--disable-web-search`، و`--no-subagents`، و`--no-plan`، مع workdir مؤقت، ورفض في CI، ومهلة تقتل مجموعة العمليات، وتراجع تدريجي عند حد المعدل. ثُبت `--max-turns` على 6 لا على 1: فالعداد يُزاد بعد جولة الأدوات، والقيمة 1 ستقتطع المخرجات.
    - **الحصة**: مجمع Grok أسبوعي و**مشترك مع Chat وImagine وVoice**، ولا تكشفه أي أداة — خلافًا لـ Codex الذي يتيح `account/rateLimits/read` حساب الاستهلاك. لذلك يحد `regen_translations.sh` التزامن إلى 2 ويحذر صراحةً.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` (24 اختبارًا). أصبحت المجموعة الكاملة **290 اختبارًا**.
  - **إصلاح خلل — كانت الاقتباسات الإنجليزية متعددة الفقرات محمية جزئيًا فقط (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل باعتباره جسم الاقتباس سوى سلسلة من أسطر `>` **المتتالية**. وما إن يغطي الاقتباس فقرات متعددة (تفصل بينها سطر `>` فارغ)، حتى كان يُلتقط آخرها فقط ويُستبدل بالعُنصر النائب؛ أما السابقة فكانت تذهب إلى LLM وتعود مترجمة — وهو عكس ما وُجد `--news` لضمانه تمامًا. يقبل التكرار الآن أسطر `>` الفارغة الداخلية ويصبح غير جشع، ليتوقف عند `>` الفارغ الذي يسبق السطر المائل، لا عند أول سطر فارغ يصادفه.
    - **الحجم المقاس** على مجموعة بيانات حقيقية من 198 مقالًا: 11 اقتباسًا معنيًا من أصل 419. لا تراجع — يلتقط التعبير النمطي الجديد العدد نفسه تمامًا من الاقتباسات، ولا تمتد إلا الأجسام متعددة الفقرات (408 أجسام مطابقة، و11 ممتدة)، كما أن سطر الإسناد `> — …` لا يمكنه الاستيعاب داخل الجسم بفضل الحفاظ على lookahead.
    - **برهان شامل من البداية إلى النهاية** على مقال حجمه 69 كيلوبايت تُرجم إلى ja/ar: تظل الفقرة الأولى من اقتباس، التي كانت تُعرض سابقًا بصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم بالطريقة نفسها إلى العربية، الآن بصيغة `> GLM-5.3 is now open-weight.`. ويعود عدد أسطر الاقتباسات الإنجليزية من 9 إلى 10، أي إلى عدد أسطر المصدر.
    - تجدر الإشارة إلى أن هذا الخلل لم تكتشفه المدققات اللاحقة، التي تتحقق من وجود الاقتباس من دون التحقق من اكتماله.
  - **توفير مقاس في provider الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ النموذج بـ `gpt-5`، بما في ذلك في `--eco`. قياسًا على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: `medium` → 45 رمز reasoning و65 رمزًا في المخرجات؛ `none` → 0 و14. لا يضيف reasoning شيئًا إلى الترجمة، وكان يُدفع ثمنه في كل مقطع من كل ملف. أصبحت القيمة الافتراضية `none` في `--eco`، وتظل `medium` في غير ذلك؛ كما تظل القيمة الصريحة الممررة عبر CLI ذات أولوية. يقبل `--reasoning_effort` الآن `none` و`xhigh` إضافة إلى `low`/`medium`/`high` (ولا تقبلها جميع النماذج: فمثلًا يرفض `minimal` القيمة `gpt-5.4-mini` — وتغطي إعادة المحاولة الحالية من دون وسيط هذه الحالة).
  - **تحديث SDK وترحيل Gemini**: استُبدل `google-generativeai` (انتهى دعمه في 2025-11-30، وأُرشف المستودع) بـ SDK الموحد **`google-genai`** — `genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير prompt النظام في `system_instruction` بدلًا من دمجه مع المقطع. أصبح `mistralai` في الإصدار **2.9.4** (وأصبح الاستيراد `from mistralai.client import Mistral`؛ أما القديم فيرفع `ImportError`، وقد تم التحقق من ذلك داخل wheel)، و`anthropic` في **0.125.0**، و`openai` في **2.54.0** — وهي آخر الإصدارات قبل الانتقال إلى `httpx2`، لتجنب تشغيل حزمتَي HTTP في venv معًا. وبناءً على ذلك، تم فك حظر `httpx` 0.28.1 و`pydantic` 2.13.5.
  - **تراجعان التقطتهما اختبارات حقيقية، لا الوثائق**:
    - يرفض `anthropic` ≥ 1.0 من جهة العميل استدعاءً غير متدفق يتنبأ فيه `max_tokens` بأكثر من 10 دقائق (`ValueError: Streaming is required...`). لم تكن هذه الحماية موجودة في 0.34.2، وكانت تعطل كل استدعاء Claude مع `max_tokens=32768`. عولج ذلك عبر `timeout` صريح (`CLAUDE_TIMEOUT`، ومدته الافتراضية 900 ثانية)، ما يتجنب التحول إلى streaming لاستدعاء لا نستخدم منه إلا الاستجابة الكاملة.
    - لا يقبل `thinking_level="minimal"` سوى جزء من كتالوج Gemini: إذ يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` بحالة 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تسلسل `minimal` → `low` → من دون thinking_config، على غرار fallback الخاص بـ OpenAI الموجود مسبقًا — فلا ينبغي لمعلمة تحسين أن تتسبب في فشل ترجمة.
  - **تجديد النماذج الافتراضية**، مع التحقق من كل نموذج عبر استدعاء حقيقي: OpenAI من `gpt-5.5` إلى **`gpt-5.6-terra`** (−60٪ على دفعة من 28)، ومن `gpt-5.4-mini` إلى **`gpt-5.6-luna`** (−73٪)؛ وClaude من `claude-sonnet-4-6` إلى **`claude-sonnet-5`** (أرخص وأحدث)، ومن `claude-haiku-4-5-20251001` إلى **`claude-haiku-4-5`** (المعرّف القياسي من دون تاريخ)؛ وGemini من `gemini-3.1-pro-preview` إلى **`gemini-3.7-flash`**، ومن `gemini-3.1-flash-lite-preview` إلى **`gemini-3.1-flash-lite`** (إصدار مستقر وأرخص من `3.5-flash-lite`).
Mistral دون تغيير، ولا يزال `mistral-large-latest` أفضل قيمة مقابل السعر من بين الأربعة. ملاحظة: لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — إذ إن Gemini 3.5 Pro، الذي أُعلن عنه في مايو 2026، لم يُطرح قط؛ وفئة 3.5/3.6/3.7 تقتصر حصراً على Flash.
  - **قياس A/B قبل التحول إلى Gemini**: تمت ترجمة `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. البنية متطابقة تماماً (21 قائمة، و18 كتلة تعليمات برمجية، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، في **8 ثوانٍ مقابل 48 ثانية**. وبما أنه لا يوجد معيار أداء عام يقارن بين هذين النموذجين في الترجمة أو النصوص البرمجية غير اللاتينية، فقد كان التحول سيستند بخلاف ذلك إلى مجرد افتراض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفذ `block.text for block in response.content` دون تصفية النوع. تُدرج نماذج الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، التي تكشف عن `.thinking` بدلاً من `.text` — وكان من شأن الترجمة أن تفشل عند مواجهة `AttributeError` معتم في المقطع الأول. تُستبعد الآن الكتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` (قائمة استبعاد للحفاظ على التسامح مع نوع غير معروف يحمل نصاً)، ويؤدي أي رد لا يحتوي على كتلة نصية إلى رفع خطأ صريح. يُمرَّر `thinking={"type": "disabled"}` في كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: إزالة النماذج التي انقضى تاريخ سحبها (فئة `magistral-*` سُحبت في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620` و`claude-3-7-sonnet-20250219` و`claude-opus-4-1-20250805` و`claude-sonnet-4-20250514`). تصحيحات الحدود: Mistral 128K → **256K** (توليدا Large 3 / Small 4)، وGemini 1 000 000 → **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` من 200K → **1M**، وفئة `gpt-5.6-*` من 400K → **1,05M**. إضافة Claude 5 (`claude-sonnet-5` و`claude-opus-5` و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وفئة `ministral-*`. ملاحظة: تظل هذه الحدود إرشادية، إذ يحد `translate()` تقسيم النص إلى `min(16000, limite)`.

  - **المزوّد `--use_codex`**: المزوّد الخامس الذي يشغّل CLI Codex الرسمي (`codex exec`) في الوضع غير التفاعلي، بدلاً من استدعاء واجهة API تُحاسَب حسب الاستخدام. تُخصم الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقاً. وهذا هو المسار الوحيد الموثّق من OpenAI لهذا الاستخدام: إذ تسرد مصفوفة التوفر حسب الخطة «Codex SDK، و`codex exec`، وسير العمل القابل للبرمجة» بوصفها متاحة على Plus/Pro/Business/Enterprise، في حين أن رموز `~/.codex/auth.json` لا تُصادق على استدعاءات Platform API (ولا يقرأها هذا البرنامج مطلقاً — إذ يتولى CLI إدارة المصادقة وتجديدها).
  - **إتاحة تثبيت ملف Codex التنفيذي عبر pip، وليس npm فقط**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK ‏`openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عاماً لاستخدام `--use_codex`. ولا تُضاف الحزمة إلى `requirements.txt`، لأن الملف التنفيذي يبلغ حجمه نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين لمزوّد اختياري. تم التحقق من البداية إلى النهاية: عند غياب `codex` من `PATH`، يعثر الحل على الملف التنفيذي المضمّن وتكتمل ترجمة كاملة في 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: أُزيل `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. فمن دون هذا الحاجز، قد يتسبب مفتاح موجود في `.env` في تحويل Codex إلى الفوترة حسب الاستخدام من دون أي إشارة ظاهرة — وهو بالضبط ما وُجد هذا المزوّد لتجنبه.
  - **إحكام اختبارات مطبات CLI**:
    - يقرأ `codex exec` الإدخال القياسي **حتى** عندما يُمرَّر الطلب كوسيط: ومن دون إغلاق الإدخال القياسي، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج (إعادة إنتاج: exit 124 بعد 180 ثانية، وصفر بايت). لذلك فإن `communicate(input=...)` إلزامي.
    - إن `codex` المثبّت عبر npm هو shim لـ Node يستدعي `spawn` الملف التنفيذي الحقيقي المكتوب بـ Rust: وهذا الأخير **حفيد** عملية Python، ويظل حياً بعد `SIGKILL` الخاص بـ `subprocess.run(timeout=)`، حيث سيواصل استهلاك الحصة. ومن هنا يأتي `Popen(start_new_session=True)` + `os.killpg`.
    - قد يخرج CLI بالرمز 0 رغم إصداره `turn.failed`: لذلك يُفحَص خرج JSONL (`--json`) بالإضافة إلى رمز الإرجاع، ويؤدي غياب الملف `-o` مع رمز 0 إلى رفع خطأ صريح بدلاً من إنشاء مقطع فارغ.
  - **التراجع عند تجاوز حد المعدل**: لا ينفذ CLI أي إعادة محاولة داخلية (`max_retries = 0`). ويُصنَّف الخطأ بناءً على بنية حمولة JSON (`status: 429` / `error.type`)، لا على سلاسل فرعية — إذ تظهر كلمة «الحصة» سواء في 429 القابل للاسترداد أو في `insufficient_quota` النهائي.
  - **حاجز CI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفاً. فالمصادقة عبر الاشتراك غير مخصصة لمشغّل مشترك، وتحذر OpenAI صراحةً من سير العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (للجودة) و`gpt-5.6-luna` (`--eco`). وتشترك فئة `gpt-5.6-*` بين CLI وPlatform API، لكن حساب ChatGPT لا يملك حق الوصول إلى جميعها: تُطبَّق قائمة السماح على جانب الخادم، من دون تحقق محلي، ويؤدي النموذج غير المعتاد إلى تحذير. في خطة Plus، توفر Luna من 250 إلى 2,000 رسالة لكل نافذة مدتها 5 ساعات، مقابل 10 إلى 100 لدى Sol: يُوصى باستخدام `--eco` لأي معالجة دفعية.
  - **إصلاح خلل — كان `regen_translations.sh` يُخرج خطأً رغم اكتمال النجاح**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` من `main()` لم يعد موجوداً عند تنفيذ trap. في ظل `set -u`، كان ذلك يرفع `failed_log: unbound variable` ويجعل البرنامج يخرج بالرمز 1 رغم صحة الترجمات الـ28 — ما كان سيوقف `release.sh --auto` (`set -e`) مباشرة بعد إعادة التوليد، في أكثر المراحل تكلفة. أصبح المتغير عاماً، ويتحقق trap من وجوده. ومن الآثار الجانبية المفيدة أن إخفاقات الترجمة الحقيقية، التي كانت مخفية حتى الآن بسبب هذا الخطأ، عادت للظهور في الملخص الختامي.
  - **`REGEN_MODEL`**: متغير بيئي جديد من `regen_translations.sh` يفرض نموذجاً محدداً فوق الإعداد الافتراضي للمزوّد، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج الأعلى ضمن حصة الاشتراك بدلاً من نموذج `--eco` الموجّه لمعالجة الكميات.
  - **`regen_translations.sh`**: `REGEN_PROVIDER=codex` متاح عند التفعيل الصريح فقط (ولا يُكتشف تلقائياً أبداً، لتجنب استهلاك حصة الاشتراك من دون علم المستخدم). يُجدَّد الرمز مرة واحدة بالتسلسل قبل فتح التوازي — إذ إن تجديد Codex دوري ولمرة واحدة، وستؤدي المهام المتزامنة إلى إبطال جلسة `codex login` — كما خُفّض التوازي إلى 4.
  - **إعادة الهيكلة المرتبطة**: انتقل `_dispatch_provider_call` من 8 إلى 6 معاملات عبر `_resolve_provider()` يعيد اسم المزوّد، بدلاً من تمرير قيمة منطقية رابعة عبر السلسلة بأكملها. وتظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` بالحد الأدنى.
  - **الاختبارات**: ملف جديد هو `tests/test_codex_provider.py` (48 اختباراً) يغطي argv، والبيئة المنقّاة، وعقد منع المقدمات، والإخفاقات الصامتة، والمهلة/killpg، والتراجع، والفحص المسبق، وحل المزوّد، وتتابع استدلال Gemini، وتصفية كتل Claude، واقتباسات الأخبار متعددة الفقرات. أصبحت المجموعة الكاملة تضم 290 اختباراً.
  - **التحقق الفعلي**: يترجم `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex، مع بنية متطابقة تماماً مع الترجمات المرجعية (14 كتلة تعليمات برمجية، و24 عنواناً، و25 سطراً في الجدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل تعليمات برمجية متطابقة حرفياً، ومن دون أي بقايا لعناصر نائبة). وفي مقال إخباري حجمه 69 كيلوبايت بوضع `--news`، تجتاز المخرجات `gpt-5.6-luna` و`gpt-5.6-sol` كلتاهما أداة التحقق التطبيقية اللاحقة على en/ja/ar. وقد قيس الاستهلاك عبر `account/rateLimits/read`، وبقي دون عتبة التقريب في العداد (0% من نافذة الخمس ساعات) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL الخاص بنسبة الخبر مع الأقواس المتداخلة أو البادئة الفرنسية (2026-05-11):

  - **الخلل المُصلح**: كان استخراج عناوين URL الخاصة بنسبة الخبر في `_protect_news_quotes` يستخدم التعبير النمطي `re.search(r"\((.+?)\)", attribution)` (التقاط كسول بين قوسين). في نسبة من النوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` الخارجية + `]()` الخاصة برابط markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة مع تضمين البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` الختامي). ونتيجة لذلك، كان `_validate_news_post` يبحث عن هذه السلسلة في النص المترجم ويفشل دائماً (لسببين: `)` مبتور، وعبارة "relayé par" مترجمة إلى `relayed by`/`weitergeleitet von`/...). ولم يكن بالإمكان أن ينجح التتابع الكامل low → medium → high → gpt-5.5.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — إذ يستهدف تحديداً `](url)` الخاص برابط markdown، ويلتقط **عنوان URL الخالص فقط** (من دون بادئة فرنسية أو اقتطاع)، مع الحفاظ على الثابت عبر العناصر النائبة `#URL{N}#` أثناء الترجمة. وهو متين أمام النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس خارجية
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py`، ضمن الفئة `TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة المطابقة تماماً للخلل المُعاد إنتاجه من Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (متغير مع `via`)
  - **فجوة التغطية**: يتحقق `check-editorial-coverage.py` من الصياغة التحريرية، لكنه لا يتحقق من قابلية الترجمة بواسطة المترجم. ومن التحسينات الممكنة (خارج نطاق v1.9.2) إضافة فحص يحاكي استخراج النسبة في dry-run لاكتشاف الأنماط المحفوفة بالمخاطر قبل النشر.

- **1.9.1** إصلاح i18n لوسم CTA في ملاحظة علامة الترجمة (2026-05-10):

  - **الخلل المُصلح**: ظل وسم `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط marker أعلى الملفات المترجمة **بالفرنسية** لجميع اللغات المستهدفة، بدلاً من اتباع `target_lang`. ولا يراه LLM مطلقاً (إذ يجمعه Python للحفاظ على عنوان URL وslug الخاص بالمستودع)، ولذلك لم تتمكن مرحلة الترجمة من تصحيحه. وكان هذا تراجعاً صامتاً منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد هو `_VIEW_PROJECT_LABELS` يربط اللغات الـ15 بوسمها المحلي. يمرر `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` الآن اللغة المستهدفة. ويُستخدم `fr` كبديل عند عدم معرفة اللغة (لأسباب أمنية، ومنعاً لخطأ KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link` (من target_lang `ja` إلى وسم اليابانية المتوقع). واختباران جديدان: `test_source_link_label_localized_per_target_lang` (بمعلمات تغطي 7 لغات ذات نصوص لاتينية، وإيديوغرافية، وأبجدية) و`test_source_link_label_falls_back_to_french_for_unknown_target`. المجموع: 40 اختباراً في `test_translation_note_position.py` (بدلاً من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع ذو قيمة افتراضية `target_lang="fr"` — يواصل المستدعون البرمجيون الخارجيون من دون `args.target_lang` العمل دون تعديل.
- **1.9** إصلاح الفشل الصامت + مجموعة أدوات جودة كاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + تنسيق marker "embed card"**:
    - خيارات CLI جديدة (إضافات، مع الحفاظ على القيم الافتراضية → **غير كاسرة للتوافق**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): تضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - `legacy` يعيد السلوك نفسه تمامًا في v1.8 (فقرة عريضة `**…**`) **بايتًا ببايت**.
        - `marker` يُصدر تعريف مرجع رابط Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`)، يتبعه **blockquote من 3 فقرات** منظم لعرض يشبه "GitHub repo embed card": عنوان المشروع داخل code inline (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف يترجمه LLM، ورابط CTA (`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. ويمكن استغلاله أثناء البناء بواسطة إضافة remark (راجع blog jls42.org → الإضافة `remark-translation-banner`).
    - **الثوابت التي لا تُرسل أبدًا إلى LLM**: يُجمّع عنوان المستودع وعنوان URL الخاص بـ GitHub على جانب Python بعد ترجمة الجملة الوصفية. لا يرى LLM مطلقًا slug `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تغيير أي renderer أو فاصل أو scheme.
    - **إدراج واعٍ بـ Frontmatter**: في الوضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة `---` الختامية** لـ frontmatter YAML (لضمان أمان Astro Content Collections / gray-matter). يكتشف المساعد `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف ويحافظ على سلامته؛ **ويرفع `RuntimeError`** عند وجود frontmatter مفتوح من دون fence إغلاق (فيُعاد الملف إلى `failed_files` بدل كتابته بملاحظة في موضع خاطئ).
    - **Sanitizer بنموذج whitelist**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع استخدام `unknown` كقيمة احتياطية إذا أصبح فارغًا. ويتوافق مع المدقّق الموجود في إضافة remark الخاصة بـ Astro، ويعادل المحارف التي قد تكسر تنسيق marker (المسافة، وعلامة الاقتباس، والقوس، والفاصلة، وغيرها).
    - **إعادة هيكلة داخلية**: استُبدل `_append_translation_note` (دالة ضخمة واحدة) بـ 7 مساعدين نقيين (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فُصل Builder عن Composer (يعيد Builder كتلة نقية بلا فاصل، بينما يطبق Composer قيمة `\n\n` وفق الموضع)؛ وتشترك عملية الإنتاج والمساعد المصدر في نفس مُجمّع الفقرات الثلاث.
    - **`_quote_lines` مع الحفاظ على الأسطر الفارغة**: يسبق كل سطر بـ `> `، محولًا الأسطر الفارغة إلى `>` فقط. وهذا يتيح لـ mdast رؤية 3 فقرات مميزة داخل blockquote (العنوان / الوصف / الرابط) بدل فقرة واحدة تحتوي على فواصل أسطر.
    - **`_build_translation_note_block` التكيفي**: وفق عدد الفقرات التي حافظ عليها LLM (3 = تنسيق البطاقة الكامل، 2 = جملة + رابط، 1 = احتياطي). لم يعد الاحتياطي ذو الفقرة الواحدة **يغلّف بـ `**...**`** عند اكتشاف رابط Markdown `](` (فالعرض الهش لـ `<strong>` حول رابط).
    - **التوافق مع الإصدارات السابقة**: `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` في جانب `_compose_with_notes` — تواصل Namespace التي لا تحتوي على هذه السمات (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) عملها دون تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور على مقتطف المصدر حرفيًا) + طبقة احتمالية (`langdetect`)
    - قائمة السماح `finish_reason` / `stop_reason`: رفع `RuntimeError` عند أي حالة خارج قائمة السماح (الاقتطاع، وcontent_filter، وغيرهما)
    - `max_tokens` في Claude: من `4096` إلى `32768` (لتجنب الاقتطاع الكامن في المقاطع ذات 16k، مع هامش بينيّ النصوص من FR→JA/ZH/KO/AR/HI)
    - تقسيم واعٍ بالعناوين: إعطاء الأولوية لـ H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى exit code غير صفري: يعيد `translate_markdown_file` حالة نمطية `success` / `failure` / `skipped`، ويعيد `main()` `sys.exit(1)` إذا فشل ملف واحد على الأقل (في وضعي الملف الواحد والدفعي)
    - حارس المحتوى الفارغ لدى جميع المزوّدين، ونسبة سلامة المصدر/الناتج (≥ 500 حرفًا، ورفض إذا كانت < 5%)، والتحقق من placeholders البرمجية (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، و`BadRequestError` retry من دون `reasoning_effort`
    - إضافة الاعتمادية `langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("نوع EurekAI الكامل"، 14 hook):
    - Pre-commit: ruff (lint+format)، وshellcheck، وprettier (md/yaml/json)، وdetect-secrets (4 مفاتيح API محمية)، وLizard (CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، وEOF، والملفات الكبيرة، وshebangs، وغيرها)
    - Pre-push: mypy (وضع lax تدريجي)، وOpengrep SAST (translate.py + scripts/)، وpip-audit (وضع reporting أولي)، وunittest discover (tests/ + scripts/tests/)
    - أغلفة محلية داخل `scripts/` تستخدم `./venv/bin/python`
    - `scripts/audit_verdict.py`: محلل JSON لـ pip-audit مع 11 اختبار unittest، وهو منفذ Python من محلل jls42-astro
    - إصلاح 7 مخالفات أولية لـ ruff: B904 (raise from) ×2، وB007 (unused dirs)، وC408 (dict literal)، وC419 (list-comp)، وSIM105 (contextlib.suppress)، وSIM110 (any())
    - يستبعد Lizard مؤقتًا `translate.py` (4 دوال ذات CCN بين 21 و47، وإعادة الهيكلة مخططة) — بوابة صارمة على scripts/
  - **SonarCloud وتغطية شاملة**:
    - سير عمل GitHub Actions `SonarCloud` (sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع التغطية عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى README (Quality Gate، وتقييمات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code)
    - `tests/test_silent_failure.py` (`unittest` من stdlib): يغطي الوصلات الست لسلسلة أخطاء الفشل الصامت
    - `tests/test_orchestration.py` (+79 اختبارًا): يغطي طبقة orchestration الخاصة بـ `translate.py` (`_resolve_*_filename`، و`_existing_translation_exists`، و`_record_translation_status`، و`_write_output_file`، و`translate_directory`، و`_validate_input_paths`، و`_init_*_client`، و`_select_provider_client`، و`_normalize_collapsed_markdown`، و`_cleanup_source_flag`، و`_validate_news_flags_*`، و`_openai_create_with_fallback` TypeError + BadRequestError fallbacks، وتنسيق prompt لسلسلة o1، وفروع early-return في `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` (stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **Coverage on new code**: من 75.5% إلى نحو 98% (translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك E2E `marker+top|bottom|both` و`legacy+top|bottom|both`)، والبادئة متعددة الأسطر، والتوافق العكسي بايتًا ببايت (golden literal)، وsanitizer، وتقسيم frontmatter (بما في ذلك الرفع عند عدم إغلاق fence)، وتنسيق الفقرات الثلاث، والاحتياطي ذي الفقرتين، وحارس الفقرة الواحدة + رابط Markdown، وحاجزًا حرجًا `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان وURL لا يُرسلان أبدًا إلى LLM. **نجح 190 اختبارًا**، دون أي تراجع.
  - التوثيق: `README.md` (بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` (سير عمل pre-commit ومراقبة CI مفصلة)، وإعادة توليد 28 ترجمة
- **1.8** وضع `--news` + تحديث النماذج لعام 2026 (2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - OpenAI للجودة: من `gpt-5` إلى `gpt-5.4`
    - OpenAI الاقتصادي: من `gpt-5-mini` إلى `gpt-5.4-mini`
    - Gemini للجودة: من `gemini-3-pro-preview` إلى `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` (400k) و`gemini-3.1-pro-preview` (1M)
  - وضع `--news` الأولي: حماية الاقتباسات EN باستخدام placeholders `#NEWSQUOTE\d+#`، وmapping `LANG_FLAGS` (15 لغة)، ومعالجة الأعلام وفق اللغة الهدف
  - التحقق من placeholders الخاصة بالأخبار قبل الاستعادة (تراجع: كان LLM يحذف placeholder فينتج بصمت مخرجًا بلا اقتباس)
  - جعل البرنامج النصي `regen_translations.sh` محمولًا (مسارات مطلقة، دون اعتماد على pwd)
  - إضافة رابط Français إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة
- **1.7** مستجدات:
  - خيار `--keep_filename` للحفاظ على اسم الملف الأصلي عند الترجمة
  - دعم الملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على code inline**: أصبحت backticks (`` `...` ``) محمية الآن أثناء الترجمة
  - تحسين system prompt:
    - معالجة أفضل لعلامات الاقتباس في YAML frontmatter
    - حماية متغيرات القالب `{variable}`
    - منع ملاحظات المترجم غير المطلوبة
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** مستجدات:
  - دعم Google Gemini API للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI: `gpt-5` (الجودة)، `gpt-5-mini` (الاقتصادي)
    - Claude: `claude-sonnet-4-5` (الجودة)، `claude-haiku-4-5` (الاقتصادي)
    - Gemini: `gemini-3-pro-preview` (الجودة)، `gemini-3-flash-preview` (الاقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف واحد (`--file`) دون استعراض مجلد
  - نمط تسمية جديد مبسط: `{base}-{lang}.md`
  - خيار `--include_model` للحفاظ على التنسيق القديم مع اسم النموذج
  - دعم النماذج غير المدرجة مع حد افتراضي للرموز (128k)
  - ترجمة README إلى 14 لغة
- **1.5** تحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** تحديث `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** تحديث `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` وتحديث `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين prompts الترجمة:**
    - أُثريت prompts الخاصة بالترجمات المباشرة وملاحظات الترجمة لمزيد من الوضوح والكفاءة، مع تضمين تعليمات مفصلة للحفاظ على metadata وعناصر التنسيق الخاصة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم imports لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص ومعالجة code blocks للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات الإخراج:**
    - عكس النموذج واللغة في اسم ملفات الإخراج (مثل `f"{base}-{args.target_lang}-{args.model}.md"`)، مما يسهل تنظيم الترجمات والعثور عليها.
  - **تحسينات متنوعة:**
    - تنظيف الكود بحذف الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية البرنامج النصي وقابليته للقراءة.
- **1.4** مستجدات:
  - دعم Anthropic Claude API للترجمة
  - تحسين prompts لمزيد من الوضوح والكفاءة
  - تعديلات طفيفة لتحسين صيانة الكود
- **1.3** تحسينات وميزات جديدة:
  - تحسين معالجة code blocks
  - تحسين معالجة ملفات الإخراج
  - تحسين اكتشاف الملفات الموجودة
  - خيار `--force` لفرض الترجمة
  - عكس النموذج واللغة في اسم ملف الإخراج
- **1.2** إصلاح changelog
- **1.1** إضافة دعم Mistral AI API
- **1.0** الإصدار الأولي - دعم OpenAI API

**مقالة مترجمة من الفرنسية إلى العربية باستخدام gpt-5.6-luna.**
