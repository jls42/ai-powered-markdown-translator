### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.13.0** الموفّر `--use_openrouter`: موجّه مدفوع إلى نحو 430 نموذجًا، من بينها النماذج الصينية المفتوحة (2026-09-05):

  - **المسار التاسع للموفّرين، طُرح مع الثامن.** لم يُنشر الإصدار 1.12.0 على PyPI: إذ يصدر الموجّهان OpenCode وOpenRouter معًا. يتيح [OpenRouter](https://openrouter.ai) بمفتاح واحد الوصول إلى نماذج لا يعرضها أي موفّر آخر هنا — Kimi وQwen وDeepSeek وZ.ai — عبر رصيد موحّد تُحتسب تكلفته وفق الاستخدام. ولأن endpoint متوافق مع OpenAI، فالعميل هو نفسه المستخدم مع xAI؛ **وكل ما يميّز هذا الموفّر يتمثل في فحص تمهيدي**، وكل قاعدة فيه ناتجة عن قياس على API.

  - **يُقدَّم النموذج نفسه عبر عشرات المستضيفين ذوي الحدود المختلفة، ولا يراعي التوجيه ذلك.** وفق القياس: 33 مستضيفًا لـ`z-ai/glm-5.2` و23 لـ`z-ai/glm-5.3-flash` — أحدهم محدود عند **2,048 token للإخراج**. ولذلك كانت ترجمة طويلة من بين 23 ترجمة تُقتطع عشوائيًا من دون أدنى إشارة. يقرأ الفحص التمهيدي `/api/v1/models/{modèle}/endpoints`، ويستبعد المستضيفين الذين تقل حدود إخراجهم عن 8,000 token، والذين تكون حالتهم متدهورة، والذين لا يعلنون أي حد، ثم يثبّت الباقين. إن `provider.only` **من دون** `allow_fallbacks: false` ليس سوى تفضيل: إذ يعود الموجّه إلى مستضيف مستبعد، فيفقد التثبيت أي قيمة. وإذا لم يستوفِ أي مستضيف الحد، يتوقف الأمر: فمواصلة الترجمة تعني قبول الاقتطاع الصامت الذي وُجد هذا الفحص التمهيدي لمنعه.

  - **تُحتسب تكلفة الاستدلال بسعر الإخراج، وهو مفعّل افتراضيًا في نماذج كثيرة.** الطلب نفسه على `z-ai/glm-5.2`، والإجابة «حسنًا»: **107 tokens إكمال بالإعداد الافتراضي للنموذج، و2 عند تعطيل الاستدلال**. وفي الترجمة، حيث لا يضيف الاستدلال شيئًا، يعني ذلك زيادة بمقدار 18 ضعفًا لكل مقطع من كل ملف. لذلك يُعطّل افتراضيًا. أما **النماذج الـ288 من أصل 431** التي تفرضه (`reasoning.mandatory`) فتجيب بـ`400 « Reasoning is mandatory for this endpoint and cannot be disabled »`: لا يُرسل إليها شيء بدل تخمين مستوى الجهد — إذ يخصص الجهد **نسبة مئوية من `max_tokens`** يستهلكها الاستدلال أولًا، ولذلك فإن اختيار قيمة عشوائية ينقل خطر الصفحة الفارغة بدل تقليله.

  - **يتلقى النموذج الذي يفرض الاستدلال أدنى مستوى جهد يقبله، وقد حُدد ذلك بالقياس.** كان الخيار الأول عدم إرسال شيء، تجنبًا للتخمين بدلًا من النموذج. وعند اختباره على `z-ai/glm-5.3-flash`، الذي إعداد الكتالوج الافتراضي له هو `max`، أدى هذا الخيار إلى إخراج **مقتطع عند 32,768 token** قبل نهاية الترجمة — فضاعت لغتان من أصل أربع عشرة. ولم تكن زيادة السعة لتغيّر شيئًا: فالجهد يخصص نسبة منها، وينمو الاستدلال معها. لذلك يقرأ الموفّر `supported_efforts` خلال الفحص التمهيدي ويطلب أدنى مستوى، مع الرجوع إلى «لا شيء» عندما لا يعلن الكتالوج أي مستوى صالح للاستخدام. وفي اختبار مضاد على اللغة التي أخفقت: كانت تفشل بسبب استنفاد الميزانية، وأصبحت الآن تنجح خلال 9 دقائق ببنية مطابقة للمصدر.

  - **أصبح عطل المستضيف الوسيط يحمل اسمه الآن.** يوحّد الموجّه هذه الحالة في `finish_reason=error` مع `native_finish_reason` فارغ — وفق قياسين، على لغتين، بعد 750 ثانية بالضبط. كانت الرسالة العامة تدفع إلى البحث عن خلل في المستند أو التقسيم؛ أما الآن فتوضح أن العطل لدى المورّد وأن إعادة المحاولة تكفي غالبًا.

  - **إن `finish_reason=length` مع إخراج فارغ ليس اقتطاعًا.** بل يعني أن الاستدلال استهلك الميزانية قبل أول محرف مفيد — وقد قيس ذلك عند 15,850 token للاستدلال مقابل 148 مفيدة. وتتطلب الحالتان إجراءين متعاكسين: فتقليل حجم المقاطع لا يفيد في الحالة الأولى. وتفرّق الرسالة بينهما صراحة. وأُضيف حاجزان آخران بناءً على القياسات: يستجيب الموجّه بالحالة **200 وجسم لا يحتوي إلا على خطأ** عند فشل المستضيف الوسيط (كان `choices[0]` يطلق `TypeError` مبهمًا يحجب الرسالة)، كما تُقرأ نافذة السياق من الكتالوج ثم تُسجّل في `MODEL_TOKEN_LIMITS` — إذ إن `DEFAULT_TOKEN_LIMIT` غير صحيح لـ44 نموذجًا في الكتالوج، منها نموذجان محدودان عند 4,095 token.

  - **`--model fournisseur/modèle` إلزامي، وتُتحقق صيغته قبل أي اتصال بالشبكة.** OpenRouter ليس مورّدًا: فالاختيار يحدد السعر والترخيص ومعالجة البيانات، ولا يجوز اتخاذه نيابةً عن المستخدم. ولأن slug يُدرج في URL الخاص بالفحص التمهيدي، فليست عملية التحقق مجرد تحسين لقابلية الاستخدام، بل هي الحاجز الذي يمنع حقن مسار فيه: تقبل regex ذات namespace المشتركة بين الموجّهين `a/b/..`، ومن هنا يأتي الرفض الصريح للمقطع الأب. ليس لـ`--eco` أي تأثير، وتوضح الرسالة ذلك.

  - **صُححت ثلاث صياغات، إحداها كانت خاطئة.** كان تحذير OpenAI بشأن `codex exec` يستهدف حقن ملف جلسة شخصي في runner مشترك، لا كون المستودع عامًا: وقد ورد بمعنى معكوس في README وCLAUDE.md والشيفرة. تغيّر موقع مصادقة OpenCode في الإصدار 1.18.27 (الجدول `credential` في `opencode.db` بدلًا من `auth.json`)؛ ظل الثابت «لا يُقرأ هنا مطلقًا» صحيحًا، لكن العنوان كان قديمًا. وأخيرًا، لم يعد قسم OpenCode يعرض مسارات لم تُختبر قط على أنها متكافئة: فقد قِيست بوابة Zen وOllama من البداية إلى النهاية، بينما لم تُقَس GitHub Copilot وLM Studio وllama.cpp، ويوضح README ذلك الآن.

  - **حملة قياس وجدول بالنماذج الموصى بها في README.** نُفذت أكثر من ثلاثمائة ترجمة على ثلاث مجموعات من المستندات — مقالة مدونة كثيفة في وضع `--news`، وهذا README بصيغة Markdown القياسية، وأربعة ملفات README لمشروعات معروفة أُخذت كما هي من GitHub — إلى أربع عشرة لغة. يميّز الجدول بين أمرين كانا يختلطان: الترجمات التي **تكتمل** والترجمات التي تكون **بنيتها مطابقة** للمصدر. لم تفقد ثلاثة نماذج أي معلومة قط في المستندين الكثيفين: `gemini-3.7-flash`، و`gpt-5.6-sol` عبر اشتراك ChatGPT، و`z-ai/glm-5.2` عبر OpenRouter — ولم تتجاوز اختلافاتها زوجًا من `**` لم يُنقلا في لغة أو لغتين. والخلاصة الأساسية هي أن **العامل الفاصل هو كثافة المستند، لا وضع `--news`**: يفشل Grok عبر الاشتراك ثلاث عشرة مرة من أصل أربع عشرة في مقالة المدونة، وينجح في أربعة عشر README عامًا من أصل ستة عشر، والسبب هو فقدان التماسك في مقطع طويل، وقد تحقق منه باختبار مضاد. ويتضمن الجدول تحذيره الخاص: فهو غير شامل ومؤرخ، والمدد لا تمثل ترتيبًا، وتظل المنهجية الصحيحة هي القياس على مستندات المستخدم نفسه.

  - **صُحح مقارن البنية قبل نشر أرقامه، لأنه كان ينتج نتيجتين إيجابيتين زائفتين في الكتابات غير اللاتينية.** لم تكن regex التي تتوقف عند `)` تفصل URL متبوعًا بقوس صيني بعرض كامل `）`، ولذلك كانت السلسلة المستخرجة تختلف رغم تطابق URL؛ كما كان اقتباس من خمسة أسطر بالفرنسية يشغل ثلاثة أسطر بالصينية يؤدي إلى انخفاض العد القائم على الأسطر. وقد تحقق من التصحيحين باختبار مضاد: فلا يزال حذف URL أو قسم أو شيفرة مضمنة يُكتشف. ومن دونهما، كانت نتائج Gemini وCodex ستُنشر عند إحدى عشرة لغة من أصل أربع عشرة بدلًا من ثلاث عشرة واثنتي عشرة.

  - **الاختبارات**: ملف جديد `tests/test_openrouter_provider.py` ‏(39 اختبارًا) — التحقق من النموذج ورفض المقطع الأب، وتثبيت المستضيفين (الحد والحالة والحد غير المعلن والحد الأدنى المشترك)، وبقاء `allow_fallbacks` دائمًا false، وتعطيل الاستدلال أو إبقاؤه وفق `mandatory`، وعقد الإخراج الكامل (خطأ داخل استجابة 200، وعدم وجود أي خيار، وتمييز الصفحة الفارغة عن الاقتطاع، و`finish_reason` غير الطبيعي، والمحتوى الفارغ)، وفشل الفحص التمهيدي بأسلوب مغلق عند تعذر الوصول إلى الكتالوج أو غياب slug أو عدم وجود مستضيف سليم، وحصرية flag وتسمية اسم الملف. أصبح إجمالي الحزمة **427 اختبارًا**.

  - **إعادة هيكلة: قُسمت الوحدة الوحيدة ذات 4,253 سطرًا إلى وحدات، من دون تغيير سطر واحد من السلوك.** وُزّع `src/aipmt/translate.py` على `config` و`markdown` و`segmentation` و`guards` و`placeholders` و`news` و`prompts` و`notes` و`naming` و`pipeline` و`cli` وحزمة فرعية `providers/` (وحدة لكل موفّر، و`base` كأساس، و`registry` للحل وdispatch). كل عملية نقل هي commit يحمل دليلًا آليًا: تقارن أداة تحقق AST لجميع عقد المستوى الأعلى في الحزمة بلقطة مرجعية، وتتحقق من موقع كل رمز، وبقاء علامات الأمان حرفيًا، وعدم وجود ملف غير متتبّع — وهي أدوات مؤقتة أزيلت في الإصدار التالي. أما التغييرات المرئية: فقد أصبح `aipmt.translate` واجهة تعيد عرض الأسماء الـ64 التي كانت الوحدة تعرضها من دون بادئة `_`، مع الحفاظ على تطابق الكائنات (`__all__` يحتوي تسعة منها، وهي API المدعومة؛ أما البقية فهي أسماء بديلة للتوافق)، وتوقف عن إعادة تصدير أسماء التبعيات والمكتبة القياسية الـ29 التي كان يجمعها `import *`؛ ولم يعد التنفيذ المباشر للملف (`python src/aipmt/translate.py`) موجودًا — ويظل `aipmt` و`python -m aipmt` الصيغتين المدعومتين؛ وأصبح `__module__` للدوال العامة هو الخاص بوحدة تعريفها؛ وتُستورد SDK بعد تحميل `.env` بدلًا من قبله، من دون آثار معروفة. حُفظت الاختبارات الـ427 حتى على مستوى المعرّفات، ونُقلت إلى الوحدات التي تختبرها: أصبحت 91 عملية patch كانت تمر عبر الواجهة تستهدف الآن الوحدة التي تطّلع على الاسم (ظل تسعة منها ناجحًا من دون patch الخاص بها، وفق القياس)، وتثبّت سبعة اختبارات عقد الواجهة، كما أُعيدت كتابة أدوات البوابات قبل أول عملية نقل كي لا يتحول أي منها إلى النجاح بمجرد توقفه عن التحقق — نطاق Lizard بحسب المجلدات مع حد أدنى، وقراءة flags من parser المُنشأ، وحد أدنى للتغطية بحسب الحزمة، و`release.sh` الذي يسرد الوحدات المتتبّعة.

- **1.12.0** الموفّر `--use_opencode`: ‏OpenCode، الوكيل مفتوح المصدر، نحو المورّد الذي يختاره المستخدم — نموذج محلي، أو مجاني بلا حساب، أو اشتراك، أو مفتاح (2026-09-04):

  - **المسار الثامن للموفّرين، وهو مختلف في طبيعته عن المسارات السبعة الأولى.** لا يُعد [OpenCode](https://opencode.ai) ‏(MIT) مورّد نماذج، بل _موجّهًا_ نحو الجهات التي أعدّها المستخدم في OpenCode نفسه: مفتاح API، أو اشتراك (GitHub Copilot وChatGPT وSuperGrok)، أو بوابة OpenCode Zen — التي تقدم نماذج مجانية **من دون حساب** — أو نموذج **محلي** (Ollama وLM Studio وllama.cpp). يتحكم السكربت في `opencode run` بالوضع غير التفاعلي، كما يتحكم في Codex وGrok، ويعيد استخدام أساس العمليات الفرعية نفسه (مجموعة عمليات مستقلة، ثم `SIGTERM` فـ`SIGKILL` عند timeout، وstdin مغلق دائمًا، وبيئة منقّحة). وقد تحقق منه عبر **ترجمتين فعليتين**: ملف README هذا كاملًا إلى الإنجليزية عبر `opencode/mimo-v2.5-free` — خلال 49 ثانية، بتمرير واحد، وبنية مطابقة للملف المصدر (32 عنوانًا، و26 إغلاقًا للشيفرة، و18 رابطًا، و37 URL، و37 سطر جدول، و135 شيفرة مضمنة) — وملف اختبار عبر `ollama/qwen2.5:7b`، محليًا ومن دون أي مفتاح.

  - **`--model provider/modèle` إلزامي، وهو اختيار مقصود.** من دون `--model`، يعود OpenCode إلى إعداده الافتراضي، الذي يكون في التثبيت الجديد `opencode/big-pickle`، وهو نموذج مجاني «خفي» قد تُستخدم تفاعلاته في التدريب — وقد أثبت القياس أن هذا هو النموذج الذي أجاب. إن اختيار ذلك بصمت نيابةً عن المستخدم سيكون تحديدًا من نوع التحول الخفي الذي يتعقبه هذا المستودع؛ ولذلك تسمّي رسالة الخطأ الأمر الذي يسرد النماذج (`opencode models`) وتعرض ثلاثة أمثلة (محلي ومجاني واشتراك). ليس لـ`--eco` أي تأثير، وتوضح الرسالة ذلك. ويُمرر `--reasoning_effort` كما هو بصفته `--variant` الخاص بـOpenCode، وفقط بناءً على طلب صريح.

  - **عزل مُقاس، لا مُفترض.** يعرّف إعداد مضمن (`OPENCODE_CONFIG_CONTENT`، وهو الأخير في ترتيب دمج OpenCode، ولذلك تكون له الأولوية على إعداد المستخدم من دون استبداله) وكيلًا باسم `aipmt` تُرفض له جميع الأدوات (`permission: {"*": "deny"}`): فلا يعرضها السجل أصلًا على النموذج، وعندما يُطلب منه «سرد الملفات وتشغيل `id`» يجيب بأنه لا يملك أدوات. تُعطّل مشاركة الجلسة، وتُستبعد plugins الخارجية (`--pure`)، ولا يُستخدم `--auto` مطلقًا، ويكون مجلد العمل مؤقتًا وفارغًا. وقد قيس حقنان صامتان وعُطّلا: فمن دون `OPENCODE_DISABLE_CLAUDE_CODE`، يدخل `~/.claude/CLAUDE.md` الخاص بالمستخدم في **كل** prompt ‏(515 token إدخال بدلًا من 186 لعبارة «مرحبًا» بسيطة)؛ ومن دون `OPENCODE_DISABLE_PROJECT_CONFIG` يدخل أيضًا `AGENTS.md` الخاص بالمجلد الحالي — وقد طُبقت على الترجمة تعليمة تقول «اختم كل إجابة بكلمة BANANA». أما `~/.config/opencode/AGENTS.md` العام فيظل محقونًا: فلا يوجد مفتاح يعطّله، كما أن التحايل عليه عبر `XDG_CONFIG_HOME` محوّر سيحجب أيضًا مورّدي المستخدم. لذلك وُثّق الأمر بدل ترقيعه.

  - **لا يثبت `exit 0` شيئًا؛ إنها CLI ثالثة، وتستدعي الاحتياطات نفسها — مع فخين خاصين بها.** لا يؤدي `--agent` غير المعروف إلى إفشال `opencode run`: بل يصدر تحذيرًا على stderr ثم يرجع **بصمت** إلى وكيل البرمجة مع أدوات مفعّلة. ولذلك، إن لم يُؤخذ الإعداد المضمن في الحسبان، فستنطلق الترجمة بوكيل قادر على الكتابة؛ ومن ثم يتحقق عقد الإخراج من غياب هذه الرسالة، بالإضافة إلى: رمز إرجاع 0، وعدم وجود أي حدث `error`، وعدم وجود `tool_use`، وأن يكون آخر `step_finish` في `stop` (إذ إن `length` استجابة مقتطعة)، ونص غير فارغ. أما الفخ الثاني فهو أن حدث الخطأ بصيغة JSON **مبهم** — «خطأ غير متوقع في الخادم. راجع سجلات الخادم للحصول على التفاصيل.»، مع مرجع بسيط — وأن السبب الحقيقي (`ProviderModelNotFoundError: Model not found: foo/bar. Did you mean…` و`ProviderAuthError`…) لا يوجد إلا في السجلات. ومن هنا يأتي `--print-logs --log-level ERROR` وقراءة الحقل `error="…"` من stderr، من دون تتبع Bun الذي يليه. وهكذا يفشل النموذج غير المعروف خلال ثانية واحدة مع تسمية السبب. ويمنع `--title` كذلك استدعاء LLM طفيليًا: فمن دونه ينشئ OpenCode عنوانًا للجلسة عبر دورة إضافية على `small_model`.

  - **الأسرار: الترشيح بالنمط نفسه المستخدم مع Codex وGrok، باستثناء واحد محدد بالاسم.** يُحتفظ بـ`OPENCODE_API_KEY`: فهو مفتاح OpenCode نفسه (بوابة Zen واشتراك Go)، وموجّه إليه باسمه — أي ما يعادل `auth.json` الخاص به، وليس مفتاحًا يديره aipmt أو يمكنه احتساب تكلفته. يُعدّ المورّدون داخل OpenCode ‏(`opencode auth login` و`opencode.json`)، ولا يُعدّون أبدًا داخل `.env` الخاص بـaipmt، ولا يصل أي مفتاح منه إلى العملية الفرعية. ولا يوجد رفض في CI، بخلاف CLI الخاصة بالاشتراكات: فاستخدام مفتاح API أو نموذج مستضاف ذاتيًا على runner استخدامان مشروعان.

  - **أصبح حاجز منع اجتياز المسارات يتحقق من القيمة المدرجة، لا القيمة الخام.** يحتوي `provider/modèle` على `/` كان حاجز الإصدار 1.10.0 يرفضه — وكان محقًا، لأن `--model` كان يُدرج في اسم الملف `--include_model`. تستبدل تسمية اسم الملف الآن `/` و`\` و`:` بـ`-` قبل أي إدراج (`ollama/qwen2.5:7b` ← `ollama-qwen2.5-7b`، إذ إن `:` غير قانوني في Windows)، ويفحص الحاجز السابق هذه التسمية: فيصبح `../../evil` الاسم البسيط `doc-en-..-..-evil.md` داخل الوجهة، ولا يظل مرفوضًا إلا `..` وحده، وكذلك `--target_lang ../x`. ويظل حاجز النطاق `_ensure_within_directory` الطبقة الثانية من دون تغيير.
  - **النماذج المجانية والنماذج المحلية، وما تم قياسه.** يترجم `opencode/mimo-v2.5-free` فقرةً في 16 ث وملف README هذا في 49 ث؛ ويستغرق `opencode/big-pickle` مدة 40 ث لمعالجة 200 كلمة، كما ظل طلبان متزامنان فيه بلا استجابة لمدة 5 دقائق، بينما ينجح كل منهما عند تشغيله منفردًا؛ ولم يُرجع `opencode/nemotron-3.5-lightning-free` أي استجابة خلال 3 دقائق. ومن هنا جاء `REGEN_PROVIDER=opencode` مع إلزامية `REGEN_MODEL` وتشغيل **مهمتين** بالتوازي. محليًا، يضبط Ollama غالبًا سياقًا من 4 096 token، بينما يصل طول المقاطع إلى 16 000 محرف: لذلك لا بد من `Modelfile` مع `PARAMETER num_ctx 32768`، وتعتمد الجودة على النموذج — فقد عكس نموذج 7B ترتيب قائمة وأفسد سياج كتلة برمجية في ملف الاختبار، بينما حافظ نموذج البوابة على كل شيء.

  - **لم تعد ترجمات هذا المستودع تمر مطلقًا عبر API مدفوعة.** كان `regen_translations.sh` يستخدم OpenAI API بمجرد وجود مفتاح في `.env`، ولم يكن يتيح Codex إلا باختيار صريح. وهذا بالضبط ما حدث أثناء إعداد هذا الإصدار: أُرسلت الترجمات الـ28 عبر OpenAI API، ثم أُرسل CHANGELOG الهندي عبر Gemini API، مع أن اشتراك ChatGPT موجود تحديدًا لتجنب الدفع حسب الاستخدام. أُزيل الاكتشاف التلقائي للمفاتيح: **Codex هو الخيار الافتراضي، مع `gpt-5.6-sol`**، وهو نموذج الجودة؛ وتتطلب `openai` و`gemini` و`grok` استخدام `REGEN_ALLOW_PAID_API=1` بالإضافة إلى `REGEN_PROVIDER`، وهو استثناء مسمّى كي تُطبَّق القاعدة لحظة اتخاذ القرار؛ ويفشل أي `REGEN_PROVIDER` مجهول بدل الرجوع إلى API. تثبّت عشرة اختبارات الخيار الافتراضي والرفض والاستثناء. وأُعيد إنجاز الترجمات الـ28 لهذا الإصدار عبر Codex.

  - **وُحِّدت آلية التراجع التدريجي عند بلوغ حد المعدل** (`_retry_on_rate_limit`): كانت حلقتا Codex وGrok متطابقتين باستثناء التسمية، وكانت نسخة ثالثة ستتجاوز عتبة التكرار. ترث أخطاء CLI الثلاثة من `_CliCallError` واحدة؛ ويمنع اختبار خروج أي واحد منها عن هذا التسلسل الهرمي، وإلا فلن تلتقطه الحلقة المشتركة.

  - **الاختبارات**: ملف جديد `tests/test_opencode_provider.py` ‏(51 اختبارًا) — عقد الإخراج الكامل، والرجوع إلى الوكيل، وقراءة السبب من السجلات، وإزالة أجزاء النص المكررة وتجاهل الأجزاء الاصطناعية، وإنهاء مجموعة العمليات عند انتهاء المهلة، والتراجع التدريجي عند 429، وإلزامية النموذج والتحقق منه، والفحص المسبق بلا أسرار، وحل مسار الملف التنفيذي، وتوصيل التوجيه، وتسمية اسم الملف، والاختبار المضاد لاجتياز المسار. يوسّع `tests/test_review_hardening.py` حصرية flags وغياب الأسرار لتشمل provider الجديد. تتطلب البوابة الآن توثيق **22 flag** من argparse. المجموعة الكاملة تضم **382 اختبارًا**.

- **1.11.1** تصحيح للتوثيق: يذكر README أخيرًا مسارات provider السبعة (2026-09-03):

  - **كانت صفحة PyPI للإصدار 1.11.0 تقول «4 APIs + ‏Codex CLI».** لكن الشيفرة تتيح سبعة مسارات — OpenAI وMistral وClaude وGemini وGrok عبر API؛ وCodex ‏(ChatGPT) وGrok عبر الاشتراك، من دون رسوم حسب الاستخدام. كان نمطا Grok غائبين عن المقدمة وعن بند _تعدد موفري الخدمة_، وكررت الترجمات الـ14 الخطأ نفسه. ولأن الوصف الطويل للحزمة يظل ثابتًا لكل إصدار، تطلّب تصحيح الواجهة رقم إصدار جديدًا: وهذا هو السبب الوحيد لوجود هذا الإصدار. **لا تغييرات في الشيفرة.**
  - وُوفِّق `CLAUDE.md` مع ما أدخله النشر: عدّادات البوابة (16، و17 في `--full`)، وسير العمل النشط الأحد عشر، وعدّادا Sonar/Codacy غير الظاهرين في `gh pr checks` ‏(hotspots وCodacy API)، ونقل `# nosemgrep` عبر `ruff-format`، وبيئات GitHub التي يتطلبها تبادل OIDC، وحقيقة أن _الناشر المعلّق_ لا يحجز الاسم.

- **1.11.0** النشر على PyPI: ‏`pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-09-03):

  - **تحول السكربت أحادي الملف إلى حزمة قابلة للتثبيت.** انتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول console باسم `aipmt` والمكافئ `python -m aipmt`. يظل المستودع المستنسخ ضروريًا للمساهمة — إذ تعيش فيه الاختبارات والترجمات الـ28 وأدوات الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` مطلقًا**، لأن التعارض حقيقي وصامت. تثبّت حزمة PyPI المسماة `translate` ‏(الإصدار v3.8.1، وآخر رفع في 2026-07-06) مجلدًا يحمل الاسم نفسه. أُعيد إنتاج المشكلة في venv: يتغلب المجلد على module، ويختفي `translate.main`، وتتعطل نقطة الدخول عند `AttributeError` — ومع ذلك يُرجع `pip check` الرسالة «لم يُعثر على متطلبات معطلة» مع rc=0. كان يكفي وجود `pip install translate` بسيط لدى المستخدم لتعطيل CLI من دون تشخيص قابل للاستفادة منه. وفي الاختبار المضاد على wheel الفعلي: ثُبّت `pip install translate` فوق الحزمة، وظل `aipmt --help` يعيد rc=0 قبل التثبيت وبعده، وتعايشت واجهتا CLI.
    - **اسم توزيع طويل وأمر قصير.** يجعل `ai-powered-markdown-translator` العثور على الحزمة ممكنًا عبر بحث PyPI؛ أما الاختصار وحده فلن يعثر عليه من لا يعرف المشروع مسبقًا، مع أن النشر يهدف تحديدًا إلى تسهيل اكتشافه. استُبعد مرشحان معقولان بعد التحقق: الاسم `ai-markdown-translator` محجوز على npm منذ 2024 لأداة تؤدي الغرض نفسه وتسبق هذا المستودع بـ17 شهرًا؛ كما أن `aimt` يختلف بحرف واحد فقط عن `aim` ‏(الإصدار v3.29.1)، وهي حزمة نشطة في المجال **نفسه** — أسوأ وضع ممكن لالتباس دائم. وهناك فخ منهجي أيضًا: يُرجع `pypi.org/project/<nom>/` الرمز 200 لأي اسم كان بسبب صفحة مكافحة الروبوتات، ولا يُعتد إلا بـJSON API.
    - **تخطيط `src/` بدل الحزمة المسطحة.** كان من شأن الحزمة المسطحة الحفاظ على عبارات `sys.path.insert(..., "..")` الست في الاختبارات، وهذه هي المشكلة تحديدًا: فهي تجعل الاختبارات تستورد شجرة المصدر بدل الحزمة، ما قد يخفي أي خطأ في التحزيم. التكلفة الفعلية: قاعدة استبدال إضافية واحدة.

  - **أصبح بالإمكان أخيرًا ضبط المفاتيح مرةً واحدةً بصورة دائمة.** لم تكن CLI المثبتة تملك أي إعداد دائم: لم يكن متاحًا سوى متغير البيئة وملف `.env` في المجلد الحالي. صحيح أن `find_dotenv` يصعد حتى جذر النظام، ولذلك كان يعثر على `~/.env` **عند العمل داخل المجلد الشخصي للمستخدم**، لكنه لا يعثر على شيء عند العمل في مكان آخر — أي تغطية تعتمد على مكان تشغيل الأمر لا على قرار تصميمي. لذلك أُضيفت طبقة ثالثة: `~/.config/aipmt/.env`، أسفل الطبقتين القائمتين.

    - **الأولوية ليست مكتوبة صراحةً في الشيفرة**، بل تنتج عن `override=False`، وهي القيمة الافتراضية لـ`load_dotenv`: فلا تملأ كل طبقة إلا ما تركته سابقتها فارغًا. ومن هنا يأتي الترتيب: متغير البيئة ← ملف `.env` الخاص بالمشروع ← إعداد المستخدم، وقد جرى التحقق منه باختبارات **سلوكية** لا بنيوية — فعكس ترتيب الاستدعاءين يُفشلها، وكذلك حذف الطبقة الثالثة.
    - **التنسيق `.env` وليس TOML**، عن قصد: ‏`python-dotenv` تبعية موجودة أصلًا، والصياغة موثقة بالفعل في ملفات README الخمسة عشر، ويُستخدم الملف نفسه للنطاقين. لا تبعية جديدة ولا صياغة جديدة. يتبع الموقع `XDG_CONFIG_HOME` إذا كان **مطلقًا** — إذ تقتضي المواصفة تجاهل القيمة النسبية، وإلا عاد موقع الإعداد تابعًا للمجلد الحالي — ويتبع `APPDATA` على Windows.
    - **استُبعد خياران، مع بيان السبب.** مخزن مفاتيح النظام (`keyring`) أكثر أمانًا على حاسوب مكتبي، لكنه يفشل في البيئات headless — مثل الخادم والحاوية وCI — وهي تحديدًا حالة استخدام الترجمة المجمعة؛ لذا فهو مرشح جيد كخيار صريح، لكنه خيار افتراضي سيئ. أما flag باسم `--api-key` فسيضع المفتاح في سجل shell ويجعله ظاهرًا في `ps`.
    - **عند غياب المفتاح، لم يعد يظهر تتبع للاستدعاءات.** كان المستخدم يتلقى stack trace من Python يشير إلى `site-packages`، ورسالة تذكر «البيئة أو .env» من دون توضيح مكان إنشاء الثاني. أما الآن فتسرد الرسالة المواقع الثلاثة مع المسار الدقيق لكل منها، وينتهي الأمر بالرمز 2. نطاق شبكة الأمان **ضيق عن قصد**: ‏`except ValueError` حول مرحلة الإعداد وحدها. فتغليف التنفيذ كله سيحوّل خطأً حقيقيًا يحدث أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الفشل الذي يتعقبه هذا المستودع. ويقرأ اختبار مصدر `main()` لمنع ذلك.

  - **تصحيح — كان ملف `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** لا يصعد `load_dotenv()` من المجلد الحالي عند استدعائه بلا وسيط، بل يبدأ من الملف **المستدعي**، أي من `site-packages`. قيس ذلك بنقطة دخول console حقيقية شُغلت من مشروع يملك ملف `.env` خاصًا به: يعيد `find_dotenv()` القيمة `''` ولا يُحمَّل المفتاح، بينما يعثر عليه `find_dotenv(usecwd=True)`. لم يكن الخطأ موجودًا ما دامت الأداة لا تعمل إلا من المستودع المستنسخ؛ وكان النشر سيجعله منهجيًا، مع عَرَض وحيد هو الإبلاغ عن مفتاح API «مفقود» رغم صحة الإعداد.

  - **كان يمكن لثلاث بوابات أن تتحول إلى الأخضر بعد أن توقفت عن التحقق من أي شيء.** لذلك شُدِّدت **قبل** النقل، عن قصد: فكتابة آلية حماية بعد التغيير الذي يُفترض بها اكتشافه لا تثبت شيئًا. كل منها أخضر على المستودع الأصلي ويتحول إلى الأحمر على نسخة منقولة — وقد قيس الاتجاهان.

    - **يتجاهل Lizard المسار الغائب من دون قول شيء**: ‏rc=0، و«تم تحليل 0 ملف». كانت بوابة التعقيد ستنخفض من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع إخراج حجمه صفر بايت. أصبح النطاق الآن مصفوفةً يُتحقق من وجود كل عنصر فيها.
    - **لا يفشل `coverage run --source=` عند استهداف module غائب**: يظهر تحذير على stderr فقط، مع rc=0 لكل من unittest و`coverage xml`، ويُنشر التقرير رغم ذلك — بعد انخفاضه من 1453 إلى 141 statement. كان المشروع سيبدو سليمًا لأنه لم يعد خاضعًا إلا لتحليل ضئيل جدًا. يحمي التقرير حدان أدنيان: الإجمالي، وأكبر ملف جرى قياسه.
    - **مسبار حداثة الترجمات أعمى بنيويًا عن صيغة الاستدعاء**: فهو يرتكز على flags الخاصة بـargparse، وهي تحديدًا ما لا يغيره تغيير اسم الملف. أُعيد إنتاج الحالة: نُقل module، وظلت ملفات README الخمسة عشر توثّق أمرًا غير موجود، وكانت النتيجة «لا توجد ترجمة قديمة». لذلك يتحقق قسم سابع من **الصيغة** لا من الخيارات، وتُقارن وصلة Lizard بالنطاق الفعلي للسكربت — فمفتاحها `files:` لا يجعل pre-commit يفشل عندما لا يعود متطابقًا، بل يجعله **يتخطاها**.

  - **لم يعد `requires-python = ">=3.10"` مجرد ادعاء.** كان `sonar-project.properties` يعلن بالفعل دعم 3.10-3.12 من دون أن يكون أي شيء قد اختبر ذلك قط، إذ لم يكن على جهاز التطوير سوى 3.12 — وهو تناقض داخلي كان النشر سيجعله علنيًا. ينفذ سير عمل اختبارات الآن المجموعة على 3.10 و3.11 و3.12، مع تثبيت **الحزمة** وبالتالي اختبار حدودها المعلنة.

  - **حدود دنيا من دون حد أقصى.** يظل `requirements.txt` ملف القفل المختبَر، بينما يصبح `[project.dependencies]` العقد العام: فنشر الإصدارات الدقيقة الموجودة في ملف القفل سيسبب تعارضات لأي مستخدم لديه حزم أخرى. ولا يوجد حد أقصى `<N+1` أيضًا — إذ سيتعارض مباشرةً مع `check-deps-fresh.sh`، الذي يجعل بوابة الإصدار تفشل عند أي تأخر في إصدار رئيسي. تنجح مجموعة الحدود الدنيا، بينما يخرج الاختبار المضاد `openai==1.0.0` بالحالة `ResolutionImpossible`، ما يثبت أن الفحص يميّز الحالات بدل قبول كل شيء. وتمنع آلية حماية كذلك اختلاف إصدار `pyproject.toml` عن إصدار CHANGELOG: فلا يسمح PyPI بإعادة استخدام رقم إصدار.

  - **جرى التحقق من البداية إلى النهاية في venv جديد**: ملف wheel بحجم يقارب 70 Ko لا يحتوي إلا على `aipmt/*.py` وdist-info والترخيص؛ و`aipmt --help` يعيد rc=0 مع 22 flag؛ ويعرض `python -m aipmt` العبارة «usage: aipmt» لا «usage: \_\_main\_\_.py»؛ ويعمل تثبيت `pipx`؛ والأهم **تنفيذ ترجمة فعلية من الفرنسية إلى الإنجليزية من أي مجلد مستخدم**، مع الحفاظ على الخط العريض والقائمة وinline code والرابط وURL، وعدم ترجمة كتلة الشيفرة. تنجح الاختبارات الـ318 السابقة للنقل مع قائمة معرّفات متطابقة بايتًا ببايت قبله وبعده — وهذا ما يثبت أن أي اختبار لم يُعطَّل، لا مجرد ظهور «OK»؛ وأضيف إليها اثنا عشر اختبارًا للإعداد ذي الطبقات الثلاث، ليصبح المجموع 330.

- **1.10.0** ‏Provider ‏`--use_codex` ‏(حصة اشتراك ChatGPT)، وتحديث SDK والنماذج، وتصحيح اقتباسات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — آليتا حماية وضعتهما PR من دون الالتزام بهما في كل موضع**:

    - **كان الفحص المسبق لـCodex يمرر `.env` كاملًا إلى الملف التنفيذي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: فترث العملية الفرعية `os.environ` كاملًا، وبالتالي كل محتوى `.env` الذي حمّله `load_dotenv`. وعند القياس بملف تنفيذي زائف مزود بأدوات رصد، وصلت **سبعة أسرار** إلى الفحص المسبق — مفاتيح providers الستة بالإضافة إلى `GITHUB_TOKEN` — مقابل **صفر** لنظيره `_grok_preflight`، الذي كان يمرر `env=_grok_env()` فعلًا. كان التناقض داخليًا في PR: إذ يوجد `_strip_secret_env` تحديدًا للحفاظ على هذا الثابت، على بُعد بضعة أسطر فقط. استُخرج `_codex_env_base()` وأصبح مشتركًا بين المسارين؛ والقياس بعد التصحيح: صفر سر في كلا الجانبين.
    - **لم تشمل خاصية «`--deny` يفشل بصورة مغلقة» الصيغة المستخدمة.** كان التعليق يبرر عزل Grok كله بأن قاعدةً ذات prefix مجهول تجعل التشغيل يُرفض. وعند القياس على `grok 1.0.13`، اتضح أن هذا التحقق لا يوجد **إلا للصيغة ذات الأقواس**: يرفض `--deny 'CeciNestPasUnOutil(*)'` التشغيل برسالة «بادئة أداة مجهولة»، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. لكن `GROK_DENY_RULES` لم يكن يستخدم سوى الأسماء المجردة — ولذلك كان من الممكن أن تؤدي إعادة تسمية أداة من جانب xAI إلى إزالة طبقة العزل الوحيدة المقيسة بلا أي إشارة، على جهاز لا ينطبق عليه أصلًا sandbox نظام التشغيل. نُقلت القواعد الثماني المسماة إلى `Prefix(*)`، مع التحقق من أن كلًا منها prefix معروف لدى CLI؛ ويظل catch-all ‏`*` بصيغته الحرفية، وهي الصيغة الوحيدة المقبولة. ويمنع اختبار الرجوع إلى الصيغة غير المتحقق منها.
    - **وتبيّن سلامة بقية الجوانب**: لا يوجد حقن أوامر، إذ تُستخدم صيغة القائمة في كل موضع ولا يُستخدم `shell=True` مطلقًا، ويُمرر محتوى المستند عبر stdin أو `--prompt-file`؛ ولا توجد إزالة تسلسل غير آمنة، إذ لا يُستخدم إلا `json.loads` مع حارس للنوع؛ ولم يُعثر على طريقة لتجاوز تصحيح اجتياز المسار باستخدام سبع حمولات اختبار؛ كما يطبق CLI بالفعل `--deny '*'`، إذ رُصد `DENY_ENFORCED` عند محاولة قراءة خارج workdir.
    - وكان فحص الحداثة المضاف أعلاه يلتف بالمناسبة على مبدئه نفسه: إذ كانت الحزمة التي يفشل طلب PyPI الخاص بها تُتخطى بصمت وتظل البوابة خضراء. أما الآن فيعد الحزم التي قورنت فعليًا، ويفشل إذا كانت التغطية ناقصة.

  - **حُدِّثت التبعيات، وأضيفت شبكتا أمان لمنع تكرار التأخر**:
    - **كان التأخر حقيقيًا ومستمرًا**: `openai` 2.54 → **3.6.0**، و`anthropic` 0.125 → **1.2.0**، و`certifi` 2024.8.30 → **2026.7.22** — أي تأخر لمدة عامين في مخزن الشهادات الجذرية الذي يتحقق من TLS لجميع استدعاءات providers. السبب المحدد: **لم يكن `.github/dependabot.yml` موجودًا**. من دون هذا الملف، لا يفعّل GitHub سوى _security updates_، ولا يقترح Dependabot طلب PR إلا لتبعية مستهدفة بثغرة CVE — وهذا يفسر سبب ترقيته `urllib3` و`idna` مع ترك SDK اثنين يبتعدان بإصدار رئيسي كامل.
    - **يتعايش الإصداران الرئيسيان من دون تعارض**، خلافًا لما أوحى به استنتاج سابق: ينتقل `openai` 3.x و`anthropic` 1.x إلى **`httpx2`**، بينما يظل `mistralai` و`google-genai` على `httpx<1`، لكنها توزيعات منفصلة. جرى التحقق عبر تثبيت فعلي، ثم عبر **مسارات providers السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الكود المضمن والروابط في كل مخرَج. كان «تجنب مكدسين لـHTTP» تفضيلًا لا عائقًا: وقد حسم القياس المسألة.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: كانت `google-auth` و`cryptography` ومكدس `opentelemetry` مثبتة في venv العمل من دون التصريح بها مطلقًا — ولذلك لم يكن التثبيت الجديد يعيد إنتاج ما يجري اختباره. وفي المقابل، كانت `tokenizers` و`huggingface-hub` و`PyYAML` مدرجة فيه من دون أن يستوردها أو يتطلبها أي شيء، بوصفها بقايا من `mistralai` 1.x. أُعيد توليد الملف بوصفه الإغلاق الكامل لـvenv مبني من التبعيات المباشرة وحدها. لا يُبلغ `pip-audit` عن أي ثغرة معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** (جديد) يفعّل تحديثات الإصدارات الأسبوعية لـpip وgithub-actions. جُمعت الإصدارات الثانوية والتصحيحات في طلب PR واحد — فالترقية التصحيحية في كل طلب PR ينتهي بها الأمر إلى التجاهل، والضجيج عدو التحديث؛ أما **الإصدارات الرئيسية فمنفصلة**، ويتطلب كل منها تحققًا عبر استدعاء فعلي.
    - **`scripts/check-deps-fresh.sh`** (جديد وموصول بالـgate) يجعل التأخر ظاهرًا في نتيجة المشروع: يقترح Dependabot، لكنه لا يضمن، وقد تتراكم طلبات PR الخاصة به. التأخر في إصدار رئيسي ← فشل؛ وفي إصدار ثانوي ← تحذير، لأن gate دائم الاحمرار ينتهي به الأمر إلى التجاهل؛ وتعذر الوصول إلى PyPI ← تخطٍ صريح محليًا و**fail-closed في CI**، لأن الفحص الذي لم يُنفذ لا يُعد نجاحًا. جرى التحقق في الاتجاهين: يلتقط الحالة الدقيقة السابقة للإصلاح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير عند وجود إصدار ثانوي.

  - **إصلاحات ناتجة عن مراجعة طلب PR هذا** — فحص خمسة وكلاء مراجعة الـdiff بدقة؛ وقد **أُعيد إنتاج** جميع النقاط أدناه **بالقياس** قبل إصلاحها، وكانت اثنتان منها تراجعين أُدخلا أعلاه في الإصدار نفسه.

    - **تراجع جرى إصلاحه — كان `_NEWS_CITATION_REGEX` يعاني من backtracking أُسّي.** أدخل إصلاح الفقرات المتعددة `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: فتقاسم المسافات بين `[ \t]+` و`.*` ملتبس، ويتضاعف هذا الالتباس من تكرار إلى آخر. جرى القياس على أسطر `>   texte` — وهي إزاحة Markdown سليمة تمامًا — لا تطابق النمط: **2,589 ms لـ14 سطرًا** مقابل 0.04 ms بعد الإصلاح، بمعامل يقارب 9 لكل سطر مضاف. في وضع `--news`، كان blockquote طويل وغير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب قابل للتحديد. يستهلك التكرار الآن السطر كاملًا دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يترك سوى طريقة واحدة للمطابقة في كل تكرار. جرى التحقق على المجموعة الفعلية المؤلفة من 231 مقالًا: **صفر اختلاف** في الالتقاط، ونفس الاقتباسات الـ423، مع استمرار توسيع الأجسام الـ14 متعددة الفقرات.
    - **كان تفعيل علامتَي provider معًا يؤدي إلى الفوترة بحسب الاستخدام بصمت.** كان `--use_codex --use_mistral` مقبولًا؛ يختبر `_select_provider_client` ‏Mistral أولًا، ويمنح `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. وهكذا كان المستخدم يطلب حصة اشتراكه ويحصل على فوترة بحسب الاستخدام، من دون أدنى تحذير: وهو تحديدًا نمط الفشل الذي وُجد `--use_codex` لمنعه. تمر علامات providers الست الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: سطر أوامر يجمع بين provider اثنين، وكان يُقبل سابقًا بصمت، يفشل الآن عند `argument --use_mistral: not allowed with argument --use_codex`.
    - **كان gate نهاية العمل يتحول إلى الأخضر عند تعطل مجساته.** كانت أربعة من فحوص `scripts/check-release-ready.sh` الثلاثة عشر تتبع نمط «التقاط stdout، والاستنتاج إذا كان فارغًا» من دون التحقق من رمز الإرجاع إطلاقًا: كان الاستثناء (ملف أُعيدت تسميته، `FileNotFoundError`) يكتب إلى stderr، ويترك stdout فارغًا، ثم يخلص الفحص إلى «لا شيء يستدعي الإبلاغ». وهكذا أُعيد إنتاج فخ «إن `exit 0` لا يثبت شيئًا» داخل السكربت المكتوب لمنعه. يفرض helper باسم `probe()` الآن رمز إرجاع يساوي صفرًا **مع** حارس نهاية، وترفض المجسات الاستنتاج من مجموعة معالم فارغة — لأن التأكيد على مجموعة فارغة يكون صحيحًا دائمًا. الدليل: أدت إضافة المجموعة الحصرية أعلاه إلى تمرير علامات providers عبر كائن `*_group`، فلم تعد regex القديمة `parser\.add_argument\(` تطابقه؛ وخرجت **ست علامات من أصل إحدى وعشرين** من النطاق بصمت، مع بقاء الـgate أخضر.
    - **كان فحص الأسرار يفوّت أربعة providers من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: كان `sk-proj-…` (تنسيق OpenAI الحالي) و`sk-ant-api03-…` ينقطعان عند الشرطة الثانية، ولم يكن `AIza…` مشمولًا. وُسّع النمط، واستُبعد `.secrets.baseline` من الفحص. علاوة على ذلك، كان الحارس `.env` يستعلم عبر `git diff --cached`، الذي لا يرى سوى الـindex: لذلك لم يكن `.env` **الملتزم به سابقًا** — وهو أسوأ سيناريو — يظهر مطلقًا. وهو يستعلم الآن عبر `git ls-files`.
    - **لم تكن «تهيئة token» الخاصة بـCodex تهيئة فعلية.** أظهر القياس أن `codex login status` لا يلمس `~/.codex/auth.json` (لم يتغير mtime ولا الحجم)، وتقول المساعدة الخاصة به «عرض حالة تسجيل الدخول». ومع ذلك، كان التعليق يزعم تحديث الـtoken «مرة واحدة، تسلسليًا»، ومن ثم تحييد خطر التحديثات المتزامنة لـtoken دوّار أحادي الاستخدام. لم تكن الحماية المعلنة موجودة؛ ويصف التعليق الآن ما يفعله الكود، بينما يظل الإجراء الوقائي الحقيقي هو `max_jobs=4`. كما يحترم الفحص `CODEX_BIN`، الذي كان يتجاهله — إذ كانت محطة عمل لا تحتوي على `codex` في `PATH` تفشل برسالة «غير مصادق عليه»، وهو تشخيص مضلل.
    - **كان `.env` يُحمّل في subshell.** تُستدعى `detect_provider` ضمن command substitution، ولذلك لم تكن صادراتها تصل إلى النطاق الأعلى: كان أي `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` معرّف في `.env` يظل غير مرئي للقراءات المنفذة في `main()`، التي كانت تستنتج «يتعذر العثور على ملف Grok التنفيذي» رغم صحة الإعداد.
    - **تجاوز التوازي الحد المعلن بنسبة 50%.** كان الحارس موضوعًا بعد تشغيل زوج README/CHANGELOG: وبلغت الذروة المقيسة **3 لـ`max_jobs=2`**. وفي Grok، حيث تُشارك الحصة الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يكن السكربت يلتزم بالحد الذي فرضه على نفسه. أما العد النهائي فكان يُعرض من دون مقارنته مطلقًا بـ28 — ولذلك كان غياب ملف يمر من دون ملاحظة.
    - **عقد مخرجات Grok: غياب `stopReason` أصبح الآن فشلًا.** كان الكود يطبّق «`end_turn` **أو غائب**» حيث كان العقد المعلن يتطلب `end_turn`. وكانت حمولة بلا هذا الحقل — أو أُعيدت تسمية حقلها بفعل تحديث للـCLI — تحوّل الحارس بصمت إلى no-op. كذلك لم يعد `max_turn_requests` يُصنف على أنه rate limit (فقد استُنفدت ميزانية الجولات: إعادة المحاولة تعيد النتيجة نفسها، مقابل انتظار 90 s)، وأُزيل `quota` من علامات rate limit — للسبب الذي كان docstring الخاص بـ`_codex_is_rate_limited` ينص عليه أصلًا من دون أن يطبقه Grok.
    - **أصبحت سلسلة Gemini ذات memoization بحسب النموذج.** كانت تبدأ مجددًا من `minimal` عند كل مقطع، رغم أن النموذج الافتراضي يرفضه: كان المسار الاسمي يدفع تكلفة رحلة ذهاب وإياب 400 لكل مقطع، ويعيد طباعة التحذير نفسه. والتحذير الذي يتكرر مئات المرات يتوقف الناس عن قراءته — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مكتوبة بشكل ثابت لـCodex، وتوجه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق قاعدة subprocess يعمم «shim» على أداتَي CLI، رغم أن ملف Grok التنفيذي هو ELF أصلي (التبرير الصحيح هو «وكيل يُنشئ subprocess خاصة به»)؛ وُسمت اثنتا عشرة نتيجة SAST على `subprocess` بـ`# nosec` / `# nosemgrep` مع تبرير، إذ إن صيغة القائمة من دون `shell=True` تجعل الحقن مستحيلًا، ومحتوى المستند لا يمر مطلقًا عبر argv.
    - **لم يعد أي سر يدخل إلى subprocess الخاصة بالوكلاء.** لم تكن قوائم المنع الاسمية تحمي سوى ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). أظهر القياس أن **سبعة أسرار أخرى** كانت تدخل رغم ذلك إلى كل subprocess — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح أداة CLI الأخرى، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة المرور. غير أن أداتَي CLI هاتين **وكلاء**، ويعمل وكيل Grok من دون sandbox لنظام التشغيل قابلة للتطبيق على كثير من محطات Linux. باتت التصفية تتم الآن **بحسب نمط الاسم** (`API_KEY` و`_TOKEN` و`SECRET` و`PASSWORD` و`CREDENTIALS`) لا بقائمة اسمية، ما يشمل أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن يعرفها هذا الكود. ولا تحتاج أدوات CLI إلى أي منها: فبيانات المصادقة موجودة في `~/.codex` و`~/.grok`، ولا تكون في البيئة مطلقًا — وقد جرى التحقق عبر **ترجمة فعلية ناجحة** باستخدام كل واحد من الـproviders الاثنين مع البيئة المحصنة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` ‏(21 اختبارًا) يثبت حصرية علامات providers، وعقد `stopReason`، وخطية regex الأخبار، ورسالة الرفض في CI، وmemoization الخاصة بـGemini، وغياب أي سر من بيئة subprocess. وهذا التأكيد الأخير **عام** — إذ يفشل عند مفتاح لا تسميه أي قائمة — بينما كان اختبار التنقية القائم انعكاسًا لثابته، ولم يكن قادرًا على اكتشاف أي شيء سوى تعطل حلقته الخاصة. مجموعة كاملة تضم **311 اختبارًا**.
  - **مزودان جديدان لـ Grok**: ‏`--use_grok` (واجهة API من xAI، المفتاح `XAI_API_KEY`، والفوترة حسب الاستخدام) و`--use_grok_cli` (واجهة CLI الرسمية لـ Grok Build، ويُحتسب استخدامها من اشتراك Grok — بالمبدأ نفسه الذي تتبعه `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: نظرًا إلى توافق نقطة نهاية xAI مع OpenAI، يُعاد استخدام العميل و`_call_openai` كما هما، ولا يتغير سوى `base_url`. لم يلزم سوى تعديل واحد يفيد الجميع: يقبل `finish_reason` الآن الصيغة `end_turn` التي تُصدرها xAI حيث تُصدر OpenAI الصيغة `stop`. النماذج: `grok-4.6` (الجودة) و`grok-4.3` (الاقتصادي). تجدر الإشارة إلى أن نموذج Grok الاقتصادي يظل الأغلى في المستودع — $1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ `mistral-small-latest`: يُختار هذا المزود لتنوع النماذج، لا للسعر.
    - **وضع CLI**: صُمم على غرار Codex، مع أربعة اختلافات فرضها الواقع — يُمرر الطلب عبر ملف (`--prompt-file`، إذ لا تقرأ واجهة CLI من stdin وسيكون أي مقطع في argv ظاهرًا في `ps`)، والمخرجات كائن JSON وحيد على stdout (لا JSONL ولا ملف `-o`)، ولا يتيح الاشتراك سوى `grok-4.6` و`grok-4.5`، كما يتعذر تطبيق sandbox (انظر أدناه). جرى توحيد تشغيل العمليات الفرعية مع Codex في `_codex_run_process`، من دون المساس ببقية مزود Codex الذي سبق اختباره.
    - **القيمة `exit 0` لا تثبت شيئًا، وفقًا للقياس**: عندما لا يكون المستخدم مصادقًا عليه، تكتب واجهة CLI القيمة `{"type":"error","message":"Not signed in."}` إلى **stdout** مع رمز خروج **0**. ويتصرف الرفض أو تجاوز عدد الجولات بالطريقة نفسها. لذلك يشترط عقد المخرجات تحقق أربعة شروط معًا: رمز خروج 0، وغياب حمولة خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع الفحص المسبق المنطق نفسه: تنتهي `grok models` بالرمز 0 حتى عند انقطاع الاتصال، ولا يمكن الاستنتاج إلا من وجود عبارة «غير مصادق عليه» في stdout.
    - **العزل: تفاوت مقصود وموثق.** بينما يعمل Codex في `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـ Grok على كثير من أجهزة Linux الحديثة، لسببين مستقلين على مستوى النظام لا يمكن تجاوزهما من دون `sudo`: يحظر AppArmor مساحات أسماء المستخدمين غير المميّزة منذ Ubuntu 24.04 (`bwrap: setting up uid map: Permission denied`، وأُعيد إنتاج المشكلة خارج Grok)، وتفشل قائمة حظر مقابس وقت تشغيل الحاويات عندما تكون `/run/podman` في `0700` (لا يعالج المحلل سوى `ErrorKind::NotFound`، بينما يصبح EACCES خطأً فادحًا). الفخ الأساسي: ملف تعريف **مدمج** يتعذر تطبيقه **يبدأ التشغيل بلا عزل بصمت**. لذلك لا يطلب البرنامج النصي أي ملف تعريف افتراضيًا ولا يتراجع بصمت مطلقًا — بل يصدر تحذيرًا على stderr. وتعتمد الحماية على قواعد `--deny` في واجهة CLI، بما فيها قاعدة الالتقاط الشامل `*`، وهي الطبقة الوحيدة التي ثبت بالقياس أنها _تغلق عند الفشل_ (إذ تؤدي قاعدة ذات بادئة مجهولة إلى رفض بدء التشغيل). تتيح `GROK_TRANSLATE_SANDBOX=read-only` اشتراط ذلك، وعندها يفشل بدء التشغيل إذا تعذر على الجهاز الالتزام به.
    - **ضوابط الحماية**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (إذ سيؤدي وجود مفتاح إلى التحول نحو الفوترة حسب الاستخدام؛ كما أن `GROK_SANDBOX` موروثة ستفرض ملف تعريف غير قابل للتطبيق مع رسالة مضللة)، وتُعطل مفاتيح MCP/hooks/skills/agents، مع `--disable-web-search` و`--no-subagents` و`--no-plan`، ودليل عمل مؤقت، ورفض التشغيل في CI، ومهلة زمنية تقتل مجموعة العمليات، وتراجع تدريجي عند بلوغ حد المعدل. ضُبطت `--max-turns` على 6 لا على 1: إذ يُزاد العداد بعد جولة الأدوات، وكانت القيمة 1 ستقتطع المخرجات.
    - **الحصة**: مجمّع Grok أسبوعي و**مشترك مع Chat وImagine وVoice**، ولا يعرضه أي أمر — بخلاف Codex الذي تتيح `account/rateLimits/read` قياس استهلاكه. لذلك تحدد `regen_translations.sh` التزامن بعمليتين وتصدر تحذيرًا صريحًا.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` (24 اختبارًا). المجموعة الكاملة تضم **290 اختبارًا**.
  - **إصلاح خطأ — لم تكن الاقتباسات الإنجليزية متعددة الفقرات محمية إلا جزئيًا (وضع `--news`)**: لم تكن `_NEWS_CITATION_REGEX` تقبل في متن الاقتباس سوى سلسلة من أسطر `>` **المتتالية**. وما إن يمتد اقتباس عبر عدة فقرات (تفصل بينها سطر `>` فارغ)، حتى لا يُلتقط ويُستبدل بالعنصر النائب سوى آخرها؛ أما الفقرات السابقة فكانت تُرسل إلى LLM وتعود مترجمة — وهو النقيض التام لما وُجدت `--news` لضمانه. بات التكرار يقبل الآن أسطر `>` الفارغة الداخلية وأصبح غير جشع، ليتوقف عند سطر `>` الفارغ السابق للسطر المائل، لا عند أول سطر فارغ يصادفه.
    - **النطاق المقاس** على مجموعة فعلية من 198 مقالة: شملت المشكلة 11 اقتباسًا من أصل 419. لا توجد أي انتكاسة — يلتقط التعبير النمطي الجديد العدد نفسه تمامًا من الاقتباسات، ولا يمتد سوى متن الاقتباسات متعددة الفقرات (408 متون متطابقة، و11 ممتدة)، وما زال يتعذر ابتلاع سطر الإسناد `> — …` ضمن المتن (مع الإبقاء على الاستباق).
    - **إثبات شامل** على مقالة بحجم 69 كيلوبايت تُرجمت إلى ja/ar: تظل الآن الفقرة الأولى من اقتباس، التي كانت تُعرض سابقًا بصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم كذلك إلى العربية، على حالها بصيغة `> GLM-5.3 is now open-weight.`. وعاد عدد أسطر الاقتباس الإنجليزية من 9 إلى 10، وهو عددها في المصدر.
    - تجدر الإشارة إلى أن أدوات التحقق اللاحقة لم تكتشف هذا العيب، لأنها تتحقق من وجود الاقتباس من دون التأكد من اكتماله.
  - **توفير مقاس لدى المزود الافتراضي**: كانت `_openai_extra_kwargs` ترسل `reasoning_effort="medium"` بمجرد أن يبدأ اسم النموذج بـ `gpt-5`، بما في ذلك عند استخدام `--eco`. أظهر القياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات ما يلي: `medium` ← 45 وحدة reasoning و65 وحدة مخرجات؛ و`none` ← 0 و14. لا يضيف الاستدلال شيئًا إلى الترجمة، مع أن تكلفته كانت تُدفع عن كل مقطع من كل ملف. أصبحت القيمة الافتراضية `none` عند استخدام `--eco`، وتظل `medium` في ما عدا ذلك؛ وتبقى للقيمة الصريحة الممررة عبر CLI الأولوية. تقبل `--reasoning_effort` الآن `none` و`xhigh` إلى جانب `low`/`medium`/`high` (ولا تقبل جميع النماذج كل هذه القيم: فمثلًا ترفض `gpt-5.4-mini` القيمة `minimal` — وتغطي إعادة المحاولة الحالية من دون المعامل هذه الحالة).
  - **تحديث حزم SDK وترحيل Gemini**: استُبدلت `google-generativeai` (انتهى دعمها في 2025-11-30 وأُرشف مستودعها) بحزمة SDK الموحدة **`google-genai`** — ‏`genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير طلب النظام في `system_instruction` بدلًا من ضمه إلى المقطع. رُفعت `mistralai` إلى **2.9.4** (وأصبح الاستيراد `from mistralai.client import Mistral`؛ أما القديم فيثير `ImportError`، وفقًا لما تم التحقق منه داخل wheel)، و`anthropic` إلى **0.125.0**، و`openai` إلى **2.54.0** — وهي أحدث الإصدارات قبل الانتقال إلى `httpx2`، تفاديًا لتعايش حزمتين من HTTP داخل venv. وبناءً على ذلك، فُتح تثبيت `httpx` 0.28.1 و`pydantic` 2.13.5.
  - **انتكاستان اكتشفتهما اختبارات فعلية، لا الوثائق**:
    - ترفض `anthropic` بالإصدار ≥ 1.0 من جهة العميل استدعاءً غير متدفق عندما توحي `max_tokens` بأنه سيستغرق أكثر من 10 دقائق (`ValueError: Streaming is required...`). لم يكن هذا الضابط موجودًا في 0.34.2، وكان يعطل كل استدعاء لـ Claude باستخدام `max_tokens=32768`. جرى إصلاحه عبر `timeout` صريحة (`CLAUDE_TIMEOUT`، و900 ثانية افتراضيًا)، ما يجنب التحول إلى التدفق في استدعاء لا تُستخدم منه سوى الاستجابة الكاملة.
    - لا تقبل `thinking_level="minimal"` سوى مجموعة فرعية من كتالوج Gemini: تدعمها `gemini-3.1-flash-lite`، بينما ترفضها `gemini-3.7-flash` و`gemini-3.1-pro-preview` بخطأ 400. ومن هنا جاءت `_gemini_generate_with_fallback`، وهي سلسلة تراجع `minimal` ← `low` ← من دون thinking_config، على غرار آلية التراجع القائمة في OpenAI — فلا ينبغي أن يؤدي معامل تحسين إلى إفشال ترجمة مطلقًا.
  - **تجديد النماذج الافتراضية**، بعد التحقق من كل منها باستدعاء فعلي: OpenAI ‏`gpt-5.5` ← **`gpt-5.6-terra`** (−60% في دفعة من 28) و`gpt-5.4-mini` ← **`gpt-5.6-luna`** (−73%)؛ وClaude ‏`claude-sonnet-4-6` ← **`claude-sonnet-5`** (أقل تكلفة وأحدث) و`claude-haiku-4-5-20251001` ← **`claude-haiku-4-5`** (المعرّف القياسي بلا تاريخ)؛ وGemini ‏`gemini-3.1-pro-preview` ← **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` ← **`gemini-3.1-flash-lite`** (إصدار مستقر وأقل تكلفة من `3.5-flash-lite`). بقي Mistral بلا تغيير، إذ تظل `mistral-large-latest` صاحبة أفضل نسبة جودة إلى سعر بين النماذج الأربعة. تجدر الإشارة إلى أنه لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — فنموذج Gemini 3.5 Pro، المُعلن عنه في مايو 2026، لم يصدر قط؛ وسلسلة 3.5/3.6/3.7 مخصصة حصريًا لـ Flash.
  - **اختبار A/B مقاس قبل تحويل Gemini**: ترجمت `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. كانت البنية متطابقة تمامًا (21 قائمة، و18 كتلة تعليمات برمجية، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، خلال **8 ثوانٍ مقابل 48 ثانية**. ونظرًا إلى غياب أي معيار أداء عام يقارن هذين النموذجين في الترجمة أو النصوص غير اللاتينية، لكان التحويل قائمًا على مجرد افتراض لولا ذلك.
  - **ترشيح كتل استجابة Claude**: كانت `_call_claude` تنفذ `block.text for block in response.content` من دون ترشيح النوع. تتخلل النماذج ذات الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking` تعرض `.thinking` بدلًا من `.text` — ما كان سيعطل الترجمة بسبب `AttributeError` مبهم في أول مقطع. تُستبعد الآن كتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` (بقائمة سلبية، حفاظًا على التسامح مع نوع مجهول يحمل نصًا)، وتؤدي الاستجابة الخالية من أي كتلة نصية إلى خطأ صريح. وتُمرر `thinking={"type": "disabled"}` مع كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: حُذفت النماذج التي انقضى تاريخ سحبها (سلسلة `magistral-*` التي سُحبت في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620`، و`claude-3-7-sonnet-20250219`، و`claude-opus-4-1-20250805`، و`claude-sonnet-4-20250514`). وصُححت الحدود: Mistral من 128K إلى **256K** (جيل Large 3 / Small 4)، وGemini من 1 000 000 إلى **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` من 200K إلى **1M**، وعائلة `gpt-5.6-*` من 400K إلى **1,05M**. وأُضيفت Claude 5 ‏(`claude-sonnet-5` و`claude-opus-5` و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وسلسلة `ministral-*`. تجدر الإشارة إلى أن هذه الحدود تظل إرشادية، إذ تحدد `translate()` سقف التقسيم عند `min(16000, limite)`.
  - **المزوّد `--use_codex`**: المزوّد الخامس الذي يشغّل CLI الرسمي لـ Codex ‏(`codex exec`) في الوضع غير التفاعلي، بدلًا من استدعاء API تُحتسب تكلفتها حسب الاستخدام. تُخصم الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقًا. وهذا هو المسار الوحيد الذي وثّقته OpenAI لهذا الاستخدام: إذ تُدرج مصفوفة التوفّر حسب الخطة «Codex SDK و`codex exec` ومسارات العمل القابلة للبرمجة» على أنها متاحة ضمن Plus/Pro/Business/Enterprise، بينما لا تصادق رموز `~/.codex/auth.json` على استدعاءات API Platform (ولا يقرأها هذا السكربت مطلقًا — إذ تظل المصادقة وتجديدها تحت إدارة CLI).
  - **إمكانية تثبيت ملف Codex التنفيذي عبر pip، وليس عبر npm فقط**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK ‏`openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عمومي لاستخدام `--use_codex`. لا تُضاف الحزمة إلى `requirements.txt`: إذ يبلغ حجم الملف التنفيذي نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين من أجل مزوّد اختياري. جرى التحقق من البداية إلى النهاية: عند غياب `codex` عن `PATH`، يعثر الحلّ على الملف التنفيذي المضمّن في الحزمة وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: تُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. فمن دون هذا الحاجز، قد يؤدي وجود مفتاح في `.env` إلى تحويل Codex إلى الفوترة حسب الاستخدام من دون أي إشارة مرئية — وهو تحديدًا ما وُجد هذا المزوّد لتجنّبه.
  - **مشكلات CLI المحصّنة بالاختبارات**:
    - يقرأ `codex exec` ‏stdin **حتى** عندما يُمرَّر الموجّه كوسيط: ومن دون إغلاق stdin، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج مطلقًا (أُعيد إنتاج الحالة: exit 124 بعد 180 ثانية، وصفر بايت). لذلك فإن `communicate(input=...)` إلزامي.
    - إن `codex` المثبّت عبر npm عبارة عن shim بلغة Node يقوم عبر `spawn` بتشغيل الملف التنفيذي الحقيقي المكتوب بلغة Rust: وهذا الأخير **عملية حفيدة** لعملية Python وينجو من `SIGKILL` الخاص بـ `subprocess.run(timeout=)`، حيث كان سيواصل استهلاك الحصة. ومن هنا استخدام `Popen(start_new_session=True)` مع `os.killpg`.
    - قد يخرج CLI بالرمز 0 رغم إصداره `turn.failed`: لذلك يُفحَص خرج JSONL ‏(`--json`) بالإضافة إلى رمز الإرجاع، ويؤدي غياب الملف `-o` مع الرمز 0 إلى رفع خطأ صريح بدلًا من إنتاج مقطع فارغ.
  - **التراجع التدريجي عند بلوغ حد المعدّل**: لا يطبّق CLI أي إعادة محاولة داخلية (`max_retries = 0`). يجري التصنيف استنادًا إلى بنية حمولة JSON ‏(`status: 429` / `error.type`)، لا إلى السلاسل الفرعية — فكلمة «الحصة» تظهر في كل من خطأ 429 القابل للتعافي و`insufficient_quota` النهائي.
  - **حاجز CI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفًا. فالمصادقة عبر الاشتراك غير مخصّصة لمشغّل مشترك، كما أن OpenAI لا توصي صراحةً بمسار العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` ‏(`--eco`). تشترك CLI وAPI Platform في عائلة `gpt-5.6-*`، لكن حساب ChatGPT لا يملك صلاحية الوصول إلى جميع نماذجها: تُطبّق قائمة السماح من جهة الخادم من دون تحقق محلي، ويُطلق النموذج غير المعتاد تحذيرًا. ضمن خطة Plus، يوفّر Luna ما بين 250 و2 000 رسالة لكل نافذة مدتها 5 ساعات، مقابل 10 إلى 100 رسالة لـ Sol: لذا فإن `--eco` هو الوضع الموصى به لأي معالجة دفعية.
  - **إصلاح خلل — كان `regen_translations.sh` يخرج بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغيّر `local` تابع لـ `main()` لا يعود موجودًا عند تنفيذ trap. تحت `set -u`، كان ذلك يثير `failed_log: unbound variable` ويجعل السكربت يخرج بالرمز 1 رغم صحة الترجمات الـ28 — ما كان سيوقف `release.sh --auto` ‏(`set -e`) مباشرةً بعد إعادة التوليد، عند أكثر المراحل كلفة. أصبح المتغيّر عموميًا وصار trap يتحقق من وجوده. ومن الآثار الجانبية المفيدة أن إخفاقات الترجمة الحقيقية، التي كان هذا الخطأ يحجبها سابقًا، أصبحت ظاهرة مجددًا في الملخص الختامي.
  - **`REGEN_MODEL`**: متغيّر بيئة جديد لـ `regen_translations.sh` يفرض نموذجًا محددًا بدلًا من النموذج الافتراضي للمزوّد، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج الأعلى ضمن حصة الاشتراك بدلًا من النموذج `--eco` الموجّه للحجم.
  - **`regen_translations.sh`**: أصبح `REGEN_PROVIDER=codex` متاحًا عبر اشتراك صريح (ولا يُكتشف تلقائيًا مطلقًا، لتجنب استهلاك حصة الاشتراك من دون علم المستخدم). يُجدَّد الرمز مرة واحدة تسلسليًا قبل بدء التنفيذ المتوازي — إذ إن تجديد Codex دوّار وأحادي الاستخدام، وستؤدي المهام المتزامنة إلى إبطال الجلسة `codex login` — ويُخفَّض التزامن إلى 4.
  - **إعادة الهيكلة المرتبطة**: ينتقل `_dispatch_provider_call` من 8 معاملات إلى 6 عبر `_resolve_provider()` يُعيد اسم المزوّد، بدلًا من قيمة منطقية رابعة يجري تمريرها عبر السلسلة بأكملها. وتظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` مع `Namespace` بالحد الأدنى.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` ‏(48 اختبارًا) يغطي argv، والبيئة المنقّحة، وعقد منع المقدمة، والإخفاقات الصامتة، والمهلة/killpg، والتراجع التدريجي، والفحص التمهيدي، وحلّ المزوّد، وتسلسل الاستدلال في Gemini، وترشيح كتل Claude، واستشهادات الأخبار متعددة الفقرات. تضم المجموعة الكاملة 290 اختبارًا.
  - **التحقق الفعلي**: عند ترجمة `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex، تكون البنية مطابقة تمامًا للترجمات المرجعية (14 كتلة تعليمات برمجية، و24 عنوانًا، و25 صفًا في الجداول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل تعليمات برمجية متطابقة حرفًا بحرف، ومن دون أي بقايا للعناصر النائبة). وفي مقال صحفي بحجم 69 كيلوبايت في وضع `--news`، يجتاز كل من الخرجين `gpt-5.6-luna` و`gpt-5.6-sol` مدقّق التطبيق اللاحق للغات en/ja/ar. أما الاستهلاك المقاس عبر `account/rateLimits/read` فظل دون حد تقريب العداد (0% من نافذة الساعات الخمس) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL الخاص بإسناد الأخبار عند وجود أقواس متداخلة أو بادئة فرنسية (2026-05-11):

  - **إصلاح خلل**: كان استخراج عناوين URL الخاصة بالإسناد في `_protect_news_quotes` يستخدم التعبير النمطي `re.search(r"\((.+?)\)", attribution)` (التقاطًا غير جشع بين قوسين). وفي إسناد من النوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` المحيطة مع `]()` الخاصة برابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة تتضمن البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائية). ونتيجةً لذلك، كان `_validate_news_post` يبحث عن هذه السلسلة في الخرج المترجم ويفشل دائمًا (لسببين: `)` مبتور، وعبارة "relayé par" مترجمة إلى `relayed by`/`weitergeleitet von`/...). ولم يكن بوسع التسلسل الكامل low ← medium ← high ← gpt-5.5 اجتياز ذلك.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — إذ يستهدف تحديدًا `](url)` الخاصة برابط Markdown، ويلتقط **عنوان URL الصافي فقط** (من دون البادئة الفرنسية أو البتر)، وهو ثابت تحافظ عليه العناصر النائبة `#URL{N}#` أثناء الترجمة. ويتعامل بمتانة مع النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس محيطة
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py` ضمن الفئة `TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة الدقيقة المعاد إنتاجها لخلل Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (صيغة بديلة تتضمن `via`)
  - **ثغرة في التغطية**: يتحقق `check-editorial-coverage.py` من صحة الصياغة التحريرية، لكنه لا يتحقق من قابليتها للترجمة بواسطة المترجم. ومن التحسينات الممكنة (خارج نطاق v1.9.2) إضافة فحص يحاكي استخراج الإسناد في وضع dry-run لاكتشاف الأنماط الخطرة **قبل** النشر.

- **1.9.1** إصلاح i18n لتسمية CTA في ملاحظة علامة الترجمة (2026-05-10):

  - **إصلاح خلل**: كانت تسمية `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط العلامة أعلى الملفات المترجمة تظل **بالفرنسية** لجميع اللغات المستهدفة، بدلًا من اتباع `target_lang`. ولا يراها LLM مطلقًا (إذ تُجمَّع من جهة Python للحفاظ على عنوان URL وslug الخاصين بالمستودع)، ولذلك لم تكن مرحلة الترجمة قادرة على تداركها. وهذا تراجع صامت منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ15 بتسمياتها الموطّنة. وأصبح `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` يمرّران اللغة المستهدفة. ويُستخدم `fr` كقيمة احتياطية إذا كانت اللغة مجهولة (للسلامة وتجنب KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link` ‏(target_lang ‏`ja` ← التسمية اليابانية المتوقعة). وأُضيف اختباران جديدان: `test_source_link_label_localized_per_target_lang` (مُعلَّم عبر 7 لغات تغطي الخطوط اللاتينية والتصويرية والأبجدية) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختبارًا في `test_translation_note_position.py` (بدلًا من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع بقيمة افتراضية `target_lang="fr"` — تواصل الجهات البرمجية الخارجية المستدعية التي لا تمرّر `args.target_lang` العمل من دون تعديل.
- **1.9** إصلاح الفشل الصامت + مجموعة أدوات جودة متكاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + تنسيق marker من نوع "embed card"**:
    - خيارات CLI جديدة (إضافية، الإعدادات الافتراضية لم تتغير ← **غير كاسرة للتوافق**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): يضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين معًا.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - يعيد `legacy` إنتاج سلوك v1.8 بدقة (فقرة عريضة `**…**`) **byte-for-byte**.
        - يُصدر `marker` تعريف link reference غير مرئي في Markdown ‏(`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`)، يتبعه **blockquote من 3 فقرات** منظّم ليُعرض بأسلوب "GitHub repo embed card": عنوان المشروع ضمن inline code ‏(`**\`ai-powered-markdown-translator\`\*\*`)، ووصف يترجمه LLM، ورابط CTA ‏(`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. ويمكن استثماره أثناء build بواسطة plugin من نوع remark (راجع مدونة jls42.org ← ‏plugin ‏`remark-translation-banner`).
    - **ثوابت لا تُرسل مطلقًا إلى LLM**: يُجمَّع عنوان repo ورابط GitHub في جانب Python بعد ترجمة العبارة الوصفية. لا يرى LLM مطلقًا slug ‏`ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تغيير أي renderer أو حالة الأحرف أو scheme.
    - **إدراج يراعي frontmatter**: في وضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة `---` الختامية** الخاصة بـ YAML frontmatter (لضمان سلامة Astro Content Collections / gray-matter). يكتشف helper ‏`_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف ويحافظ على سلامته؛ كما **يرفع `RuntimeError`** عند وجود frontmatter مفتوح بلا fence ختامي (فيُدرج الملف ضمن `failed_files` بدلًا من كتابته مع ملاحظة في موضع خاطئ).
    - **Sanitizer للنموذج قائم على whitelist**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع fallback إلى `unknown` إن كانت النتيجة فارغة. يتوافق ذلك مع validator في جانب plugin من نوع remark ضمن Astro، ويحيّد المحارف التي قد تكسر تنسيق marker (المسافة، وعلامة الاقتباس، والقوس، والفاصلة، وغيرها).
    - **Refactor داخلي**: ‏`_append_translation_note` (دالة monolithic واحدة) ← 7 helpers نقية (`_translation_note_invariants`، و`_build_translation_note_phrase`، و`_assemble_translation_note_paragraphs`، و`_build_translation_note_source`، و`_sanitize_model`، و`_quote_lines`، و`_split_frontmatter`، و`_build_translation_note_block`، و`_compose_with_notes`). فُصل builder عن composer (يعيد builder كتلة نقية بلا فاصل، بينما يطبّق composer عناصر `\n\n` وفقًا للموضع)؛ ويشترك كود production وhelper المصدر في المجمّع نفسه ذي الفقرات الثلاث.
    - **‏`_quote_lines` مع الحفاظ على الأسطر الفارغة**: يضيف `> ` في بداية كل سطر، ويحوّل الأسطر الفارغة إلى `>` وحده. يتيح ذلك لـ mdast رؤية 3 فقرات منفصلة داخل blockquote (العنوان / الوصف / الرابط) بدلًا من فقرة واحدة تتضمن فواصل أسطر.
    - **‏`_build_translation_note_block` تكيّفي**: وفقًا لعدد الفقرات التي حافظ عليها LLM ‏(3 = تنسيق card كامل، و2 = عبارة + رابط، و1 = fallback). لم يعد fallback ذي الفقرة الواحدة **يغلّف المحتوى داخل `**...**`** عند اكتشاف رابط Markdown ‏`](` (إذ يكون عرض `<strong>` حول رابط هشًّا).
    - **توافق تصاعدي**: ‏`getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` في جانب `_compose_with_notes` — تواصل كائنات Namespace التي لا تتضمن هذه السمات (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) العمل من دون تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع providers ‏(OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور حرفيًا على مقتطف المصدر) + طبقة احتمالية (`langdetect`)
    - Whitelist ‏`finish_reason` / ‏`stop_reason`: رفع `RuntimeError` لأي حالة خارج whitelist ‏(truncation وcontent_filter وغيرهما)
    - ‏`max_tokens` في Claude: ‏`4096` ← ‏`32768` (لتجنب truncation كامن في segments بحجم 16k، مع هامش cross-script من FR إلى JA/ZH/KO/AR/HI)
    - Segmentation يراعي headings: إعطاء الأولوية لـ H2/H3 في النصف الثاني من segment (يبدأ كل segment بقسم دلالي كامل)
    - تمرير الأخطاء حتى exit code غير صفري: يعيد `translate_markdown_file` حالة typed من نوع `success` / ‏`failure` / ‏`skipped`، ويجعل `main()` القيمة `sys.exit(1)` إذا فشل ملف واحد على الأقل (في وضعي single-file وbatch)
    - Empty-content guard لدى جميع providers، وsanity ratio بين المصدر والمخرجات (≥ 500 محرف، وأقل من 5% = رفض)، والتحقق من placeholders الخاصة بالكود (`#CODEBLOCK`/`#INLINECODE`)، وnormalization بعد LLM (الفواصل/الروابط الملتصقة بـ heading)، و`BadRequestError` retry من دون `reasoning_effort`
    - إضافة dependency ‏`langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("نوع EurekAI متكامل"، 14 hooks):
    - Pre-commit: ‏ruff ‏(lint+format)، وshellcheck، وprettier ‏(md/yaml/json)، وdetect-secrets ‏(حماية 4 مفاتيح API)، وLizard ‏(CCN ≤ 12)، وpre-commit-hooks v5 ‏(whitespace وEOF وlarge-files وshebangs وغيرها)
    - Pre-push: ‏mypy ‏(وضع lax تدريجي)، وOpengrep SAST ‏(translate.py + scripts/)، وpip-audit ‏(وضع reporting أولي)، وunittest discover ‏(tests/ + scripts/tests/)
    - Wrappers محلية داخل `scripts/` تستخدم `./venv/bin/python`
    - ‏`scripts/audit_verdict.py`: parser بصيغة JSON لـ pip-audit مع 11 اختبار unittest، وهو port بلغة Python مقتبس من parser الخاص بـ jls42-astro
    - إصلاح 7 مخالفات أولية في ruff: ‏B904 ‏(raise from) ×2، وB007 ‏(unused dirs)، وC408 ‏(dict literal)، وC419 ‏(list-comp)، وSIM105 ‏(contextlib.suppress)، وSIM110 ‏(any())
    - يستثني Lizard مؤقتًا `translate.py` (4 دوال بقيم CCN من 21 إلى 47، وrefactor مخطط له) — gate صارم على scripts/
  - **SonarCloud + تغطية شاملة**:
    - Workflow ضمن GitHub Actions ‏`SonarCloud` ‏(sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع coverage عبر `coverage.xml`
    - 11 badges من SonarCloud في أعلى README ‏(Quality Gate، وSecurity/Reliability/Maintainability ratings، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code)
    - ‏`tests/test_silent_failure.py` ‏(`unittest` من stdlib): يغطي الحلقات الست في سلسلة أخطاء الفشل الصامت
    - ‏`tests/test_orchestration.py` ‏(+79 اختبارًا): يغطي طبقة orchestration في `translate.py` ‏(`_resolve_*_filename`، و`_existing_translation_exists`، و`_record_translation_status`، و`_write_output_file`، و`translate_directory`، و`_validate_input_paths`، و`_init_*_client`، و`_select_provider_client`، و`_normalize_collapsed_markdown`، و`_cleanup_source_flag`، و`_validate_news_flags_*`، و`_openai_create_with_fallback` مع fallbacks لـ TypeError وBadRequestError، وتنسيق prompt لسلسلة o1، وفروع early-return في `_validate_translation_output`)
    - ‏`scripts/tests/test_audit_verdict.py`: تغطية `main()` ‏(stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **Coverage للكود الجديد**: 75.5% ← نحو 98% ‏(translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك E2E ‏`marker+top|bottom|both` و`legacy+top|bottom|both`)، وإضافة البادئة إلى أسطر متعددة، والتوافق الخلفي byte-for-byte ‏(golden literal)، وsanitizer، وتقسيم frontmatter (بما في ذلك رفع خطأ عند عدم إغلاق fence)، وتنسيق الفقرات الثلاث، وfallback ذي الفقرتين، وguard لفقرة واحدة + رابط Markdown، وحاجز حماية حاسم `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان + URL لا يُرسلان مطلقًا إلى LLM. **نجح 190 اختبارًا**، بلا أي regression.
  - التوثيق: ‏`README.md` ‏(بالفرنسية + 14 ترجمة) مع badges، و`CLAUDE.md` ‏(workflow خاص بـ pre-commit + مراقبة مفصلة لـ CI)، وإعادة توليد 28 ترجمة
- **1.8** وضع `--news` + تحديث نماذج 2026 ‏(2026-03-17، tag ‏`v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - جودة OpenAI: ‏`gpt-5` ← ‏`gpt-5.4`
    - OpenAI الاقتصادي: ‏`gpt-5-mini` ← ‏`gpt-5.4-mini`
    - جودة Gemini: ‏`gemini-3-pro-preview` ← ‏`gemini-3.1-pro-preview`
  - إضافة حدود tokens لـ `gpt-5.4`، و`gpt-5.4-mini`، و`gpt-5.4-nano` ‏(400k)، و`gemini-3.1-pro-preview` ‏(1M)
  - وضع `--news` الأولي: حماية الاقتباسات الإنجليزية باستخدام placeholders ‏`#NEWSQUOTE\d+#`، وmapping ‏`LANG_FLAGS` ‏(15 لغة)، وإدارة الأعلام وفقًا للغة الهدف
  - التحقق من placeholders الخاصة بالأخبار قبل الاستعادة (regression: كان حذف LLM لـ placeholder ينتج بصمت مخرجات بلا اقتباس)
  - جعل script ‏`regen_translations.sh` قابلًا للنقل (مسارات مطلقة، بلا اعتماد على pwd)
  - إضافة رابط اللغة الفرنسية إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة
- **1.7** الميزات الجديدة:
  - خيار `--keep_filename` للاحتفاظ باسم الملف الأصلي أثناء الترجمة
  - دعم ملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على inline code**: أصبحت علامات backticks ‏(`` `...` ``) محمية أثناء الترجمة
  - تحسين system prompt:
    - تحسين التعامل مع علامات الاقتباس في YAML frontmatter
    - حماية متغيرات template ‏`{variable}`
    - منع ملاحظات المترجم غير المطلوبة
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** الميزات الجديدة:
  - دعم Google Gemini API للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI: ‏`gpt-5` ‏(جودة)، و`gpt-5-mini` ‏(اقتصادي)
    - Claude: ‏`claude-sonnet-4-5` ‏(جودة)، و`claude-haiku-4-5` ‏(اقتصادي)
    - Gemini: ‏`gemini-3-pro-preview` ‏(جودة)، و`gemini-3-flash-preview` ‏(اقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف واحد (`--file`) من دون استعراض directory
  - pattern تسمية مبسّط جديد: ‏`{base}-{lang}.md`
  - خيار `--include_model` للاحتفاظ بالتنسيق القديم المتضمن اسم النموذج
  - دعم النماذج غير المدرجة مع حد افتراضي لـ tokens ‏(128k)
  - ترجمة README إلى 14 لغة
- **1.5** التحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين prompts الترجمة:**
    - أُثريت prompts الخاصة بالترجمات المباشرة وملاحظات الترجمة لتحسين الوضوح والكفاءة، بما يشمل تعليمات مفصلة بشأن الحفاظ على metadata وعناصر التنسيق المحددة.
  - **Refactor للكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة client الخاص بـ Mistral AI.
    - إعادة تنظيم imports لتحسين سهولة القراءة والصيانة.
    - تحسين segmentation للنصوص وإدارة كتل الكود للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات المخرجات:**
    - عكس موضعي النموذج واللغة في اسم ملفات المخرجات (مثل `f"{base}-{args.target_lang}-{args.model}.md"`)، ما يسهّل تنظيم الترجمات والبحث عنها.
  - **تحسينات متنوعة:**
    - تنظيف الكود بحذف الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية script وسهولة قراءته.
- **1.4** الميزات الجديدة:
  - دعم Claude API من Anthropic للترجمة
  - تحسين prompts لزيادة الوضوح والكفاءة
  - تعديلات طفيفة لتحسين قابلية صيانة الكود
- **1.3** التحسينات والميزات الجديدة:
  - تحسين إدارة كتل الكود
  - تحسين إدارة ملفات المخرجات
  - تحسين اكتشاف الملفات الموجودة
  - خيار `--force` لفرض الترجمة
  - عكس موضعي النموذج واللغة في اسم ملف المخرجات
- **1.2** إصلاح changelog
- **1.1** إضافة دعم Mistral AI API
- **1.0** الإصدار الأولي - دعم OpenAI API

**مقال مترجم من الفرنسية إلى العربية باستخدام gpt-5.6-sol.**
