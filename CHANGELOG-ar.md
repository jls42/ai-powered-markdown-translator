### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.13.0** المزوّد `--use_openrouter`: موجّه مدفوع إلى نحو 430 نموذجًا، منها النماذج الصينية المفتوحة (2026-09-05):

  - **المسار التاسع للمزوّد، وقد صدر مع الثامن.** لم يُنشر الإصدار 1.12.0 على PyPI: إذ صدر الموجّهان OpenCode وOpenRouter معًا. يتيح [OpenRouter](https://openrouter.ai)، باستخدام مفتاح واحد، الوصول إلى نماذج لا يعرضها هنا أي مزوّد آخر — Kimi وQwen وDeepSeek وZ.ai — عبر رصيد موحّد تُحتسب تكلفته بحسب الاستخدام. ولأن نقطة النهاية متوافقة مع OpenAI، فالعميل هو نفسه المستخدم مع xAI؛ **وكل ما يميّز هذا المزوّد ينحصر في فحص تمهيدي**، وكل قاعدة من قواعده مستمدة من قياس أُجري على API.

  - **يُقدَّم النموذج نفسه عبر عشرات المستضيفين ذوي الحدود القصوى المختلفة، بينما لا يأخذ التوجيه ذلك في الحسبان.** أظهرت القياسات وجود 33 مستضيفًا لـ `z-ai/glm-5.2` و23 لـ `z-ai/glm-5.3-flash` — أحدها محدود عند **2,048 token للإخراج**. ولذلك كانت ترجمة طويلة من أصل 23 تُقتطع عشوائيًا من دون أدنى إشارة. يقرأ الفحص التمهيدي `/api/v1/models/{modèle}/endpoints`، ويستبعد المستضيفين الذين يقل حد إخراجهم عن 8,000 token، ومن لديهم حالة متدهورة، ومن لا يصرّحون بأي حد أقصى، ثم يثبّت الباقين. إن `provider.only` **من دون** `allow_fallbacks: false` ليس سوى تفضيل: إذ يعود الموجّه إلى مستضيف مستبعد، فيفقد التثبيت كل قيمته. وإذا لم يستوفِ أي مستضيف الحد الأقصى، يتوقف الأمر: فمتابعة الترجمة تعني قبول الاقتطاع الصامت الذي وُجد هذا الفحص التمهيدي لمنعه.

  - **تُحتسب تكلفة الاستدلال بتعرفة الإخراج، وهو مفعّل افتراضيًا في نماذج كثيرة.** الطلب نفسه على `z-ai/glm-5.2`، والإجابة «حسنًا»: **107 tokens للإكمال مع إعداد النموذج الافتراضي، و2 عند تعطيل الاستدلال**. وفي الترجمة، حيث لا يضيف الاستدلال شيئًا، يمثّل ذلك معاملًا قدره 18 لكل مقطع في كل ملف. لذلك يُعطّل افتراضيًا. أما **النماذج الـ288 من أصل 431** التي تفرضه (`reasoning.mandatory`)، فتجيب بـ `400 « Reasoning is mandatory for this endpoint and cannot be disabled »`: لا يُرسل إليها شيء بدلًا من تخمين مستوى الجهد — إذ يخصص الجهد **نسبة مئوية من `max_tokens`** يستهلكها الاستدلال أولًا، ولذلك لا تؤدي قيمة مختارة عشوائيًا إلا إلى نقل خطر الصفحة الفارغة بدلًا من تقليله.

  - **يتلقى النموذج الذي يفرض الاستدلال أدنى مستوى جهد يقبله، وقد حُسم ذلك بالقياس.** كان الخيار الأول ألّا يُرسل شيء، تجنبًا للتخمين بدلًا من النموذج. لكن عند اختباره على `z-ai/glm-5.3-flash`، الذي يكون إعداد الكتالوج الافتراضي له هو `max`، أنتج هذا الخيار إخراجًا **مقتطعًا عند 32,768 token** قبل نهاية الترجمة — فضاعت لغتان من أصل أربع عشرة. ولم تكن زيادة السعة لتغيّر شيئًا: فالجهد يخصص نسبة مئوية منها، ويزداد الاستدلال معها. لذلك يقرأ المزوّد `supported_efforts` أثناء الفحص التمهيدي ويطلب أدنى مستوى، مع الرجوع إلى «لا شيء» عندما لا يعلن الكتالوج عن أي مستوى قابل للاستخدام. وأكد الاختبار المضاد على اللغة التي أخفقت النتيجة: كانت تفشل بسبب نفاد الميزانية، وأصبحت الآن تنجح خلال 9 دقائق ببنية مطابقة للمصدر.

  - **يحمل عطل المستضيف الأعلى الآن اسمه.** يطبّع الموجّه هذه الحالة إلى `finish_reason=error` مع `native_finish_reason` فارغ — وقد قيس ذلك مرتين، على لغتين، عند 750 ثانية بالضبط. كانت الرسالة العامة تدفع إلى البحث عن عيب في المستند أو في التقسيم؛ أما الآن فتوضح أن العطل من جهة المزوّد وأن إعادة المحاولة تكفي غالبًا.

  - **إن `finish_reason=length` المصحوب بإخراج فارغ ليس اقتطاعًا.** بل يعني أن الاستدلال استهلك الميزانية قبل أول حرف مفيد — وقد قيس عند 15,850 token للاستدلال مقابل 148 مفيدة. وتتطلب الحالتان إجراءين متعاكسين: فتقليل حجم المقاطع لا يفيد في الحالة الأولى. وتفرّق الرسالة بينهما صراحةً. كما أُضيف حاجزان آخران بناءً على القياسات: يجيب الموجّه بالحالة **200 وجسم لا يحمل سوى خطأ** عندما يفشل المستضيف الأعلى (كان `choices[0]` يطلق `TypeError` مبهمًا يحجب الرسالة)، وتُقرأ نافذة السياق من الكتالوج ثم تُسجّل في `MODEL_TOKEN_LIMITS` — إذ إن `DEFAULT_TOKEN_LIMIT` غير صحيح لـ44 نموذجًا في الكتالوج، منها نموذجان محدودان عند 4,095 token.

  - **`--model fournisseur/modèle` إلزامي، ويُتحقق من صيغته قبل أي اتصال بالشبكة.** ليس OpenRouter مزوّدًا: فالاختيار يحدد السعر والترخيص ومعالجة البيانات، ولا يجوز اتخاذه نيابةً عن المستخدم. وبما أن slug يُدرج في URL الخاص بالفحص التمهيدي، فعملية التحقق ليست مجرد تحسين لتجربة الاستخدام، بل هي الحاجز الذي يمنع حقن مسار فيه: يقبل تعبير regex المشترك ذو namespace بين الموجّهين `a/b/..`، ومن هنا جاء الرفض الصريح لمقطع المجلد الأب. ليس لـ `--eco` أي تأثير، وهو يوضح ذلك.

  - **صُححت ثلاث صياغات، إحداها كانت خاطئة.** كان تحذير OpenAI بشأن `codex exec` يستهدف حقن ملف جلسة شخصي في runner مشترك، لا كون المستودع عامًا: وقد اقتُبس بمعنى معاكس في README وCLAUDE.md وفي الشيفرة. وتغيّر موضع مصادقة OpenCode في الإصدار 1.18.27 (الجدول `credential` في `opencode.db`، بدلًا من `auth.json`)؛ وظل الثابت «لا تُقرأ هنا أبدًا» صحيحًا، لكن العنوان أصبح قديمًا. وأخيرًا، لم يعد قسم OpenCode يعرض مسارات لم تُختبر قط على أنها متكافئة: فقد اختُبرت بوابة Zen وOllama من البداية إلى النهاية، بينما لم تُختبر GitHub Copilot وLM Studio وllama.cpp، وأصبح README يذكر ذلك الآن.

  - **حملة قياس وجدول بالنماذج الموصى بها في README.** نُفذت أكثر من ثلاثمائة ترجمة على ثلاث مجموعات من المستندات — مقالة مدونة كثيفة في وضع `--news`، وREADME هذا بصيغة Markdown القياسية، وأربعة ملفات README لمشروعات معروفة أُخذت كما هي من GitHub — إلى أربع عشرة لغة. يميّز الجدول بين أمرين كانا يُخلطان: الترجمات التي **تكتمل** وتلك التي تكون **بنيتها مطابقة** للمصدر. لم تفقد ثلاثة نماذج أي معلومة قط في المستندين الكثيفين: `gemini-3.7-flash` و`gpt-5.6-sol` عبر اشتراك ChatGPT، و`z-ai/glm-5.2` عبر OpenRouter — ولم تتجاوز اختلافاتها زوجًا من `**` لم يُنقل في لغة أو لغتين. والخلاصة الأساسية هي أن **العامل الفارق هو كثافة المستند، لا وضع `--news`**: إذ يفشل Grok عبر الاشتراك ثلاث عشرة مرة من أصل أربع عشرة في مقالة المدونة، وينجح في أربعة عشر README عامًا من أصل ستة عشر، والسبب هو انقطاعه عند المقطع الطويل، وقد تحقق منه باختبار مضاد. ويحمل الجدول تحذيره الخاص: فهو غير شامل، ومؤرخ، والمدد لا ترتب شيئًا، ويظل النهج الصحيح هو القياس على مستندات المستخدم نفسه.

  - **صُحح مقارن البنية قبل نشر أرقامه، لأنه كان ينتج نتيجتين إيجابيتين زائفتين في الكتابات غير اللاتينية.** لم يكن URL يتبعه قوس كامل العرض `）` يُقطع بواسطة regex يتوقف عند `)`، ولذلك كانت السلسلة المستخرجة تختلف رغم تطابق URL؛ كما كان اقتباس من خمسة أسطر بالفرنسية يقع في ثلاثة أسطر بالصينية يتسبب في انخفاض عدّ الأسطر. وقد تحقق من التصحيحين باختبار مضاد: لا يزال حذف URL أو قسم أو شيفرة مضمنة يُكتشف. ومن دونهما، كانت نتائج Gemini وCodex ستُنشر لإحدى عشرة لغة من أصل أربع عشرة بدلًا من ثلاث عشرة واثنتي عشرة.

  - **الاختبارات**: ملف جديد `tests/test_openrouter_provider.py` (39 اختبارًا) — التحقق من النموذج ورفض مقطع المجلد الأب، وتثبيت المستضيفين (الحد الأقصى، والحالة، والحد الأقصى غير المعلن، والحد الأدنى المشترك)، وكون `allow_fallbacks` زائفًا دائمًا، وتعطيل الاستدلال أو تركه وفق `mandatory`، وعقد الإخراج الكامل (خطأ داخل استجابة 200، وعدم وجود اختيار، وتمييز الصفحة الفارغة عن الاقتطاع، و`finish_reason` غير الطبيعي، والمحتوى الفارغ)، وفشل الفحص التمهيدي بصورة مغلقة عند تعذر الوصول إلى الكتالوج أو غياب slug أو عدم وجود مستضيف سليم، وحصرية flag وتسمية اسم الملف. وأصبح مجموع الحزمة الكاملة **427 اختبارًا**.

- **1.12.0** المزوّد `--use_opencode`: ‏OpenCode، الوكيل مفتوح المصدر، إلى المزوّد الذي يختاره — نموذج محلي، أو مجاني بلا حساب، أو اشتراك، أو مفتاح (2026-09-04):

  - **المسار الثامن للمزوّد، وهو مختلف في طبيعته عن المسارات السبعة الأولى.** ليس [OpenCode](https://opencode.ai) ‏(MIT) مزوّد نماذج، بل _موجّه_ إلى النماذج التي أعدّها المستخدم داخل OpenCode نفسه: مفتاح API، أو اشتراك (GitHub Copilot وChatGPT وSuperGrok)، أو بوابة OpenCode Zen — التي تقدم نماذج مجانية **بلا حساب** — أو نموذج **محلي** (Ollama وLM Studio وllama.cpp). يتحكم السكربت في `opencode run` بالوضع غير التفاعلي، كما يتحكم في Codex وGrok، ويعيد استخدام الأساس نفسه للعمليات الفرعية (مجموعة عمليات مستقلة، ثم `SIGTERM` فـ `SIGKILL` عند انتهاء المهلة، وstdin مغلق دائمًا، والبيئة منقحة). وقد تحقق من ذلك عبر **ترجمتين فعليتين**: ترجمة README هذا كاملًا إلى الإنجليزية عبر `opencode/mimo-v2.5-free` — في 49 ثانية، بتمرير واحد، وبنية مطابقة للملف المصدر (32 عنوانًا، و26 إغلاقًا للشيفرة، و18 رابطًا، و37 URL، و37 سطر جدول، و135 شيفرة مضمنة) — وملف اختبار عبر `ollama/qwen2.5:7b` محليًا ومن دون أي مفتاح.

  - **`--model provider/modèle` إلزامي، وهذا اختيار مقصود.** من دون `--model`، يعود OpenCode إلى إعداده الافتراضي، وهو في تثبيت جديد `opencode/big-pickle`، نموذج «خفي» مجاني قد تُستخدم المحادثات معه في التدريب — وقد أثبت القياس أن هذا هو النموذج الذي أجاب. وسيكون اختيار ذلك بصمت نيابةً عن المستخدم من جنس التحولات الخفية التي يتتبعها هذا المستودع؛ لذلك تذكر رسالة الخطأ الأمر الذي يسرد النماذج (`opencode models`) وثلاثة أمثلة (محلي، ومجاني، واشتراك). ليس لـ `--eco` أي تأثير، وهو يوضح ذلك. ويُمرر `--reasoning_effort` كما هو بوصفه `--variant` الخاص بـ OpenCode، وفقط عند طلبه صراحةً.

  - **عزل مقاس، لا مفترض.** يعرّف إعداد مضمن (`OPENCODE_CONFIG_CONTENT`، وهو الأخير في ترتيب دمج OpenCode، ولذلك تكون له الأولوية على إعداد المستخدم من دون استبداله) وكيلًا باسم `aipmt` تُرفض له جميع الأدوات (`permission: {"*": "deny"}`): فلا يعود السجل يعرضها أصلًا على النموذج، وعندما يُطلب منه «سرد الملفات وتشغيل `id`» يجيب بأنه لا يملك أدوات. عُطّل تشارك الجلسات، واستُبعدت plugins الخارجية (`--pure`)، ولا يُستخدم `--auto` أبدًا، ودليل العمل مؤقت وفارغ. وقد قيس حقنان صامتان وعُطّلا: فمن دون `OPENCODE_DISABLE_CLAUDE_CODE`، يدخل `~/.claude/CLAUDE.md` الخاص بالمستخدم في **كل** prompt ‏(515 token للإدخال بدلًا من 186 لعبارة «مرحبًا» بسيطة)؛ ومن دون `OPENCODE_DISABLE_PROJECT_CONFIG`، يدخل أيضًا `AGENTS.md` الخاص بالدليل الحالي — إذ نُفذت في الترجمة تعليمة تقول «اختم كل إجابة بكلمة BANANA». أما `~/.config/opencode/AGENTS.md` العام فيظل محقونًا: فلا يوجد مفتاح لتعطيله، كما أن التحايل عليه عبر `XDG_CONFIG_HOME` مُساء الاستخدام سيحجب أيضًا مزوّدي المستخدم. لذلك وُثّق الأمر بدلًا من ترقيعه.

  - **لا يثبت `exit 0` شيئًا؛ إنها CLI ثالثة، وبالاحتياطات نفسها — مع فخين خاصين بها.** لا يؤدي `--agent` غير المعروف إلى فشل `opencode run`: بل يظهر تحذير على stderr ثم رجوع **صامت** إلى وكيل البرمجة مع أدوات مفعّلة. ولذلك، إن لم يُؤخذ الإعداد المضمن في الحسبان، فستبدأ الترجمة بواسطة وكيل قادر على الكتابة؛ ويتحقق عقد الإخراج من غياب هذه الرسالة، إضافةً إلى: رمز إرجاع 0، وعدم وجود أي حدث `error`، وعدم وجود `tool_use`، وأن يكون آخر `step_finish` بالحالة `stop` (إذ إن `length` استجابة مقتطعة)، وأن يكون النص غير فارغ. أما الفخ الثاني، فهو أن حدث خطأ JSON **مبهم** — «خطأ غير متوقع في الخادم. راجع سجلات الخادم لمزيد من التفاصيل.» مع مرجع بسيط — وأن السبب الحقيقي (`ProviderModelNotFoundError: Model not found: foo/bar. Did you mean…` و`ProviderAuthError`…) لا يوجد إلا في السجلات. ومن هنا جاء `--print-logs --log-level ERROR` وقراءة الحقل `error="…"` من stderr، من دون أثر Bun الذي يليه. وهكذا يفشل النموذج غير المعروف خلال ثانية، مع تسمية سببه. كما يتجنب `--title` استدعاء LLM طفيليًا: فمن دونه، ينشئ OpenCode عنوانًا للجلسة عبر دورة إضافية على `small_model`.

  - **الأسرار: الترشيح بالنمط نفسه المستخدم مع Codex وGrok، باستثناء واحد محدد بالاسم.** يُحتفظ بـ `OPENCODE_API_KEY`: فهو مفتاح OpenCode نفسه (بوابة Zen واشتراك Go)، وموجّه إليه باسمه — أي ما يعادل `auth.json` الخاص به، وليس مفتاحًا يديره aipmt أو يمكنه احتساب تكلفته. ويُعدّ المزوّدون داخل OpenCode ‏(`opencode auth login` و`opencode.json`)، وليس في `.env` الخاص بـ aipmt أبدًا، فلا يصل أي مفتاح منه إلى العملية الفرعية. ولا يوجد رفض في CI، بخلاف CLI الخاصة بالاشتراكات: إذ إن استخدام مفتاح API أو نموذج مستضاف ذاتيًا على runner استخدامان مشروعان.

  - **أصبح حاجز منع اجتياز المسار يتحقق من القيمة المُدرجة، لا القيمة الخام.** يحتوي `provider/modèle` على `/` كان حاجز الإصدار 1.10.0 يرفضه — وكان محقًا، لأن `--model` كان يُدرج في اسم الملف `--include_model`. تستبدل تسمية اسم الملف الآن `/` و`\` و`:` بـ `-` قبل أي إدراج (`ollama/qwen2.5:7b` ← `ollama-qwen2.5-7b`، إذ إن `:` غير قانوني في Windows)، ويتحقق الحاجز الأعلى من هذه التسمية: فيصبح `../../evil` الاسم البسيط `doc-en-..-..-evil.md` تحت الوجهة، ولا يُرفض إلا `..` وحده، وكذلك `--target_lang ../x`. ويظل حاجز النطاق `_ensure_within_directory` طبقة ثانية بلا تغيير.

  - **النماذج المجانية والمحلية، وما تم قياسه.** يترجم `opencode/mimo-v2.5-free` فقرة في 16 ثانية وREADME هذا في 49 ثانية؛ ويستغرق `opencode/big-pickle` ‏40 ثانية لترجمة 200 كلمة، بينما بقي طلبان متزامنان عليه بلا إجابة مدة 5 دقائق مع أن كلًا منهما يكتمل منفردًا؛ ولم يجب `opencode/nemotron-3.5-lightning-free` بشيء خلال 3 دقائق. ومن هنا جاء `REGEN_PROVIDER=opencode` مع إلزامية `REGEN_MODEL` وتشغيل **مهمتين** بالتوازي. أما محليًا، فعادةً ما يضبط Ollama السياق على 4,096 token بينما تصل المقاطع إلى 16,000 حرف: ولذلك يلزم `Modelfile` مع `PARAMETER num_ctx 32768`، وتتبع الجودة النموذج — فقد عكس نموذج 7B قائمة وأفسد إغلاق كتلة شيفرة في ملف الاختبار، بينما حافظ نموذج البوابة على كل شيء.

  - **لم تعد ترجمات هذا المستودع تمر أبدًا عبر API مدفوعة.** كان `regen_translations.sh` يستخدم OpenAI API بمجرد وجود مفتاح عابر في `.env`، ولا يجعل Codex إلا خيارًا صريحًا. وهذا بالضبط ما حدث أثناء إعداد هذا الإصدار: فقد انطلقت الترجمات الـ28 عبر OpenAI API، ثم مرّ CHANGELOG الهندي عبر Gemini API، رغم أن اشتراك ChatGPT موجود تحديدًا لتجنب الدفع بحسب الاستخدام. أُزيل الاكتشاف التلقائي للمفاتيح: **أصبح Codex هو الافتراضي، مع `gpt-5.6-sol`**، وهو نموذج الجودة؛ وتتطلب `openai` و`gemini` و`grok` استخدام `REGEN_ALLOW_PAID_API=1` إضافةً إلى `REGEN_PROVIDER`، وهو استثناء مسمّى كي تُطبق القاعدة عند لحظة القرار؛ ويفشل `REGEN_PROVIDER` غير المعروف بدلًا من الرجوع إلى API. وتثبّت عشرة اختبارات السلوك الافتراضي والرفض والاستثناء. وأُعيد تنفيذ الترجمات الـ28 لهذا الإصدار عبر Codex.

  - **وُحّدت آلية التراجع عند بلوغ حد المعدل** (`_retry_on_rate_limit`): فقد كانت حلقات Codex وGrok متطابقة باستثناء التسمية، وكانت إضافة نسخة ثالثة ستتجاوز حد التكرار. وتشتق أخطاء CLI الثلاثة من `_CliCallError` واحدة؛ ويمنع اختبار خروج أي منها عن هذه الفئة، وإلا فلن تلتقطها الحلقة المشتركة.
  - **الاختبارات**: ملف جديد `tests/test_opencode_provider.py` ‏(51 اختبارًا) — عقد إخراج كامل، وآلية احتياطية للوكيل، وسبب مقروء من السجلات، وإزالة تكرار الأجزاء النصية وتجاهل الأجزاء الاصطناعية، ومهلة زمنية تقتل مجموعة العمليات، وback-off عند 429، ونموذج إلزامي خاضع للتحقق، وpreflight بلا أسرار، وحلّ مسار الملف التنفيذي، وتوصيل dispatch، وتسمية اسم الملف واختبار مضاد لاجتياز المسار. يوسّع `tests/test_review_hardening.py` حصرية flags وغياب الأسرار لتشمل provider الجديد. تتطلب البوابة الآن **22 flags** موثّقة في argparse. المجموعة الكاملة تضم **382 اختبارًا**.

- **1.11.1** تصحيح للتوثيق: يعلن README أخيرًا مسارات provider السبعة (2026-09-03):

  - **كانت صفحة PyPI للإصدار 1.11.0 تقول «4 APIs + ‏CLI الخاص بـCodex».** يوفّر الكود سبعة — OpenAI وMistral وClaude وGemini وGrok عبر API؛ وCodex ‏(ChatGPT) وGrok عبر الاشتراك، من دون فوترة حسب الاستخدام. كان وضعا Grok غائبين من العبارة التقديمية ونقطة _Multi-Provider_، وكررت الترجمات الأربع عشرة الخطأ. ولأن الوصف الطويل للحزمة ثابت لكل إصدار، تطلّب تصحيح الواجهة رقم إصدار جديدًا: وهذا هو سبب وجود هذا الإصدار الوحيد. **لا تغييرات في الكود.**
  - تمت محاذاة `CLAUDE.md` مع ما قدّمه النشر: عدّادات البوابة (16، و17 في `--full`)، وworkflows الأحد عشر النشطة، وعدّادا Sonar/Codacy غير الظاهرين في `gh pr checks` ‏(hotspots وCodacy API)، ونقل `# nosemgrep` واحد بواسطة `ruff-format`، وبيئات GitHub التي يتطلبها تبادل OIDC، وحقيقة أن _pending publisher_ لا يحجز الاسم.

- **1.11.0** النشر على PyPI: ‏`pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-09-03):

  - **أصبح السكربت أحادي الملف حزمة قابلة للتثبيت.** ينتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول console هي `aipmt` والمكافئ `python -m aipmt`. يظل المستودع المستنسخ ضروريًا للمساهمة — إذ توجد فيه الاختبارات والترجمات الثماني والعشرون وأدوات الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` مطلقًا**، لأن التعارض حقيقي وصامت. تثبّت حزمة PyPI ‏`translate` ‏(v3.8.1، آخر رفع في 2026-07-06) مجلدًا يحمل الاسم نفسه. أُعيد إنتاج ذلك في venv: يتغلب المجلد على الوحدة، ويختفي `translate.main`، وتتعطل نقطة الدخول عند `AttributeError` — بينما يجيب `pip check` بعبارة «لم يُعثر على متطلبات معطلة» مع rc=0. كان مجرد وجود `pip install translate` لدى المستخدم كافيًا لتعطيل CLI من دون تشخيص قابل للاستفادة. الاختبار المضاد على wheel الفعلي: تثبيت `pip install translate` فوق الحزمة، و`aipmt --help` بقيمة rc=0 قبل ذلك وبعده، وتتعايش أداتا CLI.
    - **اسم توزيع طويل وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للعثور عليها عبر بحث PyPI؛ إذ سيكون الاختصار وحده غير قابل للعثور عليه لمن لا يعرف المشروع مسبقًا، بينما يستهدف النشر تحديدًا إتاحة اكتشافه. استُبعد مرشحان معقولان بعد التحقق: `ai-markdown-translator` محجوز على npm منذ 2024 لأداة ذات الغرض نفسه، تسبق هذا المستودع بـ17 شهرًا؛ و`aimt` يختلف بحرف واحد عن `aim` ‏(v3.29.1)، وهي حزمة نشطة في المجال نفسه — أسوأ وضع يسمح بالالتباس طويل الأمد. وهناك فخ منهجي أيضًا: يعيد `pypi.org/project/<nom>/` حالة 200 لأي اسم كان (صفحة مضادة للروبوتات)، ولا يُعتد إلا بـJSON API.
    - **استخدام layout ‏`src/` بدلًا من حزمة مسطحة.** كانت الحزمة المسطحة ستحافظ على عناصر `sys.path.insert(..., "..")` الستة في الاختبارات، وهذا تحديدًا هو العيب: فهي تجعل الاختبارات تستورد شجرة المصدر بدلًا من الحزمة، مما يخفي أي خطأ في التغليف. التكلفة الفعلية: قاعدة استبدال إضافية واحدة.

  - **يمكن أخيرًا ضبط المفاتيح مرة واحدة نهائيًا.** لم يكن لدى CLI المثبّت أي إعداد دائم: لم يتبقَّ سوى متغير البيئة و`.env` في المجلد الحالي. صحيح أن `find_dotenv` يصعد حتى جذر النظام، ولذلك كان يعثر على `~/.env` **عند العمل داخل المجلد الشخصي**، لكنه لا يعثر على شيء عند العمل من مكان آخر — أي تغطية تعتمد على موضع تشغيل الأمر لا على قرار تصميمي. لذلك أُضيفت طبقة ثالثة: `~/.config/aipmt/.env`، أسفل الطبقتين الموجودتين.

    - **الأولوية غير مشفّرة صراحةً**، بل تنتج من `override=False`، وهي القيمة الافتراضية لـ`load_dotenv`: لا تملأ كل طبقة إلا ما تركته السابقة فارغًا. ومن هنا يأتي الترتيب: متغير البيئة ← ‏`.env` الخاص بالمشروع ← إعداد المستخدم، وقد جرى التحقق منه باختبارات سلوكية لا بنيوية — فعكس ترتيب الاستدعاءين يجعلها تفشل، وكذلك حذف الطبقة الثالثة.
    - **صيغة `.env` لا TOML**، عن قصد: ‏`python-dotenv` تبعية موجودة أصلًا، والصياغة موثّقة أصلًا في ملفات README الخمسة عشر، والملف نفسه يخدم النطاقين. لا تبعية ولا صياغة جديدة. يتبع الموضع `XDG_CONFIG_HOME` إذا كان **مطلقًا** — إذ تطلب المواصفة تجاهل القيمة النسبية، وإلا عاد موضع الإعداد معتمدًا على المجلد الحالي — و`APPDATA` على Windows.
    - **استُبعد خياران مع توضيح السبب.** مخزن مفاتيح النظام (`keyring`) أكثر أمانًا على جهاز مكتبي، لكنه يفشل في البيئات headless — الخوادم والحاويات وCI — أي في حالة الاستخدام ذاتها الخاصة بالترجمة المجمّعة؛ وهو مرشح جيد كخيار opt-in، لكنه خيار افتراضي سيئ. أما flag ‏`--api-key` فسيضع المفتاح في سجل shell ويجعله ظاهرًا في `ps`.
    - **عند غياب المفتاح، لم يعد هناك تتبع للاستدعاء.** كان المستخدم يتلقى stack trace من Python يشير إلى `site-packages`، ورسالة تذكر «البيئة أو ‎.env» من دون توضيح مكان إنشاء الثاني. تسرد الرسالة الآن المواضع الثلاثة بمساراتها الدقيقة، وينتهي الأمر بالرمز 2. شبكة الأمان **ضيقة عن قصد**: ‏`except ValueError` لمرحلة الإعداد وحدها. فتغليف التنفيذ كله سيحوّل عطلًا حقيقيًا يقع أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الفشل الذي يتعقبه هذا المستودع. يقرأ اختبار مصدر `main()` لمنع ذلك.

  - **تصحيح — كان `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** لا يصعد `load_dotenv()` من المجلد الحالي عند استدعائه بلا وسيط، بل يبدأ من الملف المستدعي، أي من `site-packages`. جرى قياس ذلك بنقطة دخول console حقيقية شُغّلت من مشروع يملك `.env` الخاص به: يعيد `find_dotenv()` القيمة `''` ولا يُحمَّل المفتاح، بينما يعثر عليه `find_dotenv(usecwd=True)`. لم يكن العطل موجودًا ما دامت الأداة تعمل من المستودع المستنسخ وحده؛ وكان النشر سيجعله منهجيًا، مع عرض وحيد هو ظهور مفتاح API على أنه «مفقود» رغم صحة الإعداد.

  - **كان يمكن لثلاث بوابات أن تصبح خضراء بعد أن تتوقف عن التحقق من أي شيء.** جرى تشديدها قبل النقل عمدًا: فآلية حماية تُكتب بعد التغيير الذي يُفترض أن تكتشفه لا تثبت شيئًا. كل واحدة منها خضراء على المستودع الأصلي وتصبح حمراء على نسخة منقولة — وقد قيس الاتجاهان.

    - **يتجاهل Lizard المسار الغائب من دون قول أي شيء**: ‏rc=0، و«جرى تحليل 0 ملف». كانت بوابة التعقيد ستنتقل من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع إخراج حجمه صفر بايت. أصبح النطاق الآن مصفوفة يجري التحقق من وجود كل عنصر فيها.
    - **لا يفشل `coverage run --source=` عند تشغيله على وحدة غائبة**: تحذير على stderr وحده، وrc=0 لكل من unittest و`coverage xml`، ومع ذلك يُنشر التقرير — بعد انخفاضه من 1453 إلى 141 statements. كان المشروع سيبدو سليمًا لأنه بالكاد خضع للتحليل. يحمي حدّان أدنيان التقرير: الإجمالي، وأكبر ملف جرى قياسه.
    - **مسبار حداثة الترجمات أعمى بنيويًا عن شكل الاستدعاء**: فهو يرتكز على flags الخاصة بـargparse، وهي تحديدًا ما لا يتغير عند إعادة تسمية الملف. أُعيد إنتاج الحالة: نُقلت الوحدة، وظلت ملفات README الخمسة عشر توثّق أمرًا غير موجود، وكانت النتيجة «لا توجد ترجمات قديمة». لذلك يتحقق قسم سابع من الشكل لا من الخيارات، وتُقارن آلية Lizard بالنطاق الفعلي للسكربت — فمفتاحها `files:` لا يجعل pre-commit يفشل حين لا يعود متطابقًا، بل يجعله يتخطى الفحص.

  - **لم يعد `requires-python = ">=3.10"` مجرد ادعاء.** كان `sonar-project.properties` يعلن أصلًا دعم 3.10-3.12 من دون أن تُختبر تلك الإصدارات يومًا، لأن جهاز التطوير لا يملك سوى 3.12 — وهو تناقض داخلي كان النشر سيجعله علنيًا. يشغّل workflow للاختبارات الآن المجموعة على 3.10 و3.11 و3.12، مع تثبيت الحزمة ومن ثم اختبار حدودها المعلنة.

  - **حدود دنيا، بلا سقف أعلى.** يظل `requirements.txt` هو lock المختبَر، ويصبح `[project.dependencies]` العقد العام: فنشر إصدارات lock الدقيقة كان سيسبب تعارضات لأي مستخدم لديه حزم أخرى. ولا يوجد سقف `<N+1` أيضًا — إذ سيتعارض مباشرةً مع `check-deps-fresh.sh`، الذي يجعل بوابة الإصدار تفشل عند أي تأخر في إصدار رئيسي. تُحل مجموعة الحدود الدنيا، ويخرج الاختبار المضاد `openai==1.0.0` بالقيمة `ResolutionImpossible`، ما يثبت أن الفحص يميّز بدلًا من قبول كل شيء. كما تمنع آلية حماية إصدار `pyproject.toml` من الاختلاف عن إصدار CHANGELOG: لا يسمح PyPI بإعادة استخدام رقم إصدار.

  - **جرى التحقق من البداية إلى النهاية داخل venv جديد**: ملف wheel بحجم يقارب 70 كيلوبايت لا يحتوي إلا على `aipmt/*.py` وdist-info والترخيص؛ و`aipmt --help` بقيمة rc=0 مع 22 flags؛ و`python -m aipmt` يعرض «usage: aipmt» لا «usage: \_\_main\_\_.py»؛ وتثبيت `pipx` يعمل؛ والأهم **ترجمة فعلية من الفرنسية إلى الإنجليزية من أي مجلد مستخدم**، مع الحفاظ على النص الغامق والقائمة والكود المضمّن والرابط وURL، وعدم ترجمة كتلة الكود. تنجح الاختبارات الـ318 السابقة للنقل مع قائمة معرّفات متطابقة بايتًا ببايت قبل النقل وبعده — وهذا ما يثبت أن أي اختبار لم يُعطّل، لا عبارة «OK»؛ وأُضيف إليها اثنا عشر اختبارًا للإعداد ذي الطبقات الثلاث، ليصبح المجموع 330.

- **1.10.0** ‏Provider ‏`--use_codex` ‏(حصة اشتراك ChatGPT)، وتحديث SDKs والنماذج، وتصحيح استشهادات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — آليتا حماية وضعتْهما PR من دون تطبيقهما في كل موضع**:

    - **كان preflight الخاص بـCodex يمرر `.env` كاملًا إلى الملف التنفيذي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: فترث العملية الفرعية `os.environ` كاملًا، وبالتالي كل `.env` الذي حمّله `load_dotenv`. جرى القياس بملف تنفيذي زائف مزوّد بأجهزة رصد: وصلت **سبعة أسرار** إلى preflight — مفاتيح providers الستة بالإضافة إلى `GITHUB_TOKEN` — مقابل **صفر** لنظيره `_grok_preflight`، الذي كان يمرر `env=_grok_env()` فعلًا. كان التناقض داخليًا في PR: إذ يوجد `_strip_secret_env` تحديدًا للحفاظ على هذا الثابت، على بعد بضعة أسطر فقط. استُخرج `_codex_env_base()` وصار مشتركًا بين المسارين؛ والقياس بعد التصحيح: 0 سر في كلا الجانبين.
    - **لم تكن خاصية «`--deny` ‏fail-closed» تغطي الصيغة المستخدمة.** كان التعليق يبرر عزل Grok كله بأن قاعدة ذات prefix مجهول تجعل التشغيل يُرفض. عند القياس على `grok 1.0.13`، لا يوجد هذا التحقق **إلا للصيغة ذات الأقواس**: يرفض `--deny 'CeciNestPasUnOutil(*)'` التشغيل («unknown tool prefix»)، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. لكن `GROK_DENY_RULES` لم يكن يستخدم سوى أسماء مجردة — ولذلك كان تغيير اسم أداة لدى xAI سيزيل طبقة العزل الوحيدة المقاسة من دون أي إشارة، على جهاز لا يطبّق أصلًا sandbox نظام التشغيل. انتقلت القواعد الثماني المسماة إلى `Prefix(*)`، مع التحقق من كل واحدة بوصفها prefix معروفًا لدى CLI؛ وظلت قاعدة catch-all ‏`*` بصيغتها الحرفية، وهي الصيغة الوحيدة المقبولة. يمنع اختبار العودة إلى الصيغة غير الخاضعة للتحقق.
    - **وقد ثبتت سلامة بقية الجوانب**: لا command injection ‏(تُستخدم صيغة القائمة في كل موضع، ولا يُستخدم `shell=True` مطلقًا، ويُمرر محتوى المستند عبر stdin أو `--prompt-file`)، ولا deserialization غير آمن (`json.loads` فقط، مع حارس نوع)، ولم يُعثر على تجاوز لتصحيح اجتياز المسار عبر سبع حمولات، ويطبّق CLI ‏`--deny '*'` فعلًا (لوحظ `DENY_ENFORCED` عند قراءة خارج workdir).
    - كما كان فحص الحداثة المضاف أعلاه يلتف على مبدئه: فالحزمة التي يفشل طلبها إلى PyPI كانت تُتخطى بصمت وتظل البوابة خضراء. أصبح الآن يحصي الحزم التي جرت مقارنتها فعليًا ويفشل إذا كانت التغطية ناقصة.

  - **تحديث التبعيات، وإضافة شبكتي أمان لمنع تكرار التأخر**:

    - **كان التأخر حقيقيًا ومستمرًا**: ‏`openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر عامين في مخزن شهادات الجذر الذي يتحقق من TLS لجميع استدعاءات providers. حُدد السبب: **لم يكن `.github/dependabot.yml` موجودًا**. من دون هذا الملف، لا يفعّل GitHub إلا _security updates_، ولا يقترح Dependabot ‏PR إلا لتبعية تستهدفها CVE — وهو ما يفسر تحديثه `urllib3` و`idna` مع ترك SDKs ينحرفان بمقدار إصدار رئيسي كامل.
    - **يتعايش الإصداران الرئيسيان بلا تعارض**، خلافًا لما أوحى به استدلال سابق: ينتقل `openai` ‏3.x و`anthropic` ‏1.x إلى **`httpx2`**، بينما يظل `mistralai` و`google-genai` على `httpx<1`، لكنها توزيعات منفصلة. جرى التحقق بتثبيت فعلي، ثم عبر **اختبار مسارات provider السبعة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الكود المضمّن والروابط في كل إخراج. كان «تجنب حزمتين من HTTP» تفضيلًا لا عائقًا: وقد حسم القياس الأمر.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: كانت `google-auth` و`cryptography` وحزمة `opentelemetry` مثبّتة في venv العمل من دون التصريح بها إطلاقًا — ولذلك لم يكن التثبيت الجديد يعيد إنتاج ما خضع للاختبار. وعلى العكس، كانت `tokenizers` و`huggingface-hub` و`PyYAML` مدرجة فيه من دون أن يستوردها أو يطلبها أي شيء، وهي بقايا من `mistralai` ‏1.x. أُعيد توليد الملف ليكون الإغلاق الكامل لـvenv مبني من التبعيات المباشرة وحدها. لا يعثر `pip-audit` على أي ثغرة معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** (جديد) يفعّل تحديثات الإصدارات الأسبوعية لـpip وgithub-actions. تُجمع الإصدارات الثانوية والتصحيحات في PR واحدة — إذ ينتهي الأمر بتجاهل PR منفصلة لكل تحديث patch، والضوضاء عدو التحديث؛ أما **الإصدارات الرئيسية فمنفصلة**، ويتطلب كل منها تحققًا عبر استدعاء فعلي.
    - **`scripts/check-deps-fresh.sh`** (جديد وموصول بالبوابة) يجعل التأخر ظاهرًا في حكم المشروع: يقترح Dependabot التحديثات لكنه لا يضمنها، وقد تتراكم PR الخاصة به. التأخر في إصدار رئيسي ← فشل؛ والإصدار الثانوي ← تحذير، لأن البوابة الحمراء دائمًا ينتهي بها الأمر إلى التجاهل؛ وتعذر الوصول إلى PyPI ← تخطٍّ صريح محليًا و**fail-closed في CI**، إذ لا يُعد الفحص غير المنفذ نجاحًا. جرى التحقق في الاتجاهين: فهو يكتشف الحالة الدقيقة السابقة للتصحيح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير عند تأخر إصدار ثانوي.

  - **تصحيحات ناتجة عن مراجعة PR هذه** — فحص خمسة وكلاء مراجعة diff بدقة؛ وقد **أُعيد إنتاج جميع النقاط أدناه بالقياس** قبل تصحيحها، وكانت اثنتان منها انحدارين أُدخلا أعلاه ضمن الإصدار نفسه.
    - **إصلاح تراجع — كان `_NEWS_CITATION_REGEX` يعاني من تراجع خلفي أُسّي.** أدخل إصلاح الفقرات المتعددة `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: كان تقاسم المسافات بين `[ \t]+` و`.*` ملتبسًا، وكان هذا الالتباس يتضاعف من تكرار إلى آخر. قيس ذلك على أسطر `>   texte` — وهي إزاحة Markdown صالحة تمامًا — لا تطابق النمط: **2 589 مللي ثانية لـ14 سطرًا** مقابل 0,04 مللي ثانية بعد التصحيح، بمعامل يقارب 9 لكل سطر إضافي. في وضع `--news`، كان اقتباس كتلي طويل وغير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب قابل للتحديد. يستهلك التكرار الآن السطر كاملًا كوحدة واحدة (`\n^>(?![ \t]*—).*`)، فلا يترك إلا طريقة واحدة للمطابقة في كل تكرار. جرى التحقق على المجموعة الفعلية المؤلفة من 231 مقالًا: **لا اختلافات** في الالتقاط، ونفس الاقتباسات البالغ عددها 423، مع استمرار توسيع المتون الأربعة عشر متعددة الفقرات.
    - **كان استخدام علامتي provider معًا يؤدي بصمت إلى احتساب الرسوم حسب الاستخدام.** كان `--use_codex --use_mistral` مقبولًا؛ يختبر `_select_provider_client` ‏Mistral أولًا، بينما يمنح `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. وهكذا كان المستخدم يطلب الاستفادة من حصة اشتراكه ويحصل بدلًا منها على فوترة حسب الاستخدام، من دون أدنى تحذير: وهو بالضبط نمط الإخفاق الذي وُجد `--use_codex` لمنعه. تمر علامات provider الست الآن عبر `add_mutually_exclusive_group`. **تغيير سلوكي**: يفشل الآن سطر الأوامر الذي يجمع بين موفّرين، بعدما كان يُقبل بصمت، عند `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة إنهاء العمل تتحول إلى اللون الأخضر عند تعطل مجسّاتها.** كانت أربعة من الفحوص الثلاثة عشر في `scripts/check-release-ready.sh` تتبع نمط «التقاط stdout والاستنتاج إذا كان فارغًا» من دون الرجوع مطلقًا إلى رمز الإرجاع: كان استثناء (ملف أُعيدت تسميته، `FileNotFoundError`) يكتب إلى stderr ويترك stdout فارغًا، فتستنتج عملية التحقق أنه «لا شيء يستدعي الإبلاغ». وهكذا تكرر داخل البرنامج النصي المكتوب لمنعها فخ «لا يثبت `exit 0` شيئًا». يفرض الآن مساعد `probe()` رمز إرجاع صفريًا **بالإضافة إلى** علامة نهاية، وترفض المجسّات الاستنتاج انطلاقًا من مجموعة معالم فارغة — لأن التوكيد على مجموعة فارغة صحيح دائمًا. توضيحًا لذلك: أدى إدخال المجموعة الحصرية أعلاه إلى تمرير علامات provider عبر كائن `*_group`، فلم يعد التعبير النمطي القديم `parser\.add_argument\(` يطابقه؛ وكانت **ست علامات من أصل إحدى وعشرين** تخرج من النطاق بصمت مع بقاء البوابة خضراء.
    - **كان فحص الأسرار يفوّت أربعة موفّرين من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: كان `sk-proj-…` (تنسيق OpenAI الحالي) و`sk-ant-api03-…` ينقطعان عند الشرطة الثانية، ولم يكن `AIza…` مشمولًا. وُسّع النمط واستُبعد `.secrets.baseline` من الفحص. كذلك كانت الحماية `.env` تستعلم من `git diff --cached`، الذي لا يرى سوى الفهرس: لم يكن `.env` **الملتزم به سابقًا** — وهو أسوأ الحالات — يظهر فيه قط. وهي تستعلم الآن من `git ls-files`.
    - **لم يكن «الإحماء المسبق للرمز المميز» في Codex إحماءً مسبقًا أصلًا.** وفق القياس، لا يلمس `codex login status` الملف `~/.codex/auth.json` (وقت التعديل والحجم بلا تغيير)، وتقول مساعدته «عرض حالة تسجيل الدخول». ومع ذلك كان التعليق يدّعي تحديث الرمز المميز «مرة واحدة، تسلسليًا»، وبالتالي تحييد خطر التحديثات المتزامنة لرمز مميز دوّار أحادي الاستخدام. لم تكن الحماية المعلنة موجودة؛ ويصف التعليق الآن ما تفعله الشيفرة فعلًا، بينما تظل الوقاية الحقيقية هي `max_jobs=4`. كما يحترم الفحص `CODEX_BIN`، الذي كان يتجاهله — إذ كانت محطة عمل لا تحتوي على `codex` ضمن `PATH` تفشل برسالة «غير مصادق عليه»، وهو تشخيص مضلل.
    - **كان `.env` يُحمّل داخل صدفة فرعية.** تُستدعى `detect_provider` ضمن استبدال أمر، ولذلك لم تكن القيم التي تصدّرها تصل إلى الأعلى: كان أي `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` معرّف في `.env` يظل غير مرئي لعمليات القراءة المنفذة في `main()`، التي كانت تستنتج «تعذر العثور على ملف Grok التنفيذي» رغم صحة الإعداد.
    - **تجاوز التوازي الحد المعلن بنسبة 50٪.** وُضعت الحماية بعد تشغيل زوج README/CHANGELOG: بلغت الذروة المقيسة **3 عند `max_jobs=2`**. وبالنسبة إلى Grok، الذي تُشارك حصته الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يكن البرنامج النصي يلتزم إذًا بالحد الذي فرضه على نفسه. أما العدد النهائي فكان يُعرض من دون مقارنته قط بالعدد 28 — ولذلك كان غياب ملف يمر من دون اكتشاف.
    - **عقد مخرجات Grok: أصبح غياب `stopReason` إخفاقًا.** كانت الشيفرة تطبق «`end_turn` **أو غائب**» حيث يشترط العقد المعلن وجود `end_turn`. كانت حمولة بلا هذا الحقل — أو أُعيدت تسمية حقلها بفعل تحديث CLI — تحول الحماية بصمت إلى عملية عديمة الأثر. كذلك لم يعد `max_turn_requests` يُصنّف كتجاوز لحد المعدل (فالذي نفد هو ميزانية الجولات: تعيد المحاولة النتيجة نفسها بكلفة انتظار 90 ثانية)، وأُزيل `quota` من علامات تجاوز حد المعدل — للسبب الذي كان توثيق `_codex_is_rate_limited` يذكره بالفعل من دون أن يطبقه Grok.
    - **أصبحت سلسلة Gemini محفوظة لكل نموذج.** كانت تبدأ مجددًا من `minimal` عند كل مقطع، رغم أن النموذج الافتراضي يرفضه: كان المسار المعتاد يدفع كلفة رحلة ذهاب وإياب تنتهي بالرمز 400 لكل مقطع ويعيد طباعة التحذير نفسه. والتحذير الذي يتكرر مئات المرات يتوقف الناس عن قراءته — وهكذا يتحول إلى ستار يحجب الأخطاء.
    - **متفرقات**: كانت رسالة الرفض في CI ثابتة ومخصصة لـCodex، فتوجّه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وعمّم تعليق البنية الأساسية للعمليات الفرعية مفهوم «shim» على أداتي CLI مع أن ملف Grok التنفيذي هو ملف ELF أصلي (والتبرير الصحيح هو «وكيل يشغّل عملياته الفرعية الخاصة»)؛ وصُنّفت اثنتا عشرة ملاحظة SAST على `subprocess` بعلامتي `# nosec` / `# nosemgrep` مع تبرير، لأن صيغة القائمة من دون `shell=True` تجعل الحقن مستحيلًا ولأن محتوى المستند لا يمر أبدًا عبر argv.
    - **لم يعد أي سر يدخل إلى العمليات الفرعية الوكيلة.** لم تكن قوائم الحظر الاسمية تحمي إلا ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). ووفق القياس، كانت **سبعة أسرار أخرى** تدخل مع ذلك إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح أداة CLI الأخرى، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة البيانات. غير أن أداتي CLI هاتين **وكيلان**، ويعمل وكيل Grok من دون sandbox لنظام التشغيل قابل للتطبيق على كثير من محطات Linux. يجري الترشيح الآن **بحسب نمط الاسم** (`API_KEY` و`_TOKEN` و`SECRET` و`PASSWORD` و`CREDENTIALS`) لا بحسب قائمة اسمية، وهو ما يشمل أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن تعرفها هذه الشيفرة. ولا تحتاج أداتا CLI إلى أي منها: توجد المصادقة في `~/.codex` و`~/.grok`، وليس في البيئة مطلقًا — وقد جرى التحقق عبر **ترجمة فعلية ناجحة** باستخدام كل واحد من الموفّرين ضمن البيئة المحصّنة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يثبّت حصرية علامات provider، وعقد `stopReason`، وخطية التعبير النمطي للأخبار، ورسالة الرفض في CI، وحفظ Gemini، وغياب كل الأسرار عن بيئة العمليات الفرعية. وهذا التوكيد الأخير **عام** — إذ يفشل عند وجود مفتاح لا تسمّيه أي قائمة — في حين كان اختبار التنقية الحالي صورة طبق الأصل من ثابته ولم يكن قادرًا على اكتشاف شيء سوى تعطل حلقته الخاصة. تضم الحزمة الكاملة **311 اختبارًا**.

  - **موفّران جديدان لـGrok**: ‏`--use_grok` (واجهة xAI API، بالمفتاح `XAI_API_KEY`، وفوترة حسب الاستخدام) و`--use_grok_cli` (أداة Grok Build CLI الرسمية، تُحتسب من اشتراك Grok — وفق المبدأ نفسه المستخدم في `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: لأن نقطة نهاية xAI متوافقة مع OpenAI، أُعيد استخدام العميل و`_call_openai` كما هما، ولا يتغير سوى `base_url`. لم يلزم سوى تعديل واحد يستفيد منه الجميع: يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي يصدرها xAI حيث يصدر OpenAI ‏`stop`. النماذج: `grok-4.6` (الجودة) و`grok-4.3` (الاقتصادي). وتجدر الإشارة إلى أن خيار Grok الاقتصادي يظل الأغلى في المستودع — $1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ`mistral-small-latest`: يُختار هذا الموفّر لتنوع النماذج، لا للسعر.
    - **وضع CLI**: صُمم على غرار Codex، مع أربعة اختلافات يفرضها الواقع — يُرسل الموجّه عبر ملف (`--prompt-file`، لأن CLI لا يقرأ stdin ولأن وضع مقطع في argv سيجعله ظاهرًا في `ps`)، والمخرجات كائن JSON واحد على stdout (وليست JSONL ولا ملف `-o`)، ولا يتيح الاشتراك سوى `grok-4.6` و`grok-4.5`، ولا يمكن تطبيق sandbox (انظر أدناه). جرى توحيد تشغيل العمليات الفرعية مع Codex في `_codex_run_process` من دون المساس ببقية موفّر Codex المختبَر سلفًا.
    - **لا يثبت `exit 0` شيئًا، وفق القياس**: عندما لا تكون المصادقة قائمة، تكتب أداة CLI ‏`{"type":"error","message":"Not signed in."}` إلى **stdout** مع رمز إرجاع **0**. ويتصرف الرفض أو تجاوز عدد الجولات بالطريقة نفسها. لذلك يشترط عقد المخرجات أربعة شروط متزامنة: رمز إرجاع 0، وغياب حمولة خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع الفحص المسبق المنطق نفسه: يخرج `grok models` بالرمز 0 حتى عند قطع الاتصال، ولا يمكن الاستنتاج إلا من وجود «غير مصادق عليه» في stdout.
    - **الاحتواء: لا تماثل مقصود وموثّق.** بينما يعمل Codex ضمن `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـGrok على كثير من محطات Linux الحديثة لسببين مستقلين على مستوى النظام لا يمكن تجاوزهما من دون `sudo`: يحظر AppArmor مساحات أسماء المستخدمين غير المميّزين منذ Ubuntu 24.04 (`bwrap: setting up uid map: Permission denied`، وأُعيد إنتاج المشكلة خارج Grok)، وتفشل قائمة حظر مقابس بيئة تشغيل الحاويات عندما يكون `/run/podman` في `0700` (لا يستدرك المحلّل سوى `ErrorKind::NotFound`، بينما يصبح EACCES خطأ فادحًا). والمأزق الأساسي هو أن ملفًا **مدمجًا** يتعذر تطبيقه يؤدي إلى التشغيل **من دون احتواء وبصمت**. لذلك لا يطلب البرنامج النصي أي ملف افتراضي ولا يتراجع بصمت أبدًا — بل يحذّر على stderr. تعتمد الحماية على قواعد `--deny` في CLI، بما فيها قاعدة التقاط الكل `*`، وهي الطبقة الوحيدة التي ثبت بالقياس أنها _تغلق عند الإخفاق_ (إذ تؤدي قاعدة ذات بادئة مجهولة إلى رفض التشغيل). يتيح `GROK_TRANSLATE_SANDBOX=read-only` فرضها، وعندئذ يفشل التشغيل إذا تعذر على الجهاز احترامها.
    - **ضوابط الأمان**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (فوجود مفتاح يحوّل التشغيل إلى فوترة حسب الاستخدام؛ كما أن `GROK_SANDBOX` موروثًا سيفرض ملفًا غير قابل للتطبيق مع رسالة مضللة)، وتُعطّل مفاتيح MCP والخطافات والمهارات والوكلاء، إلى جانب `--disable-web-search` و`--no-subagents` و`--no-plan`، ودليل عمل مؤقت، ورفض في CI، ومهلة تقتل مجموعة العمليات، وتراجع تدريجي عند تجاوز حد المعدل. ضُبط `--max-turns` على 6 لا على 1: إذ يزداد العداد بعد جولة الأدوات، وكانت القيمة 1 ستقتطع المخرجات.
    - **الحصة**: مجمّع Grok أسبوعي و**مشترك مع Chat وImagine وVoice**، ولا يعرضه أي أمر — بخلاف Codex الذي يتيح `account/rateLimits/read` تقدير الاستهلاك. لذلك يحد `regen_translations.sh` التوازي عند 2 ويحذّر من ذلك صراحة.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` (24 اختبارًا). تضم الحزمة الكاملة **290 اختبارًا**.
  - **إصلاح خلل — لم تكن اقتباسات EN متعددة الفقرات محمية إلا جزئيًا (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل كمتن للاقتباس سوى سلسلة من أسطر `>` **المتتالية**. وما إن يمتد اقتباس عبر فقرات عدة (تفصل بينها سطر `>` فارغ) حتى لا يُلتقط ويُستبدل بالعنصر النائب سوى آخرها؛ أما ما قبله فيُرسل إلى LLM ويعود مترجمًا — وهو النقيض التام لما وُجد `--news` لضمانه. يقبل التكرار الآن أسطر `>` الداخلية الفارغة وأصبح غير جشع، كي يتوقف عند `>` الفارغ السابق للسطر المائل، لا عند أول سطر فارغ يصادفه.
    - **النطاق المقيس** على مجموعة فعلية من 198 مقالًا: تأثر 11 اقتباسًا من أصل 419. ولا يوجد أي تراجع — إذ يلتقط التعبير النمطي الجديد العدد نفسه تمامًا من الاقتباسات، ولا تتغير إلا المتون متعددة الفقرات (408 متون متطابقة، و11 موسّعًا)، ولا يزال من المتعذر ابتلاع سطر الإسناد `> — …` داخل المتن (مع الإبقاء على الاستباق).
    - **دليل شامل من البداية إلى النهاية** على مقال حجمه 69 كيلوبايت تُرجم إلى ja/ar: بقيت الفقرة الأولى من أحد الاقتباسات، التي كانت تُعرض سابقًا بصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم بالمثل إلى العربية، الآن بصيغة `> GLM-5.3 is now open-weight.`. وعاد عدد أسطر الاقتباس بالإنجليزية من 9 إلى 10، وهو عددها في المصدر.
    - تجدر الإشارة إلى أن أدوات التحقق اللاحقة لم تكن تكتشف هذا العيب، لأنها تتحقق من وجود الاقتباس من دون التحقق من اكتماله.
  - **توفير مقيس لدى الموفّر الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ النموذج بـ`gpt-5`، بما في ذلك وضع `--eco`. القياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: `medium` ← 45 رمز استدلال و65 رمز مخرجات؛ `none` ← 0 و14. لا يضيف الاستدلال شيئًا إلى الترجمة، وكانت كلفته تُدفع لكل مقطع من كل ملف. أصبحت القيمة الافتراضية `none` في `--eco`، وتظل `medium` في غير ذلك؛ وتبقى الأولوية لقيمة صريحة تُمرر عبر CLI. يقبل `--reasoning_effort` الآن `none` و`xhigh` بالإضافة إلى `low`/`medium`/`high` (ولا تقبل كل النماذج جميع هذه القيم: فعلى سبيل المثال يرفض `gpt-5.4-mini` القيمة `minimal` — وتغطي هذه الحالة إعادة المحاولة الحالية من دون المعامل).
  - **تحديث حزم SDK وترحيل Gemini**: استُبدلت `google-generativeai` (انتهى دعمها في 2025-11-30 وأُرشف مستودعها) بحزمة SDK الموحّدة **`google-genai`** — ‏`genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير موجّه النظام في `system_instruction` بدلًا من ضمه إلى المقطع. انتقلت `mistralai` إلى **2.9.4** (وأصبح الاستيراد `from mistralai.client import Mistral`؛ بينما يثير الإصدار القديم `ImportError`، وفق التحقق داخل wheel)، و`anthropic` إلى **0.125.0**، و`openai` إلى **2.54.0** — وهي أحدث الإصدارات قبل الانتقال إلى `httpx2`، تجنبًا للجمع بين مكدسي HTTP داخل venv. وأُتيحت تبعًا لذلك ترقية `httpx` إلى 0.28.1 و`pydantic` إلى 2.13.5.
  - **تراجعان اكتشفتهما اختبارات فعلية، لا التوثيق**:
    - يرفض `anthropic` بالإصدار 1.0 فأحدث من جهة العميل استدعاء غير متدفق يوحي `max_tokens` الخاص به بأنه قد يستغرق أكثر من 10 دقائق (`ValueError: Streaming is required...`). لم يكن هذا الضابط موجودًا في 0.34.2، وكان يعطّل كل استدعاء لـClaude باستخدام `max_tokens=32768`. صُحح ذلك بإضافة `timeout` صريح (`CLAUDE_TIMEOUT`، و900 ثانية افتراضيًا)، ما يتجنب الانتقال إلى التدفق لاستدعاء لا تُستخدم منه إلا الاستجابة الكاملة.
    - لا يقبل `thinking_level="minimal"` سوى جزء من كتالوج Gemini: يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` بالرمز 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تسلسل `minimal` ← `low` ← من دون thinking_config، على غرار احتياط OpenAI الموجود بالفعل — إذ يجب ألا يؤدي معامل تحسين إلى إفشال ترجمة أبدًا.
  - **تجديد النماذج الافتراضية**، وقد جرى التحقق من كل منها باستدعاء فعلي: OpenAI ‏`gpt-5.5` ← **`gpt-5.6-terra`** (−60٪ على دفعة من 28) و`gpt-5.4-mini` ← **`gpt-5.6-luna`** (−73٪)؛ وClaude ‏`claude-sonnet-4-6` ← **`claude-sonnet-5`** (أرخص وأحدث) و`claude-haiku-4-5-20251001` ← **`claude-haiku-4-5`** (معرّف قياسي بلا تاريخ)؛ وGemini ‏`gemini-3.1-pro-preview` ← **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` ← **`gemini-3.1-flash-lite`** (إصدار مستقر وأقل كلفة من `3.5-flash-lite`).
 بقي Mistral دون تغيير، وظل `mistral-large-latest` يقدم أفضل نسبة جودة/سعر بين النماذج الأربعة. ملاحظة: لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — إذ أُعلن عن Gemini 3.5 Pro في مايو 2026، لكنه لم يصدر قط؛ فسلسلة 3.5/3.6/3.7 مخصصة حصريًا لفئة Flash.
  - **قياس A/B قبل التحويل إلى Gemini**: تُرجم `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. بنية متطابقة تمامًا (21 قائمة، و18 كتلة برمجية، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، خلال **8 ثوانٍ مقابل 48 ثانية**. ونظرًا إلى عدم وجود أي benchmark عام يقارن هذين النموذجين في الترجمة أو النصوص غير اللاتينية، لكان التحويل قائمًا لولا ذلك على مجرد افتراض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفذ `block.text for block in response.content` من دون تصفية النوع. تُقحم النماذج ذات الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، تكشف `.thinking` بدلًا من `.text` — وكان من شأن الترجمة أن تتعطل بسبب `AttributeError` مبهم في المقطع الأول. باتت كتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` مستبعدة الآن (بقائمة سلبية للحفاظ على تقبّل نوع غير معروف يحمل نصًا)، وتؤدي أي استجابة لا تحتوي على كتلة نصية إلى خطأ صريح. يُمرَّر `thinking={"type": "disabled"}` في كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: أُزيلت النماذج التي انقضى تاريخ سحبها (سُحبت فئة `magistral-*` في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، وكذلك `claude-3-5-sonnet-20240620` و`claude-3-7-sonnet-20250219` و`claude-opus-4-1-20250805` و`claude-sonnet-4-20250514`). تصحيحات الحدود: Mistral من 128K إلى **256K** (جيل Large 3 / Small 4)، وGemini من 1 000 000 إلى **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` من 200K إلى **1M**، وعائلة `gpt-5.6-*` من 400K إلى **1,05M**. أُضيفت Claude 5‏ (`claude-sonnet-5` و`claude-opus-5` و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وفئة `ministral-*`. ملاحظة: تظل هذه الحدود إرشادية، إذ يحد `translate()` التقسيم عند `min(16000, limite)`.

  - **Provider ‏`--use_codex`**: Provider خامس يتحكم في Codex CLI الرسمي (`codex exec`) في الوضع غير التفاعلي، بدلًا من استدعاء API تُحتسب تكلفتها حسب الاستخدام. تُخصم الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقًا. وهذه هي الطريقة الوحيدة التي توثقها OpenAI لهذا الاستخدام: إذ تذكر مصفوفة التوفر حسب الخطة أن «Codex SDK و`codex exec` وسير العمل القابل للبرمجة» متاحة في Plus/Pro/Business/Enterprise، بينما لا تصادق tokens الخاصة بـ`~/.codex/auth.json` على استدعاءات API Platform (ولا يقرأها هذا السكربت مطلقًا — إذ تظل المصادقة وتجديدها تحت إدارة CLI).
  - **إمكانية تثبيت ملف Codex التنفيذي عبر pip، وليس عبر npm فقط**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK ‏`openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عام لاستخدام `--use_codex`. لم تُضف الحزمة إلى `requirements.txt`: يبلغ حجم الملف التنفيذي نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين من أجل Provider اختياري. جرى التحقق من البداية إلى النهاية: مع غياب `codex` عن `PATH`، يعثر الحل على الملف التنفيذي المضمّن في الحزمة وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: يُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. فمن دون هذا الإجراء الوقائي، قد يؤدي وجود مفتاح في `.env` إلى تحويل Codex إلى الفوترة حسب الاستخدام من دون أي إشارة ظاهرة — وهو بالضبط ما وُجد هذا الـProvider لتجنبه.
  - **مشكلات CLI المحصّنة بالاختبارات**:
    - يقرأ `codex exec` stdin **حتى** عند تمرير prompt كوسيط: من دون إغلاق stdin، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج مطلقًا (أُعيد إنتاج الحالة: exit 124 بعد 180 ثانية، وصفر بايت). لذلك فإن `communicate(input=...)` إلزامي.
    - إن `codex` المثبت عبر npm هو shim بلغة Node ينفذ `spawn` للملف التنفيذي الحقيقي المكتوب بلغة Rust: وهذا الأخير **عملية حفيدة** لعملية Python وينجو من `SIGKILL` الخاص بـ`subprocess.run(timeout=)`، حيث كان سيواصل استهلاك الحصة. ومن هنا جاء `Popen(start_new_session=True)` + `os.killpg`.
    - يمكن أن ينتهي CLI بالرمز 0 رغم إصداره `turn.failed`: تُفحص مخرجات JSONL‏ (`--json`) إضافةً إلى رمز الإرجاع، ويؤدي غياب ملف `-o` مع الرمز 0 إلى خطأ صريح بدلًا من إنتاج مقطع فارغ.
  - **Back-off عند بلوغ rate limit**: لا يطبق CLI أي retry داخلي (`max_retries = 0`). يتم التصنيف استنادًا إلى بنية payload بصيغة JSON‏ (`status: 429` / `error.type`)، لا إلى سلاسل فرعية — إذ تظهر كلمة «الحصة» في خطأ 429 قابل للاسترداد كما تظهر في `insufficient_quota` نهائي.
  - **إجراء وقائي لـCI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفًا. فالمصادقة عبر الاشتراك غير مخصصة لـrunner مشترك، كما تنصح OpenAI صراحةً بعدم استخدام سير العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna`‏ (`--eco`). تشترك CLI وAPI Platform في عائلة `gpt-5.6-*`، لكن حساب ChatGPT لا يملك حق الوصول إليها كلها: تُطبق allowlist من جهة الخادم، من دون تحقق محلي، ويؤدي اختيار نموذج غير معتاد إلى تحذير. ضمن خطة Plus، يتيح Luna ما بين 250 و2 000 رسالة لكل نافذة مدتها 5 ساعات، مقابل 10 إلى 100 لـSol: وضع `--eco` هو الموصى به لأي معالجة دفعية.
  - **إصلاح خلل — كان `regen_translations.sh` ينتهي بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` من `main()` لم يعد موجودًا لحظة تنفيذ trap. تحت `set -u`، كان ذلك يثير `failed_log: unbound variable` ويجعل السكربت ينتهي بالرمز 1 رغم صحة الترجمات الـ28 — ما كان سيوقف `release.sh --auto`‏ (`set -e`) مباشرةً بعد إعادة التوليد، عند الخطوة الأعلى تكلفة. أصبح المتغير عامًا، وصار trap يتحقق من وجوده. أثر جانبي مفيد: عادت إخفاقات الترجمة الحقيقية، التي كان هذا الخطأ يحجبها سابقًا، إلى الظهور في الملخص الختامي.
  - **`REGEN_MODEL`**: متغير بيئة جديد لـ`regen_translations.sh` يفرض نموذجًا محددًا بدلًا من الإعداد الافتراضي للـProvider، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج الأعلى فئة ضمن حصة الاشتراك بدلًا من النموذج `--eco` الموجه للحجم.
  - **`regen_translations.sh`**: أصبح `REGEN_PROVIDER=codex` متاحًا عبر تفعيل اختياري صريح (ولا يُكتشف تلقائيًا مطلقًا، تجنبًا لاستهلاك حصة الاشتراك من دون علم المستخدم). يُجدَّد token مرة واحدة تسلسليًا قبل تفعيل التوازي — إذ إن refresh الخاص بـCodex تناوبي وأحادي الاستخدام، وكانت jobs المتزامنة ستبطل جلسة `codex login` — ويُخفض مستوى التزامن إلى 4.
  - **Refactor مرتبط**: ينتقل `_dispatch_provider_call` من 8 إلى 6 معاملات عبر `_resolve_provider()` يعيد اسم Provider، بدلًا من قيمة boolean رابعة يجري تمريرها عبر السلسلة بأكملها. تظل قيم boolean الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` محدود.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` (48 اختبارًا) يغطي argv، والبيئة المنقحة، وعقد منع الاستهلال، والإخفاقات الصامتة، وtimeout/killpg، وback-off، وpreflight، وحل الـProvider، وتسلسل استدلال Gemini، وتصفية كتل Claude، واستشهادات الأخبار متعددة الفقرات. ارتفع إجمالي المجموعة إلى 290 اختبارًا.
  - **التحقق الفعلي**: تُظهر ترجمة `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex بنية مطابقة تمامًا للترجمات المرجعية (14 كتلة برمجية، و24 عنوانًا، و25 سطر جدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل برمجية متطابقة حرفًا بحرف، ومن دون أي بقايا placeholders). وفي مقال صحفي حجمه 69 كيلوبايت بوضع `--news`، تجتاز مخرجات `gpt-5.6-luna` و`gpt-5.6-sol` كلتاهما أداة التحقق التطبيقية اللاحقة للغات en/ja/ar. الاستهلاك المقاس عبر `account/rateLimits/read`: ظل دون عتبة تقريب العداد (0% من نافذة الساعات الخمس) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL لإسناد الأخبار عند وجود أقواس متداخلة أو بادئة فرنسية (2026-05-11):

  - **إصلاح خلل**: كان استخراج عناوين URL الخاصة بالإسناد في `_protect_news_quotes` يستخدم regex ‏`re.search(r"\((.+?)\)", attribution)` (التقاط lazy بين قوسين). وفي إسناد من نوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` المحيط + `]()` الخاص برابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة وتتضمن البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائي). النتيجة: كان `_validate_news_post` يبحث عن هذه السلسلة في المخرجات المترجمة ويفشل منهجيًا (لسببين: `)` مبتور + ترجمة "relayé par" إلى `relayed by`/`weitergeleitet von`/...). ولم يكن التسلسل الكامل low ← medium ← high ← gpt-5.5 قادرًا على النجاح.
  - **الإصلاح**: أصبحت regex هي `re.search(r"\]\(([^)]+)\)", attribution)` — فهي تستهدف تحديدًا `](url)` الخاص برابط Markdown، وتلتقط **عنوان URL الخالص فقط** (من دون البادئة الفرنسية أو البتر)، وهو ثابت تحافظ عليه placeholders ‏`#URL{N}#` أثناء الترجمة. وهي متينة أمام النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس محيطة
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py` ضمن class ‏`TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة الدقيقة المعاد إنتاجها لخلل Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (نسخة متغيرة باستخدام `via`)
  - **فجوة في التغطية**: يتحقق `check-editorial-coverage.py` من الصياغة التحريرية، لكن ليس من قابليتها للترجمة بواسطة translator. وقد يتمثل تحسين محتمل (خارج نطاق v1.9.2) في check يحاكي استخراج الإسناد في dry-run لاكتشاف الأنماط الخطرة قبل النشر.

- **1.9.1** إصلاح i18n لتسمية CTA في ملاحظة علامة الترجمة (2026-05-10):

  - **إصلاح خلل**: ظلت تسمية `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط العلامة أعلى الملفات المترجمة **بالفرنسية** لجميع اللغات المستهدفة، بدلًا من اتباع `target_lang`. لا يراها LLM مطلقًا (إذ تُركّب من جهة Python للحفاظ على عنوان URL وslug الخاص بالمستودع)، ولذلك لم تتمكن مرحلة الترجمة من تصحيحها. كان ذلك انحدارًا صامتًا منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ15 بتسمياتها المحلية. بات `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` يمرران اللغة المستهدفة. يُستخدم fallback ‏`fr` إذا كانت اللغة غير معروفة (للأمان، ومن دون KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link`‏ (target_lang ‏`ja` ← التسمية اليابانية المتوقعة). اختباران جديدان: `test_source_link_label_localized_per_target_lang` (معلَّم بالمعاملات لسبع لغات تغطي الكتابات اللاتينية والإيديوغرافية وabjad) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختبارًا في `test_translation_note_position.py` (بدلًا من 38).
  - **التوافق العكسي**: توقيع بقيمة افتراضية `target_lang="fr"` — تواصل الجهات البرمجية الخارجية المستدعية التي لا تمرر `args.target_lang` العمل من دون تعديل.
- **1.9** إصلاح الفشل الصامت + أدوات جودة متكاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + تنسيق العلامة "embed card"**:
    - خيارات CLI جديدة (إضافية، والقيم الافتراضية دون تغيير ← **لا تتسبب في تغييرات كاسرة**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): يضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - يعيد `legacy` إنتاج سلوك الإصدار v1.8 بدقة (فقرة بخط عريض `**…**`) **بايت مقابل بايت**.
        - ينشئ `marker` تعريف مرجع رابط غير مرئي في Markdown ‏(`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`)، يتبعه **اقتباس من 3 فقرات** منظم لعرض شبيه بـ "GitHub repo embed card": عنوان المشروع ضمن كود مضمن (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف يترجمه LLM، ورابط دعوة إلى اتخاذ إجراء (`[Voir le projet sur GitHub ↗](URL)`) مع سهم مرئي. ويمكن استثماره أثناء البناء بواسطة ملحق remark (راجع مدونة jls42.org ← الملحق `remark-translation-banner`).
    - **ثوابت لا تُرسل مطلقًا إلى LLM**: يُركّب عنوان المستودع وعنوان URL الخاص بـ GitHub في جانب Python بعد ترجمة العبارة الوصفية. لا يرى LLM مطلقًا المعرّف `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تغيير أي عارض أو حالة أحرف أو مخطط.
    - **إدراج واعٍ بالواجهة الأمامية**: في وضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة الإغلاق `---`** للواجهة الأمامية YAML (لضمان سلامة Astro Content Collections / gray-matter). تكتشف الدالة المساعدة `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف وتحافظ على سلامته؛ كما **تُطلق `RuntimeError`** عند وجود واجهة أمامية مفتوحة بلا سياج إغلاق (فيظهر الملف ضمن `failed_files` بدلًا من كتابته مع ملاحظة في موضع خاطئ).
    - **منقّي النموذج بقائمة سماح**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع الرجوع إلى `unknown` إذا كانت النتيجة فارغة. يتوافق ذلك مع أداة التحقق في ملحق remark الخاص بـ Astro ويحيّد المحارف التي قد تفسد تنسيق العلامة (المسافة، وعلامة الاقتباس، والقوس، والفاصلة، وغيرها).
    - **إعادة هيكلة داخلية**: `_append_translation_note` (دالة واحدة ضخمة) ← 7 دوال مساعدة خالصة (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فُصل المنشئ عن المؤلف (يعيد المنشئ كتلة خالصة بلا فاصل، بينما يطبق المؤلف `\n\n` وفقًا للموضع)؛ ويستخدم كود الإنتاج والدالة المساعدة المصدرية المجمّع نفسه ذي الفقرات الثلاث.
    - **يحافظ `_quote_lines` على الأسطر الفارغة**: يضيف `> ` في بداية كل سطر، ويحوّل الأسطر الفارغة إلى `>` وحده. يتيح ذلك لـ mdast رؤية 3 فقرات مستقلة داخل الاقتباس (العنوان / الوصف / الرابط) بدلًا من فقرة واحدة تحتوي على فواصل أسطر.
    - **`_build_translation_note_block` تكيّفي**: وفقًا لعدد الفقرات التي حافظ عليها LLM (3 = تنسيق البطاقة الكامل، 2 = عبارة + رابط، 1 = تنسيق احتياطي). لم يعد التنسيق الاحتياطي ذي الفقرة الواحدة **يغلف المحتوى بـ `**...**`** عند اكتشاف رابط Markdown ‏`](` (لأن عرض `<strong>` حول رابط هش).
    - **توافق تصاعدي**: `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` ضمن `_compose_with_notes` — تواصل كائنات Namespace التي لا تحتوي على هاتين السمتين (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) العمل دون تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور حرفيًا على مقتطف من المصدر) + طبقة احتمالية (`langdetect`)
    - قائمة سماح `finish_reason` / `stop_reason`: إطلاق `RuntimeError` عند أي حالة خارج قائمة السماح (الاقتطاع، وcontent_filter، وغيرها)
    - `max_tokens` في Claude: ‏`4096` ← `32768` (يتجنب الاقتطاع الكامن في المقاطع بحجم 16k، مع هامش للتحويل بين أنظمة الكتابة FR→JA/ZH/KO/AR/HI)
    - تقسيم واعٍ بالعناوين: إعطاء الأولوية لـ H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى رمز خروج غير صفري: يعيد `translate_markdown_file` حالة ذات نوع محدد `success` / `failure` / `skipped`، وينفّذ `main()` ‏`sys.exit(1)` إذا فشل ملف واحد على الأقل (للملف المفرد والدفعة)
    - حاجز للمحتوى الفارغ لدى جميع المزوّدين، وفحص منطقي لنسبة المصدر إلى المخرجات (≥ 500 محرف، وأقل من 5% = رفض)، والتحقق من عناصر الكود النائبة (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، وإعادة محاولة `BadRequestError` من دون `reasoning_effort`
    - إضافة الاعتمادية `langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("حزمة كاملة على غرار EurekAI"، 14 خطافًا):
    - Pre-commit: ‏ruff (فحص+تنسيق)، وshellcheck، وprettier ‏(md/yaml/json)، وdetect-secrets (حماية 4 مفاتيح API)، وLizard ‏(CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، ونهاية الملف، والملفات الكبيرة، وأسطر shebang، وغيرها)
    - Pre-push: ‏mypy (وضع متساهل تدريجي)، وOpengrep SAST ‏(translate.py + scripts/)، وpip-audit (وضع الإبلاغ الأولي)، وunittest discover ‏(tests/ + scripts/tests/)
    - أغلفة محلية ضمن `scripts/` تستخدم `./venv/bin/python`
    - `scripts/audit_verdict.py`: محلل JSON لـ pip-audit مع 11 اختبار unittest، وهو تكييف بلغة Python لمحلل jls42-astro
    - إصلاح 7 مخالفات أولية في ruff: ‏B904 ‏(raise from) ×2، وB007 ‏(dirs غير مستخدمة)، وC408 ‏(dict literal)، وC419 ‏(list-comp)، وSIM105 ‏(contextlib.suppress)، وSIM110 ‏(any())
    - يستثني Lizard مؤقتًا `translate.py` (4 دوال بقيم CCN بين 21 و47، وإعادة الهيكلة مخطط لها) — بوابة صارمة على scripts/
  - **SonarCloud + تغطية شاملة**:
    - سير عمل GitHub Actions ‏`SonarCloud` ‏(sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع التغطية عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى README ‏(Quality Gate، وتقييمات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code)
    - `tests/test_silent_failure.py` ‏(`unittest` من المكتبة القياسية): يغطي الحلقات الست في سلسلة أخطاء الفشل الصامت
    - `tests/test_orchestration.py` ‏(+79 اختبارًا): يغطي طبقة التنسيق في `translate.py` ‏(`_resolve_*_filename`، و`_existing_translation_exists`، و`_record_translation_status`، و`_write_output_file`، و`translate_directory`، و`_validate_input_paths`، و`_init_*_client`، و`_select_provider_client`، و`_normalize_collapsed_markdown`، و`_cleanup_source_flag`، و`_validate_news_flags_*`، والتنسيقات الاحتياطية `_openai_create_with_fallback` الخاصة بـ TypeError وBadRequestError، وتنسيق مطالبات سلسلة o1، وفروع الإرجاع المبكر في `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` ‏(stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **التغطية على الكود الجديد**: 75.5% ← نحو 98% ‏(translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك الاختبار الشامل `marker+top|bottom|both` و`legacy+top|bottom|both`)، وإضافة البادئة إلى عدة أسطر، والتوافق التصاعدي بايت مقابل بايت (قيمة مرجعية حرفية)، والمنقّي، وتقسيم الواجهة الأمامية (بما في ذلك إطلاق استثناء عند عدم إغلاق السياج)، وتنسيق الفقرات الثلاث، والتنسيق الاحتياطي ذي الفقرتين، وحاجز الفقرة الواحدة + رابط Markdown، وحاجز أمان حرج `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان+عنوان URL لا يُرسلان مطلقًا إلى LLM. **نجح 190 اختبارًا**، بلا أي تراجع.
  - التوثيق: `README.md` ‏(بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` ‏(سير عمل pre-commit + مراقبة مفصلة لـ CI)، وإعادة إنشاء 28 ترجمة
- **1.8** وضع `--news` + ترقية نماذج 2026 ‏(2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - جودة OpenAI: ‏`gpt-5` ← `gpt-5.4`
    - OpenAI الاقتصادي: ‏`gpt-5-mini` ← `gpt-5.4-mini`
    - جودة Gemini: ‏`gemini-3-pro-preview` ← `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` ‏(400k) و`gemini-3.1-pro-preview` ‏(1M)
  - وضع `--news` الأولي: حماية الاقتباسات الإنجليزية باستخدام العناصر النائبة `#NEWSQUOTE\d+#`، وتعيين `LANG_FLAGS` ‏(15 لغة)، وإدارة الأعلام وفقًا للغة الهدف
  - التحقق من عناصر الأخبار النائبة قبل استعادتها (تراجع: كان حذف LLM للعنصر النائب يؤدي بصمت إلى مخرجات بلا اقتباس)
  - جعل السكربت `regen_translations.sh` قابلًا للنقل (مسارات مطلقة، بلا اعتماد على pwd)
  - إضافة رابط الفرنسية إلى أشرطة اللغات في README/CHANGELOG، وإعادة إنشاء 28 ترجمة
- **1.7** الميزات الجديدة:
  - خيار `--keep_filename` للاحتفاظ باسم الملف الأصلي عند الترجمة
  - دعم الملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على الكود المضمن**: أصبحت علامات backticks ‏(`` `...` ``) محمية أثناء الترجمة
  - تحسين مطالبة النظام:
    - معالجة أفضل لعلامات الاقتباس في الواجهة الأمامية YAML
    - حماية متغيرات القوالب `{variable}`
    - منع ملاحظات المترجم غير المطلوبة
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** الميزات الجديدة:
  - دعم Google Gemini API للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI: ‏`gpt-5` (الجودة)، و`gpt-5-mini` (الاقتصادي)
    - Claude: ‏`claude-sonnet-4-5` (الجودة)، و`claude-haiku-4-5` (الاقتصادي)
    - Gemini: ‏`gemini-3-pro-preview` (الجودة)، و`gemini-3-flash-preview` (الاقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف منفرد (`--file`) من دون استعراض مجلد
  - نمط تسمية مبسط جديد: `{base}-{lang}.md`
  - خيار `--include_model` للاحتفاظ بالتنسيق القديم الذي يتضمن اسم النموذج
  - دعم النماذج غير المدرجة مع حد افتراضي للرموز (128k)
  - ترجمة README إلى 14 لغة
- **1.5** التحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين مطالبات الترجمة:**
    - جرى إثراء مطالبات الترجمات المباشرة وملاحظات الترجمة لتحقيق وضوح وكفاءة أفضل، بما في ذلك تعليمات مفصلة حول الحفاظ على البيانات الوصفية وعناصر التنسيق المحددة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم الاستيرادات لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص وإدارة كتل الكود للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات المخرجات:**
    - عكس ترتيب النموذج واللغة في اسم ملفات المخرجات (على سبيل المثال، `f"{base}-{args.target_lang}-{args.model}.md"`)، ما يسهّل تنظيم الترجمات والعثور عليها.
  - **تحسينات متنوعة:**
    - تنظيف الكود بحذف الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكربت وسهولة قراءته.
- **1.4** الميزات الجديدة:
  - دعم Claude API من Anthropic للترجمة
  - تحسين المطالبات لزيادة الوضوح والكفاءة
  - تعديلات طفيفة لتحسين قابلية صيانة الكود
- **1.3** التحسينات والميزات الجديدة:
  - تحسين إدارة كتل الكود
  - تحسين إدارة ملفات المخرجات
  - تحسين اكتشاف الملفات الموجودة
  - خيار `--force` لفرض الترجمة
  - عكس ترتيب النموذج واللغة في اسم ملف المخرجات
- **1.2** إصلاح سجل التغييرات
- **1.1** إضافة دعم Mistral AI API
- **1.0** الإصدار الأولي - دعم OpenAI API

**مقال مترجم من الفرنسية إلى العربية باستخدام gpt-5.6-sol.**
