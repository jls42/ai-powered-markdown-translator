### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.11.0** النشر على PyPI: `pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-08-31):

  - **أصبح السكربت أحادي الملف حزمة قابلة للتثبيت.** انتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول لوحدة التحكم `aipmt` وما يعادلها `python -m aipmt`. يظل استنساخ المستودع ضروريًا للمساهمة — إذ توجد فيه الاختبارات والترجمات الـ28 وأدوات الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` مطلقًا**، لأن التعارض حقيقي وصامت. فحزمة PyPI ‏`translate` (الإصدار v3.8.1، وآخر رفع في 2026-07-06) تثبّت دليلاً بالاسم نفسه. عند إعادة الإنتاج في venv: يتغلب الدليل على الوحدة، ويختفي `translate.main`، وتتعطل نقطة الدخول عند `AttributeError` — بينما يجيب `pip check` بعبارة «لم تُعثر على متطلبات مكسورة» مع rc=0. كان من الممكن أن يؤدي `pip install translate` بسيط لدى المستخدم إلى تعطيل CLI من دون تشخيص مفيد. واختبار مضاد على wheel الحقيقي: تنفيذ `pip install translate` فوق الحزمة، مع بقاء `aipmt --help` بقيمة rc=0 قبل ذلك وبعده، وتعايش واجهتي CLI.

    - **اسم توزيع طويل، وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للعثور عليها عبر بحث PyPI؛ أما استخدام اختصار وحده فسيجعل العثور عليها مستحيلاً لمن لا يعرف المشروع مسبقًا، في حين أن الهدف من النشر تحديدًا هو أن تكون قابلة للاكتشاف. استُبعد مرشحان محتملان بعد التحقق: `ai-markdown-translator` محجوز على npm منذ 2024 لأداة تؤدي الغرض نفسه، وتسبق هذا المستودع بـ17 شهرًا؛ و`aimt` لا يختلف عن `aim` (v3.29.1) إلا بحرف واحد، وهي حزمة نشطة في المجال نفسه — أسوأ إعداد لحدوث التباس مستمر. ومطب منهجي بالمناسبة: يعيد `pypi.org/project/<nom>/` الحالة 200 لأي اسم (صفحة لمكافحة الروبوتات)، ولا يُعتد إلا بواجهة JSON البرمجية.

    - **تخطيط `src/` بدلاً من حزمة مسطحة.** كانت الحزمة المسطحة ستحافظ على وحدات `sys.path.insert(..., "..")` الست الخاصة بالاختبارات، وهذا تحديدًا هو العيب: فهي تجعل الاستيراد يتم من شجرة المصدر بدلاً من الحزمة، ما كان سيخفي أي خطأ في التغليف. التكلفة الفعلية: قاعدة استبدال إضافية.

  - **إصلاح — كان `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** لا يصعد `load_dotenv()` من دون وسيطة بدءًا من الدليل الحالي، بل من الملف المُستدعي، أي من `site-packages`. وبالقياس باستخدام نقطة دخول حقيقية لوحدة التحكم، أُطلقت من مشروع يملك ملف `.env` الخاص به: يعيد `find_dotenv()` القيمة `''` ولا تُحمّل القيمة، بينما يعثر عليها `find_dotenv(usecwd=True)`. لم يكن الخطأ موجودًا ما دامت الأداة تعمل من المستودع المستنسخ فقط؛ لكن النشر كان سيجعله منهجيًا، ولا يتمثل عَرَضه الوحيد إلا في ظهور مفتاح API «مفقود» رغم صحة الإعداد.

  - **كانت ثلاث بوابات ستمر إلى الحالة الخضراء بعد أن توقفت عن التحقق من أي شيء.** جرى تشديدها قبل النقل، عمدًا: فحاجز يُكتب بعد التغيير الذي يُفترض أن يكتشفه لا يثبت شيئًا. كل واحدة خضراء على المستودع الأصلي وتتحول إلى حمراء على نسخة منقولة — وقد قيس الاتجاهان.

    - **يتجاهل Lizard مسارًا مفقودًا من دون قول شيء**: rc=0، و«تم تحليل 0 ملف». كانت بوابة التعقيد ستنتقل من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع خرج حجمه صفر بايت. أصبح النطاق الآن مصفوفة يُتحقق من وجود كل إدخال فيها.

    - **لا يفشل `coverage run --source=` عند غياب وحدة**: تحذير على stderr فقط، وrc=0 لكل من unittest و`coverage xml`، مع نشر التقرير رغم ذلك — مبتورًا من 1453 إلى 141 عبارة. كان المشروع سيبدو سليمًا لأنه لم يعد يُحلَّل تقريبًا. وتحافظ حدّان أدنى على التقرير: الإجمالي، وأكبر ملف جرى قياسه.

    - **مسبار حداثة الترجمات أعمى بنيويًا عن صيغة الاستدعاء**: فهو يرتكز على أعلام argparse، وهي بالضبط ما لا يغيره إعادة تسمية ملف. أُعيد إنتاج الحالة: نُقلت الوحدة، بينما ظلت ملفات README الـ15 توثق أمرًا غير موجود، فكانت النتيجة «لا توجد ترجمة قديمة». لذلك يتحقق قسم سابع من **الصيغة** لا من الخيارات، كما تُقارن إضافة Lizard بنطاق السكربت الفعلي — ومفتاحها `files:` لا يجعل pre-commit يفشل عندما لا يعود يطابق شيئًا، بل يجعله **يتخطى** الخطوة.

  - **لم يعد `requires-python = ">=3.10"` مجرد ادعاء.** كان `sonar-project.properties` يعلن مسبقًا دعم 3.10-3.12 من دون أن يختبره أي شيء، لأن بيئة التطوير لم تكن تحتوي إلا على 3.12 — وهو تناقض داخلي كان النشر سيكشفه علنًا. يشغّل سير عمل الاختبارات الآن المجموعة على 3.10 و3.11 و3.12، مع تثبيت **الحزمة** وبالتالي حدودها العامة.

  - **حدود دنيا، من دون حد أقصى.** يبقى `requirements.txt` هو القفل المختبَر، بينما يصبح `[project.dependencies]` هو العقد العام: فنشر الإصدارات الدقيقة للقفل سيتسبب في تعارضات لدى أي مستخدم لديه حزم أخرى. ولا يوجد حد أقصى لـ`<N+1` أيضًا — إذ سيتعارض مباشرة مع `check-deps-fresh.sh`، الذي يجعل بوابة الإصدار تفشل عند أي تأخر في الإصدار الرئيسي. تحل مجموعة الحدود الدنيا المشكلة، ويخرج الاختبار المضاد `openai==1.0.0` بنتيجة `ResolutionImpossible`، ما يثبت أن الفحص يميز الحالات بدلاً من قبول كل شيء. كما يمنع حاجز آخر إصدار `pyproject.toml` من الانحراف عن إصدار CHANGELOG: إذ لا يسمح PyPI بإعادة استخدام رقم.

  - **جرى التحقق من البداية إلى النهاية في venv جديد**: wheel حجمه 69 768 بايت ولا يحتوي إلا على `aipmt/*.py` وdist-info والترخيص؛ و`aipmt --help` بقيمة rc=0 مع 22 علمًا؛ و`python -m aipmt` يعرض «الاستخدام: aipmt» لا «الاستخدام: \_\_main\_\_.py»؛ وتثبيت `pipx` يعمل؛ والأهم **ترجمة فعلية من fr إلى en من أي دليل مستخدم**، مع الحفاظ على التنسيق العريض والقائمة والكود المضمّن والرابط وعنوان URL، ومن دون ترجمة كتلة الكود. نجحت الاختبارات الـ318 مع قائمة معرفات مطابقة حرفيًا للبايت قبل النقل وبعده — وهذا ما يثبت عدم تعطيل أي اختبار، لا عبارة «OK».

- **1.10.0** موفر `--use_codex` (حصة اشتراك ChatGPT)، تحديث SDKs والنماذج، وإصلاح اقتباسات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — حاجزان وضعتهما PR من دون تطبيقهما في كل مكان**:

    - **كان preflight الخاص بـCodex يمرر كامل `.env` إلى الملف الثنائي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: لذلك كانت العملية الفرعية ترث كامل `os.environ`، أي كامل `.env` المحمّل بواسطة `load_dotenv`. وبالقياس باستخدام ملف ثنائي زائف مُجهز للتسجيل: وصلت **سبعة أسرار** إلى preflight — مفاتيح الموفرين الستة إضافة إلى `GITHUB_TOKEN` — مقابل **صفر** لنظيره `_grok_preflight`، الذي كان يمرر `env=_grok_env()` بصورة صحيحة. كان التناقض داخليًا في PR: إذ يوجد `_strip_secret_env` تحديدًا للحفاظ على هذا الثابت، على بُعد بضعة أسطر. استُخرج `_codex_env_base()` وأُعيد استخدامه في المسارين؛ وبعد الإصلاح أظهر القياس: صفر من الأسرار في الجانبين.

    - **لم تكن خاصية «`--deny` fail-closed» تغطي الصيغة المستخدمة.** كان التعليق يبرر كامل عزل Grok بحقيقة أن قاعدة ذات بادئة غير معروفة ترفض بدء التشغيل. لكن القياس على `grok 1.0.13` أظهر أن هذا التحقق موجود **للصيغة الموضوعة بين قوسين فقط**: يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل («بادئة أداة غير معروفة»)، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. في حين أن `GROK_DENY_RULES` لم يكن يستخدم إلا أسماء مجردة — وبالتالي كان إعادة تسمية أداة من جانب xAI ستزيل طبقة العزل الوحيدة التي جرى قياسها من دون أي إشارة، على جهاز لا ينطبق فيه عزل نظام التشغيل أصلًا. تمر القواعد الثماني المسماة الآن في `Prefix(*)`، بعد التحقق من كل واحدة منها باعتبارها بادئة معروفة لـCLI؛ بينما يبقى catch-all `*` بصيغته الحرفية، وهي الصيغة المقبولة الوحيدة. ويمنع اختبار العودة إلى الصيغة غير المتحقق منها.

    - **جرى التحقق بصورة سليمة في الجوانب الأخرى**: لا يوجد حقن أوامر (صيغة قائمة في كل مكان، ولا استخدام لـ`shell=True` مطلقًا، ومحتوى المستند عبر stdin أو `--prompt-file`)، ولا إلغاء تسلسل غير آمن (استخدام `json.loads` فقط، مع حارس نوع)، وإصلاح لاجتياز المسار من دون العثور على تجاوز عبر سبع حمولات، كما أن `--deny '*'` مطبق فعليًا بواسطة CLI (رُصد `DENY_ENFORCED` عند قراءة خارج workdir).

    - كان فحص حداثة الترجمات المضاف أعلاه يتجاوز مبدءه الخاص أثناء ذلك: فالحزمة التي يفشل طلب PyPI الخاص بها كانت تُتخطى بصمت، فتظل البوابة خضراء. وهو يحصي الآن الحزم التي قورنت فعليًا ويفشل إذا كانت التغطية غير مكتملة.

  - **تحديث التبعيات، وإضافة شبكتَي أمان لمنع تكرار التأخر**:

    - **كان التأخر حقيقيًا ومستمرًا**: انتقل `openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر عامين في مخزن شهادات الجذر الذي يتحقق من TLS لجميع استدعاءات الموفرين. والسبب المحدد هو **عدم وجود `.github/dependabot.yml`**. فمن دون هذا الملف، لا يفعّل GitHub إلا _security updates_، ولا يقترح Dependabot طلب PR إلا لتبعية مستهدفة بواسطة CVE — وهو ما يفسر تحديثه لـ`urllib3` و`idna` مع ترك SDKs اثنين يتأخران عن إصدار رئيسي.

    - **تتعايش الإصداران الرئيسيان من دون تعارض**، خلافًا لما أوحى به استدلال سابق: إذ ينتقل `openai` 3.x و`anthropic` 1.x إلى **`httpx2`**، بينما يظل `mistralai` و`google-genai` على `httpx<1`، لكنهما توزيعتان منفصلتان. جرى التحقق بالتثبيت الفعلي، ثم عبر **مسارات الموفرين السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الكود المضمّن والروابط في كل خرج. كان «تجنب مكدسين HTTP» تفضيلًا لا عائقًا: وقد حسم القياس الأمر.

    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: فقد كان `google-auth` و`cryptography` ومكدس `opentelemetry` مثبتًا في venv العمل من دون التصريح بها، ولذلك لم يكن التثبيت الجديد يعيد إنتاج ما يجري اختباره. وعلى العكس، كانت `tokenizers` و`huggingface-hub` و`PyYAML` موجودة من دون استيرادها أو طلبها من أي شيء، وهي بقايا من `mistralai` 1.x. أُعيد توليد الملف باعتباره الإغلاق الكامل لـvenv مبنيًا من التبعيات المباشرة وحدها. ولا يُظهر `pip-audit` أي ثغرة معروفة في المجموعة الجديدة.

    - يفعّل **`.github/dependabot.yml`** (جديد) تحديثات الإصدارات الأسبوعية، وpip، وgithub-actions. تُجمع التحديثات الثانوية والإصلاحية في PR واحدة — إذ ينتهي الأمر بتجاهل طلب PR لكل تحديث تصحيحي، كما أن الضجيج عدو التحديث؛ أما **الإصدارات الرئيسية فتُفصل**، ويتطلب كل منها تحققًا عبر استدعاء فعلي.

    - يجعل **`scripts/check-deps-fresh.sh`** (جديد، موصول بالبوابة) التأخر ظاهرًا في نتيجة المشروع: يقترح Dependabot ولا يضمن، وقد تتراكم طلبات PR الخاصة به. التأخر في إصدار رئيسي → فشل؛ والتأخر في إصدار ثانوي → تحذير، لأن البوابة الحمراء دائمًا ينتهي بها الأمر إلى التجاهل؛ وتعذر الوصول إلى PyPI → تخطٍ صريح محليًا و**فشل مغلق في CI**، إذ إن الفحص غير المنفذ ليس نجاحًا. جرى التحقق في الاتجاهين: فهو يلتقط الحالة الدقيقة السابقة للإصلاح (`openai 2.54.0→3.6.0`، `certifi 2024.8.30→2026.7.22`) ويكتفي بالتحذير بشأن إصدار ثانوي.

  - **إصلاحات ناتجة عن مراجعة هذا الـPR** — فحص خمسة وكلاء مراجعة الفروقات بدقة؛ وقد **أُعيد إنتاج جميع النقاط أدناه بالقياس** قبل تصحيحها، وكان اثنان منها تراجعين أُدخلا في وقت سابق ضمن الإصدار نفسه.
- **تم إصلاح تراجع — كان لدى `_NEWS_CITATION_REGEX` تراجع أُسّي.** أدى الإصلاح متعدد الفقرات إلى إدخال `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: مشاركة المسافات بين `[ \t]+` و`.*` ملتبسة، ويتضاعف هذا الالتباس من تكرار إلى آخر. عند قياسه على أسطر `>   texte` — وهي مسافة بادئة Markdown قانونية تمامًا — لا تطابق النمط: **2589 مللي ثانية لـ14 سطرًا** مقابل 0.04 مللي ثانية بعد التصحيح، بعامل يقارب 9 مع كل سطر مضاف. في وضع `--news`، كان يكفي اقتباس كتلي طويل وغير مطابق لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب قابل للتحديد. يستهلك التكرار الآن السطر بأكمله دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يبقى سوى طريقة واحدة للمطابقة في كل تكرار. تم التحقق على المجموعة الفعلية المكوّنة من 231 مقالة: **لا اختلاف في الاستخراج**، والاقتباسات الـ423 نفسها، مع استمرار توسيع الأجسام متعددة الفقرات الـ14.
    - **كان وجود علامتي provider في الوقت نفسه يؤدي إلى احتساب الاستخدام بصمت.** كان `--use_codex --use_mistral` مقبولًا؛ إذ يختبر `_select_provider_client` Mistral أولًا، بينما يعطي `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. لذلك كان المستخدم يطلب حصته ضمن الاشتراك ويحصل على فوترة حسب الاستخدام، من دون أي تحذير: وهو بالضبط نمط الفشل الذي وُجد `--use_codex` لمنعه. تمر أعلام provider الستة الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: إن سطر أوامر يجمع providerين، وكان مقبولًا حتى الآن بصمت، يفشل الآن عند `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة نهاية العمل تتحول إلى الأخضر عندما تفشل مجساتها.** كانت أربع من عمليات التحقق الثلاث عشرة في `scripts/check-release-ready.sh` تتبع نمط «التقاط stdout والاستنتاج إذا كان فارغًا» من دون فحص رمز الإرجاع: إذ تكتب الاستثناءات (مثل إعادة تسمية ملف، `FileNotFoundError`) إلى stderr، وتترك stdout فارغًا، فتستنتج عملية التحقق «لا شيء للإبلاغ عنه». وقد أُعيد إنتاج فخ «إن `exit 0` لا يثبت شيئًا» داخل السكربت المكتوب لمنعه. يفرض helper باسم `probe()` الآن رمز إرجاع صفريًا **وعلامة نهاية**، كما ترفض المجسات الاستنتاج من مجموعة فارغة من العلامات — إذ إن assertion على مجموعة فارغة تكون صحيحة دائمًا. البرهان: أدت إضافة المجموعة الحصرية أعلاه إلى تمرير أعلام provider عبر كائن `*_group`، لم تعد المطابقة القديمة `parser\.add_argument\(` تطابقه؛ فخرجت **ستة أعلام من أصل واحد وعشرين** من النطاق بصمت، بينما بقيت البوابة خضراء.
    - **كان فحص الأسرار يفوّت أربعة provider من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: إذ كان `sk-proj-…` (صيغة OpenAI الحالية) و`sk-ant-api03-…` يفشلان عند شرطتهما الثانية، كما لم يكن `AIza…` مشمولًا. تم توسيع النمط، واستُبعد `.secrets.baseline` من الفحص. وكانت الحماية `.env` تستعلم كذلك من `git diff --cached`، الذي لا يرى سوى الفهرس: لذلك لم يكن `.env` **المُرسَل إلى المستودع مسبقًا** — وهو أسوأ الحالات — يظهر فيه قط. وهي تستعلم الآن من `git ls-files`.
    - **لم يكن «الإحماء المسبق للرمز» في Codex إحماءً فعليًا.** أظهر القياس أن `codex login status` لا يلمس `~/.codex/auth.json` (فوقت التعديل والحجم لا يتغيران)، كما أن مساعدته تقول «إظهار حالة تسجيل الدخول». ومع ذلك، كان التعليق يدّعي تحديث الرمز «مرة واحدة، بالتتابع»، وبالتالي تحييد خطر التحديثات المتزامنة لرمز دوّار ذي استخدام واحد. لم تكن الحماية المعلنة موجودة؛ وأصبح التعليق الآن يصف ما يفعله الكود، بينما تظل المعالجة الحقيقية هي `max_jobs=4`. كما أن الفحص يحترم `CODEX_BIN`، الذي كان يتجاهله — إذ كان جهاز بلا `codex` في `PATH` يفشل برسالة «غير موثّق»، وهي نتيجة مضللة.
    - **كان يتم تحميل `.env` من shell فرعي.** يُستدعى `detect_provider` ضمن استبدال أمر، ولذلك لم تكن export الخاصة به تصعد إلى البيئة الأعلى: فكان `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` المعرّف في `.env` يظل غير مرئي للقراءات المنفذة في `main()`، التي تستنتج «تعذر العثور على Grok binary» رغم صحة الإعداد.
    - **كانت التزامنية تتجاوز الحد المعلن بنسبة 50٪.** وُضعت الحماية بعد إطلاق زوج README/CHANGELOG، فبلغت الذروة المقاسة **3 لـ`max_jobs=2`**. وفي Grok، الذي تُشارك حصته الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يكن الحد الذي يفرضه السكربت ملتزمًا به. أما العد النهائي فكان يُعرض من دون مقارنته بـ28 — فكان مرور ملف مفقود من دون ملاحظة.
    - **عقد مخرجات Grok: غياب `stopReason` أصبح فشلًا.** كان الكود يطبق «`end_turn` **أو غائب**» بينما يفرض العقد المعلن `end_turn`. وكانت حمولة بلا الحقل — أو بحقل أعادت تحديثات CLI تسميته — تحول الحماية إلى عملية لا تفعل شيئًا بصمت. إضافة إلى ذلك، لم يعد `max_turn_requests` مصنفًا كحد لمعدل الطلبات (فالذي نفد هو ميزانية الجولات: إعادة المحاولة تعيد النتيجة نفسها، مقابل انتظار 90 ثانية)، كما خرج `quota` من علامات حد معدل الطلبات — للسبب الذي كان docstring الخاص بـ`_codex_is_rate_limited` يذكره أصلًا من دون أن يطبقه Grok.
    - **أصبحت سلسلة Gemini محفوظة مؤقتًا حسب النموذج.** كانت تبدأ من `minimal` عند كل مقطع، رغم أن النموذج الافتراضي يرفضه: فكان المسار الاعتيادي يدفع تكلفة طلب 400 ذهابًا وإيابًا لكل مقطع، ويعيد طباعة التحذير نفسه. والتحذير المتكرر مئات المرات يتوقف عن أن يكون مقروءًا — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مضمّنة لمصلحة Codex، وتوجّه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ كما كان تعليق طبقة العمليات الفرعية يعمم «shim» على CLIين، مع أن Grok binary هو ELF أصلي (والتعليل الصحيح هو «agent ينشئ عمليات فرعية خاصة به»)؛ وتم وضع اثنتي عشرة نتيجة SAST على `subprocess` في حالة `# nosec` / `# nosemgrep` مع التعليل، إذ تجعل صيغة القائمة من دون `shell=True` الحقن مستحيلًا، كما أن محتوى المستند لا يمر مطلقًا عبر argv.
    - **لم يعد أي سر يدخل إلى العمليات الفرعية الوكيلة.** كانت قوائم المنع الاسمية تحمي فقط ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). أظهر القياس أن **سبعة أسرار أخرى** كانت تدخل رغم ذلك إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح CLI الآخر، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة المرور. وهذان CLI هما **agent**، كما أن Grok يعمل من دون sandbox لنظام التشغيل يمكن تطبيقه على كثير من أجهزة Linux. يجري الفلترة الآن **حسب نمط الاسم** (`API_KEY` و`_TOKEN` و`SECRET` و`PASSWORD` و`CREDENTIALS`) بدلًا من القائمة الاسمية، ما يغطي أيضًا المتغيرات التي يضيفها المستخدم في `.env` من دون أن يعرفها هذا الكود. ولا يلزم أي منها لـCLI: فالمصادقة موجودة في `~/.codex` و`~/.grok`، لا في البيئة مطلقًا — وقد تم التحقق من ذلك عبر **ترجمة فعلية مكتملة** باستخدام كل provider مع البيئة المقوّاة.
    - **الاختبارات**: ملف جديد باسم `tests/test_review_hardening.py` (21 اختبارًا) يثبت حصرية أعلام provider، وعقد `stopReason`، وخطية regex الخاصة بـnews، ورسالة الرفض في CI، وحفظ Gemini المؤقت، وغياب أي سر من بيئة العمليات الفرعية. هذا assertion الأخير **عام** — فهو يفشل عند وجود مفتاح لا تسميه أي قائمة — بينما كان اختبار التنقيح الموجود مرآةً لثابته، ولم يكن يستطيع اكتشاف أي شيء سوى تعطل حلقته الخاصة. أصبحت المجموعة الكاملة **311 اختبارًا**.

  - **موفّرا Grok جديدان**: `--use_grok` (واجهة xAI البرمجية، والمفتاح `XAI_API_KEY`، مع فوترة حسب الاستخدام) و`--use_grok_cli` (CLI الرسمي لـGrok Build، ويُخصم من اشتراك Grok — بالمبدأ نفسه مثل `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن نقطة نهاية xAI متوافقة مع OpenAI، يُعاد استخدام العميل و`_call_openai` كما هما، ولا يتغير سوى `base_url`. وكان التكييف الوحيد المطلوب مفيدًا للجميع: إذ يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي تصدرها xAI بينما تصدر OpenAI `stop`. النماذج: `grok-4.6` (للجودة) و`grok-4.3` (للاقتصاد). تجدر الإشارة إلى أن نموذج Grok الاقتصادي يظل الأغلى في المستودع — $1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ`mistral-small-latest`؛ لذلك يُختار هذا provider لتنوع النموذج، لا للسعر.
    - **وضع CLI**: مبني على Codex، مع أربعة اختلافات فرضها الواقع — يُمرر prompt عبر ملف (`--prompt-file`، إذ لا يقرأ CLI من stdin، كما أن وجود مقطع في argv سيظهر في `ps`)، والمخرج كائن JSON واحد على stdout (لا JSONL ولا ملف `-o`)، والاشتراك لا يكشف سوى `grok-4.6` و`grok-4.5`، ولا يمكن تطبيق sandbox (انظر أدناه). تم توحيد إطلاق العمليات الفرعية مع Codex في `_codex_run_process`، من دون المساس ببقية provider Codex المختبَر.
    - **`exit 0` لا يثبت شيئًا، وفق القياس**: عندما يكون CLI غير موثّق، يكتب `{"type":"error","message":"Not signed in."}` على **stdout** مع رمز إرجاع **0**. ويتصرف الرفض أو تجاوز عدد الجولات بالطريقة نفسها. لذلك يشترط عقد المخرجات أربعة شروط متزامنة: رمز إرجاع 0، وغياب حمولة خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع الفحص المسبق المنطق نفسه: إذ يُخرج `grok models` الرمز 0 حتى عند انقطاع الاتصال، ولا يمكن الاستنتاج إلا من وجود «not authenticated» في stdout.
    - **الاحتواء: عدم تماثل مقصود وموثّق.** بينما يعمل Codex في `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـGrok على كثير من أجهزة Linux الحديثة لسببين نظاميين مستقلين لا يمكن تجاوزهما من دون `sudo`: إذ يحظر AppArmor مساحات أسماء المستخدمين غير المميّزة منذ Ubuntu 24.04 (`bwrap: setting up uid map: Permission denied`، وقد أُعيد إنتاج ذلك خارج Grok)، كما تفشل قائمة منع مقابس تشغيل الحاويات عندما يكون `/run/podman` في `0700` (ولا يستدرك resolver إلا `ErrorKind::NotFound`، فيصبح EACCES قاتلًا). الفخ المركزي: إن ملفًا شخصيًا **مدمجًا** لا يمكن تطبيقه يؤدي إلى **بدء التشغيل من دون احتواء بصمت**. لذلك لا يطلب السكربت أي ملف شخصي افتراضيًا ولا يتراجع بصمت مطلقًا — بل يحذر على stderr. وتعتمد الحماية على قواعد `--deny` الخاصة بـCLI، بما فيها catch-all `*`، وهي الطبقة الوحيدة المقاسة ذات السلوك _fail-closed_ (إذ تؤدي قاعدة ذات بادئة غير معروفة إلى رفض بدء التشغيل). ويتيح `GROK_TRANSLATE_SANDBOX=read-only` فرض ذلك، وعندها يفشل بدء التشغيل إذا تعذر على الجهاز الوفاء به.
    - **وسائل الحماية**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (إذ قد يحول مفتاح واحد الفوترة إلى فوترة حسب الاستخدام؛ كما أن `GROK_SANDBOX` موروثًا سيفرض ملفًا شخصيًا غير قابل للتطبيق مع رسالة مضللة)، وتُعطّل مفاتيح MCP/hooks/skills/agents، و`--disable-web-search` و`--no-subagents` و`--no-plan`، مع workdir مؤقت، ورفض في CI، وtimeout يقتل مجموعة العمليات، وback-off عند حد معدل الطلبات. يُضبط `--max-turns` على 6 لا على 1: إذ يُزاد العداد بعد جولة الأدوات، والقيمة 1 ستقتطع المخرج.
    - **الحصة**: تجمع Grok أسبوعية و**مشتركة مع Chat وImagine وVoice**، ولا تكشفها أي تعليمة — خلافًا لـCodex، الذي يتيح `account/rateLimits/read` حساب الاستهلاك. لذلك يحد `regen_translations.sh` التزامن إلى 2 ويحذر صراحة.
    - **الاختبارات**: ملف جديد باسم `tests/test_grok_provider.py` (24 اختبارًا). أصبحت المجموعة الكاملة **290 اختبارًا**.
  - **إصلاح خلل — لم تكن الاقتباسات الإنجليزية متعددة الفقرات محمية إلا جزئيًا (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل، كجسم للاقتباس، إلا سلسلة من أسطر `>` **المتتالية**. وبمجرد أن يغطي الاقتباس عدة فقرات (تفصل بينها سطر `>` فارغ)، كان لا يُلتقط ويُستبدل بالعنصر النائب إلا الجزء الأخير؛ أما الأجزاء السابقة فكانت تذهب إلى LLM وتعود مترجمة — وهو عكس ما وُجد `--news` لضمانه تمامًا. يقبل التكرار الآن أسطر `>` الفارغة داخليًا ويصبح غير جشع، ليتوقف عند `>` الفارغ الذي يسبق السطر المائل، لا عند أول سطر يواجهه.
    - **الحجم المقاس** على مجموعة فعلية من 198 مقالة: 11 اقتباسًا معنيًا من أصل 419. لا تراجع — تلتقط regex الجديدة العدد نفسه تمامًا من الاقتباسات، ولا تتمدد إلا الأجسام متعددة الفقرات (408 أجسام مطابقة، و11 ممتدة)، كما لا يمكن لسطر الإسناد `> — …` أن يُمتص داخل الجسم (مع الحفاظ على lookahead).
    - **دليل شامل من البداية إلى النهاية** على مقالة حجمها 69 كيلوبايت تُرجمت إلى ja/ar: الفقرة الأولى من اقتباس، التي كانت تُخرج سابقًا بصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم بالطريقة نفسها إلى العربية، تبقى الآن بصيغة `> GLM-5.3 is now open-weight.`. ويعود عدد أسطر الاقتباس بالإنجليزية من 9 إلى 10، أي إلى عدد أسطر المصدر.
    - تجدر الإشارة إلى أن هذا الخلل لم تكتشفه أدوات التحقق اللاحقة، إذ تتحقق من وجود الاقتباس من دون التحقق من اكتماله.
  - **توفير مقاس في provider الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ النموذج بـ`gpt-5`، بما في ذلك في `--eco`. أُجري القياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: `medium` ← 45 رمز reasoning و65 رمز مخرج؛ `none` ← 0 و14. لا يضيف reasoning شيئًا إلى الترجمة، وكان يُدفع ثمنه في كل مقطع من كل ملف. يصبح الافتراضي `none` في `--eco`، ويظل `medium` بخلاف ذلك؛ وتبقى القيمة الصريحة الممررة عبر CLI ذات أولوية. يقبل `--reasoning_effort` الآن `none` و`xhigh` إضافة إلى `low`/`medium`/`high` (ولا تقبل جميع النماذج هذه القيم: فمثلًا يرفض `minimal` القيمة `gpt-5.4-mini` — وتغطي إعادة المحاولة الحالية من دون معلمة هذه الحالة).
  - **تحديث SDK وترحيل Gemini**: استُبدل `google-generativeai` (انتهى دعمه في 2025-11-30، وأُرشف المستودع) بحزمة SDK الموحدة **`google-genai`** — `genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير system prompt في `system_instruction` بدلًا من ضمه إلى المقطع. انتقل `mistralai` إلى **2.9.4** (وأصبح الاستيراد `from mistralai.client import Mistral`؛ بينما يرفع القديم `ImportError`، وقد تم التحقق منه داخل wheel)، و`anthropic` إلى **0.125.0**، و`openai` إلى **2.54.0** — وهي آخر الإصدارات قبل الانتقال إلى `httpx2`، لتجنب تشغيل مجموعتي HTTP في venv نفسه. وتم فتح `httpx` 0.28.1 و`pydantic` 2.13.5 تبعًا لذلك.
  - **تراجعان كشفتهما اختبارات فعلية، لا الوثائق**:
    - يرفض `anthropic` ابتداءً من 1.0 من جهة العميل استدعاءً غير متدفق يترك `max_tokens` يوحي بأكثر من 10 دقائق (`ValueError: Streaming is required...`). لم تكن هذه الحماية موجودة في 0.34.2، وكانت تكسر أي استدعاء Claude مع `max_tokens=32768`. وتم الإصلاح عبر `timeout` صريح (`CLAUDE_TIMEOUT`، و900 ثانية افتراضيًا)، ما يتجنب التحول إلى streaming لاستدعاء لا نستفيد منه إلا من الاستجابة الكاملة.
    - لا يُقبل `thinking_level="minimal"` إلا في جزء من كتالوج Gemini: إذ يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` برمز 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهي سلسلة `minimal` → `low` → من دون thinking_config، على غرار fallback الخاص بـOpenAI الموجود مسبقًا — فلا ينبغي لمعلمة تحسين أن تتسبب مطلقًا في فشل ترجمة.
  - **تجديد النماذج الافتراضية**، مع التحقق من كل منها عبر استدعاء فعلي: OpenAI من `gpt-5.5` إلى **`gpt-5.6-terra`** (−60٪ على دفعة من 28)، ومن `gpt-5.4-mini` إلى **`gpt-5.6-luna`** (−73٪)؛ وClaude من `claude-sonnet-4-6` إلى **`claude-sonnet-5`** (أرخص وأحدث)، ومن `claude-haiku-4-5-20251001` إلى **`claude-haiku-4-5`** (المعرّف القياسي من دون تاريخ)؛ وGemini من `gemini-3.1-pro-preview` إلى **`gemini-3.7-flash`**، ومن `gemini-3.1-flash-lite-preview` إلى **`gemini-3.1-flash-lite`** (إصدار مستقر وأرخص من `3.5-flash-lite`).
أبقي Mistral كما هو، ويظل `mistral-large-latest` أفضل قيمة مقابل السعر من بين النماذج الأربعة. تجدر الإشارة إلى أنه لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — فلم يُطرح Gemini 3.5 Pro، الذي أُعلن عنه في مايو 2026؛ إذ إن سلسلة 3.5/3.6/3.7 تقتصر حصراً على Flash.
  - **قياس A/B قبل التحويل إلى Gemini**: تُرجمت `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. ظلّ الهيكل مطابقاً تماماً (21 قائمة، و18 كتلة تعليمات برمجية، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، في **8 ثوانٍ مقابل 48 ثانية**. وبما أنه لم يكن هناك أي معيار أداء عام يقارن بين هذين النموذجين في الترجمة أو في البرامج النصية غير اللاتينية، فقد كان التحويل سيستند بخلاف ذلك إلى مجرد افتراض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفّذ `block.text for block in response.content` من دون تصفية النوع. تُدرج نماذج الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، التي تعرض `.thinking` بدلاً من `.text` — وكانت الترجمة ستفشل عند مواجهة `AttributeError` معتمة في المقطع الأول. أصبحت كتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` تُستبعد الآن (قائمة استبعاد، للحفاظ على التسامح مع نوع غير معروف يحتوي على نص)، كما أن الاستجابة التي لا تحتوي على أي كتلة نصية ترفع خطأً صريحاً. ويُمرَّر `thinking={"type": "disabled"}` في كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: حذف النماذج التي انقضى تاريخ سحبها (سلسلة `magistral-*` المسحوبة في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620` و`claude-3-7-sonnet-20250219` و`claude-opus-4-1-20250805` و`claude-sonnet-4-20250514`). تصحيحات الحدود: Mistral 128K → **256K** (توليدا Large 3 / Small 4)، وGemini 1 000 000 → **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` من 200K → **1M**، وعائلة `gpt-5.6-*` من 400K → **1,05M**. أُضيف Claude 5 (`claude-sonnet-5` و`claude-opus-5` و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وسلسلة `ministral-*`. تجدر الإشارة إلى أن هذه الحدود تظل إرشادية، إذ يضع `translate()` حداً أقصى لتقسيم النص عند `min(16000, limite)`.

  - **المزوّد `--use_codex`**: المزوّد الخامس الذي يشغّل CLI الرسمي لـ Codex (`codex exec`) في الوضع غير التفاعلي، بدلاً من استدعاء API تُحتسب تكلفته بحسب الاستخدام. تُخصم الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقاً. وهذا هو المسار الوحيد الموثق من OpenAI لهذا الاستخدام: إذ تسرد مصفوفة التوفر حسب الخطة «Codex SDK, `codex exec`, and scriptable workflows» بوصفها متاحة على Plus/Pro/Business/Enterprise، في حين أن رموز `~/.codex/auth.json` لا تصادق على استدعاءات Platform API (ولا يقرأها هذا البرنامج مطلقاً — إذ تظل المصادقة وتجديدها مُدارَين بواسطة CLI).
  - **إمكانية تثبيت ملف Codex الثنائي عبر pip، وليس npm فقط**: يبحث `_resolve_codex_binary()` عن الملف الثنائي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** المنشورة من OpenAI (وهي تبعية SDK `openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عاماً لاستخدام `--use_codex`. ولا تُضاف الحزمة إلى `requirements.txt`؛ فالملف الثنائي حجمه نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين من أجل مزوّد اختياري. تم التحقق من المسار كاملاً: مع غياب `codex` من `PATH`، عثر الحل على الملف الثنائي المضمّن واكتملت ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: أُزيل `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. ومن دون هذا الحاجز، قد يتسبب مفتاح موجود في `.env` في تحويل Codex إلى الفوترة بحسب الاستخدام من دون أي إشارة مرئية — وهو بالضبط ما وُجد هذا المزوّد لتجنبه.
  - **إغلاق مخاطر CLI بالاختبارات**:
    - يقرأ `codex exec` الإدخال القياسي **حتى** عندما تكون المطالبة مُمرَّرة كوسيط: ومن دون إغلاق الإدخال القياسي، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج (أُعيد إنتاج الحالة: exit 124 بعد 180 ثانية، مع صفر بايت). لذلك يُعد `communicate(input=...)` إلزامياً.
    - إن `codex` المثبّت عبر npm ليس سوى shim لـ Node يقوم `spawn` الملف الثنائي الحقيقي المكتوب بـ Rust: وهذا الأخير **حفيد** عملية Python، ويبقى حياً بعد `SIGKILL` الخاص بـ `subprocess.run(timeout=)`، حيث سيواصل استهلاك الحصة. ومن هنا يأتي `Popen(start_new_session=True)` + `os.killpg`.
    - قد يخرج CLI بالرمز 0 رغم إصداره `turn.failed`: لذلك يُفحَص خرج JSONL (`--json`) بالإضافة إلى رمز الإرجاع، كما أن غياب الملف `-o` مع رمز 0 يرفع خطأً صريحاً بدلاً من إنتاج مقطع فارغ.
  - **التراجع عند بلوغ حد المعدل**: لا ينفّذ CLI أي إعادة محاولة داخلية (`max_retries = 0`). ويُجرى التصنيف اعتماداً على بنية حمولة JSON (`status: 429` / `error.type`)، لا على سلاسل فرعية — إذ تظهر كلمة «الحصة» في كل من استجابة 429 القابلة للاسترداد و`insufficient_quota` النهائية.
  - **حاجز CI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` محدداً. فالمصادقة بالاشتراك غير مخصصة لعامل تشغيل مشترك، كما توصي OpenAI صراحةً بعدم استخدام هذا المسار في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (للجودة) و`gpt-5.6-luna` (`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وPlatform API، لكن حساب ChatGPT لا يملك حق الوصول إلى جميع أفرادها: تُطبَّق قائمة السماح على الخادم من دون تحقق محلي، ويؤدي النموذج غير المعتاد إلى تحذير. في خطة Plus، يوفّر Luna من 250 إلى 2,000 رسالة لكل نافذة مدتها 5 ساعات مقابل 10-100 في Sol: ويُعد `--eco` الوضع الموصى به لأي معالجة دفعية.
  - **إصلاح خلل — كان `regen_translations.sh` يخرج بخطأ رغم اكتمال النجاح**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` من `main()` لم يعد موجوداً عند تنفيذ trap. في ظل `set -u`، كان ذلك يرفع `failed_log: unbound variable` ويُخرج البرنامج بالرمز 1 رغم صحة الترجمات الثماني والعشرين — ما كان سيوقف `release.sh --auto` (`set -e`) مباشرة بعد إعادة التوليد، في أكثر المراحل تكلفة. أصبح المتغير عاماً، ويتحقق trap من وجوده. ومن الآثار الجانبية المفيدة أن إخفاقات الترجمة الحقيقية، التي كانت مخفية حتى الآن بسبب هذا الخطأ، عادت للظهور في الملخص الختامي.
  - **`REGEN_MODEL`**: متغير بيئي جديد من `regen_translations.sh` يفرض نموذجاً محدداً فوق الإعداد الافتراضي للمزوّد، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج الأعلى جودة ضمن حصة الاشتراك بدلاً من نموذج `--eco` الموجه نحو حجم المعالجة.
  - **`regen_translations.sh`**: إتاحة `REGEN_PROVIDER=codex` عبر تفعيل صريح اختياري (ولا يُكتشف تلقائياً أبداً، لتجنب استهلاك حصة الاشتراك من دون علم المستخدم). يُجدَّد الرمز مرة واحدة وبشكل تسلسلي قبل فتح التوازي — إذ إن تجديد Codex دوّار ويُستخدم مرة واحدة، وكانت المهام المتزامنة ستبطل جلسة `codex login` — كما خُفّض التوازي إلى 4.
  - **إعادة هيكلة مرتبطة**: انتقل `_dispatch_provider_call` من 8 إلى 6 معاملات عبر `_resolve_provider()` يعيد اسم مزوّد، بدلاً من تمرير قيمة منطقية رابعة عبر السلسلة بأكملها. وتظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` أدنى.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` (48 اختباراً) يغطي argv، والبيئة المنقّحة، وعقد منع الاستهلال، والإخفاقات الصامتة، والمهلة/killpg، والتراجع، والفحص القبلي، وحل المزوّد، وتسلسل استدلال Gemini، وتصفية كتل Claude، واقتباسات الأخبار متعددة الفقرات. أصبح مجموع المجموعة الكاملة 290 اختباراً.
  - **تحقق فعلي**: إن `README.md` الخاص بالمشروع، عند ترجمته إلى **14 لغة** عبر Codex، يعطي بنية مطابقة تماماً لترجمات المرجع (14 كتلة تعليمات برمجية، و24 عنواناً، و25 صفاً في الجدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل تعليمات برمجية متطابقة حرفياً، ومن دون أي بقايا للمتغيرات النائبة). وفي مقالة إخبارية حجمها 69 كيلوبايت بوضع `--news`، اجتاز كل من المخرجين `gpt-5.6-luna` و`gpt-5.6-sol` أداة التحقق التطبيقية اللاحقة على en/ja/ar. وقد قيس الاستهلاك عبر `account/rateLimits/read`، وبقي دون عتبة تقريب العداد (0% من نافذة 5 ساعات) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL للإسناد الإخباري مع الأقواس المتداخلة أو البادئة الفرنسية (2026-05-11):

  - **الخلل المُصلح**: كان استخراج عناوين URL للإسناد في `_protect_news_quotes` يستخدم التعبير النمطي `re.search(r"\((.+?)\)", attribution)` (التقاطاً كسولاً بين قوسين). في إسناد من النوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` الخارجية + `]()` الخاصة برابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة وتضم البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائية). وكانت النتيجة أن `_validate_news_post` يبحث عن هذه السلسلة في الخرج المترجم ويفشل منهجياً (لسببين: `)` مبتور، وعبارة "relayé par" مترجمة إلى `relayed by`/`weitergeleitet von`/...). ولم يكن من الممكن أن يمر التسلسل الكامل low → medium → high → gpt-5.5.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — إذ يستهدف تحديداً `](url)` الخاص برابط Markdown، ويلتقط **عنوان URL الخام فقط** (من دون البادئة الفرنسية أو البتر)، مع الحفاظ على الثابت عبر المتغيرات النائبة `#URL{N}#` أثناء الترجمة. وهو متين أمام النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس خارجية
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py` ضمن الفئة `TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة المطابقة تماماً لخلل Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (متغير يحتوي على `via`)
  - **فجوة التغطية**: يتحقق `check-editorial-coverage.py` من الصياغة التحريرية، لكنه لا يتحقق من قابلية ترجمتها بواسطة المترجم. ومن التحسينات الممكنة (خارج نطاق v1.9.2) إضافة فحص يحاكي استخراج الإسناد في تشغيل تجريبي لاكتشاف الأنماط المعرضة للخطر **قبل** النشر.

- **1.9.1** إصلاح i18n لتسمية CTA في ملاحظة علامة الترجمة (2026-05-10):

  - **الخلل المُصلح**: ظلت تسمية `[Voir le projet sur GitHub ↗]` الخاصة برابط CTA في شريط العلامة أعلى الملفات المترجمة **بالفرنسية** لجميع اللغات المستهدفة، بدلاً من اتباع `target_lang`. ولا يراها LLM مطلقاً (إذ تُجمّع من جانب Python للحفاظ على عنوان URL وslug الخاص بالمستودع)، لذلك لم تتمكن مرحلة الترجمة من تصحيحها. وكان هذا تراجعاً صامتاً منذ إضافة تنسيق `marker` في الإصدار v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الخمس عشرة بتسميتها المحلية. وينقل `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` اللغة المستهدفة الآن. ويُستخدم `fr` كبديل عند عدم معرفة اللغة (لأسباب أمنية، ومنعاً لخطأ KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link` (من target_lang `ja` → التسمية اليابانية المتوقعة). واختباران جديدان: `test_source_link_label_localized_per_target_lang` (مُعَلَّم لسبع لغات تغطي الأبجديات اللاتينية والتصويرية والأبجدية) و`test_source_link_label_falls_back_to_french_for_unknown_target`. أصبح المجموع 40 اختباراً في `test_translation_note_position.py` (بدلاً من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع ذو قيمة افتراضية `target_lang="fr"` — يواصل المستدعون البرمجيون الخارجيون الذين لا يمررون `args.target_lang` العمل من دون تعديل.
- **1.9** إصلاح الفشل الصامت + مجموعة أدوات جودة كاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07) :
  - **ملاحظة ترجمة متعددة المواضع + تنسيق marker "embed card"** :
    - خيارات CLI جديدة (إضافية، مع بقاء القيم الافتراضية دون تغيير → **غير مسببة لكسر التوافق**) :
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): تضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`) :
        - `legacy` يعيد إنتاج سلوك v1.8 بدقة (فقرة عريضة `**…**`) **بايتًا ببايت**.
        - `marker` يصدر تعريف مرجع رابط Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`) يتبعه **اقتباس محظور من 3 فقرات** منظم لعرض يشبه "بطاقة تضمين مستودع GitHub": عنوان المشروع في كود مضمن (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف مترجم بواسطة LLM، ورابط CTA (`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. ويمكن استغلاله أثناء البناء بواسطة إضافة remark (راجع مدونة jls42.org → الإضافة `remark-translation-banner`).
    - **ثوابت لا تُرسل مطلقًا إلى LLM**: يُجمّع عنوان المستودع وعنوان URL الخاص بـ GitHub من جهة Python بعد ترجمة الجملة الوصفية. ولا يرى LLM مطلقًا المعرّف `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تعديل أي renderer أو فاصل أو scheme.
    - **إدراج واعٍ بالـ Frontmatter**: في نمط `top` أو `both`، تُدرج الملاحظة **بعد كتلة `---` الختامية** لـ frontmatter بصيغة YAML (لضمان الأمان مع Astro Content Collections / gray-matter). يكتشف المساعد `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف ويحافظ على سلامته؛ كما **يرفع `RuntimeError`** عند وجود frontmatter مفتوح من دون fence إغلاق (فيُرسل الملف إلى `failed_files` بدل كتابته مع ملاحظة في موضع خاطئ).
    - **Sanitizer للنموذج مع whitelist**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع استخدام `unknown` كقيمة احتياطية إذا أصبح فارغًا. ويتوافق ذلك مع المدقق الموجود في إضافة remark الخاصة بـ Astro ويحيّد المحارف التي قد تكسر تنسيق marker (المسافة، علامة الاقتباس، القوس، الفاصلة، وغيرها).
    - **إعادة هيكلة داخلية**: تحوّل `_append_translation_note` (دالة ضخمة واحدة) إلى 7 مساعدين نقيين (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فُصل الـ builder عن الـ composer (يعيد الـ builder كتلة نقية من دون فاصل، بينما يطبّق الـ composer قيم `\n\n` وفقًا للموضع)؛ وتشترك مرحلة الإنتاج والمساعد المصدر في نفس مُجمّع الفقرات الثلاث.
    - **`_quote_lines` مع الحفاظ على الأسطر الفارغة**: يسبق كل سطر بـ `> `، ويحوّل الأسطر الفارغة إلى `>` فقط. وهذا يسمح لـ mdast برؤية 3 فقرات منفصلة داخل الاقتباس المحظور (العنوان / الوصف / الرابط) بدلًا من فقرة واحدة تحتوي على فواصل أسطر.
    - **`_build_translation_note_block` تكيفي**: وفقًا لعدد الفقرات التي حافظ عليها LLM (3 = تنسيق البطاقة الكامل، 2 = عبارة + رابط، 1 = قيمة احتياطية). ولا تُغلّف القيمة الاحتياطية ذات الفقرة الواحدة بعد الآن بـ `**...**` عند اكتشاف رابط Markdown `](` (لأن عرض `<strong>` حول رابط هش).
    - **التوافق العكسي**: يظل `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` على جانب `_compose_with_notes` متوافقين؛ إذ تستمر Namespace التي لا تتضمن هذه السمات (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) في العمل من دون تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة** :
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور على مقتطف المصدر حرفيًا) + طبقة احتمالية (`langdetect`).
    - قائمة السماح `finish_reason` / `stop_reason`: رفع `RuntimeError` عند أي حالة خارج قائمة السماح (الاقتطاع، content_filter، وغير ذلك).
    - **Claude** `max_tokens`: من `4096` إلى `32768` (لتجنب الاقتطاع الكامن في مقاطع 16k، مع هامش عبر النصوص المختلفة FR→JA/ZH/KO/AR/HI).
    - تقسيم واعٍ بالعناوين: إعطاء الأولوية لـ H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي مكتمل).
    - تمرير الأخطاء حتى رمز خروج غير صفري: يعيد `translate_markdown_file` حالة نمطية `success` / `failure` / `skipped`، ويُرجع `main()` `sys.exit(1)` إذا فشل ملف واحد على الأقل (في وضعي الملف الواحد والدفعي).
    - حارس المحتوى الفارغ لدى جميع المزوّدين، ونسبة سلامة المصدر/الناتج (≥ 500 محرف، ورفض إذا كانت < 5%)، والتحقق من placeholders البرمجية (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، و`BadRequestError` إعادة المحاولة من دون `reasoning_effort`.
    - إضافة الاعتمادية `langdetect==1.0.9`.
  - **أدوات جودة pre-commit** ("نوع EurekAI الكامل"، 14 hook) :
    - Pre-commit: ‏ruff (lint+format)، وshellcheck، وprettier (md/yaml/json)، وdetect-secrets (4 مفاتيح API محمية)، وLizard (CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، EOF، الملفات الكبيرة، shebangs، وغيرها).
    - Pre-push: ‏mypy (وضع lax تدريجي)، وOpengrep SAST (translate.py + scripts/)، وpip-audit (وضع إعداد التقارير الأولي)، وunittest discover (tests/ + scripts/tests/).
    - أغلفة محلية داخل `scripts/` تستخدم `./venv/bin/python`.
    - `scripts/audit_verdict.py`: محلل JSON لـ pip-audit مع 11 اختبار unittest، وهو منفذ Python لمحلل jls42-astro.
    - إصلاح 7 مخالفات أولية في ruff: ‏B904 (raise from) ×2، وB007 (unused dirs)، وC408 (dict literal)، وC419 (list-comp)، وSIM105 (contextlib.suppress)، وSIM110 (any()).
    - يستثني Lizard مؤقتًا `translate.py` (4 دوال بقيمة CCN تتراوح بين 21 و47، مع إعادة هيكلة مخططة) — بوابة صارمة على scripts/.
  - **SonarCloud وتغطية شاملة** :
    - سير عمل GitHub Actions `SonarCloud` ‏(sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع التغطية عبر `coverage.xml`.
    - 11 شارة SonarCloud في أعلى README (Quality Gate، وتصنيفات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code).
    - `tests/test_silent_failure.py` ‏(`unittest` من stdlib): يغطي الوصلات الست لسلسلة أخطاء الفشل الصامت.
    - `tests/test_orchestration.py` ‏(+79 اختبارًا): يغطي طبقة تنسيق `translate.py` (`_resolve_*_filename`، `_existing_translation_exists`، `_record_translation_status`، `_write_output_file`، `translate_directory`، `_validate_input_paths`، `_init_*_client`، `_select_provider_client`، `_normalize_collapsed_markdown`، `_cleanup_source_flag`، `_validate_news_flags_*`، `_openai_create_with_fallback` من TypeError + بدائل BadRequestError، وتنسيق prompt لسلسلة o1، وفروع الإرجاع المبكر في `_validate_translation_output`).
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` (stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess.
    - **التغطية على الشيفرة الجديدة**: من 75.5% إلى نحو 98% (translate.py بنسبة 98% وscripts/audit_verdict.py بنسبة 97%).
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك E2E ‏`marker+top|bottom|both` و`legacy+top|bottom|both`)، والبادئة متعددة الأسطر، والتوافق العكسي بايتًا ببايت (golden literal)، وsanitizer، وفصل frontmatter (بما في ذلك الرفع عند عدم إغلاق fence)، وتنسيق الفقرات الثلاث، والقيمة الاحتياطية ذات الفقرتين، وحارس الفقرة الواحدة + رابط Markdown، وحاجزًا حرجًا `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان وعنوان URL لا يُرسلان مطلقًا إلى LLM. **نجح 190 اختبارًا**، من دون أي تراجع.
  - التوثيق: `README.md` (بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` (سير عمل pre-commit + مراقبة CI مفصلة)، وإعادة توليد 28 ترجمة.
- **1.8** نمط `--news` + تحديث نماذج 2026 (2026-03-17، الوسم `v1.8`) :
  - تحديث النماذج الافتراضية (مارس 2026) :
    - OpenAI للجودة: من `gpt-5` إلى `gpt-5.4`
    - OpenAI الاقتصادي: من `gpt-5-mini` إلى `gpt-5.4-mini`
    - Gemini للجودة: من `gemini-3-pro-preview` إلى `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` (400k) و`gemini-3.1-pro-preview` (1M).
  - النمط `--news` الأولي: حماية الاقتباسات EN باستخدام placeholders `#NEWSQUOTE\d+#`، وتعيين `LANG_FLAGS` (15 لغة)، وإدارة الأعلام حسب اللغة الهدف.
  - التحقق من placeholders الخاصة بالأخبار قبل الاستعادة (تراجع: كان LLM يحذف placeholder فينتج بصمت مخرجًا من دون اقتباس).
  - جعل السكربت `regen_translations.sh` قابلًا للنقل (مسارات مطلقة، من دون اعتماد على pwd).
  - إضافة رابط Français إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة.
- **1.7** المستجدات :
  - خيار `--keep_filename` للاحتفاظ باسم الملف الأصلي أثناء الترجمة.
  - دعم الملف `.env` لتحميل مفاتيح API تلقائيًا.
  - **الحفاظ على الكود المضمن**: أصبحت backticks (`` `...` ``) محمية الآن أثناء الترجمة.
  - تحسين prompt النظام :
    - إدارة أفضل لعلامات الاقتباس في YAML frontmatter.
    - حماية متغيرات القالب `{variable}`.
    - منع ملاحظات المترجم غير المطلوبة.
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org).
- **1.6** المستجدات :
  - دعم Google Gemini API للترجمة (`--use_gemini`).
  - تحديث النماذج الافتراضية لعام 2026 :
    - OpenAI: ‏`gpt-5` (الجودة)، و`gpt-5-mini` (الاقتصادي).
    - Claude: ‏`claude-sonnet-4-5` (الجودة)، و`claude-haiku-4-5` (الاقتصادي).
    - Gemini: ‏`gemini-3-pro-preview` (الجودة)، و`gemini-3-flash-preview` (الاقتصادي).
  - النمط الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة.
  - ترجمة ملف واحد (`--file`) من دون استعراض مجلد.
  - نمط تسمية جديد ومبسّط: `{base}-{lang}.md`.
  - خيار `--include_model` للاحتفاظ بالتنسيق القديم الذي يتضمن اسم النموذج.
  - دعم النماذج غير المدرجة مع حد رموز افتراضي (128k).
  - ترجمة README إلى 14 لغة.
- **1.5** التحسينات :
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** تحديث `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** تحديث `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` وتحديث `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين prompts الترجمة:**
    - أُغنيت prompts للترجمات المباشرة ولملاحظات الترجمة من أجل وضوح وكفاءة أفضل، بما في ذلك تعليمات مفصلة للحفاظ على metadata وعناصر التنسيق الخاصة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم imports لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص وإدارة كتل الكود للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات الإخراج:**
    - عكس النموذج واللغة في أسماء ملفات الإخراج (مثل `f"{base}-{args.target_lang}-{args.model}.md"`)، ما يسهّل تنظيم الترجمات والعثور عليها.
  - **تحسينات متنوعة:**
    - تنظيف الكود بحذف الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكربت وسهولة قراءته.
- **1.4** المستجدات :
  - دعم Claude API من Anthropic للترجمة.
  - تحسين prompts لزيادة الوضوح والكفاءة.
  - تعديلات طفيفة لتحسين صيانة الكود.
- **1.3** التحسينات والميزات الجديدة :
  - تحسين إدارة كتل الكود.
  - تحسين إدارة ملفات الإخراج.
  - تحسين اكتشاف الملفات الموجودة.
  - خيار `--force` لفرض الترجمة.
  - عكس النموذج واللغة في اسم ملف الإخراج.
- **1.2** إصلاح changelog.
- **1.1** إضافة دعم Mistral AI API.
- **1.0** الإصدار الأولي - دعم OpenAI API.

**مقالة مترجمة من الفرنسية إلى العربية باستخدام gpt-5.6-luna.**
