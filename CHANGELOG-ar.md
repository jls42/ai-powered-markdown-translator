### سجل التغييرات

🌍 [Français](CHANGELOG.md) | [English](CHANGELOG-en.md) | [Español](CHANGELOG-es.md) | [中文](CHANGELOG-zh.md) | [Deutsch](CHANGELOG-de.md) | [日本語](CHANGELOG-ja.md) | [한국어](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [हिन्दी](CHANGELOG-hi.md) | [Italiano](CHANGELOG-it.md) | [Nederlands](CHANGELOG-nl.md) | [Polski](CHANGELOG-pl.md) | [Português](CHANGELOG-pt.md) | [Română](CHANGELOG-ro.md) | [Svenska](CHANGELOG-sv.md)

- **1.15.0** موفر `--use_antigravity`: الترجمة عبر حصة اشتراك Google AI Pro أو Ultra، باستخدام أداة CLI الرسمية `agy`؛ وبالنسبة لجميع الموفرين، لم يعد Ctrl-C يترك وكيلاً عالقاً أثناء التشغيل، ولم يعد المقطع الفارغ يتسبب في فشل الملف، ولم يعد `.env` الخاص بالمشروع يحدد الملف التنفيذي المُشغّل (2026-09-26):

  - **المسار العاشر للموفرين: اشتراك Google، دون فوترة بحسب الاستخدام.** يمكن لمن يدفع مقابل Google AI Pro أو Ultra الآن خصم ترجماته من هذه الحصة بدلاً من الدفع لكل رمز عبر واجهة برمجة تطبيقات Gemini API. يُعد `agy`، وهو أداة CLI الرسمية لـ Antigravity، المسار الوحيد نحو هذه الحصة: إذ لم يعد Gemini CLI يخدم حسابات Pro أو Ultra أو الحسابات المجانية منذ 18 يونيو 2026، ولا تتم مصادقة Antigravity SDK إلا عبر مفتاح API أو عبر Google Cloud. يقودها `--use_antigravity` في الوضع غير الرسومي (headless)، تماماً مثل Codex وGrok CLI، على نفس الأساس — مجموعة عمليات مستقلة، و`SIGTERM` ثم `SIGKILL` عند انتهاء المهلة، وبيئة منقاة. تم التحقق من البداية إلى النهاية على agy 1.2.11: دليل Markdown — واجهة أمامية (front matter)، خط عريض، روابط بين قوسين، كود سطري، كتلة كود مدرجة داخل قائمة، جدول، اقتباس — تُرجم إلى الإنجليزية في 14 ثانية، بما في ذلك الفحص المسبق، ببنية مطابقة للأصل، وإضافة ملاحظة الترجمة؛ ثم، بعد إصلاحات المراجعة، إلى الألمانية في 19 ثانية، ببنية مطابقة، ودون كتابة أي شيء في `~/.gemini`.

  - **لم تبقَ أي قناة مدفوعة مفتوحة، ويتم التحقق من ذلك مع كل تشغيل.** يتعرف agy على مسارات أخرى غير الاشتراك — مفتاح API، مشروع Google Cloud، بوابة عبور، أرصدة ذكاء اصطناعي مدفوعة — والعديد من متغيرات البيئة تحوله إليها دون عرض أي إشعار. تم القياس: يرسل `AGY_GATEWAY_URL` المستند إلى بوابة تابعة لجهة خارجية، ويتحول `AGY_ADC_AUTH` إلى مشروع Google Cloud مفوتر، ويحول `CLOUD_CODE_URL` نقطة النهاية الموثقة إلى أي عنوان. بالتالي، فإن بيئة العملية الفرعية هي قائمة سماح وليست قائمة حظر: `PATH`، واللغة والمنطقة الزمنية، والطرفية، والهوية، والبروكسيات والشهادات الخاصة بالجهاز ومسار الجلسة — وهو ما أثبت `env -i` أن agy يحتاجه — دون أي مفتاح. أما قائمة الحظر بحسب بادئات العائلات (`AGY_`، `GOOGLE_`، `GEMINI_`، `CLOUD_CODE_`...)، والتي جُربت أولاً، فقد أغفلت بعضها: كشفت المراجعة في سلاسل الملف التنفيذي عن تجاوزات لنقاط النهاية لم تغطها أي بادئة، مثل `AICODE_ENDPOINT_URL`، `UNLEASH_URL`، `GCE_METADATA_HOST`. هناك إعدادان يخرجان عن نطاق البيئة ولا يمكن قراءتهما إلا عبر `agy -p /config`: أرصدة الذكاء الاصطناعي المدفوعة التي تتولى الأمر عند نفاد الحصة، والاتصال عبر مشروع Google Cloud. يرفض الفحص المسبق الذي لا يستهلك أي حصة — استهلاك صفري، تم قياسه — الترجمة إذا كان أحدهما نشطاً أو إذا تم تكوين مفتاح API، ويعتبر الإعداد الغائب عن `/config` بمثابة رفض لكل من الثلاثة: أي إصدار من agy قد يعيد تسمية أحدهما يجب ألا يتجاوز فحصاً لم يتحقق من أي شيء. أخيراً، يجب أن يثبت سجل كل استدعاء المصادقة عبر الاشتراك (`authMethod=consumer`)، وأي طريقة مصادقة أخرى تظهر فيه، بأي من الصيغتين اللتين يستخدمهما الملف التنفيذي، تؤدي إلى رفض الاستجابة: فالاستدعاء نفسه هو الفيصل، وليس التكوين الذي تم إعداده.

  - **وكيل الترجمة لا يمتلك أي أدوات، ويتم إثبات تحميله في كل استدعاء.** يكتب كل استدعاء، في دليل مؤقت قابل للتجاهل، وكيلاً بتنسيق وكلاء المشاريع في agy: `tools: []`، `excludeDefaultComponents: true`، `inheritCustomizations: false`. حوالي 800 رمز إدخال بدلاً من 12,400 مع الوكيل الافتراضي، والإعداد الثالث ليس زائداً عن الحاجة: كان `excludeDefaultComponents` وحده يترك خطاف مساحة العمل ينفذ أوامره ويدرج نصه. الفخ الحقيقي يكمن في مكان آخر: لا يؤدي الوكيل غير الموجود إلى فشل agy. رمز الإرجاع 0، والحالة `SUCCESS`، والوكيل البرمجي ذو الـ 57 أداة نشطة هو من يجيب — فعلى «Le chat dort.»، أجاب «صه، دعنا لا نوقظه! 🐱💤 ما الذي يمكنني فعله لك اليوم؟». لا شيء على stdout أو stderr: سجل الاستدعاء وحده هو الذي يوضح ذلك. بالتالي، يتم رفض الاستجابة إذا أعلن السجل عن هذا التراجع، وأيضاً إذا لم يحمل علامة التأكيد الإيجابية لتحميل الوكيل — حيث يؤدي أي تغيير في تنسيق السجل إلى رفض الترجمة بدلاً من إزالة الحماية بصمت. يجب أن تشغل كل علامة سطراً كاملاً في السجل، بالتنسيق المقاس — مستوى الخطورة، التاريخ، الوقت، الخيط، الملف والسطر: يحتوي الملف التنفيذي أيضاً على سطر ينسخ رسالة المستخدم في السجل — لم يُشاهد أبداً في مستوى التسجيل الافتراضي عبر 78 سجلاً حقيقياً — ولا يجب أن يتمكن أي مستند يقتبس هذه الرسائل من تلبية الحماية بدلاً من agy.

  - **لا يتم المساس بإعدادات agy وسجلاته الخاصة بالمستخدم.** يعمل كل استدعاء مع HOME ودليل مؤقت ودليل عمل خاصة، يتم حذفها لاحقاً. ومع ذلك، تتم مصادقة agy عبر سلسلة مفاتيح النظام، والتي لا يقرأها aipmt أبداً. لا يتم توارث أي شيء: لا أذونات المستخدم — التي كانت تترك الوكيل الافتراضي يشغل `ls` و`git` دون طلب أي إذن —، ولا قواعده العامة، أو الإضافات، أو خوادم MCP، أو الخطافات والمهارات. ولا يُضاف أي شيء إلى سجله، حيث كان كل استدعاء يترك محادثة تبلغ حوالي 200 كيلوبايت، بما في ذلك النص المترجم: كانت حملة الفحص قد تركت هناك حوالي 95 محادثة وطردت 17 مدخلاً من ذاكرة تخزين مؤقت محددة بـ 100 مدخل. تم التحقق: لم يُكتب أي ملف في `~/.gemini/antigravity-cli`.

  - **الرفض المغلق حيث لا تصمد العزلة.** في Linux دون مسار جلسة D-Bus — مثل جلسة SSH، أو حاوية، أو خادم —، يحفظ agy رمزه في ملف داخل `~/.gemini` بدلاً من سلسلة المفاتيح، كما يوضح سجل التغييرات الخاص به (1.1.3)، ويؤدي عزل كل استدعاء إلى إخفاء هذا الملف. يظهر الرفض الآن قبل أي تشغيل مع سببه الحقيقي، بدلاً من الانتظار لمدة دقيقة لرمز تسجيل الدخول يتبعه اقتراح بإعادة الاتصال لا يحل أي شيء. وفي Windows، يقرأ agy كلاً من `USERPROFILE` و`APPDATA` و`TEMP`، وليس `HOME` أو `TMPDIR` — وفقاً لوثائق Go وسلاسل الملف التنفيذي —: لن يعزل أي شيء هناك الاستدعاء عن إعدادات المستخدم أو سجله، ويتم رفض `--use_antigravity` هناك طالما لم يتم قياس ذلك. ينجح macOS دون أن يتم قياسه؛ حيث تصمد حماية الفوترة التي تقرأ التكوين الفعلي هناك.

  - **`exit 0` لا يثبت شيئاً، أداة CLI الرابعة.** لا تُقبل الاستجابة إلا برمز إرجاع صفري، وكائن JSON بحالة `SUCCESS`، وسجل متوافق، ودون إجراءات مرفوضة، أو أوامر، أو أخطاء في الـ JSON، ودون تحذيرات اقتطاع على stderr، ومع نص غير فارغ. لا يُستخدم `--print-timeout` أبداً: عند انتهاء صلاحيته، يعيد agy المخرجات الجزئية في `SUCCESS` مع الرمز 0؛ المهلة خارجية وتنهي مجموعة العمليات بالكامل. أي مقطع يبدأ بـ `/commande` أو `!commande` أو `@fichier` تتم ترجمته ولا يُنفذ أو يُعالج أبداً — فبدون `--disable-slash-commands`، كان أمر «/help» يُنفذ بدلاً من الترجمة. لم يعد المقطع الفارغ يصل إلى agy، حيث يعيده المسار كما هو (انظر أدناه)، ويظل مرفوضاً قبل الاستدعاء كشبكة أمان أخيرة: عند إدخال فارغ، يفتح agy واجهته التفاعلية برمز 0. تتبع إعادة المحاولة الحقل `retryable` من السطر `AGY_ERROR` الذي يكتبه agy على stderr منذ إصداره 1.2.6: فهو يعيد بالفعل محاولة الأخطاء العابرة بنفسه — 502، 503، 504، الحد الأقصى في الدقيقة —، وهو وحده من يعلم ما إذا كان الفشل المتبقي لا يزال كذلك. في حال غياب هذا الحقل، لا تتم إعادة المحاولة عندما تشير الرسالة إلى نفاد الحصة — في Google، يحمل `RESOURCE_EXHAUSTED` نفس الرمز 429 لحد الدقيقة، ولكن نافذة الـ 5 ساعات المنتهية لا تعود خلال 90 ثانية — وتقتصر المحاولة على تقييد المعدل فقط. تحتفظ الاستجابة بنهاية سطر المقطع: ينهي agy استجابته دائماً بفاصل أسطر، وهو ما كان يقسم الفقرة المجزأة في منتصف الجملة إلى قسمين عند الوصل. تؤدي الجلسة غير المتصلة إلى رسالة تفيد بتشغيل `agy` لإعادة الاتصال. يمنع `AGY_CLI_DISABLE_AUTO_UPDATE=true` أخيراً مجلد HOME الجديد من تشغيل فاحص التحديثات في كل استدعاء ضمن جلسته الخاصة، بعيداً عن متناول المهلة — بينما القيمة `1` ليس لها أي تأثير.

  - **النماذج: جهد التفكير جزء من الاسم.** المعرفات هي تلك الخاصة بـ `agy models` — `gemini-3.7-flash-medium`، `gemini-3.8-flash-low`، `gemini-3.1-pro-high`، `claude-sonnet-4-6`... الاسم الأساسي وحده، والذي قد يرفضه agy، يتم رفضه قبل الاستدعاء، مع اللواحق المتوفرة: `-medium` أو `-low` لنموذج Flash، و`-high` أو `-low` لنموذج Pro. الافتراضي هو `gemini-3.7-flash-medium`، و`gemini-3.7-flash-low` في `--eco`، والمحددان بالحملة الموضحة أدناه. تم استبعاد `gemini-3.8-flash-medium`: لكونه الأبطأ في الاختبار التجريبي، من 103 إلى 128 ثانية لكل README، وكان يخصص 79% من مخرجاته في أحد الفحوصات للتفكير، مخصوماً من الحصة. `--reasoning_effort` ليس له أي تأثير ويشير إلى ذلك. يستهلك كل من Claude وGPT-OSS من حصة منفصلة وأصغر بكثير — حوالي 1% من نافذة الـ 5 ساعات لكل استدعاء مقاس، مقابل 0.05% في Flash — ويذكر تحذير بذلك. يمكن قراءة الحصة المتبقية مجاناً عبر `agy -p /usage`. مرفوض في بيئات التكامل المستمر (CI)، مثل Codex وGrok CLI: حيث يعيش الاتصال في سلسلة مفاتيح شخصية، وتقترح الرسالة استخدام `--use_gemini` مع مفتاح Google.

  - **حملة 26 سبتمبر: تم قياس الإعدادات الافتراضية، ولم تُفترض.** باستخدام `aipmt --use_antigravity` نفسه، أربع ترجمات متزامنة، تمت مقارنة كل منها بأصلها عبر `scripts/compare_structure.py`. اختبار تجريبي على هذا الـ README باللغات اليابانية والعربية والهندية: قدمت متغيرات Flash الأربعة — 3.7 و3.8، بجهد منخفض أو متوسط — بنية متطابقة بالكامل، في مدة من 42 إلى 58 ثانية لكل لغة بالجهد المنخفض، و72 إلى 84 في `gemini-3.7-flash-medium`، و103 إلى 128 في `gemini-3.8-flash-medium`. نظراً لعدم وجود أي اختلاف في البنية للمفاضلة، يظل الخيار الافتراضي للجودة ضمن العائلة التي تمتلك أكبر قدر من الأدلة — Gemini 3.7 Flash، 14 لغة من أصل 14 دون أي اختلاف في المقال الكثيف عبر API —، عند الجهد المتوسط، والوضع الاقتصادي هو الجهد المنخفض، وهو الأسرع. حملة كاملة في `gemini-3.7-flash-medium`، بأربع عشرة لغة: مقال الرصد الكثيف لقياسات الـ README، في `--news`، كُتبت 14 ترجمة من أصل 14 وجميعها دون أي اختلاف، بمتوسط 3 دقائق و14 ثانية لكل لغة؛ وفي الإنجليزية، أزال النموذج بنفسه الأسطر الثلاثة للترجمة الفرنسية دون اختلاق أي علم — وهو الفشل الذي استبعد Gemma 4 وQwen 3.5 —، وبقيت الاقتباسات الإنجليزية سليمة. هذا الـ README، في مراجعته للإصدار 1.14.0: كُتبت 14 من أصل 14، 13 منها دون أي اختلاف — كلمة واحدة عريضة أقل في الكورية —، بمتوسط دقيقة و22 ثانية. وفي `--eco`، المقال بالإنجليزية واليابانية والعربية والهندية: أربعة من أصل أربعة دون أي اختلاف، بمتوسط دقيقة و52 ثانية. الحصة، المقروءة عبر `agy -p /usage` قبل وبعد: الحملة الكاملة، 32 ترجمة و2.2 مليون حرف من المصدر، استهلكت 26.3 نقطة من نافذة الـ 5 ساعات و4.4 من الأسبوعية، أي ما يقارب نصف نقطة لـ README من 40,000 حرف — على مستوى الحساب المقاس، حيث يعتمد الحد الأسبوعي على المستوى.

  - **شروط الاستخدام ومعالجة البيانات، موضحة بصراحة.** تحظر شروط Antigravity (القسم 6) وأسئلتها الشائعة الوصول إلى الخدمة عبر برنامج تابع لجهة خارجية باستخدام تسجيل دخول Antigravity — مع ذكر Claude Code وOpenClaw وOpenCode هناك — تحت طائلة تعليق الحساب. لا يقرأ aipmt الرمز المميز أو يعيد استخدامه أبداً: بل يشغل الملف التنفيذي الرسمي في الوضع غير الرسومي الذي توثقه Google للبرامج النصية والـ CI، وقد اعتبر أحد مسؤولي Google في المنتدى الرسمي أن تشغيل `agy -p` من برنامج نصي محلي للعمل الخاص أمر «قياسي» — وهي إجابة وليست التزاماً. لا يوجد نص يحسم حالة الأداة الموزعة: يوضح الـ README أن المستخدم يتحمل مسؤولية حسابه. يتيح القسم 5 من نفس الشروط لـ Google استخدام المحادثات — المطالبات، والردود، والبيانات الوصفية — لتحسين منتجاتها وتعلم الآلة لديها، ومراجعتها بواسطة مراجعين بشريين، دون تمييز بين العرض المجاني وPro وUltra: يخصص الـ README هذا المسار للمستندات العامة، ويشير تحذيره النهائي إلى ذلك.

  - **يمكن أن تمر ترجمات هذا المستودع عبر هذا المسار، دون استثناء.** يُعد `REGEN_PROVIDER=antigravity` اشتراكاً، وليس واجهة برمجة تطبيقات مفوترة: يتم تنفيذه دون `REGEN_ALLOW_PAID_API`، مع أربع مهام — عملت الحملة بأكملها بأربع مهام متزامنة دون أي خطأ — والحد الأقصى البالغ 1,800 ثانية لكل مهمة لـ Codex، وهو ما يبرره القياس: تُرجم ملف CHANGELOG بأكمله باللغة الهندية، وهو الملف الذي يفقد فيه Codex العناصر البديلة، دون أي اختلاف في 4 دقائق و33 ثانية، أي أسرع بست مرات ونصف من الحد الأقصى. تقدر إعادة التوليد الكاملة، وفقاً للحملة، بربع نافذة الـ 5 ساعات. يسبق الفحص المسبق للوحدة المهام الـ 28 كفحص وحيد: أي agy غير متصل أو مضبوط للفوترة لا يشغل أياً منها. يظل الخيار الافتراضي هو Codex مع `gpt-5.6-sol`.

  - **أصبح كل من Ctrl-C وإغلاق الطرفية يوقفان الوكيل الآن، لجميع أدوات CLI الأربع.** تعمل Codex وGrok CLI وOpenCode وAntigravity في جلستها الخاصة، خارج مجموعة المقدمة: لا يصل إليها `SIGINT` الخاص بـ Ctrl-C، وعند الضغط على Ctrl-C، لا ينتظرها `Popen` سوى ربع ثانية قبل إعادة التحكم. تم التحقق من ذلك بواسطة المراجعة على ملف تنفيذي وهمي: كان الوكيل ينجو من aipmt وينهي دوره مستهلكاً الحصة، للحصول على نتيجة مهملة — أربعة في وقت واحد أثناء إعادة التوليد. يؤدي `KeyboardInterrupt` أو `SystemExit` أثناء الاستدعاء الآن إلى إنهاء مجموعة الوكيل قبل التصعيد. كان إغلاق الطرفية أو انقطاع جلسة SSH (`SIGHUP`) ينهي Python دون أي تنظيف: كان الوكيل يستمر، ويبقى الدليل الخاص بـ Antigravity على القرص مع سجله الذي يحمل عنوان الحساب. يتبع `SIGHUP` الآن مسار `SIGTERM`: إنهاء مجموعة الوكيل، ثم الخروج بالرمز 129. يبقى `SIGHUP` الذي تم تجاهله، عبر تشغيل تحت `nohup`، كما هو: فهذا خيار المستخدم. يمر الفحص المسبق لـ Antigravity — `agy --version`، ثم `agy -p /config` — عبر نفس المعالج: تؤدي الإشارة المستلمة أثناء هذا الفحص إلى إيقاف agy وحذف الدليل الخاص أيضاً، بدلاً من تركه على القرص مع سجله.

  - **لم يعد بإمكان مشغل العمليات الفرعية استهداف الجلسة بأكملها.** في Linux، يساوي `killpg(1, sig)` القيمة `kill(-1, sig)`: حيث تنطلق الإشارة إلى جميع عمليات المستخدم. حدث هذا أثناء كتابة هذا الإصدار، من خلال حملة اختبارات التحول (mutation testing): استدعى أحد المتحولات `Popen` مضاعفاً بـ `MagicMock` مجرد، ومر الخطأ الناتج عبر مسار المقاطعة الجديد، وتسبب pid العملية الوهمية، الذي يحوله Python إلى 1، في إنهاء الجلسة الرسومية بأكملها. أصبح `_codex_kill_group`، المشترك بين أدوات CLI الأربع، لا يستهدف مجموعة إلا إذا كان pid عدداً صحيحاً حقيقياً أكبر من 1، ويعيد `getpgid` نفس الـ pid هذا — فالوكيل المشغل في جلسته الخاصة هو قائد مجموعته — وألا تكون مجموعة العملية الحالية؛ وإلا، فسيتم استهداف الابن المباشر فقط. في بيئة الإنتاج، لا يغير هذا الصمام الوقائي أي شيء: فهو يغلق الحالة التي قد يتسبب فيها خطأ برمجي في تصعيد شيء آخر. أصبحت النسخ المزدوجة من `Popen` التي لا ينبغي استخدامها تفشل تماماً، وتحمل العمليات الوهمية pid مستحيلاً، وتعمل حملات اختبار التحول داخل مساحة أسماء PID.

  - **لم يعد المقطع الفارغ (مساحة بيضاء) يتسبب في فشل الملف، أياً كان المزود.** يمكن لـ `segment_text` عزل النهاية الفارغة لمستند ما: ينتج عن 16,001 حرفاً تنتهي بثلاث فواصل أسطر مقطعٌ ثانٍ مكوّن من فاصلتي أسطر. كان Antigravity يرفضه قبل إجراء الاستدعاء — تم تكرار الحالة عبر المراجعة — ولدى أحد مزودي واجهة برمجة التطبيقات، كانت الاستجابة الفارغة تقع تحت طائلة حارس المحتوى الفارغ: في كلتا الحالتين، كان الملف بأكمله يفشل، بعد استهلاك الحصة للمقاطع السابقة. يُعاد المقطع الفارغ الآن كما هو، دون أي استدعاء.

  - **الأمان: لم يعد بإمكان ملف `.env` الخاص بالمشروع اختيار الملف الثنائي المنفَّذ.** يحدد كل من `CODEX_BIN` و`GROK_BIN` و`OPENCODE_BIN` و`AGY_BIN` البرنامج الذي تشغله عملية الترجمة بصلاحيات المستخدم: عند تعيينها عبر `.env` لمستودع تم استنساخه للتو، كانت تؤدي إلى تشغيل ملف من ذلك المستودع بمجرد بدء أول ترجمة. لذا ينضم اللاحق `_BIN` إلى `_BASE_URL` و`_API_BASE` و`_ENDPOINT` في مرشح طبقة المشروع، مع `GROK_HOME`، الذي يُبحث تحته عن ثنائي Grok (`$GROK_HOME/bin/grok`). تظل هذه المتغيرات مقبولة من البيئة ومن `~/.config/aipmt/.env`، ويُبلَّغ عن كل رفض عبر stderr مع الإشارة إلى مكان تعيينها.

  - **الاختبارات**: ملف جديد `tests/test_antigravity_provider.py` (103 اختبارات)، يغطي 100% من التعليمات والتفرعات في الوحدة — argv دقيق ولا يكون `-p` مطلقاً، مقطع عبر stdin، عميل مكتوب دون أدوات أو وراثة، بيئة مقتصرة على قائمة السماح، دليل خاص يُحذف بعد النجاح أو الفشل أو انتهاء المهلة أو الإشارة، عقد إخراج كامل، تراجع العميل ومسار الفوترة مقروءان من أسطر السجل بالتنسيق الفعلي، `AGY_ERROR` و`retryable`، فحص فوترة مبدئي صارم، رفض تحت Windows ودون ناقل جلسة، لواحق الجهد، نهاية السطر مسترجعة من المقطع، توصيل التوزيع وتفرع إعادة التوليد. لا يوجد اختبار يشغل agy: ثنائي مزيف وُضع في بداية `PATH` لم يتم استدعاؤه في أي تشغيل، بما في ذلك ضمن ظروف التكامل المستمر (CI)، مع إخفاء ناقل الجلسة و`CI=true`. يقطع `tests/test_provider_base.py` عملية فرعية حقيقية عبر Ctrl-C وعبر `SIGHUP`، بما في ذلك تحت `nohup`، ويُحكم إجراء الحماية لـ `killpg` — pid `MagicMock`، قيمة منطقية، 1 أو أقل، المجموعة الحالية أو مجموعة أخرى: لا تُرسل إشارة مجموعة أبداً؛ يُشغل `tests/test_key_resolution.py` عملية جديدة لكل متغير `_BIN`؛ ويترجم `tests/test_orchestration.py` مستنداً تشكل نهايته الفارغة مقطعاً منفصلاً. تبلغ الحزمة الكاملة الآن **650 اختباراً**.

- **1.14.1** Mistral: لم يعد تجاوز معدل الطلبات يتسبب في فشل الملف، وتأكيد النماذج الافتراضية عبر القياس (2026-09-17):

  - **كان خطأ HTTP 429 يؤدي إلى فقدان الملف بأكمله.** لا تعيد حزمة SDK `mistralai` المحاولة افتراضياً، على عكس حزم OpenAI وAnthropic، كما أن حدود Mistral منخفضة وخاصة بكل نموذج: 15 طلباً في الدقيقة لـ Large 3، و100,000 رمز في الدقيقة لـ Small 4، وفقاً للقياس على حساب المستودع. أسفرت حملة مكونة من أربع ترجمات متزامنة على Small عن فقدان خمس من أصل أربع وثمانين. يعيد العميل المحاولة الآن عند مواجهة 429 و5xx وأخطاء الاتصال، مع انتظار تصاعدي من ثانيتين إلى 60 ثانية لمدة خمس دقائق كحد أقصى: لا يوضح خطأ 429 من Mistral مدة الانتظار، وتُحسب حدوده بالدقيقة. اختبار مضاد على واجهة برمجة التطبيقات: عشرون استدعاءً متتالياً على Large 3، نجحت جميعها، وجاء الاستدعاء السادس عشر بعد انتظار دام 33 ثانية.
  - **لم تعد الاستجابة المكونة من كتل التفكير تعطل الترجمة.** يقوم Small 4 وMedium 3.5 بالتفكير والاستنتاج عند طلب ذلك، وتتحول الاستجابة حينئذٍ إلى قائمة من كتل `thinking` ثم `text` بدلاً من سلسلة نصية. لا يقوم أي منهما بالتفكير افتراضياً (وفقاً للقياس)، ولكن أي تغيير في الإعداد الافتراضي من جانب المزود كان سينتهي بـ `AttributeError`: تم استبعاد كتل التفكير، تماماً كما هو الحال مع Claude.
  - **تأكيد النماذج الافتراضية: `mistral-large-latest` و`mistral-small-latest`.** تم قياس النموذج الأحدث الوحيد، Mistral Medium 3.5، في مواجهتهما عبر أربع عشرة لغة، على مقال المتابعة التقنية المكثف في جدول التوافق وعلى ملف README. في المقال، يفقد عناوين فرعية أو يغير مستواها في 9 لغات من أصل 14 — ترجمة واحدة فقط دون أي تفاوت، مقابل 6 لـ Large 3 و8 لـ Small 4 — كما أن تكلفته تبلغ 3 أضعاف Large 3 في المدخلات و5 أضعاف في المخرجات. تشير الأسماء المستعارة إلى Large 3 (`mistral-large-2512`) وSmall 4 (`mistral-small-2603`).
  - **محاذاة `MODEL_TOKEN_LIMITS` مع واجهة برمجة التطبيقات**: 262,144 رمزاً لمجموعة Mistral بدلاً من 256,000، و131,072 لـ `ministral-3b`، ولم تعد المعرّفات المؤرخة، بما فيها `mistral-medium-2604`، تطلق التحذير "غير مدرج". لا يطرأ أي تغيير على تقسيم المقاطع، المحدد بسقف 16,000 حرف.

- **1.14.0** جدول توافق في بداية ملف README، وحملة القياس التي يستند إليها (2026-09-09):

  - **النتيجة حسب النموذج في أعلى ملف README، باللغة الفرنسية.** كان هناك أكثر من ثلاثمائة ترجمة مكدسة في منتصف المستند، موزعة على ثلاثة جداول كان يتعين قراءتها ومطابقتها. يحسم الجدول الجديد النتيجة في سطر واحد لكل نموذج، ويوضح بالتحديد ما يختلف: "نسيان كلمة عريضة، بالعربية واليابانية" بدلاً من نسبة رقمية. لم يُرصد أي تفاوت بواسطة أداة المقارنة لثلاثة نماذج على المستندين المكثفين — `gemini-3.7-flash`، و`gpt-5.6-sol` عبر اشتراك ChatGPT، و`z-ai/glm-5.2` عبر OpenRouter.
  - **حملة إعادة قياس شملت 176 ترجمة.** كانت القياسات الأصلية تعود إلى 4 سبتمبر وأُجريت على ملف README تغير كثيراً منذ ذلك الحين. أُعيد قياس ثمانية نماذج على ملف README هذا، واثنين على مقال متابعة في المدونة بالوضع `--news`، وواحد على أربعة ملفات README عامة — FastAPI وOllama وtldr-pages وVue.js. برزت ثلاثة نتائج تستحق التدوين: يفقد Mistral **عنوان قسم بالكامل** في العربية والهندية والكورية، دون أن يكتشف ذلك أي حارس في المسار — فالعنوان غير قابل للاستبدال برمز مميز، ولا توجد آلية حالياً للتحقق منه أثناء الترجمة، حتى وإن اكتشفته أداة المقارنة بعد وقوعه؛ ويفشل Grok في خمس لغات من مقال متابعة، حيث يفقد أربعة مقاطع برمجية مضمنة وثلاثة عناوين URL منذ المقطع الأول، على الرغم من تماسكه في ملف README؛ ويقدم Gemini 55 ترجمة دون تفاوت من أصل 56 على ملفات README العامة الأربعة.
  - **إدراج أداة مقارنة الهيكل في المستودع.** كان ملف README يصف هذا البروتوكول بأنه الطريقة المثلى لتقييم أي نموذج — "قارن عدد الأقسام والروابط وعناوين URL المميزة والكتل البرمجية" — دون توفير الأداة. أداة `scripts/compare_structure.py` هي التي أنتجت هذه الجداول، مع أحد عشر اختباراً تثبت قدرتها على اكتشاف فقدان أي عنوان URL أو كتلة برمجية أو قسم أو كود مضمن أو سطر جدول أو اقتباس. وتتضمن التصحيحين الخاصين بأنظمة الكتابة غير اللاتينية اللذين جعلاها موثوقة: لم يعد عنوان URL المتبوع بقوس كامل العرض يُحسب على أنه مختلف، ولم يعد الاقتباس الأقصر باللغة الصينية يُعتبر اقتباساً مفقوداً.
  - **فحص الحزمة قبل النشر وبعده.** تُنفَّذ حزمة الاختبارات على الشيفرة المصدرية وتظل ناجحة بينما تكون الحزمة الموزعة معطوبة — كوحدة منسية في إعدادات setuptools أو نقطة دخول يتعذر حلها. يبني `scripts/check-package-smoke.sh --wheel` حزمة التوزيع، ويثبتها في بيئة وهمية مؤقتة خارج المستودع، ويتحقق من أنها لا تحتوي إلا على الحزمة، ومن استجابة شكلي التنفيذ، ومن استيراد جميع الوحدات بنجاح، ثم يترجم مستنداً حقيقياً ويقارن هيكله. يقوم `--pypi` بالشيء نفسه انطلاقاً من الفهرس: وهو التحقق الوحيد الذي يثبت أن هذه البايتات بالتحديد هي التي تم إرسالها، علماً بأن PyPI لا يسمح أبداً بإعادة نشر إصدار معين. تنفذ بوابة `--full` الجزء غير المتصل بالإنترنت منها، دون إجراء استدعاءات للنماذج أو تكبد أي تكلفة.
  - **تصحيح سبع ادعاءات في ملف README، تم تكرارها جميعاً والتأكد منها قبل إعادة كتابتها.** كانت عبارة "لا يصل أي شيء خاطئ إلى قرصك" غير صحيحة: فالنموذج الذي يحذف قسماً أو يعدل تاريخاً في الواجهة الأمامية يعيد ملفاً يكتبه المسار بالحالة `success`، وقد تم تكرار ذلك باستخدام استجابة وهمية. يوضح ملف README الآن ما تحميه الرموز المميزة — كتل التعليمات البرمجية، الكود المضمن، عناوين URL، المراسي، الاقتباسات — وما لا تتحقق منه حراس المسار حالياً. عبارة "دون تفاوت" لا تعني "مطابق": تحسب أداة المقارنة العناصر دون قراءة محتواها، ولا تبلغ عن حذف عنوان من المستوى 4، أو استبدال نص كود مضمن، أو تبديل راية — وهي ثلاث حالات تم قياسها، وتمت إزالة عبارة "نفس الرايات" لعدم احتواء أداة المقارنة على عداد للرايات. لم يكن التثبيت لغرض المساهمة ينتج أداة قابلة للاستخدام: لا يثبت `pip install -r requirements.txt` سوى التبعيات، وكان `python -m aipmt` يستجيب بـ `No module named aipmt`، وكان `pip install -e .` مفقوداً — تماماً مثل سطر تثبيت `pre-commit` الغائب عن ملفي التبعيات. كان الوعد المقدم بشأن OpenRouter والسياقات الصغيرة يعاكس ما في الشيفرة: يُرفض أي نموذج بنافذة تقل عن **16,400 رمز** قبل أي استدعاء، وتم قياس ذلك عند 4,095 و8,192 و16,384. لم يكن المرشح الذي يمنع ملف `.env` الخاص بالمشروع من إعادة توجيه استدعاءات واجهة برمجة التطبيقات موثقاً في أي مكان؛ وهو موثق الآن مع قائمة المتغيرات المعنية. رقمان أخيران: كانت خلية Grok في الجدول الرئيسي مأخوذة من حملة 9 سبتمبر في حين أشار عمودها إلى 4 سبتمبر، وعبارة "رابط مفكك" المنسوبة إلى Gemini كانت عكس القياس الفعلي — فهو يحول ثلاثة عناوين URL مجردة إلى روابط، ولا يفقد أياً منها.

  - **لم يعد فشل الكتابة يترك ترجمة مقتطعة على القرص.** يقتطع `open(cible, "w")` الملف قبل ملئه: وكان أي خطأ أثناء الكتابة — كامتلاء القرص — يترك ملفاً مستهدفاً جزئياً. الحالة المكررة: كُتبت ثمانية بايتات، وأُعيدت الحالة `failure`، ثم عند إعادة التشغيل التالية عُثر على هذا الملف، وأُعيدت الاستجابة `skipped` مع الاحتفاظ به. كان الفشل يتحول إلى ترجمة مقتطعة دون أي تنبيه، وكانت قيمة رمز الخروج 0. يُكتب المحتوى الآن في ملف مؤقت في الدليل نفسه — إذ لا تكون عملية `os.replace` ذرية إلا على نظام الملفات نفسه — ثم يُعاد تسميته، مع حذف الملف المؤقت في حال فشل الكتابة، بما في ذلك عند المقاطعة. يأتي هذا الملف المؤقت من `mkstemp`، وقد أظهرت المراجعة أن استخدام اسم يمكن التنبؤ به لم يكن سوى نقل للمشكلة: رابط رمزي وُضع عند `cible.md.aipmt-tmp` أدى إلى كتابة الترجمة في الملف المستهدف خارج دليل الإخراج، مع إرجاع `success`؛ كما أن تشغيلين متزامنين على الهدف نفسه تشاركا هذا الاسم، حيث أعادت العملية الثانية التسمية بينما كانت الأولى لا تزال تكتب في الـ inode نفسه — أُعلن عن نجاح الهدف بينما اختلط المحتوى (`BBBAAAAA` بدلاً من `BBBBBBBB`، وفقاً للقياس). ينشئ `mkstemp` في الوضع `O_CREAT | O_EXCL` تحت اسم فريد، مما يغلق الثغرتين معاً. تُستعاد صلاحيات الهدف الحالي صراحةً قبل الاستبدال: ينشئ `mkstemp` بصلاحيات 0600، وكان الهدف المعين عمداً بصلاحيات 0600 سيتحول لولا ذلك إلى 0664، كما أن هدفاً بصلاحيات 0644 يُقدَّم عبر خادم ويب كان سيصبح غير قابل للقراءة. تُحكم ثمانية اختبارات هذه المنظومة بالكامل، ويؤدي تعديلان في الشيفرة إلى فشلها فوراً.

  - **بيان النقاء قادر على التصريح عن علامة كتم تمت إعادة كتابتها.** كان الفحص يتطلب بقاء كل سطر يحمل `# nosec` أو `# nosemgrep` أو `NOSONAR` من المرجع كلمة بكلمة. غير أن نقل عملية الكتابة إلى ملف مؤقت يعيد تسمية المتغير المفتوح: يظل كتم Sonar كما هو، لكن رقمه/سطره يتغير. تصرح الفئة `markers` من البيان عن هذا الزوج `old` ← `new` مع ذكر السبب. يتطلب ذلك شرطين، وجاء الثاني من إحدى المراجعات: يجب أن يكون البديل موجوداً، **وأن يحمل عمليات الكتم نفسها الموجودة في السطر الأصلي**. فبدون هذا الشرط، كان التصريح عن `value = 1  # NOSONAR` ← `value = 1` يعطي نتيجة ناجحة، بحيث يكفي مجرد تصريح لإزالة علامة — وهو بالضبط ما وُجد هذا الفحص لمنعه. وأي اختفاء غير مصرّح به يظل مرفوضاً.

  - **سحب حملتين من الجداول بسبب انقطاعهما لعوامل خارجة عن النموذج.** فقد Grok جلسة واجهة سطر الأوامر الخاصة به بعد اثنتي عشرة لغة من ملف README هذا — حيث رُفضت لغتان في أربع ثوانٍ دون استدعاء النموذج — كما تعطل `qwen3.8-flash` بسبب خطأ HTTP 429 من موفر الاستضافة الأصلي بعد لغتين. الحملة المنقطعة لا يُعتد بتقييمها: فإما أن تُعاد بالكامل أو لا تُدرج على الإطلاق. وفي السياق نفسه، تمت إعادة ضبط المدد الزمنية في جدول "README الخاص بهذا المشروع" بناءً على حملة 9 سبتمبر — إذ كانت تعود لمراجعة من 508 أسطر، مقابل 785 سطراً تم قياسها، وكانت تسجل دقيقتين و4 ثوانٍ لـ `gpt-5.6-sol` بينما أظهر القياس 6 دقائق و46 ثانية.

  - **إعادة صياغة وهيكلة ملف README بالكامل: 600 سطر بدلاً من 1,007، وفق الترتيب المنطقي للقارئ.** ما تقوم به الأداة، التثبيت، الإعداد، الخطوات الأولى، ثم جدول التوافق — وُضع بعد التثبيت، حين يصبح المستخدم على دراية بما يُشغله —، الخيارات، فصل لكل مزود، القياسات التفصيلية، وأخيراً كيفية المساهمة. أُزيلت روايات الحملات، والطرائف المتعلقة بالقياسات، والتكرارات بين الأقسام؛ وكل رقم تم الاحتفاظ به يطابق ما في الجداول، وكل ادعاء بالسلوك يتطابق مع شيفرة هذا الإصدار. يظل سجل إعادة الهيكلة محفوظاً في سجل git.

  - **يوضح الجدول الرئيسي الآن عدد اللغات التي ينطبق عليها كل تفاوت.** كان يذكر سابقاً "✅ 14 لغة" ثم في العمود المجاور "كلمة عريضة" — دون توضيح ما إذا كان ذلك ينطبق على لغة واحدة أم على اللغات الأربع عشرة جميعها. لم يكن القارئ قادراً على الجزم، وكان الانطباع سلبياً رغم جودة القياس: في ملف README هذا، لدى Gemini لغة واحدة فقط من أصل أربع عشرة تحتوي على كلمة عريضة إضافية، ولدى Sol لغتان، ولدى GLM ثلاث. تحتوي كل خلية الآن على عدد اللغات المعنية وتحديدها، ورقم أعمدة النتيجة محدد في جملة واحدة — عدد اللغات، من أصل أربع عشرة، التي تمت كتابة ترجمتها دون أي اختلاف —، مع وسيلة إيضاح توضح معنى الرموز الثلاثة. التصنيف مبني على معيار المالك: ✅ لا يوجد أي اختلاف؛ ⚠️ كل شيء مترجم ولم يتغير سوى التنسيق، ولا ينقص شيء؛ ❌ رُفضت لغة واحدة على الأقل، أو ينقص محتوى في ملف تمت كتابته.

  - **إعادة احتساب كامل حملة المقال باستخدام أداة المقارنة الحالية.** نُشر رقمان قبل تصحيح أداة المقارنة فيما يخص أنظمة الكتابة غير اللاتينية، وكانت تحتسب لهما تفاوتات غير موجودة: ارتفع `qwen3.8-flash` من ثلاث عشرة إلى أربع عشرة لغة دون تفاوت، و`qwen3.7-flash` من سبع إلى ثمانٍ. كما تم تصحيح الفقرة الخاصة بـ `gpt-oss` في الوقت نفسه: المقاطع التي تركها بالفرنسية لم تصل قط إلى أي ملف مُسلَّم، إذ قام حارس المقتطفات غير المترجمة برفض اللغات الأربع المعنية — وهو الفرق بين نموذج يتم إيقافه ونموذج يمر دون ملاحظته، وملف README يوضح ذلك صراحة.

  - **إخلاء مسؤولية في نهاية ملف README.** تستبعد المادتان 15 و16 من رخصة GPL v3 أي ضمانات بالفعل، ولكن قلّما يقرأ أحد رخصة الاستخدام قبل تثبيت الأداة. لذا تم توضيح أربع نقاط بشكل جلي: يجب مراجعة الترجمة الآلية قبل النشر، حيث لا تغطي آليات الحماية العناوين أو الجداول أو الواجهة الأمامية أو المعنى؛ يُرسل المستند المترجم إلى المزود المختار وفق شروطه الخاصة، مع إمكانية إعادة استخدام المحادثات من قِبل بعض النماذج المجانية — ويبقى استخدام نموذج محلي هو السبيل الوحيد لعدم خروج البيانات؛ استدعاءات واجهة برمجة التطبيقات مدفوعة ولا يضع البرنامج حداً أقصى للإنفاق؛ والقياسات المنشورة هي ملاحظات مؤرخة وليست ضمانات.

  - **يمكن الوصول إلى عائلة `gpt-5.6-*` بالكامل عبر اشتراك ChatGPT**، وفقاً للقياس: يوفر `--model gpt-5.6-terra` عبر Codex النموذج الذي تقدمه واجهة برمجة التطبيقات افتراضياً. ويبقى النموذج الافتراضي هو `gpt-5.6-sol`، النموذج الوحيد الذي تم قياسه على أربع عشرة لغة دون أي فقدان في مستند مكثف، وهو الذي يتولى ترجمة هذا المستودع.

- **1.13.1** حداثة التبعيات: لم يعد التأخير مجرد تحذير يمكن تجاهله (2026-09-09):

  - **التحذير غير المحسوب هو تحذير يمر دون ملاحظة.** كان `check-deps-fresh.sh` يُشير منذ عدة أيام إلى أن `openai` و`anthropic` قد تأخرا، لكن سطر ملخص بوابة الفحص (gate) لم يكن يحسب سوى حالات الفشل: كان يُعلن «جاهز: N فحوصات باللون الأخضر» في حين كان التأخير مكتوبًا قبل بضعة أسطر. لم يكن بإمكان أي شخص يقرأ السطر الأخير — وهو ما يفعله الجميع — أن يعلم بذلك. أصبح الحكم الآن يتضمن عدد التحذيرات، ويقوم عدّاد مخصص بإحصائها بدلاً من طباعتها تباعًا أثناء التشغيل.
  - **التأخير يأتي مصحوبًا بملاحظات الإصدار.** فالرقم لا يوضح ما تغير، والبحث الذاتي عن الملف المناسب هو تحديدًا العائق الذي يؤدي إلى تخطي هذه الخطوة. يعرض الفحص عنوان CHANGELOG الخاص بكل حزمة متأخرة، سواء كانت إصدارات رئيسية أو فرعية، مع ذكر السبب: إذ سبق لإصدار فرعي من SDK أن عدّل أمورًا يعتمد عليها هذا المشروع.
  - **`openai` 3.8.0 → 3.10.0 و`anthropic` 1.3.0 → 1.4.0، قراءة ملاحظات الإصدار قبل التثبيت.** من جانب OpenAI، تغيير سلوك واحد فقط خلال هذه الفترة — رأس `Retry-After` رقمي يتجاوز سعة الفاصلة العائمة لم يعد يؤدي إلى التراجع إلى تراجع أسي قصير (backoff) بل يُرجع الخطأ الأصلي دون إعادة المحاولة — ولا شيء يمس ما يعتمد عليه هذا المشروع: فتسامح النماذج مع الحقول الإضافية لم يتغير، وتم التحقق منه في الكود عند كلا الوسمين، وبالتالي فإن `native_finish_reason` و`error` اللذين يضيفهما OpenRouter إلى الخيار يستمران في المرور بنجاح. ومن جانب Anthropic، يرفض حارس جديد بواسطة `TypeError` صريح كائنًا من الحزمة `httpx` حيث يتوقع SDK وجود `httpx2`؛ هذا المشروع لا يمرر سوى أرقام عشرية، وتم التحقق من ذلك. والقاعدة التي ترفض أي استدعاء غير متدفق يتجاوز عشر دقائق متطابقة بايت لبايت بين الإصدارين: فـ `timeout` الصريح هو بالفعل ما يعفي من ذلك، حيث تتجاوز رموزه البالغة 32,768 رمزًا عتبة 21,333.
  - **تم التحقق**: مجموعات اختبارات تضم 502 + 31 اختبارًا على الإصدارات المحدثة، وانغلاق `requirements.txt` مطابق للمثبت (41 تثبيتًا محددًا)، واستدعاء فعلي واحد لكل SDK — OpenAI وClaude — على مستند في الوضع `--news`، مع بنية مطابقة للمصدر.

- **1.13.0** مزود `--use_openrouter`: موجه مدفوع إلى ما يقارب 430 نموذجًا، بما في ذلك النماذج الصينية المفتوحة (2026-09-05):

  - **مسار المزود التاسع، صدر مع الثامن.** لم يتم نشر 1.12.0 على PyPI: فالموجهان، OpenCode وOpenRouter، يصدران معًا. يتيح [OpenRouter](https://openrouter.ai) الوصول بمفتاح واحد إلى نماذج لا يعرضها أي مزود آخر هنا — Kimi وQwen وDeepSeek وZ.ai — برصيد واحد يُحاسب حسب الاستخدام. وبما أن نقطة النهاية متوافقة مع OpenAI، فإن العميل هو نفسه الخاص بـ xAI؛ **كل ما يميز هذا المزود يكمن في فحص مسبق (preflight)**، وكل قاعدة من قواعده مستمدة من قياسات فعلية على API.

  - **النموذج نفسه يُقدم عبر عشرات المستضيفين بحدود قصوى مختلفة، والتوجيه لا يراعي ذلك.** مقاس: 33 مستضيفًا لـ `z-ai/glm-5.2`، و23 لـ `z-ai/glm-5.3-flash` — أحدهم بحد أقصى يبلغ **2,048 رمز مخرجات**. بالتالي، كانت هناك ترجمة طويلة من أصل 23 تخرج مقتطعة عشوائيًا، دون أي تنبيه. يقرأ الفحص المسبق `/api/v1/models/{modèle}/endpoints`، ويستبعد المستضيفين الذين يقل حدهم عن 8,000 رمز مخرجات، والذين يعانون من حالة متدهورة، والذين لا يصرحون بأي حد أقصى، ثم يثبت الباقين. `provider.only` **بدون** `allow_fallbacks: false` ليس سوى تفضيل: حيث يعود الموجه إلى مستضيف مستبعد، ويصبح التثبيت بلا قيمة. وإذا لم يلتزم أي مستضيف بالحد الأقصى، يتوقف الأمر: إذ إن الاستمرار في الترجمة سيعني قبول الاقتطاع الصامت الذي وجد هذا الفحص المسبق لمنعه.

  - **التفكير الاستدلالي يُحاسب بسعر المخرجات، وهو مفعل افتراضيًا على العديد من النماذج.** الطلب نفسه على `z-ai/glm-5.2`، مع الإجابة «OK»: **107 رموز إكمال وفق الإعداد الافتراضي للنموذج، و2 عند تعطيل التفكير**. في عملية الترجمة، التي لا يضيف إليها التفكير أي فائدة، يمثل هذا زيادة بمقدار 18 ضعفًا لكل مقطع في كل ملف. لذلك يتم تعطيله افتراضيًا. أما النماذج التي تفرضه والبالغ عددها **288 من أصل 431** (`reasoning.mandatory`) فتستجيب بـ `400 « Reasoning is mandatory for this endpoint and cannot be disabled »`: بالنسبة لها، يقرأ الفحص المسبق مستويات الجهد المقبولة ويطلب أدناها (النقطة التالية) — فالجهد يخصص **نسبة مئوية من `max_tokens`** يستهلكها التفكير أولاً، بحيث يؤدي اختيار قيمة عشوائية إلى نقل خطر الصفحة الفارغة بدلاً من تقليله.

  - **النموذج الذي يفرض التفكير يتلقى أدنى مستوى جهد يقبله، وقد تقرر ذلك بناءً على قياس عملي.** كان الخيار الأول هو عدم إرسال أي شيء، لتجنب التخمين نيابة عن النموذج. وعند اختباره على `z-ai/glm-5.3-flash`، الذي تكون القيمة الافتراضية له في الدليل هي `max`، أدى هذا الخيار إلى مخرجات **مقتطعة عند 32,768 رمزًا** قبل انتهاء الترجمة — فُقدت لغتان من أصل أربع عشرة. ولم يكن لزيادة السعة الإجمالية أي فائدة: فالجهد يخصص نسبة مئوية منها، ويتسع التفكير مع زيادتها. لذلك يقرأ المزود `supported_efforts` أثناء الفحص المسبق ويطلب أدنى مستوى، مع التراجع إلى «لا شيء» عندما لا يُعلن الدليل عن أي قيمة صالحة للاستخدام. اختبار التحقق العكسي على اللغة التي حدث فيها الخطأ: كانت تفشل بسبب استنفاد الميزانية، وأصبحت الآن تنجز في 9 دقائق ببنية مطابقة للمصدر.

  - **عطل المستضيف الأصلي أصبح يُذكر باسمه الصريح الآن.** يقوم الموجه بتوحيد هذه الحالة إلى `finish_reason=error` مع قيمة فارغة لـ `native_finish_reason` — تم قياسها مرتين، على لغتين، عند 750 ثانية بالضبط. كانت الرسالة العامة تدفع للبحث عن خلل في المستند أو في التقسيم؛ أما الآن فهي توضح أن العطل من جانب المزود وأن إعادة المحاولة غالبًا ما تكفي.

  - **`finish_reason=length` مع مخرجات فارغة ليس اقتطاعًا.** بل هو استهلاك الميزانية بواسطة التفكير قبل ظهور أول حرف مفيد — تم قياس 15,850 رمز تفكير مقابل 148 رمزًا مفيدًا. وتتطلب الحالتان إجراءين متعاكسين: فتقليل حجم المقاطع لا يفيد بشيء في الحالة الأولى. وتفرق الرسالة بينهما صراحةً. وهناك حارسان آخران مستمدان من القياسات: يستجيب الموجه بـ **200 مع نص لا يحمل سوى خطأ** عندما يفشل المستضيف الأصلي (كان `choices[0]` يطلق `TypeError` غامضًا يحجب الرسالة)، وتُقرأ نافذة السياق من الدليل ثم تُسجل في `MODEL_TOKEN_LIMITS` — `DEFAULT_TOKEN_LIMIT` غير صحيح لـ 44 نموذجًا في الدليل، اثنان منها بحد أقصى 4,095 رمزًا.

  - **`--model fournisseur/modèle` إلزامي، ويتم التحقق من صيغته قبل إجراء أي اتصال بالشبكة.** ليس OpenRouter مزودًا منفردًا: فاختيار النموذج يحدد السعر والترخيص ومعالجة البيانات، ولا يتم اتخاذه نيابة عن المستخدم. وبما أن المعرّف المختصر يُدمج في عنوان URL للفحص المسبق، فإن التحقق ليس مجرد تحسين لتجربة الاستخدام، بل هو حارس أمني يمنع حقن المسارات: فالتعبير النمطي ذو نطاق الأسماء المشترك بين كلا الموجهين يقبل `a/b/..`، ومن هنا جاء الرفض الصريح للمسار الأب. `--eco` ليس له أي تأثير، ويصرح بذلك.

  - **تصحيح ثلاث صياغات، إحداها كانت خاطئة.** كان تحذير OpenAI بشأن `codex exec` يستهدف حقن ملف جلسة شخصي على بيئة تشغيل مشتركة، وليس الطبيعة العامة للمستودع: فقد استُشهد به بمعنى معاكس في ملف README، وفي CLAUDE.md وفي الكود. كما تغير موقع مصادقة OpenCode في الإصدار 1.18.27 (الجدول `credential` في `opencode.db`، وليس `auth.json` بعد الآن)؛ وظلت القاعدة الثابتة «لا تُقرأ هنا أبدًا» صحيحة، لكن العنوان كان قديمًا. وأخيرًا، لم يعد قسم OpenCode يعرض مسارات لم يتم اختبارها مطلقًا على أنها متكافئة: فقد تم قياس بوابة Zen وOllama بالكامل من البداية إلى النهاية، بينما لم يتم ذلك لـ GitHub Copilot وLM Studio وllama.cpp، ويوضح ملف README ذلك الآن.

  - **حملة قياسات، وجدول بالنماذج الموصى بها في ملف README.** تم تنفيذ أكثر من ثلاثمائة ترجمة على ثلاث مجموعات من المستندات — مقال مدونة مكثف في الوضع `--news`، وملف README هذا بصيغة Markdown القياسية، وأربعة ملفات README لمشاريع مشهورة مأخوذة كما هي من GitHub — إلى أربع عشرة لغة. يميز الجدول بين أمرين كان يتم الخلط بينهما: الترجمات التي **تكتمل بنجاح** وتلك التي تكون **بنيتها مطابقة** للمصدر. ثلاثة نماذج لم تفقد أي معلومة على الإطلاق في المستندين المكثفين: `gemini-3.7-flash`، و`gpt-5.6-sol` عبر اشتراك ChatGPT، و`z-ai/glm-5.2` عبر OpenRouter — وكانت الاختلافات الوحيدة لديهم هي زوج من `**` لم يُنقل في لغة أو لغتين. والاستنتاج الرئيسي هو أن **العامل الحاسم هو كثافة المستند، وليس الوضع `--news`**: فنموذج Grok عبر الاشتراك يفشل في ثلاث عشرة مرة من أصل أربع عشرة على مقال المدونة وينجح في أربعة عشر ملف README عامًا من أصل ستة عشر، والسبب هو تعثره في المقاطع الطويلة وهو ما تم التحقق منه باختبار العكس. ويتضمن الجدول تحذيره الخاص: فهو ليس شاملاً، وتاريخه محدد، والمدد الزمنية لا تُستخدم في التصنيف، وتظل الطريقة المثلى هي إجراء القياسات على مستنداتك الخاصة.

  - **تم تصحيح أداة مقارنة البنية قبل نشر أرقامها، لأنها كانت تنتج إيجابيات كاذبة في النصوص غير اللاتينية.** فرابط URL متبوع بقوس كامل العرض `）` لم يكن يُفصل بتعبير نمطي يتوقف عند `)`، وكانت السلسلة المستخرجة تختلف رغم تطابق رابط URL؛ كما أن اقتباسًا من خمسة أسطر بالفرنسية يُختصر إلى ثلاثة أسطر بالصينية كان يؤدي إلى فشل في العد السطري. تم التحقق من كلا التصحيحين باختبارات عكسية: فحذف رابط URL أو قسم أو كود مضمن يظل قابلاً للاكتشاف. ودون هذه التصحيحات، لكان قد نُشر لـ Gemini وCodex تحقيق إحدى عشرة لغة من أصل أربع عشرة بدلاً من ثلاث عشرة واثنتي عشرة.

  - **الاختبارات**: ملف جديد `tests/test_openrouter_provider.py` (76 اختبارًا) — التحقق من صحة النموذج ورفض المسار الأب، وتثبيت المستضيفين (الحد الأقصى، الحالة، الحد الأقصى غير المصرح به، الحد الأدنى المشترك)، و`allow_fallbacks` دائمًا false، وتعطيل التفكير أو تركه وفقًا لـ `mandatory`، وعقد المخرجات الكامل (خطأ ضمن استجابة 200، عدم وجود أي خيار، التمييز بين الصفحة الفارغة والاقتطاع، `finish_reason` غير طبيعي، محتوى فارغ)، وإغلاق آمن عند الفشل للفحص المسبق عند تعذر الوصول إلى الدليل أو غياب المعرف المختصر أو غياب مستضيف سليم، وحصرية الراية وتسمية اسم الملف. مجموعة الاختبارات الكاملة بلغت **502 اختبار**.

  - **إعادة هيكلة (Refactor): تقسيم الوحدة الفردية المكونة من 4,253 سطرًا إلى وحدات متعددة، دون تغيير سطر واحد من السلوك.** تم توزيع `src/aipmt/translate.py` على `config`، `markdown`، `segmentation`، `guards`، `placeholders`، `news`، `prompts`، `notes`، `naming`، `pipeline`، `cli` وحزمة فرعية `providers/` (وحدة لكل provider، مع `base` كأساس، و`registry` للحل والإرسال dispatch). كل عملية نقل تمثل commit مع إثبات آلي: يقوم مدقق بمقارنة شجرة القواعد المجردة (AST) لجميع عُقد المستوى الأول في الحزمة بلقطة مرجعية snapshot، ويتحقق من موقع كل رمز، وبقاء علامات الأمان حرفيًا (verbatim)، وغياب أي ملفات غير متتبعة — أدوات مؤقتة ستُزال في الإصدار التالي. ما تغير وبات مرئيًا: أصبحت `aipmt.translate` واجهة (façade) تعيد كشف الأسماء الـ 64 التي كانت الوحدة تعرضها بدون بادئة `_`، وفقًا لمطابقة الكائنات (تحتوي `__all__` على تسعة منها، وهي واجهة برمجة التطبيقات المدعومة؛ والباقي أسماء مستعارة للتوافقية)، والتوقف عن إعادة تصدير أسماء الاعتماديات والمكتبة القياسية البالغ عددها 29 التي كان `import *` يجمعها؛ لم يعد التشغيل المباشر للملف (`python src/aipmt/translate.py`) موجودًا — ويظل `aipmt` و`python -m aipmt` هما الصيغتان المدعومتان؛ أصبح `__module__` للدوال العامة هو الخاص بوحدة تعريفها؛ ويتم استيراد حزم SDK بعد تحميل `.env` وليس قبله، دون أي عواقب معروفة. تم الحفاظ على جميع الاختبارات البالغ عددها 427 بنفس المعرّفات وترحيلها إلى الوحدات التي تختبرها: 91 تصحيحًا (patches) كانت تمر عبر الواجهة باتت تستهدف الآن الوحدة التي تستعلم عن الاسم (اثنان منها ظلا باللون الأخضر بدون الترقيع، وفق القياس)، وتُحكم سبعة اختبارات للعقود إغلاق الواجهة، كما أُعيدت كتابة أدوات بوابات الفحص (gates) قبل أول عملية نقل حتى لا يتحول أي منها إلى الأخضر نتيجة التوقف عن التحقق — نطاق Lizard في دلائل مع حد أدنى، قراءة الأعلام من المحلل المبني، حد أدنى للتغطية لكل حزمة، و`release.sh` الذي يعدد الوحدات المتابعة.
  - **إصلاح (مراجعة طلب السحب pull request)**: يرفض OpenRouter إدخال الفهرس الخالي من `context_length` بدلاً من تسجيل القيمة الافتراضية البالغة 128,000 رمز كقيمة قياسية — ما كان يُعطّل أيضًا تحذير «النموذج غير مدرج» — وعندما يكون `finish_reason` فارغًا (النوع الموثق `string | null`)، فإن السبب الخام للمستضيف هو المعتمد، حيث يساوي `max_tokens` القيمة `length`؛ وأي خطأ يرافقه الخيار نفسه يرفض المحتوى الجزئي المصاحب له، بنفس رسالة العطل في المصدر الأساسي القياسي — متضمنًا تفاصيل المستضيف والسبب الخام والنصيحة معًا. يرد OpenCode بخطأ العقد الخاص به، وليس بـ `AttributeError`، عند حدث `part: null`، ويرفض تدفق JSONL الذي يحتوي على سطر حدث غير قابل للقراءة بدلاً من قبول نص جزئي. تقوم واجهات سطر الأوامر (CLI) الوكيلة الثلاث بإنهاء مجموعة الوكيل عندما تتلقى هذه العملية `SIGTERM` أثناء الاستدعاء — إذ كان `timeout` الخاص بإعادة التوليد يترك الوكيل يعمل ويستهلك حصته — ويتبع `SIGKILL` الخاص بالمجموعة دائمًا مهلة السماح، حيث يموت الـ shim بنظافة تاركًا العملية التابعة له قيد التشغيل. تم إعادة ربط الزوج `# fmt: off` / `# fmt: on` الذي انفصل نتيجة التقسيم؛ وتذكر تعليمات المساعدة لـ `--reasoning_effort` موفري الخدمة الأربعة الذين يستخدمونه.
  - **إصلاح (مراجعة ثانية)**: أصبح حد المخرجات المطلوب من OpenRouter يحجز الآن جزء السياق الذي يشغله كل من التوجيه (prompt) والمقطع — يغطي `context_length` المدخلات والمخرجات معًا، وكانت ستة نماذج في الفهرس تُرسل طلباتها دون مساحة كافية للمدخلات؛ ويتم رفض السياق القصير للغاية قبل احتساب أي تكلفة. يعود أساس واجهات CLI الوكيلة إلى استخدام `terminate` ثم `kill` في البيئات التي لا تتوفر فيها مجموعات عمليات POSIX، بدلاً من السماح لـ `AttributeError` بتجاوز حماية انتهاء المهلة (timeout) واستمرار الانتظار. يُبحث عن علامة `429` لـ OpenCode كعدد وليس كسلسلة فرعية، حيث كان معرّف خطأ مثل `err_84290b` يؤدي إلى تأخير تراجعي (back-off) مدته 90 ثانية قبل أن يفشل في النهاية. وأخيرًا، يتم عرض نقطة نهاية OpenRouter غير القياسية أثناء الفحص الأولي (preflight): إذ يكفي وجود `.env` في المشروع لتعيينها، وهو ما كان سيتسبب بإرسال المفتاح الحقيقي لاحقًا.
  - **أمان: لم يعد بإمكان `.env` الخاص بالمشروع إعادة توجيه استدعاءات واجهة برمجة التطبيقات (API).** يبحث `find_dotenv(usecwd=True)` عن الملف بدءًا من الدليل الحالي والدلائل الأصلية: وكان بإمكان بنية أدلة غير موثوقة — مثل مستودع تم استنساخه للتو — أن تضع فيه `OPENROUTER_BASE_URL` أو `XAI_BASE_URL` أو `OPENAI_BASE_URL` (حيث يُقرأ الأخير بواسطة SDK نفسه) دون معرفة أي مفتاح، ليتم إرسال المفتاح الحقيقي، القادم من البيئة أو إعدادات المستخدم، لاحقًا في ترويسة المصادقة إلى خادم جهة خارجية. يعتمد الفلتر على نمط (PATTERN) وليس على قائمة: بناءً على إحصاء حزم SDK المثبتة، تتم قراءة اثني عشر متغير توجيه، ستة منها يقرؤها عميل Anthropic وحده — وكان حصرها يدويًا سيتجاهل نصفها. لذلك، يُرفض من طبقة المشروع أي متغير ينتهي بـ `_BASE_URL` أو `_API_BASE` أو `_ENDPOINT`، والبروكسيات ومخازن الشهادات (إذ إن الإشارة إلى جهة اعتماد خاضعة للتحكم تجعل المعترض لا يمكن تمييزه عن الخادم الحقيقي)، بالإضافة إلى `XDG_CONFIG_HOME` و`APPDATA`: إذ إن تعيينهما كان يعادل تحديد الملف الذي يشكل طبقة المستخدم، وبالتالي تجاوز الفلتر بطريقة ملتوية. لا تُقبل هذه المتغيرات إلا من الطبقتين اللتين يتحكم فيهما المستخدم: البيئة المصدرة و`~/.config/aipmt/.env`. بالإضافة إلى ذلك، تُقرأ طبقة المشروع دون استيفاء (interpolation): يقوم `load_dotenv` بتوسيع `${VAR}` افتراضيًا، وكان ملف `.env` غير موثوق يحتوي على `NOM_ANODIN=${OPENAI_API_KEY}` يعيد نسخ المفتاح الحقيقي تحت اسم لا يتعرف عليه فلتر الأنماط للعمليات الفرعية — ليدخل بذلك في بيئة `codex exec`، خلافًا للقاعدة المعلنة. وأخيرًا، لا يعرض الرفض سوى اسم المتغير: إذ كان عنوان URL بصيغة `https://${CLE}@hôte/` يسرب المفتاح المستوفى في السجلات، حتى أثناء رفضه. ويتم توضيح الرفض عبر stderr مع الخطوات المتبعة — حيث يُعلن عن خادم ترحيل المؤسسة (enterprise relay) في إعدادات المستخدم.
  - **إصلاح: يتم حساب حد المخرجات لـ OpenRouter لكل استدعاء.** يغطي `context_length` المدخلات والمخرجات معًا، والاحتياطي الثابت المعاير على النصوص اللاتينية لا يضمن أي حدود: وفق القياس باستخدام أداة تجزئة الرموز (tokenizer) `o200k_base`، فإن 16,000 حرف تعادل 3,200 رمز باللغة الفرنسية، لكنها تعادل 12,300 باليابانية و17,500 بالرموز التعبيرية (emoji). لذا يتم تحديد الميزانية من النص المرسل فعليًا، مع زيادتها بعدد بايتات UTF-8: في جميع أدوات التجزئة المعتمدة على دمج البايتات (byte-pair encoding على مستوى البايت، وSentencePiece مع الرجوع للبايت، وهي الفئات المستخدمة في الفهرس)، يعادل الرمز الواحد بايتًا واحدًا على الأقل، وهي الزيادة التقديرية الوحيدة المتاحة هنا، حيث يوجه OpenRouter إلى عشرات من أدوات التجزئة غير المعروفة. لم تكن أي نسبة متوسطة ملائمة — فالرسم الفكري في المستوى التكميلي ينخفض إلى 1.33 بايت لكل رمز، وحرف الدمج ينخفض إلى 1.00 —، وباتت المدخلات مع المخرجات تتسع الآن داخل النافذة تلقائيًا بحكم التصميم. ويتم رفض أي مقطع شديد الكثافة بالنسبة للنموذج المختار قبل الاستدعاء بدلاً من رفضه بعد احتساب التكلفة.

- **1.12.0** مزود الخدمة `--use_opencode`: OpenCode، الوكيل مفتوح المصدر، نحو المزود الذي يختاره المستخدم — نموذج محلي، مجاني بدون حساب، اشتراك أو مفتاح (2026-09-04):

  - **مسار المزود الثامن، ذو طبيعة مختلفة عن المسارات السبعة الأولى.** [OpenCode](https://opencode.ai) (ترخيص MIT) ليس مزود نماذج بل هو _موجّه_ (router) نحو النماذج التي ضبطها المستخدم في OpenCode نفسه: مفتاح API، أو اشتراك (GitHub Copilot، وChatGPT، وSuperGrok)، أو بوابة OpenCode Zen — التي توفر نماذج مجانية **بدون حساب** —، أو نموذج **محلي** (Ollama، وLM Studio، وllama.cpp). يتحكم السكربت في `opencode run` في الوضع غير التفاعلي، تمامًا كما يتحكم في Codex وGrok، ويعيد استخدام نفس أساس العمليات الفرعية (مجموعة عمليات خاصة، و`SIGTERM` ثم `SIGKILL` عند انتهاء المهلة، وstdin مغلق دائمًا، وبيئة منقاة). تم التحقق عبر **ترجمتين فعليتين**: ترجمة ملف README هذا بالكامل إلى الإنجليزية عبر `opencode/mimo-v2.5-free` — خلال 49 ثانية، بتمريرة واحدة، وبنية مطابقة للملف المصدري (32 عنوانًا، و26 إغلاق كتلة برمجية، و18 رابطًا، و37 عنوان URL، و37 سطر جدول، و135 نصًا برمجيًا مضمنًا) — وملف تجريبي عبر `ollama/qwen2.5:7b`، محليًا، دون أي مفتاح.

  - **`--model provider/modèle` إلزامي، وهو خيار مقصود.** بدون `--model`، يعود OpenCode إلى خياره الافتراضي، وهو في التثبيت النظيف `opencode/big-pickle`، نموذج مجاني "خفي" قد تُستخدم المحادثات معه للتدريب — وبحسب القياس، كان هذا النموذج هو الذي يجيب. واختيار ذلك بصمت نيابة عن المستخدم يمثل تمامًا نوع التبديل الخفي الذي يتتبعه هذا المستودع؛ لذا تذكر رسالة الخطأ الأمر الذي يسرد النماذج (`opencode models`) وثلاثة أمثلة (محلي، مجاني، اشتراك). `--eco` ليس له أي تأثير وتوضح الرسالة ذلك. ويتم تمرير `--reasoning_effort` كما هو كـ `--variant` لـ OpenCode، فقط عند الطلب الصريح.

  - **عزل مقاس ومثبت، وليس مفترضًا.** يُعرّف التكوين المضمن (`OPENCODE_CONFIG_CONTENT`، وهو الأخير في ترتيب الدمج لـ OpenCode، وبالتالي له الأولوية على تكوين المستخدم دون استبداله) وكيلاً `aipmt` يتم فيه رفض جميع الأدوات (`permission: {"*": "deny"}`): لم يعد السجل يعرضها أصلًا على النموذج، الذي عند مطالبته بـ "سرد الملفات وتشغيل `id`" يجيب بأنه لا يملك أي أدوات. مشاركة الجلسة معطلة، والإضافات الخارجية مستبعدة (`--pure`)، وبدون `--auto` أبدًا، ودليل عمل فارغ ومؤقت. تم رصد عمليتي حقن صامتتين وقطعهما: بدون `OPENCODE_DISABLE_CLAUDE_CODE`، يدخل ملف `~/.claude/CLAUDE.md` الخاص بالمستخدم في **كل** توجيه (515 رمز إدخال بدلاً من 186 لعبارة "مرحبًا" بسيطة)؛ وبدون `OPENCODE_DISABLE_PROJECT_CONFIG`، يدخل `AGENTS.md` في الدليل الحالي أيضًا — حيث تم تطبيق تعليمة "إنهاء كل استجابة بكلمة BANANA" على الترجمة. أما `~/.config/opencode/AGENTS.md` العام فيظل محقونًا: فلا يوجد خيار لاستبعاده، والالتفاف عليه عبر `XDG_CONFIG_HOME` مخصص سيخفي أيضًا مزودي المستخدم. تم توثيق ذلك بدلاً من اللجوء إلى حلول ترقيعية.

  - **`exit 0` لا يثبت شيئًا، ثالث واجهة سطر أوامر، نفس ردود الفعل — مع فخين خاصين بهذا المزود.** إن وجود `--agent` غير معروف لا يجعل `opencode run` يفشل: بل يكتفي بتحذير على stderr ورجوع **صامت** إلى وكيل البرمجة، مع تفعيل الأدوات. فإذا لم يُؤخذ التكوين المضمن في الحسبان، فستنطلق الترجمة بوكيل قادر على الكتابة؛ يتحقق عقد المخرجات من غياب هذه الرسالة، بالإضافة إلى: رمز الخروج 0، وعدم وجود أي حدث `error`، وعدم وجود `tool_use`، وأن يكون آخر `step_finish` هو `stop` (حيث يعتبر `length` استجابة مجتزأة)، ونص غير فارغ. الفخ الثاني: حدث الخطأ بصيغة JSON **مبهم** — «Unexpected server error. Check server logs for details.»، مع مرجع بسيط فقط — والسبب الفعلي (`ProviderModelNotFoundError: Model not found: foo/bar. Did you mean…`، `ProviderAuthError`…) لا يوجد إلا في السجلات. ومن هنا جاء `--print-logs --log-level ERROR` وقراءة حقل `error="…"` من stderr، دون تتبع أثر Bun اللاحق له. وبذلك يفشل أي نموذج غير معروف في غضون ثانية واحدة، مع تحديد سببه. كما يتجنب `--title` إجراء استدعاء LLM غير مرغوب فيه: فبدونه، يقوم OpenCode بإنشاء عنوان للجلسة عبر دورة إضافية على `small_model`.

  - **الأسرار: نفس التصفية حسب الأنماط المستخدمة مع Codex وGrok، باستثناء اسمي واحد.** يتم الاحتفاظ بـ `OPENCODE_API_KEY`: فهو المفتاح الخاص بـ OpenCode نفسه (بوابة Zen، واشتراك Go)، الموجه إليه باسمه — وهو ما يعادل `auth.json` الخاص به، وليس مفتاحًا يديره aipmt أو يمكنه فوترته. يتم تكوين موفري الخدمة داخل OpenCode (`opencode auth login`، `opencode.json`)، وليس أبدًا في ملف `.env` الخاص بـ aipmt، حيث لا يصل أي مفتاح منه إلى العملية الفرعية. لا يوجد رفض في بيئة التكامل المستمر (CI)، على عكس واجهات CLI القائمة على الاشتراكات: إذ يعتبر مفتاح API أو نموذج مستضاف ذاتيًا على runner استخدامًا مشروعًا.

  - **أصبحت حماية مسار الملفات (anti-traversal) تتحقق الآن من القيمة المستوفاة، وليس القيمة الخام.** يحتوي `provider/modèle` على `/` كانت حماية الإصدار 1.10.0 ترفضه — وهو محق في ذلك، نظرًا لأن `--model` يُستوفى داخل اسم الملف `--include_model`. يستبدل مُعرّف اسم الملف الآن كلاً من `/` و`\` و`:` بـ `-` قبل أي استيفاء (`ollama/qwen2.5:7b` ← `ollama-qwen2.5-7b`، حيث يُعد `:` غير قانوني في Windows)، وتقوم الحماية المسبقة بفحص هذا المُعرّف: يصبح `../../evil` مجرد الاسم `doc-en-..-..-evil.md` تحت الهدف، بينما يظل `..` وحده مرفوضًا، وكذلك `--target_lang ../x`. وتظل حماية النطاق `_ensure_within_directory` هي الطبقة الثانية دون تغيير.

  - **النماذج المجانية والنماذج المحلية، ما تم قياسه.** يترجم `opencode/mimo-v2.5-free` فقرة في 16 ثانية وهذا الـ README في 49 ثانية؛ ويستغرق `opencode/big-pickle` مدة 40 ثانية لكل 200 كلمة، وظل طلبان متزامنان دون إجابة لمدة 5 دقائق في حين ينجح كل طلب بمفرده؛ ولم يستجب `opencode/nemotron-3.5-lightning-free` بأي شيء خلال 3 دقائق. ومن هنا جاء `REGEN_PROVIDER=opencode` مع إلزامية `REGEN_MODEL` وتشغيل **مهمتين (2 jobs)** بالتوازي. أما على الصعيد المحلي، غالبًا ما يقوم Ollama بضبط سياق من 4,096 رمزًا في حين تصل المقاطع إلى 16,000 حرف: لذا فإن استخدام `Modelfile` مع `PARAMETER num_ctx 32768` أمر لا بد منه، وتعتمد الجودة على النموذج — حيث قام نموذج 7B بعكس قائمة وإفساد إغلاق كتلة برمجية في الملف التجريبي، بينما حافظ نموذج البوابة على كل شيء سليمًا.

  - **لم تعد ترجمات هذا المستودع تمر أبدًا عبر واجهة برمجة تطبيقات (API) مدفوعة.** كان `regen_translations.sh` يستخدم واجهة برمجة تطبيقات OpenAI بمجرد وجود مفتاح في `.env`, ولم يكن يجعل من Codex سوى خيار تفعيل اختياري (opt-in). وهذا تمامًا ما حدث أثناء إعداد هذا الإصدار: أُرسلت الترجمات الـ 28 إلى واجهة برمجة تطبيقات OpenAI، ثم سِجل التغييرات (CHANGELOG) باللغة الهندية إلى Gemini، في حين أن اشتراك ChatGPT موجود تحديدًا لتجنب الدفع حسب الاستخدام. تم إلغاء الكشف التلقائي عن المفاتيح: **Codex هو الخيار الافتراضي، مع `gpt-5.6-sol`**، نموذج الجودة؛ وتتطلب كل من `openai` و`gemini` و`grok` استخدام `REGEN_ALLOW_PAID_API=1` بالإضافة إلى `REGEN_PROVIDER`، وهو استثناء صريح لتطبيق القاعدة عند اتخاذ القرار؛ ويفشل أي `REGEN_PROVIDER` غير معروف بدلاً من الرجوع التلقائي إلى واجهة برمجة التطبيقات. تضمن عشرة اختبارات السلوك الافتراضي والرفض والاستثناء. وقد أُعيدت الترجمات الـ 28 لهذا الإصدار عبر Codex.

  - **عزل وتوحيد آلية التراجع عند حد المعدل (rate limit back-off)** (`_retry_on_rate_limit`): كانت الحلقات الخاصة بـ Codex وGrok متطابقة باستثناء تسمياتها، وكان وجود نسخة ثالثة سيتجاوز حد التكرار المسموح به. تشتق أخطاء واجهات CLI الثلاث من نفس `_CliCallError`؛ ويمنع اختبار معين خروج أي من الثلاثة عنها، وإلا فلن تراها الحلقة المشتركة بعد ذلك.

  - **الاختبارات**: ملف جديد `tests/test_opencode_provider.py` (61 اختبارًا) — عقد المخرجات الكامل، وتراجع الوكيل، والسبب المقروء من السجلات، وإزالة تكرار أجزاء النصوص وتجاهل الأجزاء الاصطناعية، وانتهاء المهلة الذي ينهي مجموعة العمليات، والتراجع عند الخطأ 429، وإلزامية النموذج والتحقق منه، والفحص الأولي الخالي من الأسرار، وحل المسار الثنائي، وربط الإرسال dispatch، وتسمية اسم الملف واختبار التحقق العكسي من مسار الملفات. يوسع `tests/test_review_hardening.py` حصرية الأعلام وغياب الأسرار لتشمل المزود الجديد. وتتطلب بوابة الفحص (gate) الآن **22 علمًا (flags)** موثقًا في argparse. بلغت المجموعة الكاملة **382 اختبارًا**.

- **1.11.1** تصحيح التوثيق: يُعلن README أخيرًا عن مسارات المزودين السبعة (2026-09-03):

  - **كانت صفحة PyPI للإصدار 1.11.0 تنص على « 4 واجهات برمجة تطبيقات (APIs) + واجهة سطر الأوامر (CLI) لـ Codex ».** يكشف الكود البرمجي عن سبع واجهات — OpenAI وMistral وClaude وGemini وGrok عبر API؛ وCodex (ChatGPT) وGrok عبر الاشتراك، دون فوترة قائمة على الاستخدام. كان وضعا Grok مفقودَين في الجملة الترويجية ونقطة _Multi-Provider_، وكانت الترجمات الـ 14 تكرر هذا الخطأ. ونظرًا لأن الوصف المطول للحزمة يكون مجمدًا لكل إصدار، فقد تطلب تصحيح الواجهة إصدار رقم جديد: وهذا هو السبب الوحيد لوجود هذا الإصدار. **لا توجد أي تغييرات في الكود البرمجي.**
  - تم ضبط `CLAUDE.md` ليتوافق مع ما استحدثه النشر: عدادات بوابة الفحص (16، و17 في `--full`)، وسير العمل (workflows) الأحد عشر النشطة، وعدادا Sonar/Codacy غير المرئيين في `gh pr checks` (النقاط الساخنة hotspots، وAPI لـ Codacy)، ونقل `# nosemgrep` بواسطة `ruff-format`، وبيئات GitHub التي يتطلبها تبادل OIDC، وحقيقة أن الـ _pending publisher_ لا يحجز الاسم.

- **1.11.0** النشر على PyPI: `pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، دون استنساخ المستودع (2026-09-03):

  - **أصبح السكربت أحادي الملف حزمة قابلة للتثبيت.** ينتقل `translate.py` من المجلد الجذري إلى `src/aipmt/translate.py`، مع نقطة دخول للكونسول `aipmt` والمكافئ `python -m aipmt`. يظل المستودع المستنسخ ضروريًا للمساهمة — حيث تتواجد الاختبارات، و28 ترجمة، وأدوات الجودة — ولكن لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس أبدًا `translate`**، لأن التعارض حقيقي وصامت. فحزمة PyPI المسماة `translate` (الإصدار 3.8.1، آخر رفع 2026-07-06) تثبت دليلاً يحمل الاسم نفسه. تمت إعادة إنتاج المشكلة داخل venv: يتفوق الدليل على الوحدة، ويختفي `translate.main`، وتتعطل نقطة الدخول مع الخطأ `AttributeError` — ويرد `pip check` بـ « No broken requirements found » مع rc=0. كان مجرد تنفيذ `pip install translate` لدى المستخدم كافيًا لتعطيل واجهة سطر الأوامر (CLI) دون أي تشخيص قابل للاستفادة منه. الفحص المضاد على ملف wheel الفعلي: `pip install translate` فوق الحزمة، و`aipmt --help` يعطي rc=0 قبل وبعد، وتتعايش كلتا واجهتي سطر الأوامر معًا.
    - **اسم توزيع طويل، وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للعثور عليها عبر بحث PyPI؛ فأي اسم مقتصر على أوائل الحروف فقط سيكون غير قابل للعثور عليه لمن لا يعرف المشروع مسبقًا، في حين أن الهدف من النشر هو العثور عليه بالتحديد. تم استبعاد مرشحين محتملين بعد التحقق: فاسم `ai-markdown-translator` محجوز على npm منذ 2024 لأداة لها الغرض نفسه، وهو سابق لهذا المستودع بـ 17 شهرًا؛ واسم `aimt` يفصله حرف واحد عن `aim` (الإصدار 3.29.1)، وهي حزمة نشطة في المجال نفسه — وهو أسوأ سيناريو لحدوث ارتباك دائم. فخ منهجي بالمناسبة: يُرجع `pypi.org/project/<nom>/` الرمز 200 لأي اسم (صفحة الحماية من البوتات)، وتبقى واجهة برمجة التطبيقات JSON هي المرجع المعتمد الوحيد.
    - **تخطيط `src/` بدلاً من الحزمة المسطحة.** كان من شأن الحزمة المسطحة الحفاظ على عناصر `sys.path.insert(..., "..")` الستة الخاصة بالاختبارات، وهذا هو العيب تحديدًا: فهي تجعل الاختبارات تستورد شجرة المصدر بدلاً من الحزمة نفسها، مما يخفي أي خطأ في التحزيم. التكلفة الفعلية: قاعدة استبدال إضافية واحدة.

  - **أخيرًا، يتم تكوين المفاتيح لمرة واحدة وإلى الأبد.** لم تكن واجهة سطر الأوامر المثبتة تمتلك أي إعدادات دائمة: وكان المتاح فقط هو متغير البيئة وملف `.env` في الدليل الحالي. وبالتأكيد، كان `find_dotenv` يصعد حتى جذر النظام وبالتالي يعثر على `~/.env` **عند العمل داخل الدليل الشخصي للمستخدم**، ولكن لا شيء عند العمل في مكان آخر — وهي تغطية تعتمد على المكان الذي أُطلق منه الأمر، وليست خيارًا تصميميًا مقصودًا. لذا تمت إضافة طبقة ثالثة: `~/.config/aipmt/.env`، أسفل الطبقتين الموجودتين.

    - **الأولوية ليست مكتوبة برمجياً بشكل صارم**، بل تنبع من `override=False`، وهي القيمة الافتراضية لـ `load_dotenv`: فكل طبقة تملأ فقط ما تركته الطبقة السابقة فارغًا. ومن هنا جاء الترتيب: متغير البيئة ← `.env` الخاص بالمشروع ← إعدادات المستخدم، وقد تم التحقق من ذلك عبر اختبارات السلوك وليس البنية — فعكس ترتيب الاستدعاءين يؤدي إلى فشلها، وكذلك يؤدي حذف الطبقة الثالثة إلى الفشل.
    - **صيغة `.env` وليست TOML**، عن عمد: فـ `python-dotenv` يُعد بالفعل من التبعيات، والصيغة موثقة مسبقًا في ملفات README الـ 15، ويُستخدم الملف نفسه للنطاقين. لا توجد تبعيات ولا صيغ جديدة. يتبع المسار `XDG_CONFIG_HOME` إذا كان **مطلقًا** — فالمواصفات تتطلب تجاهل أي قيمة نسبية، وإلا لأصبح موقع الإعدادات معتمدًا من جديد على الدليل الحالي — و`APPDATA` تحت نظام Windows.
    - **خياران تم استبعادهما، مع بيان السبب.** سلسلة المفاتيح الخاصة بالنظام (`keyring`) أكثر أمانًا على جهاز سطح المكتب ولكنها تفشل في البيئات غير الرسومية (headless) — كالخوادم، والحاويات، وبيئات التكامل المستمر (CI) — أي حالة الاستخدام الأساسية للترجمة المجمعة على وجه التحديد؛ مرشح جيد كخيار اختياري (opt-in)، وسيئ كإعداد افتراضي. بينما الراية `--api-key` ستجعل المفتاح ينتهي به المطاف في سجل سطر الأوامر (shell history) ويصبح مرئيًا في `ps`.
    - **دون وجود مفتاح، لا مزيد من تتبع المكدس (traceback).** كان المستخدم يتلقى تتبع مكدس Python يشير إلى `site-packages`، ورسالة تذكر «البيئة أو .env» دون تحديد مكان إنشاء الملف الأخير. والآن، تُعدّد الرسالة المواقع الثلاثة بمساراتها الدقيقة، ويخرج الأمر بالرمز 2. شبكة الأمان **مضيقة عمدًا**: `except ValueError` تطبق فقط على مرحلة الإعداد والتكوين. فتغليف التنفيذ بأكمله كان سيحول أي خطأ برمجي حقيقي يحدث أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الفشل الذي يتتبعه هذا المستودع ويمنعه. يقوم اختبار بقراءة كود المصدر لـ `main()` لحظر ذلك.

  - **تصحيح — كان يتم تجاهل ملف `.env` الخاص بالمستخدم بمجرد تثبيت الأداة.** إن `load_dotenv()` بدون وسائط لا يصعد من الدليل الحالي بل من الملف المُستدعي، وبالتالي من `site-packages`. تم قياس ذلك باستخدام نقطة دخول كونسول حقيقية تم تشغيلها من مشروع يمتلك ملف `.env` الخاص به: يُرجع `find_dotenv()` القيمة `''` ولا يتم تحميل المفتاح، في حين يعثر عليه `find_dotenv(usecwd=True)`. لم يكن هذا الخطأ موجودًا طالما كانت الأداة تعمل فقط من المستودع المستنسخ؛ وكان النشر سيجعله خطأً نمطيًا ودائمًا، وعَرَضه الوحيد هو ظهور رسالة مفتاح API «مفقود» رغم صحة الإعدادات.

  - **ثلاث بوابات فحص (gates) كانت ستعطي الضوء الأخضر رغم توقفها التام عن فحص أي شيء.** لقد تم تشديدها قبل النقل عمدًا: فحاجز الحماية المكتوب بعد التغيير المفترض أن يرصده لا يثبت شيئًا. كل منها يعطي اللون الأخضر على المستودع الأصلي ويتحول إلى الأحمر على النسخة المنقولة — وقد تم قياس كلا الاتجاهين.

    - **يتجاهل Lizard أي مسار مفقود بصمت تام**: rc=0، و« 0 file analyzed ». كانت بوابة التعقيد ستتحول من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع مخرجات بحجم صفر بايت. أصبح النطاق الآن مصفوفة يتم التحقق من وجود كل عنصر فيها.
    - **`coverage run --source=` على وحدة مفقودة لا يفشل**: تحذير على stderr فقط، وrc=0 لكل من unittest و`coverage xml`، ويُنشر التقرير رغم ذلك — مع تقليصه من 1453 إلى 141 عبارة برمجية. كان المشروع سيبدو سليمًا لمجرد أنه لم يعد يُفحص تقريبًا. أصبح هناك حدان أدنيان يحميان التقرير: الإجمالي، وأكبر ملف تم قياسه.
    - **مسبار حداثة الترجمات أعمى هيكليًا عن صيغة الاستدعاء**: فهو يرتكز على رايات argparse، وهي بالضبط ما لا يغيره تغيير اسم الملف. تمت إعادة إنتاج المشكلة: تم نقل الوحدة، بينما لا تزال ملفات README الـ 15 توثق أمرًا غير موجود، والنتيجة «لا توجد ترجمات قديمة». لذلك، يتحقق قسم سابع من الصيغة وليس الخيارات، وتتم مواجهة خطاف Lizard مع النطاق الفعلي للسكربت — فمفتاحه `files:` لا يُفشل pre-commit عندما لا يعود يطابق، بل يجعله يتخطاه تمامًا.

  - **`requires-python = ">=3.10"` لم يعد مجرد ادعاء.** كان `sonar-project.properties` يعلن بالفعل عن دعم 3.10-3.12 دون أن يختبر ذلك أي إجراء على الإطلاق، نظرًا لأن جهاز التطوير يحتوي على 3.12 فقط — وهو تناقض داخلي كان النشر سيجعله علنيًا. يقوم سير عمل الاختبارات الآن بتشغيل المجموعة على الإصدارات 3.10 و3.11 و3.12، مع تثبيت الحزمة نفسها وبالتالي حدودها العامة المعلنة.

  - **حدود دنيا، دون وضع سقف أعلى.** يظل `requirements.txt` هو ملف القفل المختبر، ويصبح `[project.dependencies]` هو العقد العام: فنشر الإصدارات الدقيقة لملف القفل كان سيسبب تعارضات لدى أي مستخدم لديه حزم أخرى. ولا يوجد سقف أعلى `<N+1` أيضًا — فهو سيتعارض بشكل مباشر مع `check-deps-fresh.sh`، الذي يُفشل بوابة الإصدار عند أي تأخر عن إصدار رئيسي (major). تحل مجموعة الحدود الدنيا هذه المشكلة، ويخرج الفحص المضاد `openai==1.0.0` بـ `ResolutionImpossible`، مما يثبت أن الفحص يميز الخطأ بدلاً من قبول كل شيء. بالإضافة إلى ذلك، يمنع حاجز حماية إصدار `pyproject.toml` من الاختلاف عن إصدار CHANGELOG: إذ لا يسمح PyPI بإعادة استخدام رقم إصدار سابق.

  - **تم التحقق منه بالكامل في بيئة venv جديدة**: ملف wheel بحجم حوالي 70 كيلوبايت يحتوي فقط على `aipmt/*.py`، ومعلومات dist-info والترخيص؛ و`aipmt --help` برمز خروج rc=0 مع 22 راية؛ و`python -m aipmt` يعرض « usage: aipmt » وليس « usage: \_\_main\_\_.py »؛ وتثبيت `pipx` يعمل بشكل سليم؛ والأهم من ذلك **ترجمة فعلية من الفرنسية إلى الإنجليزية (fr→en) من أي دليل مستخدم عشوائي**، مع الحفاظ على الخط العريض والقوائم والكود المضمن والروابط وعناوين URL، ودون ترجمة كتل الأكواد البرمجية. تجتاز الاختبارات الـ 318 السابقة للهجرة بنجاح مع قائمة معرفات مطابقة حتى مستوى البايت قبل/بعد — وهذا هو ما يثبت عدم تعطيل أي اختبار، وليس مجرد ظهور كلمة « OK »؛ مع إضافة اثني عشر اختبارًا لإعدادات الطبقات الثلاث، ليصبح المجموع 330 اختبارًا.

- **1.10.0** مزود `--use_codex` (حصة اشتراك ChatGPT)، وتحديث حزم SDK والنماذج، وإصلاح اقتباسات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — حاجزا حماية وضعتهما طلبات السحب (PR) دون الالتزام بهما في كل مكان**:

    - **كان الفحص الأولي (preflight) لـ Codex يمرر كامل `.env` إلى الملف التنفيذي.** كان `_codex_preflight` يستدعي `subprocess.run` **بدون `env=`**: وكانت العملية الفرعية ترث كامل `os.environ`، وبالتالي كل محتوى `.env` الذي تم تحميله بواسطة `load_dotenv`. تم قياس ذلك بملف تنفيذي وهمي ومُراقب: وصلت **سبعة أسرار** إلى الفحص الأولي — مفاتيح المزودين الستة بالإضافة إلى `GITHUB_TOKEN` واحد — مقابل **صفر** لنظيره `_grok_preflight`، الذي كان يمرر `env=_grok_env()` بشكل صحيح. كان التناقض داخليًا في طلب السحب: فـ `_strip_secret_env` موجود خصيصًا للحفاظ على هذا الثابت، على بعد بضعة أسطر فقط. تم استخراج `_codex_env_base()` ومشاركته بين المسارين؛ القياس بعد التصحيح: 0 سر في كلا الجانبين.
    - **خاصية « `--deny` fail-closed » لم تكن تغطي الصيغة المستخدمة.** كان التعليق يبرر كامل عزل Grok بحقيقة أن أي قاعدة ذات بادئة غير معروفة تؤدي إلى رفض بدء التشغيل. عند القياس على `grok 1.0.13`، تبين أن هذا التحقق **لا يوجد إلا للصيغة التي تستخدم الأقواس**: إذ يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل (« unknown tool prefix »)، بينما يتم قبول `--deny 'CeciNestPasUnOutil'` بصمت. ولكن `GROK_DENY_RULES` لم يكن يستخدم سوى الأسماء المجردة — وبالتالي فإن إعادة تسمية أداة من جانب xAI كانت ستزيل طبقة العزل المقاسة الوحيدة دون أي إشعار، على جهاز لا تنطبق عليه بيئة عزل نظام التشغيل (sandbox) في الأصل. تم تحويل القواعد الثماني المسماة إلى `Prefix(*)`، مع التحقق من كل منها كبادئة معروفة لواجهة سطر الأوامر؛ وتبقى قاعدة الالتقاط الشامل `*` بصيغتها الحرفية، وهي الصيغة الوحيدة المقبولة. يمنع اختبار مخصص العودة إلى الصيغة غير المتحقق منها.
    - **تم التحقق من النظافة في النواحي الأخرى**: لا يوجد أي حقن للأوامر (استخدام صيغة القوائم في كل مكان، وعدم استخدام `shell=True` مطلقًا، وتمرير محتوى المستند عبر stdin أو `--prompt-file`)، ولا يوجد فك تسلسل غير آمن (استخدام `json.loads` فقط مع حماية النوع)، وإصلاح اجتياز المسار دون العثور على أي تجاوز عبر سبع حمولات اختبارية، وتطبيق `--deny '*'` فعليًا بواسطة واجهة سطر الأوامر (تمت ملاحظة `DENY_ENFORCED` عند القراءة خارج مجلد العمل workdir).
    - تجاوز فحص الحداثة المضاف أعلاه مبدأه الخاص بالمناسبة: فالحزمة التي يفشل طلب PyPI الخاص بها كانت تُتخطى بصمت مع إعطاء البوابة الضوء الأخضر. وهو الآن يحسب الحزم التي تمت مقارنتها بالفعل ويفشل إذا كانت التغطية غير مكتملة.

  - **ترقية التبعيات، وتوفير شبكتي أمان لمنع تكرار التأخير**:

    - **كان التأخير حقيقيًا وطويل الأمد**: `openai` من 2.54 ← **3.6.0**، و`anthropic` من 0.125 ← **1.2.0**، و`certifi` من 2024.8.30 ← **2026.7.22** — أي تأخير لمدة عامين في مخزن شهادات الجذر الذي يتحقق من TLS لجميع استدعاءات المزودين. السبب المحدد: **لم يكن هناك ملف `.github/dependabot.yml`**. وبدون هذا الملف، لا يُفعّل GitHub سوى تحديثات الأمان (_security updates_)، ولا يقترح Dependabot طلب سحب إلا لتبعية مستهدفة بثغرة أمنية (CVE) — وهو ما يفسر قيامه بترقية `urllib3` و`idna` مع ترك حزمتي SDK تبتعدان بإصدار رئيسي كامل.
    - **يتعايش الإصداران الرئيسيان دون تعارض**، على عكس ما كانت تثيره مخاوف استنتاج سابق: حيث يهاجر `openai` 3.x و`anthropic` 1.x إلى **`httpx2`** بينما يظل `mistralai` و`google-genai` على `httpx<1`، إلا أنهما توزيعتان منفصلتان. تم التحقق من ذلك عن طريق التثبيت الفعلي، ثم من خلال **مسارات المزودين السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الكود المضمن والروابط في كل مخرج. كان «تجنب وجود مكدسي HTTP» مجرد تفضيل وليس عائقًا تقنيًا: وقد حسم القياس الفعلي الأمر.
    - **لم يكن `requirements.txt` يصف البيئة الحقيقية**: كان `google-auth` و`cryptography` ومكدس `opentelemetry` مثبتة في بيئة venv للعمل دون التصريح عنها نهائيًا — وبالتالي لم يكن التثبيت النظيف يعيد إنتاج ما تم اختباره. وعلى العكس من ذلك، كان `tokenizers` و`huggingface-hub` و`PyYAML` مدرجة فيه دون استيرادها أو طلبها من قِبل أي شيء، وهي بقايا من `mistralai` 1.x. تمت إعادة إنشاء الملف ليمثل الإغلاق الكامل لبيئة venv المبنية من التبعيات المباشرة فقط. ولا يُظهر `pip-audit` أي ثغرات أمنية معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** (جديد) يفعل تحديثات الإصدارات الأسبوعية لـ pip وgithub-actions. يتم تجميع الإصدارات الفرعية والتصحيحية في طلب سحب واحد — فطلب سحب منفصل لكل تصحيح ينتهي به الأمر مهملاً، والضجيج هو عدو التحديثات المستمرة؛ بينما تكون **الإصدارات الرئيسية منفصلة**، ويتطلب كل منها التحقق عبر استدعاء فعلي.
    - **`scripts/check-deps-fresh.sh`** (جديد، ومربوط ببوابة الفحص) يجعل التأخير مرئيًا في نتيجة فحص المشروع: يقترح Dependabot التحديثات لكنه لا يضمنها، ويمكن أن تتراكم طلبات السحب الخاصة به. التأخر في إصدار رئيسي ← فشل الفحص؛ إصدار فرعي ← تحذير، لأن بقاء البوابة باللون الأحمر باستمرار يؤدي في النهاية إلى تجاهلها؛ تعذر الوصول إلى PyPI ← تخطٍ صريح محليًا و**فشل فوري ومحكم (fail-closed) في بيئة CI**، حيث إن الفحص الذي لم يُنفّذ لا يُعد نجاحًا. تم التحقق في كلا الاتجاهين: فهو يرصد الحالة الدقيقة لما قبل التصحيح (`openai 2.54.0→3.6.0`، `certifi 2024.8.30→2026.7.22`) ويكتفي بالتحذير عند وجود تأخر في إصدار فرعي.

  - **تصحيحات ناتجة عن مراجعة طلب السحب هذا** — قام خمسة وكلاء مراجعة بفحص الفروق (diff) بدقة؛ وقد تم **إعادة إنتاج النقاط الموضحة أدناه عبر القياس الفعلي** قبل تصحيحها، وكان اثنان منها عبارة عن تراجعات أُدخلت في وقت سابق من هذا الإصدار نفسه.

    - **تم تصحيح تراجع — كان `_NEWS_CITATION_REGEX` يعاني من تتبع خلفي (backtracking) أسي.** أدخل الإصلاح متعدد الفقرات `(?:[ \t]*$|[ \t]+.*)` في التكرار: تقاسم المسافات بين `[ \t]+` و `.*` غامض، ويتضاعف هذا الغموض من تكرار إلى آخر. تم القياس على أسطر `>   texte` — وهي إزاحة Markdown صالحة تمامًا — لا تطابق النمط: **2,589 مللي ثانية لـ 14 سطرًا** مقابل 0.04 مللي ثانية بعد التصحيح، بمعامل ~9 لكل سطر إضافي. في وضع `--news`، كان اقتباس كتلي (blockquote) طويل وغير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة (job timeout)، دون سبب يمكن تحديده. يستهلك التكرار الآن السطر بأكمله دفعة واحدة (`\n^>(?![ \t]*—).*`)، مما يترك طريقة واحدة فقط للمطابقة لكل تكرار. تم التحقق على مجموعة حقيقية تضم 231 مقالاً: **صفر فروقات** في الالتقاط، ونفس الـ 423 اقتباسًا، والـ 14 نصًا متعدد الفقرات لا تزال ممتدة.
    - **رايتان متزامنتان لمزود الخدمة (provider flags) كانتا تفرضان فوترة حسب الاستخدام، بصمت.** كان `--use_codex --use_mistral` مقبولاً؛ يختبر `_select_provider_client` مزود Mistral أولاً، بينما يمنح `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكلاهما كان يتقارب نحو Mistral. بالتالي كان المستخدم يطلب حصة اشتراكه ولكنه يحصل على فوترة حسب الاستخدام، دون أي تحذير: بالضبط نمط الفشل الذي وُجد `--use_codex` لمنعه. تمر رايات المزودين الست الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: سطر الأوامر الذي يجمع بين مزودين، والذي كان يُقبل بصمت حتى الآن، يفشل الآن مع `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة (gate) نهاية العمل تعطي ضوءًا أخضر عندما تتعطل مجساتها.** أربعة من أصل ثلاثة عشر فحصًا في `scripts/check-release-ready.sh` اتبعت نمط «التقاط stdout، والاستنتاج إذا كان فارغًا» دون مراجعة رمز الإرجاع (return code) مطلقًا: كان هناك استثناء (ملف تمت إعادة تسميته، `FileNotFoundError`) يكتب على stderr، ويترك stdout فارغًا، ويخلص الفحص إلى «لا شيء للإبلاغ عنه». فخ «أن `exit 0` لا يثبت شيئًا» تكرر داخل السكريبت المكتوب لمنعه. يفرض المساعد `probe()` الآن رمز إرجاع صفريًا **و**حارس نهاية (sentinel)، وترفض المجسات الاستنتاج بشأن مجموعة معالم فارغة — إذ يكون التأكيد على مجموعة فارغة صحيحًا دائمًا. إثبات: أدت إضافة المجموعة الحصرية أعلاه إلى تمرير رايات المزود عبر كائن `*_group`، والذي لم يعد التعبير النمطي القديم `parser\.add_argument\(` يطابقه؛ حيث خرجت **ست رايات من أصل إحدى وعشرين** من النطاق بصمت، مع إعطاء البوابة ضوءًا أخضر.
    - **فحص الأسرار (secrets scan) كان يفوّت أربعة مزودين من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: كان `sk-proj-…` (تنسيق OpenAI الحالي) و `sk-ant-api03-…` يتعطلان عند الشرطة الثانية، ولم يكن `AIza…` مغطى. تم توسيع النمط، واستُبعد `.secrets.baseline` من الفحص. إضافة إلى ذلك، كانت الحراسة `.env` تستعلم عن `git diff --cached`، الذي لا يرى سوى الفهرس (index): وبالتالي فإن `.env` **المُثبت بالفعل (already committed)** — وهو أسوأ الحالات — لم يكن يظهر فيه أبدًا. وهي تستعلم الآن عن `git ls-files`.
    - **«تهيئة الرمز المميز (token warm-up)» لـ Codex لم تكن كذلك في الواقع.** مقاسًا: لا يمس `codex login status` الملف `~/.codex/auth.json` (mtime والحجم لم يتغيرا)، وتذكر تعليمات المساعدة الخاصة به «Show login status». مع ذلك، كان التعليق يزعم أنه يحدّث الرمز المميز «مرة واحدة، بشكل متسلسل»، مما يحيد خطر التحديثات المتزامنة على رمز دوار أحادي الاستخدام. الحماية المعلنة لم تكن موجودة؛ يصف التعليق الآن ما يفعله الكود بالفعل، ويظل الحل الحقيقي هو `max_jobs=4`. علاوة على ذلك، يراعي الفحص الآن `CODEX_BIN`، والذي كان يتجاهله سابقًا — حيث كان أي جهاز لا يحتوي على `codex` في `PATH` يفشل برسالة «غير موثق»، وهو تشخيص مضلل.
    - **تم تضمين (source) `.env` داخل غلاف فرعي (subshell).** يتم استدعاء `detect_provider` كاستبدال للأوامر، بحيث لم تكن عمليات التصدير (exports) الخاصة بها تنتقل إلى الأعلى: ظل أي `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` محدد في `.env` غير مرئي لعمليات القراءة المنفذة في `main()`، والتي كانت تخلص إلى «تعذر العثور على الملف الثنائي لـ Grok» رغم صحة التهيئة.
    - **تجاوز التزامن الحد المعلن بنسبة 50%.** تم وضع الحراسة بعد إطلاق زوج README/CHANGELOG: بلغت الذروة المقاسة **3 لـ `max_jobs=2`**. في Grok، الذي تُشارك حصته الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يتم الالتزام بالحد الذي يفرضه السكريبت على نفسه. أما العدد النهائي، فكان يُعرض دون مقارنته مطلقًا بـ 28 — فكان الملف المفقود يمر دون أن يُلاحظ.
    - **عقد مخرجات Grok: غياب `stopReason` يُعد الآن فشلاً.** كان الكود يطبق «`end_turn` **أو غائب**» في حين يتطلب العقد المعلن `end_turn`. كانت الحمولة (payload) الخالية من الحقل — أو التي تتم إعادة تسمية حقلها عبر تحديث لـ CLI — تحول الحراسة إلى عملية غير فعالة صامتة (no-op). بالإضافة إلى ذلك، لم يعد `max_turn_requests` مصنفًا كحد معدل طلبات (rate limit) (بل إن ميزانية الأدوار قد استُنفدت: وإعادة المحاولة تعيد نفس النتيجة، مع تكلفة انتظار قدرها 90 ثانية)، وتمت إزالة `quota` من علامات حد المعدل — للسبب الذي أوضحته بالفعل سلسلة التوثيق (docstring) الخاصة بـ `_codex_is_rate_limited` دون أن يطبقه Grok.
    - **تمت ميمومة (memoization) تتابع Gemini حسب النموذج.** كان يبدأ من جديد من `minimal` عند كل مقطع، على الرغم من أن النموذج الافتراضي يرفضه: كان المسار الاسمي يدفع ثمن رحلة ذهاب وإياب بخطأ 400 لكل مقطع ويعيد طباعة التحذير نفسه. التحذير المتكرر مئات المرات يتوقف المرء عن قراءته — وهكذا يتحول إلى قناع يحجب المشاكل.
    - **متفرقات**: كانت رسالة الرفض في التكامل المستمر (CI) مكتوبة بشكل ثابت (hardcoded) لـ Codex وتوجه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلاً من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و «Openai»؛ كما كان تعليق أساس العمليات الفرعية يعمم مفهوم «shim» على كلا واجهتي سطر الأوامر (CLI) في حين أن الملف الثنائي لـ Grok هو ملف ELF أصلي (المبرر الصحيح هو «وكيل يفرّع عملياته الفرعية الخاصة»)؛ تم تعليم اثنتي عشرة نتيجة من نتائج SAST على `subprocess` كـ `# nosec` / `# nosemgrep` مع تقديم التبرير، حيث إن صيغة القائمة بدون `shell=True` تجعل الحقن مستحيلاً ولا يمر محتوى المستند عبر argv أبدًا.
    - **لم يعد أي سر يدخل إلى العمليات الفرعية للوكيل (agentic subprocesses).** لم تكن قوائم الحظر بالاسم تحمي سوى ثابت **الفوترة** (Codex بدون `OPENAI_API_KEY`، وGrok بدون `XAI_API_KEY`). مقاسًا: كانت **سبعة أسرار أخرى** تدخل مع ذلك إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح الـ CLI الآخر، و `OPENAI_BASE_URL`، وهو ليس سرًا ولكنه يعيد توجيه حركة المرور. والحال أن هذين الـ CLI هما **وكلاء (agents)**، وعميل Grok يعمل بدون بيئة معزولة (OS sandbox) قابلة للتطبيق على العديد من أجهزة Linux. يتم التصفية الآن **حسب نمط الاسم** (`API_KEY`، `_TOKEN`، `SECRET`، `PASSWORD`، `CREDENTIALS`) وليس عبر قائمة بالأسماء، مما يغطي أيضًا المتغيرات التي يضيفها المستخدم في `.env` الخاص به دون علم هذا الكود بها. لا توجد حاجة لأي منها في أدوات CLI: فالمصادقة تعيش في `~/.codex` و `~/.grok`، ولا توجد أبدًا في البيئة — تم التحقق من ذلك من خلال **ترجمة حقيقية ناجحة** عبر كلا المزودين مع البيئة المحصنة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يُحكم إغلاق حصرية رايات المزودين، وعقد `stopReason`، وخطية التعبير النمطي للأخبار، ورسالة الرفض في CI، وميمومة Gemini، وغياب أي سر في بيئة العمليات الفرعية. هذا التأكيد الأخير **عام (generic)** — فهو يفشل عند وجود مفتاح لا تسميه أي قائمة — في حين كان اختبار التنقية الحالي مرآة لثوابته ولم يكن بإمكانه اكتشاف أي شيء سوى تعطل حلقته الخاصة. ارتفعت المجموعة الكاملة إلى **311 اختبارًا**.

  - **مزودان جديدان لـ Grok**: `--use_grok` (واجهة برمجة تطبيقات xAI، المفتاح `XAI_API_KEY`، مع فوترة حسب الاستخدام) و `--use_grok_cli` (أداة CLI الرسمية Grok Build، تُخصم من اشتراك Grok — نفس مبدأ `--use_codex`).
    - **وضع API، حوالي 40 سطرًا**: نظرًا لتوافق نقطة نهاية xAI مع OpenAI، تتم إعادة استخدام العميل و `_call_openai` كما هما، مع تغيير `base_url` فقط. كان هناك تعديل واحد ضروري واستفاد منه الجميع: يقبل `finish_reason` الآن `end_turn`، وهي الصيغة الصادرة عن xAI حيث تصدر OpenAI الصيغة `stop`. النماذج: `grok-4.6` (الجودة) و `grok-4.3` (الاقتصادي). تجدر الإشارة إلى أن النموذج الاقتصادي لـ Grok يظل الأغلى في المستودع — $1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ `mistral-small-latest`: يتم اختيار هذا المزود لتنوع النماذج، وليس من أجل السعر.
    - **وضع CLI**: مصمم على غرار Codex، مع أربعة اختلافات فرضها الواقع العملي — يتم إرسال المحث (prompt) عبر ملف (`--prompt-file`، لا يقرأ الـ CLI من stdin وسيكون أي مقطع في argv مرئيًا في `ps`)، والمخرجات عبارة عن كائن JSON وحيد على stdout (لا JSONL ولا ملف `-o`)، والاشتراك لا يعرض سوى `grok-4.6` و `grok-4.5`، ولا يمكن تطبيق بيئة المعاينة المعزولة (sandbox) (انظر أدناه). تم تجميع إطلاق العمليات الفرعية في دالة مشتركة مع Codex في `_codex_run_process`، دون المساس ببقية مزود Codex الذي تم اختباره بالفعل.
    - **`exit 0` لا يثبت شيئًا، مقاسًا**: عند عدم التوثيق، يكتب الـ CLI الرسالة `{"type":"error","message":"Not signed in."}` على **stdout** مع رمز إرجاع **0**. ويتصرف الرفض أو تجاوز الأدوار بنفس الطريقة. لذلك يتطلب عقد المخرجات أربعة شروط متزامنة: رمز إرجاع 0، وعدم وجود حمولة خطأ، و `stopReason == end_turn`، ونص غير فارغ. يتبع الفحص المسبق (preflight) نفس المنطق: يخرج `grok models` بالرمز 0 حتى عند قطع الاتصال، وفقط وجود عبارة «not authenticated» في stdout هو ما يتيح الاستنتاج.
    - **العزل (Confinement): تباين مقصود وموثق.** في حين يعمل Codex في وضع `--sandbox read-only`، فإن بيئة العزل (sandbox) لـ Grok غير قابلة للتطبيق على العديد من أجهزة Linux الحديثة، وذلك لسببين مستقلين في النظام لا يمكن تجاوزهما بدون `sudo`: يحظر AppArmor مساحات أسماء المستخدمين (user namespaces) غير المميزة منذ Ubuntu 24.04 (`bwrap: setting up uid map: Permission denied`، أُعيد إنتاجها خارج Grok)، وتفشل قائمة حظر مقابس بيئة تشغيل الحاويات عندما يكون `/run/podman` في وضع `0700` (حيث لا يلتقط المحلل سوى `ErrorKind::NotFound`، ويصبح خطأ EACCES قاتلاً). الفخ الرئيسي: الملف الشخصي **المدمج** الذي لا يمكن تطبيقه **يبدأ العمل دون عزل في صمت**. لذلك لا يطلب السكريبت أي ملف شخصي افتراضيًا ولا يتراجع بصمت أبدًا — بل يصدر تحذيرًا على stderr. تعتمد الحماية على قواعد `--deny` الخاصة بـ CLI، بما في ذلك القاعدة الشاملة `*`، وهي الطبقة الوحيدة التي تم قياسها بأنها _fail-closed_ (أي قاعدة ببادئة غير معروفة تؤدي إلى رفض بدء التشغيل). يتيح `GROK_TRANSLATE_SANDBOX=read-only` فرض ذلك، وفي هذه الحالة يفشل بدء التشغيل إذا كان الجهاز لا يستطيع الالتزام به.
    - **إجراءات وقائية**: تتم إزالة `XAI_API_KEY` و `GROK_API_KEY` و `GROK_SANDBOX` من بيئة العملية الفرعية (أي مفتاح سيؤدي إلى التحول إلى الفوترة حسب الاستخدام؛ وتوريث `GROK_SANDBOX` سيفرض ملفًا شخصيًا غير قابل للتطبيق مع رسالة مضللة)، وتعطيل مفاتيح تبديل MCP/الخطافات (hooks)/المهارات (skills)/الوكلاء (agents)، و `--disable-web-search`، و `--no-subagents`، و `--no-plan`، ودليل عمل للاستخدام الواحد (disposable workdir)، ورفض في CI، ومهلة توقف تنهي مجموعة العمليات (process group)، وتراجع تدريجي (back-off) عند حد المعدل. تم تعيين `--max-turns` إلى 6 وليس 1: حيث يتم زيادة العداد بعد دور الأدوات، وكان التعيين إلى 1 سيقتطع المخرجات.
    - **الحصة (Quota)**: حصة Grok أسبوعية و**مشتركة مع Chat وImagine وVoice**، ولا يوجد أمر يعرضها — على عكس Codex حيث يتيح `account/rateLimits/read` قياس الاستهلاك رقميًا. لذلك يقيد `regen_translations.sh` التزامن إلى 2 ويحذر صراحة.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` (24 اختبارًا). ارتفعت المجموعة الكاملة إلى **290 اختبارًا**.
  - **تم إصلاح خطأ — كانت الاقتباسات الإنجليزية (EN) متعددة الفقرات محمية جزئيًا فقط (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل، كنص للاقتباس، سوى سلسلة من أسطر `>` **المتتالية**. بمجرد أن يغطي الاقتباس عدة فقرات (مفصولة بسطر `>` فارغ)، كان يتم التقاط السطر الأخير فقط واستبداله بالنائب (placeholder)؛ بينما كانت الأسطر السابقة تُرسل إلى LLM وتعود مترجمة — وهو عكس ما وُجد `--news` لضمانه تمامًا. يقبل التكرار الآن الأسطر `>` الفارغة الداخلية وأصبح غير شَرِه (non-greedy)، ليتوقف عند `>` الفارغ الذي يسبق السطر المائل وليس عند أول سطر يصادفه.
    - **النطاق المقاس** على مجموعة حقيقية تضم 198 مقالاً: 11 اقتباسًا متأثرًا من أصل 419. لا يوجد أي تراجع — يلتقط التعبير النمطي الجديد نفس عدد الاقتباسات تمامًا، وتتمدد النصوص متعددة الفقرات فقط (408 نصوص متطابقة، و11 ممتدة)، ولا يمكن استيعاب سطر الإسناد `> — …` في النص (تم الحفاظ على الاستشراف للأمام / lookahead).
    - **إثبات شامل من البداية إلى النهاية (end-to-end)** على مقال بحجم 69 كيلوبايت مترجم إلى اليابانية/العربية: الفقرة الأولى من الاقتباس، التي كانت تُعرض سابقًا كـ `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم بالمثل إلى العربية، تظل الآن `> GLM-5.3 is now open-weight.`. ويعود عدد أسطر الاقتباس باللغة الإنجليزية من 9 إلى 10، وهو ما يطابق المصدر.
    - يُشار إلى أن: هذا الخلل لم يتم اكتشافه بواسطة أدوات التحقق اللاحقة، والتي تتحقق من وجود الاقتباس دون التأكد من اكتماله.
  - **توفير مقاس في المزود الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ النموذج بـ `gpt-5`، بما في ذلك في `--eco`. القياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: `medium` → 45 رمز استدلال (reasoning tokens) و65 رمز مخرجات؛ `none` → 0 و14. الاستدلال لا يضيف شيئًا للترجمة، وكان يتم دفع ثمنه على كل مقطع في كل ملف. تصبح القيمة الافتراضية `none` في `--eco` وتظل `medium` خلاف ذلك؛ وتظل القيمة الصريحة الممررة عبر CLI ذات أولوية. يقبل `--reasoning_effort` الآن `none` و `xhigh` بالإضافة إلى `low`/`medium`/`high` (لا تقبل جميع النماذج كافة القيم: فـ `minimal` مرفوض مثلاً من قِبل `gpt-5.4-mini` — وتغطي إعادة المحاولة بدون معلمات الحالية هذه الحالة).
  - **تحديث حزم SDK والترحيل إلى Gemini**: تم استبدال `google-generativeai` (انتهى دعمه في 2025-11-30، وأُرشف المستودع) بحزمة SDK الموحدة **`google-genai`** — `genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير محث النظام في `system_instruction` بدلاً من دمجه في المقطع. تمت ترقية `mistralai` إلى **2.9.4** (أصبح الاستيراد `from mistralai.client import Mistral`؛ والاستيراد القديم يثير `ImportError`، تم التحقق في حزمة wheel)، و `anthropic` إلى **0.125.0** و `openai` إلى **2.54.0** — أحدث الإصدارات قبل التحول إلى `httpx2`، لتجنب التعايش بين حزمتي HTTP في البيئة الافتراضية (venv). تم إلغاء حظر `httpx` 0.28.1 و `pydantic` 2.13.5 تبعًا لذلك.
  - **تراجعان تم اكتشافهما عبر اختبارات فعلية، وليس عبر الوثائق**:
    - يرفض `anthropic` ≥ 1.0 من جانب العميل أي استدعاء غير متدفق (non-streamed) يتوقع فيه `max_tokens` استغراق أكثر من 10 دقائق (`ValueError: Streaming is required...`). لم يكن هذا الإجراء الوقائي موجودًا في 0.34.2 وكان يعطل أي استدعاء لـ Claude باستخدام `max_tokens=32768`. تم التصحيح عبر `timeout` صريح (`CLAUDE_TIMEOUT`، 900 ثانية افتراضيًا)، مما يتجنب التبديل إلى التدفق (streaming) لاستدعاء لا نستخدم منه سوى الاستجابة الكاملة.
    - لا يُقبل `thinking_level="minimal"` إلا من خلال جزء من كتالوج Gemini: حيث يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و `gemini-3.1-pro-preview` بخطأ 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تتابع متتالي `minimal` → `low` → لا يوجد thinking_config، على غرار آلية التراجع الاحتياطي (fallback) الخاصة بـ OpenAI الموجودة بالفعل — لا ينبغي أبدًا لمعلمة تحسين أن تتسبب في فشل الترجمة.
  - **تحديث النماذج الافتراضية**، تم التحقق من كل منها عبر استدعاء فعلي: OpenAI `gpt-5.5` → **`gpt-5.6-terra`** (−60% في دفعة من 28) و `gpt-5.4-mini` → **`gpt-5.6-luna`** (−73%)؛ Claude `claude-sonnet-4-6` → **`claude-sonnet-5`** (أرخص وأحدث) و `claude-haiku-4-5-20251001` → **`claude-haiku-4-5`** (معرّف قياسي بدون تاريخ)؛ Gemini `gemini-3.1-pro-preview` → **`gemini-3.7-flash`** و `gemini-3.1-flash-lite-preview` → **`gemini-3.1-flash-lite`** (نسخة مستقرة، وأرخص من `3.5-flash-lite`).
 ظلت Mistral دون تغيير، مع بقاء `mistral-large-latest` هو الأفضل من حيث نسبة الجودة إلى السعر بين الأربعة. تجدر الإشارة إلى: لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — فنموذج Gemini 3.5 Pro، الذي أُعلن عنه في مايو 2026، لم يصدر أبدًا؛ حيث تقتصر السلسلة 3.5/3.6/3.7 حصريًا على Flash.
  - **قياس A/B قبل التبديل إلى Gemini**: تُرجم `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. بنية متطابقة تمامًا (21 قائمة، 18 كتلة برمجية، 13 رابط HTML، 13 صورة، مع الحفاظ على جميع عناوين URL)، في **8 ثوانٍ مقابل 48 ثانية**. ونظرًا لعدم وجود أي اختبار أداء عام يقارن بين هذين النموذجين في الترجمة أو النصوص غير اللاتينية، كان التبديل سيعتمد لولا ذلك على مجرد افتراض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفذ `block.text for block in response.content` دون تصفية النوع. تُدرج نماذج التفكير التكيفي (Sonnet 5 وما بعدها) كتلة `thinking`، التي تعرض `.thinking` وليس `.text` — وكانت الترجمة ستتعطل بسبب `AttributeError` غير الشفاف في المقطع الأول. يتم الآن استبعاد الكتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` (قائمة استبعاد سلبية، للبقاء متسامحًا مع أي نوع غير معروف يحتوي على نص)، كما تؤدي الاستجابة الخالية من أي كتلة نصية إلى إطلاق خطأ صريح. يتم تمرير `thinking={"type": "disabled"}` مع كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: حذف النماذج التي انقضى تاريخ إيقافها (تمت إزالة فئة `magistral-*` في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620`، و`claude-3-7-sonnet-20250219`، و`claude-opus-4-1-20250805`، و`claude-sonnet-4-20250514`). تصحيحات الحدود: Mistral من 128K ← **256K** (جيل Large 3 / Small 4)، وGemini من 1,000,000 ← **1,048,576** (حد الإدخال الفعلي)، و`claude-opus-4-5` من 200K ← **1M**، وعائلة `gpt-5.6-*` من 400K ← **1.05M**. إضافة Claude 5 (`claude-sonnet-5`، `claude-opus-5`، `claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest` وسلسلة `ministral-*`. تجدر الإشارة إلى: تظل هذه الحدود إرشادية، حيث يضع `translate()` سقفًا للتقسيم عند `min(16000, limite)`.

  - **المزود `--use_codex`**: مزود خامس يتحكم في واجهة سطر الأوامر (CLI) الرسمية لـ Codex (`codex exec`) في وضع غير تفاعلي، بدلاً من استدعاء واجهة برمجة تطبيقات (API) مسبوقة الدفع حسب الاستخدام. يتم خصم الترجمة من حصة اشتراك ChatGPT المدفوع مسبقًا. هذا هو المسار الوحيد الموثق من OpenAI لهذا الاستخدام: تسرد مصفوفة التوفر حسب الخطة "Codex SDK, `codex exec`, and scriptable workflows" على أنها متاحة في Plus/Pro/Business/Enterprise، بينما لا تصادق رموز `~/.codex/auth.json` على استدعاءات Platform API (ولا تتم قراءتها مطلقًا بواسطة هذا السكربت — حيث تظل المصادقة وتحديثها مُدارة بواسطة الـ CLI).
  - **ملف Codex التنفيذي قابل للتثبيت عبر pip، وليس عبر npm فقط**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم `PATH`، ثم حزمة بايثون الرسمية **`openai-codex-cli-bin`** المنشورة بواسطة OpenAI (وهي التبعية لـ SDK `openai-codex`). وبالتالي لم يعد مشروع Python بحاجة إلى تثبيت npm عام لاستخدام `--use_codex`. لم تتم إضافة الحزمة إلى `requirements.txt`: يبلغ حجم الملف التنفيذي حوالي 250 ميجابايت، وهو ما كان سيُفرض على جميع المستخدمين لمزود اختياري. تم التحقق من البداية إلى النهاية: مع غياب `codex` من `PATH`، يعثر البحث على الملف التنفيذي المضمن وتكتمل الترجمة بالكامل في 6 ثوانٍ.
  - **ضمان "وضع الاشتراك"**: تتم إزالة `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. بدون هذا الإجراء الوقائي، يمكن لمفتاح موجود في `.env` تحويل Codex إلى الفوترة حسب الاستخدام دون أي إشارة مرئية — وهو بالضبط ما وُجد هذا المزود لتجنبه.
  - **معالجة مآزق الـ CLI المحمية بالاختبارات**:
    - يقرأ `codex exec` الدخل القياسي stdin **حتى** عندما يُمرر التوجيه كمعامل: بدون إغلاق stdin، ينتظر الأمر حتى انتهاء المهلة دون استدعاء النموذج على الإطلاق (تمت إعادة إنتاجه: خروج برمز 124 بعد 180 ثانية، بصفر بايت). لذا فإن `communicate(input=...)` إلزامي.
    - ملف `codex` المثبت عبر npm هو مِعْقَب (shim) لـ Node ينفذ `spawn` للملف التنفيذي الحقيقي المكتوب بلغة Rust: وهذا الأخير هو **عملية حفيدة** لعملية Python ويظل باقيًا بعد `SIGKILL` لـ `subprocess.run(timeout=)`، حيث سيستمر في استهلاك الحصة. ومن هنا جاء استخدام `Popen(start_new_session=True)` + `os.killpg`.
    - يمكن للـ CLI الخروج برمز 0 مع إصداره لـ `turn.failed`: يتم فحص مخرجات JSONL (`--json`) بالإضافة إلى رمز الإرجاع، ويؤدي غياب الملف `-o` مع رمز 0 إلى إطلاق خطأ صريح بدلاً من إنتاج مقطع فارغ.
  - **التراجع التدريجي (Back-off) عند تجاوز حد المعدل**: لا ينفذ الـ CLI أي إعادة محاولة داخلية (`max_retries = 0`). يتم التصنيف بناءً على بنية حمولة JSON (`status: 429` / `error.type`)، وليس على سلاسل فرعية — حيث تظهر كلمة "quota" في كل من خطأ 429 القابل للاسترداد وفي خطأ `insufficient_quota` النهائي.
  - **حماية التكامل المستمر (CI)**: يتم رفض `--use_codex` إذا تم تحديد `CI` أو `GITHUB_ACTIONS`. المصادقة القائمة على الاشتراك ليست مخصصة لبيئات التشغيل المشتركة (shared runners)، وتوصي OpenAI صراحة بعدم استخدام سير العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` (`--eco`). عائلة `gpt-5.6-*` مشتركة بين الـ CLI وPlatform API، لكن حساب ChatGPT لا يمتلك حق الوصول إلى كل شيء فيها: يتم تطبيق القائمة المسموح بها من جانب الخادم دون تحقق محلي، ويؤدي استخدام نموذج غير معتاد إلى إطلاق تحذير. في خطة Plus، توفر Luna من 250 إلى 2,000 رسالة لكل نافذة مدتها 5 ساعات مقابل 10 إلى 100 لـ Sol: يُعد `--eco` الوضع الموصى به لأي معالجة مجمعة.
  - **إصلاح خطأ — كان `regen_translations.sh` يخرج بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` لـ `main()` لم يعد موجودًا في لحظة تنفيذ فخ الالتقاط (trap). تحت `set -u`، أدى ذلك إلى إطلاق `failed_log: unbound variable` وخروج السكربت برمز 1 رغم أن الترجمات الـ 28 كانت صحيحة — وهو ما كان سيتسبب في مقاطعة `release.sh --auto` (`set -e`) مباشرة بعد إعادة التوليد، في المرحلة الأكثر استهلاكًا للموارد. أصبح المتغير عامًا ويتحقق فخ الالتقاط من وجوده. تأثير جانبي مفيد: إخفاقات الترجمة الحقيقية، التي كانت محجوبة حتى الآن بسبب هذا الخطأ، أصبحت مرئية مجددًا في الملخص النهائي.
  - **`REGEN_MODEL`**: متغير بيئة جديد لـ `regen_translations.sh` يفرض نموذجًا محددًا فوق الإعداد الافتراضي للمزود، على سبيل المثال `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج المتقدم من حصة الاشتراك بدلاً من النموذج `--eco` الموجه للأحجام الكبيرة.
  - **`regen_translations.sh`**: ميزة `REGEN_PROVIDER=codex` متاحة عبر اشتراك صريح (لا تُكتشف تلقائيًا أبدًا، لعدم استهلاك حصة الاشتراك دون علم المستخدم). يتم تحديث الرمز المميز مرة واحدة بالتسلسل قبل بدء المعالجة المتوازية — نظرًا لأن تحديث Codex دوري وللاستخدام الفردي، فإن المهام المتزامنة قد تبطل جلسة `codex login` — وتم خفض مستوى التزامن إلى 4.
  - **إعادة صياغة مرتبطة**: تم تقليص معلمات `_dispatch_provider_call` من 8 إلى 6 عبر `_resolve_provider()` الذي يُرجع اسم المزود، بدلاً من قيمة منطقية رابعة يتم تمريرها عبر السلسلة بأكملها. تظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` بالحد الأدنى من `Namespace`.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` (48 اختبارًا) يغطي argv، والبيئة المنقحة، وعقد منع التمهيد، والإخفاقات الصامتة، وtimeout/killpg، والتراجع التدريجي، والتحقق المسبق (preflight)، وتحديد المزود، وتسلسل التفكير لـ Gemini، وتصفية كتل Claude، واقتباسات الأخبار متعددة الفقرات. أصبحت المجموعة الكاملة تضم 290 اختبارًا.
  - **التحقق الفعلي**: ترجمة `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex تعطي بنية متطابقة تمامًا مع الترجمات المرجعية (14 كتلة برمجية، 24 عنوانًا، 25 صفًا في الجداول، 13 رابط HTML، 13 صورة، 19 عنوان URL، كتل برمجية متطابقة حرفًا بحرف، وانعدام تام لبقايا العناصر النائبة). بالنسبة لمقال إخباري بحجم 69 كيلوبايت في وضع `--news`، يجتاز كل من مخرجات `gpt-5.6-luna` و`gpt-5.6-sol` أداة التحقق التطبيقية التابعة للغات en/ja/ar. الاستهلاك المقاس عبر `account/rateLimits/read`: ظل تحت عتبة التقريب للعداد (0% من نافذة الـ 5 ساعات) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL للإحالة الإخبارية مع الأقواس المتداخلة أو البادئة الفرنسية (2026-05-11):

  - **إصلاح الخطأ**: كان استخراج عناوين URL للإحالة في `_protect_news_quotes` يستخدم التعبير النمطي (regex) `re.search(r"\((.+?)\)", attribution)` (التقاط كسول بين قوسين). وفي إحالة من النوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` الشاملة + `]()` لرابط Markdown)، كان الالتقاط يتوقف عند أول `)` يواجهه ← سلسلة مجتزأة + متضمنة البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (بدون `)` النهائي). النتيجة: كان `_validate_news_post` يبحث عن هذه السلسلة في المخرجات المترجمة ويفشل بانتظام (لسببين: اجتزاء `)` + ترجمة "relayé par" إلى `relayed by` / `weitergeleitet von` /...). وبالتالي لم يكن بإمكان التسلسل الكامل low ← medium ← high ← gpt-5.5 أن ينجح.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — يستهدف تحديدًا `](url)` لرابط Markdown، ويلتقط **عنوان URL النقي فقط** (بدون بادئة فرنسية أو اجتزاء)، وهو ثابت محفوظ بواسطة العناصر النائبة `#URL{N}#` أثناء الترجمة. قوي في التعامل مع النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية بدون أقواس شاملة
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py` الفئة `TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة الدقيقة التي تم إعادة إنتاجها لخطأ Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (صيغة بديلة مع `via`)
  - **تغطية الثغرة**: يتحقق `check-editorial-coverage.py` من الصيغة التحريرية ولكن ليس من قابلية الترجمة بواسطة المترجم. أحد التحسينات المحتملة (خارج نطاق v1.9.2) هو إجراء فحص يحاكي استخراج الإحالة في تشغيل تجريبي (dry-run) لاكتشاف الأنماط المحفوفة بالمخاطر قبل النشر.

- **1.9.1** إصلاح التدويل (i18n) لتسمية CTA في ملاحظة ترجمة marker (2026-05-10):

  - **إصلاح الخطأ**: كانت التسمية `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط marker أعلى الملفات المترجمة تظل **باللغة الفرنسية** لجميع اللغات المستهدفة، بدلاً من اتباع `target_lang`. لا يراها نموذج اللغة الكبير (LLM) أبدًا (يتم تجميعها من جانب Python للحفاظ على عنوان URL والـ slug للمستودع)، وبالتالي لم تتمكن مرحلة الترجمة من تداركها. تراجع صامت منذ إضافة التنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ 15 بتسميتها المترجمة محليًا. ينقل كل من `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` الآن اللغة المستهدفة. اعتماد احتياطي (Fallback) على `fr` إذا كانت اللغة غير معروفة (للأمان، دون حدوث KeyError).
  - **الاختبارات**: تم تعديل `test_source_emits_three_paragraphs_repo_title_description_link` (اللغة المستهدفة `ja` ← التسمية اليابانية المتوقعة). اختباران جديدان: `test_source_link_label_localized_per_target_lang` (تم ضبط معلماته عبر 7 لغات تغطي النصوص اللاتينية، والأيدوغرامية، والأبجدية) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختبارًا في `test_translation_note_position.py` (بدلاً من 38).
  - **التوافق مع الإصدارات السابقة (Backward-compat)**: توقيع دالة مع قيمة افتراضية `target_lang="fr"` — تواصل عمليات الاستدعاء البرمجية الخارجية دون `args.target_lang` العمل دون تعديل.

- **1.9** إصلاح silent-failure + أدوات جودة شاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + علامة تنسيق "embed card"**:
    - خيارات CLI جديدة (إضافية، القيم الافتراضية بدون تغيير ← **non breaking**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): تضع الملاحظة في الأعلى، أو في الأسفل، أو في كلا الموضعين من الملف المترجم.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - `legacy` يعيد إنتاج سلوك الإصدار v1.8 بدقة (فقرة بخط عريض `**…**`) **byte-for-byte**.
        - `marker` يصدر تعريف مرجع رابط Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`) متبوعًا بـ **blockquote مكون من 3 فقرات** مهيكل لعرض من نوع "GitHub repo embed card": عنوان المشروع في كود مضمن (`**\`ai-powered-markdown-translator\`\*\*`)، والوصف المترجم بواسطة LLM، ورابط CTA (`[Voir le projet sur GitHub ↗](URL)`) مع سهم مرئي. قابل للاستخدام عند البناء عبر إضافة remark (راجع مدونة jls42.org ← إضافة `remark-translation-banner`).
    - **ثوابت لا تُرسل أبدًا إلى LLM**: يتم تجميع عنوان المستودع ورابط GitHub من جانب Python بعد ترجمة الجملة الوصفية. لا يرى LLM أبدًا الـ slug `ai-powered-markdown-translator` أو `https://github.com/jls42/...`، مما يضمن عدم تغيير أي معالج عرض/حالة أحرف/مخطط (scheme).
    - **إدراج متوافق مع Frontmatter (Frontmatter-aware)**: في الوضع `top` أو `both`، يتم إدراج الملاحظة **بعد كتلة `---` للإغلاق** لـ frontmatter YAML (أمان Astro Content Collections / gray-matter). تكتشف الدالة المساعدة `_split_frontmatter` العلامة `---\n…\n---\n` في بداية الملف وتحافظ على سلامتها؛ **تثير `RuntimeError`** عند وجود frontmatter مفتوح بدون سياج إغلاق (يتم رفع الملف في `failed_files` بدلاً من كتابته بملاحظة في غير موضعها الصحيح).
    - **مُنظِّف النموذج بالقائمة البيضاء (whitelist)**: يستبدل `_sanitize_model` أي حرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع تراجع (fallback) إلى `unknown` إذا كان فارغًا. يتوافق مع أداة التحقق من جانب إضافة Astro remark ويُبطل مفعول الأحرف التي قد تكسر تنسيق العلامة (المسافة، علامة الاقتباس، القوس، الفاصلة، إلخ).
    - **إعادة هيكلة داخلية**: `_append_translation_note` (دالة واحدة متجانسة) ← 7 دوال مساعدة نقية (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فصل المنشئ/المؤلف (المنشئ يعيد كتلة نقية بدون فاصل، والمؤلف يطبق `\n\n` وفقًا للموضع)؛ ويشترك الإنتاج والدالة المساعدة للمصدر في نفس مجمع الفقرات الـ 3.
    - **`_quote_lines` المحافظ على الأسطر الفارغة**: يسبق كل سطر بـ `> `، مع تحويل الأسطر الفارغة إلى `>` بمفرده. يتيح لـ mdast رؤية 3 فقرات متميزة في blockquote (العنوان / الوصف / الرابط) بدلاً من فقرة واحدة مع فواصل أسطر.
    - **`_build_translation_note_block` تكيفي**: وفقًا لعدد الفقرات التي احتفظ بها LLM (3 = تنسيق بطاقة كامل، 2 = جملة + رابط، 1 = تراجع/fallback). التراجع المكون من فقرة واحدة **لم يعد يُغلف بـ `**...**`** عند اكتشاف رابط Markdown `](` (عرض هش لـ `<strong>` حول الرابط).
    - **التوافق مع الإصدارات السابقة**: `getattr(args, "note_position", "bottom")` و `getattr(args, "note_format", "legacy")` من جانب `_compose_with_notes` — تواصل كائنات Namespace التي لا تحتوي على هذه السمات (الاختبارات الحالية، والاستدعاءات البرمجية الخارجية) العمل دون أي تعديل.
  - **إصلاح الفشل الصامت (silent-failure) في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع المزودين (OpenAI، Mistral، Claude، Gemini): طبقة حتمية (العثور على مقتطف المصدر حرفياً) + طبقة احتمالية (`langdetect`)
    - قائمة بيضاء لـ `finish_reason` / `stop_reason`: إثارة `RuntimeError` عند أي حالة خارج القائمة البيضاء (القص / truncation، تصفية المحتوى / content_filter، إلخ)
    - `max_tokens` لـ Claude: `4096` ← `32768` (يتجنب القص الكامن في مقاطع 16k، وهامش التحويل بين النصوص المختلفة FR←JA/ZH/KO/AR/HI)
    - تقسيم مدرك للعناوين (heading-aware segmentation): أولوية H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى كود خروج غير صفري: يُرجع `translate_markdown_file` حالة محددة النوع `success` / `failure` / `skipped`، و `main()` `sys.exit(1)` إذا فشل ملف واحد على الأقل (ملف فردي والدفعات)
    - حماية المحتوى الفارغ (Empty-content guard) لدى جميع المزودين، ونسبة التحقق المنطقي بين المصدر/المخرجات (≥ 500 حرف، < 5% = رفض)، والتحقق من العناصر النائبة للكود (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بالعنوان)، وإعادة محاولة `BadRequestError` بدون `reasoning_effort`
    - إضافة التبعية `langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("من نوع EurekAI الكامل"، 14 خطافاً):
    - Pre-commit: ruff (lint+format)، shellcheck، prettier (md/yaml/json)، detect-secrets (4 مفاتيح API محمية)، Lizard (CCN ≤ 12)، pre-commit-hooks v5 (المسافات البيضاء، نهاية الملف EOF، الملفات الكبيرة، shebangs، إلخ)
    - Pre-push: mypy (الوضع المتساهل التدريجي)، Opengrep SAST (translate.py + scripts/)، pip-audit (وضع إعداد التقارير الأولي)، unittest discover (tests/ + scripts/tests/)
    - أغلفة محلية (Wrappers) في `scripts/` تستخدم `./venv/bin/python`
    - `scripts/audit_verdict.py`: محلل JSON لـ pip-audit مع 11 اختبار unittest، نقل إلى Python مقتبس من محلل jls42-astro
    - تصحيح 7 انتهاكات أولية لـ ruff: B904 (raise from) ×2، B007 (unused dirs)، C408 (dict literal)، C419 (list-comp)، SIM105 (contextlib.suppress)، SIM110 (any())
    - يستبعد Lizard مؤقتًا `translate.py` (4 دوال ذات CCN 21-47، وإعادة هيكلة مخطط لها) — بوابة صارمة على scripts/
  - **SonarCloud + تغطية شاملة**:
    - سير عمل GitHub Actions `SonarCloud` (sonarcloud.yml + sonar-project.properties): تحليل عند كل push و pull-request، والتغطية عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى ملف README (Quality Gate، تقييمات Security/Reliability/Maintainability، Coverage، Vulnerabilities، Bugs، Code Smells، Duplicated Lines، Technical Debt، Lines of Code)
    - `tests/test_silent_failure.py` (`unittest` stdlib): يغطي الحلقات الست لسلسلة خطأ silent-failure
    - `tests/test_orchestration.py` (+79 اختبارًا): يغطي طبقة التنسيق لـ `translate.py` (`_resolve_*_filename`، `_existing_translation_exists`، `_record_translation_status`، `_write_output_file`، `translate_directory`، `_validate_input_paths`، `_init_*_client`، `_select_provider_client`، `_normalize_collapsed_markdown`، `_cleanup_source_flag`، `_validate_news_flags_*`، بدائل TypeError + BadRequestError لـ `_openai_create_with_fallback`، وتنسيق موجه سلسلة o1، وتفريعات early-return لـ `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` (stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **تغطية الكود الجديد (Coverage on new code)**: 75.5% ← ~98% (translate.py 98%، scripts/audit_verdict.py 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك اختبارات E2E لـ `marker+top|bottom|both` و `legacy+top|bottom|both`)، والبادئة متعددة الأسطر، والتوافق مع الإصدارات السابقة byte-for-byte (golden literal)، والمُنظِّف، وتقسيم frontmatter (بما في ذلك إثارة استثناء عند عدم إغلاق السياج)، وتنسيق الفقرات الـ 3، والتراجع إلى فقرتين، وحماية الفقرة الواحدة + رابط Markdown، وحاجز حماية حرج `TestLLMPayloadExcludesInvariants` يؤكد عدم إرسال العنوان + الرابط إلى LLM أبدًا. **اجتياز 190 اختبارًا**، و0 تراجع.
  - التوثيق: `README.md` (الفرنسية + 14 ترجمة) مع الشارات، `CLAUDE.md` (سير عمل pre-commit + مراقبة CI مفصلة)، وإعادة إنشاء 28 ترجمة
- **1.8** وضع `--news` + ترقية نماذج 2026 (2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - OpenAI الجودة: `gpt-5` ← `gpt-5.4`
    - OpenAI الاقتصادي: `gpt-5-mini` ← `gpt-5.4-mini`
    - Gemini الجودة: `gemini-3-pro-preview` ← `gemini-3.1-pro-preview`
  - إضافة حدود الرموز (tokens) لـ `gpt-5.4` و `gpt-5.4-mini` و `gpt-5.4-nano` (400 ألف) و `gemini-3.1-pro-preview` (1 مليون)
  - وضع `--news` الأولي: حماية الاقتباسات الإنجليزية باستخدام عناصر نائبة `#NEWSQUOTE\d+#`، وتعيين `LANG_FLAGS` (15 لغة)، وإدارة الأعلام حسب اللغة الهدف
  - التحقق من العناصر النائبة للأخبار قبل الاستعادة (تراجع: كان النموذج الذي يحذف العنصر النائب يُنتج بصمت مخرجات بدون اقتباس)
  - جعل السكربت `regen_translations.sh` قابلاً للنقل (مسارات مطلقة، دون الاعتماد على pwd)
  - إضافة رابط اللغة الفرنسية في أشرطة اللغات بملفي README/CHANGELOG، وإعادة إنشاء 28 ترجمة
- **1.7** الميزات الجديدة:
  - الخيار `--keep_filename` للاحتفاظ باسم الملف الأصلي أثناء الترجمة
  - دعم ملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على الكود المضمن**: علامات الاقتباس المائلة (`` `...` ``) محمية الآن أثناء الترجمة
  - تحسين الموجه الأساسي للنظام (system prompt):
    - معالجة أفضل لعلامات الاقتباس في YAML frontmatter
    - حماية متغيرات القالب `{variable}`
    - حظر ملاحظات المترجم غير المطلوبة
  - تم اختباره بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** الميزات الجديدة:
  - دعم واجهة برمجة تطبيقات Google Gemini للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI: `gpt-5` (الجودة)، `gpt-5-mini` (اقتصادي)
    - Claude: `claude-sonnet-4-5` (الجودة)، `claude-haiku-4-5` (اقتصادي)
    - Gemini: `gemini-3-pro-preview` (الجودة)، `gemini-3-flash-preview` (اقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف واحد (`--file`) دون مسح دليل كامل
  - نمط تسمية مبسط جديد: `{base}-{lang}.md`
  - الخيار `--include_model` للاحتفاظ بالتنسيق القديم مع اسم النموذج
  - دعم النماذج غير المدرجة مع حد افتراضي للرموز (128 ألف)
  - ترجمة README إلى 14 لغة
- **1.5** تحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين موجهات الترجمة:**
    - تم إثراء الموجهات الخاصة بالترجمات المباشرة وملاحظات الترجمة لتحقيق مزيد من الوضوح والفعالية، بما في ذلك تعليمات مفصلة حول الحفاظ على البيانات الوصفية وعناصر التنسيق المحددة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم عمليات الاستيراد (imports) لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص ومعالجة كتل الكود للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات المخرجات:**
    - عكس موضع النموذج واللغة في اسم ملفات المخرجات (على سبيل المثال، `f"{base}-{args.target_lang}-{args.model}.md"`)، مما يسهل تنظيم الترجمات والبحث عنها.
  - **تحسينات متنوعة:**
    - تنظيف الكود بحذف الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكربت وسهولة قراءته.
- **1.4** الميزات الجديدة:
  - دعم واجهة برمجة تطبيقات Claude من Anthropic للترجمة
  - تحسين الموجهات لتحقيق مزيد من الوضوح والفعالية
  - تعديلات طفيفة لتحسين صيانة الكود
- **1.3** تحسينات وميزات جديدة:
  - تحسين إدارة كتل الكود
  - تحسين إدارة ملفات المخرجات
  - تحسين اكتشاف الملفات الموجودة مسبقًا
  - الخيار `--force` لفرض الترجمة
  - عكس موضع النموذج واللغة في اسم ملف المخرجات
- **1.2** إصلاح سجل التغييرات (changelog)
- **1.1** إضافة دعم واجهة برمجة تطبيقات Mistral AI
- **1.0** الإصدار الأولي - دعم واجهة برمجة تطبيقات OpenAI

**مقال مترجم من الفرنسية إلى العربية باستخدام gemini-3.7-flash-medium.**
