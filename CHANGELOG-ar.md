### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.14.1** Mistral: لم يعد تجاوز حد معدل الطلبات يؤدي إلى فشل الملف، وقد تأكدت النماذج الافتراضية بالقياس (2026-09-17):

  - **كان خطأ HTTP 429 يتسبب في فقدان الملف بأكمله.** لا يعيد SDK ‏`mistralai` المحاولة افتراضيًا، خلافًا لنظيريه من OpenAI وAnthropic، كما أن حدود Mistral منخفضة وخاصة بكل نموذج: 15 طلبًا في الدقيقة لـ Large 3، و100,000 token في الدقيقة لـ Small 4، وفقًا للقياس على حساب المستودع. فقدت حملة بأربع ترجمات متزامنة على Small خمس ترجمات من أصل أربع وثمانين. يعيد العميل الآن المحاولة عند أخطاء 429 و5xx وأخطاء الاتصال، مع انتظار متزايد من ثانيتين إلى 60 ثانية لمدة لا تتجاوز خمس دقائق: لا يحدد خطأ 429 من Mistral مدة الانتظار، وتُحتسب حدوده بالدقيقة. اختبار مضاد على API: عشرون استدعاءً متتاليًا على Large 3، نجحت جميعها، ونجح الاستدعاء السادس عشر بعد انتظار دام 33 ثانية.
  - **لم تعد الاستجابة المؤلفة من كتل استدلال تُفسد الترجمة.** يستخدم Small 4 وMedium 3.5 الاستدلال عند طلب ذلك منهما، وعندئذ تصبح الاستجابة قائمة من كتل `thinking` ثم `text` بدلًا من سلسلة نصية. لا يستخدم أي منهما الاستدلال افتراضيًا وفقًا للقياس، لكن أي تغيير في الإعداد الافتراضي من جانب المزوّد كان سينتهي بـ `AttributeError`: تُستبعد كتل الاستدلال، كما يحدث مع Claude.
  - **تأكيد النموذجين الافتراضيين: `mistral-large-latest` و`mistral-small-latest`.** قيس النموذج الأحدث الوحيد، Mistral Medium 3.5، مقارنة بهما عبر اللغات الأربع عشرة، على مقالة الرصد الكثيفة في جدول التوافق وعلى README. في المقالة، يفقد العناوين الفرعية أو يغيّر مستوياتها في 9 لغات من أصل 14 — ترجمة واحدة فقط بلا اختلاف، مقابل 6 لـ Large 3 و8 لـ Small 4 — كما تبلغ تكلفته ثلاثة أضعاف Large 3 في الإدخال وخمسة أضعافه في الإخراج. تشير الأسماء المستعارة إلى Large 3 ‏(`mistral-large-2512`) وSmall 4 ‏(`mistral-small-2603`).
  - **مواءمة `MODEL_TOKEN_LIMITS` مع API**: ‏262,144 token لمجموعة Mistral بدلًا من 256,000، و131,072 لـ `ministral-3b`، كما لم تعد المعرّفات المؤرخة، ومنها `mistral-medium-2604`، تطلق تحذير «غير مدرج». ولا يغيّر ذلك التقسيم المحدود بـ 16,000 محرف.

- **1.14.0** جدول توافق في مقدمة README، وحملة القياس التي يستند إليها (2026-09-09):

  - **الحكم على كل نموذج، في أعلى README، باللغة الفرنسية.** كانت أكثر من ثلاثمئة ترجمة تنتظر في منتصف المستند، موزعة على ثلاثة جداول كان لا بد من قراءتها ومقارنتها. يحسم الجدول الجديد النتيجة بسطر واحد لكل نموذج، ويبيّن خصوصًا موضع الاختلاف: «كلمة واحدة غامقة منسية، بالعربية واليابانية» بدلًا من نسبة. لا تظهر في ثلاثة نماذج أي اختلافات يكتشفها المقارن في المستندين الكثيفين — `gemini-3.7-flash`، و`gpt-5.6-sol` عبر اشتراك ChatGPT، و`z-ai/glm-5.2` عبر OpenRouter.
  - **حملة إعادة قياس شملت 176 ترجمة.** تعود القياسات الأصلية إلى 4 سبتمبر، وكانت تخص README تغيّر كثيرًا منذ ذلك الحين. أعيد قياس ثمانية نماذج على README هذا، ونموذجين على مقالة رصد من المدونة في وضع `--news`، ونموذج واحد على أربعة ملفات README عامة — FastAPI وOllama وtldr-pages وVue.js. استحقت ثلاث نتائج أن تُدوّن: يفقد Mistral **عنوان قسم كاملًا** بالعربية والهندية والكورية، من دون أن تكتشفه أي آلية حماية في pipeline — لا يمكن استبدال العنوان بـ token، ولا يتحقق منه شيء حاليًا أثناء الترجمة، حتى وإن اكتشفه المقارن بعد ذلك؛ ويفشل Grok في خمس لغات من مقالة رصد، إذ يفقد أربعة مقاطع inline code وثلاثة عناوين URL منذ المقطع الأول، مع أنه يصمد عند ترجمة README؛ ويُنتج Gemini عدد 55 ترجمة بلا اختلاف من أصل 56 عبر ملفات README العامة الأربعة.
  - **إدخال مقارن البنية إلى المستودع.** كان README يصف هذا البروتوكول بأنه الطريقة الصحيحة لتقييم نموذج — «قارنوا عدد الأقسام والروابط وعناوين URL المميزة وكتل code» — من دون توفير الأداة. ‏`scripts/compare_structure.py` هو الذي أنشأ هذه الجداول، مع أحد عشر اختبارًا تثبت أنه يكتشف فقدان عنوان URL أو كتلة code أو قسم أو inline code أو سطر جدول أو اقتباس. ويتضمن التصحيحين المتعلقين بالكتابات غير اللاتينية اللذين جعلاه موثوقًا: لم يعد عنوان URL المتبوع بقوس كامل العرض يُحسب مختلفًا، ولم يعد الاقتباس الأقصر بالصينية يُحسب اقتباسًا مفقودًا.
  - **فحص الحزمة قبل النشر وبعده.** تعمل مجموعة الاختبارات على المصادر وتظل ناجحة، بينما تكون الحزمة الموزعة معطلة — وحدة منسية في إعدادات setuptools، ونقطة دخول لا يمكن حلّها. يبني `scripts/check-package-smoke.sh --wheel` ملف wheel، ويثبّته في venv مؤقت خارج المستودع، ويتحقق من أنه لا يحتوي سوى الحزمة، ومن استجابة طريقتي التشغيل، وإمكان استيراد جميع الوحدات، ثم يترجم مستندًا حقيقيًا ويقارن بنيته. يفعل `--pypi` الشيء نفسه انطلاقًا من الفهرس: فهذا هو الفحص الوحيد الذي يثبت أن هذه الـ bytes نفسها هي التي نُشرت، ولا يسمح PyPI مطلقًا بإعادة نشر إصدار. يشغّل gate ‏`--full` الجزء غير المتصل منه، من دون استدعاء نموذج أو تكلفة.
  - **تصحيح سبعة ادعاءات في README، بعد إعادة إنتاجها جميعًا قبل إعادة صياغتها.** كان الادعاء «لا يصل شيء خاطئ إلى قرصك» غير صحيح: إذا حذف نموذج قسمًا أو عدّل تاريخًا في front matter، فإنه يُرجع ملفًا يكتبه pipeline بالحالة `success`، وقد أعيد إنتاج ذلك باستجابة محاكية. يوضح README الآن ما تحميه tokens — كتل code وinline code وعناوين URL والمراسي والاقتباسات — وما لا تتحقق منه حمايات pipeline حاليًا. لا تعني عبارة «بلا اختلاف» «متطابقًا»: يحصي المقارن العناصر من دون قراءة محتواها، ولا يبلغ عن حذف عنوان من المستوى الرابع، ولا استبدال نص inline code، ولا تبديل راية — ثلاث حالات قِيست، وحُذفت عبارة «الرايات نفسها» لأن المقارن لا يملك عدادًا للرايات. لم يكن تثبيت بيئة المساهمة يُنتج أداة قابلة للاستخدام: لا يثبّت `pip install -r requirements.txt` إلا الاعتماديات، وكان `python -m aipmt` يجيب بـ `No module named aipmt`، وكان `pip install -e .` مفقودًا — وكذلك سطر تثبيت `pre-commit`، الغائب عن ملفي الاعتماديات. كان وعد OpenRouter بشأن السياقات الصغيرة يعلن عكس ما يفعله code: يُرفض أي نموذج تقل نافذة سياقه عن **16,400 token** قبل أي استدعاء، وفق قياسات عند 4,095 و8,192 و16,384. لم يكن المرشح الذي يمنع ملف `.env` خاصًا بالمشروع من إعادة توجيه استدعاءات API موثقًا في أي مكان؛ وقد وُثق الآن، مع قائمة المتغيرات المعنية. وأخيرًا، رقمان: جاءت خلية Grok في الجدول الرئيسي من حملة 9 سبتمبر، بينما كان عمودها يعلن حملة 4 سبتمبر؛ وكانت عبارة «رابط واحد أُلغي» المنسوبة إلى Gemini عكس القياس — فهو يحوّل ثلاثة عناوين URL مجردة إلى روابط ولا يفقد أيًا منها.

  - **لم يعد فشل الكتابة يترك ترجمة مبتورة على القرص.** يبتر `open(cible, "w")` الملف قبل ملئه: وكان حدوث خطأ أثناء الكتابة — امتلاء القرص — يترك فيه ملفًا هدفًا جزئيًا. أُعيد إنتاج الحالة: كُتبت ثمانية bytes، وأُرجعت الحالة `failure`، ثم وجد التشغيل التالي هذا الملف وأجاب بـ `skipped` واحتفظ به. وهكذا كان الفشل يتحول إلى ترجمة مبتورة لا يشير إليها شيء بعد ذلك، مع exit code يساوي 0. يُكتب المحتوى الآن في ملف مؤقت داخل المجلد نفسه — لا تكون `os.replace` ذرية إلا على file system واحد — ثم يُعاد تسميته، ويُحذف الملف المؤقت إذا فشلت الكتابة، بما في ذلك عند المقاطعة. يأتي هذا الملف المؤقت من `mkstemp`، وأظهرت مراجعة أن اسمًا متوقعًا لم يكن ليفعل سوى نقل المشكلة: كان symbolic link موضوع عند `cible.md.aipmt-tmp` يؤدي إلى كتابة الترجمة في الملف الذي يشير إليه، خارج مجلد الإخراج، مع إرجاع `success`؛ وكان تشغيلان متزامنان على الهدف نفسه يتشاركان الاسم، فيعيد الثاني التسمية بينما لا يزال الأول يكتب إلى inode نفسه — يُعلن نجاح الهدف، لكن المحتوى يكون مختلطًا (`BBBAAAAA` بدلًا من `BBBBBBBB`، وفق القياس). ينشئ `mkstemp` الملف في `O_CREAT | O_EXCL` باسم فريد، ما يغلق الثغرتين. تُستعاد أذونات الهدف الموجود صراحة قبل الاستبدال: ينشئ `mkstemp` الملف بوضع 0600، وإلا كان هدف مضبوط عمدًا على 0600 سيتحول إلى 0664، وكان هدف بوضع 0644 يقدمه web server سيصبح غير قابل للقراءة. تثبّت ثمانية اختبارات هذا كله، ويؤدي تعديلان تجريبيان في code إلى فشلها.

  - **يمكن لبيان النقاء التصريح بإعادة كتابة علامة حذف.** كان الفحص يشترط بقاء كل سطر في المرجع يحمل `# nosec` أو `# nosemgrep` أو `NOSONAR` حرفيًا. لكن نقل الكتابة إلى ملف مؤقت يغيّر اسم المتغير المفتوح: يظل تعطيل Sonar نفسه، بينما يتغير السطر. تصرّح فئة `markers` في البيان بالزوج `old` ← `new` مع سببه. وهناك شرطان، أُضيف ثانيهما نتيجة مراجعة: يجب أن يكون البديل موجودًا، **وأن يحمل عمليات التعطيل نفسها الموجودة في السطر الأصلي**. من دونه، كان التصريح بـ `value = 1  # NOSONAR` ← `value = 1` يمر بنجاح، بحيث كان مجرد تصريح يكفي لإزالة علامة — وهو بالضبط ما وُجد هذا الفحص لمنعه. ويظل أي اختفاء غير مصرّح به مرفوضًا.

  - **حذف حملتين من الجداول لأنهما توقفتا بسبب شيء غير النموذج.** فقد Grok جلسة CLI بعد اثنتي عشرة لغة من README هذا — رُفضت لغتان في أربع ثوانٍ من دون استدعاء النموذج — وخُنق `qwen3.8-flash` بخطأ HTTP 429 من المستضيف upstream بعد لغتين. لا تُقيّم حملة متوقفة: إما أن تعاد بالكامل أو لا تعاد مطلقًا. وفي السياق نفسه، أعيدت معايرة مدد جدول «README هذا المشروع» وفق حملة 9 سبتمبر — فقد كانت تعود إلى مراجعة من 508 أسطر، مقابل 785 سطرًا قِيست، وكانت تعلن دقيقتين و04 ثوانٍ لـ `gpt-5.6-sol`، بينما أعطى القياس 6 دقائق و46 ثانية.

  - **إعادة بناء README: ‏600 سطر بدلًا من 1,007، وفق ترتيب يناسب القارئ.** ما تفعله الأداة، والتثبيت، والإعداد، والخطوات الأولى، ثم جدول التوافق — الموضوع بعد التثبيت، حين يعرف المرء ما الذي سيشغله —، ثم الخيارات، وفصل لكل provider، والقياسات التفصيلية، وأخيرًا طريقة المساهمة. حُذفت روايات الحملات وحكايات القياس والتكرارات بين الأقسام؛ وكل رقم جرى الاحتفاظ به مأخوذ من الجداول، وكل ادعاء متعلق بالسلوك يطابق code هذا الإصدار. ويظل سجل إعادة البناء محفوظًا في تاريخ git.

  - **يوضح الجدول الرئيسي الآن عدد اللغات التي يتناولها كل اختلاف.** كان يعلن «✅ ‏14 لغة»، ثم يقول في العمود المجاور «كلمة واحدة غامقة» — من دون توضيح ما إذا كان ذلك ينطبق على لغة واحدة أم على اللغات الأربع عشرة. لم يكن بوسع القارئ أن يحسم الأمر، وكان الانطباع سيئًا حيث القياس جيد: في README هذا، لدى Gemini لغة واحدة فقط من أصل أربع عشرة تحتوي كلمة غامقة زائدة، ولدى Sol لغتان، ولدى GLM ثلاث. تحمل كل خلية الآن عدد اللغات المعنية وأسماءها، ويُعرّف رقم أعمدة الحكم في جملة واحدة — عدد اللغات، من أصل أربع عشرة، التي كُتبت فيها الترجمة ولم يختلف فيها شيء —، ويوضح مفتاح معنى الرموز الثلاثة. والتقسيم من وضع المالك: ✅ لا شيء مختلف؛ ⚠️ تُرجم كل شيء ولم يتغير سوى markup، ولا شيء مفقود؛ ❌ رُفضت لغة واحدة على الأقل، أو يوجد محتوى مفقود في ملف مكتوب.

  - **أعيد احتساب حملة المقالة كاملة باستخدام المقارن الحالي.** نُشر رقمان قبل تصحيح المقارن للتعامل مع الكتابات غير اللاتينية، وكان يحسب لهما اختلافات غير موجودة: ينتقل `qwen3.8-flash` من ثلاث عشرة إلى أربع عشرة لغة بلا اختلاف، وينتقل `qwen3.7-flash` من سبع إلى ثمانٍ. وصُحح المقطع المتعلق بـ `gpt-oss` في الوقت نفسه: لم تصل مقاطعه التي بقيت بالفرنسية قط إلى ملف مُسلّم، إذ رفضت آلية حماية المقاطع غير المترجمة اللغات الأربع المعنية — وهذا هو الفرق بين نموذج نوقفه ونموذج يمر من دون أن نلاحظه، وقد سمّاه README بوضوح.

  - **بند إخلاء مسؤولية في نهاية README.** تستبعد المادتان 15 و16 من GPL v3 أصلًا أي ضمان، لكن لا أحد يقرأ ترخيصًا قبل تثبيت أداة. لذلك تُذكر أربع نقاط بوضوح: يجب مراجعة الترجمة الآلية قبل النشر، إذ لا تغطي آليات الحماية العناوين أو الجداول أو front matter أو المعنى؛ ويُرسل المستند المترجم إلى provider المختار، وفق شروطه الخاصة، وقد تعيد بعض النماذج المجانية استخدام المحادثات — ويظل النموذج المحلي السبيل الوحيد لمنع خروج البيانات؛ وتُفوّتَر استدعاءات API ولا يضع البرنامج حدًا للإنفاق؛ والقياسات المنشورة ملاحظات مؤرخة وليست ضمانات.

  - **يمكن الوصول إلى عائلة `gpt-5.6-*` بأكملها عبر اشتراك ChatGPT**، وفق القياس: يوفّر `--model gpt-5.6-terra` عبر Codex النموذج الذي يقدمه API افتراضيًا. يظل الافتراضي هو `gpt-5.6-sol`، النموذج الوحيد الذي قيس عبر أربع عشرة لغة من دون فقدان على مستند كثيف، وهو الذي يترجم هذا المستودع.

- **1.13.1** حداثة الاعتماديات: لم يعد التأخر مجرد تحذير قد لا يقرأه أحد (2026-09-09):

  - **التحذير الذي لا يُحصى هو تحذير يُفوّت.** كان `check-deps-fresh.sh` يشير منذ عدة أيام إلى تأخر `openai` و`anthropic`، لكن سطر ملخص gate لم يكن يحصي سوى حالات الفشل: فقد كان يعلن «جاهز: N من الفحوصات ناجحة»، مع أن تأخرًا كان مذكورًا قبل ذلك ببضعة أسطر. لم يكن بوسع من يقرأ السطر الأخير — وهذا ما يفعله الجميع — أن يعرف ذلك. يتضمن الحكم الآن عدد التحذيرات، ويحصيها عداد مخصص بدلًا من طباعتها أثناء التنفيذ.
  - **تأتي ملاحظات الإصدار مع التنبيه بالتأخر.** لا يوضح رقم الإصدار ما الذي تغير، والاضطرار إلى العثور على الملف الصحيح بنفسك هو تحديدًا الاحتكاك الذي يؤدي إلى تجاوز هذه الخطوة. يعرض الفحص عنوان CHANGELOG لكل حزمة متأخرة، سواء كان التحديث major أم minor، مع السبب: فقد سبق لإصدار minor من SDK أن غيّر أمورًا يعتمد عليها هذا المشروع.
  - **`openai` ‏3.8.0 ← 3.10.0 و`anthropic` ‏1.3.0 ← 1.4.0، بعد قراءة ملاحظات الإصدار قبل التثبيت.** من جانب OpenAI، لم يحدث في هذه الفترة سوى تغيير سلوكي واحد — لم يعد ترويس `Retry-After` رقمي يتجاوز float يؤدي إلى الرجوع إلى backoff قصير، بل يُرجع الخطأ الأصلي من دون إعادة محاولة — ولا شيء يمس ما يعتمد عليه هذا المشروع: لم تتغير سماحية النماذج للحقول الإضافية، وهو ما جرى التحقق منه في code عند tag كليهما، ولذلك يستمر مرور `native_finish_reason` و`error` اللذين يضيفهما OpenRouter إلى أحد الخيارات. ومن جانب Anthropic، ترفض آلية حماية جديدة عبر `TypeError` صريح كائنًا من حزمة `httpx` حيث يتوقع SDK ‏`httpx2`؛ ولا يمرر هذا المشروع سوى قيم float، وفق التحقق. والقاعدة التي ترفض استدعاءً غير streamed تتجاوز مدته عشر دقائق متطابقة byte مقابل byte بين الإصدارين: إن `timeout` الصريح هو الذي يعفيه منها بالفعل، إذ تتجاوز قيمته البالغة 32,768 token حد 21,333.
  - **تم التحقق**: مجموعات تضم 502 + 31 اختبارًا على الإصدارات المرفوعة، وإغلاق `requirements.txt` مطابق لما هو مثبّت (41 تثبيتًا للإصدارات)، واستدعاء حقيقي واحد لكل SDK — OpenAI وClaude — على مستند في وضع `--news`، ببنية مطابقة للمصدر.
- **1.13.0** Provider ‏`--use_openrouter`‏: موجّه مدفوع إلى نحو 430 نموذجًا، بما فيها النماذج الصينية المفتوحة (2026-09-05):

  - **المسار التاسع للـ provider، وقد صدر مع الثامن.** لم يُنشر 1.12.0 على PyPI: إذ يصدر الموجّهان، OpenCode وOpenRouter، معًا. يتيح [OpenRouter](https://openrouter.ai) الوصول بمفتاح واحد إلى نماذج لا يعرضها هنا أي provider آخر — Kimi وQwen وDeepSeek وZ.ai — عبر رصيد موحّد تُحتسب تكلفته حسب الاستخدام. ولأن الـ endpoint متوافق مع OpenAI، فالعميل هو نفسه المستخدم مع xAI؛ **وكل ما يميّز هذا الـ provider موجود في preflight واحد**، وكل قاعدة فيه مستمدة من قياس أُجري على الـ API.

  - **يُقدَّم النموذج نفسه عبر عشرات المستضيفين ذوي الحدود القصوى المختلفة، والتوجيه لا يراعي ذلك.** وفق القياس: 33 مستضيفًا لـ `z-ai/glm-5.2`، و23 لـ `z-ai/glm-5.3-flash` — أحدهم محدود عند **2,048 token للإخراج**. لذلك كانت ترجمة طويلة من أصل 23 تُقتطع عشوائيًا من دون أدنى إشارة. يقرأ الـ preflight ‏`/api/v1/models/{modèle}/endpoints`، ويستبعد المستضيفين الذين يقل حد إخراجهم عن 8,000 token، والذين تكون حالتهم متدهورة، والذين لا يصرّحون بأي حد أقصى، ثم يثبّت الباقين. إن استخدام `provider.only` **من دون** `allow_fallbacks: false` ليس سوى تفضيل: إذ يعود الموجّه إلى مستضيف مستبعد، ويصبح التثبيت بلا قيمة. وإذا لم يستوفِ أي مستضيف الحد الأقصى، يتوقف الأمر: فمتابعة الترجمة تعني قبول الاقتطاع الصامت الذي وُجد هذا الـ preflight لمنعه.

  - **يُحتسب الاستدلال بسعر الإخراج، وهو مفعّل افتراضيًا في نماذج كثيرة.** الطلب نفسه على `z-ai/glm-5.2`، والإجابة «حسنًا»: **107 tokens للإكمال وفق الإعداد الافتراضي للنموذج، و2 عند تعطيل الاستدلال**. في الترجمة، حيث لا يضيف الاستدلال شيئًا، يعني ذلك معاملًا قدره 18 لكل مقطع من كل ملف. لذلك يُعطّل افتراضيًا. أما **النماذج الـ288 من أصل 431** التي تفرضه (`reasoning.mandatory`)، فتجيب بـ `400 « Reasoning is mandatory for this endpoint and cannot be disabled »`: وفي حالتها يقرأ الـ preflight مستويات الجهد المقبولة ويطلب أدناها (النقطة التالية) — إذ يخصص الجهد **نسبة مئوية من `max_tokens`** يستهلكها الاستدلال أولًا، ولذلك فإن اختيار قيمة عشوائية ينقل خطر الصفحة الفارغة بدلًا من تقليله.

  - **يتلقى النموذج الذي يفرض الاستدلال أدنى مستوى جهد يقبله، وقد حُسم ذلك بناءً على قياس.** كان الخيار الأول ألّا يُرسل شيء، تجنبًا للتخمين بدلًا من النموذج. وعند اختباره على `z-ai/glm-5.3-flash`، الذي تكون قيمة الكتالوج الافتراضية له `max`، أدى هذا الخيار إلى إخراج **مقتطع عند 32,768 token** قبل نهاية الترجمة — فضاعت لغتان من أصل أربع عشرة. ولم تكن زيادة الحصة لتغيّر شيئًا: فالجهد يخصص منها نسبة مئوية، ويكبر الاستدلال معها. لذلك يقرأ الـ provider ‏`supported_efforts` أثناء الـ preflight ويطلب أدنى قيمة، مع الرجوع إلى «لا شيء» حين لا يعلن الكتالوج أي قيمة قابلة للاستخدام. وفي اختبار معاكس على اللغة التي فشلت: كانت تفشل بسبب نفاد الميزانية، وأصبحت الآن تنجح خلال 9 دقائق ببنية مطابقة للمصدر.

  - **يحمل عطل المستضيف upstream اسمه الآن.** يطبّع الموجّه هذه الحالة إلى `finish_reason=error` مع `native_finish_reason` فارغ — وقد قيس ذلك مرتين، بلغتين، عند 750 ثانية بالضبط. كانت الرسالة العامة توجّه البحث إلى خلل في المستند أو في التقسيم؛ أما الآن فتوضح أن العطل لدى المورّد وأن إعادة المحاولة تكفي غالبًا.

  - **إن `finish_reason=length` المصحوب بإخراج فارغ ليس اقتطاعًا.** بل هو استهلاك الاستدلال للميزانية قبل أول محرف مفيد — وقد قيس عند 15,850 token للاستدلال مقابل 148 token مفيدة. تتطلب الحالتان إجراءين متعاكسين: فتقليل حجم المقاطع لا يفيد في الحالة الأولى. وتفرّق الرسالة بينهما صراحة. كما أُضيف حاجزان آخران بناءً على قياسات: يجيب الموجّه بالحالة **200 مع جسم لا يحتوي إلا على خطأ** عندما يفشل المستضيف upstream (كان `choices[0]` يطلق `TypeError` مبهمًا يحجب الرسالة)، وتُقرأ نافذة السياق من الكتالوج ثم تُسجّل في `MODEL_TOKEN_LIMITS` — إذ إن `DEFAULT_TOKEN_LIMIT` غير صحيح لـ44 نموذجًا في الكتالوج، بينها نموذجان محدودان عند 4,095 token.

  - **إن `--model fournisseur/modèle` إلزامي، ويجري التحقق من صيغته قبل أي اتصال بالشبكة.** ليس OpenRouter مورّدًا: فالاختيار يترتب عليه السعر والترخيص ومعالجة البيانات، ولا يجوز اتخاذه بدلًا من المستخدم. ولأن الـ slug يُدرج في URL الخاص بالـ preflight، فالتحقق ليس مجرد تحسين لتجربة الاستخدام، بل هو الحاجز الذي يمنع حقن مسار فيه: تقبل الـ regex المشتركة والمقسّمة بحسب namespace بين الموجّهين `a/b/..`، ومن هنا يأتي الرفض الصريح للمقطع الأب. ليس لـ `--eco` أي تأثير، وهو يصرّح بذلك.

  - **صُححت ثلاث صياغات، إحداها كانت خاطئة.** كان تحذير OpenAI بشأن `codex exec` يستهدف حقن ملف جلسة شخصي في runner مشترك، لا كون المستودع عامًا: وقد ورد بمعنى معكوس في README وCLAUDE.md وفي الشيفرة. تغيّر موضع مصادقة OpenCode في 1.18.27 (جدول `credential` في `opencode.db`، بدلًا من `auth.json`)؛ وظل الثابت «لا يُقرأ هنا أبدًا» صحيحًا، لكن العنوان كان متقادمًا. وأخيرًا، لم يعد قسم OpenCode يعرض مسارات لم تُختبر قط على أنها متكافئة: فقد قيس كل من بوابة Zen وOllama من البداية إلى النهاية، بينما لم تُقَس GitHub Copilot وLM Studio وllama.cpp، ويذكر README ذلك الآن.

  - **حملة قياس، وجدول للنماذج الموصى بها في README.** نُفذت أكثر من ثلاثمئة ترجمة على ثلاث مجموعات من المستندات — مقالة مدونة كثيفة في وضع `--news`، وREADME هذا بصيغة Markdown القياسية، وأربعة ملفات README لمشاريع معروفة مأخوذة كما هي من GitHub — إلى أربع عشرة لغة. يميّز الجدول بين أمرين كانا يُخلطان: الترجمات التي **تكتمل** وتلك التي تكون **بنيتها مطابقة** للمصدر. لم تفقد ثلاثة نماذج أي معلومة قط في المستندين الكثيفين: `gemini-3.7-flash` و`gpt-5.6-sol` عبر اشتراك ChatGPT، و`z-ai/glm-5.2` عبر OpenRouter — ولم تكن اختلافاتها الوحيدة سوى زوج من `**` لم يُنقل في لغة أو لغتين. والخلاصة الأساسية هي أن **العامل الفاصل هو كثافة المستند، لا وضع `--news`**: يفشل Grok عبر الاشتراك ثلاث عشرة مرة من أصل أربع عشرة في مقالة المدونة، وينجح في أربعة عشر ملف README عامًا من أصل ستة عشر، والسبب هو فقدانه التماسك في مقطع طويل، وقد تأكد ذلك باختبار معاكس. ويحمل الجدول تحذيره الخاص: فهو غير شامل، ومؤرخ، والمدد لا ترتّب شيئًا، وتظل المنهجية الصحيحة هي القياس على المستندات الخاصة بالمستخدم.

  - **صُحح مقارن البنية قبل نشر أرقامه، لأنه كان ينتج نتيجتين إيجابيتين زائفتين في الكتابات غير اللاتينية.** لم تكن regex التي تتوقف عند `)` تفصل URL متبوعًا بقوس كامل العرض `）`، ولذلك كانت السلسلة المستخرجة تختلف مع أن الـ URL متطابق؛ كما كان اقتباس من خمسة أسطر بالفرنسية يمتد على ثلاثة أسطر بالصينية يخفض عدًّا قائمًا على الأسطر. جرى التحقق من التصحيحين باختبار معاكس: فلا يزال حذف URL أو قسم أو code inline يُكتشف. ولولاهما، لكانت نتائج Gemini وCodex المنشورة إحدى عشرة لغة من أصل أربع عشرة بدلًا من ثلاث عشرة واثنتي عشرة.

  - **الاختبارات**: ملف جديد `tests/test_openrouter_provider.py` ‏(76 اختبارًا) — التحقق من النموذج ورفض المقطع الأب، وتثبيت المستضيفين (الحد الأقصى، والحالة، والحد الأقصى غير المعلن، والحد الأدنى المشترك)، وبقاء `allow_fallbacks` خاطئًا دائمًا، وتعطيل الاستدلال أو تركه وفق `mandatory`، وعقد الإخراج الكامل (خطأ داخل استجابة 200، وعدم وجود أي اختيار، وتمييز الصفحة الفارغة عن الاقتطاع، وقيمة `finish_reason` غير الطبيعية، والمحتوى الفارغ)، وإغلاق الـ preflight عند الفشل إذا تعذر الوصول إلى الكتالوج، أو غاب الـ slug، أو لم يوجد مستضيف سليم، وحصرية الـ flag وتسمية اسم الملف. بلغ مجموع الحزمة **502 اختبارًا**.

  - **Refactor: قُسّم الـ module الوحيد المكوّن من 4,253 سطرًا إلى modules، من دون تغيير سطر واحد من السلوك.** وُزّع `src/aipmt/translate.py` على `config` و`markdown` و`segmentation` و`guards` و`placeholders` و`news` و`prompts` و`notes` و`naming` و`pipeline` و`cli` وحزمة فرعية `providers/` ‏(module واحد لكل provider، و`base` كأساس، و`registry` للحل والـ dispatch). كل عملية نقل هي commit له إثبات آلي: يقارن مدقق AST لجميع عقد المستوى الأعلى في الحزمة بـ snapshot مرجعي، ويتحقق من موضع كل رمز، وبقاء علامات الأمان حرفيًا، وعدم وجود ملف غير متتبَّع — وهي أدوات مؤقتة أزالتها النسخة التالية. أما التغييرات المرئية فهي: أصبح `aipmt.translate` واجهة تعيد إظهار، بهوية الكائن نفسها، الأسماء الـ64 التي كان الـ module يعرضها من دون بادئة `_` ‏(يحمل `__all__` تسعة منها، وهي الـ API المدعومة؛ أما البقية فهي aliases للتوافق)، وتوقف عن إعادة تصدير أسماء التبعيات والمكتبة القياسية الـ29 التي كان يلتقطها `import *`؛ ولم يعد التنفيذ المباشر للملف (`python src/aipmt/translate.py`) موجودًا — وتظل `aipmt` و`python -m aipmt` الصيغتين المدعومتين؛ وأصبح `__module__` للدوال العامة تابعًا لـ module تعريفها؛ وصارت حزم SDK تُستورد بعد تحميل `.env` لا قبله، من دون أثر معروف. حُفظت الاختبارات الـ427 حتى على مستوى المعرّف، ونُقلت إلى الـ modules التي تختبرها: بات 91 patch كانت تمر عبر الواجهة تستهدف الآن الـ module الذي يطالع الاسم (وظل اثنان منها ناجحين حتى من دون الـ patch الخاص بهما، وفق القياس)، وتثبّت سبعة اختبارات عقد الواجهة، وأُعيدت كتابة أدوات الـ gates قبل أول عملية نقل كي لا يتمكن أي منها من التحول إلى الأخضر بمجرد توقفه عن التحقق — نطاق Lizard على مستوى المجلدات مع حد أدنى، وقراءة الـ flags من الـ parser المُنشأ، وحد أدنى للتغطية لكل حزمة، و`release.sh` الذي يسرد الـ modules المتتبَّعة.
  - **إصلاح (مراجعة pull request)**: يرفض OpenRouter بطاقة كتالوج بلا `context_length` بدلًا من تسجيل القيمة الافتراضية البالغة 128,000 token على أنها قياس — وهو ما كان يعطّل أيضًا تحذير «النموذج غير مدرج» — وعندما يكون `finish_reason` فارغًا (النوع الموثق `string | null`)، يكون السبب الخام الصادر عن المستضيف هو المعتمد، إذ يساوي `max_tokens` القيمة `length`؛ كما يؤدي الخطأ المحمول في الاختيار نفسه إلى رفض المحتوى الجزئي المصاحب له، بالرسالة نفسها المستخدمة لعطل upstream المطبّع — مع جمع تفاصيل المستضيف والسبب الخام والنصيحة. يجيب OpenCode بخطأ العقد الخاص به، لا بـ `AttributeError`، عند حدث `part: null`، ويرفض stream بصيغة JSONL إذا تعذرت قراءة أحد أسطر أحداثه بدلًا من قبول نص جزئي. تقتل واجهات CLI الوكيلة الثلاث مجموعة الوكيل عندما تتلقى هذه العملية `SIGTERM` أثناء الاستدعاء — كان `timeout` الخاص بالـ regen يترك الوكيل حيًا يستهلك حصته — كما يتبع `SIGKILL` الخاص بالمجموعة دائمًا مهلة السماح، إذ إن shim ينتهي بصورة سليمة كان يترك العملية الحفيدة حيّة. أُعيد وصل زوج `# fmt: off` / `# fmt: on` فصله التقسيم؛ وتسمّي مساعدة `--reasoning_effort` الـ providers الأربعة التي تستخدمه.
  - **إصلاح (المراجعة الثانية)**: بات الحد الأقصى للإخراج المطلوب من OpenRouter يحتفظ بالجزء من السياق الذي يشغله الـ prompt والمقطع — إذ يغطي `context_length` الإدخال والإكمال معًا، وكانت طلبات ستة نماذج في الكتالوج تُرسل من دون مساحة للإدخال؛ ويُرفض السياق الأقصر من اللازم قبل احتساب أي تكلفة. يعود أساس واجهات CLI الوكيلة إلى `terminate` ثم `kill` عندما لا تكون مجموعات عمليات POSIX موجودة، بدلًا من السماح لـ `AttributeError` بتجاوز حاجز الـ timeout وإطالة الانتظار. ويُبحث عن علامة `429` الخاصة بـ OpenCode كرقم لا كسلسلة فرعية، إذ كان معرّف خطأ مثل `err_84290b` يفعّل 90 ثانية من الـ back-off قبل أن يفشل على أي حال. وأخيرًا، يُعرض أي endpoint غير قياسي لـ OpenRouter أثناء الـ preflight: يكفي وجود `.env` في المشروع لتعيين واحد، والمفتاح الحقيقي هو ما كان سيُرسل إليه لاحقًا.
  - **الأمان: لم يعد بإمكان `.env` في المشروع إعادة توجيه استدعاءات الـ API.** يبحث `find_dotenv(usecwd=True)` عن الملف انطلاقًا من المجلد الحالي ثم المجلدات الأب: كان بإمكان شجرة غير موثوقة — مثل مستودع نُسخ للتو — أن تضع فيها `OPENROUTER_BASE_URL` أو `XAI_BASE_URL` أو `OPENAI_BASE_URL` ‏(والأخيرة تقرؤها حزمة SDK نفسها) من دون معرفة أي مفتاح، ثم يُرسل المفتاح الحقيقي، الآتي من البيئة أو من إعدادات المستخدم، في ترويسة التفويض إلى خادم تابع لطرف ثالث. يعتمد المرشّح على نمط لا على قائمة: فوفق حزم SDK المثبّتة، تُقرأ اثنا عشر متغيرًا للتوجيه، يقرأ عميل Anthropic وحده ستة منها — وكانت قائمة يدوية ستغفل نصفها. لذلك تُرفض من طبقة المشروع جميع المتغيرات التي تحتوي على `_BASE_URL` أو `_API_BASE` أو `_ENDPOINT`، والـ proxies ومخازن الشهادات (فتعيين سلطة خاضعة للتحكم يجعل الوسيط غير قابل للتمييز عن خادم حقيقي)، وكذلك `XDG_CONFIG_HOME` و`APPDATA`: إذ كان تعيينهما يعني تحديد الملف الذي يشكّل طبقة المستخدم، ومن ثم الالتفاف على المرشّح بطريق غير مباشر. لا تُقبل هذه المتغيرات إلا من الطبقتين اللتين يتحكم فيهما المستخدم، وهما البيئة المصدّرة و`~/.config/aipmt/.env`. كما تُقرأ طبقة المشروع من دون interpolation: يوسّع `load_dotenv` القيمة `${VAR}` افتراضيًا، وكان `.env` غير موثوق يحتوي على `NOM_ANODIN=${OPENAI_API_KEY}` ينسخ المفتاح الحقيقي تحت اسم لا يتعرف عليه الترشيح القائم على النمط للعمليات الفرعية — فيدخل عندئذ إلى بيئة `codex exec`، خلافًا للثابت المعلن. وأخيرًا، لا يعرض الرفض سوى اسم المتغير: فقد كان URL بالصيغة `https://${CLE}@hôte/` يسرّب المفتاح المدرج في السجلات، مع أنه كان يُرفض. يُبلَّغ عن الرفض عبر stderr مع توضيح الإجراء المطلوب — إذ يُعلَن عن relay مؤسسي في إعدادات المستخدم.
  - **إصلاح: يُحسب غلاف إخراج OpenRouter لكل استدعاء.** يغطي `context_length` الإدخال والإكمال معًا، ولا يضمن احتياطي ثابت مضبوط على نص لاتيني أي حد: وفق القياس باستخدام tokenizer ‏`o200k_base`، تساوي 16,000 محرف 3,200 token بالفرنسية، لكن 12,300 باليابانية و17,500 في emoji. لذلك تُستمد الميزانية من النص المرسل فعليًا، مع زيادتها بعدد وحدات بايت UTF-8 الخاصة به: ففي كل tokenizer يعتمد دمج وحدات البايت — BPE على مستوى البايت، وSentencePiece مع byte fallback، وهي العائلات التي يستخدمها الكتالوج — يقابل كل token بايت واحد على الأقل، وهذه هي الزيادة الوحيدة المتاحة هنا، حيث يوجّه OpenRouter الطلبات إلى عشرات الـ tokenizers غير المعروفة. لم تكن أي نسبة متوسطة مناسبة — إذ ينخفض الرمز الكتابي من المستوى الإضافي إلى 1.33 بايت لكل token، والمحرف المركب إلى 1.00 — وبات الإدخال والإخراج معًا يلائمان النافذة بحكم التصميم. ويُرفض المقطع الأعلى كثافة مما يحتمله النموذج المختار قبل الاستدعاء بدلًا من رفضه بعد احتساب التكلفة.

- **1.12.0** Provider ‏`--use_opencode`‏: ‏OpenCode، الوكيل مفتوح المصدر، نحو المورّد الذي يختاره — نموذج محلي، أو مجاني بلا حساب، أو اشتراك، أو مفتاح (2026-09-04):
  - **المسار الثامن للمزوّد ذو طبيعة مختلفة عن المسارات السبعة الأولى.** إن [OpenCode](https://opencode.ai) (MIT) ليس مزوّد نماذج، بل _موجّهًا_ نحو المزوّدين الذين أعدّهم المستخدم في OpenCode نفسه: مفتاح API، أو اشتراك (GitHub Copilot، ChatGPT، SuperGrok)، أو بوابة OpenCode Zen — التي تتيح نماذج مجانية **من دون حساب** —، أو نموذج **محلي** (Ollama، LM Studio، llama.cpp). يتحكم السكربت في `opencode run` بالوضع غير التفاعلي، كما يفعل مع Codex وGrok، ويعيد استخدام البنية الأساسية نفسها للعمليات الفرعية (مجموعة عمليات مستقلة، ثم `SIGTERM` وبعده `SIGKILL` عند انتهاء المهلة، مع إغلاق stdin دائمًا وتنقية البيئة). جرى التحقق عبر **ترجمتين فعليتين**: ملف README هذا كاملًا إلى الإنجليزية باستخدام `opencode/mimo-v2.5-free` — خلال 49 ثانية، بتمرير واحد، وبنية مطابقة للملف المصدر (32 عنوانًا، و26 إغلاقًا لمقاطع الكود، و18 رابطًا، و37 URL، و37 سطرًا في الجداول، و135 كودًا مضمّنًا) — وملف اختبار باستخدام `ollama/qwen2.5:7b` محليًا، من دون أي مفتاح.

  - **إن `--model provider/modèle` إلزامي، وهذا اختيار مقصود.** من دون `--model`، يعود OpenCode إلى الإعداد الافتراضي الخاص به، وهو في التثبيت الجديد `opencode/big-pickle`، وهو نموذج مجاني «خفي» قد تُستخدم المحادثات معه في التدريب — وقد أثبت القياس أن هذا هو النموذج الذي استجاب. اختيار ذلك بصمت بدلًا من المستخدم سيكون بالضبط نوع التحويل غير المرئي الذي يتعقبه هذا المستودع؛ لذلك تسمّي رسالة الخطأ الأمر الذي يسرد النماذج (`opencode models`) وتقدّم ثلاثة أمثلة (محلي، مجاني، اشتراك). ليس لـ `--eco` أي تأثير، وهو يصرّح بذلك. يُمرَّر `--reasoning_effort` كما هو بوصفه `--variant` الخاص بـ OpenCode، وفقط عند طلب صريح.

  - **عزل مقاس، لا مفترض.** يعرّف إعداد مضمّن (`OPENCODE_CONFIG_CONTENT`، وهو الأخير في ترتيب الدمج لدى OpenCode، ولذلك تكون له الأولوية على إعداد المستخدم من دون استبداله) وكيلًا باسم `aipmt` تُرفض له جميع الأدوات (`permission: {"*": "deny"}`): فلا يعود السجل يعرضها أصلًا على النموذج، وعندما يُطلب منه «سرد الملفات وتشغيل `id`» يجيب بأنه لا يملك أدوات. جرى تعطيل مشاركة الجلسة واستبعاد plugins الخارجية (`--pure`)، ومنع `--auto` دائمًا، مع دليل عمل مؤقت وفارغ. جرى قياس حقنتين صامتتين وإيقافهما: من دون `OPENCODE_DISABLE_CLAUDE_CODE`، يدخل `~/.claude/CLAUDE.md` الخاص بالمستخدم في **كل** prompt ‏(515 token إدخال بدلًا من 186 لعبارة «مرحبًا» بسيطة)؛ ومن دون `OPENCODE_DISABLE_PROJECT_CONFIG`، يدخل أيضًا `AGENTS.md` الخاص بالدليل الحالي — وقد طُبّقت على الترجمة تعليمة تقول «اختم كل إجابة بكلمة BANANA». أما `~/.config/opencode/AGENTS.md` العام فيظل محقونًا: فلا يوجد مفتاح يعطّله، كما أن التحايل عليه عبر `XDG_CONFIG_HOME` محرّف سيحجب أيضًا مزوّدي المستخدم. جرى توثيق ذلك بدلًا من ترقيعه.

  - **إن `exit 0` لا يثبت شيئًا؛ إنها CLI ثالثة، وتستدعي الحذر نفسه — مع فخّين خاصّين بها.** لا يتسبب `--agent` غير المعروف في فشل `opencode run`: يظهر تحذير على stderr ثم يحدث رجوع **صامت** إلى وكيل البرمجة مع أدوات فعّالة. لذلك، إذا لم يؤخذ الإعداد المضمّن في الحسبان، فستنطلق الترجمة عبر وكيل قادر على الكتابة؛ ولهذا يتحقق عقد الإخراج من غياب هذه الرسالة، إضافة إلى: رمز إرجاع 0، وعدم وجود أي حدث `error`، وعدم وجود أي `tool_use`، وأن يكون آخر `step_finish` بحالة `stop` (إذ يمثّل `length` استجابة مبتورة)، وأن يكون النص غير فارغ. الفخ الثاني: حدث خطأ JSON **مبهم** — «خطأ غير متوقع في الخادم. راجع سجلات الخادم للحصول على التفاصيل.»، مع مرجع بسيط — ولا يوجد السبب الحقيقي (`ProviderModelNotFoundError: Model not found: foo/bar. Did you mean…`، ‏`ProviderAuthError`…) إلا في السجلات. ومن هنا يأتي `--print-logs --log-level ERROR` وقراءة حقل `error="…"` من stderr، من دون تتبّع Bun الذي يليه. وبذلك يفشل النموذج غير المعروف خلال ثانية واحدة مع ذكر سببه. كما يتجنب `--title` استدعاء LLM طفيليًا: فمن دونه، ينشئ OpenCode عنوانًا للجلسة عبر دورة إضافية على `small_model`.

  - **الأسرار: الترشيح بالنمط نفسه المستخدم مع Codex وGrok، باستثناء واحد مسمّى صراحةً.** يُحتفظ بـ `OPENCODE_API_KEY`: فهو مفتاح OpenCode نفسه (بوابة Zen، واشتراك Go)، وموجّه إليه باسمه — أي ما يعادل `auth.json` الخاص به، وليس مفتاحًا يديره aipmt أو يستطيع احتساب تكلفته. يُعدّ المزوّدون داخل OpenCode ‏(`opencode auth login`، ‏`opencode.json`)، وليس أبدًا في `.env` الخاص بـ aipmt، الذي لا يصل أي مفتاح منه إلى العملية الفرعية. لا يوجد رفض في CI، بخلاف CLI الخاصة بالاشتراكات: فمفتاح API أو نموذج مستضاف ذاتيًا على runner استخدامان مشروعان.

  - **أصبح حاجز منع اجتياز المسارات يفحص القيمة بعد الاستيفاء، لا القيمة الخام.** يحتوي `provider/modèle` على `/` كان حاجز الإصدار 1.10.0 يرفضه — وكان محقًا، لأن `--model` يُستوفى داخل اسم الملف `--include_model`. تستبدل تسمية اسم الملف الآن `/` و`\` و`:` بـ `-` قبل أي استيفاء (`ollama/qwen2.5:7b` ← `ollama-qwen2.5-7b`، لأن `:` غير قانوني في Windows)، ويفحص الحاجز السابق هذه التسمية: يصبح `../../evil` الاسم البسيط `doc-en-..-..-evil.md` تحت الهدف، بينما يظل `..` وحده مرفوضًا، وكذلك `--target_lang ../x`. ويظل حاجز النطاق `_ensure_within_directory` طبقة الحماية الثانية من دون تغيير.

  - **النماذج المجانية والمحلية، وما جرى قياسه.** يترجم `opencode/mimo-v2.5-free` فقرة خلال 16 ثانية وملف README هذا خلال 49 ثانية؛ ويستغرق `opencode/big-pickle` ‏40 ثانية لترجمة 200 كلمة، وظل طلبان متزامنان عليه بلا استجابة لمدة 5 دقائق، بينما ينجح كل منهما منفردًا؛ ولم يُرجع `opencode/nemotron-3.5-lightning-free` أي شيء خلال 3 دقائق. ومن هنا يأتي `REGEN_PROVIDER=opencode` مع إلزامية `REGEN_MODEL` وتشغيل **مهمتين** بالتوازي. محليًا، يضبط Ollama غالبًا 4,096 token للسياق، بينما تصل المقاطع إلى 16,000 محرف: لذلك يلزم `Modelfile` مع `PARAMETER num_ctx 32768`، وتتبع الجودة النموذج المستخدم — فقد عكس نموذج 7B ترتيب قائمة وأفسد إغلاق مقطع كود في ملف الاختبار، بينما حافظ نموذج البوابة على كل شيء.

  - **لم تعد ترجمات هذا المستودع تمر أبدًا عبر API مدفوعة.** كان `regen_translations.sh` يستخدم OpenAI API بمجرد العثور على مفتاح في `.env`، ولا يجعل Codex إلا خيارًا صريحًا. وهذا بالضبط ما حدث عند إعداد هذا الإصدار: انطلقت الترجمات الـ28 عبر OpenAI API، ثم انطلق CHANGELOG الهندي عبر Gemini API، مع أن اشتراك ChatGPT موجود تحديدًا لتجنّب الدفع بحسب الاستخدام. أزيل الاكتشاف التلقائي للمفاتيح: **أصبح Codex هو الإعداد الافتراضي، مع `gpt-5.6-sol`**، أي نموذج الجودة؛ وتتطلب `openai` و`gemini` و`grok` استخدام `REGEN_ALLOW_PAID_API=1` إضافةً إلى `REGEN_PROVIDER`، وهو استثناء مسمّى لكي تُطبّق القاعدة لحظة اتخاذ القرار؛ ويفشل `REGEN_PROVIDER` غير المعروف بدلًا من الرجوع إلى API. تثبّت عشرة اختبارات الإعداد الافتراضي والرفض والاستثناء. وأُعيد إنجاز الترجمات الـ28 لهذا الإصدار عبر Codex.

  - **جرت مركزة التراجع التدريجي عند بلوغ حد المعدل** (`_retry_on_rate_limit`): كانت حلقات Codex وGrok متطابقة باستثناء التسمية، وكانت إضافة نسخة ثالثة ستتجاوز عتبة التكرار. ترث أخطاء CLI الثلاثة من `_CliCallError` مشتركة؛ ويمنع اختبار خروج أي واحد منها عن هذا التسلسل، وإلا فلن تراه الحلقة المشتركة.

  - **الاختبارات**: ملف جديد `tests/test_opencode_provider.py` ‏(61 اختبارًا) — عقد إخراج كامل، والرجوع إلى الوكيل، وقراءة السبب من السجلات، وإزالة تكرار أجزاء النص وتجاهل الأجزاء الاصطناعية، وقتل مجموعة العمليات عند انتهاء المهلة، والتراجع التدريجي عند 429، وإلزامية النموذج والتحقق منه، وفحص تمهيدي بلا أسرار، وحلّ مسار الملف التنفيذي، وتوصيل dispatch، وتسمية اسم الملف، واختبار مضاد لاجتياز المسارات. يوسّع `tests/test_review_hardening.py` حصرية flags وغياب الأسرار لتشمل المزوّد الجديد. تتطلب البوابة الآن توثيق **22 flag** خاصة بـ argparse. وبلغت المجموعة الكاملة **382 اختبارًا**.

- **1.11.1** تصحيح للتوثيق: يعلن README أخيرًا مسارات المزوّد السبعة (2026-09-03):

  - **كانت صفحة PyPI للإصدار 1.11.0 تقول «4 APIs + ‏Codex CLI».** بينما يوفّر الكود سبعة مسارات — OpenAI وMistral وClaude وGemini وGrok عبر API؛ وCodex ‏(ChatGPT) وGrok عبر الاشتراك، من دون فوترة بحسب الاستخدام. كان وضعا Grok غائبين عن المقدمة وعن بند _تعدد المزوّدين_، وكررت الترجمات الـ14 الخطأ. ولأن الوصف الطويل للحزمة ثابت لكل إصدار، فقد تطلب تصحيح الواجهة رقم إصدار جديدًا: وهذا هو السبب الوحيد لوجود هذا الإصدار. **لا توجد أي تغييرات في الكود.**
  - جرت مواءمة `CLAUDE.md` مع ما أدخله النشر: عدّادات البوابة (16، و17 في `--full`)، وworkflows الإحدى عشرة النشطة، وعدّادا Sonar/Codacy غير الظاهرين في `gh pr checks` ‏(hotspots، وCodacy API)، ونقل `# nosemgrep` بواسطة `ruff-format`، وبيئات GitHub التي يتطلبها تبادل OIDC، وحقيقة أن _الناشر المعلّق_ لا يحجز الاسم.

- **1.11.0** النشر على PyPI: ‏`pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-09-03):

  - **أصبح السكربت أحادي الملف حزمة قابلة للتثبيت.** انتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول console باسم `aipmt` والمكافئ `python -m aipmt`. يظل المستودع المستنسخ ضروريًا للمساهمة — إذ توجد فيه الاختبارات والترجمات الـ28 وأدوات الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` أبدًا**، لأن التعارض حقيقي وصامت. تثبّت حزمة PyPI المسماة `translate` ‏(الإصدار v3.8.1، وآخر رفع في 2026-07-06) دليلًا يحمل الاسم نفسه. جرت إعادة إنتاج المشكلة داخل venv: يتغلب الدليل على الوحدة، ويختفي `translate.main`، وتتعطل نقطة الدخول عند `AttributeError` — بينما يجيب `pip check` بعبارة «لم يُعثر على متطلبات معطّلة» مع rc=0. كان مجرّد وجود `pip install translate` لدى المستخدم كافيًا لتعطيل CLI من دون تشخيص قابل للاستفادة. أما الاختبار المضاد على wheel الفعلي: فقد ثُبّت `pip install translate` فوق الحزمة، وأعاد `aipmt --help` القيمة rc=0 قبل ذلك وبعده، وتعايشت أداتا CLI.
    - **اسم توزيع طويل، وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للاكتشاف عبر بحث PyPI؛ إذ سيكون الاختصار وحده غير قابل للعثور بالنسبة إلى من لا يعرف المشروع مسبقًا، بينما يهدف النشر تحديدًا إلى جعله قابلًا للاكتشاف. جرى استبعاد مرشحين معقولين بعد التحقق: الاسم `ai-markdown-translator` محجوز على npm منذ 2024 لأداة تؤدي الغرض نفسه وتسبق هذا المستودع بـ17 شهرًا؛ والاسم `aimt` يختلف بحرف واحد عن `aim` ‏(الإصدار v3.29.1)، وهي حزمة نشطة في **المجال نفسه** — أسوأ وضع ممكن لالتباس دائم. وهناك أيضًا فخ منهجي: يعيد `pypi.org/project/<nom>/` الحالة 200 لأي اسم (صفحة مضادة للروبوتات)، ولا يُعتد إلا بـ JSON API.
    - **تخطيط `src/` بدلًا من حزمة مسطحة.** كان من شأن الحزمة المسطحة أن تحافظ على عبارات `sys.path.insert(..., "..")` الست في الاختبارات، وهذه هي المشكلة تحديدًا: فهي تستورد شجرة المصدر بدلًا من الحزمة، ما كان سيخفي أي خطأ في التحزيم. التكلفة الفعلية: قاعدة استبدال إضافية واحدة.

  - **يمكن أخيرًا إعداد المفاتيح مرة واحدة بصورة دائمة.** لم تكن CLI المثبّتة تملك أي إعداد **دائم**: فلم يبق سوى متغير البيئة و`.env` في الدليل الحالي. صحيح أن `find_dotenv` يصعد حتى جذر النظام، ولذلك كان يعثر على `~/.env` **عند العمل داخل الدليل الشخصي للمستخدم**، لكنه لا يجد شيئًا عند العمل من مكان آخر — أي تغطية تعتمد على مكان تشغيل الأمر، لا على خيار تصميمي. لذلك أُضيفت طبقة ثالثة: `~/.config/aipmt/.env`، تحت الطبقتين الموجودتين.

    - **الأولوية غير مكتوبة في الكود**، بل تنتج عن `override=False`، وهي القيمة الافتراضية لـ `load_dotenv`: فكل طبقة لا تملأ إلا ما تركته سابقتها فارغًا. ومن هنا يأتي الترتيب: متغير البيئة ← ‏`.env` الخاص بالمشروع ← إعداد المستخدم، وقد جرى التحقق منه عبر اختبارات **سلوكية** لا بنيوية — فعكس ترتيب الاستدعاءين يُفشلها، وكذلك حذف الطبقة الثالثة.
    - **التنسيق `.env` وليس TOML**، عن قصد: فـ `python-dotenv` تبعية موجودة أصلًا، والصياغة موثقة بالفعل في ملفات README الخمسة عشر، والملف نفسه يخدم النطاقين. لا تبعية جديدة ولا صياغة جديدة. يتبع الموقع `XDG_CONFIG_HOME` إذا كان **مطلقًا** — إذ تطلب المواصفة تجاهل القيمة النسبية، وإلا عاد موقع الإعداد معتمدًا على الدليل الحالي — ويتبع `APPDATA` في Windows.
    - **خياران مستبعدان، مع توضيح السبب.** مخزن مفاتيح النظام (`keyring`) أكثر أمانًا على محطة عمل مكتبية، لكنه يفشل في البيئات بلا واجهة — الخادم، والحاوية، وCI — أي في حالة الاستخدام الأساسية نفسها للترجمة الدفعية؛ فهو مرشح جيد كخيار صريح، لكنه إعداد افتراضي سيئ. أما flag باسم `--api-key` فستضع المفتاح في سجل shell وتجعله ظاهرًا في `ps`.
    - **عند غياب المفتاح، لا يظهر تتبّع للاستدعاءات بعد الآن.** كان المستخدم يتلقى stack trace من Python يشير إلى `site-packages`، ورسالة تسمّي «البيئة أو .env» من دون توضيح مكان إنشاء الثاني. وهي تسرد الآن المواقع الثلاثة مع المسار الدقيق لكل منها، ويخرج الأمر بالرمز 2. شبكة الأمان **ضيقة عن قصد**: ‏`except ValueError` على مرحلة الإعداد وحدها. فتغليف التنفيذ كله كان سيحوّل عطلًا حقيقيًا وقع أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الإخفاق الذي يتعقبه هذا المستودع. ويقرأ اختبار مصدر `main()` لمنع ذلك.

  - **تصحيح — كان `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** لا يصعد `load_dotenv()` من الدليل الحالي عند استدعائه من دون وسيط، بل من الملف **المستدعي**، أي من `site-packages`. جرى قياس ذلك باستخدام نقطة دخول console حقيقية شُغلت من مشروع يملك `.env` خاصًا به: يعيد `find_dotenv()` القيمة `''` ولا يُحمّل المفتاح، بينما يعثر عليه `find_dotenv(usecwd=True)`. لم يكن الخطأ موجودًا عندما كانت الأداة لا تعمل إلا من المستودع المستنسخ؛ لكن النشر كان سيجعله منهجيًا، بعرض وحيد هو ظهور مفتاح API على أنه «مفقود» رغم صحة الإعداد.

  - **كان من الممكن أن تصبح ثلاث بوابات خضراء بعد أن توقفت تمامًا عن التحقق من أي شيء.** جرى تشديدها **قبل** النقل، عن قصد: فحاجز الحماية المكتوب بعد التغيير الذي يُفترض أن يلتقطه لا يثبت شيئًا. كل بوابة خضراء في المستودع الأصلي وتتحول إلى الحمراء في نسخة منقولة — وقد قيس الاتجاهان.

    - **يتجاهل Lizard مسارًا غير موجود من دون قول أي شيء**: ‏rc=0، و«تم تحليل 0 ملف». كان من الممكن أن تنتقل بوابة التعقيد من 158 function و2247 nloc إلى 3 functions و34 nloc، مع إخراج حجمه صفر byte. أصبح النطاق الآن مصفوفة يُتحقق من وجود كل عنصر فيها.
    - **لا يفشل `coverage run --source=` عند تطبيقه على وحدة غائبة**: يظهر تحذير على stderr فقط، مع rc=0 لكل من unittest و`coverage xml`، ويُنشر التقرير رغم ذلك — بعد انخفاضه من 1453 إلى 141 statement. كان المشروع سيبدو سليمًا لأنه لم يعد يُحلل إلا بالكاد. يحمي التقرير حدّان أدنيان: الإجمالي، وأكبر ملف جرى قياسه.
    - **مسبار حداثة الترجمات أعمى بنيويًا عن صيغة الاستدعاء**: فهو يرتكز على flags الخاصة بـ argparse، وهي تحديدًا ما لا يتغير عند إعادة تسمية الملف. جرت إعادة إنتاج ذلك: نُقلت الوحدة، وظلت ملفات README الخمسة عشر توثق أمرًا غير موجود، وكانت النتيجة «لا توجد ترجمة قديمة». لذلك يتحقق قسم سابع من **الصيغة** لا من الخيارات، ويُقارن hook الخاص بـ Lizard بالنطاق الفعلي للسكربت — فالمفتاح `files:` الخاص به لا يُفشل pre-commit عندما يتوقف عن المطابقة، بل يجعله **يتخطاه**.
  - **لم يعد `requires-python = ">=3.10"` مجرد ادعاء.** كان `sonar-project.properties` يعلن بالفعل عن 3.10-3.12 من دون أن يُختبر ذلك قط، إذ لم تكن محطة التطوير تحتوي إلا على 3.12 — وهو تناقض داخلي كان النشر سيكشفه للعلن. يشغّل سير عمل للاختبارات الآن الحزمة على 3.10 و3.11 و3.12، مع تثبيت الحزمة وبالتالي حدودها العامة.

  - **حدود دنيا، بلا سقف.** يظل `requirements.txt` ملف القفل المختبَر، ويصبح `[project.dependencies]` العقد العام: فنشر إصدارات ملف القفل الدقيقة سيتسبب في تعارضات لدى أي مستخدم لديه حزم أخرى. ولا يوجد سقف `<N+1` أيضًا — إذ سيتناقض مباشرةً مع `check-deps-fresh.sh`، الذي يُفشل بوابة الإصدار عند أي تأخر في إصدار رئيسي. تُحل مجموعة الحدود الدنيا، ويخرج الاختبار المضاد `openai==1.0.0` بالرمز `ResolutionImpossible`، ما يثبت أن الفحص يميّز بدلًا من قبول كل شيء. كما يمنع حاجزٌ إصدارَ `pyproject.toml` من الاختلاف عن إصدار CHANGELOG: لا يسمح PyPI بإعادة استخدام رقم إصدار.

  - **تم التحقق من البداية إلى النهاية داخل venv جديد**: ملف wheel بحجم نحو 70 كيلوبايت لا يحتوي إلا على `aipmt/*.py` وdist-info والترخيص؛ و`aipmt --help` بالرمز rc=0 مع 22 علمًا؛ و`python -m aipmt` يعرض «usage: aipmt» لا «usage: \_\_main\_\_.py»؛ وتثبيت `pipx` يعمل؛ والأهم من ذلك **ترجمة فعلية من الفرنسية إلى الإنجليزية انطلاقًا من أي دليل مستخدم**، مع الحفاظ على الخط العريض والقائمة والكود المضمّن والرابط وURL، وعدم ترجمة كتلة الكود. تنجح الاختبارات الـ318 السابقة للترحيل بقائمة معرّفات متطابقة بايتًا ببايت قبل الترحيل وبعده — وهذا ما يثبت عدم تعطيل أي اختبار، لا رسالة «OK»؛ وتُضاف إليها اثنا عشر اختبارًا للتهيئة ذات الطبقات الثلاث، ليصبح المجموع 330.

- **1.10.0** موفّر `--use_codex` (حصة اشتراك ChatGPT)، وتحديث SDK والنماذج، وإصلاح اقتباسات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — حاجزان وقائيان أضافهما PR من دون الالتزام بهما في كل المواضع**:

    - **كان الفحص المسبق لـCodex يمرّر `.env` كاملًا إلى الملف التنفيذي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: ورثت العملية الفرعية `os.environ` كاملًا، ومن ثم كامل `.env` الذي حمّله `load_dotenv`. أظهر القياس باستخدام ملف تنفيذي زائف مزوّد بأدوات رصد أن **سبعة أسرار** كانت تصل إلى الفحص المسبق — مفاتيح الموفّرين الستة إضافةً إلى `GITHUB_TOKEN` — مقابل **صفر** لنظيره `_grok_preflight`، الذي كان يمرّر `env=_grok_env()` فعلًا. كان التناقض داخل PR نفسه: يوجد `_strip_secret_env` تحديدًا للحفاظ على هذا الثابت، على بُعد بضعة أسطر. استُخرج `_codex_env_base()` وأصبح مشتركًا بين المسارين؛ والقياس بعد التصحيح: 0 سر في كلا الجانبين.
    - **لم تشمل خاصية «الإغلاق عند الفشل في `--deny`» الصيغة المستخدمة.** كان التعليق يبرر عزل Grok بالكامل بأن قاعدةً ذات بادئة مجهولة تؤدي إلى رفض بدء التشغيل. أظهر القياس على `grok 1.0.13` أن هذا التحقق لا يوجد **إلا للصيغة الموضوعة بين قوسين**: يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل («unknown tool prefix»)، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. لكن `GROK_DENY_RULES` لم يكن يستخدم سوى أسماء مجردة — ولذلك كان من شأن إعادة تسمية أداة من جانب xAI أن تزيل طبقة العزل الوحيدة المقاسة بلا أي إشارة، على محطة لا ينطبق فيها أصلًا sandbox نظام التشغيل. انتقلت القواعد الثماني المسماة إلى `Prefix(*)`، مع التحقق من كل واحدة بوصفها بادئة معروفة لدى CLI؛ وتظل قاعدة الالتقاط الشامل `*` بصيغتها الحرفية، وهي الصيغة الوحيدة المقبولة. يمنع اختبارٌ الرجوع إلى الصيغة غير المتحقق منها.
    - **تم التحقق من سلامة بقية الجوانب**: لا يوجد حقن أوامر (صيغة قائمة في كل المواضع، ولا يُستخدم `shell=True` مطلقًا، ومحتوى المستند يُمرر عبر stdin أو `--prompt-file`)، ولا توجد إزالة تسلسل غير آمنة (`json.loads` فقط، مع حاجز للنوع)، ولم يُعثر على تجاوز لإصلاح اجتياز المسارات باستخدام سبع حمولات، كما أن `--deny '*'` يطبقه CLI فعلًا (لوحظ `DENY_ENFORCED` عند قراءة خارج workdir).
    - كان فحص الحداثة المضاف أعلاه يتجاوز، بالمناسبة، مبدأه الخاص: كانت الحزمة التي يفشل طلبها إلى PyPI تُتخطى بصمت، وتظل البوابة خضراء. وهو الآن يحصي الحزم التي قورنت فعليًا ويفشل إذا كانت التغطية غير مكتملة.

  - **تحديث التبعيات، وإضافة شبكتي أمان لمنع تكرار التأخر**:

    - **كان التأخر حقيقيًا ومزمنًا**: `openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر سنتين في مخزن الشهادات الجذرية الذي يتحقق من TLS لجميع استدعاءات الموفّرين. تم تحديد السبب: **لم يكن `.github/dependabot.yml` موجودًا**. من دون هذا الملف، لا يفعّل GitHub سوى _تحديثات الأمان_، ولا يقترح Dependabot طلب PR إلا لتبعية مستهدفة بـCVE — وهذا يفسر تحديثه `urllib3` و`idna` مع ترك اثنين من SDK يتأخران بإصدار رئيسي كامل.
    - **يتعايش الإصداران الرئيسيان من دون تعارض**، خلافًا لما أوحى به استدلال سابق: ينتقل `openai` 3.x و`anthropic` 1.x إلى **`httpx2`**، بينما يظل `mistralai` و`google-genai` على `httpx<1`، لكنها توزيعات منفصلة. تم التحقق بواسطة تثبيت فعلي، ثم عبر **مسارات الموفّرين السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الكود المضمّن والروابط في كل مخرج. كان «تجنب مكدسي HTTP» تفضيلًا لا عائقًا: وحسم القياس المسألة.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: كانت `google-auth` و`cryptography` ومكدس `opentelemetry` مثبتة في venv العمل من دون التصريح بها قط — لذلك لم يكن التثبيت الجديد يعيد إنتاج ما يجري اختباره. وفي المقابل، كانت `tokenizers` و`huggingface-hub` و`PyYAML` مدرجة فيه من دون أن يستوردها أو يتطلبها أي شيء، وهي بقايا من `mistralai` 1.x. أُعيد إنشاء الملف بوصفه الإغلاق الكامل لـvenv مبني من التبعيات المباشرة وحدها. لا يبلغ `pip-audit` عن أي ثغرة معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** (جديد) يفعّل تحديثات الإصدار الأسبوعية لـpip وgithub-actions. تُجمع الإصدارات الثانوية والتصحيحية في PR واحد — إذ ينتهي الأمر بتجاهل طلب PR مستقل لكل تحديث تصحيحي، والضجيج عدو التحديث؛ أما **الإصدارات الرئيسية فتظل منفصلة**، ويتطلب كل منها تحققًا عبر استدعاء فعلي.
    - **`scripts/check-deps-fresh.sh`** (جديد وموصول بالبوابة) يجعل التأخر ظاهرًا في حكم المشروع: يقترح Dependabot التحديثات، لكنه لا يضمنها، وقد تتراكم طلبات PR الخاصة به. التأخر في إصدار رئيسي ← فشل؛ وفي إصدار ثانوي ← تحذير، لأن البوابة التي تظل حمراء دائمًا ينتهي بها الأمر إلى التجاهل؛ وتعذر الوصول إلى PyPI ← تخطٍّ صريح محليًا و**إغلاق عند الفشل في CI**، إذ إن الفحص الذي لم يُنفذ لا يُعد نجاحًا. تم التحقق في الاتجاهين: يلتقط الحالة الدقيقة السابقة للتصحيح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير عند وجود إصدار ثانوي.

  - **إصلاحات نتجت عن مراجعة PR هذا** — فحص خمسة وكلاء مراجعة الفرق بدقة؛ وقد **أُعيد إنتاج** جميع النقاط أدناه **بالقياس** قبل تصحيحها، وكان اثنان منها تراجعين أُدخلا أعلاه في الإصدار نفسه.

    - **تراجع تم إصلاحه — كان `_NEWS_CITATION_REGEX` يعاني من تراجع خلفي أُسّي.** أدخل إصلاح الفقرات المتعددة `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: كان توزيع المسافات بين `[ \t]+` و`.*` ملتبسًا، وكان هذا الالتباس يتضاعف من تكرار إلى آخر. أظهر القياس على أسطر `>   texte` — وهي مسافة بادئة مشروعة تمامًا في Markdown — لا تطابق النمط: **2,589 مللي ثانية لـ14 سطرًا** مقابل 0.04 مللي ثانية بعد التصحيح، بعامل يقارب 9 لكل سطر مضاف. في وضع `--news`، كان blockquote طويل وغير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب يمكن تحديده. يستهلك التكرار الآن السطر كاملًا دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يترك سوى طريقة واحدة للمطابقة في كل تكرار. تم التحقق على المجموعة الفعلية المؤلفة من 231 مقالًا: **صفر اختلاف** في الالتقاط، ونفس الاقتباسات الـ423، مع استمرار توسيع الأجسام الـ14 متعددة الفقرات.
    - **كان علَما موفّر متزامنان يؤديان إلى فوترة حسب الاستخدام بصمت.** كان `--use_codex --use_mistral` مقبولًا؛ ويختبر `_select_provider_client` Mistral أولًا، بينما يمنح `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. وهكذا كان المستخدم يطلب حصة اشتراكه ويحصل على فوترة حسب الاستخدام، من دون أدنى تحذير: وهذا بالضبط نمط الفشل الذي وُجد `--use_codex` لمنعه. تمر أعلام الموفّرين الستة الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: سطر أوامر يجمع بين موفّرين، وكان يُقبل بصمت حتى الآن، يفشل الآن عند `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة نهاية العمل تصبح خضراء عندما تتعطل مجساتها.** كانت أربعة من فحوص `scripts/check-release-ready.sh` الثلاثة عشر تتبع نمط «التقاط stdout، والاستنتاج إذا كان فارغًا» من دون فحص رمز الخروج قط: كان الاستثناء (ملف أُعيدت تسميته، `FileNotFoundError`) يكتب إلى stderr ويترك stdout فارغًا، فيستنتج الفحص «لا شيء يستدعي الإبلاغ». وهكذا أُعيد إنتاج فخ «لا يثبت `exit 0` شيئًا» داخل البرنامج النصي المكتوب لمنعه. يفرض المساعد `probe()` الآن رمز خروج صفريًا **و**علامة نهاية، وترفض المجسات الاستنتاج من مجموعة علامات فارغة — إذ يكون التوكيد على مجموعة فارغة صحيحًا دائمًا. الدليل: أدت إضافة المجموعة الحصرية أعلاه إلى تمرير أعلام الموفّرين عبر كائن `*_group`، ولم يعد التعبير النمطي القديم `parser\.add_argument\(` يطابقه؛ فخرجت **ستة أعلام من أصل واحد وعشرين** من النطاق بصمت، وظلت البوابة خضراء.
    - **كان مسح الأسرار يفوّت أربعة موفّرين من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: وكان `sk-proj-…` (تنسيق OpenAI الحالي) و`sk-ant-api03-…` ينقطعان عند الشرطة الثانية، ولم يكن `AIza…` مشمولًا. وُسّع النمط، واستُبعد `.secrets.baseline` من المسح. كما كان الحاجز `.env` يستعلم عبر `git diff --cached`، الذي لا يرى إلا الفهرس: لذلك لم يكن `.env` **المُدرج بالفعل في commit** — وهي أسوأ حالة — يظهر فيه قط. وهو يستعلم الآن عبر `git ls-files`.
    - **لم يكن «إحماء الرمز المميز» في Codex إحماءً فعلًا.** أظهر القياس أن `codex login status` لا يلمس `~/.codex/auth.json` (ظل وقت التعديل والحجم من دون تغيير)، وتقول مساعدته «Show login status». ومع ذلك كان التعليق يدّعي تحديث الرمز المميز «مرة واحدة، تسلسليًا»، ومن ثم تحييد خطر التحديثات المتزامنة لرمز مميز دوّار أحادي الاستخدام. لم تكن الحماية المعلنة موجودة؛ ويصف التعليق الآن ما يفعله الكود، بينما يظل الحل الحقيقي هو `max_jobs=4`. كما يحترم الفحص `CODEX_BIN`، الذي كان يتجاهله — إذ كانت محطة لا تحتوي على `codex` ضمن `PATH` تفشل برسالة «غير مصادق عليه»، وهو تشخيص مضلل.
    - **كان `.env` يُحمّل بواسطة source داخل shell فرعي.** يُستدعى `detect_provider` ضمن استبدال أمر، ولذلك لم تكن صادراته تنتقل إلى الأعلى: كان `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` المعرّف داخل `.env` يظل غير مرئي للقراءات المنفذة في `main()`، التي كانت تستنتج «تعذر العثور على ملف Grok التنفيذي» رغم صحة التهيئة.
    - **تجاوز التزامن الحد المعلن بنسبة 50%.** وُضع الحاجز بعد إطلاق زوج README/CHANGELOG: وبلغت الذروة المقاسة **3 لـ`max_jobs=2`**. وبالنسبة إلى Grok، الذي يتشارك حصته الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يكن البرنامج النصي يلتزم إذًا بالحد الذي فرضه على نفسه. أما العد النهائي فكان يُعرض من دون مقارنته قط بالعدد 28 — وكان غياب ملف يمر من دون ملاحظة.
    - **عقد مخرجات Grok: أصبح غياب `stopReason` فشلًا.** كان الكود يطبق «`end_turn` **أو غائب**» حيث يشترط العقد المعلن `end_turn`. وكانت حمولة بلا هذا الحقل — أو أُعيدت تسمية حقلها بفعل تحديث CLI — تحوّل الحاجز إلى عملية صامتة بلا أثر. كذلك لم يعد `max_turn_requests` مصنفًا على أنه تجاوز لمعدل الطلبات (فالمستنفد هو ميزانية الجولات: إعادة المحاولة تعيد النتيجة نفسها، بكلفة انتظار 90 ثانية)، وخرج `quota` من علامات تجاوز المعدل — للسبب الذي كانت سلسلة توثيق `_codex_is_rate_limited` تذكره بالفعل من دون أن يطبقه Grok.
    - **تُحفظ سلسلة Gemini مؤقتًا بحسب النموذج.** كانت تبدأ مجددًا من `minimal` عند كل مقطع، مع أن النموذج الافتراضي يرفضه: كان المسار الاسمي يدفع كلفة رحلة ذهاب وإياب 400 لكل مقطع ويعيد طباعة التحذير نفسه. فالتحذير الذي يتكرر مئات المرات يتوقف الناس عن قراءته — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مكتوبة مباشرةً لـCodex وتوجّه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق أساس العمليات الفرعية يعمم «shim» على أداتي CLI، مع أن ملف Grok التنفيذي هو ELF أصلي (والتبرير الصحيح هو «وكيل ينشئ عملياته الفرعية الخاصة»)؛ ووُسمت اثنتا عشرة نتيجة SAST على `subprocess` بالعلامتين `# nosec` / `# nosemgrep` مع تبرير، إذ تجعل صيغة القائمة من دون `shell=True` الحقن مستحيلًا، ولا يمر محتوى المستند عبر argv قط.
    - **لم يعد أي سر يدخل إلى العمليات الفرعية الوكيلة.** لم تكن قوائم الرفض الاسمية تحمي إلا ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). أظهر القياس أن **سبعة أسرار أخرى** كانت تدخل مع ذلك إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح CLI الآخر، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة البيانات. غير أن أداتي CLI هاتين هما **وكيلان**، ويعمل وكيل Grok من دون sandbox لنظام التشغيل قابل للتطبيق على كثير من محطات Linux. أصبح الترشيح يُجرى الآن **بحسب نمط الاسم** (`API_KEY` و`_TOKEN` و`SECRET` و`PASSWORD` و`CREDENTIALS`)، لا بحسب قائمة اسمية، ما يشمل أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن يعرفها هذا الكود. لا يحتاج CLI إلى أي منها: تعيش المصادقة داخل `~/.codex` و`~/.grok`، لا في البيئة أبدًا — وقد تم التحقق عبر **ترجمة فعلية مكتملة** بواسطة كل واحد من الموفّرين في البيئة المحصنة.
    - **الاختبارات**: ملف `tests/test_review_hardening.py` جديد (21 اختبارًا) يثبّت حصرية أعلام الموفّرين، وعقد `stopReason`، وخطية التعبير النمطي للأخبار، ورسالة الرفض في CI، وحفظ Gemini المؤقت، وغياب أي سر من بيئة العمليات الفرعية. وهذا التوكيد الأخير **عام** — فهو يفشل عند وجود مفتاح لا تسميه أي قائمة — بينما كان اختبار التنقية الموجود مرآة لثابته ولم يكن يستطيع اكتشاف أي شيء سوى تعطل حلقته الخاصة. المجموعة الكاملة تضم **311 اختبارًا**.
  - **موفّران جديدان لـ Grok**: ‏`--use_grok` (واجهة API من xAI، المفتاح `XAI_API_KEY`، والفوترة حسب الاستخدام) و`--use_grok_cli` (واجهة CLI الرسمية لـ Grok Build، تُحتسب من اشتراك Grok — بالمبدأ نفسه المتّبع في `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن نقطة نهاية xAI متوافقة مع OpenAI، أُعيد استخدام العميل و`_call_openai` كما هما، ولم يتغيّر سوى `base_url`. لم يلزم سوى تكييف واحد يستفيد منه الجميع: يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي تُصدرها xAI حيث تُصدر OpenAI ‏`stop`. النماذج: `grok-4.6` (للجودة) و`grok-4.3` (للوضع الاقتصادي). تجدر الإشارة إلى أن نموذج Grok الاقتصادي يظل الأغلى في المستودع — ‏$1.25/$2.50 لكل مليون، مقابل $0.15/$0.60 لـ `mistral-small-latest`: يُختار هذا الموفّر لتنوّع النماذج، لا للسعر.
    - **وضع CLI**: صُمّم على غرار Codex، مع أربعة اختلافات فرضها الواقع — يُمرَّر الموجّه عبر ملف (`--prompt-file`، لأن واجهة CLI لا تقرأ stdin، ولأن مقطعًا في argv سيكون ظاهرًا في `ps`)، ويكون الخرج كائن JSON واحدًا على stdout (لا JSONL ولا ملف `-o`)، ولا يتيح الاشتراك سوى `grok-4.6` و`grok-4.5`، كما لا يمكن تطبيق sandbox (انظر أدناه). جرى توحيد تشغيل العمليات الفرعية مع Codex في `_codex_run_process`، من دون المساس ببقية موفّر Codex الذي سبق اختباره.
    - **‏`exit 0` لا يثبت شيئًا، وفقًا للقياس**: عندما لا يكون المستخدم مصادقًا عليه، تكتب واجهة CLI ‏`{"type":"error","message":"Not signed in."}` إلى **stdout** مع رمز خروج **0**. ويتصرف الرفض أو تجاوز عدد الجولات بالطريقة نفسها. لذلك يتطلب عقد الخرج تحقق أربعة شروط متزامنة: رمز خروج 0، وغياب حمولة خطأ، و`stopReason == end_turn`، ونص غير فارغ. يتبع الفحص المسبق المنطق نفسه: ينتهي `grok models` بالرمز 0 حتى عند قطع الاتصال، ولا يمكن الاستنتاج إلا من وجود «not authenticated» في stdout.
    - **العزل: عدم تماثل مقصود وموثّق.** بينما يعمل Codex ضمن `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـ Grok على كثير من أجهزة Linux الحديثة، لسببين نظاميين مستقلين لا يمكن تجاوزهما من دون `sudo`: يمنع AppArmor مساحات أسماء المستخدمين غير المميّزين منذ Ubuntu 24.04 ‏(`bwrap: setting up uid map: Permission denied`، وأُعيد إنتاج المشكلة خارج Grok)، وتفشل قائمة حظر مقابس وقت تشغيل الحاويات عندما يكون `/run/podman` في `0700` (لا يعالج المحلّل سوى `ErrorKind::NotFound`، بينما يصبح EACCES خطأً فادحًا). المطبّ الأساسي: ملف تعريف **مدمج** يتعذر تطبيقه يؤدي إلى التشغيل من دون عزل **بصمت**. لذلك لا يطلب البرنامج النصي أي ملف تعريف افتراضي، ولا يتراجع بصمت مطلقًا — بل يصدر تحذيرًا على stderr. تعتمد الحماية على قواعد `--deny` في واجهة CLI، بما فيها قاعدة الالتقاط الشامل `*`، وهي الطبقة الوحيدة التي ثبت بالقياس أنها _fail-closed_ (تؤدي قاعدة ذات بادئة مجهولة إلى رفض التشغيل). يتيح `GROK_TRANSLATE_SANDBOX=read-only` فرض ذلك، وعندها يفشل التشغيل إذا تعذر على الجهاز الالتزام به.
    - **وسائل الحماية**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (إذ قد يؤدي وجود مفتاح إلى التحويل نحو الفوترة حسب الاستخدام؛ كما أن `GROK_SANDBOX` موروثًا سيفرض ملف تعريف غير قابل للتطبيق مع رسالة مضللة)، وتُعطّل مفاتيح MCP وhooks وskills وagents، مع `--disable-web-search` و`--no-subagents` و`--no-plan`، ودليل عمل مؤقت، ورفض التشغيل في CI، ومهلة زمنية تقتل مجموعة العمليات، وتراجع تدريجي عند بلوغ حد المعدل. ضُبط `--max-turns` على 6 لا على 1: يُزاد العداد بعد جولة الأدوات، وكانت القيمة 1 ستؤدي إلى اقتطاع الخرج.
    - **الحصة**: مجمّع Grok أسبوعي و**مشترك مع Chat وImagine وVoice**، ولا يعرضه أي أمر — بخلاف Codex الذي يتيح `account/rateLimits/read` فيه قياس الاستهلاك. لذلك يحد `regen_translations.sh` التزامن إلى عمليتين ويصدر تحذيرًا صريحًا.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` (24 اختبارًا). المجموعة الكاملة تضم **290 اختبارًا**.
  - **إصلاح خلل — لم تكن الاقتباسات الإنجليزية متعددة الفقرات محمية إلا جزئيًا (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل، بصفته متن الاقتباس، إلا سلسلة من أسطر `>` **المتتالية**. ما إن يمتد اقتباس عبر عدة فقرات (تفصل بينها سطر `>` فارغ)، لا يُلتقط ويُستبدل بعنصر نائب سوى آخرها؛ أما الفقرات السابقة فكانت تُرسل إلى LLM وتعود مترجمة — وهو النقيض تمامًا مما وُجد `--news` لضمانه. يقبل التكرار الآن أسطر `>` الداخلية الفارغة، وأصبح غير جشع، بحيث يتوقف عند `>` الفارغ السابق للسطر المائل بدلًا من أول سطر فارغ يصادفه.
    - **النطاق المقاس** على مجموعة فعلية من 198 مقالًا: تأثر 11 اقتباسًا من أصل 419. لا توجد أي انتكاسة — يلتقط التعبير النمطي الجديد العدد نفسه تمامًا من الاقتباسات؛ وحدها المتون متعددة الفقرات تتسع (408 متون متطابقة و11 متنًا موسعًا)، ولا يزال من المتعذر امتصاص سطر النسبة `> — …` داخل المتن (مع الإبقاء على lookahead).
    - **إثبات شامل من البداية إلى النهاية** على مقال حجمه 69 كيلوبايت مترجم إلى ja/ar: الفقرة الأولى من اقتباس، التي كانت تُعرض سابقًا بالصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم كذلك بالعربية، تظل الآن `> GLM-5.3 is now open-weight.`. عاد عدد أسطر الاقتباس الإنجليزية من 9 إلى 10، وهو العدد الموجود في المصدر.
    - تجدر الإشارة إلى أن أدوات التحقق اللاحقة لم تكتشف هذا الخلل، لأنها تتحقق من وجود الاقتباس من دون التأكد من اكتماله.
  - **توفير مقاس في الموفّر الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` متى بدأ النموذج بـ `gpt-5`، بما في ذلك عند استخدام `--eco`. القياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: ‏`medium` ← 45 رمز استدلال و65 رمز خرج؛ ‏`none` ← 0 و14. لا يضيف الاستدلال شيئًا إلى الترجمة، وكانت تكلفته تُدفع لكل مقطع من كل ملف. تصبح القيمة الافتراضية `none` في `--eco`، وتظل `medium` في غير ذلك؛ وتبقى للقيمة الصريحة الممررة عبر CLI الأولوية. يقبل `--reasoning_effort` الآن `none` و`xhigh`، بالإضافة إلى `low`/`medium`/`high` (لا تقبل جميع النماذج كل هذه القيم: فمثلًا يرفض `gpt-5.4-mini` القيمة `minimal` — وتغطي إعادة المحاولة الحالية من دون هذا المَعْلَمة تلك الحالة).
  - **تحديث حزم SDK وترحيل Gemini**: استُبدل `google-generativeai` (انتهى دعمه في 2025-11-30 وأُرشف مستودعه) بحزمة SDK الموحّدة **`google-genai`** — ‏`genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير موجّه النظام في `system_instruction` بدلًا من دمجه مع المقطع. رُقّي `mistralai` إلى **2.9.4** (أصبح الاستيراد `from mistralai.client import Mistral`؛ أما الإصدار القديم فيطلق `ImportError`، وفقًا للتحقق داخل wheel)، و`anthropic` إلى **0.125.0**، و`openai` إلى **2.54.0** — وهي أحدث الإصدارات قبل الانتقال إلى `httpx2`، تجنبًا لتعايش حزمتين من HTTP في venv. وبناءً على ذلك، أُزيل التثبيت عن `httpx` 0.28.1 و`pydantic` 2.13.5.
  - **انتكاسان اكتشفتهما اختبارات فعلية، لا الوثائق**:
    - يرفض `anthropic` بالإصدار ≥ 1.0 من جهة العميل استدعاءً غير متدفق إذا كان `max_tokens` يوحي بمدة تتجاوز 10 دقائق (`ValueError: Streaming is required...`). لم تكن وسيلة الحماية هذه موجودة في 0.34.2، وكانت تعطل كل استدعاء لـ Claude باستخدام `max_tokens=32768`. أُصلح ذلك بإضافة `timeout` صريح (`CLAUDE_TIMEOUT`، بقيمة افتراضية 900 ثانية)، ما يتجنب الانتقال إلى التدفق لاستدعاء لا يُستخدم منه سوى الرد الكامل.
    - لا يقبل `thinking_level="minimal"` إلا جزء من كتالوج Gemini: يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` بخطأ 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تسلسل تراجعي `minimal` ← `low` ← من دون thinking_config، على غرار آلية التراجع الموجودة أصلًا في OpenAI — إذ يجب ألا تتسبب مَعْلَمة تحسين أبدًا في فشل ترجمة.
  - **تجديد النماذج الافتراضية**، وقد جرى التحقق من كل منها باستدعاء فعلي: ‏OpenAI ‏`gpt-5.5` ← **`gpt-5.6-terra`** (−60% على دفعة من 28) و`gpt-5.4-mini` ← **`gpt-5.6-luna`** (−73%)؛ ‏Claude ‏`claude-sonnet-4-6` ← **`claude-sonnet-5`** (أرخص وأحدث) و`claude-haiku-4-5-20251001` ← **`claude-haiku-4-5`** (المعرّف القياسي من دون تاريخ)؛ ‏Gemini ‏`gemini-3.1-pro-preview` ← **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` ← **`gemini-3.1-flash-lite`** (إصدار مستقر وأرخص من `3.5-flash-lite`). بقي Mistral دون تغيير، إذ يظل `mistral-large-latest` أفضل نسبة جودة إلى سعر بين الأربعة. تجدر الإشارة إلى أنه لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — فلم يصدر Gemini 3.5 Pro قط رغم الإعلان عنه في مايو 2026؛ وسلسلة 3.5/3.6/3.7 مخصصة حصريًا لـ Flash.
  - **اختبار A/B مقاس قبل الانتقال إلى Gemini**: تُرجم `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. البنية متطابقة تمامًا (21 قائمة، و18 كتلة برمجية، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، خلال **8 ثوانٍ مقابل 48 ثانية**. وبغياب أي معيار أداء عام يقارن هذين النموذجين في الترجمة أو النصوص غير اللاتينية، كان الانتقال سيعتمد لولا ذلك على افتراض محض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفذ `block.text for block in response.content` من دون تصفية النوع. تُدرج النماذج ذات الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، تكشف `.thinking` لا `.text` — ما كان سيؤدي إلى تعطل الترجمة بسبب `AttributeError` مبهم عند أول مقطع. تُستبعد الآن كتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` (قائمة سلبية، للحفاظ على التسامح مع نوع مجهول يحمل نصًا)، وتُطلق الاستجابة الخالية من أي كتلة نصية خطأً صريحًا. يُمرر `thinking={"type": "disabled"}` في كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: حُذفت النماذج التي انقضى تاريخ سحبها (سلسلة `magistral-*` المسحوبة في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620`، و`claude-3-7-sonnet-20250219`، و`claude-opus-4-1-20250805`، و`claude-sonnet-4-20250514`). تصحيحات الحدود: ‏Mistral ‏128K ← **256K** (جيل Large 3 / Small 4)، وGemini ‏1 000 000 ← **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` ‏200K ← **1M**، وعائلة `gpt-5.6-*` ‏400K ← **1,05M**. أُضيف Claude 5 ‏(`claude-sonnet-5`، و`claude-opus-5`، و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وسلسلة `ministral-*`. تجدر الإشارة إلى أن هذه الحدود تظل إرشادية، إذ يحد `translate()` التقسيم عند `min(16000, limite)`.
  - **Provider ‏`--use_codex`**: خامس provider يتحكم في CLI الرسمي لـ Codex ‏(`codex exec`) في الوضع غير التفاعلي، بدلًا من استدعاء API تُحتسب تكلفتها حسب الاستخدام. تُخصم الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقًا. وهذا هو المسار الوحيد الذي توثقه OpenAI لهذا الاستخدام: إذ تدرج مصفوفة التوفّر حسب الخطة «Codex SDK و`codex exec` وسير العمل القابل للبرمجة» بوصفها متاحة في Plus/Pro/Business/Enterprise، بينما لا تصادق رموز `~/.codex/auth.json` على استدعاءات API Platform (ولا يقرأها هذا السكربت مطلقًا — إذ تظل المصادقة وتحديثها تحت إدارة CLI).
  - **ملف Codex التنفيذي قابل للتثبيت عبر pip، وليس عبر npm فقط بعد الآن**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK ‏`openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عام لاستخدام `--use_codex`. لا تُضاف الحزمة إلى `requirements.txt`: إذ يبلغ حجم الملف التنفيذي نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين من أجل provider اختياري. جرى التحقق من البداية إلى النهاية: مع غياب `codex` عن `PATH`، يعثر الحل على الملف التنفيذي المضمّن في الحزمة وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: يُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. من دون هذا الحاجز، قد يؤدي وجود مفتاح في `.env` إلى تحويل Codex إلى الفوترة حسب الاستخدام دون أي إشارة ظاهرة — وهو بالضبط ما وُجد هذا provider لتجنبه.
  - **مزالق CLI محمية باختبارات**:
    - يقرأ `codex exec` الإدخال القياسي **حتى** عند تمرير الموجّه كوسيط: من دون إغلاق الإدخال القياسي، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج مطلقًا (أُعيد إنتاجه: خروج 124 بعد 180 ثانية، وصفر بايت). لذلك يُعد `communicate(input=...)` إلزاميًا.
    - إن `codex` المثبّت بواسطة npm هو shim لـ Node ينفّذ `spawn` للملف التنفيذي الحقيقي المكتوب بـ Rust: وهذا الأخير **عملية حفيدة** لعملية Python وينجو من `SIGKILL` الخاص بـ `subprocess.run(timeout=)`، حيث سيواصل استهلاك الحصة. ومن هنا استخدام `Popen(start_new_session=True)` + `os.killpg`.
    - قد يخرج CLI بالرمز 0 رغم إصداره `turn.failed`: لذلك يُفحص خرج JSONL ‏(`--json`) إلى جانب رمز الإرجاع، كما أن غياب ملف `-o` مع رمز 0 يطلق خطأً صريحًا بدلًا من إنتاج مقطع فارغ.
  - **تراجع أُسّي عند بلوغ حد المعدل**: لا ينفّذ CLI أي إعادة محاولة داخلية (`max_retries = 0`). يجري التصنيف بناءً على بنية حمولة JSON ‏(`status: 429` / `error.type`)، لا على السلاسل الفرعية — إذ تظهر كلمة «الحصة» في خطأ 429 قابل للتعافي وفي `insufficient_quota` نهائي على حد سواء.
  - **حاجز CI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفًا. فالمصادقة عبر الاشتراك غير مخصصة لـ runner مشترك، وتحذّر OpenAI صراحةً من سير العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` ‏(`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وAPI Platform، لكن حساب ChatGPT لا يملك حق الوصول إلى جميع نماذجها: إذ تُطبّق قائمة السماح من جهة الخادم، من دون تحقق محلي، ويؤدي اختيار نموذج غير معتاد إلى إطلاق تحذير. في خطة Plus، يوفّر Luna ما بين 250 و2,000 رسالة لكل نافذة مدتها 5 ساعات، مقابل 10 إلى 100 لـ Sol: لذا فإن `--eco` هو الوضع الموصى به لكل معالجة دفعية.
  - **إصلاح خلل — كان `regen_translations.sh` يخرج بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` تابع لـ `main()` لم يعد موجودًا عند تنفيذ trap. تحت `set -u`، كان ذلك يطلق `failed_log: unbound variable` ويجعل السكربت يخرج بالرمز 1 رغم صحة الترجمات الـ28 — ما كان سيوقف `release.sh --auto` ‏(`set -e`) مباشرةً بعد إعادة التوليد، في أكثر المراحل تكلفةً. أصبح المتغير عامًا، وصار trap يختبر وجوده. أثر جانبي مفيد: أصبحت إخفاقات الترجمة الحقيقية، التي كان هذا الخطأ يخفيها حتى الآن، ظاهرةً مجددًا في الملخص النهائي.
  - **`REGEN_MODEL`**: متغير بيئة جديد في `regen_translations.sh` يفرض نموذجًا محددًا بدلًا من النموذج الافتراضي للـ provider، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج المتقدم ضمن حصة الاشتراك بدلًا من النموذج `--eco` الموجّه للحجم.
  - **`regen_translations.sh`**: أصبح `REGEN_PROVIDER=codex` متاحًا عبر تفعيل صريح (ولا يُكتشف تلقائيًا مطلقًا، تفاديًا لاستهلاك حصة اشتراك من دون علم المستخدم). يُحدّث الرمز مرة واحدة تسلسليًا قبل بدء التوازي — لأن تحديث Codex دوري وأحادي الاستخدام، وستؤدي المهام المتزامنة إلى إبطال جلسة `codex login` — كما يُخفض التزامن إلى 4.
  - **إعادة الهيكلة المرتبطة**: ينتقل `_dispatch_provider_call` من 8 معاملات إلى 6 عبر `_resolve_provider()` يعيد اسم provider، بدلًا من تمرير قيمة منطقية رابعة عبر السلسلة بأكملها. وتظل القيم المنطقية الصريحة مقدّمة على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` أدنى.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` ‏(48 اختبارًا) يغطي argv، والبيئة المنقّاة، وعقد منع المقدمة، والإخفاقات الصامتة، والمهلة الزمنية/killpg، والتراجع الأُسّي، والفحص المسبق، وحل الـ provider، وتسلسل الاستدلال في Gemini، وتصفية كتل Claude، واستشهادات الأخبار متعددة الفقرات. تضم الحزمة الكاملة 290 اختبارًا.
  - **تحقق فعلي**: يترجم `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex ببنية مطابقة تمامًا للترجمات المرجعية (14 كتلة برمجية، و24 عنوانًا، و25 سطر جدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل برمجية متطابقة حرفًا بحرف، ومن دون أي بقايا لعناصر نائبة). وفي مقال صحفي حجمه 69 كيلوبايت بوضع `--news`، يجتاز خرجا `gpt-5.6-luna` و`gpt-5.6-sol` كلاهما مدقّق التطبيق اللاحق للغات en/ja/ar. أما الاستهلاك المقاس عبر `account/rateLimits/read` فقد ظل أدنى من عتبة تقريب العداد (0٪ من نافذة 5 ساعات) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL لإسناد الأخبار الذي يتضمن أقواسًا متداخلة أو بادئة فرنسية (2026-05-11):

  - **إصلاح خلل**: كان استخراج عناوين URL للإسناد في `_protect_news_quotes` يستخدم regex ‏`re.search(r"\((.+?)\)", attribution)` (التقاطًا كسولًا بين قوسين). في إسناد من النوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` محيطة + `]()` لرابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة تتضمن البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائية). النتيجة: كان `_validate_news_post` يبحث عن هذه السلسلة في الخرج المترجم ويفشل دائمًا (لسببين: `)` مبتور + ترجمة "relayé par" إلى `relayed by`/`weitergeleitet von`/...). ولم يكن التسلسل الكامل low ← medium ← high ← gpt-5.5 قادرًا على النجاح.
  - **الإصلاح**: أصبح regex هو `re.search(r"\]\(([^)]+)\)", attribution)` — إذ يستهدف تحديدًا `](url)` الخاص برابط Markdown، ويلتقط **عنوان URL الصافي فقط** (من دون بادئة فرنسية أو بتر)، وهو ثابت تحافظ عليه العناصر النائبة `#URL{N}#` أثناء الترجمة. ويتحمل النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس محيطة
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py` ضمن الفئة `TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة الدقيقة المعاد إنتاجها من خلل Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (نسخة متغيرة تتضمن `via`)
  - **فجوة التغطية**: يتحقق `check-editorial-coverage.py` من صحة الصياغة التحريرية، لا من قابلية ترجمتها بواسطة المترجم. ومن التحسينات الممكنة (خارج نطاق v1.9.2) إضافة فحص يحاكي استخراج الإسناد في dry-run لاكتشاف الأنماط الخطرة **قبل** النشر.

- **1.9.1** إصلاح i18n لتسمية CTA في ملاحظة علامة الترجمة (2026-05-10):

  - **إصلاح خلل**: كانت تسمية `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط العلامة أعلى الملفات المترجمة تبقى **بالفرنسية** لجميع اللغات المستهدفة، بدلًا من اتباع `target_lang`. لا يراها LLM مطلقًا (إذ تُجمّع من جهة Python للحفاظ على عنوان URL وslug الخاص بالمستودع)، ولذلك لم تكن مرحلة الترجمة قادرة على تداركها. وهو انحدار صامت منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ15 بتسمياتها الموطّنة. وأصبح `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` يمرران اللغة المستهدفة. مع fallback إلى `fr` إذا كانت اللغة مجهولة (للسلامة ومنع KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link` ‏(target_lang ‏`ja` ← التسمية اليابانية المتوقعة). واختباران جديدان: `test_source_link_label_localized_per_target_lang` (معلّم على 7 لغات تغطي الكتابات اللاتينية والتصويرية والأبجدية الصامتة) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختبارًا في `test_translation_note_position.py` (بدلًا من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع بقيمة افتراضية `target_lang="fr"` — تستمر الجهات البرمجية الخارجية المستدعية من دون `args.target_lang` في العمل بلا أي تعديل.
- **1.9** إصلاح الفشل الصامت + أدوات جودة متكاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + تنسيق العلامة "embed card"**:
    - خيارات CLI جديدة (إضافية، الإعدادات الافتراضية دون تغيير → **غير كاسرة للتوافق**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): يضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - يعيد `legacy` إنتاج سلوك v1.8 بدقة (فقرة غامقة `**…**`) **بايتًا ببايت**.
        - يُصدر `marker` تعريف مرجع رابط Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`)، يتبعه **اقتباس من 3 فقرات** مُهيكل ليُعرض على هيئة "GitHub repo embed card": عنوان المشروع ضمن كود سطري (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف يترجمه LLM، ورابط دعوة إلى الإجراء (`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. ويمكن الاستفادة منه في أثناء البناء عبر ملحق remark (راجع مدونة jls42.org ← الملحق `remark-translation-banner`).
    - **ثوابت لا تُرسل مطلقًا إلى LLM**: يُجمّع عنوان المستودع وعنوان URL الخاص بـ GitHub في جانب Python بعد ترجمة العبارة الوصفية. ولا يرى LLM مطلقًا المعرّف اللطيف `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تغيير أي عارض أو حالة أحرف أو مخطط.
    - **إدراج واعٍ بالـ frontmatter**: في وضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة `---` الختامية** للـ frontmatter بتنسيق YAML (أمان Astro Content Collections / gray-matter). يكتشف المساعد `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف ويحافظ على سلامته؛ كما **يطلق `RuntimeError`** عند وجود frontmatter مفتوح من دون سياج إغلاق (فيُدرج الملف ضمن `failed_files` بدلًا من كتابته مع ملاحظة في موضع خاطئ).
    - **منقٍّ للنموذج بقائمة سماح**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع استخدام `unknown` كقيمة احتياطية إذا كانت النتيجة فارغة. ويتوافق ذلك مع أداة التحقق في ملحق remark الخاص بـ Astro، ويحيّد المحارف التي قد تكسر تنسيق العلامة (المسافة، وعلامة الاقتباس، والقوس، والفاصلة، وغيرها).
    - **إعادة هيكلة داخلية**: `_append_translation_note` (دالة واحدة ضخمة) ← 7 مساعدين خالصين (`_translation_note_invariants`، و`_build_translation_note_phrase`، و`_assemble_translation_note_paragraphs`، و`_build_translation_note_source`، و`_sanitize_model`، و`_quote_lines`، و`_split_frontmatter`، و`_build_translation_note_block`، و`_compose_with_notes`). فُصل المنشئ عن المؤلف (يعيد المنشئ كتلة خالصة بلا فاصل، ويطبق المؤلف `\n\n` وفقًا للموضع)؛ ويشترك كود الإنتاج ومساعد المصدر في المُجمِّع نفسه ذي الفقرات الثلاث.
    - **يحافظ `_quote_lines` على الأسطر الفارغة**: يضيف `> ` قبل كل سطر، مع تحويل الأسطر الفارغة إلى `>` وحده. ويتيح ذلك لـ mdast رؤية 3 فقرات منفصلة داخل الاقتباس (العنوان / الوصف / الرابط) بدلًا من فقرة واحدة تحتوي على فواصل أسطر.
    - **`_build_translation_note_block` تكيّفي**: وفقًا لعدد الفقرات التي حافظ عليها LLM (3 = تنسيق البطاقة الكامل، 2 = عبارة + رابط، 1 = قيمة احتياطية). ولم تعد القيمة الاحتياطية ذات الفقرة الواحدة **تُغلَّف بـ `**...**`** عند اكتشاف رابط Markdown ‏`](` (إذ يكون عرض `<strong>` حول رابط هشًا).
    - **توافق تصاعدي**: `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` في جانب `_compose_with_notes` — تستمر مساحات الأسماء التي لا تحتوي على هاتين السمتين (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) في العمل من دون تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI، وMistral، وClaude، وGemini): طبقة حتمية (العثور حرفيًا على مقتطف المصدر) + طبقة احتمالية (`langdetect`)
    - قائمة سماح `finish_reason` / `stop_reason`: إطلاق `RuntimeError` عند أي حالة خارج قائمة السماح (الاقتطاع، وcontent_filter، وغيرهما)
    - `max_tokens` في Claude: ‏`4096` ← `32768` (يتجنب الاقتطاع الكامن في المقاطع ذات 16k، مع هامش للتحويل بين أنظمة الكتابة FR→JA/ZH/KO/AR/HI)
    - تقسيم واعٍ بالعناوين: إعطاء الأولوية لـ H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى رمز خروج غير صفري: يعيد `translate_markdown_file` حالة ذات نوع `success` / `failure` / `skipped`، ويستخدم `main()` القيمة `sys.exit(1)` إذا فشل ملف واحد على الأقل (في وضع الملف الواحد والدُفعات)
    - حارس المحتوى الفارغ لدى جميع المزوّدين، وفحص معقولية نسبة المصدر إلى الناتج (≥ 500 محرف، و< 5% = رفض)، والتحقق من العناصر النائبة للكود (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، وإعادة محاولة `BadRequestError` من دون `reasoning_effort`
    - إضافة الاعتماد `langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("حزمة EurekAI المتكاملة"، 14 خطافًا):
    - Pre-commit: ‏ruff ‏(lint+format)، وshellcheck، وprettier ‏(md/yaml/json)، وdetect-secrets (حماية 4 مفاتيح API)، وLizard ‏(CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، وEOF، والملفات الكبيرة، وshebangs، وغيرها)
    - Pre-push: ‏mypy (وضع متساهل تدريجي)، وOpengrep SAST ‏(translate.py + scripts/)، وpip-audit (وضع الإبلاغ الأولي)، وunittest discover ‏(tests/ + scripts/tests/)
    - أغلفة محلية ضمن `scripts/` تستخدم `./venv/bin/python`
    - `scripts/audit_verdict.py`: محلل JSON لـ pip-audit مع 11 اختبار unittest، وهو نقل إلى Python لمحلل jls42-astro
    - إصلاح 7 مخالفات أولية من ruff: ‏B904 ‏(raise from) ×2، وB007 ‏(مجلدات غير مستخدمة)، وC408 ‏(dict literal)، وC419 ‏(list-comp)، وSIM105 ‏(contextlib.suppress)، وSIM110 ‏(any())
    - يستبعد Lizard مؤقتًا `translate.py` (4 دوال ذات CCN بين 21 و47، وإعادة الهيكلة مخطط لها) — بوابة صارمة على scripts/
  - **SonarCloud + تغطية شاملة**:
    - سير عمل GitHub Actions ‏`SonarCloud` ‏(sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع التغطية عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى README ‏(Quality Gate، وتصنيفات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code)
    - `tests/test_silent_failure.py` ‏(`unittest` من المكتبة القياسية): يغطي الحلقات الست في سلسلة أخطاء الفشل الصامت
    - `tests/test_orchestration.py` ‏(+79 اختبارًا): يغطي طبقة التنسيق في `translate.py` ‏(`_resolve_*_filename`، و`_existing_translation_exists`، و`_record_translation_status`، و`_write_output_file`، و`translate_directory`، و`_validate_input_paths`، و`_init_*_client`، و`_select_provider_client`، و`_normalize_collapsed_markdown`، و`_cleanup_source_flag`، و`_validate_news_flags_*`، والقيم الاحتياطية لـ `_openai_create_with_fallback` من TypeError + BadRequestError، وتنسيق الموجّه لسلسلة o1، وفروع الإرجاع المبكر في `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` ‏(stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **التغطية على الكود الجديد**: 75.5% ← نحو 98% ‏(translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك E2E ‏`marker+top|bottom|both` و`legacy+top|bottom|both`)، وإضافة البادئة متعددة الأسطر، والتوافق الرجعي بايتًا ببايت (قيمة golden حرفية)، والمنقّي، وتقسيم frontmatter (بما في ذلك إطلاق استثناء عند عدم إغلاق السياج)، وتنسيق الفقرات الثلاث، والقيمة الاحتياطية ذات الفقرتين، وحارس الفقرة الواحدة + رابط Markdown، وحاجز حماية حرج `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان وعنوان URL لا يُرسلان مطلقًا إلى LLM. **نجح 190 اختبارًا**، بلا أي انحدار.
  - التوثيق: `README.md` ‏(بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` ‏(سير عمل pre-commit + مراقبة CI بالتفصيل)، وإعادة توليد 28 ترجمة
- **1.8** وضع `--news` + ترقية نماذج 2026 ‏(2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - جودة OpenAI: ‏`gpt-5` ← `gpt-5.4`
    - OpenAI الاقتصادي: `gpt-5-mini` ← `gpt-5.4-mini`
    - جودة Gemini: ‏`gemini-3-pro-preview` ← `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` ‏(400k) و`gemini-3.1-pro-preview` ‏(1M)
  - وضع `--news` الأولي: حماية الاقتباسات الإنجليزية بعناصر نائبة `#NEWSQUOTE\d+#`، وربط `LANG_FLAGS` ‏(15 لغة)، وإدارة الأعلام حسب اللغة المستهدفة
  - التحقق من عناصر الأخبار النائبة قبل استعادتها (انحدار: كان حذف LLM للعنصر النائب يُنتج بصمت مخرجًا بلا اقتباس)
  - جعل السكربت `regen_translations.sh` قابلًا للنقل (مسارات مطلقة، ولا يعتمد على pwd)
  - إضافة رابط الفرنسية إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة
- **1.7** الميزات الجديدة:
  - خيار `--keep_filename` للاحتفاظ باسم الملف الأصلي عند الترجمة
  - دعم ملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على الكود السطري**: أصبحت علامات backticks ‏(`` `...` ``) محمية في أثناء الترجمة
  - تحسين موجّه النظام:
    - إدارة أفضل لعلامات الاقتباس في YAML frontmatter
    - حماية متغيرات القالب `{variable}`
    - منع ملاحظات المترجم غير المطلوبة
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** الميزات الجديدة:
  - دعم Google Gemini API للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI: ‏`gpt-5` (الجودة)، و`gpt-5-mini` (اقتصادي)
    - Claude: ‏`claude-sonnet-4-5` (الجودة)، و`claude-haiku-4-5` (اقتصادي)
    - Gemini: ‏`gemini-3-pro-preview` (الجودة)، و`gemini-3-flash-preview` (اقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف واحد (`--file`) من دون اجتياز مجلد
  - نمط تسمية مبسط جديد: `{base}-{lang}.md`
  - خيار `--include_model` للاحتفاظ بالتنسيق القديم مع اسم النموذج
  - دعم النماذج غير المدرجة مع حد افتراضي للرموز (128k)
  - ترجمة README إلى 14 لغة
- **1.5** التحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين موجّهات الترجمة:**
    - أُثريت موجّهات الترجمات المباشرة وملاحظات الترجمة لتحسين الوضوح والكفاءة، بما في ذلك تعليمات مفصلة بشأن الحفاظ على البيانات الوصفية وعناصر التنسيق المحددة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم عمليات الاستيراد لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص وإدارة كتل الكود للحفاظ على التنسيق الأصلي في أثناء الترجمة.
  - **إدارة ملفات الإخراج:**
    - عكس ترتيب النموذج واللغة في اسم ملفات الإخراج (على سبيل المثال، `f"{base}-{args.target_lang}-{args.model}.md"`)، ما يسهّل تنظيم الترجمات والعثور عليها.
  - **تحسينات متنوعة:**
    - تنظيف الكود عبر إزالة الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكربت وسهولة قراءته.
- **1.4** الميزات الجديدة:
  - دعم Claude API من Anthropic للترجمة
  - تحسين الموجّهات لزيادة الوضوح والكفاءة
  - تعديلات طفيفة لتحسين قابلية صيانة الكود
- **1.3** تحسينات وميزات جديدة:
  - تحسين إدارة كتل الكود
  - تحسين إدارة ملفات الإخراج
  - تحسين اكتشاف الملفات الموجودة
  - خيار `--force` لفرض الترجمة
  - عكس ترتيب النموذج واللغة في اسم ملف الإخراج
- **1.2** إصلاح سجل التغييرات
- **1.1** إضافة دعم Mistral AI API
- **1.0** الإصدار الأولي - دعم OpenAI API

**مقال مترجم من الفرنسية إلى العربية باستخدام gpt-5.6-sol.**
