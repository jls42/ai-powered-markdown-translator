### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.14.0** جدول توافق في مقدمة README، وحملة القياس التي يستند إليها (2026-09-09):

  - **الحكم حسب النموذج، في أعلى README، بالفرنسية.** كانت أكثر من ثلاثمئة ترجمة تنتظر في منتصف المستند، موزعة على ثلاثة جداول كان لا بد من قراءتها ومقارنتها. يحسم الجدول الجديد الأمر بسطر واحد لكل نموذج، ويذكر خصوصًا موضع الاختلاف: «كلمة واحدة منسية بالخط العريض، في العربية واليابانية» بدلًا من نسبة. لم يكتشف المقارن أي اختلاف لدى ثلاثة نماذج في المستندين الكثيفين — `gemini-3.7-flash` و`gpt-5.6-sol` عبر اشتراك ChatGPT، و`z-ai/glm-5.2` عبر OpenRouter.
  - **حملة إعادة قياس شملت 176 ترجمة.** تعود القياسات الأصلية إلى 4 سبتمبر، وكانت تخص README تغيّر كثيرًا منذ ذلك الحين. أُعيد قياس ثمانية نماذج على README هذا، ونموذجين على مقال رصد من المدونة في وضع `--news`، ونموذج واحد على أربعة ملفات README عامة — FastAPI وOllama وtldr-pages وVue.js. استحقت ثلاث نتائج أن تُدوّن: يفقد Mistral **عنوان قسم كاملًا** في العربية والهندية والكورية، من دون أن تلاحظ ذلك أي آلية حماية في مسار المعالجة — فلا يمكن استبدال العنوان برمز، ولا شيء يتحقق منه حاليًا أثناء الترجمة، رغم أن المقارن يكتشفه لاحقًا؛ ويفشل Grok في خمس لغات ضمن مقال رصد، إذ يفقد أربعة مقاطع من التعليمات البرمجية المضمنة وثلاثة عناوين URL منذ المقطع الأول، مع أنه يصمد على README؛ ويُنتج Gemini 55 ترجمة بلا اختلاف من أصل 56 عبر ملفات README العامة الأربعة.
  - **دخول مقارن البنية إلى المستودع.** كان README يصف هذا البروتوكول بأنه الطريقة الصحيحة لتقييم نموذج — «قارنوا عدد الأقسام والروابط وعناوين URL المميزة وكتل التعليمات البرمجية» — من دون توفير الأداة. `scripts/compare_structure.py` هو الذي أنشأ هذه الجداول، ومعه أحد عشر اختبارًا تثبت أنه يكتشف فقدان عنوان URL وكتلة تعليمات برمجية وقسم وتعليمة برمجية مضمنة وصف جدول واقتباس. ويتضمن التصحيحين الخاصين بالكتابات غير اللاتينية اللذين جعلاه موثوقًا: لم يعد عنوان URL المتبوع بقوس كامل العرض يُحسب على أنه مختلف، ولم يعد الاقتباس الأقصر باللغة الصينية يُعد اقتباسًا مفقودًا.
  - **فحص الحزمة قبل النشر وبعده.** تعمل مجموعة الاختبارات على المصادر وتظل ناجحة رغم أن الحزمة الموزعة معطلة — وحدة منسية في إعداد setuptools، ونقطة دخول لا يمكن حلّها. يبني `scripts/check-package-smoke.sh --wheel` ملف wheel، ويثبته في venv مؤقت خارج المستودع، ويتحقق من أنه لا يحتوي إلا على الحزمة، وأن طريقتي التشغيل تستجيبان، وأن جميع الوحدات قابلة للاستيراد، ثم يترجم مستندًا حقيقيًا ويقارن بنيته. يفعل `--pypi` الشيء نفسه انطلاقًا من الفهرس: فهو الفحص الوحيد الذي يثبت أن وحدات البايت هذه تحديدًا هي التي نُشرت، ولا يسمح PyPI أبدًا بإعادة نشر إصدار. تنفّذ بوابة `--full` الجزء غير المتصل منه، من دون استدعاء نموذج أو تكبد تكلفة.
  - **تصحيح سبع عبارات في README، بعد إعادة إنتاج كل واحدة منها قبل إعادة صياغتها.** كانت عبارة «لا يصل أي شيء خاطئ إلى قرصك» غير صحيحة: فالنموذج الذي يحذف قسمًا أو يعدّل تاريخًا في front matter يُنتج ملفًا يكتبه مسار المعالجة بالحالة `success`، وقد أُعيد إنتاج ذلك باستجابة محاكاة. يوضح README الآن ما تحميه الرموز — كتل التعليمات البرمجية والتعليمات البرمجية المضمنة وعناوين URL والمراسي والاقتباسات — وما لا تتحقق منه آليات حماية مسار المعالجة حاليًا. لا تعني عبارة «بلا اختلاف» أن المحتوى «متطابق»: فالمقارن يعدّ العناصر من دون قراءة محتواها، ولا يبلغ عن حذف عنوان من المستوى 4، ولا استبدال نص تعليمة برمجية مضمنة، ولا تبديل علم — وهي ثلاث حالات جرى قياسها، وحُذفت عبارة «الأعلام نفسها»، إذ لا يملك المقارن أي عدّاد للأعلام. لم يكن تثبيت بيئة المساهمة ينتج أداة قابلة للاستخدام: لا يثبّت `pip install -r requirements.txt` سوى الاعتماديات، وكان `python -m aipmt` يجيب بـ`No module named aipmt`، وكان `pip install -e .` مفقودًا — وكذلك سطر تثبيت `pre-commit`، الغائب عن ملفي الاعتماديات. كان الوعد المتعلق بالسياقات الصغيرة في OpenRouter يعلن عكس ما يفعله البرنامج: يُرفض أي نموذج تقل نافذة سياقه عن **16 400 tokens** قبل أي استدعاء، وقد قيس ذلك عند 4 095 و8 192 و16 384. لم يكن المرشح الذي يمنع ملف `.env` تابعًا للمشروع من إعادة توجيه استدعاءات API موثقًا في أي مكان؛ وأصبح الآن موثقًا مع قائمة المتغيرات المعنية. وأخيرًا، رقمان: كانت إحدى خلايا Grok في الجدول الرئيسي مأخوذة من حملة 9 سبتمبر، بينما كان عمودها يشير إلى حملة يوم 4، وكانت عبارة «رابط واحد مفكك» المنسوبة إلى Gemini عكس نتيجة القياس — فهو يحوّل ثلاثة عناوين URL مجردة إلى روابط، ولا يفقد أيًا منها.

  - **لم يعد فشل الكتابة يترك ترجمة مبتورة على القرص.** يقتطع `open(cible, "w")` الملف قبل ملئه: وكان حدوث خطأ أثناء الكتابة — امتلاء القرص — يترك هدفًا جزئيًا. وقد أُعيد إنتاج ذلك: كُتبت ثمانية بايتات، وأُعيدت الحالة `failure`، ثم وجد التشغيل التالي هذا الملف وأجاب بـ`skipped` واحتفظ به. وهكذا كان الفشل يتحول إلى ترجمة مبتورة لا يشير إليها شيء بعد ذلك، مع رمز خروج يساوي 0. أصبح المحتوى يُكتب الآن في ملف مؤقت ضمن المجلد نفسه — لا تكون `os.replace` ذرية إلا ضمن نظام الملفات نفسه — ثم يُعاد تسميته، مع حذف الملف المؤقت إذا فشلت الكتابة، بما في ذلك عند المقاطعة. يأتي هذا الملف المؤقت من `mkstemp`، وقد أظهرت مراجعة أن الاسم المتوقع لم يكن ليؤدي إلا إلى نقل المشكلة: فقد كان رابط رمزي موضوع عند `cible.md.aipmt-tmp` يجعل الترجمة تُكتب في الملف المستهدف خارج مجلد الإخراج، مع إعادة `success`؛ كما كانت عمليتا تشغيل متزامنتان على الهدف نفسه تتشاركان هذا الاسم، فتعيد الثانية التسمية بينما تظل الأولى تكتب في inode نفسه — يُعلن نجاح الهدف، لكن المحتوى يكون مختلطًا (`BBBAAAAA` بدلًا من `BBBBBBBB`، وفق القياس). ينشئ `mkstemp` الملف في `O_CREAT | O_EXCL` باسم فريد، ما يغلق الثغرتين. وتُنسخ صلاحيات الهدف الموجود صراحةً قبل الاستبدال: ينشئ `mkstemp` الملف بصلاحيات 0600، ومن دونه كان الهدف المضبوط عمدًا على 0600 يتحول إلى 0664، وكان الهدف ذي الصلاحيات 0644 الذي يقدمه خادم ويب سيصبح غير قابل للقراءة. تثبّت ثمانية اختبارات هذا كله، ويؤدي تعديلان تجريبيان في البرنامج إلى إخفاقها.

  - **يمكن لبيان النقاء الآن التصريح عن علامة حذف أُعيدت كتابتها.** كان الفحص يشترط بقاء كل سطر يحمل `# nosec` أو `# nosemgrep` أو `NOSONAR` من المرجع حرفيًا. لكن نقل الكتابة إلى ملف مؤقت يعيد تسمية المتغير المفتوح: يظل استثناء Sonar نفسه، لكن سطره يتغير. تصرّح فئة `markers` في البيان بهذا الزوج `old` → `new` مع سببه. وهناك شرطان، أتى الثاني منهما من مراجعة: يجب أن يوجد البديل، **وأن يحمل الاستثناءات نفسها التي يحملها السطر الأصلي**. من دون ذلك، كان التصريح بـ`value = 1  # NOSONAR` → `value = 1` يمر بنجاح، بحيث يكفي تصريح واحد لإزالة علامة — وهو بالضبط ما وُجد هذا الفحص لمنعه. ويظل الاختفاء غير المصرح به مرفوضًا.

  - **إزالة حملتين من الجداول لأنهما توقفتا بسبب شيء آخر غير النموذج.** فقد Grok جلسة CLI بعد اثنتي عشرة لغة من README هذا — رُفضت لغتان خلال أربع ثوانٍ، من دون استدعاء النموذج — وتعرض `qwen3.8-flash` للخنق بسبب HTTP 429 من مضيفه الأعلى بعد لغتين. لا تُقيّم حملة متوقفة: إما أن تُعاد بالكامل أو لا تُعاد إطلاقًا. وفي السياق نفسه، أُعيدت معايرة مدد جدول «README لهذا المشروع» وفق حملة 9 سبتمبر — فقد كانت تخص مراجعة من 508 أسطر، مقابل 785 سطرًا جرى قياسها، وكانت تعلن دقيقتين و04 ثوانٍ لـ`gpt-5.6-sol`، بينما يعطي القياس 6 دقائق و46 ثانية.

  - **إعادة صياغة README: أصبح 600 سطر بدلًا من 1 007، وبترتيب يناسب القارئ.** ما تفعله الأداة، ثم التثبيت والإعداد والخطوات الأولى، وبعدها جدول التوافق — موضوعًا بعد التثبيت، حين يعرف القارئ ما الذي سيشغله —، ثم الخيارات، وفصل لكل provider، والقياسات التفصيلية، وأخيرًا كيفية المساهمة. أزيل سرد الحملات وحكايات القياس والتكرار بين الأقسام؛ وكل رقم جرى الاحتفاظ به هو رقم وارد في الجداول، وكل عبارة عن السلوك تطابق البرنامج في هذا الإصدار. ويظل سجل إعادة الصياغة محفوظًا في تاريخ git.

  - **أصبح الجدول الرئيسي يذكر الآن عدد اللغات التي ينطبق عليها كل اختلاف.** كان يعلن «✅ 14 لغة»، ثم يقول في العمود المجاور «كلمة واحدة بالخط العريض» — من دون توضيح ما إذا كان ذلك ينطبق على لغة واحدة أم على اللغات الأربع عشرة. لم يكن بوسع القارئ الحسم، وكان الانطباع سيئًا رغم أن القياس جيد: في README هذا، لدى Gemini لغة واحدة فقط من أصل أربع عشرة تحتوي كلمة إضافية بالخط العريض، ولدى Sol لغتان، ولدى GLM ثلاث. أصبحت كل خلية تعرض الآن عدد اللغات المعنية وأسماءها، كما عُرّف رقم أعمدة الحكم في جملة واحدة — عدد اللغات، من أصل أربع عشرة، التي كُتبت ترجمتها ولم يختلف فيها شيء —، ويوضح مفتاح معنى الرموز الثلاثة. والتقسيم هو تقسيم المالك: ✅ لا شيء مختلف؛ ⚠️ تُرجم كل شيء، ولم يتغير سوى بعض الترميز، ولا شيء مفقود؛ ❌ رُفضت لغة واحدة على الأقل، أو فُقد محتوى من ملف مكتوب.

  - **إعادة حساب حملة المقال كاملةً باستخدام المقارن الحالي.** كان رقمان قد نُشرا قبل تصحيح المقارن للتعامل مع الكتابات غير اللاتينية، وكان يحسب لهما اختلافات غير موجودة: ينتقل `qwen3.8-flash` من ثلاث عشرة إلى أربع عشرة لغة بلا اختلاف، و`qwen3.7-flash` من سبع إلى ثمانٍ. وصُحح في الوقت نفسه المقطع المتعلق بـ`gpt-oss`: لم تصل المقاطع التي تركها بالفرنسية قط إلى ملف مُسلّم، إذ رفضت آلية اكتشاف المقتطفات غير المترجمة اللغات الأربع المعنية — وهذا هو الفرق بين نموذج يُوقَف ونموذج يمر من دون أن يُلاحظ، ويسمي README هذا الفرق بوضوح.

  - **بند إخلاء مسؤولية في نهاية README.** تستبعد المادتان 15 و16 من GPL v3 بالفعل أي ضمان، لكن لا أحد يقرأ ترخيصًا قبل تثبيت أداة. لذلك وُضّحت أربع نقاط: يجب مراجعة الترجمة الآلية قبل نشرها، لأن آليات الحماية لا تغطي العناوين ولا الجداول ولا front matter ولا المعنى؛ ويُرسل المستند المراد ترجمته إلى المزوّد المختار وفق شروطه، وقد تعيد بعض النماذج المجانية استخدام التبادلات — ويظل النموذج المحلي السبيل الوحيد الذي لا تخرج معه البيانات؛ وتُحتسب رسوم استدعاءات API، ولا يضع البرنامج حدًا أقصى للإنفاق؛ والقياسات المنشورة ملاحظات مؤرخة وليست ضمانات.

  - **يمكن الوصول إلى عائلة `gpt-5.6-*` كاملةً عبر اشتراك ChatGPT**، وفق القياس: يتيح `--model gpt-5.6-terra` عبر Codex النموذج الذي تقدمه API افتراضيًا. ويظل الخيار الافتراضي `gpt-5.6-sol`، وهو النموذج الوحيد الذي قيس بأنه يترجم أربع عشرة لغة بلا فقدان في مستند كثيف، وهو الذي يترجم هذا المستودع.

- **1.13.1** حداثة الاعتماديات: لم يعد التأخر مجرد تحذير يمكن ألا يُقرأ (2026-09-09):

  - **التحذير الذي لا يُحتسب هو تحذير يُفوّت.** كان `check-deps-fresh.sh` يشير منذ عدة أيام إلى تأخر `openai` و`anthropic`، لكن سطر ملخص البوابة لم يكن يحتسب سوى حالات الفشل: كان يعلن «جاهز: N من الفحوصات ناجحة» رغم ورود التأخر قبل ذلك ببضعة أسطر. لم يكن من يقرأ السطر الأخير — وهذا ما يفعله الجميع — قادرًا على معرفة ذلك. يعرض الحكم الآن عدد التحذيرات، ويحصيها عدّاد مخصص بدلًا من طباعتها تباعًا.
  - **يأتي التأخر مصحوبًا بملاحظات الإصدار.** لا يوضح رقم الإصدار ما الذي تغيّر، والاضطرار إلى العثور على الملف المناسب بنفسك هو تحديدًا الاحتكاك الذي يؤدي إلى تجاوز هذه الخطوة. يعرض الفحص عنوان CHANGELOG لكل حزمة متأخرة، سواء كان التحديث رئيسيًا أو ثانويًا، مع السبب: فقد سبق أن عدّل إصدار ثانوي من SDK أمورًا يعتمد عليها هذا المشروع.
  - **`openai` من 3.8.0 إلى 3.10.0 و`anthropic` من 1.3.0 إلى 1.4.0، بعد قراءة ملاحظات الإصدار قبل التثبيت.** من جهة OpenAI، لم يحدث سوى تغيير سلوكي واحد خلال هذه الفترة — لم يعد رأس `Retry-After` الرقمي الذي يتجاوز سعة العدد العائم يؤدي إلى الرجوع إلى backoff قصير، بل يعيد الخطأ الأصلي من دون محاولة أخرى — ولا شيء يمس ما يعتمد عليه هذا المشروع: لم تتغير سماحية النماذج تجاه الحقول الإضافية، وقد جرى التحقق من ذلك في البرنامج عند الوسمين، ولذلك يظل `native_finish_reason` و`error` اللذان يضيفهما OpenRouter إلى أحد الخيارات مقبولين. ومن جهة Anthropic، ترفض آلية حماية جديدة عبر `TypeError` صريح كائنًا من حزمة `httpx` عندما يتوقع SDK القيمة `httpx2`؛ وهذا المشروع لا يمرر سوى أعداد عائمة، وقد جرى التحقق من ذلك. والقاعدة التي ترفض استدعاءً غير متدفق يتجاوز عشر دقائق متطابقة بايتًا ببايت بين الإصدارين: إن `timeout` الصريح هو فعلًا ما يعفيه منها، إذ تتجاوز رموزه البالغ عددها 32 768 الحد البالغ 21 333.
  - **جرى التحقق**: مجموعتا اختبارات تضمّان 502 + 31 اختبارًا على الإصدارات المحدّثة، وإغلاق `requirements.txt` مطابق لما هو مثبت (41 تثبيتًا للإصدارات)، واستدعاء حقيقي عبر SDK — OpenAI وClaude — على مستند في وضع `--news`، ببنية مطابقة للمصدر.

- **1.13.0** المزوّد `--use_openrouter`: موجّه مدفوع إلى نحو 430 نموذجًا، منها النماذج الصينية المفتوحة (2026-09-05):

  - **المسار التاسع للمزوّد، يُطرح مع الثامن.** لم يُنشر الإصدار 1.12.0 على PyPI: يُطرح الموجّهان OpenCode وOpenRouter معًا. يتيح [OpenRouter](https://openrouter.ai) بمفتاح واحد الوصول إلى نماذج لا يوفّرها هنا أي provider آخر — Kimi وQwen وDeepSeek وZ.ai — باستخدام رصيد موحد تُحتسب تكلفته بحسب الاستخدام. وبما أن endpoint متوافق مع OpenAI، فالعميل هو نفسه المستخدم مع xAI؛ **وكل ما يميز هذا provider موجود في preflight**، وكل قاعدة فيه ناتجة عن قياس على API.

  - **يُقدَّم النموذج نفسه عبر عشرات المضيفين ذوي الحدود القصوى المختلفة، والتوجيه لا يراعي ذلك.** وفق القياس: 33 مضيفًا لـ`z-ai/glm-5.2` و23 لـ`z-ai/glm-5.3-flash` — أحدها محدود عند **2 048 tokens للإخراج**. لذلك كانت ترجمة طويلة عبر 23 تصل مبتورة عشوائيًا، من دون أدنى إشارة. يقرأ preflight القيمة `/api/v1/models/{modèle}/endpoints`، ويستبعد المضيفين الذين يقل إخراجهم عن 8 000 tokens، والذين في حالة متدهورة، والذين لا يصرّحون بأي حد أقصى، ثم يثبّت الآخرين. إن `provider.only` **من دون** `allow_fallbacks: false` ليس سوى تفضيل: إذ يعود الموجّه إلى مضيف مستبعد، فلا يعود للتثبيت أي قيمة. وإذا لم يستوفِ أي مضيف الحد الأقصى، يتوقف الأمر: فالترجمة رغم ذلك تعني قبول الاقتطاع الصامت الذي وُجد preflight هذا لمنعه.

  - **يُحتسب سعر الاستدلال وفق تعرفة الإخراج، وهو مفعّل افتراضيًا في كثير من النماذج.** الطلب نفسه على `z-ai/glm-5.2`، والاستجابة «حسنًا»: **107 tokens للإكمال وفق الإعداد الافتراضي للنموذج، و2 عند تعطيل الاستدلال**. في الترجمة، التي لا يضيف إليها الاستدلال شيئًا، يعني ذلك معاملًا قدره 18 لكل مقطع من كل ملف. لذلك يُعطّل افتراضيًا. أما **النماذج الـ288 من أصل 431** التي تفرضه (`reasoning.mandatory`)، فتجيب بـ`400 « Reasoning is mandatory for this endpoint and cannot be disabled »`: يقرأ preflight مستويات الجهد المقبولة لها ويطلب أدناها (النقطة التالية) — إذ يخصص مستوى الجهد **نسبة مئوية من `max_tokens`** يستهلكها الاستدلال أولًا، ولذلك فإن اختيار قيمة عشوائية ينقل خطر الصفحة البيضاء بدلًا من تقليله.
  - **يتلقى النموذج الذي يفرض الاستدلال أدنى مستوى جهد يقبله، وقد حُدِّد ذلك بالقياس.** كان الخيار الأول ألّا يُرسل أي شيء، لتجنب التخمين نيابةً عن النموذج. وعند اختباره على `z-ai/glm-5.3-flash`، الذي تكون قيمته الافتراضية في الكتالوج `max`، أدى هذا الخيار إلى مخرَج **مبتور عند 32,768 token** قبل نهاية الترجمة — فضاعت لغتان من أصل أربع عشرة. ولم تكن زيادة السعة لتغيّر شيئًا: فالجهد يخصص نسبةً منها، وينمو الاستدلال معها. لذلك يقرأ provider القيمة `supported_efforts` في preflight ويطلب أدنى مستوى، مع الرجوع إلى «لا شيء» عندما لا يعلن الكتالوج عن أي مستوى قابل للاستخدام. وفي الاختبار المضاد على اللغة التي كانت تفشل: كانت تتوقف بسبب نفاد الميزانية، وأصبحت الآن تنجح خلال 9 دقائق ببنية مطابقة للمصدر.

  - **يحمل عطل المستضيف في المنبع اسمه الآن.** يطبّع router هذه الحالة إلى `finish_reason=error` مع قيمة `native_finish_reason` فارغة — وقد قيس ذلك مرتين، بلغتين، عند 750 ثانية بالضبط. كانت الرسالة العامة تدفع إلى البحث عن عيب في المستند أو في التقسيم؛ أما الآن فتوضح أن العطل لدى المورّد وأن إعادة المحاولة تكفي غالبًا.

  - **إن `finish_reason=length` مع مخرَج فارغ ليس بترًا.** بل هو استهلاك الاستدلال للميزانية قبل ظهور أول محرف مفيد — وقد قيس عند 15,850 token للاستدلال مقابل 148 مفيدة. وتتطلب الحالتان إجراءين متعاكسين: لا يفيد تقليل حجم المقاطع في الحالة الأولى. وتميز الرسالة بينهما صراحةً. وأضيف حاجزان آخران بناءً على القياسات: يعيد router استجابة **200 بجسم لا يحمل سوى خطأ** عندما يفشل المستضيف في المنبع (كان `choices[0]` يطلق `TypeError` مبهمًا يحجب الرسالة)، كما تُقرأ نافذة السياق من الكتالوج ثم تُسجَّل في `MODEL_TOKEN_LIMITS` — إذ إن `DEFAULT_TOKEN_LIMIT` غير صحيح لـ44 نموذجًا في الكتالوج، منها نموذجان بحد أقصى يبلغ 4,095 token.

  - **إن `--model fournisseur/modèle` إلزامي، وتُتحقق صيغته قبل أي اتصال بالشبكة.** ليس OpenRouter مورّدًا: فالاختيار يحدد السعر والترخيص ومعالجة البيانات، ولا يجوز اتخاذه نيابةً عن المستخدم. وبما أن slug يُدرج في عنوان URL الخاص بـpreflight، فليست عملية التحقق مجاملةً لتحسين سهولة الاستخدام، بل هي الحاجز الذي يمنع حقن مسار فيه: تقبل regex ذات namespace المشتركة بين الـrouterين القيمة `a/b/..`، ومن هنا يأتي الرفض الصريح لمقطع الدليل الأب. لا أثر لـ`--eco`، وتوضح الرسالة ذلك.

  - **صُححت ثلاث صياغات، كانت إحداها خاطئة.** كان تحذير OpenAI بشأن `codex exec` يتعلق بحقن ملف جلسة شخصي في runner مشترك، لا بكون المستودع عامًا: وقد استُشهد به بعكس معناه في README وCLAUDE.md وفي الشيفرة. تغيّر موضع مصادقة OpenCode في الإصدار 1.18.27 (الجدول `credential` في `opencode.db`، بدلًا من `auth.json`)؛ وظل الثابت «لا تُقرأ هنا مطلقًا» صحيحًا، لكن العنوان أصبح قديمًا. وأخيرًا، لم يعد قسم OpenCode يعرض المسارات التي لم تُختبر قط على أنها متكافئة: فقد قيس مسارا بوابة Zen وOllama من البداية إلى النهاية، بينما لم تُقَس GitHub Copilot وLM Studio وllama.cpp، ويوضح README ذلك الآن.

  - **حملة قياس وجدول للنماذج الموصى بها في README.** نُفذت أكثر من ثلاثمئة ترجمة على ثلاث مجموعات من المستندات — مقال مدونة كثيف في وضع `--news`، وREADME هذا بصيغة Markdown القياسية، وأربعة ملفات README لمشاريع معروفة أُخذت كما هي من GitHub — إلى أربع عشرة لغة. يميز الجدول بين أمرين كانا يُخلطان: الترجمات التي **تكتمل** وتلك التي تكون **بنيتها مطابقة** للمصدر. لم تفقد ثلاثة نماذج أي معلومة على الإطلاق في المستندين الكثيفين: `gemini-3.7-flash` و`gpt-5.6-sol` عبر اشتراك ChatGPT و`z-ai/glm-5.2` عبر OpenRouter — وكانت اختلافاتها الوحيدة زوجًا من `**` لم يُنقل في لغة أو لغتين. والخلاصة الأساسية هي أن **العامل الفاصل هو كثافة المستند، لا وضع `--news`**: يفشل Grok عبر الاشتراك ثلاث عشرة مرة من أصل أربع عشرة في مقال المدونة، وينجح في أربعة عشر ملف README عامًا من أصل ستة عشر، والسبب هو فقدانه التماسك في مقطع طويل، وقد تحقق ذلك باختبار مضاد. ويتضمن الجدول تحذيره الخاص: فهو غير شامل ومؤرخ، والمدد لا تمثل ترتيبًا، ويظل النهج الصحيح هو القياس على مستندات المستخدم نفسه.

  - **صُحح مقارن البنية قبل نشر أرقامه، لأنه كان ينتج نتيجتين إيجابيتين كاذبتين مع أنظمة الكتابة غير اللاتينية.** لم تكن regex التي تتوقف عند `)` تفصل عنوان URL متبوعًا بقوس كامل العرض `）`، ولذلك كانت السلسلة المستخرجة تختلف رغم تطابق عنوان URL؛ كما كان اقتباس فرنسي من خمسة أسطر يشغل ثلاثة أسطر بالصينية فيؤدي إلى انخفاض العدّ القائم على الأسطر. وثُبت التصحيحان باختبار مضاد: ما زال حذف عنوان URL أو قسم أو كود مضمن يُكتشف. ولولاهما، لكانت نتائج Gemini وCodex المنشورة إحدى عشرة لغة من أصل أربع عشرة بدلًا من ثلاث عشرة واثنتي عشرة.

  - **الاختبارات**: ملف جديد `tests/test_openrouter_provider.py` ‏(76 اختبارًا) — التحقق من النموذج ورفض مقطع الدليل الأب، وتثبيت المستضيفين (الحد الأقصى، والحالة، والحد الأقصى غير المعلن، والحد الأدنى المشترك)، وبقاء `allow_fallbacks` دائمًا بالقيمة false، وقطع الاستدلال أو تركه وفق `mandatory`، وعقد المخرَج الكامل (خطأ داخل استجابة 200، وعدم وجود أي اختيار، وتمييز الصفحة البيضاء عن البتر، وقيمة `finish_reason` غير الطبيعية، والمحتوى الفارغ)، وإغلاق preflight عند الفشل إذا تعذر الوصول إلى الكتالوج، أو غاب slug، أو لم يوجد مستضيف سليم، وحصرية flag وتسمية اسم الملف. المجموعة الكاملة أصبحت **502 اختبارًا**.

  - **إعادة هيكلة: قُسِّمت الوحدة الوحيدة المكوّنة من 4,253 سطرًا إلى وحدات، من دون تغيير أي سطر من السلوك.** وُزِّع `src/aipmt/translate.py` على `config` و`markdown` و`segmentation` و`guards` و`placeholders` و`news` و`prompts` و`notes` و`naming` و`pipeline` و`cli` وحزمة فرعية `providers/` (وحدة لكل provider، و`base` كأساس، و`registry` للحل وdispatch). كل عملية نقل هي commit ذو إثبات آلي: يقارن مدقق AST لجميع عُقد المستوى الأول في الحزمة بلقطة مرجعية، ويتحقق من موضع كل رمز، وبقاء علامات الأمان حرفيًا، وعدم وجود ملف غير متعقَّب — وكانت أداة مؤقتة أزيلت في الإصدار التالي. أما التغييرات الظاهرة فهي: أصبح `aipmt.translate` واجهةً تعيد تصدير الأسماء الـ64 التي كانت الوحدة تعرضها بلا بادئة `_`، مع الحفاظ على هوية الكائنات (`__all__` يحمل تسعة منها، وهي API المدعومة؛ أما البقية فهي أسماء مستعارة للتوافق)، وتوقف عن إعادة تصدير أسماء التبعيات والمكتبة القياسية الـ29 التي كان يجمعها `import *`؛ ولم يعد التنفيذ المباشر للملف (`python src/aipmt/translate.py`) موجودًا — وتظل `aipmt` و`python -m aipmt` الصيغتين المدعومتين؛ وأصبحت قيمة `__module__` للدوال العامة هي وحدة تعريفها؛ كما صارت SDK تُستورد بعد تحميل `.env` بدلًا من قبله، من دون أثر معروف. حُفظت الاختبارات الـ427 مع معرفاتها ونُقلت إلى الوحدات التي تختبرها: أصبحت 91 عملية patch كانت تمر عبر الواجهة تستهدف الآن الوحدة التي تطّلع على الاسم (ظل اثنان منها ينجحان من دون patch الخاص بهما، وفق القياس)، وتثبّت سبعة اختبارات عقد الواجهة، وأعيدت كتابة أدوات gates قبل أول عملية نقل حتى لا يتمكن أي منها من التحول إلى الأخضر بالتوقف عن التحقق — نطاق Lizard على مستوى الأدلة مع حد أدنى، وقراءة flags من parser المبني، وحد أدنى للتغطية لكل حزمة، و`release.sh` الذي يسرد الوحدات المتعقَّبة.
  - **إصلاح (مراجعة pull request)**: يرفض OpenRouter سجل كتالوج بلا `context_length` بدلًا من تسجيل القيمة الافتراضية البالغة 128,000 token على أنها قياس — وهو ما كان يعطّل أيضًا تحذير «النموذج غير مدرج» — وعندما تكون `finish_reason` فارغة (النوع الموثق `string | null`)، يُعتمد سبب المستضيف الخام، إذ تساوي `max_tokens` القيمة `length`؛ ويؤدي الخطأ الذي يحمله الاختيار نفسه إلى رفض المحتوى الجزئي المرافق له، بالرسالة نفسها المستخدمة لعطل المنبع المطبّع — فتجمع تفاصيل المستضيف والسبب الخام والنصيحة. يستجيب OpenCode بخطأ العقد الخاص به، لا بـ`AttributeError`، عند حدث `part: null`، ويرفض تدفق JSONL إذا تعذرت قراءة أحد أسطر الأحداث بدلًا من قبول نص جزئي. تقتل واجهات CLI الوكيلة الثلاث مجموعة الوكيل عندما تتلقى العملية `SIGTERM` أثناء الاستدعاء — كان `timeout` الخاص بإعادة التوليد يترك الوكيل حيًا يستهلك حصته — ويتبع `SIGKILL` الخاص بالمجموعة دائمًا مهلة السماح، إذ إن shim ينتهي بصورة سليمة يترك العملية الحفيدة حية. وأعيد وصل زوج `# fmt: off` / `# fmt: on` الذي فصله التقسيم؛ وأصبحت مساعدة `--reasoning_effort` تسمي providers الأربعة الذين يستخدمونه.
  - **إصلاح (المراجعة الثانية)**: أصبح الحد الأقصى للمخرَج المطلوب من OpenRouter يحجز الجزء الذي يشغله prompt والمقطع من السياق — إذ يغطي `context_length` الإدخال والإكمال معًا، وكانت طلبات ستة نماذج في الكتالوج تُرسل بلا مساحة للإدخال؛ ويُرفض السياق الأقصر من اللازم قبل أي فوترة. يعود أساس واجهات CLI الوكيلة إلى `terminate` ثم `kill` حيث لا تتوفر مجموعات عمليات POSIX، بدلًا من السماح لـ`AttributeError` بتجاوز حاجز timeout واستمرار الانتظار. ويُبحث عن علامة OpenCode ‏`429` بوصفها رقمًا لا سلسلةً فرعية، إذ كان معرّف خطأ مثل `err_84290b` يؤدي إلى 90 ثانية من back-off قبل أن يفشل في النهاية على أي حال. وأخيرًا، يُعرض endpoint غير القياسي لـOpenRouter في preflight: يكفي ملف `.env` خاص بالمشروع لتعيينه، وهو المفتاح الفعلي الذي كان سيُرسل بعد ذلك.
  - **الأمان: لم يعد بإمكان ملف `.env` خاص بالمشروع إعادة توجيه استدعاءات API.** يبحث `find_dotenv(usecwd=True)` عن الملف بدءًا من الدليل الحالي ثم الأدلة الأب: كان بإمكان شجرة أدلة غير موثوقة — مثل مستودع نُسخ للتو — أن تضع فيها `OPENROUTER_BASE_URL` أو `XAI_BASE_URL` أو `OPENAI_BASE_URL` (وتقرأ SDK الأخيرة بنفسها) من دون معرفة أي مفتاح، ثم يُرسل المفتاح الحقيقي القادم من البيئة أو إعدادات المستخدم في ترويسة التفويض إلى خادم تابع لجهة خارجية. يعتمد المرشح على نمط، لا على قائمة: وفق حصر SDK المثبتة، تُقرأ اثنتا عشرة متغيرًا للتوجيه، يقرأ عميل Anthropic وحده ستة منها — وكان تعداد يدوي سيغفل نصفها. لذلك تُرفض من طبقة المشروع جميع المتغيرات المطابقة لـ`_BASE_URL` أو `_API_BASE` أو `_ENDPOINT`، وكذلك proxies ومخازن الشهادات (إذ يجعل تحديد سلطة يسيطر عليها طرف ما الوسيط غير قابل للتمييز عن خادم حقيقي)، إضافةً إلى `XDG_CONFIG_HOME` و`APPDATA`: فتعيينهما كان يعني تحديد الملف الذي يشكل طبقة المستخدم، ومن ثم الالتفاف على المرشح بصورة غير مباشرة. ولا تُقبل هذه المتغيرات إلا من الطبقتين اللتين يتحكم فيهما المستخدم: البيئة المصدَّرة و`~/.config/aipmt/.env`. كما تُقرأ طبقة المشروع من دون interpolation: يوسّع `load_dotenv` القيمة `${VAR}` افتراضيًا، وكان ملف `.env` غير موثوق يحتوي على `NOM_ANODIN=${OPENAI_API_KEY}` ينسخ المفتاح الحقيقي تحت اسم لا يتعرف عليه الترشيح النمطي للعمليات الفرعية — فيدخل عندئذٍ إلى بيئة `codex exec`، خلافًا للثابت المعلن. وأخيرًا، لا يعرض الرفض سوى اسم المتغير: فقد كان عنوان URL بصيغة `https://${CLE}@hôte/` يسرّب المفتاح بعد interpolation إلى السجلات، حتى أثناء رفضه. ويُعلن الرفض عبر stderr مع توضيح الإجراء المطلوب — إذ يُضبط relay الخاص بالشركة في إعدادات المستخدم.
  - **إصلاح: تُحسب سعة مخرَج OpenRouter لكل استدعاء.** يغطي `context_length` الإدخال والإكمال معًا، ولا يضمن احتياطي ثابت مضبوط على نص لاتيني أي حد فعلي: وفق القياس باستخدام tokenizer ‏`o200k_base`، تعادل 16,000 محرف 3,200 token بالفرنسية، و12,300 باليابانية، و17,500 مع emoji. لذلك تُستمد الميزانية من النص المرسل فعليًا، مع زيادة تساوي عدد وحدات UTF-8 byte فيه: ففي كل tokenizer قائم على دمج byte — سواء BPE على مستوى byte أو SentencePiece مع fallback إلى byte، وهي العائلات التي يستخدمها الكتالوج — يمثل token واحد byte واحدًا على الأقل، وهذه هي الزيادة الوحيدة المتاحة هنا، حيث يوجّه OpenRouter الطلبات إلى عشرات tokenizer غير المعروفة. ولم تكن أي نسبة متوسطة مناسبة — إذ ينخفض المحرف الإيديوغرافي من المستوى التكميلي إلى 1.33 byte لكل token، والمحرف المركّب إلى 1.00 — وأصبح الإدخال مع المخرَج يقعان داخل النافذة بحكم التصميم. ويُرفض المقطع الأعلى كثافة مما يحتمله النموذج المختار قبل الاستدعاء، لا بعد الفوترة.

- **1.12.0** ‏Provider ‏`--use_opencode`: ‏OpenCode، الوكيل مفتوح المصدر، نحو المورّد الذي يختاره المستخدم — نموذج محلي، أو مجاني بلا حساب، أو اشتراك، أو مفتاح (2026-09-04):

  - **المسار الثامن للـprovider، وهو مختلف في طبيعته عن المسارات السبعة الأولى.** إن [OpenCode](https://opencode.ai) ‏(MIT) ليس مورّد نماذج، بل _router_ نحو النماذج التي أعدّها المستخدم داخل OpenCode نفسه: مفتاح API، أو اشتراك (GitHub Copilot وChatGPT وSuperGrok)، أو بوابة OpenCode Zen — التي توفر نماذج مجانية **بلا حساب** — أو نموذج **محلي** (Ollama وLM Studio وllama.cpp). يتحكم السكربت في `opencode run` بوضع غير تفاعلي، كما يفعل مع Codex وGrok، ويعيد استخدام أساس العمليات الفرعية نفسه (مجموعة عمليات مستقلة، و`SIGTERM` ثم `SIGKILL` عند timeout، وإغلاق stdin دائمًا، وتنقية البيئة). وقد جرى التحقق عبر **ترجمتين فعليتين**: ترجمة README هذا كاملًا إلى الإنجليزية باستخدام `opencode/mimo-v2.5-free` — خلال 49 ثانية، بتمريرة واحدة، وبنية مطابقة للملف المصدر (32 عنوانًا، و26 إغلاقًا للكود، و18 رابطًا، و37 عنوان URL، و37 سطر جدول، و135 كودًا مضمنًا) — وترجمة ملف اختبار باستخدام `ollama/qwen2.5:7b` محليًا ومن دون أي مفتاح.

  - **إن `--model provider/modèle` إلزامي، وهو اختيار مقصود.** من دون `--model`، يعود OpenCode إلى قيمته الافتراضية الخاصة، التي تكون في التثبيت الجديد `opencode/big-pickle`، وهو نموذج «stealth» مجاني قد تُستخدم محادثاته في التدريب — وقد أثبت القياس أن هذا النموذج هو الذي استجاب. إن اختيار ذلك بصمت نيابةً عن المستخدم سيكون بالضبط نوع التحول الخفي الذي يتعقبه هذا المستودع؛ لذلك تسمي رسالة الخطأ الأمر الذي يسرد النماذج (`opencode models`) وتقدم ثلاثة أمثلة (محلي، ومجاني، واشتراك). لا أثر لـ`--eco`، وتوضح الرسالة ذلك. وتُمرر `--reasoning_effort` كما هي بوصفها `--variant` الخاصة بـOpenCode، وعند الطلب الصريح فقط.

  - **عزل مقاس، لا مفترض.** يعرّف إعداد inline ‏(`OPENCODE_CONFIG_CONTENT`، وهو الأخير في ترتيب الدمج لدى OpenCode، ومن ثم له الأولوية على إعداد المستخدم من دون استبداله) وكيلًا `aipmt` تُرفض جميع أدواته (`permission: {"*": "deny"}`): فلا يعرضها السجل أصلًا على النموذج، وعندما يُطلب منه «سرد الملفات وتشغيل `id`» يجيب بأنه لا يملك أدوات. كما عُطلت مشاركة الجلسة، واستُبعدت plugins الخارجية (`--pure`)، ولم يُستخدم `--auto` مطلقًا، مع دليل عمل مؤقت وفارغ. وقد قيس حقنان صامتان وأُوقفا: من دون `OPENCODE_DISABLE_CLAUDE_CODE`، يدخل ملف المستخدم `~/.claude/CLAUDE.md` في **كل** prompt ‏(515 token للإدخال بدلًا من 186 لعبارة «مرحبًا» بسيطة)؛ ومن دون `OPENCODE_DISABLE_PROJECT_CONFIG`، يدخل أيضًا ملف `AGENTS.md` الخاص بالدليل الحالي — وقد طُبقت على الترجمة تعليمة تقول «اختم كل إجابة بكلمة BANANA». أما ملف `~/.config/opencode/AGENTS.md` العام فيظل محقونًا: فلا يوجد مفتاح لتعطيله، والتحايل عليه عبر `XDG_CONFIG_HOME` مُساء الاستخدام سيحجب أيضًا مورّدي المستخدم. لذلك جرى توثيقه بدلًا من ترقيعه.
  - **وجود `exit 0` لا يثبت شيئًا، إنها CLI ثالثة، وردود الفعل نفسها — مع فخّين خاصّين بها.** لا يتسبب `--agent` غير المعروف في فشل `opencode run`: يظهر تحذير على stderr ثم يحدث رجوع **صامت** إلى وكيل البرمجة، مع بقاء الأدوات نشطة. لذلك، إذا لم تؤخذ الإعدادات المضمّنة في الحسبان، فستبدأ الترجمة بواسطة وكيل قادر على الكتابة؛ ويتحقق عقد الإخراج من غياب هذه الرسالة، بالإضافة إلى: رمز إرجاع 0، وعدم وجود أي حدث `error`، وعدم وجود أي `tool_use`، وأن يكون آخر `step_finish` في `stop` (إذ إن `length` استجابة مبتورة)، وأن يكون النص غير فارغ. الفخ الثاني: حدث خطأ JSON **مبهم** — «خطأ غير متوقع في الخادم. راجع سجلات الخادم للحصول على التفاصيل.»، مع مرجع بسيط — بينما لا يظهر السبب الحقيقي (`ProviderModelNotFoundError: Model not found: foo/bar. Did you mean…`، و`ProviderAuthError`...) إلا في السجلات. ومن هنا يأتي `--print-logs --log-level ERROR` وقراءة حقل `error="…"` من stderr، من دون تتبّع Bun الذي يليه. وهكذا يفشل النموذج غير المعروف خلال ثانية واحدة، مع ذكر سببه. ويمنع `--title` أيضًا استدعاء LLM طفيليًا: فمن دونه، ينشئ OpenCode عنوانًا للجلسة عبر جولة إضافية على `small_model`.

  - **الأسرار: الترشيح بالنمط نفسه المستخدم مع Codex وGrok، باستثناء واحد محدد بالاسم.** تُحفَظ `OPENCODE_API_KEY`: فهي مفتاح OpenCode نفسه (بوابة Zen، واشتراك Go)، وموجّهة إليه باسمه — أي ما يعادل `auth.json` الخاص به، وليست مفتاحًا يديره aipmt أو يمكنه احتساب تكلفته. يُضبط مقدّمو الخدمة داخل OpenCode (`opencode auth login`، و`opencode.json`)، وليس أبدًا داخل `.env` الخاص بـaipmt، إذ لا يصل أي مفتاح منه إلى العملية الفرعية. ولا يوجد رفض في CI، بخلاف CLI الخاصة بالاشتراكات: فاستخدام مفتاح API أو نموذج مستضاف ذاتيًا على runner استخدام مشروع.

  - **أصبحت آلية الحماية من اجتياز المسارات تتحقق من القيمة بعد الاستيفاء، لا من القيمة الخام.** يحتوي `provider/modèle` على `/` كانت آلية الحماية في 1.10.0 ترفضه — وكان ذلك محقًا، لأن `--model` كان يُستوفى داخل اسم الملف `--include_model`. تستبدل تسمية اسم الملف الآن `/` و`\` و`:` بـ`-` قبل أي استيفاء (`ollama/qwen2.5:7b` ← `ollama-qwen2.5-7b`، لأن `:` غير قانوني على Windows)، ثم تفحص آلية الحماية السابقة هذه التسمية: يصبح `../../evil` مجرد الاسم `doc-en-..-..-evil.md` تحت الوجهة، ولا يبقى مرفوضًا إلا `..` وحده، وكذلك `--target_lang ../x`. وتظل آلية حماية النطاق `_ensure_within_directory` طبقة ثانية من دون تغيير.

  - **النماذج المجانية والنماذج المحلية، وما جرى قياسه.** يترجم `opencode/mimo-v2.5-free` فقرة خلال 16 ثانية وREADME هذا خلال 49 ثانية؛ ويستغرق `opencode/big-pickle` مدة 40 ثانية لترجمة 200 كلمة، كما ظل طلبان متزامنان عليه بلا استجابة مدة 5 دقائق، بينما ينجح كل واحد منهما منفردًا؛ ولم يقدّم `opencode/nemotron-3.5-lightning-free` أي استجابة خلال 3 دقائق. لذلك يُستخدم `REGEN_PROVIDER=opencode` مع إلزامية `REGEN_MODEL` وتشغيل **مهمتين** بالتوازي. وعلى الجانب المحلي، يضبط Ollama غالبًا سياقًا من 4,096 tokens، بينما يبلغ طول المقاطع 16,000 حرف؛ لذا لا بد من `Modelfile` مع `PARAMETER num_ctx 32768`، وتعتمد الجودة على النموذج — فقد عكس نموذج 7B ترتيب قائمة وأفسد محدِّد إغلاق كتلة code في ملف الاختبار، بينما حافظ نموذج البوابة على كل شيء.

  - **لم تعد ترجمات هذا المستودع تمر مطلقًا عبر API مدفوعة.** كان `regen_translations.sh` يستخدم OpenAI API بمجرد وجود مفتاح عابر في `.env`، ولم يجعل Codex إلا خيارًا يُفعَّل صراحةً. وهذا بالضبط ما حدث عند إعداد هذا الإصدار: أُرسلت الترجمات الـ28 عبر OpenAI API، ثم أُرسل CHANGELOG الهندي عبر Gemini API، مع أن اشتراك ChatGPT موجود تحديدًا لتجنّب الدفع بحسب الاستخدام. أُزيل الاكتشاف التلقائي للمفاتيح: **أصبح Codex هو الخيار الافتراضي، مع `gpt-5.6-sol`**، أي نموذج الجودة؛ وتتطلب `openai` و`gemini` و`grok` وجود `REGEN_ALLOW_PAID_API=1` بالإضافة إلى `REGEN_PROVIDER`، وهو استثناء مسمّى لتُطبّق القاعدة لحظة اتخاذ القرار؛ ويفشل `REGEN_PROVIDER` غير المعروف بدل الرجوع إلى API. تثبّت عشرة اختبارات الخيار الافتراضي والرفض والاستثناء. وأُعيد إنجاز الترجمات الـ28 لهذا الإصدار عبر Codex.

  - **وُحِّدت آلية التراجع التدريجي عند بلوغ حد المعدل** (`_retry_on_rate_limit`): كانت حلقتا Codex وGrok متطابقتين باستثناء التسمية، وكانت نسخة ثالثة ستتجاوز عتبة التكرار. وترث أخطاء CLI الثلاثة من `_CliCallError` واحدة؛ ويمنع اختبار خروج أي واحد من الأخطاء الثلاثة عنها، وإلا فلن ترصده الحلقة المشتركة بعد ذلك.

  - **الاختبارات**: ملف جديد `tests/test_opencode_provider.py` (61 اختبارًا) — عقد إخراج كامل، والرجوع إلى الوكيل، وقراءة السبب من السجلات، وإزالة تكرار أجزاء النص وتجاهل الأجزاء الاصطناعية، وقتل مجموعة العمليات عند انتهاء المهلة، والتراجع التدريجي عند 429، وإلزامية النموذج والتحقق منه، وفحص تمهيدي بلا أسرار، وتحديد مسار الملف التنفيذي، وتوصيل dispatch، وتسمية اسم الملف، واختبار مضاد لاجتياز المسارات. يوسّع `tests/test_review_hardening.py` حصرية flags وغياب الأسرار لتشملا provider الجديد. وتتطلب البوابة الآن توثيق **22 flag** من argparse. المجموعة الكاملة: **382 اختبارًا**.

- **1.11.1** تصحيح توثيقي: يعلن README أخيرًا مسارات provider السبعة (2026-09-03):

  - **كانت صفحة PyPI للإصدار 1.11.0 تقول «4 APIs + ‏Codex CLI».** لكن code يوفّر سبعة مسارات — OpenAI وMistral وClaude وGemini وGrok عبر API؛ وCodex ‏(ChatGPT) وGrok عبر الاشتراك، من دون فوترة بحسب الاستخدام. كان نمطا Grok غائبين عن العبارة التقديمية وعن بند _Multi-Provider_، وكررت الترجمات الـ14 الخطأ. ولأن الوصف الطويل للحزمة ثابت لكل إصدار، فقد تطلّب تصحيح الواجهة رقم إصدار جديدًا: وهذا هو سبب وجود هذا الإصدار الوحيد. **لا تغييرات في code.**
  - تمت مواءمة `CLAUDE.md` مع ما أدخله النشر: عدّادات البوابة (16، و17 في `--full`)، وworkflows الأحد عشر النشطة، وعدّادا Sonar/Codacy غير الظاهرين في `gh pr checks` (hotspots، وCodacy API)، ونقل `# nosemgrep` بواسطة `ruff-format`، وبيئات GitHub التي يتطلبها تبادل OIDC، وحقيقة أن _pending publisher_ لا يحجز الاسم.

- **1.11.0** النشر على PyPI: ‏`pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-09-03):

  - **أصبح السكربت ذو الملف الواحد حزمة قابلة للتثبيت.** انتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول console هي `aipmt` وما يعادلها `python -m aipmt`. يظل المستودع المستنسخ ضروريًا للمساهمة — إذ توجد فيه الاختبارات والترجمات الـ28 وأدوات الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم import هو `aipmt` وليس `translate` مطلقًا**، لأن التصادم حقيقي وصامت. تثبّت حزمة PyPI المسماة `translate` (الإصدار v3.8.1، وآخر رفع في 2026-07-06) مجلدًا يحمل الاسم نفسه. وقد أُعيد إنتاج المشكلة في venv: يتغلب المجلد على module، ويختفي `translate.main`، وتتعطل نقطة الدخول عند `AttributeError` — ومع ذلك يجيب `pip check` بعبارة «لم يُعثر على متطلبات معطلة» مع rc=0. وكان مجرد وجود `pip install translate` لدى المستخدم كافيًا لتعطيل CLI من دون تشخيص قابل للاستفادة. اختبار مضاد على wheel الفعلية: تثبيت `pip install translate` فوق الحزمة، مع `aipmt --help` وrc=0 قبل ذلك وبعده، وتعايش كلتا CLI.
    - **اسم توزيع طويل، وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للعثور عليها عبر بحث PyPI؛ أما الاختصار وحده فلن يعثر عليه من لا يعرف المشروع مسبقًا، بينما يهدف النشر تحديدًا إلى تسهيل العثور عليه. استُبعد مرشحان معقولان بعد التحقق: `ai-markdown-translator` محجوز على npm منذ 2024 لأداة تؤدي الغرض نفسه وتسبق هذا المستودع بـ17 شهرًا؛ و`aimt` يختلف بحرف واحد عن `aim` (الإصدار v3.29.1)، وهي حزمة نشطة في المجال **نفسه** — أسوأ وضع ممكن لالتباس دائم. وهناك فخ منهجي أيضًا: يعيد `pypi.org/project/<nom>/` الحالة 200 لأي اسم كان (صفحة لمكافحة bots)، ولا يُعتد إلا بـJSON API.
    - **تخطيط `src/` بدل الحزمة المسطحة.** كان من شأن الحزمة المسطحة الحفاظ على عمليات `sys.path.insert(..., "..")` الست في الاختبارات، وهذه هي المشكلة تحديدًا: فهي تجعل الاختبارات تستورد شجرة المصدر بدل الحزمة، ما كان سيخفي أي خطأ في التغليف. التكلفة الفعلية: قاعدة استبدال إضافية واحدة.

  - **أصبح من الممكن أخيرًا ضبط المفاتيح مرة واحدة بصورة دائمة.** لم تكن CLI المثبتة تملك أي إعداد دائم: لم يبق سوى متغير البيئة و`.env` في المجلد الحالي. صحيح أن `find_dotenv` يصعد حتى جذر النظام، ولذلك كان يعثر على `~/.env` **عند العمل تحت المجلد الشخصي للمستخدم**، لكنه لم يكن يعثر على شيء عند العمل في مكان آخر — أي إن التغطية كانت تعتمد على الموضع الذي شُغّل منه الأمر، لا على قرار تصميمي. لذلك أُضيفت طبقة ثالثة: `~/.config/aipmt/.env`، تحت الطبقتين الموجودتين.

    - **الأولوية غير مرمّزة صراحةً**، بل تنتج من `override=False`، وهي القيمة الافتراضية لـ`load_dotenv`: لا تملأ كل طبقة إلا ما تركته السابقة فارغًا. ومن هنا يأتي الترتيب: متغير البيئة ← ‏`.env` الخاص بالمشروع ← إعداد المستخدم، وقد جرى التحقق منه باختبارات **سلوك** لا اختبارات بنية — فعكس ترتيب الاستدعاءين يفشلها، وكذلك حذف الطبقة الثالثة.
    - **التنسيق `.env` وليس TOML**، عن قصد: ‏`python-dotenv` تبعية موجودة أصلًا، والصياغة موثقة بالفعل في ملفات README الخمسة عشر، والملف نفسه يخدم النطاقين. لا تبعية جديدة ولا صياغة جديدة. يتبع الموقع `XDG_CONFIG_HOME` إذا كان **مطلقًا** — إذ تتطلب المواصفة تجاهل القيمة النسبية، وإلا عاد موقع الإعداد تابعًا للمجلد الحالي — ويتبع `APPDATA` على Windows.
    - **خياران مستبعدان، مع سبب استبعاد كل منهما.** مخزن أسرار النظام (`keyring`) أكثر أمانًا على جهاز مكتبي، لكنه يفشل في البيئات headless — الخادم والحاوية وCI — أي في حالة الاستخدام الأساسية للترجمة المجمّعة؛ وهو مرشح جيد كخيار صريح، لكنه خيار افتراضي سيئ. أما flag ‏`--api-key` فسيضع المفتاح في سجل shell ويجعله ظاهرًا في `ps`.
    - **عند غياب المفتاح، لم يعد يظهر تتبّع للاستدعاءات.** كان المستخدم يتلقى stack trace من Python يشير إلى `site-packages`، ورسالة تذكر «البيئة أو .env» من دون توضيح مكان إنشاء الثاني. وأصبحت الرسالة الآن تسرد المواقع الثلاثة مع المسار الدقيق لكل منها، ويخرج الأمر بالرمز 2. شبكة الأمان **ضيقة عن قصد**: ‏`except ValueError` على مرحلة الإعداد وحدها. فتغليف التنفيذ كله سيحوّل bug حقيقيًا يقع أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الفشل الذي يتتبعه هذا المستودع. ويقرأ اختبار مصدر `main()` لمنع ذلك.

  - **تصحيح — كان `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** لا يصعد `load_dotenv()` من المجلد الحالي عند استدعائه بلا argument، بل من الملف **المستدعي**، أي من `site-packages`. وقد قيس ذلك باستخدام نقطة دخول console حقيقية شُغّلت من مشروع لديه `.env` خاص به: يعيد `find_dotenv()` القيمة `''` ولا يُحمَّل المفتاح، بينما يعثر عليه `find_dotenv(usecwd=True)`. لم يكن bug موجودًا ما دامت الأداة لا تعمل إلا من المستودع المستنسخ؛ لكن النشر كان سيجعله منهجيًا، مع عرض وحيد هو ظهور مفتاح API على أنه «مفقود» رغم صحة الإعداد.

  - **كان من الممكن أن تتحول ثلاث بوابات إلى اللون الأخضر بعد أن توقفت عن التحقق من أي شيء.** لذلك شُدّدت **قبل** النقل، عن قصد: فحاجز الحماية المكتوب بعد التغيير الذي يُفترض أن يرصده لا يثبت شيئًا. كل واحدة منها خضراء على المستودع الأصلي وتتحول إلى الأحمر على نسخة منقولة — وقد قيس الاتجاهان.

    - **يتجاهل Lizard المسار الغائب من دون قول شيء**: ‏rc=0، و«تم تحليل 0 ملف». وكان من الممكن أن تنتقل بوابة التعقيد من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع إخراج حجمه صفر بايت. أصبح النطاق الآن مصفوفة يُتحقق من وجود كل عنصر فيها.
    - **لا يفشل `coverage run --source=` عند استخدامه على module غائب**: يظهر تحذير على stderr فقط، مع rc=0 في unittest وكذلك في `coverage xml`، ويُنشر التقرير رغم ذلك — بعد انخفاضه من 1453 إلى 141 statement. وكان سيبدو المشروع سليمًا لأنه لم يعد يخضع للتحليل تقريبًا. يحمي التقرير حدّان أدنيان: الإجمالي، وأكبر ملف جرى قياسه.
    - **مسبار حداثة الترجمات أعمى بنيويًا عن صيغة الاستدعاء**: فهو يرتكز على flags الخاصة بـargparse، وهي تحديدًا ما لا يتغير عند إعادة تسمية الملف. وقد أُعيد إنتاج ذلك: نُقل module، وظلت ملفات README الخمسة عشر توثّق أمرًا غير موجود، وكانت النتيجة «لا توجد ترجمات قديمة». لذلك يتحقق قسم سابع من **الصيغة** لا من الخيارات، ويُقارن hook الخاص بـLizard بالنطاق الفعلي للسكربت — فالمفتاح `files:` الخاص به لا يجعل pre-commit يفشل عندما يتوقف عن المطابقة، بل يجعله **يتخطاه**.

  - **لم يعد `requires-python = ">=3.10"` مجرد ادعاء.** كان `sonar-project.properties` يعلن بالفعل دعم 3.10-3.12 من دون أن يكون أي شيء قد اختبره يومًا، لأن جهاز التطوير لم يكن يحتوي إلا على 3.12 — وهو تناقض داخلي كان النشر سيجعله علنيًا. يشغّل workflow للاختبارات الآن المجموعة على 3.10 و3.11 و3.12، مع تثبيت **الحزمة**، وبالتالي اختبار حدودها العامة.

  - **حدود دنيا، من دون حد أقصى.** يظل `requirements.txt` هو lock المختبَر، ويصبح `[project.dependencies]` العقد العام: فنشر الإصدارات الدقيقة من lock كان سيتسبب في تعارضات لدى كل مستخدم لديه حزم أخرى. ولا يوجد حد أقصى لـ`<N+1` أيضًا — إذ سيتعارض مباشرةً مع `check-deps-fresh.sh`، الذي يجعل بوابة release تفشل عند أي تأخر في إصدار رئيسي. تُحل مجموعة الحدود الدنيا بنجاح، ويخرج الاختبار المضاد `openai==1.0.0` في `ResolutionImpossible`، ما يثبت أن الفحص يميّز بدل قبول كل شيء. كما تمنع آلية حماية إصدار `pyproject.toml` من الاختلاف عن إصدار CHANGELOG: فلا تسمح PyPI بإعادة استخدام رقم إصدار.

  - **جرى التحقق من البداية إلى النهاية داخل venv جديد**: ‏wheel بحجم يقارب 70 كيلوبايت لا تحتوي إلا على `aipmt/*.py` وdist-info والترخيص؛ و`aipmt --help` مع rc=0 و22 flag؛ و`python -m aipmt` يعرض «usage: aipmt» لا «usage: \_\_main\_\_.py»؛ وتثبيت `pipx` يعمل؛ والأهم **ترجمة فعلية من fr إلى en انطلاقًا من أي مجلد مستخدم**، مع الحفاظ على النص العريض والقائمة وcode المضمّن والرابط وURL، وعدم ترجمة كتلة code. تنجح الاختبارات الـ318 السابقة للنقل مع قائمة معرّفات متطابقة بايتًا ببايت قبل النقل وبعده — وهذا ما يثبت عدم تعطيل أي اختبار، لا عبارة «OK»؛ وأُضيف إليها اثنا عشر اختبارًا للإعداد ذي الطبقات الثلاث، ليصبح المجموع 330.

- **1.10.0** ‏Provider ‏`--use_codex` (حصة اشتراك ChatGPT)، وتحديث SDK والنماذج، وإصلاح اقتباسات news متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — حاجزا حماية وضعتهما PR من دون تطبيقهما في كل المواضع**:
    - **كان فحص Codex التمهيدي يمرّر `.env` بالكامل إلى الملف الثنائي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: إذ كانت العملية الفرعية ترث `os.environ` كاملًا، وبالتالي كامل `.env` الذي حمّله `load_dotenv`. أظهر القياس باستخدام ملف ثنائي وهمي مزوّد بأدوات رصد أن **سبعة أسرار** كانت تصل إلى الفحص التمهيدي — مفاتيح مزوّدي الخدمة الستة بالإضافة إلى `GITHUB_TOKEN` — مقابل **صفر** لدى نظيره `_grok_preflight`، الذي كان يمرّر `env=_grok_env()` فعلًا. كان التناقض داخليًا في PR: إذ يوجد `_strip_secret_env` تحديدًا للحفاظ على هذا الثابت، على بُعد بضعة أسطر. جرى استخراج `_codex_env_base()` ومشاركته بين المسارين؛ والقياس بعد التصحيح: 0 سرّ من الجانبين.
    - **لم تكن خاصية «`--deny` بالإغلاق عند الفشل» تغطي الصيغة المستخدمة.** كان التعليق يبرّر احتواء Grok بالكامل بكون قاعدة ذات بادئة مجهولة تؤدي إلى رفض بدء التشغيل. وقد أظهر القياس على `grok 1.0.13` أن هذا التحقق لا يوجد **إلا للصيغة الموضوعة بين قوسين**: يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل («بادئة أداة مجهولة»)، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. لكن `GROK_DENY_RULES` لم يكن يستخدم سوى أسماء مجردة — وبالتالي كان من شأن إعادة تسمية أداة من جانب xAI أن تزيل طبقة الاحتواء الوحيدة المقاسة من دون أي إشارة، على جهاز لا ينطبق عليه أصلًا sandbox نظام التشغيل. نُقلت القواعد الثماني المسماة إلى `Prefix(*)`، مع التحقق من كل واحدة منها باعتبارها بادئة معروفة لدى CLI؛ وتظل قاعدة الالتقاط الشامل `*` بصيغتها الحرفية، وهي الصيغة الوحيدة المقبولة. يمنع اختبارٌ العودة إلى الصيغة غير المتحقق منها.
    - **وتبيّن أن بقية الجوانب سليمة**: لا يوجد أي حقن للأوامر (صيغة القائمة مستخدمة في كل موضع، ولا يُستخدم `shell=True` مطلقًا، ومحتوى المستند يمر عبر stdin أو `--prompt-file`)، ولا توجد إزالة تسلسل غير آمنة (`json.loads` فقط، مع حارس للنوع)، ولم يُعثر على تجاوز لتصحيح اجتياز المسار عبر سبع حمولات، كما أن CLI يطبّق `--deny '*'` فعليًا (لوحظ `DENY_ENFORCED` عند قراءة خارج workdir).
    - وكان فحص الحداثة المضاف أعلاه يتجاوز مبدئه الخاص أيضًا: إذ كان يتخطى بصمت أي حزمة يفشل طلبها إلى PyPI، فتظل البوابة خضراء. وهو يحصي الآن الحزم التي جرت مقارنتها فعليًا ويفشل إذا كانت التغطية غير مكتملة.

  - **رُقّيت التبعيات إلى المستوى المطلوب، وأضيفت شبكتا أمان لمنع تكرار التأخر**:

    - **كان التأخر حقيقيًا ومزمنًا**: `openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر عامين في مخزن الشهادات الجذرية الذي يتحقق من TLS لجميع استدعاءات مزوّدي الخدمة. جرى تحديد السبب: **لم يكن `.github/dependabot.yml` موجودًا**. من دون هذا الملف، لا يفعّل GitHub سوى _تحديثات الأمان_، ولا يقترح Dependabot طلب PR إلا لتبعية تستهدفها ثغرة CVE — وهذا يفسر ترقيته `urllib3` و`idna` مع ترك مجموعتي SDK تنجرفان بعيدًا بمقدار إصدار رئيسي كامل.
    - **يتعايش الإصداران الرئيسيان من دون تعارض**، خلافًا لما أوحى به استدلال سابق: ينتقل `openai` 3.x و`anthropic` 1.x إلى **`httpx2`**، بينما يبقى `mistralai` و`google-genai` على `httpx<1`، لكنها توزيعات منفصلة. جرى التحقق عبر تثبيت فعلي، ثم عبر **مسارات مزوّدي الخدمة السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الشيفرة المضمّنة والروابط في كل مخرَج. كان «تجنّب مكدّسي HTTP» تفضيلًا لا عائقًا؛ وقد حسم القياس المسألة.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: كانت `google-auth` و`cryptography` ومكدّس `opentelemetry` مثبتة في venv العمل من دون التصريح بها قط — ولذلك لم يكن التثبيت النظيف يعيد إنتاج ما جرى اختباره. وعلى العكس، كانت `tokenizers` و`huggingface-hub` و`PyYAML` مدرجة فيه من دون أن يستوردها أو يتطلبها أي شيء، وهي بقايا من `mistralai` 1.x. أُعيد توليد الملف ليكون الإغلاق الكامل لـ venv مبني فقط من التبعيات المباشرة. لا يُبلغ `pip-audit` عن أي ثغرة معروفة في المجموعة الجديدة.
    - يفعّل **`.github/dependabot.yml`** (جديد) تحديثات الإصدارات الأسبوعية لكل من pip وgithub-actions. تُجمع الإصدارات الثانوية والتصحيحية في PR واحد — إذ ينتهي الأمر بتجاهل ترقية تصحيحية لكل PR، والضجيج عدو التحديث؛ أما **الإصدارات الرئيسية فتظل منفصلة**، ويتطلب كل منها تحققًا باستدعاء فعلي.
    - يجعل **`scripts/check-deps-fresh.sh`** (جديد وموصول بالبوابة) التأخر ظاهرًا في حكم المشروع: يقترح Dependabot، لكنه لا يضمن، وقد تتراكم طلبات PR الخاصة به. تأخر إصدار رئيسي ← فشل؛ إصدار ثانوي ← تحذير، لأن البوابة التي تبقى حمراء دائمًا ينتهي بها الأمر إلى التجاهل؛ تعذّر الوصول إلى PyPI ← تخطٍّ صريح محليًا و**إغلاق عند الفشل في CI**، لأن الفحص غير المنفّذ لا يُعد نجاحًا. جرى التحقق في الاتجاهين: فهو يلتقط الحالة الدقيقة السابقة للتصحيح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير بشأن إصدار ثانوي.

  - **تصحيحات ناتجة عن مراجعة PR هذا** — فحص خمسة وكلاء مراجعة الفروقات بدقة؛ وقد **أُعيد إنتاج** جميع النقاط أدناه **بالقياس** قبل تصحيحها، وكان اثنان منها انحدارين أُدخلا أعلاه في هذا الإصدار نفسه.

    - **انحدار جرى تصحيحه — كان `_NEWS_CITATION_REGEX` يعاني من تراجع أُسّي.** أدخل إصلاح الفقرات المتعددة `(?:[ \t]*$|[ \t]+.*)` داخل التكرار: إذ كان تقاسم المسافات بين `[ \t]+` و`.*` ملتبسًا، وكان هذا الالتباس يتضاعف من تكرار إلى آخر. أظهر القياس على أسطر `>   texte` — وهي إزاحة Markdown مشروعة تمامًا — لا تطابق النمط: **2,589 مللي ثانية لـ14 سطرًا** مقابل 0.04 مللي ثانية بعد التصحيح، مع معامل يقارب 9 لكل سطر مضاف. في وضع `--news`، كان اقتباس كتلي طويل وغير مطابق كافيًا لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب يمكن تحديده. يستهلك التكرار الآن السطر كاملًا دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يترك سوى طريقة واحدة للمطابقة في كل تكرار. جرى التحقق على المتن الفعلي المكوّن من 231 مقالة: **صفر اختلاف** في الالتقاط، ونفس الاقتباسات البالغ عددها 423، مع استمرار توسيع الأجسام الأربعة عشر متعددة الفقرات.
    - **كان استخدام علامتي مزوّد خدمة في آن واحد يؤدي إلى فوترة حسب الاستخدام بصمت.** كان `--use_codex --use_mistral` مقبولًا؛ يختبر `_select_provider_client` مزوّد Mistral أولًا، ويعطي `_resolve_provider` الأولوية للقيم المنطقية الصريحة — فكان كلاهما ينتهي إلى Mistral. وبذلك كان المستخدم يطلب حصة اشتراكه ويحصل على فوترة حسب الاستخدام، من دون أدنى تحذير: وهو بالضبط نمط الإخفاق الذي وُجد `--use_codex` لمنعه. تمر علامات مزوّدي الخدمة الست الآن عبر `add_mutually_exclusive_group`. **تغيير في السلوك**: يفشل الآن عند `argument --use_mistral: not allowed with argument --use_codex` سطر أوامر يجمع مزوّدين، بعدما كان يُقبل بصمت.
    - **كانت بوابة نهاية العمل تظل خضراء عندما تتعطل مجسّاتها.** اتبعت أربعة من فحوص `scripts/check-release-ready.sh` الثلاثة عشر نمط «التقاط stdout والاستنتاج إذا كان فارغًا» من دون فحص رمز الإرجاع قط: كان الاستثناء (ملف أُعيدت تسميته، `FileNotFoundError`) يكتب إلى stderr ويترك stdout فارغًا، فيستنتج الفحص أنه «لا شيء يستدعي الإبلاغ». وهكذا أُعيد إنتاج فخ «لا يثبت `exit 0` شيئًا» داخل السكربت المكتوب لمنعه. يفرض مساعد `probe()` الآن رمز إرجاع صفريًا **وعلامة نهاية**، وترفض المجسّات الاستنتاج من مجموعة علامات فارغة — لأن أي توكيد على مجموعة فارغة يكون صحيحًا دائمًا. الدليل: أدت إضافة المجموعة الحصرية أعلاه إلى تمرير علامات مزوّدي الخدمة عبر كائن `*_group`، فلم يعد regex القديم `parser\.add_argument\(` يطابقها؛ وخرجت **ست علامات من أصل إحدى وعشرين** من النطاق بصمت، مع بقاء البوابة خضراء.
    - **كان فحص الأسرار يفوّت أربعة مزوّدين من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: ولذلك كان `sk-proj-…` (صيغة OpenAI الحالية) و`sk-ant-api03-…` ينقطعان عند الشرطة الثانية، ولم يكن `AIza…` مشمولًا. وُسّع النمط، واستُبعد `.secrets.baseline` من الفحص. إضافة إلى ذلك، كان الحارس `.env` يستعلم عبر `git diff --cached`، الذي لا يرى سوى الفهرس: فلم يكن `.env` **الملتزم به سابقًا** — وهو أسوأ الحالات — يظهر فيه قط. وهو يستعلم الآن عبر `git ls-files`.
    - **لم تكن «تهيئة الرمز» في Codex تهيئة فعلية.** أظهر القياس أن `codex login status` لا يمس `~/.codex/auth.json` (لم يتغير وقت التعديل ولا الحجم)، وتقول مساعدته «إظهار حالة تسجيل الدخول». ومع ذلك كان التعليق يزعم تحديث الرمز «مرة واحدة، بالتتابع»، ومن ثم تحييد خطر عمليات التحديث المتزامنة لرمز دوّار أحادي الاستخدام. لم تكن الحماية المعلنة موجودة؛ وأصبح التعليق الآن يصف ما تفعله الشيفرة فعلًا، بينما يبقى التدبير الحقيقي هو `max_jobs=4`. كما يحترم الفحص `CODEX_BIN` الآن، بعدما كان يتجاهله — إذ كان جهاز لا يحتوي على `codex` ضمن `PATH` يفشل برسالة «غير مصادق عليه»، وهو تشخيص مضلل.
    - **كان `.env` يُحمّل في shell فرعي.** يُستدعى `detect_provider` ضمن استبدال أمر، ولذلك لم تكن عمليات التصدير الخاصة به تصعد إلى الأعلى: فكان `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` المعرّف في `.env` يظل غير مرئي لعمليات القراءة المنفّذة في `main()`، التي كانت تستنتج «تعذر العثور على ملف Grok الثنائي» رغم صحة الإعداد.
    - **كانت درجة التوازي تتجاوز الحد المعلن بنسبة 50%.** كان الحارس موضوعًا بعد تشغيل زوج README/CHANGELOG: وبلغت الذروة المقاسة **3 لـ`max_jobs=2`**. وفي Grok، حيث تُشارك الحصة الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يكن السكربت يلتزم إذًا بالحد الذي فرضه على نفسه. أما العد النهائي فكان يُعرض من دون مقارنته قط بالعدد 28 — ولذلك كان غياب ملف يمر من دون اكتشاف.
    - **عقد مخرجات Grok: أصبح غياب `stopReason` إخفاقًا.** كانت الشيفرة تطبق «`end_turn` **أو غائب**» بينما يتطلب العقد المعلن `end_turn`. كانت حمولة بلا هذا الحقل — أو أُعيدت تسمية الحقل فيها بفعل تحديث CLI — تحول الحارس إلى عملية عديمة الأثر بصمت. كذلك لم يعد `max_turn_requests` مصنفًا على أنه تجاوز لحد المعدل (بل استُنفدت ميزانية الجولات: إعادة المحاولة تعيد النتيجة نفسها، مقابل انتظار 90 ثانية)، وأُزيل `quota` من علامات تجاوز حد المعدل — للسبب الذي كان docstring الخاص بـ`_codex_is_rate_limited` يذكره أصلًا من دون أن يطبقه Grok.
    - **تُحفظ سلسلة Gemini مؤقتًا حسب النموذج.** كانت تبدأ مجددًا من `minimal` عند كل مقطع، رغم أن النموذج الافتراضي يرفضه: فكان المسار الاعتيادي يدفع تكلفة رحلة ذهاب وإياب باستجابة 400 لكل مقطع، ويعيد طباعة التحذير نفسه. والتحذير الذي يتكرر مئات المرات يتوقف الناس عن قراءته — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مكتوبة صراحةً لـCodex وتوجّه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ وكان تعليق أساس العمليات الفرعية يعمم «shim» على أداتي CLI رغم أن ملف Grok الثنائي هو ELF أصلي (التعليل الصحيح هو «وكيل ينشئ عملياته الفرعية الخاصة»)؛ وعُلّمت اثنتا عشرة نتيجة SAST في `subprocess` بالعلامتين `# nosec` / `# nosemgrep` مع التعليل، إذ تجعل صيغة القائمة من دون `shell=True` الحقن مستحيلًا، ولا يمر محتوى المستند عبر argv مطلقًا.
    - **لم يعد أي سر يدخل إلى العمليات الفرعية الوكيلة.** لم تكن قوائم المنع الاسمية تحمي سوى ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). أظهر القياس أن **سبعة أسرار أخرى** كانت لا تزال تدخل إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح CLI الأخرى، و`OPENAI_BASE_URL`، الذي ليس سرًا لكنه يعيد توجيه حركة المرور. غير أن أداتي CLI هاتين **وكيلتان**، ويعمل وكيل Grok من دون sandbox لنظام التشغيل قابل للتطبيق على كثير من أجهزة Linux. تجري التصفية الآن **بحسب نمط الاسم** (`API_KEY` و`_TOKEN` و`SECRET` و`PASSWORD` و`CREDENTIALS`)، لا بحسب قائمة اسمية، وهو ما يشمل أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن تعرفها هذه الشيفرة. لا تحتاج أداتا CLI إلى أي منها: تعيش المصادقة في `~/.codex` و`~/.grok`، وليس في البيئة مطلقًا — وقد جرى التحقق عبر **ترجمة فعلية مكتملة** باستخدام كل من مزوّدي الخدمة مع البيئة المحصنة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يثبّت حصرية علامات مزوّدي الخدمة، وعقد `stopReason`، وخطية regex الأخبار، ورسالة الرفض في CI، والحفظ المؤقت في Gemini، وغياب أي سر من بيئة العمليات الفرعية. وهذا التوكيد الأخير **عام** — إذ يفشل عند وجود مفتاح لا تسمّيه أي قائمة — بينما كان اختبار التنقية القائم مرآة لثابته، ولم يكن قادرًا على اكتشاف أي شيء سوى تعطل حلقته الخاصة. بلغ إجمالي المجموعة **311 اختبارًا**.
  - **موفّران جديدان لـ Grok**: ‏`--use_grok` ‏(واجهة API من xAI، المفتاح `XAI_API_KEY`، والفوترة حسب الاستخدام) و`--use_grok_cli` ‏(واجهة CLI الرسمية لـ Grok Build، ويُحتسب استخدامها من اشتراك Grok — بالمبدأ نفسه المتّبع في `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن نقطة نهاية xAI متوافقة مع OpenAI، أُعيد استخدام العميل و`_call_openai` كما هما، ولم يتغير سوى `base_url`. لم يلزم سوى تكييف واحد يستفيد منه الجميع: يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي تصدرها xAI حيث تصدر OpenAI الصيغة `stop`. النماذج: `grok-4.6` ‏(الجودة) و`grok-4.3` ‏(الاقتصادي). وتجدر الإشارة إلى أن نموذج Grok الاقتصادي يظل الأغلى في المستودع — ‏$1.25/$2.50 لكل مليون مقابل ‏$0.15/$0.60 لـ `mistral-small-latest`: يُختار هذا الموفّر لتنوّع النماذج، لا للسعر.
    - **وضع CLI**: صُمّم على غرار Codex، مع أربعة اختلافات فرضها الواقع — يُمرَّر الطلب عبر ملف (`--prompt-file`، لأن واجهة CLI لا تقرأ stdin، ولأن أي مقطع في argv سيكون ظاهرًا في `ps`)، والمخرَج كائن JSON وحيد على stdout (لا JSONL ولا ملف `-o`)، ولا يتيح الاشتراك سوى `grok-4.6` و`grok-4.5`، كما لا يمكن تطبيق sandbox (انظر أدناه). جرى توحيد تشغيل العمليات الفرعية مع Codex في `_codex_run_process`، من دون المساس ببقية موفّر Codex الذي سبق اختباره.
    - **`exit 0` لا يثبت شيئًا، وقد قِيس ذلك**: عندما لا يكون المستخدم موثّقًا، تكتب واجهة CLI ‏`{"type":"error","message":"Not signed in."}` على **stdout** مع رمز خروج **0**. ويتصرف الرفض أو تجاوز عدد الجولات بالطريقة نفسها. لذلك يتطلب عقد المخرجات تحقق أربعة شروط متزامنة: رمز خروج 0، وغياب حمولة خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع الفحص المسبق المنطق نفسه: ينتهي `grok models` بالرمز 0 حتى عند قطع الاتصال، ولا يمكن الاستنتاج إلا من وجود «غير موثّق» في stdout.
    - **العزل: تفاوت مقصود وموثّق.** فبينما يعمل Codex داخل `--sandbox read-only`، يتعذر تطبيق sandbox الخاص بـ Grok على كثير من أجهزة Linux الحديثة، لسببين نظاميين مستقلين لا يمكن تجاوزهما من دون `sudo`: يحظر AppArmor مساحات أسماء المستخدمين غير المميّزين منذ Ubuntu 24.04 ‏(`bwrap: setting up uid map: Permission denied`، وقد أُعيد إنتاج المشكلة خارج Grok)، وتفشل قائمة حظر مقابس بيئة تشغيل الحاويات عندما يكون `/run/podman` في `0700` ‏(لا يعالج المحلّل سوى `ErrorKind::NotFound`، بينما يصبح EACCES خطأً فادحًا). والمأزق الأساسي أن ملفًا شخصيًا **مدمجًا** يتعذر تطبيقه يؤدي إلى التشغيل **من دون عزل بصمت**. لذلك لا يطلب البرنامج النصي أي ملف شخصي افتراضيًا ولا يتراجع بصمت مطلقًا — بل يصدر تحذيرًا على stderr. وتعتمد الحماية على قواعد `--deny` الخاصة بواجهة CLI، بما فيها قاعدة الالتقاط الشامل `*`، وهي طبقة الحماية الوحيدة التي ثبت بالقياس أنها _تغلق عند الفشل_ (إذ تؤدي قاعدة ذات بادئة مجهولة إلى رفض التشغيل). يتيح `GROK_TRANSLATE_SANDBOX=read-only` فرضها، وعندئذ يفشل التشغيل إذا تعذر على الجهاز احترامها.
    - **ضوابط الأمان**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (إذ يؤدي وجود مفتاح إلى التحول إلى الفوترة حسب الاستخدام؛ كما أن `GROK_SANDBOX` موروثًا سيفرض ملفًا شخصيًا غير قابل للتطبيق مع رسالة مضللة)، وتُعطّل مفاتيح MCP/hooks/skills/agents، مع `--disable-web-search` و`--no-subagents` و`--no-plan`، ودليل عمل مؤقت، ورفض التشغيل في CI، ومهلة تقتل مجموعة العمليات، وتراجع تدريجي عند بلوغ حد المعدل. ضُبط `--max-turns` على 6 لا على 1: إذ تزداد قيمة العداد بعد جولة الأدوات، وكانت القيمة 1 ستقتطع المخرجات.
    - **الحصة**: مجموعة Grok أسبوعية و**مشتركة مع Chat وImagine وVoice**، ولا يعرضها أي أمر — خلافًا لـ Codex، حيث يتيح `account/rateLimits/read` قياس الاستهلاك كميًا. لذلك يحد `regen_translations.sh` التزامن عند 2 ويصدر تحذيرًا صريحًا.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` ‏(24 اختبارًا). المجموعة الكاملة تضم **290 اختبارًا**.
  - **إصلاح خلل — لم تكن الاقتباسات الإنجليزية متعددة الفقرات محمية إلا جزئيًا (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل في متن الاقتباس سوى سلسلة من أسطر `>` **المتتالية**. وما إن يمتد اقتباس عبر عدة فقرات (تفصل بينها سطر `>` فارغ) حتى لا يُلتقط ويُستبدل بالعنصر النائب سوى آخرها؛ أما الفقرات السابقة فكانت تُرسل إلى LLM وتعود مترجمة — وهو النقيض التام لما وُجد `--news` لضمانه. بات التكرار يقبل الآن أسطر `>` الداخلية الفارغة وأصبح غير جشع، ليتوقف عند سطر `>` الفارغ الذي يسبق السطر المائل، لا عند أول سطر فارغ يصادفه.
    - **النطاق المقاس** على مجموعة فعلية من 198 مقالة: تأثر 11 اقتباسًا من أصل 419. لا توجد أي انتكاسة — يلتقط التعبير النمطي الجديد العدد نفسه تمامًا من الاقتباسات، ولا تمتد سوى المتون متعددة الفقرات (408 متون متطابقة، و11 ممتدة)، ولا يزال يتعذر امتصاص سطر الإسناد `> — …` داخل المتن (مع الإبقاء على الاستشراف).
    - **دليل شامل من البداية إلى النهاية** على مقالة بحجم 69 كيلوبايت تُرجمت إلى اليابانية والعربية: ظلت الفقرة الأولى من اقتباس، التي كانت تُعرض سابقًا بصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم كذلك إلى العربية، الآن بصيغة `> GLM-5.3 is now open-weight.`. وعاد عدد أسطر الاقتباس الإنجليزية من 9 إلى 10، مطابقًا للمصدر.
    - تجدر الإشارة إلى أن أدوات التحقق اللاحقة لم تكتشف هذا العيب، لأنها تتحقق من وجود الاقتباس من دون التأكد من اكتماله.
  - **توفير مقاس لدى الموفّر الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ اسم النموذج بـ `gpt-5`، حتى في `--eco`. القياس باستخدام `gpt-5.4-mini` لترجمة جملة من عشر كلمات: ‏`medium` ← ‏45 رمز استدلال و65 رمز إخراج؛ ‏`none` ← ‏0 و14. لا يضيف الاستدلال شيئًا إلى الترجمة، ومع ذلك كانت تكلفته تُدفع لكل مقطع من كل ملف. أصبحت القيمة الافتراضية `none` في `--eco`، وظلت `medium` في غير ذلك؛ وتبقى للقيمة الصريحة الممررة عبر CLI الأولوية. يقبل `--reasoning_effort` الآن `none` و`xhigh` إلى جانب `low`/`medium`/`high` ‏(ولا تقبل جميع النماذج كل هذه القيم: فعلى سبيل المثال، يرفض `gpt-5.4-mini` القيمة `minimal` — وتغطي إعادة المحاولة الحالية من دون المعامل هذه الحالة).
  - **تحديث حزم SDK وترحيل Gemini**: استُبدلت `google-generativeai` ‏(انتهى دعمها في 2025-11-30 وأُرشف مستودعها) بحزمة SDK الموحّدة **`google-genai`** — ‏`genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير طلب النظام في `system_instruction` بدلًا من دمجه مع المقطع. رُقّي `mistralai` إلى **2.9.4** ‏(وأصبح الاستيراد `from mistralai.client import Mistral`؛ أما القديم فيطلق `ImportError`، وقد جرى التحقق من ذلك في wheel)، و`anthropic` إلى **0.125.0**، و`openai` إلى **2.54.0** — وهي أحدث الإصدارات السابقة للانتقال إلى `httpx2`، تجنبًا لتشغيل مجموعتي HTTP معًا في venv. وبناءً على ذلك، فُك تثبيت `httpx` عند 0.28.1 و`pydantic` عند 2.13.5.
  - **انتكاسان التقطتهما اختبارات فعلية، لا الوثائق**:
    - يرفض `anthropic` بالإصدار 1.0 أو أحدث من جهة العميل استدعاءً غير متدفق إذا كان `max_tokens` يوحي بأنه سيستغرق أكثر من 10 دقائق (`ValueError: Streaming is required...`). لم يكن هذا الضابط موجودًا في 0.34.2، وكان يعطّل كل استدعاء Claude يتضمن `max_tokens=32768`. أُصلح ذلك باستخدام `timeout` صريح (`CLAUDE_TIMEOUT`، و900 ثانية افتراضيًا)، ما يجنب الانتقال إلى التدفق في استدعاء لا تُستخدم منه إلا الاستجابة الكاملة.
    - لا يقبل `thinking_level="minimal"` سوى جزء من قائمة نماذج Gemini: يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` بخطأ 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تسلسل تراجعي `minimal` ← ‏`low` ← من دون thinking_config، على غرار آلية التراجع الموجودة مسبقًا لـ OpenAI — إذ يجب ألا يؤدي معامل تحسين أبدًا إلى إفشال ترجمة.
  - **تجديد النماذج الافتراضية**، بعد التحقق من كل منها باستدعاء فعلي: ‏OpenAI ‏`gpt-5.5` ← **`gpt-5.6-terra`** ‏(أقل بنسبة 60% في دفعة من 28) و`gpt-5.4-mini` ← **`gpt-5.6-luna`** ‏(أقل بنسبة 73%)؛ ‏Claude ‏`claude-sonnet-4-6` ← **`claude-sonnet-5`** ‏(أرخص وأحدث) و`claude-haiku-4-5-20251001` ← **`claude-haiku-4-5`** ‏(معرّف قياسي بلا تاريخ)؛ ‏Gemini ‏`gemini-3.1-pro-preview` ← **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` ← **`gemini-3.1-flash-lite`** ‏(إصدار مستقر وأرخص من `3.5-flash-lite`). لم يتغير Mistral، إذ يظل `mistral-large-latest` صاحب أفضل نسبة جودة إلى السعر بين الأربعة. وتجدر الإشارة إلى أنه لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — أما Gemini 3.5 Pro، الذي أُعلن عنه في مايو 2026، فلم يصدر قط؛ وسلسلة 3.5/3.6/3.7 مخصصة حصريًا لفئة Flash.
  - **اختبار A/B مقاس قبل تحويل Gemini**: ترجم `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. كانت البنية متطابقة تمامًا (21 قائمة، و18 كتلة برمجية، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، في **8 ثوانٍ مقابل 48 ثانية**. ولعدم وجود أي معيار أداء عام يقارن هذين النموذجين في الترجمة أو النصوص غير اللاتينية، كان التحويل سيعتمد لولا ذلك على مجرد افتراض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفذ `block.text for block in response.content` من دون تصفية حسب النوع. تُدرج النماذج ذات الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking` بين الكتل، وهي تعرض `.thinking` لا `.text` — ما كان سيعطّل الترجمة بخطأ `AttributeError` مبهم عند أول مقطع. باتت كتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` تُستبعد الآن (باستخدام قائمة سلبية للحفاظ على تقبّل نوع مجهول يحمل نصًا)، وتؤدي الاستجابة الخالية تمامًا من أي كتلة نصية إلى خطأ صريح. ويُمرر `thinking={"type": "disabled"}` في كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: حُذفت النماذج التي انقضى تاريخ سحبها (سلسلة `magistral-*` المسحوبة في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، إضافة إلى `claude-3-5-sonnet-20240620` و`claude-3-7-sonnet-20250219` و`claude-opus-4-1-20250805` و`claude-sonnet-4-20250514`). تصحيحات الحدود: ‏Mistral من 128K إلى **256K** ‏(جيل Large 3 / Small 4)، وGemini من 1 000 000 إلى **1 048 576** ‏(حد الإدخال الفعلي)، و`claude-opus-4-5` من 200K إلى **1M**، وعائلة `gpt-5.6-*` من 400K إلى **1.05M**. أُضيفت Claude 5 ‏(`claude-sonnet-5` و`claude-opus-5` و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وسلسلة `ministral-*`. وتجدر الإشارة إلى أن هذه الحدود تظل استرشادية، إذ يضع `translate()` حدًا أقصى للتقسيم عند `min(16000, limite)`.
  - **المزوّد `--use_codex`**: المزوّد الخامس الذي يشغّل CLI الرسمي لـ Codex ‏(`codex exec`) في الوضع غير التفاعلي، بدلاً من استدعاء API تُحتسب تكلفتها حسب الاستخدام. تُخصم الترجمة من حصة اشتراك ChatGPT المدفوع مسبقًا. وهذا هو المسار الوحيد الذي توثّقه OpenAI لهذا الاستخدام: إذ تدرج مصفوفة التوفّر حسب الخطة «Codex SDK و`codex exec` ومسارات العمل القابلة للبرمجة» على أنها متاحة في Plus/Pro/Business/Enterprise، بينما لا تصادق رموز `~/.codex/auth.json` على استدعاءات API Platform (ولا يقرأها هذا السكربت مطلقًا — إذ يظل CLI مسؤولًا عن المصادقة وتجديدها).
  - **ملف Codex التنفيذي قابل للتثبيت عبر pip، وليس عبر npm فقط بعد الآن**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK ‏`openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عام لاستخدام `--use_codex`. لا تُضاف الحزمة إلى `requirements.txt`: إذ يبلغ حجم الملف التنفيذي نحو 250 ميغابايت، ما كان سيفرضه على جميع المستخدمين من أجل مزوّد اختياري. تم التحقق من المسار كاملًا: عند غياب `codex` عن `PATH`، يعثر الحلّ على الملف التنفيذي المضمّن في الحزمة وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: يُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. من دون هذا الإجراء الوقائي، قد يتسبب مفتاح موجود في `.env` في تحويل Codex إلى الفوترة حسب الاستخدام من دون أي إشارة ظاهرة — وهو بالضبط ما وُجد هذا المزوّد لتجنّبه.
  - **مشكلات CLI الشائعة المثبّتة باختبارات**:
    - يقرأ `codex exec` الإدخال القياسي **حتى** عندما يُمرَّر المحث كوسيط: من دون إغلاق الإدخال القياسي، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج إطلاقًا (أُعيد إنتاج الحالة: خروج 124 بعد 180 ثانية، وصفر بايت). لذلك فإن `communicate(input=...)` إلزامي.
    - إن `codex` المثبّت عبر npm هو وسيط Node ينفّذ `spawn` للملف التنفيذي الحقيقي المكتوب بلغة Rust: ويكون هذا الملف **عملية حفيدة** لعملية Python، ويظل حيًا بعد `SIGKILL` الخاص بـ`subprocess.run(timeout=)`، حيث كان سيواصل استهلاك الحصة. ومن هنا استخدام `Popen(start_new_session=True)` + `os.killpg`.
    - قد يخرج CLI بالرمز 0 رغم إصداره `turn.failed`: لذلك يُفحص خرج JSONL ‏(`--json`) إضافة إلى رمز الإرجاع، ويؤدي غياب الملف `-o` مع رمز 0 إلى رفع خطأ صريح بدلاً من إنتاج مقطع فارغ.
  - **تراجع تدريجي عند بلوغ حد المعدّل**: لا يطبّق CLI أي إعادة محاولة داخلية (`max_retries = 0`). يجري التصنيف بناءً على بنية حمولة JSON ‏(`status: 429` / `error.type`)، لا على السلاسل الفرعية — إذ تظهر كلمة «الحصة» في كل من خطأ 429 القابل للاسترداد و`insufficient_quota` النهائي.
  - **إجراء وقائي لـCI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفًا. فالمصادقة عبر الاشتراك غير مخصّصة لمشغّل مشترك، وتنصح OpenAI صراحةً بعدم استخدام مسار العمل هذا في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` ‏(`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وAPI Platform، لكن حساب ChatGPT لا يملك صلاحية الوصول إلى جميع نماذجها: تُطبّق قائمة السماح من جهة الخادم من دون تحقق محلي، ويؤدي تحديد نموذج غير معتاد إلى ظهور تحذير. في خطة Plus، يوفّر Luna ما بين 250 و2,000 رسالة لكل نافذة مدتها 5 ساعات، مقابل 10 إلى 100 لـSol: ويُعد `--eco` الوضع الموصى به لجميع عمليات المعالجة الدفعية.
  - **إصلاح خلل — كان `regen_translations.sh` ينتهي بخطأ رغم النجاح الكامل**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` تابع لـ`main()` لم يعد موجودًا عند تنفيذ المصيدة. وتحت `set -u`، كان ذلك يرفع `failed_log: unbound variable` ويُنهي السكربت بالرمز 1 رغم صحة الترجمات الـ28 — ما كان سيوقف `release.sh --auto` ‏(`set -e`) مباشرة بعد إعادة التوليد، في المرحلة الأعلى تكلفة. أصبح المتغير عامًا، وصارت المصيدة تتحقق من وجوده. ومن الآثار الجانبية المفيدة أن إخفاقات الترجمة الحقيقية، التي كان هذا الخطأ يحجبها سابقًا، أصبحت مرئية مجددًا في الملخص الختامي.
  - **`REGEN_MODEL`**: متغير بيئة جديد في `regen_translations.sh` يفرض نموذجًا محددًا متجاوزًا النموذج الافتراضي للمزوّد، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة التوليد باستخدام النموذج المتقدم ضمن حصة الاشتراك بدلاً من نموذج `--eco` الموجّه للأحجام الكبيرة.
  - **`regen_translations.sh`**: أصبح `REGEN_PROVIDER=codex` متاحًا عبر تفعيل صريح (ولا يُكتشف تلقائيًا مطلقًا، لتجنّب استهلاك حصة الاشتراك من دون علم المستخدم). يُجدَّد الرمز مرة واحدة وبشكل تسلسلي قبل بدء التوازي — لأن تجديد Codex دوّار وأحادي الاستخدام، وستؤدي المهام المتزامنة إلى إبطال جلسة `codex login` — كما يُخفَّض التزامن إلى 4.
  - **إعادة الهيكلة المرتبطة**: ينتقل `_dispatch_provider_call` من 8 إلى 6 معاملات عبر `_resolve_provider()` يعيد اسم المزوّد، بدلاً من تمرير قيمة منطقية رابعة عبر السلسلة بأكملها. وتظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` بالحد الأدنى.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` ‏(48 اختبارًا) يغطي argv، والبيئة المنقّحة، وعقد منع المقدمات، والإخفاقات الصامتة، والمهلة/killpg، والتراجع التدريجي، والفحص المسبق، وحلّ المزوّد، وتسلسل الاستدلال في Gemini، وترشيح كتل Claude، واستشهادات الأخبار متعددة الفقرات. تضم المجموعة الكاملة 290 اختبارًا.
  - **التحقق الفعلي**: عند ترجمة `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex، تكون بنيته مطابقة تمامًا لبنية الترجمات المرجعية (14 كتلة برمجية، و24 عنوانًا، و25 سطرًا في الجداول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل برمجية متطابقة حرفًا بحرف، ومن دون أي بقايا لعناصر نائبة). وفي مقال صحفي حجمه 69 كيلوبايت ضمن وضع `--news`، يجتاز كل من خرجي `gpt-5.6-luna` و`gpt-5.6-sol` مدقّق التطبيق اللاحق للغات en/ja/ar. أما الاستهلاك المقاس عبر `account/rateLimits/read` فقد ظل دون عتبة التقريب في العداد (0% من نافذة الساعات الخمس) ضمن وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL لإسناد الأخبار عند وجود أقواس متداخلة أو بادئة فرنسية (2026-05-11):

  - **إصلاح خلل**: كان استخراج عناوين URL الخاصة بالإسناد في `_protect_news_quotes` يستخدم التعبير النمطي `re.search(r"\((.+?)\)", attribution)` (التقاط كسول بين قوسين). وفي إسناد من نوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` الخارجي + `]()` الخاص برابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة تتضمن كذلك البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائي). والنتيجة أن `_validate_news_post` كان يبحث عن هذه السلسلة في الخرج المترجم ويفشل دائمًا (لسببين: `)` مبتور + ترجمة «نقلًا عن» إلى `relayed by`/`weitergeleitet von`/...). ولم يكن بوسع التسلسل الكامل low ← medium ← high ← gpt-5.5 اجتياز التحقق.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — إذ يستهدف تحديدًا `](url)` الخاص برابط Markdown، ويلتقط **عنوان URL الصافي فقط** (من دون البادئة الفرنسية أو البتر)، وهو ثابت تحافظ عليه العناصر النائبة `#URL{N}#` أثناء الترجمة. وهو متين أمام النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس خارجية
  - **الاختبارات**: اختباران جديدان في الفئة `TestNewsCitationExtraction` ضمن `test_silent_failure.py`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة الدقيقة التي أعادت إنتاج خلل Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (صيغة بديلة باستخدام `via`)
  - **فجوة التغطية**: يتحقق `check-editorial-coverage.py` من صحة الصياغة التحريرية، لكنه لا يتحقق من قابليتها للترجمة بواسطة المترجم. ومن التحسينات الممكنة (خارج نطاق v1.9.2) إضافة فحص يحاكي استخراج الإسناد في تشغيل تجريبي لاكتشاف الأنماط الخطرة **قبل** النشر.

- **1.9.1** إصلاح i18n لتسمية CTA في ملاحظة علامة الترجمة (2026-05-10):

  - **إصلاح خلل**: كانت تسمية `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط العلامة أعلى الملفات المترجمة تظل **بالفرنسية** لجميع اللغات المستهدفة، بدلاً من اتباع `target_lang`. ولا يراها LLM مطلقًا (إذ يجري تركيبها من جهة Python للحفاظ على عنوان URL ومعرّف slug الخاص بالمستودع)، لذلك لم تكن مرحلة الترجمة قادرة على تصحيحها. وهو انحدار صامت منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الـ15 بتسمياتها المترجمة. ويمرّر `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` الآن اللغة المستهدفة. ويُستخدم `fr` كقيمة احتياطية إذا كانت اللغة مجهولة (للأمان، ومن دون KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link` ‏(target_lang ‏`ja` ← التسمية اليابانية المتوقعة). واختباران جديدان: `test_source_link_label_localized_per_target_lang` (معلَّم على 7 لغات تغطي الكتابات اللاتينية والتصويرية والأبجدية الصامتة) و`test_source_link_label_falls_back_to_french_for_unknown_target`. الإجمالي: 40 اختبارًا في `test_translation_note_position.py` (بدلاً من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع بقيمة افتراضية `target_lang="fr"` — تواصل الجهات البرمجية الخارجية المستدعية التي لا تمرّر `args.target_lang` العمل من دون تعديل.
- **1.9** إصلاح الفشل الصامت + أدوات جودة متكاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + تنسيق العلامة "embed card"**:
    - خيارات CLI جديدة (إضافية، الإعدادات الافتراضية دون تغيير ← **غير كاسرة للتوافق**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): يضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - يعيد `legacy` إنتاج سلوك v1.8 بدقة (فقرة غامقة `**…**`) **بايتًا ببايت**.
        - يُصدر `marker` تعريف مرجع رابط Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`)، يتبعه **اقتباس مكوّن من 3 فقرات** ومنظّم لعرض من نوع "GitHub repo embed card": عنوان المشروع ضمن كود سطري (`**\`ai-powered-markdown-translator\`\*\*`)، ووصف يترجمه LLM، ورابط CTA (`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. يمكن استخدامه أثناء البناء بواسطة إضافة remark (راجع مدونة jls42.org ← إضافة `remark-translation-banner`).
    - **ثوابت لا تُرسل مطلقًا إلى LLM**: يُجمَّع عنوان المستودع ورابط GitHub في جانب Python بعد ترجمة العبارة الوصفية. لا يرى LLM مطلقًا المعرّف المختصر `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تغيير أي عارض أو حالة أحرف أو مخطط.
    - **إدراج مدرك للـ frontmatter**: في وضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة `---` الختامية** للـ frontmatter بصيغة YAML (لضمان أمان Astro Content Collections / gray-matter). تكتشف الدالة المساعدة `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف وتحافظ على سلامته؛ كما **ترفع `RuntimeError`** عند وجود frontmatter مفتوح بلا سياج إغلاق (فيُدرج الملف ضمن `failed_files` بدلًا من كتابته مع ملاحظة في موضع خاطئ).
    - **قائمة سماح لتعقيم اسم النموذج**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع استخدام `unknown` احتياطيًا إن كانت النتيجة فارغة. يتوافق ذلك مع المدقّق في إضافة remark الخاصة بـ Astro ويحيّد المحارف التي قد تكسر تنسيق العلامة (المسافة، علامة الاقتباس، القوس، الفاصلة، وغيرها).
    - **إعادة هيكلة داخلية**: `_append_translation_note` (دالة واحدة ضخمة) ← 7 دوال مساعدة خالصة (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فُصل المُنشئ عن المُركّب (يعيد المُنشئ كتلة خالصة بلا فاصل، ويطبّق المُركّب عناصر `\n\n` وفق الموضع)؛ ويشترك كود الإنتاج والدالة المساعدة المصدرية في المُجمّع نفسه ذي الفقرات الثلاث.
    - **يحافظ `_quote_lines` على الأسطر الفارغة**: يضيف `> ` إلى بداية كل سطر، ويحوّل الأسطر الفارغة إلى `>` فقط. يتيح ذلك لـ mdast رؤية 3 فقرات منفصلة داخل الاقتباس (العنوان / الوصف / الرابط) بدلًا من فقرة واحدة تحتوي على فواصل أسطر.
    - **`_build_translation_note_block` تكيّفي**: وفق عدد الفقرات التي حافظ عليها LLM (3 = تنسيق البطاقة الكامل، 2 = عبارة + رابط، 1 = تنسيق احتياطي). لم يعد التنسيق الاحتياطي ذي الفقرة الواحدة **يغلّف النص بـ `**...**`** عند اكتشاف رابط Markdown من نوع `](` (إذ يكون عرض `<strong>` حول رابط هشًا).
    - **التوافق التصاعدي**: `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` في جانب `_compose_with_notes` — تستمر كائنات Namespace التي لا تحتوي على هاتين السمتين (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) في العمل بلا تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور على مقتطف المصدر حرفيًا) + طبقة احتمالية (`langdetect`)
    - قائمة سماح `finish_reason` / `stop_reason`: رفع `RuntimeError` عند أي حالة خارج قائمة السماح (الاقتطاع، وcontent_filter، وغيرها)
    - `max_tokens` في Claude: `4096` ← `32768` (يتجنب الاقتطاع الكامن في المقاطع ذات 16k، مع هامش للتحويل بين أنظمة الكتابة من FR إلى JA/ZH/KO/AR/HI)
    - تقسيم مدرك للعناوين: إعطاء الأولوية لـ H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي كامل)
    - تمرير الأخطاء حتى رمز خروج غير صفري: يعيد `translate_markdown_file` حالة ذات نوع محدد `success` / `failure` / `skipped`، وينفّذ `main()` الأمر `sys.exit(1)` إذا فشل ملف واحد على الأقل (سواء لملف واحد أو دفعة)
    - حارس للمحتوى الفارغ لدى جميع المزوّدين، وفحص معقولية نسبة المصدر إلى الناتج (≥ 500 محرف، وأقل من 5% = رفض)، والتحقق من عناصر الكود النائبة (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، وإعادة محاولة `BadRequestError` من دون `reasoning_effort`
    - إضافة الاعتمادية `langdetect==1.0.9`
  - **أدوات جودة pre-commit** ("نمط EurekAI المتكامل"، 14 خطافًا):
    - Pre-commit:‏ ruff (فحص+تنسيق)، وshellcheck، وprettier ‏(md/yaml/json)، وdetect-secrets (حماية 4 مفاتيح API)، وLizard ‏(CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، ونهاية الملف، والملفات الكبيرة، وأسطر shebang، وغيرها)
    - Pre-push:‏ mypy (وضع متساهل تدريجي)، وOpengrep SAST ‏(translate.py + scripts/)، وpip-audit (وضع الإبلاغ الأولي)، وunittest discover ‏(tests/ + scripts/tests/)
    - أغلفة محلية ضمن `scripts/` تستخدم `./venv/bin/python`
    - `scripts/audit_verdict.py`: محلّل JSON لـ pip-audit مع 11 اختبار unittest، وهو نقل إلى Python لمحلّل jls42-astro
    - إصلاح 7 مخالفات أولية في ruff:‏ B904 ‏(raise from) ×2، وB007 ‏(مجلدات غير مستخدمة)، وC408 ‏(قيمة dict حرفية)، وC419 ‏(list-comp)، وSIM105 ‏(contextlib.suppress)، وSIM110 ‏(any())
    - يستبعد Lizard مؤقتًا `translate.py` (4 دوال بقيم CCN بين 21 و47، وإعادة الهيكلة مخططة) — بوابة صارمة على scripts/
  - **SonarCloud + تغطية شاملة**:
    - سير عمل GitHub Actions ‏`SonarCloud` ‏(sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع التغطية عبر `coverage.xml`
    - 11 شارة SonarCloud في أعلى README ‏(Quality Gate، وتقييمات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code)
    - `tests/test_silent_failure.py` ‏(`unittest` من المكتبة القياسية): يغطي الحلقات الست في سلسلة أخطاء الفشل الصامت
    - `tests/test_orchestration.py` ‏(+79 اختبارًا): يغطي طبقة التنسيق في `translate.py` ‏(`_resolve_*_filename`، `_existing_translation_exists`، `_record_translation_status`، `_write_output_file`، `translate_directory`، `_validate_input_paths`، `_init_*_client`، `_select_provider_client`، `_normalize_collapsed_markdown`، `_cleanup_source_flag`، `_validate_news_flags_*`، وحالات TypeError + BadRequestError الاحتياطية في `_openai_create_with_fallback`، وتنسيق الموجّه لسلسلة o1، وفروع العودة المبكرة في `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` ‏(stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess
    - **التغطية في الكود الجديد**: 75.5% ← نحو 98% ‏(translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%)
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك اختبارات E2E ‏`marker+top|bottom|both` و`legacy+top|bottom|both`)، وإضافة البادئة متعددة الأسطر، والتوافق السابق بايتًا ببايت (قيمة golden حرفية)، والمعقّم، وتقسيم frontmatter (بما في ذلك رفع خطأ عند عدم إغلاق السياج)، وتنسيق الفقرات الثلاث، والتنسيق الاحتياطي ذي الفقرتين، وحارس الفقرة الواحدة + رابط Markdown، وحاجز أمان حرج `TestLLMPayloadExcludesInvariants` يتحقق من عدم إرسال العنوان+الرابط مطلقًا إلى LLM. **نجح 190 اختبارًا**، بلا أي تراجع.
  - التوثيق: `README.md` ‏(بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` ‏(سير عمل pre-commit + مراقبة مفصلة لـ CI)، وإعادة توليد 28 ترجمة
- **1.8** وضع `--news` + ترقية نماذج 2026 ‏(2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - جودة OpenAI:‏ `gpt-5` ← `gpt-5.4`
    - OpenAI الاقتصادي: `gpt-5-mini` ← `gpt-5.4-mini`
    - جودة Gemini:‏ `gemini-3-pro-preview` ← `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` ‏(400k) و`gemini-3.1-pro-preview` ‏(1M)
  - وضع `--news` الأولي: حماية الاقتباسات الإنجليزية بعناصر نائبة `#NEWSQUOTE\d+#`، وربط `LANG_FLAGS` ‏(15 لغة)، وإدارة الأعلام وفق اللغة الهدف
  - التحقق من العناصر النائبة للأخبار قبل الاستعادة (تراجع: كان حذف LLM للعنصر النائب ينتج بصمت مخرجات بلا اقتباس)
  - جعل السكربت `regen_translations.sh` قابلًا للنقل (مسارات مطلقة، بلا اعتماد على pwd)
  - إضافة رابط اللغة الفرنسية إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة
- **1.7** الميزات الجديدة:
  - الخيار `--keep_filename` للاحتفاظ باسم الملف الأصلي أثناء الترجمة
  - دعم ملف `.env` لتحميل مفاتيح API تلقائيًا
  - **الحفاظ على الكود السطري**: أصبحت علامات backtick ‏(`` `...` ``) محمية أثناء الترجمة
  - تحسين موجّه النظام:
    - تحسين معالجة علامات الاقتباس في YAML frontmatter
    - حماية متغيرات القالب `{variable}`
    - منع ملاحظات المترجم غير المطلوبة
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org)
- **1.6** الميزات الجديدة:
  - دعم Google Gemini API للترجمة (`--use_gemini`)
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI:‏ `gpt-5` ‏(الجودة)، `gpt-5-mini` ‏(الاقتصادي)
    - Claude:‏ `claude-sonnet-4-5` ‏(الجودة)، `claude-haiku-4-5` ‏(الاقتصادي)
    - Gemini:‏ `gemini-3-pro-preview` ‏(الجودة)، `gemini-3-flash-preview` ‏(الاقتصادي)
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة
  - ترجمة ملف واحد (`--file`) من دون استعراض مجلد
  - نمط تسمية جديد ومبسّط: `{base}-{lang}.md`
  - الخيار `--include_model` للاحتفاظ بالتنسيق القديم المتضمن اسم النموذج
  - دعم النماذج غير المدرجة مع حد افتراضي للرموز (128k)
  - ترجمة README إلى 14 لغة
- **1.5** التحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** التحديث من `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** التحديث من `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` والتحديث من `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين موجّهات الترجمة:**
    - أُثريت موجّهات الترجمات المباشرة وملاحظات الترجمة لتحسين الوضوح والكفاءة، بما في ذلك تعليمات مفصلة بشأن الحفاظ على البيانات الوصفية وعناصر التنسيق المحددة.
  - **إعادة هيكلة الكود:**
    - استبدال `MistralClient` بالفئة `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم عمليات الاستيراد لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص ومعالجة كتل الكود للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات الإخراج:**
    - عكس ترتيب النموذج واللغة في أسماء ملفات الإخراج (على سبيل المثال، `f"{base}-{args.target_lang}-{args.model}.md"`)، ما يسهّل تنظيم الترجمات والعثور عليها.
  - **تحسينات متنوعة:**
    - تنظيف الكود بإزالة الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية السكربت وسهولة قراءته.
- **1.4** الميزات الجديدة:
  - دعم Claude API من Anthropic للترجمة
  - تحسين الموجّهات لزيادة الوضوح والكفاءة
  - تعديلات طفيفة لتحسين قابلية صيانة الكود
- **1.3** التحسينات والميزات الجديدة:
  - تحسين معالجة كتل الكود
  - تحسين إدارة ملفات الإخراج
  - تحسين اكتشاف الملفات الموجودة
  - الخيار `--force` لفرض الترجمة
  - عكس ترتيب النموذج واللغة في اسم ملف الإخراج
- **1.2** إصلاح سجل التغييرات
- **1.1** إضافة دعم Mistral AI API
- **1.0** الإصدار الأولي - دعم OpenAI API

**مقال مترجم من الفرنسية إلى العربية باستخدام gpt-5.6-sol.**
