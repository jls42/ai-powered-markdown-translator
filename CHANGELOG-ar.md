### سجل التغييرات

🌍 [الفرنسية](CHANGELOG.md) | [الإنجليزية](CHANGELOG-en.md) | [الإسبانية](CHANGELOG-es.md) | [الصينية](CHANGELOG-zh.md) | [الألمانية](CHANGELOG-de.md) | [اليابانية](CHANGELOG-ja.md) | [الكورية](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [الهندية](CHANGELOG-hi.md) | [الإيطالية](CHANGELOG-it.md) | [الهولندية](CHANGELOG-nl.md) | [البولندية](CHANGELOG-pl.md) | [البرتغالية](CHANGELOG-pt.md) | [الرومانية](CHANGELOG-ro.md) | [السويدية](CHANGELOG-sv.md)

- **1.11.0** النشر على PyPI: `pip install ai-powered-markdown-translator` ثم الأمر `aipmt`، من دون استنساخ المستودع (2026-08-31):

  - **أصبح البرنامج النصي ذي الملف الواحد حزمة قابلة للتثبيت.** انتقل `translate.py` من الجذر إلى `src/aipmt/translate.py`، مع نقطة دخول لوحدة التحكم `aipmt` وما يعادلها `python -m aipmt`. يظل المستودع المستنسخ ضروريًا للمساهمة — إذ توجد فيه الاختبارات والترجمات الـ28 وأدوات الجودة — لكنه لم يعد ضروريًا للاستخدام.

    - **اسم الاستيراد هو `aipmt` وليس `translate` مطلقًا**، لأن التعارض حقيقي وصامت. فحزمة PyPI المسماة `translate` (الإصدار v3.8.1، وآخر رفع في 2026-07-06) تثبّت مجلدًا بالاسم نفسه. أُعيد إنتاج ذلك في بيئة افتراضية: يتغلب المجلد على الوحدة، ويختفي `translate.main`، وتتوقف نقطة الدخول عند `AttributeError` — بينما يجيب `pip check` بعبارة «لم يتم العثور على متطلبات معطوبة» مع rc=0. كان من الممكن أن يؤدي `pip install translate` بسيط لدى المستخدم إلى تعطيل CLI من دون تشخيص قابل للاستغلال. وفي اختبار مضاد على wheel الحقيقي: `pip install translate` فوق الحزمة، و`aipmt --help` يعطي rc=0 قبل التثبيت وبعده، وتتعايش واجهتا CLI.
    - **اسم توزيع طويل وأمر قصير.** يجعل `ai-powered-markdown-translator` الحزمة قابلة للعثور عليها عبر البحث في PyPI؛ أما استخدام اختصار وحده فسيجعلها غير قابلة للعثور عليها لمن لا يعرف المشروع مسبقًا، في حين أن هدف النشر تحديدًا هو أن يتم العثور عليها. استُبعد مرشحان معقولان بعد التحقق: `ai-markdown-translator` محجوز على npm منذ 2024 لأداة تؤدي الغرض نفسه، وتسبق هذا المستودع بـ17 شهرًا؛ و`aimt` يختلف بحرف واحد عن `aim` (v3.29.1)، وهي حزمة نشطة في **المجال نفسه** — أسوأ إعداد لحدوث التباس دائم. ملاحظة منهجية مهمة: يعيد `pypi.org/project/<nom>/` الرمز 200 لأي اسم (صفحة لمكافحة الروبوتات)، ولا يُعتد إلا بواجهة JSON البرمجية.
    - **تخطيط `src/` بدلًا من حزمة مسطحة.** كانت الحزمة المسطحة ستحافظ على اختبارات `sys.path.insert(..., "..")` الستة، وهذا هو العيب تحديدًا: فهي تجعل الاختبارات تستورد شجرة المصدر بدلًا من الحزمة، ما كان سيخفي أي خطأ في التغليف. التكلفة الفعلية: قاعدة استبدال إضافية.

  - **أصبح من الممكن أخيرًا إعداد المفاتيح مرة واحدة وإلى الأبد.** لم تكن لدى CLI المثبتة أي إعدادات **مستمرة**: لم يبقَ سوى متغير البيئة و`.env` للمجلد الحالي. صحيح أن `find_dotenv` كان يصعد حتى جذر النظام، ولذلك كان يعثر على `~/.env` **عند العمل داخل مجلد المستخدم**، لكنه لم يكن يعثر على شيء عند العمل في مكان آخر — تغطية تعتمد على مكان إطلاق الأمر، لا على قرار تصميمي. لذلك أُضيفت طبقة ثالثة: `~/.config/aipmt/.env`، أسفل الطبقتين الموجودتين.

    - **الأولوية غير مشفّرة صراحةً**، بل تنشأ من `override=False`، وهي القيمة الافتراضية لـ`load_dotenv`: كل طبقة لا تملأ إلا ما تركته الطبقة السابقة فارغًا. ومن هنا يأتي الترتيب: متغير البيئة ← `.env` الخاص بالمشروع ← إعدادات المستخدم، وقد جرى التحقق منه باختبارات **السلوك** لا باختبارات البنية — إذ يؤدي عكس ترتيب الاستدعاءين إلى فشلها، وكذلك إزالة الطبقة الثالثة.
    - **التنسيق هو `.env` وليس TOML**، عن قصد: فـ`python-dotenv` اعتماد موجود أصلًا، وبنيته موثقة مسبقًا في ملفات README الـ15، ويُستخدم الملف نفسه للنطاقين. لا اعتماد جديد ولا بنية جديدة. يتبع الموقع `XDG_CONFIG_HOME` إذا كان **مطلقًا** — إذ تطلب المواصفة تجاهل قيمة نسبية، وإلا فسيعود موقع الإعدادات مرتبطًا بالمجلد الحالي — و`APPDATA` على Windows.
    - **استُبعد خياران، مع توضيح السبب.** تكون حلقة مفاتيح النظام (`keyring`) أكثر أمانًا على جهاز مكتبي، لكنها تفشل في الوضع غير التفاعلي — خادم أو حاوية أو CI — أي في حالة الاستخدام نفسها للترجمة الدفعية؛ لذا فهي مرشح جيد للاستخدام الاختياري، لكنها افتراضي سيئ. أما الخيار `--api-key` فسيضع المفتاح في سجل shell ويجعله ظاهرًا في `ps`.
    - **عند غياب المفتاح، لم يعد هناك أثر لاستدعاء الدالة.** كان المستخدم يتلقى مكدس Python يشير إلى `site-packages`، ورسالة تذكر «البيئة أو .env» من دون أن توضح أين يُنشئ الخيار الثاني. أما الآن فتسرد المواقع الثلاثة مع مسار كل منها بدقة، ويخرج الأمر بالرمز 2. والشبكة **ضيقة عمدًا**: يلتقط `except ValueError` مرحلة الإعداد وحدها. أما تغليف التنفيذ كله فسيحوّل خطأً حقيقيًا يحدث أثناء الترجمة إلى رسالة مطمئنة — وهو نمط الفشل الذي يتعقبه هذا المستودع. ويقرأ أحد الاختبارات مصدر `main()` لمنع ذلك.

  - **إصلاح — كان `.env` الخاص بالمستخدم يُتجاهل بعد تثبيت الأداة.** لا يصعد `load_dotenv()` من دون وسيط من المجلد الحالي، بل من ملف **المستدعي**، أي من `site-packages`. وقد قيس ذلك باستخدام نقطة دخول حقيقية لوحدة التحكم، أُطلقت من مشروع يملك `.env` الخاص به: يعيد `find_dotenv()` القيمة `''` ولا يُحمّل المفتاح، بينما يعثر عليه `find_dotenv(usecwd=True)`. لم يكن الخلل موجودًا ما دامت الأداة تعمل من المستودع المستنسخ فقط؛ لكن النشر كان سيجعله منهجيًا، ولا يكون عَرَضه الوحيد سوى ظهور مفتاح API «مفقود» رغم صحة الإعداد.

  - **كانت ثلاثة اختبارات بوابة ستمر بنجاح بعد أن توقفت عن التحقق من أي شيء.** جرى تشديدها **قبل** النقل، عمدًا: فالضمان المكتوب بعد التغيير الذي يفترض أن يكتشفه لا يثبت شيئًا. ينجح كل منها على المستودع الأصلي ويفشل على نسخة منقولة — وقد قيس الاتجاهان.

    - **يتجاهل Lizard مسارًا غير موجود من دون قول شيء**: rc=0، و«تم تحليل 0 ملف». كانت بوابة التعقيد ستنتقل من 158 دالة / 2247 nloc إلى 3 دوال / 34 nloc، مع خرج حجمه صفر بايت. وأصبح النطاق الآن مصفوفة يُتحقق من وجود كل عنصر فيها.
    - **لا يفشل `coverage run --source=` عند غياب وحدة**: تحذير على stderr فقط، وrc=0 لكل من unittest و`coverage xml`، مع نشر التقرير رغم ذلك — مبتورًا من 1453 إلى 141 تعليمة. كان المشروع سيبدو سليمًا لأنه لم يعد يُحلَّل تقريبًا. وتحافظ عتبتان دنيا على التقرير: الإجمالي، وأكبر ملف جرى قياسه.
    - **مسبار حداثة الترجمات أعمى بنيويًا عن شكل الاستدعاء**: فهو يرتكز على أعلام argparse، وهي بالضبط الأشياء التي لا يغيرها إعادة تسمية الملف. أُعيد إنتاج الحالة: نُقلت الوحدة، وظلت ملفات README الـ15 توثق أمرًا غير موجود، فكانت النتيجة «لا توجد ترجمة قديمة». لذلك يتحقق قسم سابع من **الشكل** لا من الخيارات، وتُقارن وصلة Lizard بنطاق البرنامج النصي الفعلي — إذ إن مفتاحها `files:` لا يجعل pre-commit يفشل عندما يتوقف عن المطابقة، بل يجعله **يتخطاها**.

  - **لم يعد `requires-python = ">=3.10"` مجرد ادعاء.** كان `sonar-project.properties` يعلن مسبقًا دعم 3.10-3.12 من دون أن يختبره شيء على الإطلاق، لأن بيئة التطوير لا تحتوي إلا على 3.12 — وهو تناقض داخلي كان النشر سيجعله علنيًا. وأصبح workflow للاختبارات ينفذ المجموعة على 3.10 و3.11 و3.12، مع تثبيت **الحزمة** وبالتالي حدودها العامة.

  - **حدود دنيا بلا سقف أعلى.** يظل `requirements.txt` هو القفل المختبَر، بينما يصبح `[project.dependencies]` هو العقد العام: فنشر إصدارات القفل الدقيقة سيسبب تعارضات لدى كل مستخدم لديه حزم أخرى. ولا يوجد سقف `<N+1` أيضًا — إذ سيتعارض مباشرةً مع `check-deps-fresh.sh`، الذي يجعل بوابة الإصدار تفشل عند أي تأخر في الإصدار الرئيسي. تحل مجموعة الحدود الدنيا المشكلة، ويخرج الاختبار المضاد `openai==1.0.0` بالرمز `ResolutionImpossible`، ما يثبت أن الفحص يميز الحالات بدل قبول كل شيء. كما يمنع حارس آخر إصدار `pyproject.toml` من الاختلاف عن إصدار سجل التغييرات: فلا يسمح PyPI بإعادة استخدام رقم.

  - **تم التحقق من البداية إلى النهاية في بيئة افتراضية جديدة**: wheel حجمه 69,768 بايت ولا يحتوي إلا على `aipmt/*.py` وdist-info والترخيص؛ و`aipmt --help` يخرج rc=0 مع 22 علمًا؛ و`python -m aipmt` يعرض «usage: aipmt» لا «usage: \_\_main\_\_.py»؛ وتثبيت `pipx` يعمل؛ والأهم **ترجمة فعلية من fr إلى en من أي مجلد مستخدم**، مع الحفاظ على الخط العريض والقائمة والشفرة المضمنة والرابط وURL، ومن دون ترجمة كتلة الشفرة. نجحت الاختبارات الـ318 بقائمة معرفات مطابقة حرفيًا للبايت قبل النقل وبعده — وهذا هو ما يثبت أن أي اختبار لم يُعطَّل، وليس عبارة «OK».

- **1.10.0** موفر `--use_codex` (حصة اشتراك ChatGPT)، تحديث SDK والنماذج، وإصلاح اقتباسات الأخبار متعددة الفقرات (2026-08-29):

  - **مراجعة أمنية — ضمانان وضعتهما PR من دون تطبيقهما في كل المواضع**:

    - **كان preflight الخاص بـ Codex يمرر كامل `.env` إلى البرنامج الثنائي.** كان `_codex_preflight` يستدعي `subprocess.run` **من دون `env=`**: فكانت العملية الفرعية ترث كامل `os.environ`، أي كامل `.env` الذي حمّله `load_dotenv`. وباستخدام برنامج ثنائي زائف مُجهّز للقياس، وصلت **سبعة أسرار** إلى preflight — مفاتيح الموفّرين الستة إضافة إلى `GITHUB_TOKEN` — مقابل **صفر** لنظيره `_grok_preflight`، الذي كان يمرر `env=_grok_env()` كما ينبغي. كان التناقض داخليًا في PR: إذ إن `_strip_secret_env` موجود تحديدًا للحفاظ على هذا الثابت، وعلى بضع سطور فقط. واستُخرج `_codex_env_base()` وأصبح مشتركًا بين المسارين؛ والقياس بعد الإصلاح: صفر أسرار في الجانبين.
    - **خاصية «`--deny` يفشل بطريقة آمنة» لم تكن تغطي الصيغة المستخدمة.** كان التعليق يبرر العزل الكامل لـGrok بحقيقة أن قاعدة ذات بادئة غير معروفة ترفض بدء التشغيل. لكن القياس على `grok 1.0.13` أظهر أن هذا التحقق موجود **للصيغة بين قوسين فقط**: يرفض `--deny 'CeciNestPasUnOutil(*)'` بدء التشغيل («بادئة أداة غير معروفة»)، بينما يُقبل `--deny 'CeciNestPasUnOutil'` بصمت. في حين أن `GROK_DENY_RULES` لم يستخدم إلا أسماء مجردة — وبالتالي كان من الممكن لإعادة تسمية أداة من جانب xAI أن تزيل طبقة العزل الوحيدة التي جرى قياسها، من دون أي إشارة، على جهاز لا ينطبق فيه عزل نظام التشغيل أصلًا. تمر القواعد الثماني المسماة الآن بصيغة `Prefix(*)`، مع التحقق من أن كل واحدة منها بادئة معروفة لدى CLI؛ ويظل catch-all `*` بصيغته الحرفية، وهي الصيغة الوحيدة المقبولة. ويمنع اختبار العودة إلى الصيغة غير المتحقق منها.
    - **تم التحقق من سلامة الجوانب الأخرى**: لا توجد حقن أوامر (صيغة قائمة في كل مكان، ولا `shell=True` مطلقًا، ومحتوى المستند عبر stdin أو `--prompt-file`)، ولا إلغاء تسلسل غير آمن (`json.loads` فقط، مع حارس نوع)، وإصلاح اجتياز المسار من دون العثور على تجاوز عبر سبع حمولات، كما أن `--deny '*'` مطبق فعلًا من قِبل CLI (`DENY_ENFORCED` لوحظ عند قراءة خارج workdir).
    - كان فحص حداثة الحزم المضاف أعلاه يتحايل بدوره على مبدئه: إذ كان يتخطى بصمت حزمةً فشل طلبها إلى PyPI، فتظل البوابة ناجحة. وهو يحصي الآن الحزم التي قورنت فعليًا، ويفشل إذا كانت التغطية غير مكتملة.

  - **تحديث التبعيات، مع شبكتي أمان حتى لا يتكرر التأخر**:

    - **كان التأخر حقيقيًا ومستمرًا**: `openai` من 2.54 إلى **3.6.0**، و`anthropic` من 0.125 إلى **1.2.0**، و`certifi` من 2024.8.30 إلى **2026.7.22** — أي تأخر عامين في مخزن شهادات الجذر الذي يتحقق من TLS لجميع استدعاءات الموفّرين. والسبب محدد: **لم يكن هناك `.github/dependabot.yml`**. فمن دون هذا الملف لا يفعّل GitHub إلا _security updates_، ولا يقترح Dependabot طلب PR إلا لتبعية تستهدفها CVE — وهو ما يفسر تحديثه لـ`urllib3` و`idna` مع ترك SDKَين يتأخران عن إصدار رئيسي.
    - **تتعايش الإصداران الرئيسيان من دون تعارض**، خلافًا لما أوحى به استدلال سابق: ينتقل `openai` 3.x و`anthropic` 1.x إلى **`httpx2`**، بينما يبقى `mistralai` و`google-genai` على `httpx<1`، لكنهما توزيعتان منفصلتان. تم التحقق بالتثبيت الفعلي، ثم عبر **مسارات الموفّرين السبعة المختبرة من البداية إلى النهاية** — OpenAI وClaude وMistral وGemini وGrok API وCodex CLI وGrok CLI — مع الحفاظ على الشفرة المضمنة والروابط في كل مخرج. كان «تجنب مكدسين HTTP» تفضيلًا لا عائقًا: وقد حسم القياس الأمر.
    - **لم يكن `requirements.txt` يصف البيئة الفعلية**: فقد كان `google-auth` و`cryptography` ومكدس `opentelemetry` مثبتة في بيئة العمل الافتراضية من دون التصريح بها — ولذلك لم يكن التثبيت الجديد يعيد إنتاج ما جرى اختباره. وعلى العكس، كانت `tokenizers` و`huggingface-hub` و`PyYAML` موجودة فيها من دون استيرادها أو طلبها من أي شيء، وهي بقايا `mistralai` 1.x. أُعيد توليد الملف بوصفه الإغلاق الكامل لبيئة افتراضية مبنية من التبعيات المباشرة وحدها. ولا يعثر `pip-audit` على أي ثغرة معروفة في المجموعة الجديدة.
    - **`.github/dependabot.yml`** (جديد) يفعّل تحديثات الإصدارات الأسبوعية وpip وgithub-actions. تُجمع التحديثات الثانوية والتصحيحية في PR واحد — إذ ينتهي الأمر بتجاهل طلب PR لكل تحديث تصحيحي، والضوضاء عدو التحديث؛ أما **الإصدارات الرئيسية فتُفصل**، ويتطلب كل منها تحققًا عبر استدعاء فعلي.
    - **`scripts/check-deps-fresh.sh`** (جديد، موصول بالبوابة) يجعل التأخر ظاهرًا في حكم المشروع: يقترح Dependabot التحديثات لكنه لا يضمنها، وقد تتراكم طلبات PR الخاصة به. تأخر إصدار رئيسي → فشل؛ وتأخر إصدار ثانوي → تحذير، لأن البوابة الحمراء دائمًا ينتهي بها الأمر إلى التجاهل؛ وتعذر الوصول إلى PyPI → تخطٍ صريح محليًا و**فشل آمن في CI**، إذ إن الفحص غير المنفذ ليس نجاحًا. تم التحقق في الاتجاهين: فهو يلتقط الحالة نفسها الموجودة قبل الإصلاح (`openai 2.54.0→3.6.0` و`certifi 2024.8.30→2026.7.22`)، ويكتفي بالتحذير عند وجود تأخر ثانوي.

  - **إصلاحات ناتجة عن مراجعة PR هذه** — دقق خمسة وكلاء مراجعة في الفرق؛ وقد **أُعيد إنتاج جميع النقاط الواردة أدناه بالقياس** قبل إصلاحها، وكان اثنان منها تراجعين أُدخلا في وقت سابق ضمن الإصدار نفسه.
- **تم إصلاح التراجع — كان لدى `_NEWS_CITATION_REGEX` تراجع أُسّي.** أدى الإصلاح متعدد الفقرات إلى إدخال `(?:[ \t]*$|[ \t]+.*)` في التكرار: مشاركة المسافات بين `[ \t]+` و`.*` ملتبسة، وهذا الالتباس يتضاعف من تكرار إلى آخر. قيس ذلك على أسطر `>   texte` — وهي مسافة بادئة Markdown قانونية تمامًا — لا تطابق النمط: **2 589 ms لـ14 سطرًا** مقابل 0.04 ms بعد الإصلاح، مع عامل يقارب 9 لكل سطر مضاف. في وضع `--news`، كان يكفي اقتباس كتلي طويل وغير مطابق لتجميد الترجمة حتى انتهاء مهلة المهمة، من دون سبب قابل للتحديد. يستهلك التكرار الآن السطر بأكمله دفعة واحدة (`\n^>(?![ \t]*—).*`)، فلا يبقى سوى طريقة واحدة للمطابقة في كل تكرار. جرى التحقق على المجموعة الفعلية المكوّنة من 231 مقالة: **لا اختلافات** في الالتقاط، والاقتباسات الـ423 نفسها، مع استمرار توسيع الأجسام متعددة الفقرات الـ14.
    - **كان تفعيل علامتي provider معًا يؤدي إلى الفوترة حسب الاستخدام بصمت.** كان `--use_codex --use_mistral` مقبولًا؛ يفحص `_select_provider_client` Mistral أولًا، ويعطي `_resolve_provider` الأولوية للقيم المنطقية الصريحة — وكان كلاهما ينتهي إلى Mistral. لذلك كان المستخدم يطلب من حصة اشتراكه ويحصل على فوترة حسب الاستخدام، من دون أي تحذير: وهذا بالضبط نمط الفشل الذي وُجد `--use_codex` لمنعه. تمر علامات provider الست الآن عبر `add_mutually_exclusive_group`. **تغيير سلوكي**: إن سطر الأوامر الذي يجمع providerين، وكان مقبولًا بصمت حتى الآن، يفشل الآن عند `argument --use_mistral: not allowed with argument --use_codex`.
    - **كانت بوابة نهاية العمل تتحول إلى اللون الأخضر عندما تتعطل مجساتها.** كانت أربع من أصل ثلاثة عشر عملية تحقق في `scripts/check-release-ready.sh` تتبع نمط «التقاط stdout، والاستنتاج إذا كان فارغًا» من دون فحص رمز الإرجاع: إذ كان استثناء (إعادة تسمية ملف، `FileNotFoundError`) يكتب إلى stderr، ويترك stdout فارغًا، فتستنتج عملية التحقق «لا شيء للإبلاغ». لقد أُعيد إنتاج فخ «إن `exit 0` لا يثبت شيئًا» داخل البرنامج النصي المخصص لمنعه. يفرض مساعد `probe()` الآن رمز إرجاع يساوي صفرًا **وعلامة نهاية**، وترفض المجسات الاستنتاج من مجموعة علامات فارغة — إذ إن أي تأكيد على مجموعة فارغة يكون صحيحًا دائمًا. البرهان: أدى إضافة المجموعة الحصرية أعلاه إلى تمرير علامات provider عبر كائن `*_group`، لم يعد التعبير النمطي القديم `parser\.add_argument\(` يطابقه؛ وكانت **ست علامات من أصل إحدى وعشرين** تخرج بصمت من النطاق، بينما تظل البوابة خضراء.
    - **كان فحص الأسرار يفوّت أربعة provider من أصل ستة.** تستبعد الفئة `[A-Za-z0-9]` الشرطة: إذ كان `sk-proj-…` (تنسيق OpenAI الحالي) و`sk-ant-api03-…` يفشلان عند شرطتهما الثانية، كما لم يكن `AIza…` مشمولًا. وُسّع النمط، واستُبعد `.secrets.baseline` من الفحص. كذلك كان الحاجز `.env` يستعلم عن `git diff --cached`، الذي لا يرى إلا الفهرس: لذلك لم يكن `.env` **الذي سبق إيداعه** — وهو أسوأ احتمال — يظهر فيه إطلاقًا. وهو يستعلم الآن عن `git ls-files`.
    - **لم يكن «الإحماء المسبق للرمز» في Codex كذلك فعليًا.** أظهرت القياسات أن `codex login status` لا يلمس `~/.codex/auth.json` (لم يتغير mtime ولا الحجم)، كما أن مساعدته تقول «إظهار حالة تسجيل الدخول». ومع ذلك كان التعليق يزعم أنه يحدّث الرمز «مرة واحدة، بالتسلسل»، وبالتالي يعطّل خطر التحديثات المتزامنة لرمز دوّار ذي استخدام واحد. لم تكن الحماية المعلنة موجودة؛ ويصف التعليق الآن ما يفعله الكود، بينما تظل المعالجة الحقيقية هي `max_jobs=4`. كما أن الفحص يحترم `CODEX_BIN`، الذي كان يتجاهله — إذ كان جهاز لا يحتوي على `codex` في `PATH` يفشل برسالة «غير موثّق»، وهي رسالة تشخيصية مضللة.
    - **كان `.env` يُحمَّل مصدره داخل shell فرعي.** يُستدعى `detect_provider` ضمن استبدال أمر، ولذلك لم تكن صادراته تعود إلى shell المستدعي: فكان `GROK_BIN` أو `GROK_HOME` أو `REGEN_MODEL` المعرّف في `.env` يظل غير مرئي للقراءات المنفذة في `main()`، التي تستنتج «تعذّر العثور على Grok binary» رغم صحة الإعداد.
    - **كانت التزامنية تتجاوز الحد المعلن بنسبة 50%.** وُضع الحاجز بعد إطلاق زوج README/CHANGELOG: وبلغت الذروة المقاسة **3 لـ `max_jobs=2`**. وبالنسبة إلى Grok، الذي تُشارك حصته الأسبوعية مع Chat/Imagine/Voice ولا يمكن قياسها، لم يكن الحد الذي يفرضه البرنامج النصي محفوظًا. أما العد النهائي فكان يُعرض من دون مقارنته أبدًا بالعدد 28 — فكان ملف مفقود يمر من دون ملاحظة.
    - **عقد مخرجات Grok: غياب `stopReason` أصبح فشلًا.** كان الكود يطبق «`end_turn` **أو غائب**» حيث يشترط العقد المعلن `end_turn`. وكان payload بلا الحقل — أو الذي تعيد تحديث CLI تسمية حقله — يحوّل الحاجز إلى no-op صامت. إضافة إلى ذلك، لم يعد `max_turn_requests` مصنفًا كـ rate limit (فالذي نفد هو ميزانية الجولات: إعادة المحاولة تعيد النتيجة نفسها، مقابل انتظار 90 ثانية)، كما يخرج `quota` من علامات rate limit — للسبب الذي كان docstring الخاص بـ `_codex_is_rate_limited` يذكره أصلًا من دون أن يطبقه Grok.
    - **أصبحت سلسلة Gemini محفوظة مؤقتًا لكل نموذج.** كانت تعود إلى `minimal` عند كل مقطع، مع أن النموذج الافتراضي يرفضه: وكان المسار الاعتيادي يدفع تكلفة ذهاب وإياب 400 لكل مقطع ويعيد طباعة التحذير نفسه. ويتوقف التحذير المتكرر مئات المرات عن أن يكون مقروءًا — وهكذا يتحول إلى قناع.
    - **متفرقات**: كانت رسالة الرفض في CI مبرمجة بثبات لـ Codex، وتوجّه مستخدم `--use_grok_cli` إلى `OPENAI_API_KEY` بدلًا من `XAI_API_KEY`؛ وكان `provider.capitalize()` يعرض «Grok_cli» و«Openai»؛ كما كان تعليق طبقة العمليات الفرعية يعمم «shim» على CLIين، مع أن Grok binary هو ELF أصلي (والتبرير الصحيح هو «agent يطلق عملياته الفرعية الخاصة»)؛ وتم وضع علامة `# nosec` / `# nosemgrep` على اثني عشر finding من SAST في `subprocess` مع تبرير، إذ إن صيغة القائمة من دون `shell=True` تجعل الحقن مستحيلًا، ومحتوى المستند لا يمر مطلقًا عبر argv.
    - **لم يعد أي سر يدخل إلى العمليات الفرعية الخاصة بـ agent.** لم تكن قوائم المنع الاسمية تحمي إلا ثابت **الفوترة** (Codex من دون `OPENAI_API_KEY`، وGrok من دون `XAI_API_KEY`). أظهرت القياسات أن **سبعة أسرار أخرى** كانت تدخل رغم ذلك إلى كل عملية فرعية — مفاتيح Anthropic وMistral وGoogle وGemini، ومفتاح CLI الآخر، و`OPENAI_BASE_URL`، وهو ليس سرًا لكنه يعيد توجيه حركة المرور. وهذان الـCLI هما **agent**، كما أن Grok يعمل من دون sandbox لنظام التشغيل يمكن تطبيقه على كثير من أجهزة Linux. تتم التصفية الآن **حسب نمط الاسم** (`API_KEY`، `_TOKEN`، `SECRET`، `PASSWORD`، `CREDENTIALS`) بدلًا من القائمة الاسمية، ما يغطي أيضًا المتغيرات التي يضيفها المستخدم إلى `.env` من دون أن يعرفها هذا الكود. ولا يلزم أي منها للـCLI: فالمصادقة موجودة في `~/.codex` و`~/.grok`، وليست في البيئة مطلقًا — وقد تم التحقق من ذلك عبر **ترجمة فعلية مكتملة** باستخدام كل من providerين مع البيئة المقوّاة.
    - **الاختبارات**: ملف جديد `tests/test_review_hardening.py` (21 اختبارًا) يثبت حصرية علامات provider، وعقد `stopReason`، وخطية regex الخاصة بـ news، ورسالة الرفض في CI، وحفظ Gemini مؤقتًا، وغياب أي سر من بيئة العمليات الفرعية. هذا التأكيد الأخير **عام** — ويفشل عند وجود مفتاح لا تسميه أي قائمة — في حين كان اختبار التنقيح الموجود مرآة لثابته، ولم يكن قادرًا على اكتشاف شيء سوى عطل حلقته الخاصة. أصبحت المجموعة الكاملة **311 اختبارًا**.

  - **موفّران جديدان لـ Grok**: `--use_grok` (واجهة xAI البرمجية، المفتاح `XAI_API_KEY`، مفوتر حسب الاستخدام) و`--use_grok_cli` (CLI الرسمي لـ Grok Build، يُخصم من اشتراك Grok — بالمبدأ نفسه مثل `--use_codex`).
    - **وضع API، نحو 40 سطرًا**: بما أن نقطة نهاية xAI متوافقة مع OpenAI، يُعاد استخدام العميل و`_call_openai` كما هما، ولا يتغير سوى `base_url`. ولم يلزم سوى تكييف واحد يستفيد منه الجميع: يقبل `finish_reason` الآن `end_turn`، وهي الصيغة التي تصدرها xAI، بينما تصدر OpenAI `stop`. النماذج: `grok-4.6` (الجودة) و`grok-4.3` (الاقتصادي). تجدر الإشارة إلى أن الإصدار الاقتصادي من Grok يظل الأغلى في المستودع — $1.25/$2.50 لكل مليون مقابل $0.15/$0.60 لـ `mistral-small-latest`؛ لذا يُختار هذا provider لتنوع النموذج، لا للسعر.
    - **وضع CLI**: منسوخ من Codex، مع أربعة اختلافات فرضها الواقع — يُمرر prompt عبر ملف (`--prompt-file`، إذ لا يقرأ CLI من stdin، كما أن مقطعًا في argv سيظهر في `ps`)، والمخرج عبارة عن كائن JSON واحد على stdout (لا JSONL ولا ملف `-o`)، ولا يعرض الاشتراك سوى `grok-4.6` و`grok-4.5`، ولا يمكن تطبيق sandbox (انظر أدناه). جرى فصل إطلاق العمليات الفرعية مع Codex في `_codex_run_process`، من دون المساس ببقية provider الخاص بـ Codex، الذي تم اختباره مسبقًا.
    - **لا يثبت `exit 0` شيئًا، وفق القياس**: عند عدم المصادقة، يكتب CLI `{"type":"error","message":"Not signed in."}` على **stdout** مع رمز إرجاع **0**. ويتصرف الرفض أو تجاوز الجولات بالطريقة نفسها. لذلك يشترط عقد المخرجات أربعة شروط متزامنة: رمز إرجاع 0، وغياب payload خطأ، و`stopReason == end_turn`، ونص غير فارغ. ويتبع preflight المنطق نفسه: يخرج `grok models` بالرمز 0 حتى عند انقطاع الاتصال، ولا يمكن الاستنتاج إلا من وجود «not authenticated» في stdout.
    - **الاحتواء: عدم تماثل مقصود وموثّق.** في حين يعمل Codex داخل `--sandbox read-only`، لا يمكن تطبيق sandbox الخاص بـ Grok على كثير من أجهزة Linux الحديثة، لسببين مستقلين على مستوى النظام ولا يمكن تجاوزهما من دون `sudo`: إذ يحظر AppArmor مساحات أسماء المستخدمين غير المميّزة منذ Ubuntu 24.04 (`bwrap: setting up uid map: Permission denied`، وقد أُعيد إنتاج ذلك خارج Grok)، كما تفشل قائمة منع مقابس تشغيل الحاويات عندما يكون `/run/podman` في `0700` (لا يعالج resolver سوى `ErrorKind::NotFound`، بينما يصبح EACCES قاتلًا). الفخ المركزي: إن ملفًا **مدمجًا** لا يمكن تطبيقه **يبدأ بلا احتواء بصمت**. لذلك لا يطلب البرنامج النصي أي ملف افتراضيًا ولا يعود بصمت أبدًا — بل يحذّر على stderr. وتعتمد الحماية على قواعد `--deny` الخاصة بـ CLI، بما فيها catch-all `*`، وهي الطبقة الوحيدة المقاسة ذات السلوك _fail-closed_ (إذ تؤدي قاعدة ذات بادئة غير معروفة إلى رفض بدء التشغيل). ويتيح `GROK_TRANSLATE_SANDBOX=read-only` فرض ذلك، وعندها يفشل بدء التشغيل إذا تعذر على الجهاز احترامه.
    - **ضمانات الحماية**: تُزال `XAI_API_KEY` و`GROK_API_KEY` و`GROK_SANDBOX` من بيئة العملية الفرعية (إذ قد يحوّل أحد المفاتيح الفوترة إلى فوترة حسب الاستخدام؛ كما أن `GROK_SANDBOX` موروثًا سيفرض ملفًا غير قابل للتطبيق مع رسالة مضللة)، وتُعطّل مفاتيح MCP/hooks/skills/agents، و`--disable-web-search`، و`--no-subagents`، و`--no-plan`، مع workdir مؤقت، ورفض في CI، ومهلة تقتل مجموعة العمليات، وback-off عند rate limit. يُضبط `--max-turns` على 6 لا على 1: إذ يزداد العداد بعد جولة الأدوات، والقيمة 1 كانت ستقتطع المخرج.
    - **الحصة**: إن pool الخاص بـ Grok أسبوعي و**مشترك مع Chat وImagine وVoice**، ولا تكشفه أي أداة — بخلاف Codex، الذي يتيح `account/rateLimits/read` حساب الاستهلاك. لذلك يحد `regen_translations.sh` التزامن إلى 2 ويحذّر صراحة.
    - **الاختبارات**: ملف جديد `tests/test_grok_provider.py` (24 اختبارًا). أصبحت المجموعة الكاملة **290 اختبارًا**.
  - **إصلاح خطأ — كانت الاقتباسات الإنجليزية متعددة الفقرات محمية جزئيًا فقط (وضع `--news`)**: لم يكن `_NEWS_CITATION_REGEX` يقبل كجسم للاقتباس إلا سلسلة من أسطر `>` **المتتالية**. وعندما كان الاقتباس يغطي عدة فقرات (تفصل بينها سطر `>` فارغ)، كان يُلتقط آخرها فقط ويُستبدل بـ placeholder؛ أما السابقة فكانت تذهب إلى LLM وتعود مترجمة — وهو النقيض تمامًا لما وُجد `--news` لضمانه. يقبل التكرار الآن أسطر `>` الفارغة الداخلية، ويصبح غير جشع، ليتوقف عند `>` الفارغ الذي يسبق السطر المائل بدلًا من أول سطر يواجهه.
    - **الحجم المقاس** على مجموعة فعلية من 198 مقالة: تأثرت 11 اقتباسًا من أصل 419. لا تراجع — تلتقط regex الجديدة العدد نفسه تمامًا من الاقتباسات، ولا تمتد إلا الأجسام متعددة الفقرات (408 أجسام متطابقة و11 جسمًا ممتدًا)، ولا يزال سطر الإسناد `> — …` غير قابل للامتصاص داخل الجسم (مع الإبقاء على lookahead).
    - **إثبات من البداية إلى النهاية** على مقالة حجمها 69 كيلوبايت تُرجمت إلى ja/ar: كانت الفقرة الأولى من اقتباس، التي كانت تُعرض سابقًا بصيغة `> GLM-5.3がオープンウェイト化。` باليابانية وتُترجم بالطريقة نفسها إلى العربية، تبقى الآن بصيغة `> GLM-5.3 is now open-weight.`. وعاد عدد أسطر الاقتباس بالإنجليزية من 9 إلى 10، أي إلى عدد أسطر المصدر.
    - تجدر الإشارة إلى أن هذا الخلل لم تكتشفه أدوات التحقق اللاحقة، إذ تتحقق من وجود الاقتباس من دون التحقق من اكتماله.
  - **توفير مقاس على provider الافتراضي**: كان `_openai_extra_kwargs` يرسل `reasoning_effort="medium"` بمجرد أن يبدأ النموذج بـ `gpt-5`، بما في ذلك في `--eco`. أُجري القياس على `gpt-5.4-mini` لترجمة جملة من عشر كلمات: `medium` → 45 رمز reasoning و65 رمز مخرج؛ `none` → 0 و14. لا يضيف reasoning شيئًا إلى الترجمة، وكان يُدفع ثمنه في كل مقطع من كل ملف. أصبحت القيمة الافتراضية `none` في `--eco` وتظل `medium` خلاف ذلك؛ وتظل القيمة الصريحة الممررة عبر CLI ذات الأولوية. يقبل `--reasoning_effort` الآن `none` و`xhigh` إضافة إلى `low`/`medium`/`high` (ولا تقبلها جميع النماذج: فمثلًا يرفض `minimal` القيمة `gpt-5.4-mini` — وتغطي إعادة المحاولة الموجودة من دون المعلمة هذه الحالة).
  - **تحديث SDK وترحيل Gemini**: استُبدل `google-generativeai` (انتهى دعمه في 2025-11-30، وأُرشف المستودع) بـ SDK الموحد **`google-genai`** — `genai.Client(api_key=...)` ثم `client.models.generate_content(model=, contents=, config=)`، مع تمرير prompt النظام في `system_instruction` بدلًا من دمجه مع المقطع. ينتقل `mistralai` إلى **2.9.4** (يصبح الاستيراد `from mistralai.client import Mistral`؛ بينما يرفع القديم `ImportError`، وقد تم التحقق من ذلك داخل wheel)، و`anthropic` إلى **0.125.0**، و`openai` إلى **2.54.0** — وهي الإصدارات الأخيرة قبل الانتقال إلى `httpx2`، لتجنب التعايش بين مكدسي HTTP داخل venv. وبناءً على ذلك تم فتح `httpx` 0.28.1 و`pydantic` 2.13.5.
  - **اكتُشف تراجعان باختبارات فعلية، لا بالوثائق**:
    - يرفض `anthropic` ≥ 1.0 من جهة العميل استدعاءً غير متدفق يتوقع فيه `max_tokens` مدة تتجاوز 10 دقائق (`ValueError: Streaming is required...`). لم تكن هذه الحماية موجودة في 0.34.2، وكانت تعطل كل استدعاء Claude مع `max_tokens=32768`. أُصلح ذلك عبر `timeout` صريح (`CLAUDE_TIMEOUT`، ومدته الافتراضية 900 ثانية)، ما يتجنب التحول إلى streaming لاستدعاء لا نستخدم منه إلا الاستجابة الكاملة.
    - لا يقبل `thinking_level="minimal"` إلا جزءًا من كتالوج Gemini: يدعمه `gemini-3.1-flash-lite`، بينما يرفضه `gemini-3.7-flash` و`gemini-3.1-pro-preview` برمز 400. ومن هنا جاء `_gemini_generate_with_fallback`، وهو تسلسل `minimal` → `low` → لا thinking_config، على غرار fallback الخاص بـ OpenAI الموجود مسبقًا — إذ يجب ألا تؤدي معلمة تحسين إلى إفشال ترجمة أبدًا.
  - **تجديد النماذج الافتراضية**، مع التحقق من كل نموذج عبر استدعاء فعلي: OpenAI `gpt-5.5` → **`gpt-5.6-terra`** (−60% على دفعة من 28) و`gpt-5.4-mini` → **`gpt-5.6-luna`** (−73%)؛ Claude `claude-sonnet-4-6` → **`claude-sonnet-5`** (أرخص وأحدث) و`claude-haiku-4-5-20251001` → **`claude-haiku-4-5`** (المعرّف الأساسي من دون تاريخ)؛ Gemini `gemini-3.1-pro-preview` → **`gemini-3.7-flash`** و`gemini-3.1-flash-lite-preview` → **`gemini-3.1-flash-lite`** (إصدار مستقر، وأرخص من `3.5-flash-lite`).
بقي Mistral دون تغيير، ولا يزال `mistral-large-latest` الأفضل من حيث القيمة مقابل السعر بين النماذج الأربعة. تجدر الإشارة إلى أنه لا يوجد أي نموذج Gemini من فئة Pro أحدث من `gemini-3.1-pro-preview` — إذ لم يُطرح Gemini 3.5 Pro، الذي أُعلن عنه في مايو 2026؛ فسلسلة 3.5/3.6/3.7 مخصّصة حصراً لـ Flash.
  - **قياس A/B قبل التحويل إلى Gemini**: ترجم `README.md` إلى اليابانية باستخدام `gemini-3.1-pro-preview` ثم `gemini-3.7-flash`. كانت البنية متطابقة تماماً (21 قائمة، و18 كتلة شفرة، و13 رابط HTML، و13 صورة، مع الحفاظ على جميع عناوين URL)، وذلك في **8 ثوانٍ مقابل 48 ثانية**. وبما أنه لم يكن هناك أي معيار أداء عام يقارن بين هذين النموذجين في الترجمة أو في النصوص البرمجية غير اللاتينية، فقد كان التحويل سيعتمد بخلاف ذلك على مجرد افتراض.
  - **تصفية كتل استجابة Claude**: كان `_call_claude` ينفّذ `block.text for block in response.content` من دون تصفية النوع. وتُدرج نماذج الاستدلال التكيفي (Sonnet 5 وما بعده) كتلة `thinking`، التي تعرض `.thinking` بدلاً من `.text` — وكانت الترجمة ستتعطل عند مواجهة `AttributeError` غير شفاف في المقطع الأول. تُستبعد الآن الكتل `thinking` و`redacted_thinking` و`tool_use` و`tool_result` (قائمة استبعاد، للحفاظ على التسامح مع نوع غير معروف يحمل نصاً)، كما تُطلق الاستجابة التي لا تحتوي على أي كتلة نصية خطأً صريحاً. وقد أُضيف `thinking={"type": "disabled"}` إلى كل استدعاء.
  - **إعادة مزامنة `MODEL_TOKEN_LIMITS`**: أُزيلت النماذج التي تجاوز تاريخ تقاعدها (سلسلة `magistral-*` التي تقاعدت في 2026-07-31، و`gemini-2.0-*` في 2026-06-01، و`gemini-3-pro-preview` في 2026-03-09، و`claude-3-5-sonnet-20240620` و`claude-3-7-sonnet-20250219` و`claude-opus-4-1-20250805` و`claude-sonnet-4-20250514`). تصحيحات الحدود: Mistral 128K → **256K** (إنشاء Large 3 / Small 4)، وGemini 1 000 000 → **1 048 576** (حد الإدخال الفعلي)، و`claude-opus-4-5` من 200K → **1M**، وعائلة `gpt-5.6-*` من 400K → **1,05M**. أُضيف Claude 5 (`claude-sonnet-5` و`claude-opus-5` و`claude-fable-5`)، و`claude-opus-4-8`، وGemini 3.5/3.6/3.7، و`mistral-medium-latest`، وسلسلة `ministral-*`. تجدر الإشارة إلى أن هذه الحدود تظل إرشادية، إذ يحد `translate()` التقسيم إلى `min(16000, limite)`.

  - **المزوّد `--use_codex`**: المزوّد الخامس الذي يشغّل CLI Codex الرسمي (`codex exec`) في الوضع غير التفاعلي، بدلاً من استدعاء API تُحاسب حسب الاستخدام. تُخصم الترجمة من حصة اشتراك ChatGPT المدفوعة مسبقاً. وهذا هو المسار الوحيد الموثّق من OpenAI لهذا الاستخدام: إذ تسرد مصفوفة التوفّر حسب الخطة «Codex SDK, `codex exec`, and scriptable workflows» على أنه متاح في Plus/Pro/Business/Enterprise، في حين أن رموز `~/.codex/auth.json` لا توثّق استدعاءات Platform API (ولا يقرأها هذا السكربت مطلقاً — إذ تظل المصادقة وتجديدها مُدارين بواسطة CLI).
  - **ملف Codex التنفيذي القابل للتثبيت عبر pip، وليس عبر npm فقط**: يبحث `_resolve_codex_binary()` عن الملف التنفيذي في `CODEX_BIN`، ثم في `PATH`، ثم في حزمة Python الرسمية **`openai-codex-cli-bin`** التي تنشرها OpenAI (وهي تبعية SDK `openai-codex`). لذلك لم يعد مشروع Python بحاجة إلى تثبيت npm عاماً لاستخدام `--use_codex`. لا تُضاف الحزمة إلى `requirements.txt`، فالملف التنفيذي حجمه نحو 250 ميغابايت، وإضافته ستُلزم جميع المستخدمين به من أجل مزوّد اختياري. وقد تم التحقق من البداية إلى النهاية: عند غياب `codex` من `PATH`، يعثر الحل على الملف التنفيذي المضمّن وتكتمل ترجمة كاملة خلال 6 ثوانٍ.
  - **ضمان «وضع الاشتراك»**: يُزال `OPENAI_API_KEY` و`CODEX_API_KEY` من بيئة العملية الفرعية. ومن دون هذا الحاجز، قد يتسبب مفتاح موجود في `.env` في تحويل Codex إلى الفوترة حسب الاستخدام من دون أي إشارة ظاهرة — وهذا بالضبط ما وُجد هذا المزوّد لتجنّبه.
  - **إحكام اختبارات مخاطر CLI**:
    - يقرأ `codex exec` الإدخال القياسي **حتى** عندما يُمرَّر الموجّه كوسيط: ومن دون إغلاق الإدخال القياسي، ينتظر الأمر حتى انتهاء المهلة من دون استدعاء النموذج (إعادة إنتاج: exit 124 بعد 180 ثانية، وصفر بايت). لذلك فإن `communicate(input=...)` إلزامي.
    - إن `codex` المثبّت بواسطة npm عبارة عن shim لـ Node يقوم `spawn` بالملف التنفيذي الحقيقي المكتوب بـ Rust: وهذا الأخير **حفيد** عملية Python ويستمر بعد `SIGKILL` الخاص بـ `subprocess.run(timeout=)`، حيث سيواصل استهلاك الحصة. ومن هنا يأتي `Popen(start_new_session=True)` + `os.killpg`.
    - يمكن أن يخرج CLI بالرمز 0 رغم إصداره `turn.failed`: لذلك تُفحَص مخرجات JSONL (`--json`) بالإضافة إلى رمز الإرجاع، كما يؤدي غياب ملف `-o` مع رمز 0 إلى إطلاق خطأ صريح بدلاً من إنتاج مقطع فارغ.
  - **التراجع عند تجاوز حد المعدل**: لا ينفّذ CLI أي إعادة محاولة داخلية (`max_retries = 0`). ويُجرى التصنيف اعتماداً على بنية حمولة JSON (`status: 429` / `error.type`)، لا على سلاسل فرعية — إذ تظهر كلمة «الحصة» في كلٍّ من 429 القابل للاسترداد و`insufficient_quota` النهائي.
  - **حاجز CI**: يُرفض `--use_codex` إذا كان `CI` أو `GITHUB_ACTIONS` معرّفاً. فالمصادقة عبر الاشتراك غير مخصّصة لمشغّل مشترك، كما تحذّر OpenAI صراحةً من هذا المسار في المستودعات العامة.
  - **النماذج**: `gpt-5.6-sol` (الجودة) و`gpt-5.6-luna` (`--eco`). عائلة `gpt-5.6-*` مشتركة بين CLI وPlatform API، لكن حساب ChatGPT لا يملك حق الوصول إلى كل شيء فيها: تُطبَّق قائمة السماح على جانب الخادم، من دون تحقق محلي، ويؤدي النموذج غير المعتاد إلى تحذير. في خطة Plus، يوفّر Luna من 250 إلى 2,000 رسالة لكل نافذة مدتها 5 ساعات، مقابل 10-100 لدى Sol: يُوصى باستخدام `--eco` لكل معالجة دفعية.
  - **إصلاح خطأ — كان `regen_translations.sh` يخرج بخطأ رغم اكتمال النجاح**: كان `trap ... EXIT` يشير إلى `failed_log`، وهو متغير `local` من `main()` لم يعد موجوداً عند تنفيذ trap. وفي ظل `set -u`، كان ذلك يرفع `failed_log: unbound variable` ويُخرج السكربت بالرمز 1 رغم صحة الترجمات الثماني والعشرين — ما كان سيوقف `release.sh --auto` (`set -e`) مباشرة بعد إعادة الإنشاء، في أكثر المراحل تكلفة. أصبح المتغير عاماً، ويتحقق trap من وجوده. ومن الآثار الجانبية المفيدة أن إخفاقات الترجمة الحقيقية، التي كانت مخفية حتى الآن بسبب هذا الخطأ، أصبحت تظهر مجدداً في الملخص الختامي.
  - **`REGEN_MODEL`**: متغير بيئة جديد من `regen_translations.sh` يفرض نموذجاً محدداً فوق الإعداد الافتراضي للمزوّد، مثل `REGEN_PROVIDER=codex REGEN_MODEL=gpt-5.6-sol` لإعادة الإنشاء باستخدام النموذج الأعلى ضمن حصة الاشتراك بدلاً من نموذج `--eco` الموجّه نحو الكميات.
  - **`regen_translations.sh`**: `REGEN_PROVIDER=codex` متاح عند التفعيل الصريح فقط (ولا يُكتشف تلقائياً أبداً، لتجنب استهلاك حصة الاشتراك من دون علم المستخدم). يُجدَّد الرمز مرة واحدة بالتتابع قبل فتح التوازي — إذ إن تجديد Codex دوّار ولمرة واحدة، وستؤدي المهام المتزامنة إلى إبطال الجلسة `codex login` — كما خُفّض التزامن إلى 4.
  - **إعادة الهيكلة المرتبطة**: انتقل `_dispatch_provider_call` من 8 إلى 6 معاملات عبر `_resolve_provider()` يعيد اسم المزوّد، بدلاً من تمرير قيمة منطقية رابعة عبر السلسلة بأكملها. وتظل القيم المنطقية الصريحة ذات أولوية على `args` للحفاظ على الاختبارات التي تستدعي `translate(..., use_mistral=True)` باستخدام `Namespace` أدنى.
  - **الاختبارات**: ملف جديد `tests/test_codex_provider.py` (48 اختباراً) يغطي argv، والبيئة المنقّاة، وعقد منع المقدمات، والإخفاقات الصامتة، وانتهاء المهلة/killpg، والتراجع، والفحص المسبق، وحل المزوّد، وتسلسل الاستدلال في Gemini، وتصفية كتل Claude، والاقتباسات الإخبارية متعددة الفقرات. أصبحت المجموعة الكاملة تضم 290 اختباراً.
  - **التحقق الفعلي**: ترجمة `README.md` الخاص بالمشروع إلى **14 لغة** عبر Codex تعطي بنية متطابقة تماماً مع الترجمات المرجعية (14 كتلة شفرة، و24 عنواناً، و25 صفاً في الجدول، و13 رابط HTML، و13 صورة، و19 عنوان URL، وكتل شفرة متطابقة حرفياً، ومن دون أي بقايا للعناصر النائبة). وفي مقال إخباري حجمه 69 كيلوبايت بوضع `--news`، تجتاز المخرجات `gpt-5.6-luna` و`gpt-5.6-sol` كلتاهما أداة التحقق التطبيقية اللاحقة للمعالجة على en/ja/ar. أما الاستهلاك المقاس عبر `account/rateLimits/read` فبقي دون عتبة تقريب العداد (0% من نافذة 5 ساعات) في وضع `--eco`.

- **1.9.2** إصلاح استخراج عنوان URL للإسناد الإخباري مع الأقواس المتداخلة أو البادئة الفرنسية (2026-05-11):

  - **الخطأ المُصلح**: كان استخراج عناوين URL للإسناد في `_protect_news_quotes` يستخدم التعبير النمطي `re.search(r"\((.+?)\)", attribution)` (التقاطاً كسولاً بين قوسين). وفي إسناد من نوع `(relayé par [@user sur X](https://x.com/.../123))` (أقواس متداخلة: `(` المحيطة + `]()` لرابط Markdown)، كان الالتقاط يتوقف عند أول `)` يصادفه ← سلسلة مبتورة مع تضمين البادئة الفرنسية: `relayé par [@user sur X](https://x.com/.../123` (من دون `)` النهائي). ونتيجة لذلك، كان `_validate_news_post` يبحث عن هذه السلسلة في المخرج المترجم ويفشل دائماً (لسببين: `)` مبتور، و"relayé par" مترجمة إلى `relayed by`/`weitergeleitet von`/...). ولم يكن من الممكن أن ينجح التسلسل الكامل low → medium → high → gpt-5.5.
  - **الإصلاح**: أصبح التعبير النمطي `re.search(r"\]\(([^)]+)\)", attribution)` — إذ يستهدف تحديداً `](url)` لرابط Markdown، ويلتقط **عنوان URL النقي فقط** (من دون البادئة الفرنسية أو الاقتطاع)، مع الحفاظ على الثبات بواسطة العناصر النائبة `#URL{N}#` أثناء الترجمة. وهو متين أمام النمطين الإشكاليين:
    - `(relayé par [@account sur X](url))` — أقواس متداخلة
    - `via [@source](url)` أو `selon [@author](url)` — بادئة فرنسية من دون أقواس محيطة
  - **الاختبارات**: اختباران جديدان في `test_silent_failure.py` ضمن الفئة `TestNewsCitationExtraction`:
    - `test_extract_attribution_url_with_nested_parens` (الحالة المطابقة تماماً لإعادة إنتاج خطأ Genspark CEO E2B)
    - `test_extract_attribution_url_with_french_prefix` (صيغة بديلة مع `via`)
  - **فجوة التغطية**: يتحقق `check-editorial-coverage.py` من الصياغة التحريرية، لكنه لا يتحقق من قابلية الترجمة بواسطة المترجم. وقد يكون من التحسينات الممكنة (خارج نطاق v1.9.2) إضافة فحص يحاكي استخراج الإسناد في تشغيل تجريبي لاكتشاف الأنماط المعرضة للخطر **قبل** النشر.

- **1.9.1** إصلاح i18n لوسم CTA في ملاحظة علامة الترجمة (2026-05-10):

  - **الخطأ المُصلح**: ظل وسم `[Voir le projet sur GitHub ↗]` لرابط CTA في شريط العلامة أعلى الملفات المترجمة **بالفرنسية** لجميع اللغات المستهدفة، بدلاً من اتباع `target_lang`. ولا يراه LLM مطلقاً (إذ يُجمَّع من جانب Python للحفاظ على عنوان URL وslug الخاص بالمستودع)، لذلك لم تتمكن مرحلة الترجمة من تصحيحه. وكان هذا تراجعاً صامتاً منذ إضافة تنسيق `marker` في v1.9.
  - **الإصلاح**: ثابت جديد `_VIEW_PROJECT_LABELS` يربط اللغات الخمس عشرة بوسمها المحلي. ويمرّر `_translation_note_invariants(target_lang)` و`_assemble_translation_note_paragraphs(phrase, target_lang)` اللغة المستهدفة الآن. أما القيمة الاحتياطية فهي `fr` إذا كانت اللغة غير معروفة (للأمان، وليس لتجنب KeyError).
  - **الاختبارات**: عُدّل `test_source_emits_three_paragraphs_repo_title_description_link` (target_lang `ja` → وسم ياباني متوقع). واختباران جديدان: `test_source_link_label_localized_per_target_lang` (مُعلَّم على 7 لغات تغطي الأبجديات اللاتينية، والتصويرية، والأبجدية الصامتة) و`test_source_link_label_falls_back_to_french_for_unknown_target`. المجموع: 40 اختباراً في `test_translation_note_position.py` (بدلاً من 38).
  - **التوافق مع الإصدارات السابقة**: توقيع ذو قيمة افتراضية `target_lang="fr"` — ويستمر المتصلون البرمجيون الخارجيون من دون `args.target_lang` في العمل دون تعديل.
- **1.9** إصلاح الفشل الصامت + مجموعة أدوات جودة كاملة + ملاحظة ترجمة متعددة المواضع (2026-05-07):
  - **ملاحظة ترجمة متعددة المواضع + format marker "embed card"**:
    - خيارات CLI جديدة (إضافية، مع بقاء القيم الافتراضية دون تغيير → **غير كاسرة للتوافق**):
      - `--note_position {top,bottom,both}` (الافتراضي: `bottom`): تضع الملاحظة في أعلى الملف المترجم أو أسفله أو في الموضعين.
      - `--note_format {legacy,marker}` (الافتراضي: `legacy`):
        - `legacy` يعيد بدقة سلوك v1.8 (فقرة عريضة `**…**`) **byte-for-byte**.
        - `marker` يُصدر تعريف link reference في Markdown غير مرئي (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`)، يتبعه **blockquote من 3 فقرات** منظم لعرض يشبه "GitHub repo embed card": عنوان المشروع داخل inline code (`**\`ai-powered-markdown-translator\`\*\*`)، والوصف المترجم بواسطة LLM، ورابط CTA (`[Voir le projet sur GitHub ↗](URL)`) مع سهم ظاهر. ويمكن استغلاله أثناء البناء بواسطة إضافة remark (راجع blog jls42.org → الإضافة `remark-translation-banner`).
    - **الثوابت التي لا تُرسل مطلقًا إلى LLM**: يُجمّع اسم المستودع وعنوان URL الخاص بـ GitHub من جهة Python بعد ترجمة الجملة الوصفية. لا يرى LLM مطلقًا slug `ai-powered-markdown-translator` ولا `https://github.com/jls42/...`، ما يضمن عدم تعديل أي renderer أو كسر أو scheme.
    - **إدراج مدرك لـ Frontmatter**: في الوضع `top` أو `both`، تُدرج الملاحظة **بعد كتلة `---` الختامية** لـ frontmatter بصيغة YAML (لضمان أمان Astro Content Collections / gray-matter). يكتشف helper `_split_frontmatter` وجود `---\n…\n---\n` في بداية الملف ويحافظ على سلامته؛ و**يرفع `RuntimeError`** عند وجود frontmatter مفتوح بلا fence إغلاق (ينتقل الملف إلى `failed_files` بدل كتابته مع ملاحظة في موضع خاطئ).
    - **Sanitizer للنموذج مع whitelist**: يستبدل `_sanitize_model` كل محرف خارج `[A-Za-z0-9._:/-]` بـ `_`، مع استخدام `unknown` كقيمة احتياطية إذا كان الناتج فارغًا. ويتوافق ذلك مع المدقق الموجود في إضافة remark لـ Astro ويعطّل المحارف التي قد تكسر تنسيق marker (المسافة، علامة الاقتباس، القوس، الفاصلة، وغيرها).
    - **إعادة هيكلة داخلية**: `_append_translation_note` (دالة ضخمة واحدة) → 7 مساعدين نقيين (`_translation_note_invariants`، `_build_translation_note_phrase`، `_assemble_translation_note_paragraphs`، `_build_translation_note_source`، `_sanitize_model`، `_quote_lines`، `_split_frontmatter`، `_build_translation_note_block`، `_compose_with_notes`). فُصل الـ builder عن الـ composer (يعيد الـ builder كتلة نقية بلا فاصل، بينما يطبّق الـ composer الـ `\n\n` وفقًا للموضع)؛ وتشترك عملية الإنتاج والـ helper المصدر في نفس مُجمِّع الفقرات الثلاث.
    - **`_quote_lines` مع الحفاظ على الأسطر الفارغة**: يسبق كل سطر بـ `> `، ويحوّل الأسطر الفارغة إلى `>` فقط. وهذا يتيح لـ mdast رؤية 3 فقرات منفصلة داخل blockquote (العنوان / الوصف / الرابط) بدل فقرة واحدة تحتوي على فواصل أسطر.
    - **`_build_translation_note_block` التكيفي**: وفقًا لعدد الفقرات التي حافظ عليها LLM (3 = تنسيق البطاقة الكامل، 2 = جملة + رابط، 1 = قيمة احتياطية). لم تعد القيمة الاحتياطية ذات الفقرة الواحدة **تُغلّف بـ `**...**`** عند اكتشاف رابط Markdown `](` (فعرض `<strong>` حول رابط يكون هشًا).
    - **التوافق مع الإصدارات السابقة**: `getattr(args, "note_position", "bottom")` و`getattr(args, "note_format", "legacy")` في جهة `_compose_with_notes` — تواصل Namespace التي لا تحتوي على هذه السمات (الاختبارات الحالية والاستدعاءات البرمجية الخارجية) العمل دون تعديل.
  - **إصلاح الفشل الصامت في الترجمات الطويلة**:
    - التحقق من اللغة بعد الترجمة لدى جميع المزوّدين (OpenAI وMistral وClaude وGemini): طبقة حتمية (العثور على مقتطف المصدر حرفيًا) + طبقة احتمالية (`langdetect`).
    - whitelist `finish_reason` / `stop_reason`: رفع `RuntimeError` عند أي حالة خارج القائمة المسموح بها (الاقتطاع، content_filter، وغيرها).
    - `max_tokens` Claude: `4096` → `32768` (لتجنب الاقتطاع الكامن في مقاطع 16k، مع هامش للانتقال بين النصوص FR→JA/ZH/KO/AR/HI).
    - تقسيم مدرك للعناوين: إعطاء الأولوية للعناوين H2/H3 في النصف الثاني من المقطع (يبدأ كل مقطع بقسم دلالي مكتمل).
    - تمرير الأخطاء حتى رمز خروج غير صفري: يعيد `translate_markdown_file` حالة مكتوبة النوع `success` / `failure` / `skipped`، ويعيد `main()` `sys.exit(1)` إذا فشل ملف واحد على الأقل (في الملف الواحد والدفعة).
    - حارس المحتوى الفارغ لدى جميع المزوّدين، ونسبة سلامة المصدر/الناتج (≥ 500 محرف، ورفض إذا كانت < 5%)، والتحقق من placeholders البرمجية (`#CODEBLOCK`/`#INLINECODE`)، والتطبيع بعد LLM (الفواصل/الروابط الملتصقة بعنوان)، و`BadRequestError` إعادة المحاولة دون `reasoning_effort`.
    - إضافة الاعتمادية `langdetect==1.0.9`.
  - **أدوات جودة قبل الإيداع** ("نوع EurekAI الكامل"، 14 hook):
    - Pre-commit: ‏ruff (الفحص والتنسيق)، ‏shellcheck، ‏prettier (md/yaml/json)، ‏detect-secrets (4 مفاتيح API محمية)، ‏Lizard (CCN ≤ 12)، وpre-commit-hooks v5 (المسافات البيضاء، EOF، الملفات الكبيرة، shebangs، وغيرها).
    - Pre-push: ‏mypy (وضع lax تدريجي)، ‏Opengrep SAST (translate.py + scripts/)، ‏pip-audit (وضع إعداد التقارير الأولي)، وunittest discover (tests/ + scripts/tests/).
    - أغلفة محلية داخل `scripts/` تستخدم `./venv/bin/python`.
    - `scripts/audit_verdict.py`: محلّل JSON لـ pip-audit مع 11 اختبار unittest، وهو منفذ Python لمحلّل jls42-astro.
    - إصلاح 7 مخالفات أولية لـ ruff: ‏B904 (raise from) ×2، ‏B007 (dirs غير مستخدمة)، ‏C408 (dict literal)، ‏C419 (list-comp)، ‏SIM105 (contextlib.suppress)، ‏SIM110 (any()).
    - يستثني Lizard مؤقتًا `translate.py` (4 دوال بدرجات CCN بين 21 و47، وإعادة الهيكلة مخططة) — بوابة صارمة على scripts/.
  - **SonarCloud وتغطية شاملة**:
    - سير عمل GitHub Actions `SonarCloud` (sonarcloud.yml + sonar-project.properties): تحليل عند كل push وpull-request، مع التغطية عبر `coverage.xml`.
    - 11 شارة SonarCloud أعلى README (Quality Gate، وتقييمات Security/Reliability/Maintainability، وCoverage، وVulnerabilities، وBugs، وCode Smells، وDuplicated Lines، وTechnical Debt، وLines of Code).
    - `tests/test_silent_failure.py` (`unittest` من stdlib): يغطي الحلقات الست لسلسلة أخطاء الفشل الصامت.
    - `tests/test_orchestration.py` (+79 اختبارًا): يغطي طبقة تنسيق `translate.py` (`_resolve_*_filename`، `_existing_translation_exists`، `_record_translation_status`، `_write_output_file`، `translate_directory`، `_validate_input_paths`، `_init_*_client`، `_select_provider_client`، `_normalize_collapsed_markdown`، `_cleanup_source_flag`، `_validate_news_flags_*`، `_openai_create_with_fallback` TypeError + بدائل BadRequestError، وتنسيق prompt لسلسلة o1، وفروع الإرجاع المبكر في `_validate_translation_output`).
    - `scripts/tests/test_audit_verdict.py`: تغطية `main()` (stdin/stdout) وكتلة `if __name__ == "__main__"` عبر subprocess.
    - **التغطية على الشيفرة الجديدة**: من 75.5% إلى نحو 98% (translate.py بنسبة 98%، وscripts/audit_verdict.py بنسبة 97%).
  - **الاختبارات**: يغطي `tests/test_translation_note_position.py` مصفوفة الموضع × التنسيق (بما في ذلك E2E `marker+top|bottom|both` و`legacy+top|bottom|both`)، والبادئة متعددة الأسطر، والتوافق byte-for-byte (golden literal)، وsanitizer، وتقسيم frontmatter (بما في ذلك الرفع عند غياب fence الإغلاق)، وتنسيق الفقرات الثلاث، والقيمة الاحتياطية لفقرتين، وحارس الفقرة الواحدة + رابط Markdown، وحاجزًا حرجًا `TestLLMPayloadExcludesInvariants` يؤكد أن العنوان وعنوان URL لا يُرسلان مطلقًا إلى LLM. **نجح 190 اختبارًا**، دون أي تراجع.
  - التوثيق: `README.md` (بالفرنسية + 14 ترجمة) مع الشارات، و`CLAUDE.md` (سير عمل pre-commit وشرح مفصل لمراقبة CI)، وإعادة توليد 28 ترجمة.
- **1.8** وضع `--news` + تحديث النماذج لعام 2026 (2026-03-17، الوسم `v1.8`):
  - تحديث النماذج الافتراضية (مارس 2026):
    - OpenAI للجودة: `gpt-5` → `gpt-5.4`
    - OpenAI الاقتصادي: `gpt-5-mini` → `gpt-5.4-mini`
    - Gemini للجودة: `gemini-3-pro-preview` → `gemini-3.1-pro-preview`
  - إضافة حدود الرموز لـ `gpt-5.4` و`gpt-5.4-mini` و`gpt-5.4-nano` (400k) و`gemini-3.1-pro-preview` (1M).
  - الوضع الأولي `--news`: حماية الاقتباسات EN باستخدام placeholders `#NEWSQUOTE\d+#`، وmapping `LANG_FLAGS` (15 لغة)، وإدارة الأعلام وفقًا للغة الهدف.
  - التحقق من placeholders الخاصة بالأخبار قبل الاستعادة (تراجع: كان LLM الذي يحذف placeholder ينتج بصمت مخرجًا بلا اقتباس).
  - جعل البرنامج النصي `regen_translations.sh` محمولًا (مسارات مطلقة، بلا اعتماد على pwd).
  - إضافة رابط الفرنسية إلى أشرطة اللغات في README/CHANGELOG، وإعادة توليد 28 ترجمة.
- **1.7** المستجدات:
  - خيار `--keep_filename` للاحتفاظ باسم الملف الأصلي أثناء الترجمة.
  - دعم الملف `.env` لتحميل مفاتيح API تلقائيًا.
  - **الحفاظ على inline code**: أصبحت backticks (`` `...` ``) محمية أثناء الترجمة.
  - تحسين system prompt:
    - معالجة أفضل لعلامات الاقتباس في YAML frontmatter.
    - حماية متغيرات القالب `{variable}`.
    - منع ملاحظات المترجم غير المطلوبة.
  - اختُبر بنجاح على 364 ملفًا (ترحيل مدونة jls42.org).
- **1.6** المستجدات:
  - دعم Google Gemini API للترجمة (`--use_gemini`).
  - تحديث النماذج الافتراضية لعام 2026:
    - OpenAI: `gpt-5` (للجودة)، `gpt-5-mini` (الاقتصادي).
    - Claude: `claude-sonnet-4-5` (للجودة)، `claude-haiku-4-5` (الاقتصادي).
    - Gemini: `gemini-3-pro-preview` (للجودة)، `gemini-3-flash-preview` (الاقتصادي).
  - الوضع الاقتصادي (`--eco`) لاستخدام نماذج أسرع وأقل تكلفة.
  - ترجمة ملف واحد (`--file`) دون استعراض دليل.
  - نمط تسمية جديد مبسّط: `{base}-{lang}.md`.
  - خيار `--include_model` للاحتفاظ بالتنسيق القديم الذي يتضمن اسم النموذج.
  - دعم النماذج غير المدرجة مع حد رموز افتراضي (128k).
  - ترجمة README إلى 14 لغة.
- **1.5** التحسينات:
  - **تحديث مفاتيح API والنماذج الافتراضية:**
    - **OpenAI:** تحديث `DEFAULT_MODEL_OPENAI` إلى `"gpt-4o"`.
    - **Mistral AI:** تحديث `DEFAULT_MODEL_MISTRAL` إلى `"mistral-large-latest"`.
    - **Claude من Anthropic:** إضافة `DEFAULT_ANTHROPIC_API_KEY` وتحديث `DEFAULT_MODEL_CLAUDE` إلى `"claude-3-5-sonnet-20240620"`.
  - **تحسين prompts الترجمة:**
    - أُثريت prompts للترجمات المباشرة ولملاحظات الترجمة من أجل وضوح وكفاءة أفضل، بما في ذلك تعليمات مفصلة للحفاظ على البيانات الوصفية وعناصر التنسيق الخاصة.
  - **إعادة هيكلة الشيفرة:**
    - استبدال `MistralClient` بالصنف `Mistral` لتهيئة عميل Mistral AI.
    - إعادة تنظيم imports لتحسين سهولة القراءة والصيانة.
    - تحسين تقسيم النصوص وإدارة كتل الشيفرة للحفاظ على التنسيق الأصلي أثناء الترجمة.
  - **إدارة ملفات الإخراج:**
    - عكس ترتيب النموذج واللغة في أسماء ملفات الإخراج (مثلًا `f"{base}-{args.target_lang}-{args.model}.md"`)، مما يسهّل تنظيم الترجمات والبحث عنها.
  - **تحسينات متنوعة:**
    - تنظيف الشيفرة بإزالة الأسطر الفارغة غير الضرورية.
    - تعديلات طفيفة لتحسين بنية البرنامج النصي وسهولة قراءته.
- **1.4** المستجدات:
  - دعم Anthropic Claude API للترجمة.
  - تحسين prompts لزيادة الوضوح والكفاءة.
  - تعديلات طفيفة لتحسين صيانة الشيفرة.
- **1.3** التحسينات والميزات الجديدة:
  - تحسين إدارة كتل الشيفرة.
  - تحسين إدارة ملفات الإخراج.
  - تحسين اكتشاف الملفات الموجودة.
  - خيار `--force` لفرض الترجمة.
  - عكس ترتيب النموذج واللغة في اسم ملف الإخراج.
- **1.2** إصلاح changelog.
- **1.1** إضافة دعم Mistral AI API.
- **1.0** الإصدار الأولي - دعم OpenAI API.

**مقالة مترجمة من الفرنسية إلى العربية باستخدام gpt-5.6-luna.**
