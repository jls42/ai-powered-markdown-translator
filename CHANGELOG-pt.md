### Registro de alterações

🌍 [Français](CHANGELOG.md) | [English](CHANGELOG-en.md) | [Español](CHANGELOG-es.md) | [中文](CHANGELOG-zh.md) | [Deutsch](CHANGELOG-de.md) | [日本語](CHANGELOG-ja.md) | [한국어](CHANGELOG-ko.md) | [العربية](CHANGELOG-ar.md) | [हिन्दी](CHANGELOG-hi.md) | [Italiano](CHANGELOG-it.md) | [Nederlands](CHANGELOG-nl.md) | [Polski](CHANGELOG-pl.md) | [Português](CHANGELOG-pt.md) | [Română](CHANGELOG-ro.md) | [Svenska](CHANGELOG-sv.md)

- **1.9** Correção de falha silenciosa + kit completo de qualidade + nota de tradução em múltiplas posições (2026-05-07) :
  - **Nota de tradução em múltiplas posições + formato marker "embed card"** :
    - Novas opções de CLI (aditivas, padrões inalterados → **non breaking**) :
      - `--note_position {top,bottom,both}` (padrão : `bottom`) : coloca a nota no topo, no final, ou em ambos os locais do arquivo traduzido.
      - `--note_format {legacy,marker}` (padrão : `legacy`) :
        - `legacy` reproduz estritamente o comportamento v1.8 (parágrafo em negrito `**…**`) **byte-for-byte**.
        - `marker` emite uma link reference definition Markdown invisível (`[ai-translation-note-<placement>]: <> "v=1 source=… target=… model=… date=…"`) seguida de um **blockquote de 3 parágrafos** estruturado para uma renderização tipo "GitHub repo embed card" : título do projeto em código inline (`**\`ai-powered-markdown-translator\`\*\*`), descrição traduzida pelo LLM, e link CTA (`[Voir le projet sur GitHub ↗](URL)`) com seta visível. Utilizável no build por um plugin remark (cf. blog jls42.org → plugin `remark-translation-banner`).
    - **Invariantes nunca enviados ao LLM** : título do repo e URL do GitHub são montados no lado Python após a tradução da frase descritiva. O LLM nunca vê o slug `ai-powered-markdown-translator` nem `https://github.com/jls42/...`, garantindo que nenhum renderer/case/scheme seja alterado.
    - **Inserção aware do frontmatter** : no modo `top` ou `both`, a nota é inserida **após o bloco `---` de fechamento** do frontmatter YAML (segurança Astro Content Collections / gray-matter). O helper `_split_frontmatter` detecta `---\n…\n---\n` no início do arquivo e preserva sua integridade ; **lança `RuntimeError`** em caso de frontmatter aberto sem fence de fechamento (o arquivo sobe em `failed_files` em vez de ser escrito com uma nota mal posicionada).
    - **Sanitizer modelo whitelist** : `_sanitize_model` substitui qualquer caractere fora de `[A-Za-z0-9._:/-]` por `_`, fallback `unknown` se vazio. Alinha com o validador no lado do plugin remark Astro e neutraliza os caracteres que quebrariam o formato do marker (espaço, aspas, parêntese, vírgula, etc.).
    - **Refatoração interna** : `_append_translation_note` (1 função monolítica) → 7 helpers puros (`_translation_note_invariants`, `_build_translation_note_phrase`, `_assemble_translation_note_paragraphs`, `_build_translation_note_source`, `_sanitize_model`, `_quote_lines`, `_split_frontmatter`, `_build_translation_note_block`, `_compose_with_notes`). Builder/composer separados (o builder retorna um bloco puro sem separador, o composer aplica os `\n\n` conforme a posição) ; produção e helper source compartilham o mesmo assembler de 3 parágrafos.
    - **`_quote_lines` preserving blank** : prefixa cada linha com `> `, transformando as linhas vazias em apenas `>`. Permite ao mdast ver 3 parágrafos distintos no blockquote (título / descrição / link) em vez de um único parágrafo com quebras de linha.
    - **`_build_translation_note_block` adaptativo** : de acordo com o número de parágrafos que o LLM preservou (3 = formato card completo, 2 = frase + link, 1 = fallback). O fallback de 1 parágrafo **não envolve mais em `**...**`** quando um link Markdown `](` é detectado (renderização frágil de `<strong>` ao redor de um link).
    - **Compatibilidade ascendente** : `getattr(args, "note_position", "bottom")` e `getattr(args, "note_format", "legacy")` no lado `_compose_with_notes` — os Namespace sem esses atributos (testes existentes, chamadas programáticas externas) continuam funcionando sem modificação.
  - **Correção de falha silenciosa em traduções longas** :
    - Validação de idioma pós-tradução em todos os provedores (OpenAI, Mistral, Claude, Gemini) : camada determinística (trecho de origem recuperado verbatim) + camada probabilística (`langdetect`)
    - Whitelist `finish_reason` / `stop_reason` : lançar `RuntimeError` em qualquer estado fora da whitelist (truncation, content_filter, etc.)
    - `max_tokens` Claude : `4096` → `32768` (evita truncation latente em segmentos 16k, margem cross-script FR→JA/ZH/KO/AR/HI)
    - Segmentação heading-aware : prioridade H2/H3 na segunda metade do segmento (cada segmento começa por uma seção semântica completa)
    - Propagação dos erros até um exit code não zero : `translate_markdown_file` retorna um status tipado `success` / `failure` / `skipped`, `main()` `sys.exit(1)` se ao menos um arquivo falhar (single-file e batch)
    - Empty-content guard em todos os provedores, sanity ratio source/output (≥ 500 chars, < 5% = recusa), validação de placeholders code (`#CODEBLOCK`/`#INLINECODE`), normalização pós-LLM (separadores/links colados a um heading), `BadRequestError` retry sem `reasoning_effort`
    - Adição da dependência `langdetect==1.0.9`
  - **Kit de qualidade pre-commit** ("type EurekAI completo", 14 hooks) :
    - Pre-commit : ruff (lint+format), shellcheck, prettier (md/yaml/json), detect-secrets (4 chaves API protegidas), Lizard (CCN ≤ 12), pre-commit-hooks v5 (whitespace, EOF, large-files, shebangs, etc.)
    - Pre-push : mypy (modo relaxado progressivo), Opengrep SAST (translate.py + scripts/), pip-audit (modo de relatório inicial), unittest discover (tests/ + scripts/tests/)
    - Wrappers locais em `scripts/` que usam `./venv/bin/python`
    - `scripts/audit_verdict.py` : parser JSON pip-audit com 11 testes unittest, port Python adaptado do parser jls42-astro
    - 7 violações iniciais do ruff corrigidas : B904 (raise from) ×2, B007 (unused dirs), C408 (dict literal), C419 (list-comp), SIM105 (contextlib.suppress), SIM110 (any())
    - Lizard exclui temporariamente `translate.py` (4 funções com CCN 21-47, refatoração planejada) — gate estrito em scripts/
  - **SonarCloud + cobertura exaustiva** :
    - Workflow GitHub Actions `SonarCloud` (sonarcloud.yml + sonar-project.properties) : análise a cada push e pull-request, coverage via `coverage.xml`
    - 11 badges SonarCloud no topo do README (Quality Gate, Security/Reliability/Maintainability ratings, Coverage, Vulnerabilities, Bugs, Code Smells, Duplicated Lines, Technical Debt, Lines of Code)
    - `tests/test_silent_failure.py` (`unittest` stdlib) : cobre os seis elos da cadeia de erro silent-failure
    - `tests/test_orchestration.py` (+79 tests) : cobre a camada de orquestração de `translate.py` (`_resolve_*_filename`, `_existing_translation_exists`, `_record_translation_status`, `_write_output_file`, `translate_directory`, `_validate_input_paths`, `_init_*_client`, `_select_provider_client`, `_normalize_collapsed_markdown`, `_cleanup_source_flag`, `_validate_news_flags_*`, `_openai_create_with_fallback` TypeError + BadRequestError fallbacks, o1-series prompt format, branches early-return de `_validate_translation_output`)
    - `scripts/tests/test_audit_verdict.py` : cobertura de `main()` (stdin/stdout) e do bloco `if __name__ == "__main__"` via subprocess
    - **Coverage on new code** : 75.5% → ~98% (translate.py 98%, scripts/audit_verdict.py 97%)
  - **Testes** : `tests/test_translation_note_position.py` cobre a matriz posição × formato (incl. E2E `marker+top|bottom|both` e `legacy+top|bottom|both`), o prefixamento multilinha, a retrocompat byte-for-byte (golden literal), o sanitizer, o split frontmatter (incl. raise sobre fence não fechado), o formato de 3 parágrafos, o fallback de 2 parágrafos, o guard de 1 parágrafo + link Markdown, e uma proteção crítica `TestLLMPayloadExcludesInvariants` que afirma que título+URL nunca são enviados ao LLM. **190 testes passam**, 0 regressão.
  - Documentação : `README.md` (FR + 14 traduções) com badges, `CLAUDE.md` (workflow pre-commit + watch CI detalhado), 28 traduções regeneradas
- **1.8** Modo `--news` + bump de modelos 2026 (2026-03-17, tag `v1.8`) :
  - Modelos padrão atualizados (março de 2026) :
    - OpenAI qualidade : `gpt-5` → `gpt-5.4`
    - OpenAI econômico : `gpt-5-mini` → `gpt-5.4-mini`
    - Gemini qualidade : `gemini-3-pro-preview` → `gemini-3.1-pro-preview`
  - Adição dos limites de tokens para `gpt-5.4`, `gpt-5.4-mini`, `gpt-5.4-nano` (400k) e `gemini-3.1-pro-preview` (1M)
  - Modo `--news` inicial : proteção das citações EN com placeholders `#NEWSQUOTE\d+#`, mapeamento `LANG_FLAGS` (15 idiomas), gestão das bandeiras por idioma alvo
  - Validação dos placeholders news antes da restauração (regressão : um LLM que removia o placeholder produzia silenciosamente uma saída sem citação)
  - Script `regen_translations.sh` tornado portátil (caminhos absolutos, sem dependência do pwd)
  - Link em francês adicionado nas language bars do README/CHANGELOG, 28 traduções regeneradas
- **1.7** Novidades :
  - Opção `--keep_filename` para manter o nome original do arquivo durante a tradução
  - Suporte ao arquivo `.env` para carregar automaticamente as chaves API
  - **Preservação do código inline** : os backticks (`` `...` ``) agora são protegidos durante a tradução
  - Melhoria do prompt do sistema :
    - Melhor gestão das aspas no YAML frontmatter
    - Proteção das variáveis de template `{variable}`
    - Proibição de notas de tradutor não solicitadas
  - Testado com sucesso em 364 arquivos (migração do blog jls42.org)
- **1.6** Novidades :
  - Suporte à API Google Gemini para tradução (`--use_gemini`)
  - Atualização dos modelos padrão 2026 :
    - OpenAI : `gpt-5` (qualidade), `gpt-5-mini` (eco)
    - Claude : `claude-sonnet-4-5` (qualidade), `claude-haiku-4-5` (eco)
    - Gemini : `gemini-3-pro-preview` (qualidade), `gemini-3-flash-preview` (eco)
  - Modo econômico (`--eco`) para usar modelos mais rápidos e menos caros
  - Tradução de arquivo único (`--file`) sem percorrer um diretório
  - Novo padrão de nomeação simplificado : `{base}-{lang}.md`
  - Opção `--include_model` para manter o antigo formato com o nome do modelo
  - Suporte a modelos não listados com limite de tokens padrão (128k)
  - README traduzido em 14 idiomas
- **1.5** Melhorias :
  - **Atualização das chaves API e dos modelos padrão :**
    - **OpenAI :** Atualização de `DEFAULT_MODEL_OPENAI` para `"gpt-4o"`.
    - **Mistral AI :** Atualização de `DEFAULT_MODEL_MISTRAL` para `"mistral-large-latest"`.
    - **Claude da Anthropic :** Adição de `DEFAULT_ANTHROPIC_API_KEY` e atualização de `DEFAULT_MODEL_CLAUDE` para `"claude-3-5-sonnet-20240620"`.
  - **Otimização dos prompts de tradução :**
    - Os prompts para traduções diretas e notas de tradução foram enriquecidos para maior clareza e eficiência, incluindo instruções detalhadas sobre a preservação dos metadados e dos elementos específicos de formatação.
  - **Refatoração do código :**
    - Substituição de `MistralClient` pela classe `Mistral` para a inicialização do cliente Mistral AI.
    - Reorganização dos imports para melhor legibilidade e manutenção.
    - Melhoria da segmentação dos textos e gestão dos blocos de código para preservar a formatação original durante a tradução.
  - **Gestão dos arquivos de saída :**
    - Inversão do modelo e do idioma no nome dos arquivos de saída (por exemplo, `f"{base}-{args.target_lang}-{args.model}.md"`), facilitando assim a organização e a busca das traduções.
  - **Melhorias diversas :**
    - Limpeza do código removendo linhas vazias desnecessárias.
    - Ajustes menores para melhorar a estrutura e a legibilidade do script.
- **1.4** Novidades :
  - Suporte à API Claude da Anthropic para tradução
  - Otimização dos prompts para maior clareza e eficiência
  - Ajustes menores para melhorar a manutenção do código
- **1.3** Melhorias e novas funcionalidades :
  - Gestão aprimorada dos blocos de código
  - Gestão aprimorada dos arquivos de saída
  - Detecção aprimorada de arquivos existentes
  - Opção `--force` para forçar a tradução
  - Inversão do modelo e do idioma no nome do arquivo de saída
- **1.2** Correção do changelog
- **1.1** Adição do suporte à API Mistral IA
- **1.0** Versão inicial - Suporte à API OpenAI

**Artigo traduzido do fr para o pt com gpt-5.4-mini.**
